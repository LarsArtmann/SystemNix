# TODO — Services

Per-service debt + go-lives: paperless, inboxclean, miniflux, mail-relay, immich, forgejo, hermes, bank-sync, browser-history, dnsblockd, discordsync, searxng, CV, twenty/manifest (docker), attic, signal, monitor365, google-sync, tq pool units, PMA units.

Domain LIBRARY of the [TODO system](../../TODO_LIST.md) — every open item for this domain, any lifecycle. The dispatch QUEUE of agent-actionable (`[ready]`) items is `TODO_LIST.md`; the tq pool harvests only the queue.

House rules (enforced by convention, see AGENTS.md → "TODO System"): file an item under the domain that owns the FIX, not the symptom it mentions; one item = one ask + `**Source:**` pointer — verification narratives go to the status report, never the item; `[x]` rows are pruned to `CHANGELOG.md` at every pass; NO system-state narratives here (AGENTS.md and `docs/services/*` runbooks own state).

Tag legend: `[ready]` agent-actionable · `[blocked:user]` needs sudo/browser/external console/owner hands · `[blocked:push]` needs an upstream push/tag (agents implement, never push) · `[blocked:deploy]` waits on a deploy · `[watch]` time-gated verification · `[decision]` owner question.

## Prioritized

- [ ] [ready] **Paperless scheduled-task failure monitoring + encrypted-tag consistency alert** — the 22h "Train Classifier" red was UI-ONLY (nothing watched the tasks API); a "paperless failed tasks > N" textfile collector + Gatus check (forgejo-mirror pattern) closes the class. Companion: alert when docs carry the `encrypted` tag while a decrypt password IS configured (wrong-password detection). **Source:** `docs/status/archived/2026-09-06_02-33_*` §4/§f.11-12
- [ ] [blocked:user] **InboxClean `main` OAuth re-consent — Gmail sync DEAD since 2026-09-04, cursor frozen at 5152620 (2026-09-06 paperless-classifier root-cause found it also blocks the `gmail` tag AUTO→NONE self-heal PATCH and ALL statement archiving/decryption)** — the 7-day testing-mode token bomb recurred (AGENTS InboxClean section; first flip the Cloud Console consent screen to "In production", THEN re-run the auth runbook for `main` — tokens issued under Testing carry the bomb even after the flip). The `work` account may be affected next. Verify: `journalctl -u inboxclean-sync` green + `curl -s http://127.0.0.1:8099/health | jq .services.gmail` shows both accounts `connected` (remember: `connected` = token PRESENCE, not refresh validity). — BLOCKED: requires human-only steps (Google Cloud Console flip + browser Google login for the copy-paste consent flow + `sudo -u inboxclean`, banned in agent sessions); state re-verified 2026-09-12: `main` still `auth_expired` at cursor 5152620, `work` never revoked and actively refreshing — only `main` needs re-consent. NOTE: the `gmail` tag demote PATCH has ALSO failed since Sep 06 with `[rejection:paperless.client_error]` under the CONNECTED `work` account — a Paperless-side rejection the re-consent may not fix (re-check after re-consent). Queue-ID remap (review finding 2026-09-13): this item was first enqueued as `000001a0935cb9377ca805e017f476f13fe9` and its landed commits 8605c6bd (BLOCKED note) + 005d1023 (status report) carry that stale ID in their footers; the canonical queue ID is `000001a0936f08db2ea47b01b7299ca8930e`. Both commits are already pushed and agent pushes are forbidden, so the footers cannot be reworded without forking history — the mapping is recorded in the remap commit's message instead (grep either ID). Re-dispatched 2026-09-13 under task ID `000001a097a2c87f00e1adb5182169e264d1` (carried by remap commit d23e47ae's footer) — every queue ID ever used for this item now resolves via `git log --grep`.
- [ ] [blocked:user] **Miniflux SSO-only flip (`services.miniflux.disableLocalAuth`) — USER decision, gated on one proven live SSO login** — the OIDC chain is proven through the token exchange (2026-09-11: the "This user already exists" callback collision vs the pre-seeded break-glass admin was root-caused; fix = Settings "Link your Pocket ID account" flow while logged in with the break-glass password — runbook `docs/services/miniflux.md`, no SQL). Remaining USER steps: link + one passkey login, then the flip is one line + deploy (break-glass = flip back to false; the admin account survives in the DB). — BLOCKED: gated on human-only steps (browser + passkey: Settings → "Link your Pocket ID account" + one live SSO login — the module's own go-live gate); state re-verified 2026-09-12 (task queue): FULL `journalctl -u miniflux` sweep shows the ONLY `/oauth2/oidc/callback` hits ever are the two 2026-09-11 14:39 `user_already_exists` 400s — the link flow was never executed, zero successful SSO logins exist, and sudo (psql cross-check of `openid_connect_id`) is banned in agent sessions; flipping now = the documented lockout class (password break-glass gone while the callback still collides; unlink refused upstream once flipped). Report: `docs/status/2026-09-12_04-31_task-000001a0936f08bb3ead16850573261dbf7c.md`.
- [ ] [blocked:user] **Paperless statement decryption go-live + duplicate cleanup + retro-decrypt repair (parts 1-2 + the live repair runs USER-gated; sync-time engineering deployed 2026-09-06)** — (1) fill the bank-statement PDF password: `sudo sops platforms/nixos/secrets/inboxclean-decrypt.yaml` (PLACEHOLDER-inert until then; papersync + qpdf + the `encrypted` fallback tag are live). (2) delete the two byte-identical duplicate bank statements from 2026-09-03: `inboxclean paperless --backfill --prune` (preview with `--dry-run`). Future duplicates are rejected at the door (`PAPERLESS_CONSUMER_DELETE_DUPLICATES=true`). (3) retro-decrypt repair — **ENGINEERING COMPLETE 2026-09-12 (task queue, InboxClean master @ 7aa5c3f)**: upstream `--backfill --decrypt-repair` replaces already-archived encrypted originals in place (source re-fetch verified against the ledger checksum, qpdf decrypt, upload with the old document's metadata, settled consumption task, delete original, ledger row repointed; duplicate-refusal converges; dry-run read-only; full suite green + lint 0, 7 e2e tests incl. wrong-password/placeholder/changed-attachment/refusal-convergence). **RUNBOOK TRAP CONFIRMED + FIXED** (docs/services/paperless.md; extraction one-liners live-verified against the deployed unit 2026-09-12): the old env reconstruction was missing `INBOXCLEAN_CONFIG` (without it work-account ledger rows resolve NO source email — exactly the 2026-08-29 auth-runbook trap) AND the unit `PATH` (qpdf is NOT on system PATH — only the sync unit's Environment carries it); the runbook now derives both from `/etc/systemd/system/inboxclean-sync.service` and adds the `--decrypt-repair` flow. REMAINING: upstream push + tag + `nix flake lock --update-input inboxclean` + deploy (deployed c65c797 lacks the flag; agent sessions never push), then the user runs `--backfill --prune` + `--backfill --decrypt-repair` (dry-run first) per the runbook. **Source:** `docs/status/archived/2026-09-06_02-33_*` §3-4/§f.5-8/18/22 — BLOCKED: remaining steps are an upstream push (banned in agent sessions) and live-host repair runs (user-gated; the password is still the PLACEHOLDER). RE-VERIFIED + RUNBOOK RE-TESTED 2026-09-12 (task queue): engineering claims checked at source (flag present in the InboxClean checkout @ 7aa5c3f, lock still c65c797, origin 10+ commits behind) and the corrected env reconstruction differential-tested against the DEPLOYED CLI — without `LLM_PROVIDER` (unit sets ollama) the CLI exits `config.api_key_required` BEFORE dispatch (default provider is openai), and the runbook's `INBOXCLEAN_TOKEN_FILE` was not a knob the code reads (removed; the unit's `GMAIL_CREDENTIALS_FILE`/`GMAIL_TOKEN_FILE` added — the main-account source re-fetch builds its client from them). The reconstruction now passes the config gate and stops only at the root-owned paperless token (the user's sudo grep supplies it). Report: `docs/status/2026-09-12_06-15_task-000001a093815838131ca53b4231759e5d68.md`
- [ ] [ready] **Attic-class follow-ups from the exit-4 fix** — (1) sweep OTHER `wantedBy=multi-user.target` oneshots with mount-gated deps for the `ConditionPathIsDirectory` skip treatment; (2) collapse dependency-cascade FAILs in post-deploy-check into one root-cause SKIP; (3) VM test for pool-mounted-but-atticd-wedged. [(4) python3 probe dedupe DONE 2026-08-26] **Source:** attic 08-04 report §e/f.18-22
- [ ] [blocked:user] **Mail Relay go-live (user steps; module built + enabled with PLACEHOLDER credential 2026-09-02)** — central Postfix null client on 127.0.0.1:25 → Resend submission (`services.mail-relay`); paperless + forgejo + system mail wired and relay-gated. Sending domain DECIDED 2026-09-02: `noreply@larsartmann.cloud` (already the module default). Until go-live every send defers in the mailq (inert, nothing crashes) and the Gatus "Mail Relay Queue" check fires as the pending signal (by design). Steps (runbook `docs/services/mail-relay.md`): (1) new Resend API key (same key fixes Pocket ID, see PERSISTENT NAG), (2) verify `larsartmann.cloud` in Resend (Domains → SPF/DKIM), (3) `sudo sops platforms/nixos/secrets/mail-relay.yaml` (replace `mail_relay_password` PLACEHOLDER) + restart postfix, (4) sendmail E2E test + paperless share-link test, (5) optionally set Immich SMTP in its admin UI (127.0.0.1:25, no auth), (6) ~~decide the Paperless INBOUND mailbox~~ ANSWERED 2026-09-02 for Gmail: attachment archiving rides InboxClean (OAuth, no app passwords — see the InboxClean→Paperless go-live item); native mail rules stay reserved for a future scanner/forwarding mailbox. **Source:** mail-relay session 2026-09-02 — BLOCKED: only the Resend-dashboard/registrar steps remain (agent cannot do external dashboards/DNS): steps 1+3 DONE (real key deployed), E2E send ACCEPTED 2026-09-18 (`status=sent 250`, queue clean — owner-address allowance); remaining: REPLACE the `v=spf1 -all` SPF record with Resend's include at the registrar + wait for "Verified", then paperless share-link / forgejo notification re-test to a non-owner inbox (runbook updated).
- [ ] [blocked:user] **Rotate the InboxClean→Paperless API token (USER CHOSE NOW, 2026-09-03 — all steps sudo, user-run)** — the value transited the 2026-09-02 conversation plaintext. Order: (1) new token at Paperless: `sudo paperless-manage drf_create_token admin` (note the printed token), (2) FROM the repo root: `sudo sops platforms/nixos/secrets/inboxclean-paperless.yaml`, replace `paperless_api_token` with the NEW value, save, (3) `nix run .#deploy` (rides the pending reboot train; the sops template's `restartUnits` restarts both inboxclean units on switch). Verify: Gatus "InboxClean Paperless Archive Auth" green within 5 min of the deploy (it is the death-detector if the paste went wrong) and `journalctl -u inboxclean-sync` shows the Paperless section with no rejection. The OLD token dies only when Paperless prunes/rotates it — deleting the old token explicitly in Paperless admin (Settings → API) closes the loop. — BLOCKED: agent sandbox forbids sudo/systemctl and the age private key is root-only, so token creation + sops edit cannot run unattended; needs the interactive user runbook (2026-09-18 attempt)
- [ ] [watch] **Verify Paperless AI actually USES the llama-rag embeddings end-to-end** — UI-saved values override env; check the effective embedding endpoint; wire `PAPERLESS_AI_LLM_RERANKER_*` if supported. **Source:** `docs/status/2026-08-20_08-06_*` §g.1/§f.2 — **UNBLOCKED 2026-09-18 (gen-784 deploy): llama-rag is re-enabled and ACTIVE** — but the deploy-time restart WEDGED both servers mid-load again (both :8848/:8849 → 503 "Loading model", 94% CPU spin) = the 2026-09-02 restart-under-saturation class (deploy ran under sustained io-PSI ~55%: qemu VM test + concurrent wave builds; the pinned 0.3.0 build itself is fine — it was live-verified at ~4s load). Next step: IO-drained manual `systemctl restart llama-embeddings llama-reranker` (expect ~4s load + /health 200), THEN run this item's end-to-end verification; the reranker wiring stays MOOT (paperless 3.1.3 has zero reranker support upstream, source-verified 2026-09-18); env wiring confirmed correct (`http://127.0.0.1:8848/v1`, model `bge-m3`); the DB-vs-env effective check (`paperless_appconfig` row) still needs sudo. If a quiet-window restart STILL wedges on the pinned build, the pin's live verification is stale — reopen the upstream bisect. **2026-09-19 verification round (task queue `000001a0b68a0a38da1c4a597637e0401f85`): PREMISE STALE — llama-rag is config-DISABLED again, not "re-enabled and ACTIVE"** — the freeze-5 escape condition FIRED the same day: the pinned 0.3.0 build SPUN under the real gen-784 units (2h33min CPU over 2h43min wall ×2, 8.6G written per lifecycle, wedged right after the vocab warning) — so both this item's "deploy-time wedge = 2026-09-02 restart-under-saturation class" reading AND its "expect ~4s load" restart premise are falsified (a shell direct-run green is NOT re-enable evidence; AGENTS llama-rag section holds the standing disable + bisect-upstream-of-llama.cpp discipline with a ≥10-min REAL-unit soak required before any re-enable). Live-verified this session on deployed gen-785 (system-785 anchored, `/run/current-system` == profile): NO `llama-embeddings`/`llama-reranker` units exist in the deployed generation and :8848/:8849 refuse connections (python probe) — the prescribed `systemctl restart` step is impossible without a config re-enable + deploy, which the escape condition forbids; paperless itself is healthy (web/consumer/scheduler/task-queue all active) and its deployed env wiring re-verified correct ON THE DELIVERY SURFACE (`/etc/systemd/system/paperless-web.service`: `PAPERLESS_AI_ENABLED=true`, `PAPERLESS_AI_LLM_EMBEDDING_ENDPOINT=http://127.0.0.1:8848/v1`, `EMBEDDING_MODEL=bge-m3`, `EMBEDDING_API_KEY=llama-server-no-auth`); llama-rag's Gatus checks/tiles are enable-gated (`inherit (cfg) enable`) so the disable leaves no permanently-red checks. IO was mid-storm at session time (io some avg60=56%) — no quiet window even for a root session. — BLOCKED: E2E needs llama-rag verifiably serving again (frozen by the freeze-5 escape condition: upstream bisect + real-unit soak first) and the `paperless_appconfig` DB-vs-env effective check needs sudo (PostgreSQL peer auth) — both impossible in an agent session; re-run this item's E2E half after the llama-rag re-enable lands.
- [ ] [blocked:user] **Delete the browser-history probe user `probe-gate@example.com` (created 2026-09-18 by the registration-gate verification)** — root-only cleanup: the server DB `/var/lib/browser-history/data.db` is 0700 DynamicUser-owned and the agent sandbox cannot sudo. SQL: remove the user row from the users read-model table + the corresponding `user.registered` journal events (or replay-safe equivalent), keeping `lars@larsartmann.cloud`. Alternatively via the admin API once an admin session exists. Verify after: `browser_history_user_count` returns to its pre-probe value and a fresh registration probe still 403s. — **2026-09-19 attempt (task queue): BLOCKED — still root-gated; two state corrections for the executor.** (1) The metric criterion is ALREADY met: live `/metrics` shows `browser_history_user_count 0` (= the pre-probe value) — the freeze #5/#6 reboots restarted the server (19:31:55 CEST, uptime-verified) and the hydrated read model contains NO users, so the probe user's `users_view` row is already gone; what remains is ONLY the `UserRegistered` journal event in the `events` table (delete must be payload `$.email`-scoped — never blanket-delete UserRegistered; lars is not an ES row). (2) The "fresh registration probe still 403s" step must NOT run until the count-gap fix (line 43) lands: with count 0 live and `MAX_USERS=1`, the pinned gate (cqrs-htmx `eb356ed2`) ACCEPTS (`Count() < maxUsers`) — the probe would 201 and manufacture new debris (the 00-35 §g.1 warning, now live-confirmed). The admin-API alternative is MOOT at the deployed rev (`0971fe9`: no adminui, no user-delete route, `agent-token` is the binary's only CLI subcommand). One-paste root runbook (backup → inspect → email-scoped delete → verify, no-probe-until-fix): `docs/status/2026-09-19_03-49_task-000001a0b74a4b7a56170d0609ddd0184ce3.md` §d. — **2026-09-19 04:57 re-run (same queue ID): mechanism SHIPPED by a parallel session (auto-commit `2c2a860c` 04:32) + independently VERIFIED; live deletion pending the next `browser-history.service` start.** A parallel session landed the declarative purge: `services.browser-history.probeRegistrationCleanup` (configuration.nix) = marker-guarded, email-scoped server `ExecStartPre` (`modules/nixos/services/_browser-history-scripts.nix`) — the DynamicUser service purges its OWN StateDirectory, eliminating the root path that blocked the 03-49 run (no sudo needed anymore; deploy.sh restarts the unit on EVERY deploy, so the next deploy executes it). This run verified it first-hand: schema re-derived from source (go-cqrs-lite sqlite `events.payload BLOB` → the script's `CAST(payload AS TEXT)` is load-bearing; cqrs-htmx `users_view` email column), `nix build .#checks.x86_64-linux.browser-history-probe-purge-fixture` PASS (both-surface delete incl. BLOB payload, decoy survival, marker-on-success-only, idempotence, missing/corrupt-db, argv guards), `nix flake check --no-build` green (incl. the in-module ExecStartPre-wiring assertion), live `/health` ok + `browser_history_user_count 0` (pre-probe value holding; boot 19:31:55 predates the 04:32 mechanism → purge NOT yet executed). NEXT VERIFIER after any server restart: `journalctl -u browser-history | grep "purged probe registration"` + count still 0 → flip to [x] and remove the `probeRegistrationCleanup` block from configuration.nix (self-neutralizing debt; the marker file check is root-only, journal + metric are the agent-verifiable signals). The fresh-probe-403 criterion stays DEFERRED-BY-SAFETY until the count-gap fix (line 43) — firing it now would 201 (count 0 < MAX_USERS=1). Report: `docs/status/2026-09-19_04-57_task-000001a0b74a4b7a56170d0609ddd0184ce3.md`. — **2026-09-19 05:19 re-run (same queue ID): BLOCKED — the purge executes only on a privileged deploy, which this sandbox cannot run (sudo/systemctl/deploy all tool-blocked); all agent-verifiable preconditions confirmed.** Live at 05:18: server up since 2026-09-18 19:31:55 CEST (`process_start_time_seconds` epoch-checked) with ZERO unit restarts since 04:30 and NO `purged probe registration` journal line; deployed unit `/run/current-system/etc/systemd/system/browser-history.service` (system-785, pre-mechanism) carries NO purge ExecStartPre (so even a manual restart of the OLD generation would not purge); `browser_history_user_count 0` (pre-probe value holding); no deploy in flight (lock file holds a stale PID). The purge fires on the NEXT `nix run .#deploy` (deploy.sh restarts the unit post-switch). Close-out for the next privileged run is unchanged (04:57 NEXT VERIFIER above): journal line + count still 0 → flip to [x] → remove the `probeRegistrationCleanup` block. — **2026-09-19 05:52 re-run (queue 000001a0b7c158b007a49f73a85955717453): BLOCKED — same deploy-gated wall; the LAST agent-verifiable precondition is now CLEARED.** All 05:19 live signals re-confirmed first-hand at 05:47 (same server PID 7177/epoch since 2026-09-18 19:31:55, `browser_history_user_count 0`, no purge journal line, deployed unit still purge-free system-785, no deploy in flight) AND the third-dispatch §f.3 insurance closed: `nix build .#checks.x86_64-linux.browser-history-probe-purge-fixture` GREEN at HEAD — the `35e1a3d8` nullglob-hardening of the fixture's marker assertions is now build-verified, so the deploy trigger needs NO further agent-side prep. Close-out unchanged (04:57 NEXT VERIFIER). Report: `docs/status/2026-09-19_05-52_task-000001a0b7c158b007a49f73a85955717453.md`. — **2026-09-19 06:20 fifth dispatch (queue 000001a0b7c158b007a49f73a85955717453): BLOCKED — pure repeat, zero agent-executable work remains; suppress further pre-deploy dispatches until the next `nix run .#deploy`.** All signals re-confirmed first-hand at 06:20: server PID 7177 up continuously since 2026-09-18 19:31:55 (no restart → the 04:32 purge mechanism has not run), `browser_history_user_count 0`, no `purged probe registration` journal line since 04:25, deployed unit still pre-mechanism (no purge ExecStartPre, current-system `b1b8759`-era), no deploy in flight (no tmpfs lock, no nh/nixos-rebuild process). Close-out unchanged (04:57 NEXT VERIFIER: journal line + count 0 → flip to [x] → remove the `probeRegistrationCleanup` block).
- [ ] [decision] **Monitor365 re-enable decision (G7)** — publish crate / make repo public / vendor / or remove the module. **Source:** archived 08-15 01-44 §g G7
- [ ] [blocked:user] **Hermes PAT go-live (user)** — scaffold shipped 2026-08-20 (sops placeholder + `hermes-git-credential` + verify canary); REMAINING: create the fine-grained PAT (Contents: Read-only) and `sops --set` it — block in `docs/services/hermes.md`
- [ ] [decision] **Twenty: ENCRYPTION_KEY rotation decision + digest-pin `twenty-postgres`/`twenty-redis`** — v2.32.0 stable since 2026-08-31 → pinning now actionable. **Source:** 08-18 20-38 §c/§f
- [ ] [watch] **Hermes workspace strategy: revisit trigger** — DEFER decision (2026-08-20) stands; reopen when root >90% or workspace clones >20G
- [ ] [blocked:user] **Paperless admin password handover** — change in UI (`paperless.home.lan`, admin/sops). **Source:** 08-17 00-59 §g.3
- [ ] [blocked:user] **Live-verify the browser-history OAuth2-first-login rejection (USER-RUN: second Pocket ID passkey ceremony, ~2 min)** — the registration gate's OAuth2 auto-provision path shares `registrationMu` + the MaxUsers count check, but the live rejection was only ever verified at CODE level (inference, not measurement — P1 close-out 2026-09-18). Steps: while logged out, attempt a FIRST login to browser-history with a SECOND Pocket ID identity (one that has never logged in there) and complete its passkey ceremony; with `MAX_USERS=1` and a user already present, expect "registration closed" and NO new browser-history user. Verify: `browser_history_user_count` unchanged + `journalctl -u browser-history` shows the rejection. If a user IS created: delete it immediately (see the probe-user cleanup item in P1) and file the bypass upstream (cqrs-htmx). **Source:** status report `2026-09-18_03-56` backlog item 6 / §b.1
- [ ] [blocked:user] **Hermes: set fallback model** (`sudo -u hermes hermes config set fallback_model`)
- [ ] [blocked:user] **Test browser-history OAuth2 login end-to-end** — `history.home.lan` → "Login with Pocket ID" → dashboard
- [ ] [blocked:user] **Verify dnsblockd dashboard auth** — `dnsblock.home.lan/dashboard` with the sops token
- [ ] [blocked:user] **WebAuthn `.lan` RP ID browser validation** — passkey registration on `history.home.lan`
- [ ] [blocked:user] **Clean up orphaned dnsblockd tracking DB** — `sudo trash /var/lib/dnsblockd/dnsblockd_tracking.db` (724 MB, Jul 15)
- [ ] [decision] **CV `evaluation.min_day_rate` value decision (user)** — upstream 2026-09-02 rate floor is wired and documented (docs/services/cv.md "Evaluation knobs") but UNSET in production settings. CV repo proposes 600 €/day (owner's 75 €/h goal × 8); its Q1 asks 600 vs 700. Once decided: set `settings.pipeline.evaluation.min_day_rate` in `cv.nix`, redeploy, then run the forced re-eval pass (paste-ready command in cv.md) to re-stamp ~755 stored apps. **Source:** CV 2026-09-02_20-40 rate-floor session
- [ ] [decision] **Docker volume prune policy (user)** — 126/130 orphan volumes (3.57 GB, 95% reclaimable); `docker volume prune` has NO age filter — enumerate + allowlist decision. **Source:** `2026-08-31_20-46_*` §g Q2
- [ ] [blocked:user] **browser-history: revoke the 4 test bring-up users (user, dashboard → Agent Tokens card)** — prod DB carries 5 users; only `lars@larsartmann.cloud` is real (tokenUserEmail). Reduces the multi-user disambiguation surface. **Source:** `docs/status/archived/2026-09-05_05-09_*` §f.10
- [ ] [decision] **Paperless T13: block REST-API password auth at Caddy (one user answer + one-line deploy)** — `DISABLE_REGULAR_LOGIN` does NOT close HTTP Basic or `/api/token/` (externally reachable, root-caused 2026-09-03); designed fix = `Authorization: Basic*` matcher under `/api/*` + exact `/api/token/` block. BLOCKED on: do you use (or plan) the paperless mobile app or any password-based API client? No → ship; Yes → keep and document. **Source:** `docs/status/archived/2026-09-03_01-10_*` §a T13/§f.2
- [ ] [blocked:user] **Immich/paperless DB integrity checks post-pool-return** — history of unclean USB removals; run before trusting the migrated DBs long-term. **Source:** `2026-08-29_17-29_*` §f.42
- [ ] [ready] **Docker image retention follow-through (post-rework)** — (1) verify the granular prune unit is DEPLOYED (committed dd3479e9 after a NOT-deployed report; the live unit may still run the old false-green command); (2) `/data/docker` usage metric + Gatus check >80% (pileup was invisible ~5 months); (3) false-green detector: alert when a prune run reclaims 0B ≥2 consecutive runs; (4) post-deploy smoke asserting the deployed ExecStart = the 4-command list; (5) root-cause `system prune --filter until` 0B on docker 29.7.2 (moby source; then upstream filing via verify-before-filing); (6) SOURCE_DATE_EPOCH trap: epoch-built images pass ANY until filter (AGENTS + policy); (7) move the timer out of the Mon 03:00 backup window; (8) `runCommand` check: no bare `docker system prune` without `-a`; (9) builder-prune policy (age-only vs `--keep-storage`); (10) document rebuild paths for the deleted local-only images; (11) identify the 4 stopped containers deleted 14:47; (12) track build-cache decay over coming Mondays + verify /data frees as snapshots expire. **Source:** `docs/status/2026-08-31_20-46_docker-prune-granular-rework-status.md` §f
- [ ] [ready] **mkDockerService hardening follow-ups** — (1) VM test (docker blip at boot → compose units converge — the wants-change is reasoned + negative-proven, never positive-tested); (2) dozzle detached-flavor conversion (attach-flavor units DIE on daemon restart); (3) restart-policy audit (any container with RestartPolicy != always); (4) Gatus check for "compose unit inactive while its containers run" split-brain. **Source:** `2026-08-31_18-44_*` §b.1/§f.6-7/33, `20-30_*` §3
- [ ] [blocked:user] **Remove stray `/var/lib/paperless` remnants** (verify pool instance healthy first)
- [ ] [ready] **Recreate remaining json-file Docker containers** — one `--force-recreate` per stack. **Source:** 08-16 23-27 §B.2
- [ ] [ready] **Investigate Pocket ID SQLITE_BUSY restart bursts** — recurring across deploys, never root-caused. **Source:** 08-16 22-00 §c.4
- [ ] [ready] **Caddy reload root-cause fix** — `PrivateTmp=true` blocks `systemctl reload` (exit 4); currently band-aided with restart
- [ ] [ready] **Hermes periodic bump workflow** — pin policy decision, scheduled bump or documented cadence + v0.21.0 release-notes re-read. **Source:** hermes 08-21 §f.23/26
- [ ] [ready] **Hermes: classify live `tools.registry` journal warnings**. **Source:** hermes 08-21 §f.21
- [ ] [ready] **Hermes build-time import smoke test** (`hermes --version` exec in the VM test). **Source:** hermes 08-14 §e.6 + 08-21 §f.13
- [ ] [ready] **Standardize Docker container hardening helper** (`mkDockerService` + `extraOptions`)
- [ ] [ready] **SearXNG streaming exploration**
- [ ] [ready] **Migrate `dns-blocker.nix` to the upstream dnsblockd module** (split-brain convergence). **Source:** 08-16 00-01 §f.6-10
- [ ] [ready] **PMA: commit-failure + journald-staleness Gatus checks** (`pma_commits_failed_total` upstream + journal-mtime probe). **Source:** 08-18 13-53 §e.2-3
- [ ] [ready] **hermes: verify ROCm env + runtime llama-server VRAM verify**. **Source:** 08-18 17-42 §F.2/5
- [ ] [ready] **dns-blocker: link the OIDC recovery runbook from the docs index** + consider a `dnsblockd-oidc-secret` healthcheck. **Source:** dnsblockd 08-21 sessions (harvested 08-22)
- [ ] [ready] **HaGeZi blocklist refresh workflow** — SRI-hash refresh cadence

## Backlog (untriaged harvest)

- [ ] [blocked:user] **google-sync pre-work (execute only if the go-live-intent BLOCKED question below answers YES): `docs/services/google-sync.md` runbook, pool free-space pre-check (≥2 TB incl. 30d grace headroom), config-check fail-fast VM test, re-check `--drive-skip-checksum-gphotos` at the pinned rclone** — agent-doable pre-work ahead of the user's 5-step OAuth go-live. **Source:** 05-56 §c.2/§f.7-11/30
- [ ] [ready] **postfix `status=bounced` journal-rate textfile metric + Gatus check** — a 550 bounce drains the queue instantly so "Mail Relay Queue" stays GREEN while sending is broken (the 550 state was invisible for 5 days); after Resend domain verification add a weekly relay canary send + a Pocket ID SMTP failure signal (Pocket ID email sat broken ~19 days with zero alerts). **Source:** 07-21 §d.2/§e.8/§f.17-19
- [ ] [ready] **Self-document the deliberately-red Turso Gatus check** — rename/tag "DiscordSync Turso Sync Active" so its expected-RED state is self-documenting. ~~create `docs/services/discordsync.md`~~ DONE 2026-09-19 (commit `91d2353d` — units, secrets, Immich go-live, exit-semantics table, monitoring map). **Source:** 08-52 §e.4/§f.5-6
- [ ] [blocked:deploy] **DiscordSync Immich cross-archive wiring: deploy + go-live chain (landed 2026-09-19, eval/build-verified, NOT deployed)** — `services.discordsync.immich.enable/url` (loopback default, no Caddy on the lookup path) + gated sops secret `platforms/nixos/secrets/discordsync-immich.yaml` (PLACEHOLDER-inert) + `discordsync-immich-verify` oneshot+daily timer + deploy.sh failed-gated convergence block + runbook section. Feature present in the LOCKED rev `06a06b00` (no flake bump needed). Deploy blocked by 5 pre-existing FOD hash drifts (jscpd/openseo/systemd-graph-webui pnpm-deps, emeet-pixyd/erraudit go-modules — proven pre-existing at baseline `8ddda419`). Chain: (1) repair the 5 FODs, (2) `nix run .#deploy` when the pressure gate opens, (3) USER: create Immich API key scoped `asset.read`+`asset.upload`, `sops` into `discordsync-immich.yaml` (no-sudo path: new file + public-key encryption per the 09-30 report), (4) post-deploy: verify-unit journal `Immich API key verified`, NO `ErrImmichConfigIncomplete`, `/lookup` toggle E2E (found + not-found + asset link), secret perms 0400 discordsync:discordsync, key absent from journald. Cheap adds riding the same deploy: post-deploy-check smoke step for the verify unit, persisted eval-level regression test (conditional template + unit/secret/timer absence when disabled), `RandomizedDelaySec` on the daily timer (00:00 nix-gc collision). **Source:** `docs/status/2026-09-19_09-30_discordsync-immich-wiring-session-status.md` §c/§f Tier 1-2 — BLOCKED: FOD wave + deploy (sudo) + API key (user, Immich UI)
- [ ] [ready] **DiscordSync Immich upstream Tier-3 (fix-at-source)** — verify a release TAG carries the ADR-062 Immich feature (tag-pinned consumers cannot get it via tags otherwise; cut one if not); upstream nixos-module `immichUrl`/`immichApiKeyFile` options so other hosts wire it declaratively; fix the malformed `healthCheck` readiness URL (three-colon — SystemNix disables it today; run verify-before-filing first); consider contributing the verify-oneshot pattern upstream as an optional module feature. **Source:** `docs/status/2026-09-19_09-30_discordsync-immich-wiring-session-status.md` §f Tier 3
- [ ] [decision] **Turso plan decision (user billing call; the only live branch of the closed item): upgrade → sync auto-resumes on the next discordsync start with zero config change; permanent local-only → remove TURSO** env from the sops template + drop the deliberately-red Gatus check; if local-only, also decide whether the stale cloud DB (frozen 2026-08-16) has retention value or should be deleted Turso-side. **Source:** `cee81cbe`, AGENTS DiscordSync section, 08-52_* — BLOCKED: Turso: upgrade the plan or make local-only permanent (and if local-only: keep or delete the stale cloud DB)?
- [ ] [decision] **Google Sync go-live intent (user): whether to invest in the pre-work now (runbook, free-space check, VM test — one bounded agent task) or leave the service parked indefinitely; the 5-step go-live remains user-owned either way. **Source:** AGENTS DORMANT banner, window report §g.3** — BLOCKED: do you want the Google Sync Drive mirror live, and if so when?
- [ ] [watch] **Post-deploy Hermes cron verification bundle** — journal free of `Restart-safe cron worker dispatch failed`; `/bin/true` present and reboot-persistent; deployed unit carries `XDG_RUNTIME_DIR=/run/user/975` + After/Wants `user@975.service`; ONE real cron fire lands as a scope in `user@975.slice` (outside the gateway cgroup — that IS the restart safety); lars' imperative linger untouched after deploy. **Source:** 2026-09-12_00-42 §f.1-6 (archived report 2026-09-12_00-42_task-000001a0927c…)
- [ ] [ready] **Root-cause the InboxClean→Paperless `gmail` tag demote PATCH rejection** — failing since 2026-09-06 with `[rejection:paperless.client_error]` under the CONNECTED `work` account (80× by Sep 12) — a Paperless-side rejection independent of the dead `main` token, contradicting the old causal claim; capture the HTTP status, fix, file upstream if InboxClean-side (verify-before-filing first). **Source:** report §0/§a.5 (archived 2026-09-12_04-13_task-000001a0935cb…)
- [ ] [watch] **Stale-historyId risk check after the `main` re-consent** — main retries with `start_history_id=5152620`, now 9+ days old; verify Gmail history-window expiry handling (full-rescan fallback) before declaring sync recovered. **Source:** archived 2026-09-12_04-13 §c.4
- [ ] [watch] **Papersync blind-window coverage for `main`** — ~9 days of unarchived mail since 2026-09-04; confirm papersync rescans the gap (or trigger a backfill) after re-consent. **Source:** archived 2026-09-12_04-13 §c.5
- [ ] [watch] **Watch `work` for the 7-day token bomb while the Console may still be in Testing** — grant-issuance date unknowable from file mtimes (refreshes rewrite); alert on first `token_revoked`. **Source:** archived 2026-09-12_04-13 §b.2
- [ ] [ready] **Miniflux runbook gate-proof block** — add the documented SSO-success journal line (`User authenticated successfully using OAuth2 … username=lars`), the sudo psql `openid_connect_id` one-liner, and the coverage note to `docs/services/miniflux.md` go-live section, so agents verify the flip gate against a documented signature instead of generic greps. **Source:** docs/status/2026-09-12_04-37 §c.2
- [ ] [ready] **Miniflux VM test extension** — assert `DISABLE_LOCAL_AUTH=1` present iff `disableLocalAuth && enableOidc`, absent otherwise. **Source:** docs/status/2026-09-12_04-37 §c.3
- [ ] [watch] **Post-flip Miniflux verification drill** — Gatus "Miniflux Login Renders" against the SSO-only (form-less) page + break-glass rollback drill (flip back → deploy → password form returns). **Source:** docs/status/2026-09-12_04-37 §c.1
- [ ] [watch] **Add upstream unlink-refusal detail to the `disableLocalAuth` option description** (post-flip). **Source:** docs/status/2026-09-12_04-37 §c.4
- [ ] [ready] **Add the mail-wiring PASS-since-2026-09-05 status to the paperless runbook monitoring map** so future harvests see the check is green (docs/services/paperless.md). **Source:** docs/status/archived/2026-09-12_00-59 §f.19
- [ ] [ready] **Paperless VM-test regression: assert the relay-gated email settings render into the unit file** — closes the phantom-RED class at eval time. **Source:** docs/status/archived/2026-09-12_00-59 §f.20
- [ ] [ready] **atticd-storage-dir: derive `ReadWritePaths` from `cfg.storagePath`** (currently hardcodes `/mnt/pool/services/atticd`) — latent 226/NAMESPACE in any pool-less env; harmless today because the bootstrap's condition-skip masks it, but it blocks the VM test from ever exercising the bootstrap success path. **Source:** docs/status/2026-09-13_00-19 §c.1
- [ ] [decision] **InboxClean `work` account history (user): work is alive 14 days past its recorded Aug-29 grant while `main` died exactly on schedule — was `work` silently re-consented, and is the Google Cloud Console consent screen now "In production"? **Source:** archived 2026-09-12_04-13 §d.3, AGENTS InboxClean CORRECTION 2026-09-12** — BLOCKED: was `work` re-consented, and is the consent screen already in production (this changes the `main` re-consent instructions)?
- [ ] [blocked:user] Post-deploy: live-verify the dnsblockd cached `/health` behavior (fix rides the deployed lock rev `94c9cb93` — confirm cache-served responses and fast 503 on staleness; close TODO row 40's fix-half if green). **Source:** AGENTS.md dnsblockd section (corrected 2026-09-13) + docs/status/2026-09-13_02-52 §c.7
- [ ] [ready] Re-audit TODO row 40 (dnsblockd `/health` wedge) against the now-deployed cached-response fix — the fix-half is done; the remaining half (goroutine-dump runbook arming for the NEXT wedge, :9090 root cause) needs re-scoping. **Source:** AGENTS.md dnsblockd section (corrected 2026-09-13)
- [ ] [watch] dnsblockd bank-sync tagging: bank-sync has ZERO tags — decide a tagging scheme if any pinning consumer ever appears. **Source:** docs/status/2026-09-13_02-52 §f.15
- [ ] [ready] **tq-serve/tq-agent-pool `startLimit*` placement bug** — both units carry `StartLimitBurst`/`StartLimitIntervalSec` inside `serviceConfig` ([Service] section) where systemd 261 silently ignores them → infinite-restart risk on the pool units; move to top-level/unitConfig (AGENTS gotcha documented, zero eval guard yet). **Source:** `2026-09-14_16-34` §f.9
- [ ] [ready] **`projects-management-automation` Environment= splitting** — quoted values with spaces warn ("Artmann" bare token); audit which PMA env vars are currently mangled/ignored and fix the renderer. **Source:** `2026-09-14_16-34` §f.10
- [ ] [ready] **project-discovery-daemon IO taming** — sustains ~7 MB/s discovery scans; investigate cadence, give it ioTier/io.max bound or cache; fold into the crush-DB P1 migration measurement. **Source:** `2026-09-14_13-54` §f.8, `12-06` §f.25
- [ ] [ready] **Hermes deferred-cleanups cluster (partly PAST DUE)** — (1) delete `hermes-acl-revoke` ExecStartPre (time-gated ≥ 2026-09-03, now 11 days overdue — `hermes.nix:~215`); (2) delete-or-generalize `scripts/hermes-state-audit.sh` (shipped 2026-08-20, never run — T8 `sudo bash scripts/hermes-state-audit.sh` + MemoryMax=24G verdict still open); (3) U1 Discord E2E test; (4) Mnemosyne ingestion of `/home/hermes` + `~/projects` (the 08-20 session's original purpose, never done); (5) state.db scheduled off-peak VACUUM (the `database is locked` turn-loss class) + old state.db archiving; (6) upstream pyproject extras-drift check vs the 18-group override on each bump. **Source:** `docs/status/archived/2026-08-20_09-45_*` §c/§f, `10-45_*` §b, `20-55_*`, `23-25_*`, `2026-08-21_05-30_*` §f.15
- [ ] [ready] **DNS-blocker h2 block-page coverage + ALPN gotcha** — Gatus endpoint check negotiating h2 (the block page differs on h2 vs h1.1 — the 08-22 debugging session proved probes can see a different reality), `curl --http2` post-deploy smoke, AGENTS gotcha "server-side TLS probes must negotiate the client's ALPN", LAN NTP-anchor consideration for dnsblockd. **Source:** `archived/2026-08-22_07-01_*` §c/§f
- [ ] [ready] **Forgejo mirror monitoring depth** — sense per-repo `mirror_updated` freshness (dead-queue class is invisible to error counters alone), functional mirror probe in post-deploy-check, AGENTS gotcha (dead queue / TouchMirror masking / restart heals), VM test for forgejo DNS-gate boot ordering. **Source:** `archived/2026-08-22_08-52_*` §c/§f (upstream filing already tracked separately)
- [ ] [blocked:user] **commit-graph.lock stale-lock removal (user sudo, forgejo repo)** — 2 stale locks block gc; pairs with the upstream mirror filings. **Source:** `archived/2026-08-31_21-35_*` §7.5, `23-55_*` §f.4
- [ ] [ready] **Pocket ID regenerateSecretsFor guard** — the auto-clear/assertion mechanism so a stale `regenerateSecretsFor` can never silently rotate a secret the consumer still holds (the dnsblockd desync class). **Source:** `archived/2026-08-22_02-46_*` §f.10-11
- [ ] [blocked:user] Live runtime probe of the deployed dnsblockd cached `/health` (cache-hit latency + forced-staleness fast-503); close the standing 2026-09-13 post-deploy row. **Source:** docs/status/2026-09-17_02-20_* §f.1 — BLOCKED: requires sudo/curl on evo-x2 (sandboxed agent sessions are denied both)
- [ ] [blocked:user] Confirm `GOTRACEBACK=all` is set on the deployed dnsblockd unit and `scripts/dnsblockd-goroutine-dump.sh` is reachable; re-scope the :9090 wedge hypothesis list (the inline-DB-probe suspect is GONE post-`94c9cb93`). **Source:** docs/status/2026-09-17_02-20_* §f.2/5 — BLOCKED: unit inspection requires sudo
- [ ] [watch] Mail-relay non-owner delivery probe in post-deploy-check (paperless share-link or SMTP send to a NON-owner address) once `larsartmann.cloud` is verified in Resend — the owner-address allowance and full verification look identical (250 both ways) from postfix, so only a non-owner delivery catches the real failure mode. **Source:** docs/status/2026-09-18_01-14_task-000001a0b198bd1f03092342367c3abb9951.md §e.2
- [ ] [ready] Paperless DRF-token age metric + Gatus check (`pocket-id-secret-rotation` pattern: read `authtoken_token.created` from the paperless PG, emit token-age textfile gauge, alert >N days) — tokens minted via `drf_create_token` have no expiry and no rotation reminder; turns rotation fire drills into monitored debt. **Source:** docs/status/2026-09-18_01-30_task-000001a0b1afa0754d965dea3f825e3ba760.md §e.4
- [ ] [ready] Forgejo issue-filing one-liners + pinned upstream rev in `docs/services/forgejo-upstream-issues.md` (per-issue curl with sops-rendered token path, never inline; pin the verification short-SHA + date in the drafts header so the claims are byte-reproducible; add the `problem:`/`bug:` title prefixes at filing time). **Source:** docs/status/2026-09-18_00-42_task-000001a0b1666334580cacecb0852d0eea69.md §e.1-2
- [ ] [ready] **Post-deploy smoke for the browser-history registration gate using the CSRF double-submit dance** — today a csrf-wall 403 is indistinguishable from a closed gate (CSRF sits IN FRONT of the gate), so no smoke can see the gate's real state; the dance = cookie from `/` + `X-Csrf-Token` from `HEAD /health`, then `POST /auth/register` must 403 `[rejection:usermgmt.registration_closed]`. **Source:** docs/status/2026-09-18_03-56_task-000001a0b21d7d821da36f61711e46394735.md §e.3/§f.4 + 2026-09-19_00-35 §f.5
- [ ] [ready] **Gatus check on `browser_history_user_count`: alert on DECREASE or exceed of its pre-probe baseline** — the metric exists but is watched nowhere; a decrease = deletion/rollback signal, an increase past baseline = registration-bypass signal (the count-gap exploitation detector). **Source:** docs/status/2026-09-18_03-56_task-000001a0b21d7d821da36f61711e46394735.md §f.5 + 2026-09-19_00-35 §f.8
- [ ] [watch] **Post-deploy hermes `-v` verification bundle** — (a) `journalctl -u hermes -b --grep "\[Discord\] Connected as"` shows the line once (the positive live proof the whole smoke task built toward), (b) measure hermes journal volume for a day at INFO verbosity and revert to adapter-scoped logging (upstream change) if it balloons, (c) add the hermes-version dependency note ("requires the `-v` deploy") to the smoke's fail message in post-deploy-check.sh. **Source:** docs/status/2026-09-18_03-13_task-000001a0b2069a1df77e4c62aef8b0488125.md §f.1-5
- [ ] [ready] **browser-history registration-surface audit: verify `/auth/import` is unreachable unauthenticated on the deployed vHost path + sweep cqrs-htmx for OTHER user-creation paths bypassing `registrationMu`** (CLI/admin tools, `NewRegisterUserCmd` call sites) — the MaxUsers invariant has two KNOWN bypass classes (count-gap + import, both queued); this closes the unknown-third class. **Source:** docs/status/2026-09-18_04-47_task-000001a0b2421c771642abfbde5442abc951.md §f.13 + 2026-09-18_03-56 §f.48
- [ ] [decision] Interim Caddy fence on `/auth/register` (loopback/LAN-only) until the count-gap fix + import-gate propagation land — converts the count-gap from "one ghost user per read-model rebuild" to "zero from off-box"; costs a one-block Caddy change + the agent's own probes need a localhost exemption — BLOCKED: does the LAN trust model stay acceptable for a single-slot bypass, or fence now (docs/status/2026-09-19_00-35_task-000001a0b68a0a7d3cf616a18addd70234ff.md §g.2)?
- [ ] [decision] Browser-history gate acceptance re-run timing: wait for the upstream count-gap fix, or delete the probe user first and re-probe (accepting the first probe may 201 AGAIN — another prod mutation, another debris user) — the re-probe may be structurally incapable of a clean "closed" verdict pre-fix — BLOCKED: is "closed = first-probe-403" only acceptable post-fix, leaving prod unprobed until then (docs/status/2026-09-19_00-35_task-000001a0b68a0a7d3cf616a18addd70234ff.md §g.1)?
