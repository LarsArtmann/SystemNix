# TODO — Upstream

LarsArtmann Go-ecosystem chains (vendorHash fixes, push/tag/relock), nixpkgs/Home-Manager/third-party contributions, go-taskqueue repo work, satellite-repo hygiene.

Domain LIBRARY of the [TODO system](../../TODO_LIST.md) — every open item for this domain, any lifecycle. The dispatch QUEUE of agent-actionable (`[ready]`) items is `TODO_LIST.md`; the tq pool harvests only the queue.

House rules (enforced by convention, see AGENTS.md → "TODO System"): file an item under the domain that owns the FIX, not the symptom it mentions; one item = one ask + `**Source:**` pointer — verification narratives go to the status report, never the item; `[x]` rows are pruned to `CHANGELOG.md` at every pass; NO system-state narratives here (AGENTS.md and `docs/services/*` runbooks own state).

Tag legend: `[ready]` agent-actionable · `[blocked:user]` needs sudo/browser/external console/owner hands · `[blocked:push]` needs an upstream push/tag (agents implement, never push) · `[blocked:deploy]` waits on a deploy · `[watch]` time-gated verification · `[decision]` owner question.

## Prioritized

- [ ] [blocked:push] **INTERIM flake input pins + upstream fixes from the 2026-09-13 broken mass-update (see `docs/INTERIM-INPUT-PINS.md`)** — the automated `nix flake update` (`f8f2965e`, 188 lock nodes) snapped ~11 moving-ref inputs to upstream HEADs whose `vendorHash`/lockfile state was stale (private LarsArtmann repos have had no CI since ~2026-09-10, so nothing caught it). The deploy works again via local `git+file` pins + last-good `github:` pins, each marked `# INTERIM` in `flake.nix`. Upstream fixes to land, then flip each URL back: **`go-nix-helpers`** `/vN` pseudo-version normalization (`v0.0.0`→`vN.0.0`, worktree commit `8c87f26` — root cause of the `file-and-image-renamer` go.mod parse failure), **`file-and-image-renamer`** vendorHash (`494c9b7`), **`go-cqrs-lite`** cqrs-lint vendorHash (worktree `d84e4d6a`), **`branching-flow`** (`46000f38`), **`art-dupl`** (`9c370324`); plus stale-vendorHash refreshes in `todo-list-ai`, `library-policy`, `go-auto-upgrade`, `projects-management-automation`, `overview`, `md-go-validator` — ALL RESOLVED 2026-09-16..09-18 (probes passed, URLs flipped to branch refs, locks tracking; SigNoz pair flipped 2026-09-18 AFTER the migration review: 1 transactional quick-filter metadata migration, zero ClickHouse schema changes — see `docs/INTERIM-INPUT-PINS.md` §B). Verified `system-774` committed + anchored. **BLOCKED:** upstream pushes are banned in agent sessions — user must push, then flip the URLs per the cleanup checklist.
- [ ] [blocked:push] **SigNoz upstream trace-instrumentation gaps — flip `wiring = "upstream"` → enforced as each lands (6 binaries)** — (1) **dnsblockd**: DONE + FLIPPED 2026-09-13 (task queue): the item's premises were stale — the scheme-aware OTLP fix was already committed AND pushed upstream (`70ca2ea`, 2026-09-01), SystemNix's lock has carried it since `94c9cb93` (2026-09-06), the deployed binary is `dnsblockd-94c9cb9` with the `otlp_endpoint` config key live (dns-blocker.nix), and spans were verified flowing live (`signoz_traces_reporting{service="dnsblockd"} 1`, :9100/metrics) — so no push/tag/relock/vendorHash was ever owed; wiring flipped to `"config"` and `maxUpstreamGaps` ratcheted 5→4 per the module doctrine. (2) **bank-sync**: ALREADY DONE — flipped to `"env"` 2026-08-31, the same day this item was authored (registry comment; upstream `901978e`), spans reporting green live. REMAINING: (3) **overview** + (4) **projects-management-automation**: TracerProvider wired, ZERO span sites. (5) **papdashboard**: metrics only. (6) **hermes**: Python SDK not wired — all four re-verified still never-emitted 2026-09-13 (`last_span_age_seconds -1`); flipping them now would false-page the enforced budget. **Source:** 2026-08-31 coverage audit — BLOCKED: parts 3-6 require real upstream code instrumentation (span sites / Go trace SDK / Python SDK) in 4 separate repos, each then needing push + tag + relock (agent sessions never push)
- [ ] [blocked:user] **Forgejo upstream: file the 3 verified mirror-outage issues** — dead-queue silence, TouchMirror masking failures, ENOENT aborts of clone credential helpers (verify against CURRENT upstream main first). **Source:** `docs/status/2026-08-22_08-52_*` §b2/§c — BLOCKED: no Codeberg account/token exists on this machine (the 08-22 report's open question g.2, never answered); all three verified against the current `forgejo` branch 2026-09-18 (zero duplicates) and ready-to-file drafts persisted in `docs/services/forgejo-upstream-issues.md` — filing is one API call once a token exists
- [ ] [ready] **browser-history gate-count gap: read-model `Count()` = 0 despite existing users (found live 2026-09-18)** — the MaxUsers gate let ONE registration through with users already present, because `readModel.Count()` returned 0 at probe time (OAuth2-provisioned/legacy users apparently absent from the counted read model). Upstream (cqrs-htmx usermgmt): make the gate count from the authoritative user store (or assert Count() ≥ 1 post-hydration), and add a regression test where the first-ever gate check runs against a hydrated multi-user read model. Until fixed, exactly one ghost registration can slip through after each read-model rebuild.
- [ ] [ready] **Upstream cqrs-htmx usermgmt: an ES-registered user was LOST across restarts (found live 2026-09-19)** — the 2026-09-18 probe user was in the read model right after registration (count 1; every subsequent probe 403'd) and is ABSENT after the 2026-09-18 freeze reboots (count 0, hydrated from `users_view`), while its `UserRegistered` journal event survives in `events`. So either the registration's SQL-view persistence (`sql_readmodel.go syncToSQL`) silently failed or the hydrate/checkpoint ordering (`sql_hydrate.go` vs projection host) dropped it — a stronger count-gap-family instance: an event-sourced registration must survive restart. Upstream investigation + regression test (register → restart → `Count()` must still be 1). **Source:** `docs/status/2026-09-19_03-49_task-000001a0b74a4b7a56170d0609ddd0184ce3.md` §b/§e
- [ ] [blocked:push] **Propagate the cqrs-htmx import-gate fix to browser-history** — cqrs-htmx master `68a2cd68` gates `importUsers()` behind MaxUsers + `registrationMu` (registration-lock hole #3, done 2026-09-18). Chain: tag cqrs-htmx (v4 sub-modules — usermgmt go.mod must carry the fix), require the new tag in browser-history's go.mod, commit browser-history's `*_templ.go` if regenerated, probe browser-history's goModules FOD, then bump the SystemNix `browser-history` input and verify the deployed binary gates a JSON user import over the cap.
- [ ] [ready] **PMA discovery daemon starvation — upstream root cause** — SystemNix mitigations deployed; the fix belongs upstream. Also unquoted `GIT_AUTHOR_NAME` env entries upstream. **Source:** 2026-08-14 buildcache session
- [ ] [ready] **PMA upstream: CI guard against go-commit pin regression (recurred 2026-09-07)** — a 313-file heuristic auto-commit (`6d4cb619`) flipped PMA's flake.lock go-commit pin from `22f0e4c` back to `7321133`, silently disabling LLM commit messages for the second time (fixed 2026-09-07: PMA `1c144c8d` + `9bbc7dfb`, SystemNix bump `00d43c01`). Add a PMA flake check that (1) fails if the locked go-commit rev predates `22f0e4c`, (2) greps the vendored `DefaultChainFromEnv` for `OPENAI_BASE_URL` support, and (3) rejects a hard `?rev=` in the go-commit input URL (the backward re-pin mechanism). **Source:** 2026-09-07 diagnosis session; AGENTS.md "PMA LLM-commit REGRESSION RECURRENCE"
- [ ] [ready] **Satellite GOEXPERIMENT sweep (21 repos)** — `scripts/report-goexperiment-gaps.sh`; fix pattern: dnsblockd devShell. **Source:** SMART-BUILDCACHE-OVERHAUL §8
- [ ] [ready] **go-nix-helpers: default `GOEXPERIMENT=jsonv2` in template/devShell** + retire the direnv `use_go_env` sniffer once all satellites self-carry the flag
- [ ] [ready] **Verify go-codec floor vs nixpkgs go (1.26.7)** — the loud-fail premise resolved with the nixpkgs bump; confirm and close
- [ ] [ready] **Hermes upstream: propose `projectsDir` RO-bind pattern for the NixOS module** (verify-before-filing first)
- [ ] [watch] **Hermes: migrate `TERMINAL_CWD` → generated config.yaml when upstream supports it** (weak motivation since v0.20.4; blocked on upstream declarative config)
- [ ] [ready] **Pin go-cqrs-lite benchstat `rev = "master"`** (floating rev, hash drift)
- [ ] [ready] **Switch root `go-cqrs-lite` flake input from `git+ssh://` to `github:`** (last ssh root input; NAR-divergence vector)
- [ ] [ready] **DiscordSync: rebase the `nix/aa56b582-vendorhash` branch into master now that nixpkgs ships go 1.26.7** — the input sits on a side branch pinned below master; rebase + relock (master requires ≥1.26.6). Also: file the `TestIOBaseline_DiskWriteBytes` environmental flake upstream. **Source:** `docs/status/2026-08-26_06-06_*` §c/§f.4-5
- [ ] [ready] **`aw-watcher-utilization` poetry-core migration** — `pkgs/aw-watcher-utilization.nix:19-24`
- [ ] [ready] **`valkey` / `aiocache` / `timm` / `xformers` broken tests** — 4 packages with `doCheck = false`
- [ ] [ready] **`taskwarrior3` build flags** — `SYSTEM_CORROSION=on` + `ENABLE_TLS_NATIVE_ROOTS=on`
- [ ] [ready] **Kitty GC resilience patch**; **KeePassXC Chromium manifests**
- [ ] [ready] **Darwin user definition requirement** (issue #6036)
- [ ] [ready] **`jscpd` lockfile** PR; **XRT boost 1.87+ compat** (`nix-amd-npu`); **upstream direnv caching pattern**; **btop io_mode issue** (verify-then-file); **moby `system prune --filter until` 0B root-cause → issue** (after the local repro)
- [ ] [ready] **PMA daemon: stop committing broken flake.lock**
- [ ] [ready] **Kernovia: reconcile `pkg/eventsourcing/shared` with TypeSpec-generated types** — add an EventType enum to the TypeSpec models (today's generated `BaseEventType` is an untyped string alias, unusable as a replacement for the typed `EventTypeEnum` constants), regenerate, migrate `event_types.go`, then the architecture ratchet goes green. **Source:** v0.6.0 tag verification 2026-09-02
- [ ] [blocked:push] **go-output: cut v0.37.1 and NEVER re-tag releases** — the v0.37.0 re-push made flake locks fetch the OLD tree while the module proxy serves NEW content ecosystem-wide (BuildFlow carries a code-side workaround comment until this lands). Rule for every LarsArtmann repo: re-tagging = poisoning the proxy/lock alignment. **Source:** `docs/status/archived/2026-09-05_05-09_*` §a.5/§e, AGENTS ecosystem-repair wave
- [ ] [ready] **BuildFlow: root-cause the flake.lock flip-flopper** — the working-tree lock alternated between a 24-node and 362-node state sub-second for ~30 min (2026-09-04/05 night) while `git status` saw no diff; no direnv/daemon identified. Needs an inotify stakeout or the owning session's confession. **Source:** `docs/status/archived/2026-09-05_05-09_*` §d.4/§f.4/§g.1
- [ ] [ready] **InboxClean upstream: `/health` should validate refresh tokens (not presence) + `invalid_grant` should surface a re-auth action** — both monitoring lies confirmed live in the 2026-09-04 OAuth incident (`connected` phantom-green 8h+; UI classifies a never-self-healing revocation as "transient"). **Source:** `docs/status/archived/2026-09-04_19-57_*`, AGENTS InboxClean section
- [ ] [ready] **paperless-ngx upstream: empty-vocabulary classifier training should degrade, not FAIL the task** (verify-before-filing first). **Source:** `docs/status/archived/2026-09-06_02-33_*` §4/§f.10
- [ ] [ready] **Migrate `overview` + `project-dependency-graph` off the removed `project-discovery-sdk/daemon` module** — both still pin `…sdk/daemon v0.19.2` (overview also pins sdk v0.21.0 mixed); the proxy still serves v0.19.2 so builds work TODAY, but any sdk bump past the daemon-removal point breaks the module graph. Migration pattern is PMA (import rewrite `project-discovery-sdk/daemon` → `project-discovery-daemon` v0.1.1+). Both repos had active parallel-session WIP 2026-09-02 — migrate on touch, not blind. **Source:** TODO sweep session 2026-09-02 (§f.49 sweep hit)
- [ ] [ready] **BuildFlow: materialize the pre-commit hook on evo-x2** — the 2026-09-02 fixes (hooksPath-aware installer, devShell binary) are upstream and pushed; run `nix run .#reinstall && buildflow precommit install` once so `.githooks/pre-commit` actually exists — the commit gates have never fired on this machine (phantom-green class). **Source:** cross-repo sweep 2026-09-02
- [ ] [ready] **picoclaw: triage the latent test failures exposed by the sqlite v1.56 bump** (config security-yaml, BM25 ranking, edit/shell/codex tool tests — pre-existing, NOT sqlite regressions) + check whether picoclaw CI runs `go test` at all. **Source:** cross-repo sweep 2026-09-02 / archived `2026-09-03_12-31` §c.7
- [ ] [ready] **Overview upstream: retry discovery** (one-shot at startup → 503 when PMA is slow)

## Backlog (untriaged harvest)

- [ ] [ready] **Queue: done-filter / same-ID idempotency short-circuit** — two of the five 2026-09-11 window items were re-dispatched after closure; worker-side first step (check the TODO row's `[ ]`/`[x]` state + `git log --grep Task-Queue-ID`) documented in `docs/services/tq.md`; queue-side done-filter as the permanent fix; contract note that zero-change runs skip builds ("iff you changed derivations"). **Source:** 08-52 §d.1/§e.1-2, 06-50 §d.1, window report §d.1
- [ ] [ready] **Upstream InboxClean: `/health` phantom-green** — overall `status: ok` while `main` was `auth_expired` 8+ days (health = token PRESENCE, not refresh validity; UI also classifies `invalid_grant` as "transient") — upstream candidates: validate refresh in the probe, surface a re-auth action; SystemNix companion: Gatus body-pattern for `auth_expired`. **Source:** archived 2026-09-12_04-13 §d.5
- [ ] [blocked:push] dnsblockd upstream housekeeping: push or discard the 2 unpushed daemon docs commits (`cf2b690`, `20afd82`); cut a release tag carrying the OTLP scheme fix + cached-/health fix; decide tag-pin vs follow-master for the SystemNix input. **Source:** docs/status/2026-09-13_02-52 §f.11-12/14 + 04-00 §c.6
- [ ] [ready] Queue-side: stable item ID across re-enqueues + record/re-attach TQ_RESULT so a lost completion doesn't re-dispatch + embed the prior-ID chain in re-dispatch prompts (go-taskqueue repo work). **Source:** docs/status/2026-09-13_01-23 §e.1-3 (archived)
- [ ] [ready] Verify the dnsblockd `/health` cached-response regression tests ship upstream (burst-serve-from-cache + stale-503 + degradeStale table were implemented in the working tree 2026-09-06 — confirm they are in the pushed history). **Source:** AGENTS.md dnsblockd section (corrected 2026-09-13)
- [ ] [ready] Consider a lightweight queue "claim" step (one-line intent note early in the report) so duplicate dispatches discover the sibling run in seconds. **Source:** docs/status/2026-09-13_03-14 §f.24
- [ ] [decision] OWNER QUESTION: for the 4 remaining trace gaps (overview, PMA, papdashboard, hermes) — queue real upstream instrumentation as executable tasks per repo, or keep as passive gap-budget debt? — BLOCKED: repo-owner decision (report §g.1 of the 02-52 report; restated here so it is not lost)
- [ ] [blocked:push] Push `~/projects/niri-session-manager` main + `v0.5.0` tag (verified unpushed: main ahead 4, `git ls-remote --tags` finds no v0.5.0), then `nix flake lock --update-input niri-session-manager` and cut the GitHub release notes. — BLOCKED: agent contract forbids push (owner decision on push authorization and on master-tracking vs tag-pinning the input)
- [ ] [ready] Fix the website repo's `builtAt` stamping (larsartmann.com serves the 1980-01-01 placeholder sentinel — a live true-positive alarm; after the fix the monitor goes quiet and silence means fresh). — BLOCKED: fix belongs to the larsartmann.com website repo (prepare verify-before-filing issue text on request)
- [ ] [decision] OWNER QUESTION: is the cv VM failure (`oidc.client_secret is required when oidc.enabled` at cv config validation) a KNOWN upstream CV tightening the fixture must adopt, or an upstream regression needing an issue first? — BLOCKED: determines whether tests/test-cv.nix seeding is mechanical or blocked on verify-before-filing
- [ ] [ready] **Repo-generic CI do-analyzer across all LarsArtmann Go repos** — provide/invoke type-parameter pairing lint (branching-flow's `doanalyzerv2` generalization); prevents silent reintroduction of the DiscordSync trap class. **Source:** `docs/status/archived/2026-09-14_01-41_task-000001a09d06ce7945994b3bef63af3ff419.md` §Residual risk
- [ ] [ready] **go-taskqueue: queue-level work-claiming/dedup** — surface an in-progress/claim marker to harvest so two runs stop hitting the same TODO item within hours (Zone 6 double-claim precedent). **Source:** window report §e.4
- [ ] [decision] Should verification-only queue runs mint an empty footer commit so the queue's commit_sha cross-reference always resolves, or is "no commit + honest TQ_RESULT" the preferred contract? — BLOCKED: needs owner decision (repo/queue contract).
- [ ] [decision] Allow docs-only queue tasks (status reporters, closeouts) to run with `require_clean=false` or a dedicated dirty-tolerant lane — this closeout requeued ~20x over 7.5 h on parallel sessions' dirty trees (CHANGELOG.md, integration.nix, test-miniflux.nix, ucr-rag.py, attic.nix, .gitleaks.toml, nix-check.yml, backup-coordination.nix, crush-daily.nix) plus 2 rate-limit deferrals — BLOCKED: needs owner decision (queue contract change: is docs-only write-safety worth the starvation?).
- [ ] [ready] tq preflight corrupted-repo lane: on `git status` failing with empty/missing-object errors, requeue with delay + distinct reason (or alert) instead of hot-looping claims every 3-14 min — the wedge starved the queue's own recovery work for ~1 h. **Source:** this run §d.2/§e.3
- [ ] [decision] Should repeated closeout runs of the same window be deduped/claimed/rate-limited at the queue level, with delta-only as the re-run contract? (This window's closeout has now run FOUR times.) — BLOCKED: needs owner decision (queue contract).
- [ ] [ready] go-taskqueue: visibility surface for the citation check (journal fact or harvest counter) — silent annotation successes are unverifiable; also measure cost on the SHA-dense SystemNix TODO_LIST in batch mode. **COST MEASURED 2026-09-16 (verification session, read-only probe of the same primitive the check uses — `git merge-base --is-ancestor` per deduped SHA-like token):** SystemNix TODO_LIST carries **112 unique SHA-like tokens (7-40 hex)**; full sweep ≈ **0.4s warm / up to ~14s cold** (first-run cold lookups dominated by object-miss searches; warm fork+exec ≈ 3.4ms/token). Verdict: acceptable inside a 5-min harvest tick even on the densest repo; if the visibility surface lands as a counter, also memoize probe results per harvest pass (the in-text dedupe already exists; cross-item memoization is the cheap win). The surface DECISION (journal fact vs harvest counter) stays open — owner/implementation call, not executed under the parallel session's active go-taskqueue work. **Source:** sixth-run report §b.6/§b.7/§f.10
- [ ] [ready] go-taskqueue: decide whether `FailureEvidence.Tail` (tq show/DLQ surface) should shrink to the 512-byte excerpt + evidence path like the journal fact now does — same noise, one surface over. **Source:** sixth-run report §b.4/§f.9
- [ ] [ready] Root-cause WHY BuildFlow minted `gobuild`/`gocache`/`gomod` instead of Go's standard `go-build`/`go` fallback names (BuildFlow-side path construction suspected); fix upstream if confirmed. **Source:** docs/status/2026-09-17_02-54_* §f.13-14
- [ ] [blocked:push] Cut a dnsblockd release tag carrying the cached-/health + OTLP-scheme fixes; then decide tag-pin vs `?ref=master` for the SystemNix input (the "two untagged fixes" rev-pin justification expires when the tag lands). **Source:** docs/status/2026-09-17_02-20_* §f.3
- [ ] [ready] Queue hygiene: auto-skip dispatch of items already `[x]` with a matching-footer commit; schedule time-gated items at their trigger time. **Source:** docs/status/2026-09-17_03-23_* §d.1/d.2
- [ ] [decision] Queue policy decisions: schedule time-gated tasks at trigger time or fund self-verifying probes; stop assigning sudo/curl-gated verification items to sandboxed agents — BLOCKED: how should the queue handle time-gated and privilege-gated work (docs/status/2026-09-17_03-23_* §g.3)?
- [ ] [decision] Queue dispatch privilege markers (a machine-readable `[SUDO]`/`[USER-RUN]` tag the walker respects, distinct from the informal BLOCKED annotation) — 2 of 5 window slots were consumed by capability-mismatched dispatches — BLOCKED: should sudo-gated items be skipped by sandboxed agents entirely, or kept as diagnose-and-annotate runs (docs/status/2026-09-18_03-45_* §g.1; extends the 2026-09-17 queue-policy row)?
- [ ] [ready] **Queue intake freshness rule: items asserting live system state ("X is re-enabled and ACTIVE") get a mandatory reconcile-against-the-relevant-AGENTS.md-section step before execution** — the paperless E2E item dispatched with a premise falsified hours earlier by freeze #5; one grep would have reshaped the whole session (complements the dispatcher subject-grep row). **Source:** docs/status/2026-09-19_00-19_task-000001a0b68a0a38da1c4a597637e0401f85.md §e.1/§f.22
- [ ] [decision] Hermes logging posture: is global `-v` (INFO for the WHOLE gateway) acceptable permanently, or should this become an upstream request for adapter-scoped logging / an env-var log level (HERMES_LOG_STDERR_LEVEL) so systemd units can raise verbosity without ExecStart edits — BLOCKED: owner call on permanent journal noise vs upstream ask (docs/status/2026-09-18_03-13_task-000001a0b2069a1df77e4c62aef8b0488125.md §g.2)?
- [ ] [ready] **storage-collector: durable UMask fix upstream** — SystemNix carries `UMask = mkForce "0022"` as a wrapper-layer patch (upstream module pins `UMask=0077` under DynamicUser → node_exporter cannot read the `.prom` at all; mkForce fights the upstream key on every eval). Fix in `~/projects/storage-collector` (crate-level chmod 644 on the prom write, or module default 0022) and drop the mkForce. **Source:** 2026-09-20 deploy-review session §1.2/§b; commit 514dba99
- [ ] [blocked:push] **clean-wizard → evo-x2 wiring** — push `~/projects/clean-wizard` first: local fix landed 2026-09-23 — HEAD `b5c782b` carries the `buildGoModule.override { go = pkgs.go_1_27; }` one-liner (FOD died `go.mod requires go >= 1.27.1 (running go 1.26.7; GOTOOLCHAIN=local)` — CV 2026-09-17 hold class); the uncommitted working-tree edit refreshes `vendorHash` (the toolchain switch invalidates it; flake.nix sits OUTSIDE `src`'s fileset, so the measured hash is valid for clean HEAD too). Then SystemNix: add input `github:LarsArtmann/clean-wizard?ref=master`, add `clean-wizard = flakePkg inputs.clean-wizard;` to `lib/lars-packages.nix` (the mkLarsPackages surface auto-flows into evo-x2 PATH via `platforms/common/packages/base.nix`), `nix flake lock --update-input clean-wizard`, `nix flake check --no-build` — do NOT wire while github master is still the broken rev: `nix flake check` stricts every package, so a broken FOD in the surface reds the whole fleet (monitor365 removal class). Verified locally 2026-09-23: `nix build .#default` green, binary runs. **Source:** this session (clean-wizard "why not on evo-x2" investigation, 2026-09-23)
