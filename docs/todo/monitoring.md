# TODO — Monitoring

Gatus, SigNoz (rules/dashboards/provisioners), textfile collectors, system-health, alert routing + noise, freshness/coverage — the cross-service observability infra. Service-specific checks live with their service in `services.md`.

Domain LIBRARY of the [TODO system](../../TODO_LIST.md) — every open item for this domain, any lifecycle. The dispatch QUEUE of agent-actionable (`[ready]`) items is `TODO_LIST.md`; the tq pool harvests only the queue.

House rules (enforced by convention, see AGENTS.md → "TODO System"): file an item under the domain that owns the FIX, not the symptom it mentions; one item = one ask + `**Source:**` pointer — verification narratives go to the status report, never the item; `[x]` rows are pruned to `CHANGELOG.md` at every pass; NO system-state narratives here (AGENTS.md and `docs/services/*` runbooks own state).

Tag legend: `[ready]` agent-actionable · `[blocked:user]` needs sudo/browser/external console/owner hands · `[blocked:push]` needs an upstream push/tag (agents implement, never push) · `[blocked:deploy]` waits on a deploy · `[watch]` time-gated verification · `[decision]` owner question.

## Prioritized

- [ ] [ready] **Textfile-collector fixed-name `.tmp` audit (the niri EACCES class) + stale `btrfs-compression.prom.tmp` check** — hardened-root collectors writing `$OUT.tmp` into the sticky 1777 dir wedge forever on a foreign-owned leftover (niri blocked EVERY deploy ~2 days; the AGENTS rule exists, the ~20-collector audit does not); apply the mktemp+chmod+trap+CAP_FOWNER pattern where found, and check whether the btrfs-compression collector's timer is healthy (stale root-owned tmp observed 2026-09-05). **Source:** `docs/status/archived/2026-09-05_00-33_*` §c.1/§f.15-18
- [ ] [ready] **system-health-metrics worst-case section sum (≈500s) exceeds the 180s unit ceiling AND the 120s timer cadence — the collector structurally cannot finish under an IO storm** — measured live 2026-09-03 00:31+: EVERY run timed out at 3min for 30+ min straight (IO PSI some avg10 47-64% from 26 crush sessions + NPU-wedge fallout), forgejo mirror scan `status 124` every run, whole textfile STALE, sev1 notify-tier paging (correctly) every 30min. Section budgets: 7×60s + 2×15s + 2×10s + 30s serial ≈ 500s worst case. Fix needs design, not a band-aid: parallelize sections OR slash journal-section budgets so worst-case < 120s cadence; interim: raise unit TimeoutStartSec. Gatus `node_textfile_scrape_error` + pre-deploy §10 cover the failure fail-closed. **Source:** 2026-09-03 T16 live finding
- [ ] [ready] **`backup_ever_succeeded` metric + backup-catchup report** — a never-worked backup is indistinguishable from a stale one (cv rode "all red" outage noise its whole life); emit MTIME≠0-gated gauge; `scripts/backup-catchup-report.sh` (stamps vs OnCalendar + prom diff + btrbk dry-run) to make the next outage a one-command review. **Source:** `docs/status/2026-08-31_16-29_*` §e.3-4/§f.15-16
- [ ] [ready] **btrbk receive-freshness + pool-snapshot-freshness monitoring** — `/mnt/pool/.snapshots/services/*` and the received root/data trees have no freshness gauges in `backups.prom` (only the daily 3d-threshold verify guard covers them). **Source:** `docs/status/2026-08-31_16-29_*` §f.17-18
- [ ] [ready] **Deferred-scrub observability + scrub-guard VM test** — the new scrub deferral guard is eval-verified only; a perpetually deferred scrub shows as never-finished red with no way to distinguish policy-deferral from breakage. Add `btrfs_scrub_deferred_total` + the VM test (fake btrbk in `activating` → defer). **Source:** `docs/status/2026-08-31_17-48_*` §b.4/§c.1-2
- [ ] [ready] **Zero-series sweep automation + provisioner-Result assertion generalization + GOTRACEBACK=all sweep** — (1) script diffing every metric name in rules/dashboards vs the ClickHouse time-series table (self-maintaining blocklist); (2) generalize the signoz-provision Result assertion to ALL deploy.sh provisioners (pocket-id, forgejo-oidc, …); (3) `GOTRACEBACK=all` on the remaining Go daemons (discordsync, browser-history). **Source:** `docs/status/2026-08-27_18-58_*` §e.1-4/8-9
- [ ] [blocked:user] **Browser-test the SigNoz UI + eyeball the 5 `systemnix-*` dashboards** — API-verified only; never rendered in a browser. **Source:** 08-16 23-27 §B.1
- [ ] [ready] **Extend the user-unit monitoring pattern to remaining blind spots** — user-unit failure alerting is LIVE (2026-08-31: `system_user_units_failed{user}` + Gatus check); remaining: flm smoke PSI-skip (cold loads stretch 27-43 min under contention; smoke will false-fail), alert-noise convention for self-healing boot races (alert-after-N-retries), dnsblockd blocklist-load profiling (79s mapping.json), hermes 1.1GB state-check decoupled from the boot critical path, discordsync 5-11 min API-gap deploy wait-or-skip, imagetoraster/libcups segfault investigation. **Source:** `2026-08-31_18-44_*` §e.4-6/§f.22-25/28-30
- [ ] [ready] **Commit the dashboard generator + eval-time dashboard JSON lint** — `/tmp/gen_dashboards.py` was ephemeral; schema lint so a v1 regression fails CI. **Source:** 08-16 23-27 §C.4
- [ ] [ready] **Test-fire "Telemetry Export Failures" → Discord** — synthetic export failure in a maintenance window. **Source:** 08-16 23-27 §B.3
- [ ] [ready] **SigNoz migrator-gap guard** — assert applied-migration IDs ⊆ known list per DB (the 1010 squash-gap class). **Source:** 08-16 23-27 §E.2
- [ ] [ready] **`file_storage` cursor persistence for the journald receiver** — `start_at=end` loses logs during collector downtime. **Source:** 08-16 23-27 §C.2
- [ ] [ready] **Caddy access.log ingestion (filelog receiver)** + **log-ingestion-volume anomaly alert** (>10min silence = pipeline dead). **Source:** 08-16 23-27 §C.1/§C.5
- [ ] [ready] **buildcache-gc observability** — `buildcache_gc_last_success_timestamp` + `_prune_ok` metrics; Discord alert on watermark nukes; `--no-block` semantics decision. **Source:** 08-16 19-12 §f
- [ ] [ready] **Declarative health-check** — `criticalSystemServices` is a hand-maintained list of 4
- [ ] [ready] **System-health collector hardening** — `timeout 5` on docker inspect/ps; NRestarts single-read; MemoryMax headroom after the 08-31 stall (128M→256M if not yet raised)
- [ ] [ready] **Gatus lint residuals + synthetic ingest probe + smoke enable-gate sweep** — method-uppercase lint DONE 08-26; remaining: authenticated POST-to-ingest probe (401-only can't catch method/body bugs), `test -e` enable-gate sweep, papdashboard ingest success-count metric. **Source:** 08-18 20-52 §e.3
- [ ] [ready] **Consider gatus `alerting` dedup** (N endpoints, one root cause, one message). **Source:** 08-22 01-46 §f.14

## Backlog (untriaged harvest)

- [ ] [watch] **Post-deploy pool-smart validation** — collector runs as root with real SAT reads (first live run), `pool_smart_*` metrics present at node_exporter, pre-deploy §10 `KNOWN_NEW_METRICS` loan CLEARED once verified, post-deploy §13 green, `systemnix-pool-storage` dashboard panels render live. **Source:** docs/status/2026-09-12_08-28 §b.1/b.3
- [ ] [ready] **Persisted regression tests for the pool-smart collector script** — the fixture harness lived in /tmp; promote the healthy/mutated/unprivileged/absent-drive cases into the repo. **Source:** docs/status/2026-09-12_08-28 §c.1
- [ ] [ready] Investigate the live "SigNoz Traces Coverage" Gatus red (`signoz_traces_missing 3`: file-and-image-renamer ×2 + gotenberg silent >40 days despite 720 h budgets) — demote to `wiring = "upstream"`, add never-seen tolerance, or exercise the services. **Source:** docs/status/2026-09-13_02-52 §f.9-10
- [ ] [ready] Split `signoz_traces_missing` semantics into never-seen vs went-dark (separate gauge or `reason` label) so event-driven services stop muddying dense-service outage detection. **Source:** docs/status/2026-09-13_02-52 §e.6
- [ ] [ready] Verify no SigNoz alert rule hardcodes the old `maxUpstreamGaps` value of 5 (metric is config-driven; one grep). **Source:** docs/status/2026-09-13_02-52 §f.18
- [ ] [ready] Shorter freshness budget design for dense always-on emitters (dnsblockd emits every few seconds; 26 h detection of a silent span-stop is generous — 6 h would catch the :9090-wedge class faster). **Source:** docs/status/2026-09-13_02-52 §f.21
- [ ] [ready] After the 4 remaining trace-gap flips: add each serviceName to the SigNoz Services-page onboarding checklist in the coverage module comment; at 0 gaps keep the budget check alive at 0 as the permanent silent-noop tripwire. **Source:** docs/status/2026-09-13_02-52 §f.6/22
- [ ] [ready] Gatus "SigNoz Traces Coverage" check body pattern uses `[BODY] != pat(*\nsignoz_traces_missing [1-9]*)` — verify it flips green automatically when `missing` returns to 0/2 after the missing-3 decision (no hardcoded count). **Source:** docs/status/2026-09-13_02-52 §f.9
- [ ] [ready] **Enabled-but-inactive consumer detection net** — textfile metric + Gatus check for `is-enabled && !is-active` across system + user managers (the pool-dropout class ran 3 h dark with every event-driven layer green); plus the one-off full sweep for OTHER services stranded inactive since any boot. **Source:** `2026-09-14_16-34` §f.8/§f.34
- [ ] [ready] **Gatus "I/O Stall Rate" permanently-red review** — a continuously-red check is alert-blindness in the making; retune or annotate the storm-era state. **Source:** `2026-09-14_13-54` §f.19
- [ ] [ready] **Signoz-coverage/observability test+panel additions** — "Telemetry Coverage" dashboard panel, `tests/test-signoz-coverage.nix` VM test, metrics-freshness layer, papdashboard `/metrics` scrape, run `tests/test-gatus-patterns.nix` heavy-job-wrapped once to verify its gatus-field assumptions, extend gatus-pattern-lint to ENFORCE anchored value-checks, monitor-layer test batch (system-health collector fixtures, SigNoz provisioner jq-projection tests, btrbk-pool-clean deploy-enqueue VM test, credential-helper offline VM test, GC-guard 4-scenario regression test as a flake check, clickhouse ownership-heal VM test, dozzle container-drift smoke). **Source:** `archived/2026-08-31_21-33_signoz-*` §f, `23-59_*` §f, `2026-08-22_06-12_*` §f, `2026-08-21_04-50_*` §f
- [ ] [ready] **Pool-usage Gatus thresholds** — >50% WARN / >70% CRIT on the HDD pool (only docker >80% exists today). **Source:** `archived/2026-08-21_21-43_*` §e.4
- [ ] [ready] Add `crush_hot_db_*` textfile metrics (migrated/skipped/failed counters + last-success timestamp, fail-closed) + Gatus freshness check so "converged" is observable without journal digs. **Source:** same report §f19
- [ ] [ready] Build a post-23:30 self-verifying receive-freshness probe (textfile metric `root_receive_age_hours` + Gatus check in backup-coordination) so nightly convergence stops depending on midnight agent sessions. **Source:** docs/status/2026-09-17_00-25_* §f.3/39
- [ ] [ready] Wire `memory_emergency_guard_churn_rearms_total` into a Gatus check or the SigNoz guard dashboard (threshold policy — see the blocked question). **Source:** docs/status/2026-09-17_01-52_* §f.6-7
- [ ] [ready] Build the generic displaced-cache detector: textfile collector flagging any non-HM-managed `~/.cache/*` real dir >1G (new-fallback-name class — this window's reap list is instance #2 of the gap). **Source:** docs/status/2026-09-17_02-54_* §e.1/f.3

