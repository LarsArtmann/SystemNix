# TODO — Security

Key rotation, secret hygiene, sops, gitleaks/push-protection policy, git-history leak residues, auth-surface questions.

Domain LIBRARY of the [TODO system](../../TODO_LIST.md) — every open item for this domain, any lifecycle. The dispatch QUEUE of agent-actionable (`[ready]`) items is `TODO_LIST.md`; the tq pool harvests only the queue.

House rules (enforced by convention, see AGENTS.md → "TODO System"): file an item under the domain that owns the FIX, not the symptom it mentions; one item = one ask + `**Source:**` pointer — verification narratives go to the status report, never the item; `[x]` rows are pruned to `CHANGELOG.md` at every pass; NO system-state narratives here (AGENTS.md and `docs/services/*` runbooks own state).

Tag legend: `[ready]` agent-actionable · `[blocked:user]` needs sudo/browser/external console/owner hands · `[blocked:push]` needs an upstream push/tag (agents implement, never push) · `[blocked:deploy]` waits on a deploy · `[watch]` time-gated verification · `[decision]` owner question.

## Prioritized

- [ ] [blocked:user] **🔑 PERSISTENT NAG: rotate leaked/stale API keys — 2026-09-11 triage (task queue):** (1) **Resend: the REAL new key IS deployed** — mail-relay collector reports `mail_relay_credential_placeholder 0` live and AGENTS 2026-09-06 records AUTH passing at smtp.resend.com (sends 550 ONLY because `larsartmann.cloud` is unverified in Resend — that domain verification is a USER dashboard step and completes BOTH the Mail Relay go-live (P1 below) AND Pocket ID delivery, whose sender is `noreply@larsartmann.cloud`); `pocket-id.yaml` was re-encrypted 2 min after `mail-relay.yaml` the same night (commit 467983e8, only `pocket_id_smtp_password` touched; render chain live 2026-09-11) — the paste is CIPHERTEXT-CONFIRMED, not just plausible: sops preserves unchanged values' ciphertexts (experimentally verified 2026-09-11 — a value's ENC blob changes IFF its plaintext changes; sibling blobs and the age data-key stanza survive saves), and 467983e8 changed exactly ONE of pocket-id.yaml's SIX data keys → pocket_id_smtp_password's plaintext was definitively replaced 2026-09-06 in a surgical edit; still unverifiable without sudo: whether it byte-equals the mail-relay key, and deliverability (gated on domain verification) — re-paste only if the post-verification test send still fails. (2) Synthetic: DONE 2026-08-18 (rotated at provider + `sops --set` into crush-daily.yaml; git date 43e11129 confirms) — history residue inert. (3) Context7 `ctx7sk-…` still LIVE per 2026-08-18 — dashboard rotation remains USER-owned; the "update MCP config" half is MOOT (verified 2026-09-11: zero context7 references in the rendered crushrc AND crush.json). Rotation is the entire fix (history-purge push HELD by user decision). **Source:** AGENTS.md "Secret Leak Incident" — BLOCKED: every remaining step is an external Resend/Context7 dashboard action or a sudo-only sops decrypt/verify (sudo banned in agent sessions)
- [ ] [blocked:user] **Secret-history-scan stays red until 2 rotations (user)** — the scanner correctly reports the 2026-08-18 residues; still-LIVE keys in public history: Gemini `AIzaSyAV…LU4k` (delete via console project `453958689374`) and Context7 `ctx7sk-a5b19…` (rotate at context7.com dashboard). After BOTH are dead: decide allowlisting the inert residues (digest-based, never plaintext) so the workflow regains signal. **Source:** AGENTS.md "Secret Leak Incident" + `2026-09-03_23-*` §f

## Backlog (untriaged harvest)

- [ ] [ready] **Create `docs/security/rotations.md` rotation ledger** — per key: created/rotated dates, sops file + key name, residue map, verification command; backfill from the 2026-09-11 key-rotation triage verdict table; document the sops ciphertext-diff pipeline (a value's ENC blob changes iff its plaintext changes — experimentally verified 2026-09-11) as the sanctioned no-sudo rotation verifier. **Source:** 07-21 §e.6/§f.14/24-25
- [ ] [blocked:user] **ROOT: close the key-rotation orbit** — sudo decrypt cross-check `pocket_id_smtp_password` == mail-relay key; relay E2E test send per `docs/services/mail-relay.md`; one Pocket ID email (invite/forgot-password). **Source:** 07-21 §c.4-5/§g.1
- [ ] [ready] **ROOT: settle the `/run/secrets/sops-nix-age-key` ghost** — absent at runtime while `quickshell.nix` + the DMS `systemnix-sops` plugin reference it; fix the render or retire the references. **Source:** 07-21 §d.5/§f.20
- [ ] [blocked:user] **Scrub the full plaintext leaked-key values from AGENTS' purge-runbook block once Context7 + Gemini keys are dead** — values become `<REDACTED>` placeholders, the command stays; stale plaintext in a tracked file violates the repo's own no-secret-values rule the moment rotation kills the keys. **Source:** 07-21 §d.4/§f.10
- [ ] [decision] **Decide agent sops-write capability (user age key as an additional `.sops.yaml` recipient)** — converts the sudo-only BLOCKED class (key rotations, Hermes PAT, InboxClean tokens) into executable work at the cost of agent write access to every secret. **Source:** 07-21 §e.9/§g.2/§f.28
- [ ] [decision] OWNER QUESTION: gitleaks policy for SHA-shaped strings in tracked docs — abbreviation-only (current) or a bounded `.gitleaks.toml` allowlist? Recommendation: abbreviation-only. — BLOCKED: repo-owner policy decision (report §g.3)
- [ ] [ready] **Crush key-hygiene leftovers** — rotate zai/gemini/minimax/kimi keys (extends the PERSISTENT NAG beyond Resend/Synthetic/Context7), retire the inert `mimo_api_key` PLACEHOLDER sops line, per-provider key + live-LSP functional verification, `:8899` llama-server keeper decision, `scripts/sops-new-secret.sh` helper, crush.db residue vacuum. **Source:** `archived/2026-08-31_21-33_crush-*` §f
- [ ] [decision] Execute the held purge runbook at the next push point (re-clone + re-filter + force push, per the AGENTS runbook — `120ada36` is now on origin), or formally retire it in favor of key-rotation-only — the backlog push removed the "held" safety of local-only history. — BLOCKED: needs owner decision (purge vs rotation-only; force-push scope).
- [ ] [blocked:user] Resend key identity + mail monitoring depth — byte-compare `pocket_id_smtp_password` vs the mail-relay key (sudo-only sops decrypt) so one rotation can be known to cover both consumers — BLOCKED: is the Pocket ID SMTP key the same Resend key as mail-relay.yaml (docs/status/2026-09-18_01-14_* §g.2)?
