# TODO — Pipeline

deploy.sh + pre/post-deploy checks, flake checks + eval-time audits, CI workflows, pre-commit, repo/docs hygiene, AGENTS.md upkeep, tq/queue conventions, auto-commit daemon protocol.

Domain LIBRARY of the [TODO system](../../TODO_LIST.md) — every open item for this domain, any lifecycle. The dispatch QUEUE of agent-actionable (`[ready]`) items is `TODO_LIST.md`; the tq pool harvests only the queue.

House rules (enforced by convention, see AGENTS.md → "TODO System"): file an item under the domain that owns the FIX, not the symptom it mentions; one item = one ask + `**Source:**` pointer — verification narratives go to the status report, never the item; `[x]` rows are pruned to `CHANGELOG.md` at every pass; NO system-state narratives here (AGENTS.md and `docs/services/*` runbooks own state).

Tag legend: `[ready]` agent-actionable · `[blocked:user]` needs sudo/browser/external console/owner hands · `[blocked:push]` needs an upstream push/tag (agents implement, never push) · `[blocked:deploy]` waits on a deploy · `[watch]` time-gated verification · `[decision]` owner question.

## Prioritized

- [ ] [ready] **Pre-deploy batch build of mkLarsPackages + cv + hermes inputs** — the 2026-08-10 suggestion, still unwired; would have surfaced both 08-29 failures in seconds instead of a 12-min aborted deploy (recurring monthly class). Add a fast flake output (e.g. `.#quick-go`). **Source:** `docs/status/2026-08-29_17-29_*` §e.1/§f.18/46
- [ ] [ready] **Known-outage classification in post-deploy-check** — FAILs whose units are down from a documented absent dependency (pool detached) → WARN with reason; keep FAIL for true regressions (alarm-fatigue protection — 9 days of identical red desensitized everything). **Source:** `docs/status/2026-08-29_17-29_*` §e.3/§f.19
- [ ] [ready] **Eval-time audits for unit-shape contracts (the cv-226 class)** — (1) `ReadWritePaths` under `/mnt/pool` ⇒ `RequiresMountsFor` + a declared creator; (2) backup-related timers MUST set `Persistent=true` (flake check); precedent: `otel-endpoint-audit.nix`, `udev-block-letter-audit.nix`, `gate-timeout-audit.nix`. (3) NEW (2026-09-05): flake-check lint rejecting `?rev=` in input URLs — a URL rev OVERRIDES flake.lock (the overview templ-components trap; `sops-key-audit.nix` is the precedent for pure-eval lints). **Source:** `docs/status/2026-08-31_16-29_*` §e.1-2/§f.5-6, `17-22` §c.2-3, `2026-09-05_05-09_*` §f.14
- [ ] [ready] **Surprise bulk `nix flake update` validation gate** — an unattended full lock update moved ~12 inputs at once and broke deploys (2026-08-29); gate bulk updates behind a validation build (wrapper or CI). **Source:** `docs/status/2026-08-29_17-29_*` §f.37-38
- [ ] [ready] **Structural §10 fix: URL-aware phantom-metric extraction** — only `pat()` conditions on `/metrics` URLs should feed the metric-presence validator (the `email_state`/`connected` exclusion regex grows otherwise); plus regression tests: convergence-guard prefix comparison + JSON-field body-pattern case in `tests/test-gatus-patterns.nix`. **Source:** `docs/status/2026-08-30_11-15_*` §c.1/§e.3/§f.7/10-11
- [ ] [ready] **§11 vendorHash freshness: wire real FOD dry-run checks** — 6/6 Go pkgs report "unable to determine status" in pre-deploy-check §11; the class broke deploys twice in August. **Source:** `docs/status/2026-08-30_11-15_*` §f.19
- [ ] [ready] **Service-completeness manifest audit (eval-time or CI): every enabled service ⇒ Gatus check + dashboard tile (PapDashboard services.json) + backup registration + docs page** — inspired by github:taciturnaxolotl/dots' `services-manifest` flake output (evaluates `atelier.services.*` into JSON incl. internal `_description`/`_runtime` options, feeding their infra dashboard + mdbook); our version inverts it into an ASSERTION source: cross-reference enabled `config.services.*` against gatus-config endpoints, papdashboard dashboard tiles, `backup-coordination` entries, and `docs/services/*.md` — closing the "AGENTS.md says every new service MUST be monitored, nothing enforces it" gap (a service added without a Gatus check today sails through every layer). **Source:** dots repo review 2026-09-10 (`lib/services-manifest.nix`, `modules/lib/mkService.nix`)
- [ ] [ready] **Pre-commit nix-eval gate for `*.nix` edits (the daemon broken-intermediate class)** — the auto-commit daemon has swept broken `*.nix` intermediates into history 4× (race between check and add); add a fast gate to the single `.githooks/` hook: `nix-instantiate --parse` per staged `*.nix` file (sub-second, catches syntax breakage) with full `nix flake check` left to CI/pre-deploy. Parse-only first — eval-per-commit is too slow for the daemon's cadence. **Source:** `docs/status/2026-09-19_15-09_immich-pareto-round2-deploy-blocked.md` §f.7
- [ ] [blocked:user] **Deploy to macOS** — darwin registry override written, NOT deployed
- [ ] [blocked:user] **CI read-only GitHub PAT (user, 1 step)** — all nix workflows have been dark for 120+ runs: 32 private LarsArtmann repos are pinned as `github:`-type lock nodes and the job-scoped `GITHUB_TOKEN` cannot read them (tarball 404). Workflows now fall back to `NIX_GITHUB_RO_TOKEN` (committed 2026-09-03): create a fine-grained PAT (Contents: Read-only, all repos in the account) at github.com/settings/personal-access-tokens, then `gh secret set NIX_GITHUB_RO_TOKEN --repo LarsArtmann/SystemNix`. Turns nix-check (incl. test-cv VM), go-deps-audit, and nixpkgs-compat green without touching flake.lock. **Source:** `docs/status/archived/2026-09-03_23-*_cv-closeout-ci-unblocked.md`
- [ ] [decision] **Deploy policy for foreign undeployed tree changes (user/team contract)** — (a) ship-as-is + smoke checks, (b) halt until owner deploys, or (c) ship + require diffstat acknowledgment (the flm-1.0.3 50-min NPU outage was case (a) going wrong). **Source:** `2026-08-31_18-44_*` §g.2
- [ ] [ready] **crush-config phase 2: serve AGENTS.md + references from the module, retire the local `~/.config/crush` dotfiles git repo** — the repo (`github:LarsArtmann/crush-config`, PRIVATE) owns the crushrc since 2026-09-05 (`homeManagerModules.crush`, `programs.crush-config`; repo flake checks render+`bash -n` the rc; extraction was gated on a byte-identical 1609-model `crush models` parity diff). Remaining split-brain: AGENTS.md + references exist BOTH live (local git repo, no remote) and in the repo (synced 2026-09-05; only `references/architecture.md` differs by the repo-side npm→pnpm line). Phase 2 = install those files via the module (`xdg.configFile`), untrack/remove them from the local repo, decide its fate (skills symlinks under `~/.config/crush/skills/` are NOT crush-config's — they point at `~/projects/SKILLS` and in-repo project checkouts, never `/nix/store` snapshots). Also decide: deploy key (`NIX_DEPLOY_KEY_CRUSH_CONFIG`, git+ssh) vs waiting for `NIX_GITHUB_RO_TOKEN` — the input is a private `github:`-type lock node, dark in CI until one of those exists (or make the repo public — it is secret-free). Phase-2 polish sub-items: renderer quoting (golangci LSP command currently unquoted) + comment-block double-newline dedupe; `tests/test-crush-config.nix` wiring test; crush smoke in post-deploy-check §13 (store-symlink + generated-header grep + isolated `crush models`); repo-side gitleaks CI; `packages.default` retirement review; consider tagging v4.0.0 once polish lands. **Source:** AGENTS.md "Crush Provider Keys", `docs/status/archived/2026-09-05_21-14_*` §c/§f, docs/services/crush.md
- [ ] [ready] **deploy.sh: lock-wait on activation contention** — poll/serialize instead of aborting after the expensive build; consider `nix run .#update` app. **Source:** 08-16 21-25 §d.2
- [ ] [ready] **Flake-check guard: HTML `pat()` needles must start with `<`**. **Source:** 08-16 21-25 §e
- [ ] [ready] **Derive AUTH_VHOSTS from caddy.nix (or eval-time assert)** — 5/7 probes used hostnames that never existed. **Source:** 08-16 22-00 §c.2
- [ ] [ready] **Centralize curl in `scripts/lib.sh` `fetch()` helper**. **Source:** 08-16 22-00 §c.3
- [ ] [ready] **Identify residual post-deploy WARNs** (null-byte line ~233; quickshell 1-error-line largely triaged transient 08-26; File Renamer "0 operations"). **Source:** 08-16 22-00 §b.2/§c.5
- [ ] [ready] **post-deploy-check: poll browser-history /health + drop deploy.sh explicit restart** — startup fast since v4.7.0; the 4.5-min false-FAIL window is obsolete. **Source:** 08-16 03-44 §e
- [ ] [blocked:user] **Create Attic cache + CI token** — `atticadm make-token`; push `signoz-frontend` (122 MiB) + the hermes uv2nix tree (daily CI rebuilds it from scratch). **Source:** hermes 08-21 §f.18
- [ ] [ready] **Extend mkOidcGate with optional diagnostic output** (lost TLS fingerprint diagnostic)
- [ ] [ready] **Post-deploy-check failure semantics + escalation** — warn-vs-fail; failure-streak file
- [ ] [ready] **VM tests for the 2026-08-14 Gatus patterns** + `tests/test-buildcache.nix` + provisioner-idempotency VM test. **Source:** monitoring-gap report §C.9, 08-14 18-29 §f.18, 08-16 06-38 §b.4
- [ ] [ready] **Verify vendorHash pre-deploy check patterns at runtime** (§11 greps untested against real output)
- [ ] [ready] **Add `--force` flag to deploy.sh for phantom metrics** (documented escape hatch)
- [ ] [ready] **Refactor discordsync to use a shared HTTP gate helper** (`mkHttpGate`) or extend mkDnsGate; move gate helpers to `lib/gates.nix`; add eval-time asserts
- [ ] [ready] **Retire stale ZFS-VM scripts/workflow remnants** (`scripts/zfs-vm-*.sh`, `systems/zfs-vm.nix`)
- [ ] [ready] **sops manifest check-mode in pre-deploy-check** (`sops-install-secrets -check-mode` — secrets drift fails BEFORE switch). **Source:** 08-18 20-38 §e.1
- [ ] [ready] **deploy-window journal anchoring + retry/backoff on external HTTP checks**. **Source:** 08-18 20-38 §e.6-7
- [ ] [ready] **Add 3 gotcha entries from the fastflowlm 203-fix session** (missed by two harvests). **Source:** 08-17 22-55 §f.10
- [ ] [ready] **Annotate appendix-only ARCHIVED reports** — 11 archived 2026-08-1x files (authoritative list in archived `2026-08-12_20-52` §b.1/§b.2)
- [ ] [ready] **AGENTS.md compression session** — ~263KB now (grew ~180KB in August); dedicated session: move incident narratives to gotchas-archive, keep enduring rules
- [ ] [ready] **Second-pass annotation of the PARTIALLY-annotated August reports (~30 files in docs/status/ still carrying unstruck items)** — the 2026-09-14 pass cleared ALL fully-unannotated 2026-08-20→08-31 files (41: unharvested items consolidated into TODO_LIST, 15 stale claims annotated inline, all 38+11 archived) after the 2026-09-06 pass cleared September; what remains is the per-file verification of the 08-31 sweep's partially-annotated remainder. Predecessor sweep stance: `docs/status/2026-08-31_22-57_*` §b.1/§g.2
- [ ] [ready] **Triage `docs/planning/` remaining UNDATED files** (go-flake-template-reference.md, NIX-COLORS-INTEGRATION-RESEARCH.md, POCKET-ID-DECLARATIVE-PLAN.md) — the 2026-09-21 docs-health pass triaged ALL 33 dated (2026-05→2026-09) planning docs: 21 annotated + archived to `docs/planning/archived/`, 1 OPEN kept in place (less-code-more-system, items routed to pipeline.md), 7 REFERENCE kept (dozzle/rustfs/go-cache research, brainstorming, btrfs research), 2 operations runbooks archived. **Source:** 2026-09-21 docs-health sweep
- [ ] [ready] **gotchas-archive narratives missing** — foreign-NixOS `/etc` symlink escape, tar `--one-file-system` ZFS, WDT 08-11 chain, nix-daemon oomd chain, hermes outage, HaGeZi GitHub-lock
- [ ] [ready] **Verify docs/CONTRIBUTING + docs/DOMAIN_LANGUAGE freshness** (README + FEATURES updated by the 2026-08-31 docs-health audit)
- [ ] [ready] **Deploy.sh backup retention** — timestamped `.bak` files; keep last 3

## Backlog (untriaged harvest)

- [ ] [ready] **Auto-commit daemon vs queue-task commit race: adopt single-command pathspec commit + immediate `git log -1` verify (or a daemon-side skip for active-task files)** — three of five window sessions were daemon-swept mid-commit; all recovered by amend, each one gamble away from sweeping foreign files into "their" commit. **Source:** 05-32 §e.3, 05-56 §d.1/§e.5, window report §d.2
- [ ] [ready] **AGENTS DORMANT/DISABLED banner sweep + confirmation-wording convention** — audit sections describing ships-disabled services without a status banner (monitor365 + google-sync have one; the google-sync one lived 11 days); adopt "confirmed <date> (doc-finding | live-probe)" wording so confirmation claims state their kind. **Source:** 05-56 §e.1-2/§f.24-25
- [ ] [ready] **Sanctioned read-only host-state probe for agent sessions** (e.g. an allowlisted `scripts/host-unit-inventory.sh`) — `systemctl list-unit-files` is sandbox-blocked, so deployed-state claims (DORMANT/live) stay doc-inferred. **Source:** 05-56 §e.3/§f.26
- [ ] [ready] **AGENTS.md size diet: split per-service runbooks fully into `docs/services/` with 3-line pointers** — the crush/Hermes/InboxClean/fastflowlm sections dwarf the architecture core; fresh-session context cost keeps growing. **Source:** 05-32 §f.34
- [ ] [blocked:user] **Run the next deploy — three verified-but-un deployed changes are stacked on master** — hermes restart-safe cron dispatch (the 1,302-error fix), pool-smart-metrics collector + Gatus checks, btrfs-rescue tier; all committed, build-verified, VM-tested where applicable, none live. **Source:** window report §a; CHANGELOG [Unreleased]
- [ ] [ready] **Fresh uncached `checks.x86_64-linux.hermes` VM run** in a low-pressure window — converts the store-cached green into untainted go-live evidence for the cron fix. **Source:** 2026-09-12_00-42 §b.2/§f.9
- [ ] [ready] **Verify-before-harvest rule in docs-health HARVEST** — any "X red since DATE" claim carried into TODO_LIST must cite a re-run of the specific check IN that pass, or be tagged `unverified-carry-forward`; this is the class that manufactured the Paperless email phantom task (3 passes) and carried the attic "deterministic RED" for 9 days (falsified 2026-09-13). **Source:** docs/status/archived/2026-09-12_00-59 §e.1/§f.1 + 2026-09-13_00-19 §a.5
- [ ] [ready] **Harvest resolution-grep** — before dispatching/re-queuing any item, grep `docs/status/` (incl. `archived/`) for later resolutions of the subject; the 05-15 PASS sat five hours newer than the cited source report. **Source:** docs/status/archived/2026-09-12_00-59 §e.3
- [ ] [ready] **Pre-commit fast path for docs-only diffs** — gitleaks + fast guards + `nix flake check --no-build`; the full VM-building check only for code diffs; ends the normalized `--no-verify` habit (which also skips gitleaks) that the deterministic-red attic check forced (now unblocked — attic green re-verified 2026-09-13). **Source:** docs/status/archived/2026-09-12_00-59 §e.2 + 2026-09-13_00-19 §b.2
- [ ] [ready] **Audit recent `--no-verify` commits for skipped-guard exposure** — quantify what the habit let through (gitleaks sweep over those commits). **Source:** docs/status/archived/2026-09-12_00-59 §f.5
- [ ] [ready] **Human-capability tag at task enqueue** — tasks whose minimal execution needs a browser login + sudo + external console (InboxClean re-consent class) get tagged HUMAN and never reach agent pools; one session was spent discovering what a tag says instantly. **Source:** docs/status/archived/2026-09-12_04-13 §d.1
- [ ] [ready] **Task-queue dispatcher guard: grep newer status docs before dispatching trace items** (same subject-search as the harvest rule, dispatcher-side). **Source:** docs/status/archived/2026-09-12_00-59 §f.7
- [ ] [ready] **Document the amend-on-daemon-race repair as the standard queue-worker commit procedure** in docs/services/tq.md (commit immediately after edit; on "nothing to commit", verify HEAD contains only your change, amend with the footer). **Source:** docs/status/archived/2026-09-12_00-59 §e.5, 2026-09-12_00-42 §e.3
- [ ] [ready] **tq runbook: minimum verification gate for no-code-change closures** — `nix flake check --no-build` + a live-system check of the claim, documented as mandatory. **Source:** docs/status/archived/2026-09-12_00-59 §f.10
- [ ] [ready] **Task reports: "verified live this pass: yes/no" column per item** — makes carry-forward vs fresh evidence visible at a glance. **Source:** docs/status/archived/2026-09-12_00-59 §f.9
- [ ] [ready] **Queue-footer discipline: each run's FIRST commit carries its Task-Queue-ID footer** — heuristic daemon commits cannot, so the queue↔git cross-reference silently never existed for landing sessions (backfill works but leaves an audit hole). **Source:** docs/status/2026-09-12_00-42 §e.3/§f.17
- [ ] [ready] **Sweep remaining TODO rows phrased "red on every deploy"/"red since DATE" for unverified premises** — start with the flm smoke probe and Pocket ID SQLITE_BUSY rows from the same 05-09 source report. **Source:** docs/status/archived/2026-09-12_00-59 §f.11
- [ ] [ready] **Reword smoke FAIL strings to name the probed artifact** — the paperless FAIL pointed investigators at the config for 3 days while the bug was the check's own file path; sweep all `post-deploy-check.sh` `report_fail` strings for path/URL nameability. **Source:** docs/status/archived/2026-09-12_00-59 §e.4/§f.14-15
- [ ] [watch] **One FULL post-deploy-check run as the new green baseline after the owed reboot** — expect paperless PASS persists, flm probe green, Pocket ID SQLITE_BUSY re-checked. **Source:** docs/status/archived/2026-09-12_00-59 §b.1/§f.18
- [ ] [ready] **Eval-time assertion: every smoke-grep target path in post-deploy-check.sh exists in the deployed closure shape** (unit file vs config file — the phantom-RED root cause). **Source:** docs/status/archived/2026-09-12_00-59 §f.32
- [ ] [ready] **Consolidate `docs/status/archived/` (571 files, Jan-2026 era) into `docs/status/archived/`** and fix inbound references — two archive dirs is a split brain. **Source:** docs/status/archived/2026-09-12_00-59 §f.30
- [ ] [ready] __Archive-sweep continuation: reference-check the remaining docs/status/2026-_ reports and archive fully-done unreferenced ones_* — this pass moved 4 (03-54, 04-13, 04-37, 06-20 window reports); 00-42 and 00-59 stay until nothing cites them. **Source:** window report §docs-health
- [ ] [ready] **Document agent-safe verification patterns in docs/CONTRIBUTING.md** — file greps of deployed unit files vs sandbox-banned systemctl (the smoke-reproduction pattern this window used). **Source:** docs/status/archived/2026-09-12_00-59 §f.31
- [ ] [ready] **`stdenv.isDarwin`/`isLinux` deprecation-warning sweep** — they spam every eval/build output; find the source modules, switch to `stdenv.hostPlatform.is*`. **Source:** docs/status/2026-09-12_00-42 §f.16
- [ ] [ready] **flake.lock node-printer helper** — tiny script printing the node for input X while handling the string-root quirk (`nodes["root"].inputs`, never bare `nodes[<input>]`). **Source:** docs/status/2026-09-12_00-42 §d.1/§f.18
- [ ] [decision] **Deploy-timing decision (user): the hermes cron fix, pool-smart metrics, and btrfs-rescue all await `nix run .#deploy` — ride the same window as the owed :52626-corpse reboot (reboot → deploy → verify) or deploy separately first? **Source:** window report §g.1** — BLOCKED: when should the next deploy run, and bundled with the reboot or standalone?
- [ ] [decision] **Go-live evidence policy (user): is a store-cached VM-test green acceptable go-live evidence for the Hermes cron fix, or do you want one fresh uncached `checks.x86_64-linux.hermes` run before deploying? **Source:** 2026-09-12_00-42 §g.2** — BLOCKED: cached-green OK, or fresh-run required?
- [ ] [blocked:user] Deploy the stacked verified work (signoz-coverage dnsblockd flip + ratchet, Hermes cron fix, pool-smart metrics, btrfs-rescue tier, /data T05 scrub gate) and confirm `signoz_traces_upstream_gaps` drops 5→4 live post-switch. **Source:** docs/status/2026-09-13_02-52 §b.2 + 2026-09-13_04-00 §c.1
- [ ] [decision] Evidence durability for the attic closure: gcroot the two VM-test outputs (`wnid5dhy…`, `8l337j29…`) or copy the inner driver ATerms (`pazpdhpa…`/`y865lzkn…`) into `docs/evidence/` — today the re-audit chain dies at the next GC cycles. — BLOCKED: owner policy decision on whether closed-row evidence must be made durable (see question in the 04-00 report §g.1)
- [ ] [ready] AGENTS.md pin-claim gate: any "the lock pinned X at time T" sentence gets `git show <rev>:flake.lock` BEFORE writing (the `e5bdc4a` memory-sourced pin error cost a review round-trip). **Source:** docs/status/2026-09-13_02-29 §e.3 + 03-14 §e.1
- [ ] [ready] AGENTS.md daemon-race commit protocol: write → commit immediately post-verify; on a daemon sweep, `git show --stat` exclusivity check BEFORE any message-amend (3 of 5 window runs raced; the first attic run lost its footer entirely). Consider excluding `docs/status/` + TODO_LIST from the daemon's heuristic sweep for active queue sessions. **Source:** docs/status/2026-09-13_02-29 §e.5 + 04-00 §d.4
- [ ] [ready] Evidence-provenance convention for closure reports: every identity/equivalence claim names the artifact actually read (top-level env / inner ATerm / closure) and its scope; drv-equivalence arguments start with the ATerm diff (`nix derivation show`), never a build. Record the test-driver drv anatomy facts (inputSrcs empty; machine systems behind driverConfiguration.json; ATerms survive closure GC). **Source:** docs/status/2026-09-13_02-29 §e.1-2/§f.4-5/14
- [ ] [ready] Grep tripwire (pre-commit docs path or CI) for unprovenanced "byte-identical"/"semantically identical"/"identical inputs" claims in docs/status without an adjacent diff transcript or provenance note. **Source:** docs/status/2026-09-13_02-29 §e.6/§f.6
- [ ] [ready] Duplicate-dispatch detection: grep `docs/status/` for the finding's distinctive quote before starting any review-fix dispatch (the pin-fix finding was dispatched twice ~45 min apart; the second run's fix was 95% done). **Source:** docs/status/2026-09-13_03-14 §e.4
- [ ] [ready] Standardize the "goes live with the next deploy" suffix on every config-landing TODO row (Hermes row precedent; the signoz flip needed it too). **Source:** docs/status/2026-09-13_02-52 §e.5/§f.20
- [ ] [ready] Run the FULL `nix flake check` (all 63 checks, with builds) once off-peak — the known-red attic excuse is gone; confirm the pre-commit hook path is green end-to-end and no OTHER check is silently red (one-off result table). **Source:** docs/status/2026-09-13_02-29 §f.12/23
- [ ] [watch] Re-run the verbatim old-drv attic rebuild (`aqs52r76…`) once on a quiet machine to re-confirm the falsification chain post-reboot. **Source:** docs/status/2026-09-13_02-29 §f.11/13
- [ ] [ready] `tq dlq` triage of the dead-lettered CV (`cmd/cv` build verify) and go-taskqueue (`cmd/tq` race/gofmt verify) tasks observed in the shared-queue journal 2026-09-13 02:40–03:43. **Source:** tq facts journal + 04-00 report §d.6
- [ ] [ready] Annotate the 02-52 signoz-flip report with the dnsblockd `/health` resolution (its §c.7/§g.2 flagged the stale AGENTS claim; the fix is verified pushed AND deployed). **Source:** docs/status/2026-09-13_02-52 §c.7 + AGENTS.md dnsblockd section
- [ ] [ready] Sweep status docs for other memory-sourced pin claims ("pin at the red", "the lock pinned") and spot-check against `git show <rev>:flake.lock`. **Source:** docs/status/2026-09-13_02-29 §f.3
- [ ] [decision] Decide whether archived status reports may be HARVEST-carried without a fresh re-run — both phantom rows (paperless, attic) were carried from archived docs. **Source:** docs/status/2026-09-13_02-29 §f.27
- [ ] [ready] NEVER-paste-full-SHA convention for evidence tables: abbreviated in tree, full form recoverable via the cited command — generalize the style applied in the 00-19 report row 8. **Source:** docs/status/2026-09-13_03-14 §f.23
- [ ] [ready] When annotating existing reports, prefer ONE consolidated annotation block per finding over scattered edits — adopt as docs-health convention. **Source:** docs/status/2026-09-13_03-14 §f.22
- [ ] [watch] Verify the attic falsification greens survive the next nixpkgs re-lock (drv name in the corrected doc becomes historical — point-in-time, no action unless a future session tries to "fix" it). **Source:** docs/status/2026-09-13_02-29 §f.24
- [ ] [ready] Record the `nix derivation show` JSON shape facts for this box (object keyed by BARE drv filename; this box's jq lacks `-S`) in AGENTS.md Shell section. **Source:** docs/status/2026-09-13_02-29 §e.7/§f.15
- [ ] [decision] OWNER QUESTION: correct-in-place vs append-only for published status reports — which is canonical for live docs and archived docs carrying falsified claims? (decides the CORRECTED-marker pattern used twice this window) — BLOCKED: repo-owner convention decision (report §g.2)
- [ ] [blocked:user] Deploy (`nix run .#deploy`) so the fixed website-deploy-monitor + disk-growth-check scripts go live — the deployed units still carry the dead guards and can fail on the next network blip; post-deploy, clear/re-prove the monitor's state-file dedup (`~/.local/state/website-deploy-monitor/last-alerted-built-at`) so the sentinel alert path re-proves. **Source:** docs/status/2026-09-13_03-40 §b.3/§f.1/§f.15 **UPDATE 2026-09-15:** deploy ATTEMPTED — pre-deploy gate BLOCKED it on the parallel session's in-flight hot-db work (`hot-db-bootstrap.after` carrying a lambda; eval red). System untouched (gate aborted pre-build). Re-run when their registry/hot-db migration lands and `nix flake check --no-build` is green. Post-deploy steps (state-file re-prove, §10 e2e loan) ride on this row.
- [ ] [decision] Git-identity follow-through: user rewrite-vs-leave decision (recommendation: mailmap sweep, NOT rewrite), then per-repo `.mailmap` across the top-10 cohort repos (go-cqrs-lite, monitor365, BuildFlow, PapDashboard, go-workflow-auditlog, samber-do-auditlog, CV, DiscordSync, cqrs-htmx, erraudit); identity-invention guard (pre-commit/CI rejecting local `user.*` overrides differing from canonical + non-allowlisted authors); fix dnsblockd's empty-email local override; decide SystemNix's own non-canonical noreply override. — BLOCKED: the rewrite-vs-leave decision is the owner's (analysis in docs/status/2026-09-14_00-00_rogue-git-identity-audit-and-declarative-identity.md §3)
- [ ] [watch] E2E-verify the §10 auto-derived loan on the next real deploy (confirm `systemctl cat gatus` resolves + monitor365 trio auto-loans; then retire the expectation); re-probe `:8085/metrics` for `discordsync_projection_dlq_legacy_unchanged` live (the sweep relied on the recorded 08-31 retirement). **Source:** docs/status/2026-09-13_04-11 §b.2-3
- [ ] [decision] OWNER QUESTION: git history disposition for the foreign-identity cohorts (~5.2k Unknown Author, ~900 Claude, 3 Crush identities across ~100 repos) — rewrite, mailmap sweep (recommended), or leave? — BLOCKED: analysis + recommendation in docs/status/2026-09-14_00-00_rogue-git-identity-audit-and-declarative-identity.md §3; decision changes ~100 repos
- [ ] [ready] **`systemd-analyze verify` sweep over ALL unit files** — file the remaining warnings as tasks (this pass only sampled; found tq startLimit + PMA Environment + pocket-id ExecStartPost race + cups legacy path). **Source:** `2026-09-14_16-34` §f.9-10/28-30
- [ ] [ready] **deploy.sh generic "stopped-for-containment" post-switch stop-list** — generalize the flm corpse-guard pattern to llama-embeddings/llama-reranker (any unit whose journal shows the wedge signatures gets re-stopped after stc's target cascade re-arms it); alternative: declarative `services.llama-rag.enable = false` while the 20260911 llama.cpp regression is unresolved (owner decision — RAG-dark tradeoff). **Source:** `2026-09-14_13-54` §f.1-2/10, `12-06` §f.8/12
- [ ] [ready] **Eval-guard follow-ups (from the 17-49 session's P0/P1)** — (1) `system-health.monitoredServices` coverage audit (enabled custom services must be registered); (2) ReadWritePaths-under-`/mnt|/data`-without-mount-gating audit (226 class, allowlist, design sketched); (3) deploy.sh provisioner-restart-list lint (`*-storage-dir`/`*-provision` oneshots must be in the restart list); (4) oneshot+`RemainAfterExit`+`restartTriggers` eval warning; (5) port-audit extensions (Caddy vHosts, gatus URLs, mkDockerService mappings, `word:NNN` boundary class) + retire/point the legacy CI port-grep; (6) `harden` throw negative tests for the remaining blocklisted keys; (7) allowlist-justification-comment lint; (8) CONTRIBUTING audit-module template + pass-branch-derivation trap doc; (9) deploy evo-x2 to bank a clean activation (eval-only guards, zero runtime delta expected). **Source:** `2026-09-14_17-49` §f.2-18/41-45, §g.1-3
- [ ] [ready] **AGENTS prevention-table sweep** — every gotcha bullet names its enforcing guard or "UNGUARDED" (the table is now the proactive-guard backlog; 6 guards landed 2026-09-14, the rest of the gap list lives in the 17-49 report §f.19-40). **Source:** `2026-09-14_17-49` §e.1/§f.42
- [ ] [blocked:user] **Git remote-ref reconciliation (from the corpse-pile triage; user-owned unless noted)** — merge-or-rebase decision for the 70/43 fork; identify the pushing clone; `git fsck --full`; forensics on the 00:54 ref deletion (tq journals, shell history); set `logAllRefUpdates`; clean/restore `forgejo-hermes-agent` (tracks a deleted remote branch); add the remote-ref canary. **Source:** `2026-09-14_09-31` §f.7-14
- [ ] [ready] **Stop flm/guard `/tmp/z6*` probe leftovers** + probe-file hygiene convention (probes to /tmp or docs/status scratch, never repo root — the daemon commits them). **Source:** `2026-09-14_14-57` §f.10, `17-49` §d.5/§f.47
- [ ] [decision] **OWNER QUESTIONS (batch): (1) deploy now or batch (eval-time guards are zero-runtime-delta; bank-sync/immich/paperless still down since the 13:35 boot until the deploy's converge runs); (2) audit-allowlist placement doctrine (adjacent-to-unit vs centralized in configuration.nix); (3) legacy CI port-grep: delete or keep for non-unit source forms; (4) llama-rag: pin llama-cpp back to 20260905-era vs bisect upstream vs config-disable; (5) git fork 70/43 disposition. **Source:** `2026-09-14_17-49` §g, `16-34` §g, `11-19` §g.1, `09-31` §f.7
- [ ] [ready] **tmp-cleanup prevention depth** — extend `tmp-cleaner-audit` to systemd USER services/HM units/raw-text cleaners (system units only today), cross-unit tmp-ENOENT journal counter + Gatus check (the forgejo 12-day outage's true detection gap), `systemd-private-*` dir-count gauge + floor assertion, `verify-guards` flake app running mutation evals for module self-assertions. **Source:** `archived/2026-08-31_23-55_*` §c/§e
- [ ] [ready] **Eval-guard additions (extends the 17-49 eval-guard row above)** — start-limit-audit CI regression test, `Restart=no` eval assertion on timer-driven oneshots, bare-module-shape flake check (the signoz-coverage wrapper class), sops mode ≥ 0444 audit for user-unit-read secrets, eval assertion: Caddy must not order behind OIDC-gated consumers, per-disk presence gauges, login-chain smoke canary (`fish --login -c true` < 5 s), fish conf.d lint against unbounded `/mnt/*` FS probes, generate `validate-gomemlimit` SERVICES list from `nix eval` + drop its stale `discordsync:8085`/`signoz-collector:8888` no-go_memstats entries + justify browser-history GOMEMLIMIT (384MiB vs 5MiB live heap) + dedupe WARN-DIVERGED rows. **Source:** `archived/2026-08-22_06-12_*` §f, `08-55_*` §f, `05-30_priority4-*` §e/§f, `2026-08-24_08-00_sddm-*` §f, `2026-08-31_23-59_*` §f
- [ ] [ready] **Convention: DONE-stamped TODO items carry the completing task's Task-Queue-ID** — so verifying sessions skip daemon-commit archaeology. **Source:** window report §e.3
- [ ] [decision] Adopt "Task-Queue-ID in DONE stamps" + a daemon footer convention so auto-commit provenance stays greppable (the PSI fix landed in unlabeled `f0e3c493`)? — BLOCKED: needs owner decision (repo policy + upstream go-taskqueue change).
- [ ] [decision] Deploy Zone 6 + post-deploy metric probe NOW, or hold for the /nix soak window (~2026-09-17) alongside crush-hot-db? — BLOCKED: needs owner deploy authorization (sudo-gated).
- [ ] [ready] **Closeout/task runs verify the footer commit landed before emitting TQ_RESULT** — the 06:10 closeout's commit failed (tq fact 3520, exit 1) and only the auto-commit daemon (`47b0585e`, 06:14) carried its report + TODO appends; check `git log -1 --grep Task-Queue-ID` post-commit. **Source:** report §b.3
- [ ] [decision] Redesign the `services.integration` declaration guard to survive an enclosing `mkIf` (repo-wide API change), or keep the co-import convention and patch each VM test? — BLOCKED: needs owner decision (module API design). UPDATE 2026-09-15: the decision now has full evidence — the mkIf-survivable shape is evaluated AND real-module-proven (row 547 + evaluation report); migration cost ~42 modules with a per-entry review constraint (entry fields must reference only own-module options/statics, since the entry value is now merged even when the service is disabled).
- [ ] [watch] If row 551 adopts the mkIf-survivable shape: retire the 12 test co-imports + update the AGENTS co-import rule + fold the probe matrix into `tests/test-integration.nix` as regression cells. **Source:** sixth-run report §f.3
- [ ] [ready] Extend `deploy-restart-audit.nix`: any unit named in deploy.sh's provisioner loop must have non-empty `wantedBy` or an explicit exemption; negative-test via extendModules (eval-cache hand-probe); sweep remaining loop members for the static-unit trap (`tq-bootstrap` first). **Source:** same report §f8
- [ ] [ready] Extend `mount-gating-audit.nix` (or new audit): reject `ReadWritePaths` entries whose unit's own script `mkdir -p`s a descendant (the 226/NAMESPACE class of review round 2). **Source:** same report §f9
- [ ] [ready] Wire the find-based symlink sweep into post-deploy-check.sh so the check survives nested dirs. **Source:** same report §f20
- [ ] [watch] Pre-deploy §10: confirm the new crush/niri metrics' auto-loans retired themselves (watch for stale-loan WARNs). **Source:** same report §f26
- [ ] [ready] Add to CONTRIBUTING.md: the same-task-ID report-discovery rule (`ls docs/status/ | grep <task-id>` + `git log --all --grep=<Task-Queue-ID>` before acting), the "(verify) ⇒ TODO item" rule, and the pressure-aware verification policy (io PSI some avg10 ≥20% ⇒ eval/fmt/fixture only, no VM builds). **Source:** same report §f28
- [ ] [decision] Auto-commit daemon: skip files modified within the last N seconds (it committed a half-written module as `1a94001a` this window); also consider native Task-Queue-ID footer carry for swept commits (ends the queue-ID-remap dance). **Source:** same report §f29-30
- [ ] [blocked:user] Deploy the stacked window fixes (Zone 6 churn re-arm `2fc4db64` + btrbk-data MemoryHigh/OOMScoreAdjust `c51cb4c1` + buildcache reap unit `buildcache.nix`) in a quiet-IO window BEFORE the 2026-09-17 23:00 btrbk-root window — the Sep 19 00:28 `btrfs-verify-pool-backups` freshness FAIL is the deadline. **Source:** docs/status/2026-09-17_03-23_* §b.1
- [ ] [ready] Eval-time assertion: every `btrfs send`-carrying unit declares a MemoryHigh (class guard, harden{}-throw pattern). **Source:** docs/status/2026-09-17_01-05_* §f.5
- [ ] [ready] Pre-deploy §10: WARN when `memory_emergency_guard_churn_units_stopped` is present at deploy time (active churn window → units currently stopped). **Source:** docs/status/2026-09-17_01-52_* §f.26
- [ ] [ready] Add an eval-time/check assertion pinning the buildcache reap-list contents (six names) in both loops so drift or removal is loud. **Source:** docs/status/2026-09-17_02-54_* §f.4
- [ ] [ready] Derive the `buildcache-usb-recovery` step-2.5 reap loop and deploy.sh's pre-switch loop from ONE shared Nix list so they cannot diverge. **Source:** docs/status/2026-09-17_02-54_* §f.8
- [ ] [ready] Codify the "verification-only close" evidence-class convention (runtime vs lock/source vs doc-chain) in `docs/CONTRIBUTING.md`. **Source:** docs/status/2026-09-17_02-20_* §f.12
- [ ] [ready] Fix the AGENTS.md sev1 section `zone1..5_trips_total` vs `zone1..6` inconsistency. **Source:** docs/status/2026-09-17_01-52_* §f.24
- [ ] [ready] Annotate `docs/status/2026-09-17_01-37_task-000001a0ac8110e2*` (duplicate Zone 6 run) and the archived BuildFlow source report §c2/c3 with their close-out pointers. **Source:** docs/status/2026-09-17_03-23_* §f.30
- [ ] [watch] COMMIT MAPPING (2026-09-17 closeout): the auto-commit daemon swept this harvest's files before the footer-carrying commit could land — report = `a37f840a`, TODO+CHANGELOG = `c8117cc2`, this commit carries the Task-Queue-ID cross-reference (queue-ID-remap precedent). **Source:** docs/status/2026-09-17_03-23_*
- [ ] [ready] Consolidated "next deploy must verify" checklist section in TODO_LIST — llama-rag re-enable, hermes `-v` (`Connected as` journal line), crush-hot-db first migration + symlink sweep + PSI baseline, scrub-mechanism fix, cv sops seeding all wait on one sudo-gated deploy; make the post-deploy pass mechanical instead of archaeology. **Source:** docs/status/archived/2026-09-18_02-45_task-000001a0b1d43f78d4325627260af417c7ed.md §e.3
- [ ] [decision] Deploy window for the pending batch (llama-rag re-enable + hermes `-v` + crush-hot-db first migration + scrub-mechanism fix): ASAP or quiet-IO window — BLOCKED: owner call on RAG-dark exposure vs QLC PSI risk (docs/status/2026-09-18_03-45_* §g.3)?
- [ ] [ready] **Pre-commit: classify `path ... is not valid` eval failures as a loud WARN naming the repair command** (mirroring pre-deploy §1's block-attribution) so a present-but-unregistered store path stops reading as a mystery red — the hermes-python-source orphan nuked every explicit commit's flake leg for a day; also settle whether docs-only commits may skip the flake leg when the failure is orphan-class (rides the pre-commit fast-path row). **Source:** docs/status/2026-09-19_00-19_task-000001a0b68a0a38da1c4a597637e0401f85.md §e.3/§f.15
- [ ] [ready] **Nix `--json` deprecation sweep over scripts/ + docs call sites** — nix CLI warns on the legacy `--json` flag usage before a future release hard-errors; grep `--json` across `scripts/*.sh` and the docs' copy-paste commands, switch to the modern per-command JSON forms, and note any that are actually `nix ... --json`-flag-shaped. Carried unfixed through three dispatches of the hermes-orphan family. **Source:** docs/status/archived/2026-09-19_03-17_task-000001a0b6ca1ef75fb4b3487e0969f8a09f.md §f.7
- [ ] [ready] **docs/CONTRIBUTING.md convention: boot-scoped vs window-scoped journal assertions** — three precedent styles exist (boot `--grep`, `--since` windows, freshness mtime); one paragraph stops each session re-deriving the tradeoff (the hermes smoke chose boot-scoped by reasoning alone). **Source:** docs/status/2026-09-18_03-13_task-000001a0b2069a1df77e4c62aef8b0488125.md §e.3/§f.8
- [ ] [ready] **Prune the stale `flake: false` `go-health-dashboard` node from flake.lock** — an old lock node with that name forces the real input to resolve as `go-health-dashboard_4`; after the next deliberate `nix flake update` (NOT mid-parallel-session), verify the lock has one node per input and `nix flake metadata . --json | jq '.locks.nodes | keys'` is clean. **Source:** docs/status/2026-09-19_19-17_health-home-lan-federation-hub-build.md §f.27
- [ ] [ready] **Investigate how parallel-session files landed unformatted (geometrikks.nix + 3 scripts + hot-user-caches.nix committed pre-`nix fmt`)** — nix fmt drift should never reach a commit; determine whether the auto-daemon snapshot window or a missing fmt step is the cause, and gate it. **Source:** docs/status/2026-09-19_19-17_health-home-lan-federation-hub-build.md §f.28
- [ ] [ready] **Daemon-vs-precommit incident filing: the auto-daemon swept staged files mid-hook, so a passing hook validated a tree it had not produced** — file into the fleet lessons with the 2026-09-19 timeline; candidate mitigation: pre-commit hook re-checks `git status` after its steps. **Source:** docs/status/2026-09-19_19-17_health-home-lan-federation-hub-build.md §f.44
- [ ] [ready] **Deploy wrapper: refuse early with a clear message when running inside an agent sandbox (no sudo)** — `nix run .#deploy` wedges on a sudo prompt it can never answer; a capability probe turns a hang into a one-line handoff message pointing at the runbook block. **Source:** docs/status/2026-09-19_19-17_health-home-lan-federation-hub-build.md §f.45
- [ ] [ready] **Smoke-baseline staleness stamps** — annotate each known FAIL in the post-deploy baseline with first-seen date + expected-clearance condition (CV-era fails should already have dropped post-14:08-heal; bank-sync clears at SCA approval; BH 503 clears on first browse; flm clears when the guard restores the socket). The "4 known FAIL(s)" line is wallpaper — stamps make rot visible. **Source:** 2026-09-20 deploy-review session §e.4; baseline lives in the post-deploy-check state file
- [ ] [ready] **FOWNER-chmod eval lint** — flag hardened root units whose Exec*/script text chmods, renames over, or `mv`s onto paths they do not own without `CAP_FOWNER` in the harden call (textfile-collector class; three live incidents: niri.prom 2026-09-04, mail-relay 2026-09-02..06, hot-user-caches 2026-09-20). Same shape as `audit-textfile-tmp.sh`. **Source:** AGENTS textfile-collector gotchas; 2026-09-20 six-unit batch item 2
- [ ] [ready] **Exposition-syntax validator as a flake check or smoke step** — the nrestarts `[not set]` poison survived days of green evals because the bug lived in PERSISTED RUNTIME STATE, not config; a python OpenMetrics exposition parser over every `.prom` in the textfile dir catches the whole-file-rejection class at check time. **Source:** 2026-09-20 deploy-review session §e.2/§e.10 (ad-hoc validator already written there — productize it)
- [ ] [ready] **Repo regression test for the system-health nrestarts integer guards** — the write-site + emit-site guards shipped fixture-tested standalone only; repo doctrine demands a `tests/` fixture (rendered-script path, `[not set]` → 0, real values preserved, poisoned-state heal). **Source:** 2026-09-20 deploy-review session §c; commit 514dba99
- [ ] [ready] **Formatter-exclusion regression guard** — a flake check asserting `docs/**/*.html` is in the EFFECTIVE treefmt excludes (parse the formatter wrapper's rendered config): the 2026-09-21 `nix fmt` override is string-patched and could silently regress; recurrence signature = daemon commits oscillating a docs HTML between ~3.6 MB and ~7.8 MB (200k-line diffs). **Source:** `docs/status/2026-09-21_14-00_superfile-integration-and-treefmt-html-exclusion-status.md` §b.3/§f.4; AGENTS "Big self-contained HTML reports" bullet
- [ ] [ready] **Abstraction helpers from the 2026-05 abstraction review (4 survivors)** — (1) `mkGatusEndpoint` helper (gatus-config.nix endpoint entries are still hand-verbose); (2) consecutive-failure counter helper in `scripts/lib.sh` (recurring pattern across collectors/gates); (3) Caddy vHosts-as-data (only `protectedVHost` exists, not a data-driven vhost list); (4) module-level service self-registration (largely realized by the integration registry + `gatus-coverage-audit.nix` — evaluate whether the residual gap justifies more). **Source:** `docs/planning/2026-05-16_less-code-more-system.md` (the only OPEN survivor of the 2026-09-21 planning triage)
- [ ] [blocked:deploy] **Post-deploy finish of the user-env single-source cleanup** — after the queued deploy (typst into base.nix) activates: `nix profile remove typst` → profile EMPTY, verify `/run/current-system/sw/bin/typst` + `nix profile list` clean, and confirm zero post-deploy smoke regressions from the base.nix addition. BLOCKED: deploy refused by the pressure gate through 2h+ of sibling-session IO load (guard trips_last_hour 4-6; queue log `~/.local/state/systemnix-deploy-queue.log`). **Source:** docs/status/2026-09-23_02-01_binary-shadow-dedup-buildflow-fod-deploy-queue.md §b.1/b.2
- [ ] [ready] **User-env single-source enforcement checks** — machine-enforce the 2026-09-22 doctrine (AGENTS Shell & DevTools): (1) a WARN-grade check that `nix profile list` is empty (as user, or via the queue shell); (2) a shadow detector flagging any `~/go/bin/*` whose name also exists in `/run/current-system/sw/bin` (allowlist `golines` until migrated). Both classes hit live: `buildflow version dev` shadowed the system copy for ~2 days, and a 2026-09-17 session ad-hoc-installed 4 profile tools. **Source:** docs/status/2026-09-23_02-01_binary-shadow-dedup-buildflow-fod-deploy-queue.md §e/§f.5-6
- [ ] [ready] **deploy-queue as a systemd user unit/timer** — tonight's 90-min inline poller died with the session shell (2026-09-19 lesson recurrence); a small `deploy-queue.service` (poll for guard-calm → `nix run .#deploy`, heartbeat to `~/.local/state/`) makes queued deploys survive sessions. **Source:** docs/status/2026-09-23_02-01_binary-shadow-dedup-buildflow-fod-deploy-queue.md §f.10
- [ ] [ready] **deploy.sh pressure gate rebound race** — gate measured PSI=OK, ran minutes of pre-deploy checks, re-measured 28% and aborted; move the final pressure read to immediately before `nh os switch` (or pass a validated drained-condition through). **Source:** docs/status/2026-09-23_02-01_binary-shadow-dedup-buildflow-fod-deploy-queue.md §d.2
- [ ] [ready] **golines → base.nix** — the last deliberate `~/go/bin` resident; migrating it lets GOBIN sit empty and the shadow detector collapse to "GOBIN must be empty". **Source:** docs/status/2026-09-23_02-01_binary-shadow-dedup-buildflow-fod-deploy-queue.md §f.7
- [ ] [ready] **Enumerate the 2026-09-22/23 nixpkgs-move FOD casualties via the existing pre-deploy batch build** — root nixpkgs moved 44a9189→6774f7bc and invalidated prepared-source FODs; TWO known same-night casualties (buildflow + art-dupl, both shimmed downstream). Run the batch build (heavy-job wrapped) to surface any remaining stale vendorHashes before the next deploy night dominoes. **Source:** docs/status/2026-09-23_02-01_binary-shadow-dedup-buildflow-fod-deploy-queue.md §c.2
- [ ] [ready] **Extend `audit-textfile-tmp.sh` class B to `scripts/*.sh` + `docs/**/*.md`** — the class-B `/run/secrets-rendered` scan covers only `.nix` under modules/platforms/tests/lib, so the borg-restore drill + both offsite-borg runbooks shipped the banned literal 2026-09-23 (three sites, reviewer-caught). Add the two file surfaces to the scan (comment-stripping already matches the house convention; docs need no stripping — the only sanctioned mentions there document past incidents, which need an allowlist or prose-pattern guard). **Source:** 2026-09-23 offsite-borg restore-drill reviewer finding; docs/status/2026-09-06_03-39 mail-relay sweep follow-up item 29 (.sh)
- [x] [ready] **Document the nixpkgs override-shape change for probe authors** — inline override attrsets passed to `extendModules` must now be `{ _type = "override"; priority = 50; content = …; }` (inner field `content`, NOT the long-documented `value`; error `attribute 'content' missing` at `lib/modules.nix:1458`, live 2026-09-23), and a PLAIN `enable = false` in configuration.nix forces external probes to priority ≤ 50 (conflicting-definition error otherwise). Both bit the offsite-borg positive-path probe (two failed attempts before green). AGENTS.md gotchas or here. **Source:** docs/status/2026-09-23_10-05_task-000001a0ccdd5cd962579627f522f4c4082d.md §f2/§d — DONE 2026-09-24 (task queue): landed as a gotcha bullet in AGENTS.md (Non-Obvious Gotchas → Nix & Nixpkgs, after the `nix eval …drvPath does NOT check NixOS assertions` bullet); both error shapes (`attribute 'content' missing` at `lib/modules.nix:1458`, `has conflicting definition values … Use lib.mkForce value or lib.mkDefault value`) re-verified live against the locked nixpkgs before writing; working recipe `lib.mkOverride 50 true` (mkForce IS mkOverride 50)
- [ ] [ready] **Fix the pre-existing `checks.disko-layout` eval failure** — `nix flake check --no-build` dies `path '…4xv74d17…-disko-samsung-tlc-vm.nix.drv' is not valid` (live 2026-09-24, proven pre-existing via worktree baseline at pre-change HEAD). Symptom class matches the AGENTS.md "eval-time code forces realization of a package output" entry after nixpkgs churn. **Consequence (CORRECTED 2026-09-25 06:40): the earlier "every commit needs `--no-verify`" reading was a FALSE PREMISE — the gate deaths are intermittent-environmental, not a standing dead gate (docs/status/2026-09-24_06-03_task-000001a0d1898ba3e48695371bc62baac77d.md §d1 flagged this exact bypass-teaching class); re-run `nix flake check --no-build` before ANY bypass.** Classify (stale store drv vs eval-time output coercion) → fix root cause → add the eval-guard negative test per repo doctrine → confirm CI (`nix-check.yml`) recovers. **Source:** docs/status/2026-09-24_01-27_task-000001a0d08c5ae1231c46d370f5b7d55560.md §d1/§f1. UPDATE 2026-09-25 (fire-3 self-review, fresh same-session run at clean HEAD `1e0f6be7`, rc=1): full set enumerated via `--keep-going` — FOUR failures, THREE shapes: (1) `formatter.x86_64-linux` → `treefmt.drv is not valid`; (2) `checks.hermes` → `ix80s6aj697xcrzlnlc074wz6zd0avf4-source is not valid`; (3) `checks.disko-layout` → `getting attributes of path "/run/binfmt": No such file or directory` (an eval-time HOST-FS probe, NOT the drv shape the row was opened with — the error shape CHANGED since 2026-09-24); (4) `nixosConfigurations.evo-x2` assertions → the SAME `ix80s6aj…` source as (2) (shared input). Fix lead: (2)+(4) share one store object — probe the daemon stale-fetch-cache class (`sudo systemctl restart nix-daemon`, re-run) BEFORE eval surgery; (3) is immune to store repair. CI note: the dead treefmt drv means `nix fmt --ci` (the CI fmt arbiter) rides the same broken formatter check. Full narrative: docs/status/2026-09-25_01-02_task-000001a0d56f35ccee2a991988d7c3a9ebd1.md. UPDATE 2026-09-25 05:33: full `nix flake check --no-build` GREEN at clean HEAD `1a4952df` — no repo change landed; `/run/binfmt` reappeared 05:32 (environmental heal, actor unknown, boot-ephemeral until the durable fix lands); all four failures plausibly shared the environmental root — re-probe at the next boot before eval surgery. UPDATE 2026-09-25 06:40 (this row's fix dispatch): gate re-verified GREEN at HEAD `97711fdd` (full `nix flake check --no-build --keep-going`, rc=0 "all checks passed"). Root cause CONFIRMED = the `hot-user-caches-nix-bootstrap` ↔ `systemd-tmpfiles-setup` boot-ordering cycle (stability row, TODO_LIST 109), NOT a repo defect — no eval surgery warranted. Durable fixes LANDED IN-TREE 2026-09-25, all UNDEPLOYED (host still boots gen 797, whose `/run/current-system` unit text carries the OLD automount-anchored wiring, read 06:37): `af87ce31` 03:10 (`hot-user-caches.nix` wantedBy/before moved to the on-demand `.mount` + `tests/test-hot-user-caches.nix` boot tripwire asserting tmpfiles-setup active + a probe rule actually applied) and `d827f478` 04:28 (`binfmt-sandbox-dir` mkdir belt in `boot.nix`). RESIDUAL (why the row stays open): post-reboot proof — needs an owner deploy carrying these fixes + a reboot; a reboot on gen 797 re-manifests the cycle. Re-check one-liner: `ls /run/binfmt && nix flake check --no-build`. CI LEG ANSWERED 2026-09-25 06:55 (read-only `gh run list/view`, run 36094651758 on the last push): CI is RED but NOT because of this row's environmental gate — the failures are the ALREADY-TRACKED stack owned by the "Is GitHub CI live signal or known-dark" owner row (this file's tail): `nix-check` dies at the repo-wide `statix check .` step (≥6 findings: architecture-catalog.nix:184/185, flake.nix:1489, test-disko-layout.nix:49, test-restic-app-dumps.nix:27, project-discovery-daemon.nix:162 + more — the local pre-commit hook lints STAGED files only, so daemon commits accumulate repo-wide statix debt invisibly) and the `vm-tests` jobs fail `Failed to fetch git repository 'ssh://git@github.com/LarsArtmann/branching-flow'` (private-input class). No new work filed here — the fix is owned by that row; this row's gate claim stays LOCAL-gate-scoped.
- [ ] [ready] **Eval-Time Guards doctrine note in docs/CONTRIBUTING.md** — state WHICH negative-probe pattern a guard supports: inline deepSeq inside the check attr (offsite-borg-positive-render, disko-samsung-tlc) is only probeable via a REPLICATED assert against a drifted extendModules render (proves the render is read, not that the committed guard executes on drift); guards extracted into shared pure lib functions are directly probe-drivable. New guard authors currently rediscover this per incident. **Source:** docs/status/2026-09-24_05-05_task-000001a0d13bb97f785bd705387c539b4235.md §e4/§f9
- [ ] [ready] **Queue dedup preflight so closeout/review-fix dispatches stop re-firing on already-closed work** — the harvester should skip items whose queue row is `[x]` AND whose Task-Queue-ID footer commit already exists in `git log` AND whose `docs/status/*_task-<ID>.md` report already exists (any one is a done signal; today the queue re-fired 4 task IDs 7 times in the 2026-09-23/24 window — every re-dispatch a verification-only session with zero code). Related queue rows: go-taskqueue work-claiming/dedup (upstream), done-signal semantics (upstream). UPDATE 2026-09-25: a 5th re-fire (task 000001a0d56f35ccee2a991988d7c3a9ebd1) was NOT verification-only — it caught the authoring session's un-harvested §f follow-ups (5 items landed in e8bd1383) that a naive skip-preflight would have skipped forever; the done-signal therefore needs a "follow-ups harvested" leg, or status reports must self-harvest §f at authoring time (convention candidate, see that report §e.1). **Source:** docs/status/2026-09-24_06-45_window-closeout-offsite-borg-gates-five-task-window.md §d1, docs/status/2026-09-25_00-39_task-000001a0d56f35ccee2a991988d7c3a9ebd1.md, TODO_LIST row "Queue dedup preflight"
- [ ] [ready] **Codify "status reports self-harvest §f at authoring time" as one sentence in the AGENTS.md TODO-system section** — the root cause of this task's re-dispatch churn (task 000001a0d56f35ccee2a991988d7c3a9ebd1 fired 3×: each fire re-did the previous session's un-landed §f harvest, one iteration smaller; the third fire completed the second's f2/f3, which had repeated the exact pattern it diagnosed); a self-harvest norm also makes the naive skip-preflight above safe. **Source:** docs/status/2026-09-25_00-39_task-000001a0d56f35ccee2a991988d7c3a9ebd1.md §e.1/§f2
- [ ] [ready] **Codify the re-dispatch verification protocol in docs/CONTRIBUTING.md (next to the Eval-Time Guards doctrine; cross-project variant → crush-config `references/lessons.md`)** — when a queue item arrives already done: (1) verify footer commits + queue-surface closure, (2) spot-check ONE load-bearing claim of the landed work live, (3) sweep the Source report's §f for un-landed follow-ups, (4) land SOMETHING footer-bearing so the dispatch is attributable. Steps 2-3 are what separate a useful re-fire from dead queue churn — the 00-39 re-fire proved it by finding 5 un-landed follow-ups behind a closed `[x]` mark. **Source:** docs/status/2026-09-25_00-39_task-000001a0d56f35ccee2a991988d7c3a9ebd1.md §e.3/§f3
- [ ] [ready] **§11 input-hygiene ghost probe: verify the probe list derives from the live lock** — monitor365 was removed from the flake `packages` surface 2026-09-15 (its prepared source is permanently unbuildable), yet the CI "Flake input hygiene" step still probes `monitor365.goModules`; confirm the probe list is lock-derived (not a stale hand list) and drop/clean the ghost entry. **Source:** docs/status/2026-09-24_03-14_task-000001a0d0d1f8b552f5e3c0ea6cab70a19e.md §f38
- [ ] [ready] **Commit-msg hook leg: reject commit subjects >72 chars** — the house commit contract caps the first line at 72; nothing enforces it and a 78-char subject shipped this window (caught only by the authoring session's own self-review, repaired by amend-forward while still unpushed HEAD). Add a one-line length check to `.githooks/commit-msg` (message checks belong in commit-msg, not pre-commit) alongside the existing gitleaks/whitespace legs. **Source:** docs/status/2026-09-25_02-20_task-000001a0d5af4c01977b1f874e6689edcaae.md §d1
- [ ] [ready] **Codify the daemon-race policy for task-queue agents** — the 2026-09-12 report's g-2 (when the auto-commit daemon sweeps your edit before your commit: amend the daemon HEAD after verifying it contains exactly your single logical change, vs leave it and land a separate footer-bearing commit on top) has NO written answer and recurred 2026-09-25 — the recipe landing landed as 2 heuristic daemon commits + 1 footer-bearing status-report commit, landing-on-top chosen by private judgment. The policy must also reconcile the docs' "update every surface in ONE commit" rules with the ~10-min daemon cadence (explicit carve-out or a pre-stage-and-commit-fast discipline). CONCRETE CONSUMER (attempt-3 addition): jan.md's "Single-victim /data repair recipe" step 3 literally says "in ONE commit" and the recipe's own authoring run needed 3 commits under the daemon — when this policy lands, reword that step (or carve it out) and update jan.md in the SAME change. **Source:** docs/status/2026-09-25_05-19_task-000001a0d68672297273301d23247ceeae6a.md §d1/§e4 + archived 2026-09-12_03-54 §g2 + docs/status/2026-09-25_05-33_task-000001a0d68672297273301d23247ceeae6a.md §d4/§e7
- [ ] [ready] **Classify tq verify failures before they burn task attempts** — three shapes need different routing: (1) pre-existing/environmental (gate dead at the PRE-attempt rev, e.g. the 05:29 `/run/binfmt` death) → gate-dead holding state + alert, NOT task failure; (2) deadline-timeout-with-progress (`context deadline exceeded` with exit_code 0 and evaluation advancing — tq fact 6663: repair-recipe attempt 2 killed 30 min after claim, mid-`checks.lsblk-column-names`, under ~50-56% IO PSI) → gate-slow, retry with a warm eval cache, NOT task failure; (3) introduced (fails where the pre-attempt baseline passed) → the ONLY shape that counts against the task. With maxAttempts=3, one more environmental/slow cycle DLQs a DONE task — the repair-recipe item was complete since attempt 1 and burned attempts 2-3 on gate failures alone. **Source:** docs/status/2026-09-25_05-33_task-000001a0d68672297273301d23247ceeae6a.md §e2 + tq facts log (task 000001a0d68672297273301d23247ceeae6a)
- [ ] [ready] **Verify-gate triage runbook paragraph in docs/CONTRIBUTING.md** — "verify failing across unrelated tasks? probe, in order: `/run/binfmt` exists → `nix flake check --no-build` at clean HEAD (pre-existing vs introduced) → nix-daemon restart (stale-fetch class) → IO pressure vs the queue's verify budget (30 min measured 2026-09-25; a cold eval cache under an IO storm exceeds it) → only then suspect the task." Pairs with the classification row above. **Source:** docs/status/2026-09-25_05-33_task-000001a0d68672297273301d23247ceeae6a.md §e4 + attempt-3 report
