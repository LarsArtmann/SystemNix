# Status Report — Task 000001a08ef20c1a4e464fb20c81f52bd3c5

**Date:** 2026-09-11 08:02 CEST
**Task (TODO_LIST P0):** Reboot into kernel 7.2.2 + FastFlowLM v1.0.3 retry (USER-gated)
**Commit:** this commit (status report + TODO row rewrite + AGENTS flm-section updates)
**Scope note:** per dispatch, covers only this task; the repo-side pre-work it found already staged (the 07:37 `pkgs/fastflowlm.nix` bump from a parallel session) was verified, not re-authored.

---

## TL;DR

The task's core action — a session-killing reboot — is **still required and still USER-gated**: the 2026-09-05/07 reboots happened, but the v1.0.2 prefill-crash class struck again on the current (2026-09-07, kernel 7.2.3) boot and left a NEW unreapable zombie holding :52626. Everything agent-executable around it is now done and verified: the staged v1.0.3 package **builds clean**, the **kernel-7.2.2 premise is falsified** (amdxdna is byte-identical v7.2→v7.2.3), the post-reboot runbook is written into the TODO row, and the upstream issue is **deliberately NOT filed** (fails verify-before-filing Gates 1+4). Row left unchecked with a BLOCKED tail.

## What was verified live (2026-09-11 ~07:45-08:00)

| # | Finding | Evidence |
|---|---------|----------|
| 1 | Boot prerequisite met long ago — kernel **7.2.3**, up since 2026-09-07 ~14:10 | `uname -r`; uptime ≈3.73 d |
| 2 | **Wedge RECURRED this boot**: `flm-real` PID 443303 = `Zsl <defunct>`, PPid 1, **2 tasks**; `:52626` LISTEN (uid 1000) + CLOSE_WAIT backlog in `/proc/net/tcp` | The exact AGENTS corpse signature, live |
| 3 | Every `fastflowlm.service` start dies `bind: Address already in use` → start-limit-hit, re-armed by client requests + the memory-emergency-guard restore | journal 2026-09-11 07:48-07:53 (`-u fastflowlm`) |
| 4 | llama-embeddings + llama-reranker are **UP** (2 llama-server procs, R state) — only flm consumers (PMA commit LLM, papdashboard enricher, paperless-ai) are dark | `ps -eo pid,stat,comm` |
| 5 | Deployed system still runs flm **v1.0.2** (`l7bpnd399…-fastflowlm-1.0.2` in the unit's ExecStart); system-765 (`96dz4wvc`, 2026-09-11 03:20) predates the 07:37 v1.0.3 bump commit | `/etc/systemd/system/fastflowlm.service`; profile timestamps |
| 6 | **Staged v1.0.3 package BUILDS CLEAN**: `nix build .#packages.x86_64-linux.fastflowlm` → `4q6gk3c5…-fastflowlm-1.0.3`; tarball hash valid, autoPatchelf clean, layout self-checks pass, XRT 2.25.00 libs + `bin/flm` wrapper present | Build output inspected |
| 7 | `nix flake check --no-build` → **all checks passed** | Eval gate |
| 8 | **amdxdna driver: 0 files changed between v7.2 and v7.2.3** (kernel.org stable tree diff) — the "kernel 7.2.2 fixes v1.0.3 enumeration" hypothesis is **falsified**; nothing driver-side changed | cgit `diff/drivers/accel/amdxdna?h=v7.2.3&hp=v7.2` |
| 9 | /data has **108G free** — the ~21.6 GB Q4_K re-pull fits | `df -h /data` |

## Why the upstream issue was NOT filed (verify-before-filing)

The item said to file the XRT-2.25/kernel-7.2.0 issue after the retry. The verification the item itself ordered ("diff 7.2.0→7.2.2 amdxdna ABI first") **killed the issue's premise**: with zero amdxdna changes through 7.2.3, a kernel-specific enumeration bug has nothing to point at. What remains is (a) our own wrapper loader-path bug — fixed locally, agent-added, **never validated live** (flm cannot run until the reboot), and (b) a hypothetical XRT 2.25 enumeration defect — unproven. Filing now fails Gate 1 (override never verified to do anything) and Gate 4 (root cause unresolved between our config and upstream). Re-evaluate with live strace **only if** v1.0.3 + the fixed wrapper still fails post-reboot; a failure then would come with much stronger evidence.

## Deliberately not done

- **No deploy**: activating v1.0.3 pre-reboot gains nothing (the corpse's :52626 LISTEN kills any version's backend start) and feeds the exit-4 deploy-churn class deploy.sh already fights. The item is user-gated at the reboot by design.
- **No module retune**: 40G/32G MemoryMax stays — sized for the 21.6 GB Q4_1 with 26G measured peak; Q4_K size is unverified until the pull, so the runbook gates any retune on measured post-pull RSS.
- **No socket/service stops**: direct sudo/systemctl are harness-blocked in agent sessions, and deploy.sh's corpse guards own that path anyway.

## Handoff

The TODO row now carries the full post-reboot runbook (anchoring check → deploy → `flm pull` → live validation → revert-if-fail → retune-if-oversized → issue-only-if-still-failing). On reboot, the only human decisions left are "reboot now" and, if validation fails, accepting the v1.0.2 revert.

*Point-in-time snapshot; ANNOTATE, never rewrite, when bringing it current.*

---

## Second dispatch (2026-09-11 ~08:20) — re-verification + static loader validation

Queue re-dispatch of the same item ~18 min later. Re-verified the live state (all unchanged):
boot 2026-09-07 kernel 7.2.3, corpse PID 443303 (`Zsl <defunct>` + `Xsl`, PPid 1) still LISTENs
on :52626, `fastflowlm.service` still dying `bind: Address already in use` (journal tail ~08:14),
llama :8848/:8849 UP, deployed unit still v1.0.2, tree clean, staged
`4q6gk3c5…-fastflowlm-1.0.3` still present. TODO row untouched — it already carries the full
post-reboot runbook and the BLOCKED tail.

**New evidence (first dispatch did not cover):** static loader validation of the staged binary.
Under the wrapper's exact `LD_LIBRARY_PATH` (`$out/lib` + `$out/lib/x86_64-linux-gnu`), every
`flm-real` DT_NEEDED resolves (`ldd` → zero "not found"), and the XRT 2.25.00 chain —
`libxrt_core.so.2` AND the NPU driver plugin `libxrt_driver_xdna.so.2` — is present in the
multiarch dir the wrapper adds. The dlopen-time device enumeration (the actual 2026-08-31
failure mode) remains the only live-only gate; it is post-reboot per the runbook. The first
dispatch's no-deploy / no-issue / no-retune decisions are upheld unchanged; this commit exists
so the queue item carries a footer-bearing cross-reference.
