# Status: task 000001a0cc8af769f497a7204704871623f0 — Offsite Borg restore path (re-dispatch verification + same-host port-23 fix)

**Date:** 2026-09-23 07:20 CEST
**Queue item:** "Implement offsite Borg leg restore path: runbook + first timed restore drill" (TODO_LIST §storage)
**Session type:** RE-DISPATCH VERIFICATION. The queue handed me an item whose deliverable was already committed by a prior run of the SAME task ID (`f845d1e9`, 06:58:40, footer matches) but which produced **no status report** (glob over `docs/status/*000001a0cc8af769*` = empty) — the prior run evidently died between commit and report. This session therefore (1) verified the landed work against sources instead of redoing it, (2) found and fixed one real defect the prior pass missed, (3) added fresh verification evidence, (4) closed the AGENTS.md memory gap.

**Verdict:** item CLOSED and now genuinely verified. Nothing broken in the tree; `nix flake check --no-build` green after every change; pre-commit hooks green on both new commits.

---

## a) FULLY DONE

| # | Deliverable | State | Evidence |
| - | ----------- | ----- | -------- |
| 1 | Restore runbook `docs/services/offsite-borg-restore.md` (176 lines: recovery-artifacts/dead-host table, StorageBox port-23-only access notes incl. RFC4716-on-22 trap, single-file restore, bare-metal sequence, drill doc + first timings, gotchas) | Landed by prior run; **read line-by-line and verified against source this session** | `f845d1e9`; cross-checked against `platforms/nixos/system/backup.nix` + `modules/nixos/services/sops.nix:827` (borg-env template carries ONLY `BORG_REPO`) |
| 2 | Timed drill `scripts/borg-restore-drill.sh` (real / `--local` / `--selftest` modes; timed connect+resolve / extract / verify; sha256 byte-verify; scratch-only extract; per-run records under `~/.local/state/borg-restore-drill/`) | Landed; **live-executed this session** | `--selftest`: PASS, 230/231/8 ms = 469 ms total, sha256-verified extraction; `bash -n` clean; negative tests: bogus `--local` → die + rc=1, real-mode-as-user → instruction message + rc=1 |
| 3 | First timed restore drill | Ran (prior: 2 runs PASS 615/585 ms; this session: 3rd PASS 469 ms) — **stand-in depth** (`--selftest`, throwaway repokey-blake2 repo, same `auto,zstd,9`) | runbook table + drill records on disk |
| 4 | Port-23 `BORG_RSH` fix in `backup.nix` (scp-form repo string carries no port; StorageBox Borg = port 23 only; `[host]:23`-shaped known_hosts pin) | Landed by prior run; verified coherent across module + drill + backup runbook | `backup.nix:180`, drill line 142, `offsite-borg.md:79` |
| 5 | **Defect fix this session:** restore runbook's SAME-HOST block exported `BORG_RSH` without `-p 23` while sourcing only `BORG_REPO` — the documented command would dial port 22 (SCP/SFTP only, no `borg serve`) and fail. One-line fix. | **Fixed** | `4e52bd8e` |
| 6 | **Memory maintenance this session:** AGENTS.md Offsite Borg paragraph now points at the restore runbook + drill (was backup-side only — future sessions could not discover the restore docs) | **Fixed** | `6579ecf4` |
| 7 | Queue closure: TODO_LIST.md line 28 + docs/todo/storage.md line 41 both `[x]` with matching DONE notes, no queue/library drift | Already coherent (prior run); verified | both files |
| 8 | Eval gate | Green after every change | `nix flake check --no-build` ×3 (incl. the unrelated parallel-session commit `1b60b2f2` riding the tree — green too) |

**Commits:** `f845d1e9` (prior run, full deliverable) → `4e52bd8e` (same-host port-23 fix) → `6579ecf4` (AGENTS.md pointer). All footered with the Task-Queue-ID. Nothing pushed.

## b) PARTIALLY DONE

1. **Real-repo drill depth.** The item asked for "the first timed restore drill"; what exists (correctly) is stand-in depth. The real-repo path is implemented but unexecutable pre-go-live by design (owner-held StorageBox hostname/username; placeholder tripwire). Drill real mode is verified only down to its failure gates (root gate, env-file presence, placeholder tripwire) — never against a live StorageBox. Go-live checklist step 9 owns the real depth; this is the correct scoping, but it means "restore path proven" = "proven modulo the WAN/SSH leg and the real repo".
2. **shellcheck on the drill script.** Not re-runnable this session (`shellcheck` absent from my shell PATH; the hook skipped it in my commits because only `.md` was staged). It plausibly ran when `f845d1e9` staged the `.sh`, but I cannot prove that retroactively. Coverage currently rests on `bash -n` + live selftest + negative tests.
3. **Drill flag surface.** `--selftest` and `--local` (negative) exercised; `--archive <pin>`, `--subset` override, and `--keep` never executed. Low risk (all three are trivial plumbing above the verified core), but untested is untested.

## c) NOT STARTED (correctly out of scope; tracked elsewhere)

1. Real-repo timed drill + timing row in the runbook table (go-live step 9).
2. Go-live itself: StorageBox hostname/username sops paste + `ssh-keyscan -p 23` host-key pin + `enable = true` (owner-gated, docs/todo/storage.md).
3. Recovery-copy policy for the passphrase (owner decision; the runbook correctly documents it as THE unrecoverable artifact).
4. VM test for `backup.nix` (flagged in the 2026-09-23 02:01 closeout review; every other backup-producing service has one — not this item's scope).
5. Post-deploy smoke entry for offsite-borg (same closeout-review gap).
6. Stale-lock (`borg break-lock`) recovery section in either runbook — noticed missing in passing; not written.

## d) TOTALLY FUCKED UP

Repo state: nothing — tree clean, all commits hook-green, eval green. The honest nearest-misses:

1. **The prior run's commit message contained an overbroad claim**: "Now coherent across backup.nix, both runbooks, and the drill" — FALSE for exactly one block (the restore runbook's same-host `BORG_RSH`). That is the commit-message-evidence-rule class: an absolute coherence claim that was never swept against every env block in both docs. It would have bitten whoever followed the runbook at 3 AM on a dead host. Caught this session only because I cross-checked the sops template instead of trusting the message.
2. **My own verification briefly lied to me**: my first negative-test run printed `rc=0` after two FAILs — because `bash … | tail` made `$?` report tail's exit code. The AGENTS.md pipefail trap, stepped in live. The script was right; my harness was wrong; re-ran without the pipe (`rc=1` both). Reported because a reviewer reading only the first run would conclude the drill doesn't fail loudly.
3. **Both of my commits were initially captured by the auto-commit daemon** as heuristic commits (`17ce3ba5`, `b241575a`). Both were verified (`git show --stat` = exactly my change) and amended per the daemon-race discipline — but the pattern cost one failed commit attempt each time; editing and committing in one step is not safe against this daemon's ~10-min cadence.

## e) WHAT WE SHOULD IMPROVE

1. **Queue runner gap (process):** a run can commit its footer'd deliverable and die before reporting → silent re-dispatch of done work. This task cost a full session on re-verification. Suggestion: treat "footer-commit exists for queue-ID X but no status report for X" as DONE-pending-closeout, or make the report step atomic with the commit.
2. **Drill records on early FAIL:** the record file is only wired at line ~179 (`exec > >(tee …)`), AFTER env checks — the most likely real-world failures (root gate, still-PLACEHOLDER repo at pre-go-live attempts) leave NO record, while the header comment promises "a per-run record (full output + timings)". Move record setup to the top; add an outcome line.
3. **The detached-Samsung gotcha in the restore runbook is wrong-ish:** it claims "borg still works but pays a full remote-cache rebuild" when `/mnt/hot` is detached. In reality borg will happily `mkdir` its cache under the UNMOUNTED mountpoint onto the ROOT fs — the root-fs shadow-dir class this repo keeps documenting. The job unit is mount-gated (`RequiresMountsFor`); the manual drill is not. The drill's real mode should gate on `mountpoint -q /mnt/hot`.
4. **Drill borg binary is floating** (`nix build nixpkgs#borgbackup`) while the job runs the locked nixpkgs' borg 1.4.5 — version skew between drill client and repo writer is possible. Benign under borg's forward-compat today; pinning the drill to the flake's nixpkgs would make drill timings measure the same binary that will do the real restore.
5. **Verification reflex:** I trusted the prior commit's "coherent" framing until I opened sops.nix — the default should be to re-derive every "coherent/everywhere/green" claim from source during a verification pass, not spot-check it.
6. **Memory-update lag:** the restore docs existed ~25 min before AGENTS.md referenced them (and would have lingered indefinitely if the prior run had reported and closed the item). The AGENTS.md update belongs in the same commit as the docs that need discovering.
7. **Unrecorded evidence:** this session's third PASS run (469 ms) lives in the drill records + this report only; the runbook table correctly stays real-repo-scoped, but a one-line "reproduced 2026-09-23" note would have made reproducibility visible where the table is read.

## f) NEXT (≈30, borg-focused first; ★ = already tracked in an existing queue row, pointers kept)

1. At go-live: `sudo bash scripts/borg-restore-drill.sh` against the real repo; append the timing row to the restore-doc table (★ go-live step 9).
2. Drill real mode: gate on `mountpoint -q /mnt/hot` with a loud die (root-fs shadow-dir class, see e.3).
3. Drill: write a FAIL record even on early death (move record setup before env checks; see e.2).
4. Restore doc: correct the detached-Samsung gotcha (cache lands on root fs, not "still works").
5. Drill: resolve borg from THIS flake's locked nixpkgs (drill binary == job binary).
6. Flake fixture check for the drill (PATH-stub borg): root-gate message, bogus `--local`, placeholder-repo tripwire via a fixture `BORG_ENV_FILE`, happy-path PASS — makes the failure paths regression-proof.
7. Prove shellcheck actually covers `scripts/*.sh` in pre-commit (was it run at `f845d1e9`?); close any gap in the hook's `.sh` leg.
8. Add a stale-lock recovery section (`borg break-lock`, when WAN drops mid-run) to the restore runbook.
9. Post-deploy smoke entry for offsite-borg, enable-gated: unit file exists, golive tripwire fires on placeholder, `.last_success` path (closeout-review gap).
10. VM test for `backup.nix` (enable=true shape: timer calendar, EnvironmentFile, BORG_RSH with `-p 23`, ReadWritePaths, integration backup row; mock-sops pattern) (closeout-review gap).
11. Recovery-copy policy: decide the passphrase escrow; add a go-live step that PROVES the recovery copy decrypts (e.g. `borg key export` from a second machine) (★ open owner item in storage.md; see Q1).
12. After first real backup: record `borg create --stats` irreplaceable-set size vs the ~800 G BX21 trigger into the backup runbook (★ pointer exists in storage.md).
13. Pre-stage an OnFailure Discord delivery proof command for `borgbackup-job-hetzner` in the runbook (delivery-proof class; executable only once enabled).
14. Decide drill cadence post-go-live: scheduled systemd timer + OnFailure (forgejo weekly-drill precedent) vs manual-only (see Q2).
15. Archive-vs-exclude audit after first real run: sample `borg list ::archive` output against the 27 exclude patterns to catch accidental inclusion (quota burn).
16. Drill: optional `borg extract -n --verify-data` deep-integrity mode (full-chunk verification) distinct from the subset drill.
17. Verify the `borg mount` FUSE path on this box once and drop the runbook's "allow_other caveat" hedge or confirm it.
18. Drill-record retention note (tiny logs, unbounded count — trivial housekeeping).
19. ★ Paperless pg_restore drill + `pg_restore --list` integrity gate (TODO_LIST line 44 — same "untested restore" class, next in line).
20. ★ twenty/manifest pg_dump restore drills (storage.md backlog row).
21. ★ restic-app-dumps: `restic check` + one-file restore drill after the first nightly run (2026-09-22 closeout follow-up).
22. ★ hermes backup leg: receive one `@home-hermes.*` snapshot into a scratch subvol + diff (2026-09-18 status suggestion).
23. ★ tq journal restore drill for `/mnt/pool/services/tq` (2026-09-08 status item 30).
24. ★ CV pipeline.sqlite restore drill (cv.md "pending, owner root shell").
25. ★ Forgejo restore-drill first live run at gate G1 (forgejo-primary plan).
26. Miniflux restore drill (dump → fresh scratch PG) — suggested in archived status docs; may need re-queueing as a fresh row.
27. Queue-runner rule from e.1: don't re-dispatch a queue ID whose footer-commit exists without a report (or auto-close it).
28. Consider a one-line "reproduced 2026-09-23 (stand-in)" note next to the drill table (e.7).
29. Sweep the OTHER env blocks in both borg runbooks against `sops.nix`/`backup.nix` one more time with a checklist (the same-host miss proves block-by-block sweeps, not "coherent" claims, are the verification unit).
30. When the leg goes live: flip the AGENTS.md Offsite Borg header from DORMANT to live state with first-run facts (archive name shape, seed duration, irreplaceable-set size).

## g) QUESTIONS (owner-only; I cannot determine these myself)

1. **Recovery-copy policy for the Borg passphrase** — where should it live (password manager / printed / both), and do you want the go-live checklist to include an explicit "prove the recovery copy decrypts a key export from a second machine" step? This is the one artifact the runbook calls unrecoverable; without the policy decision the real-repo drill proves less than it appears to.
2. **Post-go-live drill cadence** — should the restore drill become a scheduled systemd timer with OnFailure alerting (forgejo weekly-drill precedent, catches repo/credential rot automatically) or stay manual-only (avoids WAN-flap alert noise from a 1 TB-class remote)?
3. **Steam saves in the offsite archive** — `/data/SteamLibrary` is excluded as rebuildable, but it carries the ONLY stateful Steam data (~2.5 G compatdata, NFS Hot Pursuit Remastered saves under `pfx/`). Include the save/compatdata subtree in the archive (small exclude-list change) or accept the re-download/re-progress RPO?
