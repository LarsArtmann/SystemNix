# Status Report — task-000001a0c6bd943ab3a1f2f046476b881acf (RE-DISPATCH: verification-only, fix already landed)

- **Date:** 2026-09-22 08:03 CEST
- **Ticket:** review-fix (low) "Stale home.nix cargo comment and AGENTS.md buildcache section now contradict the new symlinks" — descends from original task `000001a0c5fdb2dd3166a37c82d89dc5be6c` (storage: cache-fallback sweep, rejected commit `f39e144b`)
- **Session type:** the queue re-dispatched this ticket ~40 min after the fix session closed it. Disposition: **the finding was already fully resolved before this run started; this run is a verification-only no-op.** Prior report: `2026-09-22_07-15_task-000001a0c6bd943ab3a1f2f046476b881acf.md` (annotated with a supersede note on disposition; its sections b–f remain the authoritative open-item inventory).
- **Precedent:** this is the SECOND review-fix re-dispatch today with the same shape — the buildcache reap ticket was re-dispatched at 06:24 after its fix (`d60d598c`) had already landed, and that run also correctly made no code change (`a6d079ba`). A systemic queue behavior, not a one-off — see section (e) and question 1.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | Re-dispatch disposition per contract: rejected commit `f39e144b` verified to still exist (`git cat-file -e` OK) **AND** the verbatim anchor text verified **GONE from the tree** — repo-wide grep finds it only inside status reports QUOTING the original bug, never as a live claim. Per the contract's own clause ("if the anchor text is also gone, the finding no longer applies — state that instead of inventing a change") the correct action was no change, and that is what happened | grep over `platforms/ modules/ lib/` (zero hits) + repo-wide grep (status-doc hits only), this session |
| 2 | Fix commit verified: `db2b44c0` (07:12:36) carries **exactly this ticket's footer** and its diff is precisely the reviewer's expected home.nix reword — comment-only, +4/−2, one file — replacing the falsified claim with "env'd cargo writes to CARGO_HOME here; env-less cargo falls back to `~/.cargo/registry`, which the 2026-09-22 HM symlink sweep redirects onto this same registry on the mount" | `git show db2b44c0` (diff + message inspected), `platforms/nixos/users/home.nix:592-600` |
| 3 | AGENTS.md half verified element-by-element against the finding's expected fix — all four present in the Build Cache SSD section: (1) three new symlinks (`~/.cache/pnpm`, `~/.local/state/pnpm`, `~/.cargo/registry`, AGENTS.md:739), (2) `migrate-buildcache-fallback-caches` activation block (:748), (3) reap-list change ("FOUR hand-kept surfaces", :748), (4) the deploy.sh gap from finding 1 (`d60d598c`, :748) | AGENTS.md:739,748 read in full this session |
| 4 | Repo gates green at no-op time: working tree clean; `nix flake check --no-build` → "all checks passed", rc=0 (the aarch64-darwin omission warning is the documented expected one) | command output, this session |
| 5 | No new work commit — deliberately: a duplicate-footer or empty commit would corrupt the queue↔git cross-reference (the contract mandates exactly one footer per commit and forbids inventing changes; the fix already exists footered in history) | reasoning + `git log --format=%B db2b44c0 -1` |
| 6 | Sibling report kept honest: annotated the 07:15 prior report with a supersede note pointing here, so the two same-ticket reports cannot drift on the disposition question | edit to `docs/status/2026-09-22_07-15_...md`, this session |
| 7 | Re-dispatch classified as a PATTERN, not an accident: dug up the 06:24 precedent (`a6d079ba` — buildcache reap review-fix, same verification-only no-op shape after its fix had landed) and grounded the queue-idempotency finding in two same-day occurrences | `git log --format=%B a6d079ba -1`, this session |

## b) PARTIALLY DONE

| # | Item | What works | What remains open | Effort |
|---|------|-----------|-------------------|--------|
| 1 | This fix ticket end-to-end | 100% of the finding is addressed in the tree (`db2b44c0` + intervening `59fc4294`); gates green; both same-ticket reports now agree on the disposition | The QUEUE still considers the ticket open — it re-dispatched after the footered fix commit existed. Root cause (harvest blind to footers? timestamp gating? race between fix push-time and dispatch generation?) is unknowable from git alone → question 1 | S (queue-side fix) |
| 2 | The buildcache sweep cluster (this ticket's lineage, unchanged by this run — correctly, it is not this finding) | Symlinks live; reap stack covers all four surfaces; AGENTS.md documents everything | `buildcache-init` does not provision the three new targets (post-USB-recovery the symlinks dangle until the next deploy); reap surfaces are four hand-kept lists ("single-sourcing is queued"); no VM test for the activation block; `$DRY_RUN_CMD` inconsistency in the migration block's `rm -rf`. Full inventory: prior report §b/§c/§f | S–M each |

## c) NOT STARTED (this session — all pre-existing, all named; the authoritative list is prior report §c/§f, highlights only here)

1. **Queue review-fix idempotency** (NEW, from this session): the dispatcher re-sends review-fix tickets whose footered fix commits already exist — twice today. No mechanism in the repo can fix this; it needs the queue owner (question 1).
2. **`buildcache-init` provisioning of the three new cache targets** (prior f.4) — dangling-symlink window after USB-outage recovery.
3. **Reap-surface single-sourcing + consistency gate** (prior f.5/f.6) — the drift class that produced BOTH review findings in this lineage.
4. **VM-test rebuild for the sweep** (prior f.1) — PSI-gated since the original run.
5. **mountPoint-vs-HM-symlink eval guard** (prior f.2) — 2026-09-20 nested-autofs exit-4 class.
6. **`$DRY_RUN_CMD` ratification for the HM activation `rm -rf`** (prior f.8) — blocks the audit-script design (prior f.18) → question 3.
7. **Status-report backlog items 43/44 closure** (prior f.7) — falsified by `59fc4294` since before the first fix run.
8. **38 commits unpushed** (noticed in passing; was ~35 at the prior report) — the held push is a standing user decision, but the backlog keeps growing and every new commit rides the eventual push-time re-filter / resync surface.
9. **TODO_LIST.md:21 pointer check** (prior f.17): "storage.md row marked done" never verified to resolve to an actual `[x]` row.
10. **docs/services/buildcache.md runbook** (prior f.12) — absent; the domain has now produced two review rounds + two re-dispatch rounds.
11. Prior report §c items not restated here (eval-warning fragment triage, `/mnt/hot` scrub family, historical-doc ANNOTATE passes, cross-project lessons, old `/rust-cache` cleanup check) — all remain untouched and valid.

## d) TOTALLY FUCKED UP

**Nothing from this session.** The run made zero code changes by design, broke nothing, and left the tree exactly as it found it plus one supersede annotation and this report. Verified, not vibes: `git status` clean before and after (pre-report), `nix flake check --no-build` rc=0, nothing pushed.

Considered-and-rejected alternatives, for the record (a reviewer may overrule any):

1. **Inventing a redundant change** (e.g. re-editing the comment to slightly different wording just to have a commit) — rejected: violates "no invented changes", creates a second footered fix commit, and rewrites history-adjacent context for zero tree value.
2. **Reporting without any commit** — rejected: the report instruction explicitly says commit the report with the ticket footer; the report IS this run's artifact. Side effect (flagged in question 2): the ticket now has THREE footered commits (fix, prior report, this report) — if the queue's harvest counts footers, this run may look like "fix attempted twice".
3. The original run's genuine miss (the falsified comment + deferred AGENTS.md update that caused the finding) is already on the record in the prior report §d and is NOT this session's damage.

## e) WHAT WE SHOULD IMPROVE

1. **Queue↔git idempotency for review-fix tickets** — the highest-leverage fix available today: before dispatching a review-fix, footer-scan history (or `origin/master..HEAD`) for the ticket ID; if present, dispatch as "verify-only" or skip. Two same-day re-dispatches each cost a full session (context load, gate runs, report) to conclude "already done". This is queue-owner work, not repo work (question 1).
2. **Anchor-text archaeology needs a status-report exclusion rule.** The re-dispatch protocol says "re-anchor via the verbatim anchor text"; but after a fix lands, the anchor survives ONLY inside status reports quoting it. A naive grep says "finding applies" off a QUOTE of the dead claim. This session's first grep hit exactly that. Rule for future dispositions: anchor searches must exclude `docs/status/` (or match only non-report files), and fix sessions should paraphrase-or-escape quoted anchors in reports. Cost when skipped: a wrong "the bug is still live" verdict.
3. **Re-dispatch reports should be cheap by default.** This run deliberately did not re-derive the prior session's full evidence (element-by-element re-verification of AGENTS.md was re-done because it is cheap; the §f backlog was referenced, not restated). Make that the documented shape for re-dispatch runs: verify the FINDING, link the prior report, do not regenerate the backlog inventory.
4. **The buildcache cluster's hand-kept lists keep paying review tax** — unchanged verdict from the prior report; this re-dispatch added no new drift but re-confirmed that every round trips over the same open cluster. The single-sourcing + consistency-gate pair (prior f.5/f.6) converts the whole class from "review finding" to "flake check failure".
5. **Supersede-annotation discipline for same-ticket reports worked well** — one line on the old report pointing at the new one. Cheap, keeps docs-health ANNOTATE conventions, prevents a future reader from acting on the older disposition. Keep doing it for any re-dispatch.

## f) NEXT TASKS (ranked; HARVEST input — do not let it die in this file. Items marked ◆ are NEW this session; the rest carry forward from the prior report §f, restated one-line so this list stands alone)

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | ◆ Queue owner: add footer-scan idempotency to review-fix dispatch (skip/verify-only when the ticket ID already exists in a commit message) | High | S | Process |
| 2 | Single-source the four hand-kept reap surfaces (deploy.sh ×2, buildcache-usb-recovery step 2.5, HM activation block) into one shared list | High | M | Quality |
| 3 | Add the consistency gate: HM symlink names == reap lists == buildcache-init provisioning targets (flake check or CI script) | High | M | Quality |
| 4 | Provision the three new cache targets in `buildcache-init` (pnpm-cache, pnpm-state, cargo/registry) so post-USB-recovery symlinks never dangle | High | S-M | Bug |
| 5 | Rebuild the buildcache VM test once io PSI avg10 < 20% sustained (PSI-gated since the original run) | High | M | Quality |
| 6 | Add the mountPoint-vs-HM-symlink eval guard (2026-09-20 nested-automount exit-4 class) | High | M | Quality |
| 7 | Ratify or fix `$DRY_RUN_CMD` for the HM activation `rm -rf` (blocks item 15's audit design) → question 3 | Medium | S | Quality |
| 8 | Locate and close/annotate status-report backlog items 43/44 (falsified by `59fc4294`) | Medium | S | Cleanup |
| 9 | Verify TODO_LIST.md:21's "storage.md row marked done" resolves to a real `[x]` row; fix the pointer if drifted | Low | S | Cleanup |
| 10 | Add a VM regression test for `migrate-buildcache-fallback-caches` (real-dir occupant removed; mount-absent clean skip; idempotent re-run) | Medium | M | Quality |
| 11 | ◆ Add the anchor-search exclusion rule (skip `docs/status/`) to the re-dispatch disposition runbook / tq-pool instructions | Low | S | Process |
| 12 | ◆ Document the re-dispatch report shape: verify finding → link prior report → reference (not regenerate) the backlog inventory | Low | S | Process |
| 13 | ◆ User decision surface: 38 unpushed commits (growing) — either schedule the push-time re-filter/resync window or accept the growth explicitly | High | S | Decision |
| 14 | Work the storage.md "Pool + disk-domain quality" row's 13 open sub-items via queue pacing — top sub-item: `/mnt/hot` scrub family + metrics (45 GiB live crush DBs, zero scrub coverage since 2026-09-18) | High | M+ | Feature |
| 15 | Extend the audit-script family: HM activation blocks using `rm` without `$DRY_RUN_CMD` (audit-serviceconfig-merge precedent; allowlist the deliberate cases) | Low | S | Quality |
| 16 | Triage the "the module header." eval-warning fragment from `nix eval` toplevel output (capture once, classify benign vs fix) | Low | S | Quality |
| 17 | Write `docs/services/buildcache.md` if the domain produces a third review round (two rounds + two re-dispatches so far — the threshold is close) | Low | M | Documentation |
| 18 | Annotate docs/research/2026-07-11 btrfs-wiki doc + secondary-ssd-990 proposal as SUPERSEDED re: `.cargo`/`@cargo` designs (docs-health ANNOTATE) | Low | S | Documentation |
| 19 | Add the interim AGENTS.md note: after USB-outage recovery, pre-create the three cache targets before relying on the symlinks (superseded when item 4 lands) | Low | S | Documentation |
| 20 | Record the cross-project lessons in crush-config references/lessons.md: (a) falsified-claim changes grep the entity repo-wide in the same change; (b) with the auto-commit daemon live, check `git log -1` immediately before commit | Medium | S | Process |
| 21 | Fold the btrfs+zstd buildcache conversion (`scripts/buildcache-btrfs-convert.sh`, deferred) into the same maintenance window as items 2-5 (same quiesce requirements) | Low | L | Maintenance |
| 22 | Verify the old `/rust-cache` p9 user-run cleanup is complete (`rmdir /rust-cache`, `@go`/`@npm`/`@cargo` subvol deletes); file whatever remains | Low | S | Cleanup |
| 23 | ◆ When the queue gains footer-idempotency (item 1), backfill a check over the existing queue: are OTHER open tickets already satisfied by footered commits? (two found today by accident; a scan would find them systematically) | Medium | S | Process |

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Why did the queue re-dispatch this ticket when `db2b44c0` (footer + fix) existed ~50 minutes before dispatch?** Same thing happened at 06:24 to the buildcache reap ticket. Is the dispatcher footer-blind, gated on commit timestamps vs. ticket-creation, or racing fix-session close-out? I cannot see the queue's internals from the repo; the fix (footer-scan before dispatch) is cheap but needs an owner decision.
2. **What commit shape does the queue expect from a verification-only re-dispatch?** I committed only this report with the ticket footer (no work commit — the contract forbids inventing changes). If the queue counts footered commits per ticket, the ticket now has three; if it wants a non-report artifact-of-work commit, I cannot produce one honestly. Which signal closes the ticket?
3. **Carried from the prior report (still blocking real work): should destructive `rm -rf` in HM activation blocks honor `$DRY_RUN_CMD`** (like `jan-data-link` does), or is unconditional reaping ratified for rebuildable cache data? The answer decides next-task item 7 and whether item 15's audit script has any teeth.

---

*Point-in-time snapshot of a verification-only re-dispatch run. Section (f) is HARVEST input. The authoritative open-item inventory for this ticket's lineage is the prior report (07:15) sections b–f; this report adds only the re-dispatch evidence, the queue-idempotency pattern, and the ◆ items.*
