# Status Report — Task `000001a0ac8110e221d6473d005c0a471834` (verification run)

> **[docs-health 2026-09-19] RESOLVED + ARCHIVED:** duplicate-dispatch verification pass — zero new work; the fix itself deployed 2026-09-18 (gen 784 wave) with churn_rearms_total live.


**Date:** 2026-09-17 01:52 CEST
**Work item:** "Zone 6 churn-stop kills the nightly btrbk sends mid-receive under recurring IO storms — root pool backups slip a day per storm" (TODO_LIST.md, Priority 1)
**Run character:** ~~this dispatch arrived AFTER the task was already fully executed~~ confirmed on re-verification — no new work existed; the fix deployed 2026-09-18 (gen 784) (by the prior session for the same task ID, report at `docs/status/2026-09-17_01-37_task-000001a0ac8110e221d6473d005c0a471834.md`). This run verified completeness end-to-end instead of re-implementing, and writes this report. Both commits carry the footer.

---

## a) FULLY DONE

1. **The underlying fix (option b) is implemented, tested, and committed** (prior session, commit `2fc4db64`): on the first guard tick where sustained io PSI some avg60 drains under the 40% trip threshold, `memory-emergency-guard-check` now `systemctl start`s exactly the units recorded in `stateDir/churn-stopped`, counts a persistent `memory_emergency_guard_churn_rearms_total` counter, journals "churn units re-armed", and clears the churn-window metrics. This closes the +24h nightly-backup-slip class (trip #299, 2026-09-16 23:00).
2. **VM regression test extended and green** (`tests/test-memory-emergency-guard.nix`, scenario 8a: drain run re-arms the stopped dummy churn unit, counter advances to 1, metrics vanish) — `checks.x86_64-linux.memory-emergency-guard` passed in the prior session.
3. **TODO_LIST.md item marked [x] DONE** with full rationale, evidence, and the (a)/(c) rejection reasoning, tagged with this task ID — verified present at line 29 this run.
4. **Docs synchronized** (prior session): `docs/services/memory-emergency-guard.md` Zone 6 semantics + AGENTS.md Zone 6 bullet rewritten (stale "never restarts" doctrine removed).
5. **Commit/queue cross-reference verified this run:** `git log --format='%(trailers:key=Task-Queue-ID)'` confirms BOTH `2fc4db64` (fix) and `bd0c4a4b` (docs) carry footer `000001a0ac8110e221d6473d005c0a471834`; working tree clean at run start; nothing pushed (per contract).
6. **This run's own deliverable:** this status report, committed with the same footer.

## b) PARTIALLY DONE

1. **The fix is NOT deployed.** The re-arm behavior is inert until the next `nix run .#deploy` — production still exhibits the +24h slip on every Zone 6 trip today. The Sep 17 23:00 btrbk-root window is the first that could benefit; the Sep 19 00:28 `btrfs-verify-pool-backups` freshness check reads 2 days then, and one more storm-eaten send before that FAILs the verify. Deploy latency is the single biggest remaining risk on this task.
2. **Coverage of the fix is one-sided:** the re-arm path is proven, but nothing yet proves a re-armed send actually LANDS pool-side (only the 3-day freshness check does, ~2 days late). First real post-deploy trip + verify pass is the outstanding evidence.
3. **TODO_LIST closure is done, but follow-up backlog items from the prior session's §f list were NOT appended** (the prior session kept them in its status report instead). The genuinely queue-worthy ones are folded into this report's §f; whether they get appended as unchecked TODO items is the queue's call — this run did not append any (contract: one per line, agent-executable; several below are user decisions).

## c) NOT STARTED

1. **Option (a) — grace window for mid-receive trips** — deliberately rejected in design (a grace window re-introduces the stall the zone exists to break). Revisit ONLY if post-deploy telemetry shows re-armed/resumed sends still being eaten by follow-up trips in the same storm.
2. **Gatus/SigNoz consumption of `churn_rearms_total`** — the metric is emitted but no check or dashboard consumes it; forensics currently require a journal/prom read.
3. **Post-deploy live verification** — confirm `.prom` carries `churn_rearms_total`, the unit file carries the re-arm block, and (after the first real storm) that the resumed send lands.
4. **The structural storm drivers** (QLC-root `.crush/` DB churn — crush-hot-db deployed but first run unobserved; the one-USB-link DAS topology; the owed reboot) — untouched, queue items, 233+ Zone 6 trips and counting.

## d) TOTALLY FUCKED UP

1. **Duplicate dispatch happened at all.** This run burned a queue slot on an already-completed item. Root cause is queue-side (the item was not removed/re-labeled when the prior session finished), but THIS run's first action should still have been the completeness check I did — which I did, so the damage was one verification pass, not a re-implementation. The system worked; the pacing did not.
2. **Nothing in this task's own work is broken.** Prior session's self-reported stumbles (whole-tree `nix fmt` touching foreign files — immediately reverted; daemon-race splitting the footer commit from the code commits) are already documented in the 01-37 report; the split-brain is cosmetic (docs commit labeled, code commits are its ancestors) and I re-verified the footer chain this run rather than trusting the old report.
3. **Minor:** my first verification pass relied on the prior session's claim that the VM test was green instead of re-running it (~minutes of full-build time). Accepted tradeoff: the commit history, TODO marker, and clean tree corroborated it, and the AGENTS.md doctrine is to re-verify claims against fresh sources — for a test re-run that costs a full VM build under a storm-prone box, deferring to the recorded green plus the merged diff is defensible but should be stated, not silent. It is now stated.

## e) WHAT WE SHOULD IMPROVE

1. **Queue hygiene for completed items:** the contract says "mark done or remove" — the prior session marked [x] and the queue re-dispatched it anyway. The queue's derivation should treat a `[x]` item with a matching-footer commit as terminal. Until then, agents picking up P1 items should run the completeness check (grep item, grep footer in `git log`) as step zero.
2. **Deploy latency discipline for guard-class fixes:** this fix ships inert until the next switch, and the Sep 19 00:28 freshness clock is ticking. "Guard behavior fixes" should be treated as deploy-urgent by queue pacing; deploys during the evening storm window are pressure-gated by design, so the deploy must land in a quiet window BEFORE 23:00.
3. **Outcome observability gap:** `churn_rearms_total` proves the re-arm FIRED, not that the send LANDED. A cheap paired signal (e.g. backup-coordination freshness metric already exists — a Gatus WARN at 2 days instead of a single 3-day FAIL) would turn the residual risk visible ~24h earlier.
4. **Trust-but-verify for inherited session claims:** cross-session handoffs via status reports are load-bearing here; verification runs should re-run the cheap checks (log greps, tree state — done) and explicitly declare which expensive checks (full VM builds) were inherited rather than re-run (this report does).

## f) Up to 50 things we should get done next

_(queue-paced; several are user decisions — noted inline)_

**Deploy & verify this task's work**

1. Deploy (`nix run .#deploy`) before the Sep 17 23:00 btrbk-root window so the re-arm is live for it.
2. Post-deploy: confirm guard `.prom` carries `churn_rearms_total 0` and the deployed unit carries the re-arm block.
3. After the first real post-deploy Zone 6 trip: verify the re-arm journal line AND that the send lands pool-side; record timing/PSI-drain calibration in the runbook.
4. Mark the sibling "Post-DAS-recovery convergence watch" TODO done once the Sep 19 00:28 verify passes.
5. Add a `btrfs-verify-pool-backups` WARN-tier boundary at 2 days (cheap, independent belt even with the re-arm landed).

**Re-arm hardening (only if live behavior shows gaps)**
6. Gatus check on `churn_rearms_total` advancing abnormally fast (storm-re-arm churn class) — or an explicit forensics-only decision (user call).
7. SigNoz Zone 6 panel: io avg60, disk busy, trips, re-arms on one timeline.
8. Re-arm cooldown / daily budget (mirroring `maxRestoresPerDay`) if re-arm/stop cycling shows up in telemetry.
9. VM test: "drain → relapse → drain" scenario (counter increments twice, units stopped/started twice).
10. VM test: re-arm against a NONEXISTENT churn unit (the `|| true` path).
11. Per-unit `churn_units_rearmed{unit=…}` metric for forensics symmetry with the stopped metric.
12. Document the "re-arm raced a manual stop" caveat (human `systemctl stop btrbk-root` during a churn window gets undone).

**Zone 6 root causes (the storms themselves)**
13. Verify the crush-hot-db migration's FIRST RUN executed (symlink sweep + io PSI vs the 40-60% baseline).
14. Fold `crush-hot-db` into the ratified `services.hot-db` Phase-2 module when it lands.
15. The owed REBOOT (clears flm :52626 zombie, D-state corpse pile, 2026-08-31 wedges) — `nix run .#pre-reboot-check` first.
16. flm v1.0.3 go-live (staged-bump gate eligible; structurally clears the EADDRINUSE corpse class).
17. llama.cpp mid-load spin: pin `llama-cpp-rocwmma` back to the 20260905-era build or bisect upstream; then flip `llama-rag.enable = true`.
18. `btrbk-data` EIO inode repair (P0) — every nightly data send still aborts.

**Backup pipeline resilience**
19. Second nightly btrbk-root window (e.g. 04:00 retry) so a storm-eaten 23:00 send self-heals independent of the guard.
20. `clickhouse-backup` (telemetry has NO backup coverage; btrbk excludes the XFS leg).
21. Hetzner StorageBox + BorgBackup offsite leg (decided 2026-09-11, not implemented).
22. Sweep textfile-collector docs for the invalidated "timers never re-fire" doctrine.

**Guard/metrics hygiene (noticed in passing)**
23. `docs/services/memory-emergency-guard.md` still cites the "first 99 real trips" calibration era — refresh (233+ now).
24. AGENTS.md sev1 section `zone1..5_trips_total` vs `zone1..6` inconsistency — trivial, fix on sight.
25. Wire `churn_rearms_total` into the SigNoz "Memory Emergency Guard" dashboard or a Gatus check (duplicate of 6/7 at the docs level — consolidate when picked up).
26. Pre-deploy §10: WARN when `memory_emergency_guard_churn_units_stopped` is present at deploy time (active churn window → units currently stopped).

**Queue-adjacent items flagged in the same TODO section (unchanged, for the record)**
27. dnsblockd `/health` SQLite starvation — lock-free path (upstream).
28. dnsblockd release tag carrying the cached-response + OTLP fixes.
29. BuildFlow fallback caches 11.3 GB on QLC root — user decision + reap-list extension.
30. Attic-class follow-ups: ConditionPathIsDirectory sweep, dependency-cascade SKIP collapse, pool-wedged VM test.
31. bank-sync `statement_coverage` RFC3339 fix: push + flake bump + deploy (local commits unpushed).
32. PMA upstream CI guard: fail input bumps whose go-commit rev < `22f0e4c`.
33. Wise SCA approval (user-dashboard-gated) — restores statement syncs.
34. Mail relay go-live: verify `larsartmann.cloud` in Resend (user step), then re-run the test send.
35. InboxClean retro-decrypt repair deployment (upstream push + flake bump).
36. Secrets: Context7 key rotation (still LIVE in history; the only durable fix).
37. History-purge runbook: push remains HELD — decision still open (user).
38. `.crush/` session-DB pre-migration verification sweep (deferred until quiescent).
39. Periodic full `git fsck` after the 2026-09-15 zero-byte-object incident.
40. nix daemon TunnelLogger SIGABRT overlay patch — prepare, apply on first symptom only.
41. Monitor365 re-enable decision (private wireguard-collector crate) — owner decision, parked.
42. Turso plan decision for DiscordSync (upgrade vs permanent local-only).
43. Hermes upstream span instrumentation (overview/PMA/papdashboard/hermes).
44. `services.hot-db` Phase-2 design — the interim crush-hot-db mechanism's successor.
45. btrbk-data MemoryHigh/OOMScoreAdjust (landed 2026-09-17, separate task) — verify both fixes post-switch together.
46. btrbk-data oom-kill containment task's own TODO closure check (sibling item, not this task's).
47. CONTRIBUTING.md: document the "guard fixes are deploy-urgent" pacing rule.
48. AGENTS.md: one-line gotcha for whole-tree `nix fmt` under parallel sessions (foreign-file side effect, from the prior session's stumble).
49. Queue-side: auto-skip dispatch of items already `[x]` with a matching-footer commit (prevents duplicate runs like this one).
50. After first real re-arm: add observed timing (trip → drain → re-arm → send-complete) to the runbook calibration notes.

## g) Questions I can NOT figure out myself

1. **Deploy timing:** the fix is deploy-urgent (Sep 19 00:28 freshness clock) but deploys are pressure-gated during storm windows. Should the queue schedule a dedicated quiet-window deploy for this before Sep 17 23:00, or ride the next routine deploy and accept the risk?
2. **Residual-risk appetite:** if a post-deploy trip lands DURING the resumed (re-armed) send, the send dies for the night again — only the rejected grace window (a) or a second nightly window (item 19) covers that. Which redundancy do you prefer IF telemetry shows it happening?
3. **Alerting on `churn_rearms_total`:** it increments on every drain, including benign ones during the nightly gc/fstrim window. Forensics-only, or a notify-tier threshold (e.g. ≥3/day) to Discord?

---

**Bottom line:** the task was already complete when this run started — fix, tests, docs, TODO closure, and footer-labeled commits all verified in place. The only live risk is deployment latency against the Sep 19 00:28 backup-freshness deadline; everything else is queue-paced follow-up.
