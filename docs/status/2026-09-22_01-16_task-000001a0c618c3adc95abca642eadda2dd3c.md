# Status Report — Task 000001a0c618c3adc95abca642eadda2dd3c

**Item:** "restic repo on pool for app dumps (dedup)" (TODO_LIST.md → storage)
**Date:** 2026-09-22 01:16
**Scope:** THIS task only. No unrelated research performed.

---

## Task verdict in one line

The work was already fully implemented and queue-closed by an earlier session (same day); this run **verified the artifact end-to-end at eval level, confirmed the queue state, and correctly declined to re-do or scope-creep it**. The remaining gap (first real backup run + VM test) is deploy-gated and tracked as its own queue item — exactly as designed.

---

## a) FULLY DONE

- **`modules/nixos/services/restic-app-dumps.nix`** — nixpkgs `services.restic.backups.app-dumps`: `initialize = true`, repo `/mnt/pool/backups/restic-app-dumps`, nightly 05:45 + 10m jitter, `--keep-daily 14 --keep-weekly 8`, over the 10 app-dump dirs under `/mnt/pool/backups` (excludes `root`/`data` btrbk receives + `forgejo-subvol`/`github-voice-corpus`/`pixel6` by design).
- **Password bootstrap** — `restic-app-dumps-setup` oneshot, machine-local random password file at `/var/lib/restic-app-dumps/password` (searxng-secret-key pattern, NO sops — repo is pool-local until the offsite Borg leg), deploy.sh provisioner-restarted.
- **Monitoring wiring** — `ExecStartPost` `.last_success` marker + integration-registry `backup` row → backup-coordination freshness metric + the aggregate "All Backups Healthy" Gatus check.
- **Resource hygiene** — `ioTier.background`, 12h `TimeoutStartSec` sized for the first seed, mount-gated (226 class defended).
- **Documentation** — AGENTS.md HDD-pool section carries the full design rationale; `docs/todo/storage.md` entry marked DONE with the run note; recent commits `36a946d2` + `ba456fdf` landed the docs + run report.
- **This run's verification**:
  - `nix eval .#nixosConfigurations.evo-x2.config.services.restic.backups.app-dumps.repository` → `/mnt/pool/backups/restic-app-dumps` (module fully evaluates on the live host config).
  - `nix flake check --no-build` → **all checks passed** (assertions + lints, incl. everything the module participates in).
  - TODO_LIST.md line 22 already `[x]` with the 2026-09-22 run note; working tree clean; nothing to re-commit.

## b) PARTIALLY DONE

- **Live validation of the backup pipeline itself** — the module is correct at eval time, but the first actual `restic` run, `restic check`, and a restore smoke are **deploy-gated**: nothing has backed up yet because the module landed without a deploy (or the deploy has not converged the first run). This is a *known, deliberate* state, not drift — the storage.md entry says so explicitly ("Deploy-gated: first run + restic check/restore smoke pending").
- **First-seed observability** — the freshness registry row exists, but its first green can only appear after the first successful backup; until then "All Backups Healthy" carries no restic signal.

## c) NOT STARTED (deliberately out of scope this run)

- **VM test `tests/test-restic-app-dumps.nix`** — separate queue item (TODO_LIST line 284 / storage.md line 87): password bootstrap idempotence, `restic init` on missing repo, backup lands + `.last_success` marker only on success, backup-coordination row picks up the marker.
- **Post-deploy smoke section** — a `post-deploy-check.sh` §13-style probe (repo exists, `restic snapshots` non-empty after first run) has not been added; the deploy-gated first run is currently verified by hand only.
- **Offsite replication** — the repo is pool-local; the Hetzner StorageBox + BorgBackup leg (decided 2026-09-11) does not cover restic-app-dumps yet. Losing the host loses the repo (documented trade).
- **Optional path extensions** — `forgejo-subvol` / `github-voice-corpus` / `pixel6` dumps are excluded; extending `services.restic-app-dumps.paths` is an owner decision.

## d) TOTALLY FUCKED UP

**Nothing.** No breakage found or introduced: eval green, flake check green, tree clean, docs consistent (TODO_LIST ↔ storage.md ↔ AGENTS.md ↔ module all agree). The one honest criticism below belongs in (e), not here — nothing is *broken*, one thing is *unproven*.

## e) WHAT WE SHOULD IMPROVE

1. **The task was done but UNPROVEN at runtime** — eval + flake check are the weakest signals for a backup pipeline. A backup module that evals green can still fail at first run (wrong password-file perms, marker path unwritable, prune flag rejected by the pinned restic version). The runtime proof is the actual deliverable and it is still pending behind a deploy.
2. **My first response treated "already done" as terminal** — I closed out on queue-state alone. A stricter reading of "verify your work" would have included at least a targeted VM test or a deeper eval probe (marker path, timer calendar, registry row) before declaring done. Eval of one option + repo-wide flake check is good, not exhaustive.
3. **No smoke probe added for the new surface** — repo-wide post-deploy smoke did not gain a restic block in the same change; the deploy that enables the module verifies nothing restic-specific automatically.
4. **Password-file loss = repo loss** — documented, but no backup-of-the-backup-credential exists anywhere (not even an owner-runbook note on where to copy it). Cheap to fix in docs.
5. **Single-purpose module, no lint coverage** — the gatus-pattern-lint / audits don't cover restic config shape; a wrong `--keep-*` flag would only surface at runtime. Low priority; note for the VM test.

## f) Up to 50 things to get done next

*(Scoped suggestions; most belong in `docs/todo/storage.md` / `services.md`, not all in TODO_LIST — HARVEST routes them.)*

1. Deploy the generation carrying `restic-app-dumps` (enable in configuration.nix is in-tree) and confirm the first backup run + `.last_success` marker.
2. Run `restic check` against the fresh repo post-first-run.
3. Restore smoke: restore one dump file from the repo and byte-compare against the pool original.
4. Write `tests/test-restic-app-dumps.nix` (the queued item — bootstrap idempotence, init-on-missing, marker-on-success-only, registry pickup).
5. Add a post-deploy smoke block: repo dir exists, `restic snapshots` non-empty after first run, marker fresh.
6. Verify `restic-app-dumps-setup` appears in deploy.sh's provisioner restart list and actually re-runs on deploy.
7. Confirm the backup timer's 05:45 + jitter does not collide with other pool jobs (paperless-db 02:00, geometrikks 05:15 — restic is 30 min after, fine; document the stagger table).
8. Assert the first-seed 12h TimeoutStartSec survives the seed size (measure actual first-run duration).
9. Confirm backup-coordination's `maxAge` for the restic row matches the nightly cadence (24h+ margin).
10. Decide whether `forgejo-subvol` dump joins the restic paths (it's a btrfs subvol dump dir — dedup value is high, forgejo zips share ~0 extents).
11. Owner decision: include `github-voice-corpus` / `pixel6` in `paths`?
12. Wire the restic repo into the offsite plan — Borg leg (2026-09-11 decision) should eventually replicate restic-app-dumps or supersede it; document which.
13. Document the password-file copy step (owner runbook: where to store a second copy off-pool).
14. Consider `restic unlock` guard or note for crash-interrupted first seeds (locked repo recovery runbook).
15. Add restic repo size + snapshot count to a textfile metric (pool-smart / backup-coordination pattern) for dashboard visibility.
16. Gatus: dedicated "Restic App Dumps" freshness check distinct from the aggregate (miniflux/paperless-db pattern) — or confirm aggregate coverage is enough.
17. Verify prune schedule leaves ≥14 daily snapshots after a week of runs (retention math check against pool free space).
18. Project pool growth: 10 dump dirs × 14d retention on RAID1 — record baseline size after seed, alert threshold if repo exceeds X.
19. `nix flake check` already green — add the module to docs/CONTRIBUTING.md guard inventory only if it grows assertions.
20. After 3+ successful nights, prune the manual-verification checklist from storage.md into a one-line "verified" note.
21. Long-term: evaluate whether restic should replace per-service dump retention (14d file rotation) entirely — single source of truth decision (ROADMAP fuel).
22. Long-term: check whether `restic` supports the pool's zstd compression well enough that the dump files could skip their own gzip stages (interplay with upstream dump formats).
23. Consider a monthly `restic recover`/integrity drill (calendar item or oneshot timer).
24. Add the module's oneshot to any relevant eval-time audit allowlists if deploy-restart-audit flags it (verify it doesn't).
25. Sweep AGENTS.md HDD-pool section after live validation to replace "deploy-pending" with measured facts (seed duration, repo size).

*(Items 26–50 intentionally omitted: beyond #25 the list becomes repetition or belongs to other domains — the pacing gates should pull from the queue, not this brainstorm.)*

## g) Questions I cannot figure out myself

1. **When is the next deploy window?** Everything above #1 is blocked on it — the module is enable-in-tree but the running system predates it. Should I trigger `nix run .#deploy` on the next quiet-PSI window, or do you deploy manually?
2. **Should the restic repo eventually BE the offsite leg** (replicated to Hetzner via Borg/rclone), or is it a local dedup layer only, with per-service dumps still going offsite individually? This decides whether items 10–12 are real work or dead ends.
3. **Do you want `forgejo-subvol` (and the other three excluded dirs) covered?** The exclusion was my predecessor's conservative default; you own the call, and it changes the paths list in the same module.
