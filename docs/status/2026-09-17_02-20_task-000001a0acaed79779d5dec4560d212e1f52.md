# Task 000001a0acaed79779d5dec4560d212e1f52 — Status Report

**Date:** 2026-09-17 02:20 CEST
**Work item:** "dnsblockd `/health` wedges on SQLite starvation while DNS :53 stays fine" (TODO_LIST Priority 1)
**Outcome:** Verification-only close — the fix was ALREADY shipped upstream and deployed. No code change was owed.

---

## a) FULLY DONE

1. **Premise verification (the actual work of this task).** Before writing anything, verified every claim the close depends on, from independent sources:
   - SystemNix `flake.lock` dnsblockd rev = `92aeedd0e1ea…` (jq, not eyeballed).
   - `git merge-base --is-ancestor 94c9cb93 92aeedd0` in `~/projects/dnsblockd` → OK: the lock is a descendant of the fix-carrying rev (`94c9cb93`, deployed since 2026-09-06 per AGENTS.md).
   - `internal/server/health.go:98` calls `s.healthProbe.CachedResponse()` — the lock-free path off the SQLite DB, exactly what the task asked for ("move `/health` off the DB").
   - Regression tests (burst-serve-from-cache, stale-503, degradeStaleHealth table) present in `internal/server/health_test.go` of the pushed history — the 2026-09-13 follow-up row's "confirm they are in the pushed history" premise is satisfied.
   - Net behavioral contract: SQLite starvation can no longer wedge `/health`; a stalled background cache flips the endpoint to an immediate 503 (correct RED signal) while DNS :53 stays unaffected.

2. **TODO_LIST.md closed per the file's conventions.** Row 30 flipped `[ ]` → `[x]` with a dated, queue-ID-attributed DONE annotation carrying the full evidence chain (ancestry proof, source line, test file). The remaining half (:9090 wedge root cause + goroutine-dump arming for the NEXT wedge) was explicitly NOT claimed as done and left to the standing 2026-09-13 follow-up rows — no scope inflation.

3. **Commit with the required footer.** Final commit `1e5ce035` "docs: close dnsblockd /health wedge task — fix verified shipped and deployed" carries `Task-Queue-ID: 000001a0acaed79779d5dec4560d212e1f52` (verified via `git log %(trailers)` after the amend). Not pushed, per contract.

4. **Daemon-race handled correctly.** The auto-commit daemon committed the TODO edit (commit `f380e834`, "1 changed file(s)") before my explicit commit ran — the exact race the AGENTS.md daemon-race discipline warns about. Before amending I verified `git show --stat HEAD` contained ONLY my TODO_LIST.md change (1 file, 1 ins/1 del) — no other session's files absorbed into the amend. This followed the established queue-ID-remap precedent documented in TODO row 19.

5. **Repo health verified.** `nix flake check --no-build` → all checks passed (the aarch64-darwin omission warning is expected per AGENTS.md). Pre-commit hooks (gitleaks, fmt, flake check) green on the final commit.

## b) PARTIALLY DONE

Nothing inside this task's scope. The item itself has a legitimately OPEN second half (the :9090 wedge ROOT CAUSE was never found; the goroutine-dump runbook is armed but has never fired on a live wedge), but that half is separately tracked in TODO_LIST (2026-09-13 dnsblockd follow-up rows: live post-deploy `/health` behavior confirmation, regression-test-push-history check — the latter I additionally confirmed green today) and was deliberately left there rather than silently folded into this close.

## c) NOT STARTED

- **Live behavioral confirmation on evo-x2.** My verification is lock/source-level, NOT runtime: I did not curl `:9090/health` or confirm cache-served responses on the deployed daemon. The sandbox blocked `systemctl`/`curl` (both rejected as "not allowed for security reasons"), and I chose not to fight it because (a) AGENTS.md already records the deployed binary as `dnsblockd-94c9cb9` with the fix, and (b) the lock-ancestry proof ties the deployed pin to the fix rev. This is the honest gap: ancestry proves the CODE is deployed-capable, a live probe would prove the BEHAVIOR. TODO row "Post-deploy: live-verify the dnsblockd cached `/health` behavior" (2026-09-13) remains the right home for it — it is sudo/curl-gated and the standing rows cover it.

## d) TOTALLY FUCKED UP

Nothing destructive, but three self-inflicted inefficiencies worth recording:

1. **Ran `systemctl` and `curl` against the sandbox before checking what was allowed** — both bounced with "command is not allowed for security reasons". Cost: one wasted round trip each. The AGENTS.md documents this session type's constraints implicitly (sudo-gated deploys, sandboxed agents); I should have anticipated blocked system-inspection commands and gone straight to the repo-level evidence (lock + upstream source), which is what settled it anyway.
2. **First `edit` bounced with "read the file first"** — I had the exact line text from `grep`/`sed` output but had not used the View tool on the file. One wasted cycle; the workflow rule ("read the relevant context before editing", View-tool-based) is explicit.
3. **The daemon race itself** — my first commit attempt ran into "nothing to commit, working tree clean" because the daemon swept the edit between my edit and my commit. This is KNOWN behavior (documented in AGENTS.md Critical Rules) and the standard countermeasure is `git status --short` immediately before `git add`/commit — I skipped that check. Recovered cleanly (verified-only-my-file amend), but the race was avoidable.

## e) WHAT WE SHOULD IMPROVE

1. **Task intake should encode prior knowledge.** The queue item text (written from the 23-35 report) described the wedge as unfixed, but AGENTS.md's dnsblockd section already documented the fix as shipped-and-deployed (corrected 2026-09-13), and TODO row 352 explicitly said "re-audit TODO row 40… the fix-half is done". A queue item whose own backlog already carries a newer status costs every assigned session a re-derivation. Improvement: when harvesting/scoping queue items, cross-check the AGENTS.md section cited in the item's own Source line and pre-annotate "likely already done — verify and close".
2. **Stale-item hygiene:** this row survived 4 days (09-13 re-audit → 09-17 close) as an open P1 despite being done — it inflated the P1 queue's apparent outage load. The docs-health AUDIT pass harvested around it without closing it.
3. **Verification-grade honesty in close annotations:** I recorded explicitly that the close is lock/source-level, not a live probe. Future verification-only closes should adopt this framing as a convention (which evidence class was checked), so downstream sessions know whether a runtime re-check remains.
4. **Sandbox command inventory:** the "command is not allowed for security reasons" rejections are useful feedback but silent discovery by trial-and-error is slow; a known-blocked list (systemctl, curl, …) in session context would have saved two round trips.
5. **The `--keep-going` / test-first loop was honored trivially here (docs-only), but the flake check took minutes in background — fine; no improvement needed, noted for completeness.**

## f) UP TO 50 THINGS WE SHOULD GET DONE NEXT

Ordered roughly by (impact × readiness), scoped to what this task's work surfaced or brushed against. Items 1-10 are dnsblockd-adjacent (in scope of what I noticed); 11-50 are queue-adjacent items I saw in passing in TODO_LIST — listed here only as a consolidated pointer, NOT re-scoped (the queue's own rows own the detail).

**dnsblockd / :9090 wedge family:**

1. Live-verify the deployed cached `/health` behavior on evo-x2 (cache-served responses; force a staleness window → fast 503) and close the standing 2026-09-13 post-deploy row. (sudo/curl-gated — user or privileged session.)
2. Arm + instrument the goroutine-dump runbook for the NEXT :9090 wedge: confirm `GOTRACEBACK=all` is actually set on the deployed unit (I did NOT check the unit's environment this session) and that `scripts/dnsblockd-goroutine-dump.sh` is reachable and non-interactive.
3. Cut a dnsblockd release tag carrying BOTH the OTLP scheme fix and the cached-/health fix (TODO row 334) — tag-pin vs follow-master decision for the SystemNix input rides on it.
4. Push or discard the 2 unpushed daemon docs commits in dnsblockd (`cf2b690`, `20afd82`) (TODO row 334).
5. Re-scope the :9090 root-cause hunt with the new fact that the wedge cannot be the healthHandler's inline DB probe anymore (that path is gone): if :9090 wedges AGAIN post-`94c9cb93`, the suspect set changes entirely (go-health background loop itself, stats middleware, journald echo). Update the runbook's hypothesis list accordingly.
6. The 2026-08-27 residual stands: deploy.sh's dnsblockd-restart block not firing despite an active bridge is unexplained (AGENTS: "do not rely on it"). Root-cause or delete the block.
7. Shorter freshness budget (6h vs 26h) for dense always-on span emitters like dnsblockd (TODO row 350).
8. Annotate the 02-52 signoz-flip report with the `/health` resolution (TODO row 351 — cheap, one paragraph).
9. Clean up the orphaned 724 MB dnsblockd tracking DB (TODO row 83, sudo-gated one-liner).
10. Verify dnsblockd dashboard auth with the sops token (TODO row 79).

**Queue-hygiene / process (what this run demonstrated):**
11. Re-audit remaining P1 rows whose Source sections may have been overtaken by AGENTS.md corrections (the row-30 pattern): each open P1 should carry a "last premise check" date.
12. Codify the "verification-only close" annotation convention in docs/CONTRIBUTING.md (evidence class: runtime vs lock/source vs doc-chain).
13. Add pre-queue-scoping rule: cross-check the item's cited AGENTS.md section before enqueueing.
14. Pre-commit/CI guard idea (from the daemon-race handling): nothing to enforce mechanically — the `git show --stat HEAD`-before-amend discipline is human/agent process; consider a crushrc hook or skill note.
15. The blocked-command sandbox inventory (see e.4): document which commands queue agents can expect to be rejected.

**Items seen in passing in TODO_LIST (pointers only — their own rows carry full detail):**
16. Deploy the stacked verified work (crush-hot-db deploy is the gating one; deploy-sudo-gated).
17. crush-hot-db first migration run needs a no-crush-session window + PSI before/after measurement (rows 19's REMAINING list).
18. `services.hot-db` Phase-2 module design (row 20) — crush wave folds into it.
19. SigNoz upstream trace gaps parts 3-6 (overview, PMA, papdashboard, hermes) (row 25).
20. BuildFlow fallback caches user decision + reap-list code half (row 31).
21. Attic-class follow-ups: ConditionPathIsDirectory sweep, dependency-cascade SKIP, VM test (row 32).
22. Satellite GOEXPERIMENT sweep, 21 repos (row 72).
23. Migrate dns-blocker.nix to the upstream dnsblockd module — split-brain convergence (row 181); this task reinforced that the split-brain still costs every dnsblockd session a two-repo evidence dance.
24. dns-blocker OIDC recovery runbook link + healthcheck consideration (row 190).
25. Git-identity follow-through: rewrite-vs-leave decision + per-repo .mailmap (row 411, owner decision).
26. DNS-blocker h2 block-page coverage + ALPN gotcha (row 460).
27. Guard/crash-forensics follow-ups incl. dnsblockd-cert-import (row 462).
28. Pocket ID regenerateSecretsFor guard (row 472).
29. Zone 6 churn-stop vs btrbk receive race — grace-window/re-arm design (row 29, fresh 2026-09-17 evidence).
30. btrbk-data ZERO complete received subvolumes / /data EIO inode (long-standing P0 referenced by AGENTS).
31. Hetzner StorageBox + BorgBackup offsite leg implementation (AGENTS backup section, TODO tracked).
32. flm staged v1.0.3 go-live — blocked on the owed reboot clearing the :52626 corpse (AGENTS).
33. llama.cpp mid-load spin regression — pin back or bisect gfx1150 upstream, then re-enable llama-rag (AGENTS 2026-09-14).
34. Owed reboot (corpses, driver wedges, recurred 2026-09-07) — run `nix run .#pre-reboot-check` first.
35. Per-service BTRFS subvolume doctrine ratification follow-through (2026-09-15 analysis doc triggers).
36. dnsblockd blocklist-load profiling (79s mapping.json) — folded into row 132's blind-spot list.
37. Signoz dashboard: verify `systemnix-pool-storage` provisioner spec stays API-normalized after next collector change.
38. Verify `signoz_traces_reporting{service="dnsblockd"}` still 1 after any future dnsblockd input bump (the flip's standing invariant).
39. Consider a Gatus check asserting the `/health` STALENESS behavior (503-fast) so a regression in the cached path alerts — needs a controlled staleness injection or an upstream test hook; park until the tag cut.
40. When the dnsblockd tag is cut: flip SystemNix input policy (tag-pin vs `?ref=master`) and record it in the pin-policy audit trail.
41. TODO row 335's live-verify row and row 352's re-scope row can now be partially answered from this session's evidence (regression tests CONFIRMED in pushed history) — annotate both rows to shrink their remaining scope to the runtime probe only.
42. `/health` load-shed under IO storms: once the tag is cut and live-verified, revisit whether the 30s stats-server WriteTimeout still leaks handler goroutines during a DB stall (the 2026-09-06 forensics noted CLOSE_WAIT pileup; the health fix does not touch the stats endpoints).
43. dnsblockd memory ceiling check: MemoryMax=4G/GOMEMLIMIT=3GiB against the 3.9M-entry blocklist + new code paths at next bump.
44. Tracking-DB size: 724 MB orphan (item 9) suggests the retention cleanup loop deserves a metric (`dnsblockd_tracking_db_bytes`) so orphans are visible without `du`.
45. Consider wiring the goroutine-dump script's existence into a flake check so a moved/renamed script fails loudly rather than being missing on the night it matters.
46. docs/services/dnsblockd runbook: add the 2026-09-17 close evidence (ancestry proof + source line) so the next wedge investigation starts from current truth.
47. AGENTS.md dnsblockd section: the "no dedicated release tag carries the fix yet" note (from 09-13) will go stale when item 3 lands — flag for the tag-cut session.
48. Re-check `signoz-coverage`'s dnsblockd entry `maxUpstreamGaps` ratchet is consistent with the lock after any input move (row 330's deploy-confirm work).
49. Queue pacing: P1 section now has at least one fewer phantom outage; re-triage remaining P1s against reality (several rows are sudo-gated and may belong in a user-gated section).
50. This report itself: harvest it in the next docs-health pass (annotate 02-52 report per item 8 at the same time).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Is a live runtime probe of the deployed `/health` (cache-hit latency + forced-staleness 503) wanted as a formal gate, or is lock-ancestry evidence sufficient to consider the fix "verified deployed"?** The standing TODO row asks for live confirmation, but executing it requires sandbox privileges I don't have — should the queue stop assigning me privilege-gated verification items, or should the row be re-classified as user-run?
2. **Should the SystemNix dnsblockd input flip to tag-pinned once the release tag (item 3) is cut, or stay on `?ref=master`?** The pin policy allows hard rev pins only with documented reasons; "carries two critical fixes not yet in any tag" is exactly such a reason today and exactly the thing that expires when the tag lands — the decision affects whether the 09-13 follow-up rows can ever close.
3. **For the NEXT :9090 wedge: do you want the goroutine dump taken AUTOMATICALLY (a systemd onFailure hook SIGQUIT-ing dnsblockd before the restart path) or kept manual per the runbook?** Automatic captures the evidence with zero operator latency but converts the wedge into a hard restart mid-diagnosis; the runbook currently assumes a human in the loop, and the 2026-09-06 incident showed the evidence was lost precisely because the runbook wasn't run before the restart.

---

**Evidence appendix:**

| Claim               | Command / source                                                        | Result                                                                |
| ------------------- | ----------------------------------------------------------------------- | --------------------------------------------------------------------- |
| Lock rev            | `jq -r '.nodes[.root.inputs.dnsblockd].locked.rev' flake.lock`          | `92aeedd0e1ea7147f4cce40b1e7ab4b5ac256062`                            |
| Fix ancestry        | `git merge-base --is-ancestor 94c9cb93 92aeedd0` (~/projects/dnsblockd) | OK                                                                    |
| Cache-first handler | `internal/server/health.go:98`                                          | `resp := s.healthProbe.CachedResponse()`                              |
| Regression tests    | `internal/server/health_test.go`                                        | burst/stale/degradeStale cases present                                |
| TODO close          | TODO_LIST.md row 30                                                     | `[x]`, dated, queue-ID attributed                                     |
| Commit              | `1e5ce035`                                                              | carries `Task-Queue-ID: 000001a0acaed79779d5dec4560d212e1f52` trailer |
| Repo health         | `nix flake check --no-build`                                            | all checks passed                                                     |
