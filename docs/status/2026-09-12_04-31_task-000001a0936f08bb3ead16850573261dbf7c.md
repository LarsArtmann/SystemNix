# Task-Queue Status Report — 000001a0936f08bb3ead16850573261dbf7c

**Date:** 2026-09-12 04:31 CEST
**Work item:** Miniflux SSO-only flip (`services.miniflux.disableLocalAuth`) — TODO_LIST.md, Priority 1
**Scope discipline:** report covers THIS task's work only; passing observations explicitly labeled.

---

## Executive Summary

The flip was **deliberately NOT executed**: the module's own go-live gate ("flip only AFTER one successful live SSO login") is **unsatisfied**. A full journal sweep of `miniflux.service` (entire retained history) proves the human prerequisite — the Settings → "Link your Pocket ID account" flow + one passkey SSO login — has **never happened**: the only `/oauth2/oidc/callback` hits in the entire journal are the two 2026-09-11 14:39 `user_already_exists` 400 collisions that motivated the runbook fix. Flipping now would be exactly the documented lockout class (password break-glass form removed while the OIDC callback still collides; upstream refuses unlinking once `DISABLE_LOCAL_AUTH=1`). The item is marked **BLOCKED on human-only steps** in TODO_LIST.md, following the InboxClean precedent (commit `8605c6bd`). No code/config changes were made; the repo was not left broken.

---

## a) Verification evidence (all from THIS run)

| # | Check | Result |
|---|-------|--------|
| 1 | Full journal sweep `journalctl -u miniflux` (all retained boots) for `oauth/oidc/callback/logged/already exists` | EXACTLY two callback hits ever: 2026-09-11 14:39:47 and 14:39:54 CEST, both `level=WARN msg="Bad Request" error="This user already exists."` HTTP 400, `user_agent` = macOS Chrome (the user's laptop, pre-link-flow). ZERO successful callbacks, ZERO OIDC login events after |
| 2 | Link-flow journal signature reasoning | The authenticated link branch of the same `/oauth2/oidc/callback` handler writes `openid_connect_id` and would appear as a NON-400 callback entry; a subsequent SSO login is another callback entry. Neither exists → link + passkey login not done |
| 3 | Option state | `modules/nixos/services/miniflux.nix:48-61` — `disableLocalAuth` defaults `false`, gate encoded in the description; no repo change since (clean tree), so the deployed unit carries no `DISABLE_LOCAL_AUTH` |
| 4 | Agent capability boundary | `sudo` is banned in this session (security policy confirmed: command rejected) — the psql cross-check of `users.openid_connect_id` is unavailable to any agent; the journal evidence is independently conclusive |
| 5 | Lockout-class warning re-confirmed | AGENTS.md Miniflux section + `docs/status/2026-09-11_15-09_*` §"Pre-flip trap": flipped early = password form gone + OIDC login still colliding = lockout; unlink auto-refused once flipped |

## b) NOT done (deliberately)

| # | Item | Why |
|---|------|-----|
| 1 | `disableLocalAuth = true` + deploy | Go-live gate unsatisfied (evidence above); executing it would risk total lockout — the module comment and TODO row both forbid exactly this |

## c) What unblocks the item (user steps only)

1. Browser at `https://rss.home.lan/` → log in with the break-glass password (sops `miniflux.yaml`).
2. Settings → "Link your Pocket ID account" → passkey → "account linked" flash (runbook `docs/services/miniflux.md`, no SQL).
3. Log out, then ONE live SSO login ("Sign in with Pocket ID" → passkey → dashboard).
4. Then the flip is one line (`disableLocalAuth = true`) + `nix run .#deploy`; a NEW successful callback entry in the journal is the proof-of-gate signal any future agent can check. Break-glass after the flip: set the option back to `false` + redeploy.

## d) Loop closed

TODO_LIST.md row left unchecked with a `— BLOCKED:` suffix carrying this run's evidence (convention per commit `8605c6bd`). This report is the point-in-time record; AGENTS.md untouched (its Miniflux section already documents the gate and link flow as enduring context).
