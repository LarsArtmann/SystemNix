# Task `000001a0db75dfafa622748a00da7cf19ff9` — RE-FIRE VERIFICATION (fire 2)

**Date:** 2026-09-26 04:44 CEST · **Scope:** re-dispatch verification protocol ONLY (queue item arrived already closed by fire 1 at 04:11-04:26, same task ID). No code touched.

**Headline:** the closure STANDS. Protocol steps 1-2 re-verified independently; step 3 harvested 8 un-landed §f follow-ups into the libraries + queue (the useful-re-fire delta). No drift found on either queue surface.

## 1. Footer commits + queue-surface closure

- Footer-bearing commit `3ada6e25` present (`git log --grep` → "docs: close @home-hermes 09-16 snapshot investigation — in-spec weekly", body carries the Task-Queue-ID footer).
- `TODO_LIST.md:52` `[x]` DONE + `docs/todo/storage.md:16` `[x]` DONE — mutually consistent, verdicts identical (IN-SPEC, different effective retention).
- `bash scripts/check-todo-system.sh` green after this run's appends.

## 2. Live spot-checks (independent of fire 1)

- **Snapshot inventory at 04:44 CEST** (`ls /mnt/btrfs-root/.snapshots`): BOTH prefixes carry exactly 4 dailies + 2 weeklies — `@`: 09-13, 09-20, 09-22..25; `@home-hermes`: 09-16, 09-20, 09-22..25. Matches the source-derived model exactly (mid-week hermes weekly 09-16 present, symmetric with `@`'s 09-13; the `09-22` dailies still present awaiting tonight's 23:00 run per the [watch] prediction — time-gated, correctly unverifiable now).
- **snapshots.nix re-read**: hermes block sets ONLY pool-side retention (`target_preserve_min 7d` / `target_preserve 14d 4w`) with the inheritance comment in-tree (snapshots.nix:246-263); the root instance carries `snapshot_preserve = "3d 1w"` with the corrected calendar-anchor comment (first-of-group rule, "4 dailies", re-verified in-source 2026-09-26). Fire 1's a7.1 comment fix is LANDED (provenance footnote: via `5befa617` + a daemon commit, not "diff in this commit" as the 04-26 report states — substance unaffected).
- The premise of the original item re-confirmed as a misread: `snapshot_preserve = "3d"` with no weekly matches the FORGEJO instance (snapshots.nix:336-340), not `@home-hermes`.

## 3. §f sweep — 8 un-landed follow-ups harvested (premises verified before queueing)

| §f | Item | Landed at |
| -- | ---- | --------- |
| f3/f4 | Pool-side continuity watch (hermes freshness gate rides through; FIRST pool-side hermes prune ~mid-Oct, never yet exercised) | storage.md `[watch]` + TODO_LIST |
| f5 | Forgejo "(~9 snaps)" comment fix — source semantics say ~4 at the 3-slot cadence (cadence verified: `*-*-* 05,13,21:40:00`) | storage.md `[blocked:user]` (live count sudo-gated) + TODO_LIST |
| f6 | Explicit `snapshot_preserve` on the hermes block vs documented inheritance | storage.md `[decision]` + TODO_LIST |
| f7 | Hermes restore drill (receive-and-diff; dedup-checked — referenced as a candidate in the 2026-09-23 07-47 report §38 but never tracked) | storage.md `[blocked:user]` + TODO_LIST |
| f8 | btrbk `transaction_log` persistence | storage.md `[decision]` + TODO_LIST |
| f9 | Codify premise spot-verification at queueing time | pipeline.md + TODO_LIST |
| f10 | Re-fire counts in tq verify output (go-taskqueue upstream) | pipeline.md + TODO_LIST |
| f11 | `btrfs-verify-snapshots` comment: gate checks NEWEST age per prefix only (verified at snapshots.nix:836-856) | storage.md `[ready]` + TODO_LIST |

f12 was already landed (AGENTS.md:660 "4 dailies + 2 weeklies"); f1/f2 are the tracked `[watch]` row — no duplicates created.

## 4. Verdict

Item stays CLOSED. Fire 2's delta = the §f harvest above + these two independent live confirmations. The [watch] row (prune predictions at the 09-26/09-27 23:00 runs) remains the only open leg, correctly time-gated.
