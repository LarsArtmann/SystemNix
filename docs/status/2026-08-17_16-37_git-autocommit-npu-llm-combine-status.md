# Status Report — 2026-08-17 16:37

**Session:** Git Auto-Commit Reactivation × Background NPU LLM (FastFlowLM) — _"combine these too"_
**Repos touched:** SystemNix, go-commit (upstream), projects-management-automation (upstream)

---

## a) FULLY DONE ✅

| #  | Work                                                                                                                                                                                                                                                                           | Evidence                                                |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- |
| 1  | **FastFlowLM Nix package** (`pkgs/fastflowlm.nix`) — fetchurl v1.0.1 tarball + autoPatchelfHook + protobuf_32 (XRT swemu/hwemu libs), wrapper moved to `$out/bin/flm` with layout self-check                                                                                   | Builds; `nix build .#fastflowlm` green                  |
| 2  | **FastFlowLM NixOS module** (`modules/nixos/services/fastflowlm.nix`) — socket-activated `:52625` → `systemd-socket-proxyd` proxy → backend `:52626`, 1h idle TTL (every-5min check), `ioTier.background`, `StateDirectory=/var/lib/fastflowlm`, video group, MEMLOCK=infinity | Module evaluates; toplevel build green                  |
| 3  | **Ports** `fastflowlm = 52625`, `fastflowlm-backend = 52626` in `lib/ports.nix` (port-collision check passes)                                                                                                                                                                  | `ports` helper no throw                                 |
| 4  | **ai-models path** `/data/ai/models/fastflowlm` + tmpfiles rule in `ai-models.nix`                                                                                                                                                                                             | Eval                                                    |
| 5  | **Gatus health checks** (2: failure + memory pressure) — deliberately NOT probing `:52625` (keepalive trap)                                                                                                                                                                    | `gatus-config.nix`                                      |
| 6  | **system-health monitoring** — `fastflowlm` added to monitoredServices list                                                                                                                                                                                                    | `system-health.nix`                                     |
| 7  | **Auto-commit reactivated** — `mode = "passive" → "active"` in `configuration.nix` (the 4-layer tarball defense was already deployed)                                                                                                                                          | configuration.nix:682                                   |
| 8  | **Upstream go-commit patch** — `DefaultChainFromEnv()` now reads `OPENAI_BASE_URL` + `OPENAI_MODEL`; SSRF bypass for env-sourced (trusted infra) URLs; tests updated (1 new test + 1 new fuzz corpus + TestValidateBaseURLTrusted)                                             | Committed & pushed upstream: `22f0e4c`, tagged `v0.8.0` |
| 9  | **Build verification end-to-end** — `nix flake check --no-build` all pass; `nix build .#nixosConfigurations.evo-x2.config.system.build.toplevel` green with the concurrent bug-fix diff applied                                                                                | EXIT=0                                                  |
| 10 | **AGENTS.md documentation** — FastFlowLM section added (architecture, socket-activation rationale, Gatus keepalive trap, PMA env wiring, obsolete hand-install cleanup)                                                                                                        | AGENTS.md §FastFlowLM                                   |

## b) PARTIALLY DONE 🟡

| # | Work                                                                                    | Status / Blocker                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| - | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | **"Combine": PMA auto-commit → FastFlowLM (NPU LLM) commit messages**                   | `extraEnvironment` (OPENAI_BASE_URL/OPENAI_MODEL) is in PMA module, but only honored by **go-commit v0.8.0+**. The Go proxy (**proxy.golang.org**) returns 404 for `v0.8.0` — tag pushed 2026-08-17 ~11:00 UTC, still 404 at 16:00. SystemNix's `go-commit` input deliberately kept at `e5040b3` (v0.7.0-era master) so the build stays hermetic. **Fallback until bump:** daemon uses heuristic commit messages.                                                 |
| 2 | **Upstream projects-management-automation bump** (go.mod → v0.8.0 + vendorHash refresh) | Committed + pushed (`79e57f6e`, `9d9edfd3`) then **reverted** when the Go proxy 404 made the prepared-source fail. Local Nix build needed the proxy to resolve v0.8.0 → cannot ship until proxy propagates.                                                                                                                                                                                                                                                       |
| 3 | **Concurrent-session bug fixes** on my FastFlowLM code                                  | A concurrent session fixed 3 real bugs I shipped: (1) idleCheck "stopped the socket" — kills re-activation; now stops only proxy+backend. (2) flat layout → unit ExecStart pointed at nonexistent `$out/bin/flm` → 203/EXEC → start-limit; fixed via `$out/bin` + `mkdir -p` + layout self-check. (3) `ProtectHome` blocked `$HOME` — redirected HOME to `StateDirectory`. These are **uncommitted** in the working tree and were verified in the toplevel build. |
| 4 | **VM test** for the enable-flip / PMA-mode change                                       | Existing `test-pma-identity.nix` covers identity only. No test asserts `mode = "active"` produces commits in a sandbox.                                                                                                                                                                                                                                                                                                                                           |

## c) NOT STARTED ⬜

| # | Work                                                                                                                                                        |
| - | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | **Deploy** (`nix run .#deploy`) — user action, not run this session. The deploy is what actually makes `mode="active"` + FastFlowLM service real on evo-x2. |
| 2 | Final Go-proxy propagation → `nix flake update go-commit` + PMA input → rebuild → the "combine" becomes live.                                               |
| 3 | Cleanup of obsolete hand-install (`~/.local/share/fastflowlm/`, `~/.local/bin/flm`, LD_LIBRARY_PATH exports) after deploy proves stable.                    |
| 4 | Homepage tile for FastFlowLM (deliberately skipped — any `/v1/models` probe is a keepalive).                                                                |
| 5 | `warmCalendar` pre-load option (design doc §7, optional extension).                                                                                         |

## d) TOTALLY FUCKED UP! 🔴

| # | What                                                                                                                                                                                                                                                                                                                                                                              | Root Cause                                                                                                                                                                                                        | Recovery / Lessons                                                                                                                                                                                                                                                                                                                                                                                                                        |
| - | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | **Go proxy propagation never happened within session** (~5h). The ENTIRE "combine" hinged on `v0.8.0` being resolvable — it silently blocked the end goal after all upstream work was committed/pushed/tagged.                                                                                                                                                                    | Tag pushed but `proxy.golang.org` keeps 404 with `cache-control: max-age=1451`. No prod to wait through timed out.                                                                                                | **Lesson 1:** Never gate the final integration on an unpushed-to-proxy Go tag _within a single session_. **Lesson 2:** The Go-proxy-lag failure mode is NOT fast-feedback — it silently poisons every downstream Nix FOD build. **Lesson 3 (process):** I burned ~1.5h fighting the proxy lag (polled, waited 10-15 min, tried replace directives, pseudo-versions, git+ssh replace) before pivoting to "keep v0.7.0 + env vars dormant." |
| 2 | **My FastFlowLM module had 3 shipped bugs** — (a) idle TTL "stopped the socket" (kills the whole re-activation mechanism — the ONLY way it comes back), (b) `ExecStart` pointed at `$out/flm` flat layout → status=203/EXEC → start-limit-hit blocks deploy activation, (c) `ProtectHome=true` + `WorkingDirectory=/home/lars` → 200/CHDIR; flm's `$HOME/.config` writes blocked. | Rushed writing the module without runtime verification against the real binary's behavior (HOME write needs, PATH conventions of systemd, tarball layout). A **concurrent session** caught all three — I did not. | **Lesson 4:** The design doc (2026-08-15) explicitly listed "ProtectHome = false — verify at build" — I shipped `harden {}` default instead. **Lesson 5:** `meta.mainProgram`/`lib.getExe` demand `$out/bin/<name>` — the flat-tarball extraction of `cp -r . $out/` breaks it. **Lesson 6:** never `systemctl stop <unit>.socket` in an idle-TTL when the socket is the cold-start trigger.                                              |

## e) WHAT WE SHOULD IMPROVE 🔧

1. **FastFlowLM lifecycle tests before deploy** — a `systemd` integration test (start service, curl `/v1/models` with cold load, verify idle-unload releases RSS) would have caught 2 of the 3 shipped bugs. The design doc's §6 validation plan (steps 3-7) was never executed.
2. **Go-proxy-lag awareness as a first-class risk** in every "publish a tag this session" plan. The `go-release` skill covers tag → pkg.go.dev verification; SystemNix's AGENTS.md should get a gotcha entry: _"a freshly tagged LarsArtmann Go module is NOT consumable until proxy.golang.org propagates (15 min – hours); consumer flake bumps must be staged separately."_
3. **Don't put the auto-commit daemon in the loop for infra work** — the daemon committed my mid-session state (`541a6a1a`) with a "complete" message while work was actively in progress. That timing hides partial states.
4. **Pre-deploy-check §12 (ExecStart existence)** — excellent addition from the concurrent session; should be a permanent pipeline layer, not a one-off script edit. It's currently uncommitted.
5. **PGP/attribution in the daemon** — the auto-commit commits are authored `LarsArtmann <larsartmann@users.noreply.github.com>`, not the gitIdentity "Lars Artmann" — fine, but worth recording so nobody "fixes" it.
6. **Ask 3-questions minimum before declaring a cross-repo integration "done"** — the combine is 80% there but the last 20% is stuck on an external queue with no owner.

## f) Top 25 things to get done next

| #      | Task                                                                                                                                                                | Impact       |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| ~~1~~  | ~~**`nix run .#deploy`** — activates `mode="active"` + FastFlowLM on evo-x2 (the user action; everything else is parked behind it)~~ done at `541a6a1a`, `25790607` | ~~Critical~~ |
| ~~2~~  | ~~Poll proxy.golang.org for `v0.8.0` (24-min cache semantics), then `nix flake update go-commit` + PMA input + rebuild~~ done at `7d58bafa`                         | ~~Critical~~ |
| ~~3~~  | ~~Re-apply upstream PMA go.mod → v0.8.0 + vendorHash bump (already authored, needs re-push after proxy clears)~~ done at `7d58bafa`                                 | ~~High~~     |
| ~~4~~  | ~~Commit the concurrent-session bug fixes (idleCheck socket fix, `$out/bin` layout, HOME→StateDirectory) + pre-deploy-check §12~~ done at `e5edf0bd`                | ~~High~~     |
| ~~5~~  | ~~Run the design-doc §6 validation plan: cold load, idle unload, crash restart, GC-safety of package~~ done at `99301327`, `c6f91f33`                               | ~~High~~     |
| 6      | Delete hand-install `~/.local/share/fastflowlm/`, `~/.local/bin/flm`, LD_LIBRARY_PATH exports after deploy proves stable                                            | Medium       |
| ~~7~~  | ~~Add `go-commit v0.8.0` Go-proxy-lag gotcha to AGENTS.md (Nix & Nixpkgs section)~~ done at `25790607`                                                              | ~~Medium~~   |
| 8      | Verify PMA auto-commit daemon actually produces a commit on the watched tree post-deploy (functional smoke test, not just liveness)                                 | Medium       |
| 9      | Add `systemd integration test` for fastflowlm (socket-activation → cold load → idle unload)                                                                         | Medium       |
| ~~10~~ | ~~Add pre-deploy-check §12 as a permanent eval-time or CI layer~~ done at `e5edf0bd`                                                                                | ~~Medium~~   |
| 11     | Update `docs/gotchas-archive.md` with the 203/EXEC + socket-stop + ProtectHome triple-whammy narrative                                                              | Medium       |
| ~~12~~ | ~~Decide FastFlowLM homepage tile policy (skip = current; document why)~~ done at `0d8a58ca`                                                                        | ~~Low~~      |
| 13     | Evaluate `warmCalendar` (pre-load 09:00) once cold-start latency annoys                                                                                             | Low          |
| 14     | Verify immich.nix progressive-JPEG + dns-blocker-config "Continue to site" changes that rode along in commit 541a6a1a                                               | Medium       |
| 15     | Check `docs/external-contributions/` untracked dir — new doc chain from concurrent session                                                                          | Low          |
| ~~16~~ | ~~Update FEATURES.md: FastFlowLM (DONE), PMA active mode (DONE), auto-commit→NPU combine (PARTIAL)~~ done (docs-health pass 2026-08-18)                             | ~~Medium~~   |
| ~~17~~ | ~~Update ROADMAP.md: close "FastFlowLM NPU LLM server" TODO (design complete → shipped)~~ done (docs-health pass 2026-08-18)                                        | ~~Medium~~   |
| ~~18~~ | ~~Verify the 3 concurrent fixes didn't break the idle-TTL unit's `After/Wants` graph (proxy → backend)~~ done at `99301327`, `c6f91f33`                             | ~~Medium~~   |
~~| 19     | Decide whether to keep `OPENAI_API_KEY=local` dormant env vars or strip them until v0.8.0 lands~~ moot — go-commit v0.8.0+ landed long ago; env wiring active (PMA = heaviest flm consumer per usage forensics 2026-08-22)                                                                     | Low          |
~~| 20     | Check that Gatus FastFlowLM checks don't fire during normal idle (absence of `system_service_state_failed` = green)~~ done — live since 2026-08-18 (system-health state metrics; zero idle false-fires since)                                                 | Medium       |
| 21     | Quick `systemd-analyze verify` on the 4 fastflowlm units post-deploy                                                                                                | Medium       |
| ~~22~~ | ~~Verify `flm serve` command-line flags actually match module ExecStart (`--host`, `--port`, `--pmode` exist in v1.0.1)~~ done at `99301327`                        | ~~High~~     |
| ~~23~~ | ~~Test the proxy cold-load wait (300s deadline vs 6min TimeoutStartSec) against the real cold-load time (1-3 min measured)~~ done at `99301327`                     | ~~Medium~~   |
| 24     | Add SIGTERM handling check — flm on idle-stop must release NPU cleanly (no leaked /dev/accel0 handle)                                                               | Medium       |
| 25     | After v0.8.0 lands: functional test that PMA commit messages are LLM-generated (not heuristic)                                                                      | Medium       |

## g) Questions I cannot answer myself

1. **Go-proxy propagation delay**: v0.8.0 was tagged at ~11:00 UTC today and the proxy still 404s at ~16:00. Is there any environment-level reason the `LarsArtmann` org's modules are slow/blocked on proxy.golang.org (private-repo visibility, sumdb, GONOSUMDB/GOPRIVATE on the CI/build host, org-level tag protection)? A 5-hour 404 for a public repo is abnormal. — _No: this is entirely in Google's cache semantics; I cannot influence or introspect it._
2. **Deploy timing**: Do you want me to run `nix run .#deploy` now (it activates the auto-commit + FastFlowLM in one shot; build is green), or should the deploy wait until the go-commit v0.8.0 bump is also in the same switch so the "combine" activates atomically?
3. **FastFlowLM model**: the module binds `qwen3.6-moe:35b-a3b` by default. Is that model already pulled on `/data/ai/models/fastflowlm` (the manual install pulled it), or must `flm pull` run at first deploy? If it's missing, the service cold-loads fine but returns model-not-found — I need to know whether to add a `flm pull` oneshot to the module.
