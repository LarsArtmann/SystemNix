# Task `000001a0db75dfafa622748a00da7cf19ff9` — @home-hermes 09-16 snapshot investigation

**Date:** 2026-09-26 04:11 · **Verdict: IN-SPEC (different effective retention) — no anomaly, no action needed on snapshots/btrbk.**

The queue item asked which of three explanations holds for `@home-hermes.20260916T2300` surviving 9-10 days against "`snapshot_preserve 3d`, no weekly in its config".

## a — Findings

| # | Finding | Evidence |
| - | ------- | -------- |
| a1 | **The premise misread the config.** `@home-hermes` sets only POOL-side retention (`target_preserve_min "7d"` / `target_preserve "14d 4w"`); LOCAL retention inherits the root instance's volume-level `snapshot_preserve = "3d 1w"` (snapshots.nix:227; the block comment states the inheritance verbatim, snapshots.nix:250-252). The cited "`3d`, no weekly" shape matches the FORGEJO btrbk instance (`snapshot_preserve = "3d"`, snapshots.nix:337) — the likely source of the misread. | `sed -n '226,259p'` + `:336,337p` snapshots.nix at HEAD `4c770801` |
| a2 | **09-16 is the previous calendar week's first-of-week snapshot — in-spec.** Calendar-anchor doctrine (source-verified 2026-09-25, snapshots.nix:219-225): `1w` keeps the first snapshot of each of the TWO calendar weeks covered (weeks start Sunday). At the Sep 25 23:02 prune: previous week = Sun Sep 13–Sat 19 → hermes' first snapshot in that week is 09-16 (subvol created 09-15, so its first-ever snapshot became that week's weekly — why it is mid-week, not Sunday); current week → 09-20. Root `@` shows the symmetric pair: `09-13` + `09-20`. | local sets (identical 6-entry shape: 2 weeklies + dailies 09-22..25) |
| a3 | **Parent hold RULED OUT.** The Sep 25 23:02 incremental send parent was `@home-hermes.20260924T2300` (not 09-16), and the pool is current through `@home-hermes.20260925T2300` — nothing older is needed as a send parent. | `journalctl -u btrbk-root --since "2026-09-25 22:55"`: `btrfs send -p …/@home-hermes.20260924T2300 …/@home-hermes.20260925T2300` |
| a4 | **Prune gap RULED OUT.** Same journal second (23:02:52): `btrfs subvolume delete` of BOTH `/mnt/btrfs-root/.snapshots/@.20260921T2300` AND `@home-hermes.20260921T2300` — @home-hermes pruning runs in lockstep with root `@`, and its missing locals (09-17, 09-18×3, 09-19, 09-21 — all present on the pool) prove the delete phase has been converging all along. | journal + `ls` of `.snapshots` vs `/mnt/pool/backups/root` |
| a5 | **Falsifiable closure queued as [watch]** (storage.md): both `@home-hermes.20260916T2300` and `@.20260913T2300` are predicted to be deleted at the 2026-09-27 23:00 run — the first run inside calendar week Sep 27–Oct 3, when the Sep 13–19 week falls out of the `1w` window. Ages at death: `@` 09-13 = 14d (2w sharp, doctrine-conformant), hermes 09-16 = 11d (mid-week weekly lives less — in-spec). Pool copies of both persist much longer by design (`14d 4w` hermes / `all` @). If either survives, the calendar-anchor doctrine is wrong and the item reopens. | prediction + verification command in the [watch] row |

## b — Queue surface closure

`TODO_LIST.md` storage row marked `[x]` with the compact verdict; `docs/todo/storage.md` row 16 replaced with the full DONE entry + the new `[watch]` confirmation row appended directly below it. `check-todo-system.sh` green post-edit.

## c — Not touched

- snapshots.nix (config is correct as-is; the inheritance comment already documents the effective retention).
- The other open storage rows (Docker sizing stale echo already closed by prior runs; /data repair, mystery snapshot remain `[blocked:user]`).
