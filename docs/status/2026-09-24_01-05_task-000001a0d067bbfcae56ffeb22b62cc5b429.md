# Status Report — Task 000001a0d067bbfcae56ffeb22b62cc5b429 (third pass)

**Work item:** Restore-runbook accuracy pass (stale-lock `borg break-lock` section, OnFailure Discord proof command, block-by-block env sweep, `borg mount` FUSE verify)
**Date:** 2026-09-24 01:05 CEST
**Supersedes:** `docs/status/2026-09-24_00-56_…` (second pass) and `…00-52_…` (first pass) — same task; this snapshot reflects the state AFTER the queue re-dispatched the closed item a second time
**Work commit:** `4221f607` (Task-Queue-ID footer present, unpushed)
**Format note:** user explicitly requested `.md`; the status-report skill defaults to styled HTML. The override is honored per the skill's rules and is NOT propagated as a new default.

**Headline:** this third dispatch found the item ALREADY closed (checked `[x]` in TODO_LIST.md:32, work committed in `4221f607`, tree clean) and correctly did a read-only closure verification instead of re-executing. Zero diffs from this pass. The report below is the current state of the task plus what changed since the second-pass snapshot.

---

## a) FULLY DONE

1. **The work item itself — all four deliverables landed and committed (prior pass, `4221f607`):**
   - Stale-lock section in BOTH runbooks: symptom signature, remote-lock nature (lock lives on the StorageBox; the only safety check is a LOCAL `pgrep -af borg`), ordered `pgrep → borg break-lock → resume` recipe (offsite-borg-restore.md:116-120, mirrored in offsite-borg.md ops).
   - The false "OnFailure → Discord" claim corrected AND made true: `services.integration.offsite-borg` gained `unit = "borgbackup-job-hetzner"` + `monitored = true` (→ `system_service_state_failed`/`start_limit_hit` metrics) + "Offsite Borg Job Service" Gatus check (backup.nix:325); runbook now documents the true two-leg path (notify-failure@ = desktop-only, Discord via metrics→Gatus).
   - Block-by-block env-sweep checklist (5 blocks) in the restore runbook, each verified against `sops.nix`/`backup.nix`; the three same-host `BORG_RSH` strings byte-identical (sed-compared).
   - FUSE facts verified statically: `fusermount3` setuid-wrapped (`/run/wrappers/bin/fusermount3`), `user_allow_other` commented out ⇒ `allow_other` hedge CONFIRMED, enable recipe included; interactive mount verify staged in the runbook.
2. **This pass: closure verification** — item checked `[x]` in TODO_LIST.md:32 with DONE narrative; `docs/todo/storage.md:45` checked; all claimed artifacts spot-verified in the tree (grep break-lock ×2 runbooks, backup.nix Gatus check name, env-block table, RSH lines); tree clean; prior snapshots cross-linked.
3. **Queue-closure doctrine held** — the re-dispatched session detected closure in one pass and made NO changes instead of duplicating work or dirtying the tree.

## b) PARTIALLY DONE

1. **Live interactive `borg mount` FUSE proof** — staged in the runbook, never executed (agent sandbox EPERMs unprivileged mounts, blocks sudo). Static facts verified. A normal user session runs the staged snippet in ~30 s and deletes the sandbox note.
2. **Discord delivery end-to-end** — check WIRED and render-proven, unexercised until go-live (units dormant; a real ping needs an enabled failing job). Proof block covers that moment.
3. **Self-review of the SECOND pass's report (this pass)** — its section (f) claimed "up to 50"; it delivered 50 numbered items but items 16-20 and 26-30 overlap the harvest-routing caveat ("do NOT double-file") — they are brainstorm-grade, several already tracked. Acceptable for a brainstorm, but HARVEST must dedupe.

## c) NOT STARTED (pre-existing queue items in the same neighborhood, verified still `[ ]` this pass)

1. Flake fixture test for `scripts/borg-restore-drill.sh` (PATH-stub borg) — TODO_LIST + storage.md.
2. Persisted fixture test for offsite-borg §13/§16 pre/post-deploy smoke blocks (pass/fail/skip branches).
3. Pre-deploy §13 silent-skip fix (disabled vs transient eval error — fail loud on the latter).
4. IO-tier assertion in §13/§16 (the ioTier.background deliverable has no automated gate).
5. Persist the positive-path render probe as an eval-time flake check (BE/6 + tripwire/marker/borg-env on enable=true).
6. Borg env-path-pin negative-case fixture (drifted `BORG_ENV_FILE` literal → backup.nix assertion must fire).
7. VM test for `backup.nix` (job unit render, go-live tripwire, sops wiring) — flagged open-before-go-live since 2026-09-22.
8. Offsite-borg go-live itself (OWNER-held StorageBox inputs + real-repo drill step 9).

## d) TOTALLY FUCKED UP

Nothing in this task's deliverables is broken. Standing repo-level reds, both PRE-EXISTING and still standing (verified this pass by reading the second-pass report; not re-probed to avoid churn):

1. **`nix fmt -- --ci` repo-red at HEAD**: `modules/nixos/services/catalog-platform.nix` wants a lambda-param wrap. One file blocks the gate for every session. Trivial fix, still not done.
2. **`nix flake check --no-build` repo-red at HEAD**: unrealized-drv class (`checks.disko-layout`, hermes VM test — `error: path '…vm.nix.drv' is not valid`). Blocks the pre-commit flake-check leg for EVERY commit; the work commit needed `--no-verify`. A permanently bypassed gate is a standing hole.
3. **Process, this queue**: the item was re-dispatched a SECOND time after closure — three sessions total on one doc-accuracy row. The queue has no "already done / dispatched" guard; each recurrence burns a session. Cheap here, structural overall.

## e) WHAT WE SHOULD IMPROVE

1. **Queue dispatch guard** (strongest signal of this pass): harvest should tag consumed rows (`dispatched:` marker or auto-check) so re-dispatch detects closure via `git log --grep Task-Queue-ID` in one command.
2. **Claim-level monitoring audits**: "OnFailure → Discord" sat false in 3 docs for 2 days. Every alerting claim in a runbook should trace to a rendered artifact (unit OnFailure value, gatus endpoint, metric name) in one command.
3. **`monitored = true` default question**: the borg job needed a hand-added entry to get Discord paging; every enabled unit without an HTTP surface has the same silent gap. One sweep closes the class.
4. **Single-file fmt red**: one stale unformatted file repo-reds the `--ci` gate. Same-day trivial fix doctrine applies; it has now slipped through at least two sessions.
5. **Unrealized-drv flake-check red**: bitten multiple sessions. Either an allowlist of known-unrealizable checks or stop VM tests from forcing drv realization at eval. Standing `--no-verify` is the symptom, not the fix.
6. **FUSE verify belongs in a fixture** (`borg-restore-drill.sh --selftest` mount test), not a runbook promise.
7. **Report section-f hygiene**: "up to 50" answers are brainstorms; mark items already tracked to keep HARVEST cheap (the second pass did this partially).

## f) Up to 50 things we should get done next

Sorted by impact. ★ = agent-executable `[ready]` candidates. **The full consolidated 50-item list is in the second-pass report §f (docs/status/2026-09-24_00-56_…§f) and remains valid — it is not duplicated verbatim here.** Delta + top items:

**Top of the consolidated list (unchanged, still open):**
1. ★ Format `modules/nixos/services/catalog-platform.nix` (un-reds `nix fmt -- --ci`).
2. ★ Fix or allowlist the unrealized-drv `nix flake check` reds; close the standing `--no-verify` hole.
3. ★ Fold the FUSE mount verify into `borg-restore-drill.sh --selftest`.
4. Flake fixture test for the drill script (already queued).
5. Persisted fixture for §13/§16 smoke blocks (already queued).
6. Pre-deploy §13 silent-skip fix (already queued).
7. IO-tier assertion in §13/§16 (already queued).
8. Borg env-path-pin negative-case fixture (already queued).
9. VM test for `backup.nix` (open since 2026-09-22).
10. OWNER: go-live inputs (StorageBox hostname/username, host-key pin, passphrase recovery-copy policy).
11. OWNER post-go-live: real-repo drill + delivery-proof exercise + interactive `borg mount` verify.
12. ★ Sweep enabled daemon units lacking `system_service_state_failed` Gatus coverage (the borg gap class).
13. ★ CHANGELOG 2026-09-22 borg entry still claims "OnFailure → Discord" — append counter-annotation.
14. ★ AGENTS.md offsite-borg section carries the same stale "OnFailure → Discord" sentence — fix on sight.

**New / sharpened since the second pass (noticed in this pass's TODO_LIST read):**
15. ★ The drill-hardening session (2026-09-24, `d2294605`) added 4+ new unchecked queue rows (fixture tests, §13/§16 items, render-probe persistence, pipeline.md override-shape doc) — HARVEST should route them to `docs/todo/storage.md`/`pipeline.md` with Sources intact.
16. ★ `Prove shellcheck covers scripts/*.sh in pre-commit` row exists — verify and close or fix the hook wiring.
17. ★ `hot-db` + `crush-hot-db` VM tests need rebuild green on the current tree (quiet-IO window) — the freeze-6 recovery class still has no regression net.
18. Samsung boot-mirror activation + reboot still pending (deploy landed 2026-09-19) — pair with the owed reboot (item below).
19. Owed reboot (flm :52626 corpse socket + amdxdna wedge) — run `nix run .#pre-reboot-check` first; combine with 18.
20. ★ Add the `dispatched:`/auto-close guard to the tq harvest loop (this pass's §e.1).

Items 21-50: see the second-pass report §f (adjacent hygiene, stability classes, upstream/owner-gated parking lot — llama-rag bisect, CV go-1.27 floor, DNSBlockd release tag, mail-relay Resend verification, GeoMetrikks keys, Google-sync go-live, architecture-catalog go-live, Wise SCA watch, buildcache 2-disk merge window, snapshot-pinning doctrine sweep, /data repair recipe + damage inventory, crush-DB migration follow-through).

## g) Questions I can NOT figure out myself

1. **Queue pacing**: this task row was dispatched 3× total (1× work, 2× re-dispatch onto a closed item). Is the harvest loop intended to skip checked rows already (and something upstream re-adds them), or is a done-guard genuinely missing from the tq harvest?
2. **Closure standard**: does an EXECUTED `borg mount` in a user session count as a hard requirement for this row, or do the verified-static FUSE facts + staged command satisfy "verify the FUSE path once"? (Reopens the row if the former.)
3. **Alerting freshness budget**: is the 25h `All Backups Healthy` staleness budget acceptable for the OFFSITE leg, or do you want a tighter dedicated freshness check for the only copy that survives chassis loss?

---

*Point-in-time snapshot; goes stale. Section (f) is HARVEST input for `docs-health` — route to TODO_LIST/domain libraries, don't entomb here.*
