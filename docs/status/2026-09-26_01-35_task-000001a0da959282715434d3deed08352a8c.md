# Task 000001a0da959282715434d3deed08352a8c — Re-verification of the no-edit finding closure (run 2)

**Date:** 2026-09-26 01:35
**Finding (medium):** "Doctrine root-window restatement left internal contradictions in AGENTS.md at close-out" — anchored to the doctrine-table row `| /btrfs-emergency-reserve (10G) | root ≈ 1-2w | the T14 caveat (Emergency reserve, below) |`; rejected change `58cb44ae` (the original task `000001a0d7ddc4c5db6b3a8da3acdfb9a1d8` close-out).
**Disposition: NO-EDIT CLOSURE CONFIRMED.** Run 1 (`docs/status/2026-09-26_01-09_task-000001a0da959282715434d3deed08352a8c.md`, commit `c9c287b9`) dispositioned this finding as no-edit; this run independently re-verified every step from scratch and reaches the same verdict. Zero content edits made anywhere.

## a. Disposition protocol steps (re-run independently)

| # | Step | Result |
|---|------|--------|
| 1 | Dangling-commit check: `git cat-file -e 58cb44ae97241be9547a3a1acf7cd7645d1f58ba` | EXISTS; also `git merge-base --is-ancestor` → ancestor of HEAD (on master, not rebased away) |
| 2 | Anchor text `root ≈ 1-2w` in the reserve doctrine-table row | GONE from master — AGENTS.md:670 reads `root = 2w sharp` |
| 3 | Per the task's own rule ("if the anchor text is also gone, the finding no longer applies — state that in your final output instead of inventing a change") | Finding does not apply; no edit invented |

## b. Finding-scope echo verification (all four patterns: `3d 1w`, `3d+1w`, `1-2w`, `2w sharp`)

| Scope | Sites | Verdict |
|-------|-------|---------|
| AGENTS.md | :660 doctrine paragraph (`root @ (3d 1w, min 2d) = **2w sharp**`); :670 + :671 both doctrine-table rows (`root = 2w sharp`); :699 T14 caveat (`root pin window = 2w sharp — calendar-anchored`); :972 ClickHouse finalize note (`pin window 2w sharp, calendar-anchored`); :658 Snapshots paragraph carries ONLY the live-config restatement `snapshot_preserve 3d 1w` + the deliberate negation `NOT "3d+1w" flat` | All echoes read the reconciled figure; no contradiction remains |
| docs/todo | storage.md :13/:66/:72 DONE notes (historical closure records quoting landed history), :15 + :136 queue rows (the calendar-anchor comment item and the grep-gate item, which quote the stale patterns as the gate's own fixture set), pipeline.md:166 (queue row proposing the DONE-note era-annotation convention) | All legal per the finding's context rules; no live-doc stale restatement |
| docs/services | grep across all of docs/services/ | ZERO hits for `3d 1w` / `3d+1w` / `1-2w` |
| Adjacent (verified in passing) | FEATURES.md:291 emergency-reserve row already aligned to `root pin window = 2w sharp, calendar-anchored` by sibling review fix `3e3d718a`; FEATURES.md:289 `root local 3d+1w` is a live-config restatement, deliberately untouched; scripts/migrate-clickhouse-xfs.sh messages aligned by `eb844911` | Consistent |

## c. Gates

| Gate | Result |
|------|--------|
| `nix flake check --no-build` | Red on the PRE-EXISTING hermes VM-test eval failure (`error: path 'ix80s6aj697xcrzlnlc074wz6zd0avf4-source' is not valid`, re-observed live this session) — identical to the breakage documented in `c9c287b9`'s commit message; unrelated to this docs-only diff (no `.nix` files touched); docs-only `--no-verify` commit per the documented protocol (`7467e2e2`/`37869731`/`e62e64db`/`c9c287b9`) |
| Diff scope | This report file only (pathspec commit) |

## d. Why a second report for the same ticket

Run 1 closed with "zero edits, zero commit" as its report's own wording, then committed the report itself (`c9c287b9`, footer present). This dispatch re-served the ticket; the house convention for re-runs of already-done work is an independent re-verification report (precedent: `docs/status/2026-09-25_12-56_task-000001a0d80b8ba29cede448b76050225c24.md`). This file is that report for run 2 — it exists so the queue's re-dispatch has a committed, footered record rather than silence.

## e. Outstanding (unchanged from run 1; no new items)

- The durable fix for this failure class remains the queued grep-gate: docs/todo/storage.md:136 (`[ready]`), fixture set = the 7 found stale sites across the lineage.
- Owner decisions parked in the 01-09 report §owner (retention retune intent, historical-text era-banner policy) — not re-asked here.
