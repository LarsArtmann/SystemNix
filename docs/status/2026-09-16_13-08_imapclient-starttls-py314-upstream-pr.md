# Status Report: imapclient starttls() Python 3.14 Upstream PR

**Date:** 2026-09-16 13:08 CEST
**Session scope:** Read NixOS/nixpkgs#563652 + mjs/imapclient#662, decide and execute on filing an upstream fix PR.
**Artifacts:** [mjs/imapclient#663](https://github.com/mjs/imapclient/pull/663) (head `9abf7cb7`), comments on [#662](https://github.com/mjs/imapclient/issues/662#issuecomment-5696115074) and [#563652](https://github.com/NixOS/nixpkgs/issues/563652#issuecomment-5696115368).

---

## a) FULLY DONE

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                | Evidence                                                                           |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| A1  | Read both issues **and all their comments**: nixpkgs#563652 (dotlambda: "We can patch the package, but only if we make an upstream PR first"), imapclient#662 (zero comments), #638 (5 comments — kunesj's delegation sketch, jaglac's informal test)                                                                                                                               | `gh issue view --comments` transcripts in session                                  |
| A2  | verify-before-filing Gate 2/5: upstream master still broken — `imapclient/imapclient.py:387` assigns `self._imap.file`; last commit touching the file (2f5947cc, 2026-09-07) unrelated; **no existing PR** for the starttls site                                                                                                                                                    | `gh api` file fetch + `gh search prs` sweeps (starttls / python 3.14 / IMAP4.file) |
| A3  | Prior-art analysis: #641 (merged, open()-path fix, delegation direction), #642 (closed duplicate), #629 (merged IMAP4_TLS simplification), #240/#314 (2017 delegation rejections — reasons read: "don't add more imaplib reliance" while the manual path still worked)                                                                                                              | `gh pr view` bodies + comment threads                                              |
| A4  | CPython source verification across v3.8.16/3.9.13/3.10.11/3.11.9/3.13.7/3.14.7: `IMAP4.file` is a getter-only property on 3.14 (`imaplib.py:337`); **every supported version** raises inside `starttls()` on refusal and calls `_get_capabilities()` post-TLS                                                                                                                       | `gh api repos/python/cpython` raw fetches per tag                                  |
| A5  | Fix implemented: `starttls()` delegates to `imaplib.IMAP4.starttls(ssl_context=...)`; new `tls.create_default_context()` helper shared with `tls.wrap_socket()` preserves IMAPClient's **verifying** default (imaplib's own `None` fallback is `ssl._create_stdlib_context()` = unverified — the security trap this PR closes)                                                      | PR diff, `imapclient/imapclient.py`, `imapclient/tls.py`                           |
| A6  | Upstream's **existing** mock tests (`tests/test_starttls.py`) adapted to the delegation seam + new `test_default_context_is_used` pinning the security property; house style/copyright preserved                                                                                                                                                                                    | PR diff                                                                            |
| A7  | New real-socket regression suite `tests/test_starttls_integration.py` (162 lines) + throwaway TLS fixtures (`tests/certs/{ca.pem,server.pem,server.key}`, CA with `keyUsage=keyCertSign`, SAN `localhost`+`127.0.0.1`); **mutation-verified**: 3/3 tests fail on unfixed master at 3.14 with the exact `AttributeError: property 'file' of 'IMAP4WithTimeout' object has no setter` | local run against `master@upstream` workspace copy                                 |
| A8  | Full matrix green on the exact pushed tree: Python 3.14.7 / 3.13 / 3.8 (`skipped=3`, guarded — see C2) / pypy3.9, 267 tests each                                                                                                                                                                                                                                                    | `uv run --python … python -m unittest` × 4                                         |
| A9  | Lint clean on final tree: black, isort, flake8, pylint 10.00/10, mypy                                                                                                                                                                                                                                                                                                               | `uv run --with …` runs mirroring `.github/workflows/lint.yml`                      |
| A10 | PR #663 opened: single commit, MERGEABLE, exactly 8 files, body voice-checked (`check-draft.py` 0 FAIL 0 WARN), `Fixes: #662`, downstream link to nixpkgs#563652                                                                                                                                                                                                                    | `gh api repos/mjs/imapclient/pulls/663`                                            |
| A11 | uv.lock churn removed after force-push; API-verified PR files = exactly the 8 intended, head = `9abf7cb7`, remote branch hash matches local                                                                                                                                                                                                                                         | `gh api …/pulls/663/files`, `git ls-remote`                                        |
| A12 | Full shipped diff re-read on GitHub post-push (both halves)                                                                                                                                                                                                                                                                                                                         | `gh pr diff`                                                                       |

## b) PARTIALLY DONE

| #  | Item                                 | What works                                                                                                  | What's open                                                                                                                                   | Effort          |
| -- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| B1 | "Read all comments"                  | #563652, #662, #638 read fully                                                                              | #636 and #618 (the other closed 3.14 issues) read **titles only** — their threads may contain extra reproduction detail or maintainer signals | S               |
| B2 | 3.14 blast radius inside imapclient  | `imapclient.py`, `imap4.py`, `tls.py` verified clean of other `.file` assignments; full suite green on 3.14 | `livetest.py` and `interact.py` never grepped — livetest isn't exercised by the suite, so a 3.14 break there would be invisible               | S               |
| B3 | GitHub-side CI                       | Local matrix covers the exact CI versions (3.8–3.14 + pypy3.9)                                              | Both workflows sit in `action_required` — first-time-contributor runs need maintainer approval; zero GitHub-side verification exists          | — (external)    |
| B4 | PR lifecycle                         | Filed, cross-linked, mergeable                                                                              | Sync-loop duty (fetch/rebase/push on review) not yet needed but owed per jj-fork-pr-workflow Phase 4 when the maintainer moves                | S per iteration |
| B5 | mailsuite claim in the nixpkgs issue | The issue (filed yesterday, pre-session) claims "mailsuite auto-calls starttls() — mailsuite/imap.py"       | **Never source-verified by me**; verify-before-filing retro-check owed — if wrong, correct the issue                                          | S               |
| B6 | Session lessons → memory             | None of it lost (this report)                                                                               | AGENTS.md update NOT yet made (violates the aggressive-update protocol): jj+daemon colocated-repo procedure, global-gitignore fixture trap    | S               |

## c) NOT STARTED

| #  | Item                                                                                                                                                                                                  | Why not started                                                                                                                                              | Still wanted?                              |
| -- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------ |
| C1 | **nixpkgs patch PR** for imapclient (the thing dotlambda actually gates on)                                                                                                                           | Timing decision open: carry a cherry-picked patch now vs wait for upstream merge + release bump                                                              | Yes — this is the original downstream goal |
| C2 | Separate upstream issue for the **pre-existing 3.8 breakage** (`IMAP4WithTimeout.__init__` passes `timeout=` to `imaplib.IMAP4`, which only accepts it since 3.9 — 7 suite errors on pristine master) | Disclosed in #663 body only; filing a second item as a first-time contributor is a tone/review-attention call                                                | Probably yes (Q3)                          |
| C3 | parsedmarc STARTTLS live E2E verification once the fix ships                                                                                                                                          | Nothing shipped yet; also unconfirmed parsedmarc is deployed on evo-x2                                                                                       | Yes if deployed                            |
| C4 | NEWS/release-notes entry                                                                                                                                                                              | #641 precedent ships without one; readthedocs notes are maintainer-written at cut time                                                                       | Optional offer                             |
| C5 | Backport decision to release branches                                                                                                                                                                 | No 4.x maintenance branch found (`master` is 4.x dev; only ≤3.x branches exist, which support ≤3.13-era Pythons) — likely N/A, needs maintainer confirmation | Low                                        |
| C6 | TODO_LIST/ROADMAP harvest of section (f)                                                                                                                                                              | Report just written                                                                                                                                          | Yes — feeds docs-health HARVEST            |

## d) TOTALLY FUCKED UP

| #  | What broke                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Severity                                                                                                       | Root cause                                                                                                                                                                                                                                                                                | Mitigation/status                                                                                                                                                                                            |
| -- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1 | **Auto-commit daemon × colocated jj repo shredded the change graph ~6 times**: fragments committed mid-edit under "heuristic" messages; a conflicted commit materialized `%%%`-junk INTO the two test files on disk; duplicate parallel heads (my change + a daemon re-commit of the same content); one push carried a bookmark pointing at a test-files-only fragment with my full message (caught by pre-push `jj show --stat`, ~30 s before a source-less PR went public); the first successful push shipped **uv.lock churn** (user caught it) | High — ~40+ min repair churn, one bad push that needed a force-push fix                                        | (a) long-lived dirty working copy under a continuously-committing daemon on a colocated repo, (b) verify and push in separate commands, (c) `jj new`/restore/squash interleaved with daemon snapshots, (d) user's global gitignore silently excluding `*.pem`/`*.key` from every snapshot | Fixed: absolute-tree rebuild via `jj restore --from <rev> --into @` on clean trunk ancestry + single atomic verify-and-push block + `.gitignore` negations for fixtures. Final pushed state verified via API |
| D2 | Attempted to **overwrite upstream's existing `tests/test_starttls.py`** — missed it because the repo-tree listing was truncated at 60 lines                                                                                                                                                                                                                                                                                                                                                                                                        | Medium — would have destroyed maintainer's test coverage; the write tool's modified-since-read guard caught it | Truncated `git/trees` output + assumption the file didn't exist                                                                                                                                                                                                                           | File found, read, adapted instead of replaced                                                                                                                                                                |
| D3 | **Nearly shipped a fabricated premise**: mid-session I asserted "on Python ≤3.11 imaplib returns non-OK instead of raising" — source-check proved ALL supported versions raise `Couldn't establish TLS session`                                                                                                                                                                                                                                                                                                                                    | Would have been a false claim in the PR body                                                                   | Memory-based stdlib assumption, not source                                                                                                                                                                                                                                                | Corrected before filing; PR body states the verified behavior. The verify-external-claims discipline caught it in-session                                                                                    |
| D4 | `uv run` invocations (test matrix, lint) **re-locked `uv.lock` on disk** after the final tree verification, and the daemon snapshotted that into the pushed commit through the verify→push gap                                                                                                                                                                                                                                                                                                                                                     | Medium — unrelated lock churn in a test-only PR; user flagged it                                               | Gap between "verify tree" and "push" as separate commands                                                                                                                                                                                                                                 | Restored from master, force-pushed, API-verified                                                                                                                                                             |

## e) WHAT WE SHOULD IMPROVE

1. **Daemon-evading work procedure for foreign repos** (impact: hours saved per upstream session): under the auto-commit daemon, never leave a foreign-repo working copy dirty across commands — describe+bookmark immediately after each verified step, and always run the pre-push `jj show --stat` check. The final pattern that worked (absolute `jj restore --from` rebuilds + one atomic verify-and-push command) should become the standing recipe.
2. **Pre-push expected-files gate** (impact: prevents the uv.lock class): before any `jj git push`, compare `jj diff -r <bookmark> --stat` against an explicit expected file list; after any force-push, re-verify via `gh api …/pulls/N/files` (not `gh pr diff`, which served a stale read once).
3. **Never truncate foreign repo listings** (impact: prevented file destruction only by tool luck): grep the tree for a target path before writing to it.
4. **Global-gitignore awareness for fixtures** (impact: silent file loss in any upstream contribution): run `git check-ignore -v` on every new fixture file before relying on VCS snapshotting; prefer repo-level `.gitignore` negations (portable to other contributors) over `jj file track` workarounds.
5. **Write the two AGENTS.md lessons** (jj-colocated+daemon procedure; gitignore fixture trap) — flagged in B6, still owed.
6. **Mutation-check every regression test** (already done here — keep as the standard): the 3/3-fail-on-master proof is what makes the new suite load-bearing.

## f) NEXT TASKS (ranked; feeds docs-health HARVEST)

| #  | Task                                                                                                                                                          | Impact | Effort | Category      |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------ | ------------- |
| 1  | Watch #663 for maintainer approval of CI runs + review; respond per github-voice                                                                              | High   | S      | Feature       |
| 2  | When CI runs: verify the full GitHub matrix green (incl. pypy + 3.8)                                                                                          | High   | S      | Quality       |
| 3  | Decide nixpkgs patch timing: cherry-picked patch PR now vs clean bump after upstream release (Q1)                                                             | High   | S      | Decision      |
| 4  | Prepare nixpkgs imapclient patch branch (fix + upstream PR link) so it's ready to file                                                                        | High   | S      | Feature       |
| 5  | Update AGENTS.md: jj+daemon colocated-repo procedure (D1 recipe)                                                                                              | High   | S      | Documentation |
| 6  | Source-verify the mailsuite `starttls()` auto-call claim in nixpkgs#563652; correct the issue if wrong (B5)                                                   | High   | S      | Bug           |
| 7  | Grep whole imapclient repo for other 3.14 imaplib regressions (`.file` reads, `_mesg`, private-API) incl. `livetest.py`/`interact.py` (B2)                    | High   | S      | Bug           |
| 8  | Confirm parsedmarc deployment on evo-x2; schedule post-release live E2E of the STARTTLS path if deployed (C3)                                                 | High   | S      | Quality       |
| 9  | Harvest this report into TODO_LIST/ROADMAP (docs-health HARVEST)                                                                                              | High   | S      | Documentation |
| 10 | Sync loop when maintainer comments/moves upstream: `jj git fetch` + rebase `-o master@upstream` + `jj git push -b fix-starttls-py314`                         | High   | S      | Feature       |
| 11 | If mjs prefers the `_file` variant: prepare fallback PR with version-guarded assignment + same tests                                                          | Medium | M      | Feature       |
| 12 | Verify the nixpkgs issue's other line refs (`all-packages.nix:4484` python3=python314, imapclient 4.0.1) still accurate at current nixpkgs                    | Medium | S      | Quality       |
| 13 | File upstream issue for the 3.8 `IMAP4WithTimeout timeout=` TypeError (C2, Q3)                                                                                | Medium | S      | Bug           |
| 14 | Optionally PR the 3.8 fix (skip `timeout=` passthrough below 3.9)                                                                                             | Medium | M      | Feature       |
| 15 | After merge: identify the release that will carry the fix; comment on nixpkgs issue with the version                                                          | High   | S      | Feature       |
| 16 | After nixpkgs ships the fix: parsedmarc STARTTLS live test from evo-x2                                                                                        | High   | M      | Quality       |
| 17 | Check parsedmarc's imapclient pin (`>=` vs `==`) to know when the fix flows downstream                                                                        | Medium | S      | Quality       |
| 18 | Check mailsuite's imapclient requirement chain similarly                                                                                                      | Medium | S      | Quality       |
| 19 | Post-merge: confirm #662 auto-closed via the `Fixes:` line                                                                                                    | Low    | S      | Cleanup       |
| 20 | Optionally thank/close-out comment on #638 pointing to #663 (reporters get closure)                                                                           | Low    | S      | Documentation |
| 21 | Offer a NEWS/readthedocs release-notes entry if the maintainer wants one (C4)                                                                                 | Low    | S      | Documentation |
| 22 | Ask maintainer whether a 4.0.2 patch release is planned so nixpkgs can bump clean (C5)                                                                        | Medium | S      | Decision      |
| 23 | Sync fork's master after upstream moves (`gh repo sync`) so future PRs start fresh                                                                            | Low    | S      | Cleanup       |
| 24 | Verify local `~/projects/imapclient` is left clean (only trunk + PR change; no ghost workspaces/debris)                                                       | Medium | S      | Cleanup       |
| 25 | Verify throwaway `ca.key` is not on disk anywhere (was trashed; confirm)                                                                                      | Low    | S      | Security      |
| 26 | Extract the whole upstream-PR flow (verify-before-filing + jj + daemon-evasion + fixture certs + expected-files gate) into a reusable skill via skill-creator | Medium | L      | Quality       |
| 27 | Script the pre-push expected-files gate (E2) as a small tool                                                                                                  | Medium | M      | Quality       |
| 28 | Keep a reusable openssl fixture-generation snippet (CA with keyUsage + SAN cert) for future TLS test suites                                                   | Low    | S      | Quality       |
| 29 | Sanity-check the 100-year cert NotAfter (2126) doesn't trip anything in CI/linters                                                                            | Low    | S      | Quality       |
| 30 | Consider Windows-CLI robustness of the integration test (env var + sockets) although CI is ubuntu-only                                                        | Low    | S      | Quality       |
| 31 | Track #636/#618 threads for extra reproduction data (B1)                                                                                                      | Low    | S      | Documentation |
| 32 | Inventory other LarsArtmann Python projects that depend on imapclient for a 3.14 sweep                                                                        | Medium | M      | Bug           |
| 33 | Decide whether SystemNix should carry a temporary imapclient override until nixpkgs lands the fix (only if parsedmarc is live)                                | Medium | S      | Decision      |
| 34 | Add the "upstream PR first" dotlambda requirement to SystemNix docs for future nixpkgs packaging work                                                         | Low    | S      | Documentation |
| 35 | Post-merge, delete the local fork branch debris and re-clone fresh for the next contribution                                                                  | Low    | S      | Cleanup       |
| 36 | Re-run the local matrix after GitHub CI runs to triage any env-dependent divergence                                                                           | Low    | S      | Quality       |
| 37 | Consider proposing post-TLS capability-refresh tests upstream as a follow-up enhancement (imaplib already re-fetches; client behavior could be pinned)        | Low    | M      | Quality       |
| 38 | Check Python 3.15 alphas for further imaplib changes that would affect the delegation (future-proofing)                                                       | Low    | S      | Bug           |
| 39 | Fold D1/D4 timelines into AGENTS.md cross-cutting lessons (merge with task 5)                                                                                 | Medium | S      | Documentation |
| 40 | Verify `uv.lock` equality against master one final time before any future push to this PR (`jj diff -- uv.lock` empty)                                        | Medium | S      | Quality       |

## g) QUESTIONS I CANNOT ANSWER MYSELF

1. **nixpkgs patch timing (drives task 3/4):** Should I file the nixpkgs PR now carrying the cherry-picked patch with the upstream PR linked (dotlambda can merge early, nixpkgs carries a patch until a release), or hold until mjs merges/cuts a release so nixpkgs lands it as a clean version bump? I cannot decide your tolerance for carrying a local-vs-upstream patch through nixpkgs CI.
2. **If mjs pushes back on the delegation approach** (preferring the minimal `_file`-assignment variant): do you want me to defend delegation with the #641-precedent + unverified-fallback-context argument, or concede quickly to keep the first-time-contribution frictionless? I can argue either; the relationship call is yours.
3. **The pre-existing 3.8 `IMAP4WithTimeout` TypeError:** file it as its own upstream issue now (optionally with a fix PR), or leave it disclosed in #663 only? Two simultaneous items from a first-time contributor may dilute review attention on the important one — that tradeoff is not mine to make.

---

_Report format note: skill default is a styled HTML dashboard; the explicit `.md` request in the prompt wins, so this is Markdown. Auto-commit daemon will pick this file up (Crush harness forbids manual commits without instruction)._
