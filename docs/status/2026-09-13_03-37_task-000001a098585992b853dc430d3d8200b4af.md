# Status: task-queue run 000001a098585992b853dc430d3d8200b4af — per-unit triage of the "5 long-failed units" — 2026-09-13 03:37

Task: TODO_LIST P1 row "website-deploy-monitor + the 4 other long-failed units: per-unit triage (never done)" (source: `docs/status/2026-08-30_11-15_deploy-unblocked-narhash-vendorhash-email-state.md` §d.3/§f.6). **Outcome: triage complete per unit. One REAL failure mechanism found and fixed (`website-deploy-monitor`'s degradation guards were dead code under errexit — the unit failed on any network transient, DAS-independent), one residual guard fixed (`disk-growth-check`), one REAL staleness signal confirmed (larsartmann.com serves a `1980-01-01` placeholder `builtAt`), and `btrfs-verify-snapshots` needs no change (episodic self-healing; FAIL semantics deliberately kept). The pool-tied remainder was already verified DAS-class and self-healed 2026-08-31.**

---

## a) FULLY DONE

1. **`website-deploy-monitor.service` — NOT DAS. Root cause found, reproduced, fixed.**
   - Dependencies: one HTTPS GET of `https://larsartmann.com/build-info.json` + writes to `$HOME/.local/state/` (`ProtectHome = false` in `mkDesktopNotifyService`). Zero pool/DAS contact. The 2026-08-29 doc even had it right ("Pre-existing issues noticed this session (not DAS)") before the 08-30 session group-labeled it DAS anyway.
   - **The failure mechanism:** the check script's three degradation guards were DEAD CODE. `writeShellApplication` runs `errexit`+`pipefail`; `info=$(curl …)`, `built_at=$(… | jq …)` and `built_epoch=$(date --date=…)` each return the inner command's non-zero status and kill the script BEFORE the `[ -z … ]` guards written for exactly those failures. Reproduced locally: curl DNS-failure → script exit 6 ("fetch failed" guard never reached); HTML error page → jq exit 5 ("no builtAt field" guard never reached). Any boot before DNS was ready (OnBootSec=5min during the crash-reboot-heavy era) or any CDN/origin error page (the CI-billing-blocked era) failed the unit instead of degrading to a logged skip → OnFailure → the long-failed state.
   - **Fix** (daemon commit `e573995d`, 2 files): errexit-hardened all three captures (`|| true`) + added the missing unparseable-`builtAt` guard. All five paths now verified by direct execution: network fail → "fetch failed" exit 0; HTML body → "no builtAt field" exit 0; garbage date → "unparseable builtAt" exit 0; fresh marker → fresh branch; 1980 sentinel → stale branch. `nix flake check --no-build` all green; `nix fmt --ci` 0 changed.
   - **REAL signal CONFIRMED (§f.6's hypothesis was true):** the live marker serves `{"builtAt": "1980-01-01T00:00:00.000Z", "gitSha": "5531f16"}` — a placeholder sentinel. The site IS stale in the sense the monitor watches for: the deploy pipeline is not stamping a real `builtAt` (or the standing deploy was built from a path that never injected it). The monitor now behaves as designed: degraded fetches log-and-skip, the sentinel trips the once-per-build desktop notification. The `builtAt` stamping itself belongs to the website repo (upstream, out of scope here — flagged below).

2. **`disk-growth-check.service` — NOT DAS. Historical failure already fixed 2026-09-02; residual guard fixed today.**
   - Checks `df /data` — the INTERNAL NVMe BTRFS partition (hardware-configuration.nix), no pool dependency. The 08-30 group label was wrong for this unit.
   - Historical failure = the documented 226/NAMESPACE bug (ReadWritePaths to a then-missing `/var/lib/disk-growth`), already fixed by commit `9e266e6c` (2026-09-02, StateDirectory pattern; the module comment records the live bug).
   - Residual instance of the SAME errexit class found in this triage: `current_bytes=$(df … | tail -1 | tr -d ' ')` under pipefail would exit before the empty-capture guard. Fixed in `e573995d` with `|| true` so an unreadable /data still reaches the guard and exits 1 (an alert, not a unit crash). Verified both ways: live `/data` reads 887423537152 bytes; failure path hits the guard with exit 1.

3. **`btrfs-verify-snapshots.service` — NOT pool-mounted, INDIRECTLY DAS-coupled during the outage; no change needed.**
   - Reads `/mnt/btrfs-root/.snapshots` (NVMe root chain). During Aug 22–30 it failed for a REAL reason the group label obscured: btrbk is also the snapshotter, its `RequiresMountsFor=/mnt/pool` gate failed nightly BEFORE the snapshot phase → zero local snapshots taken the whole outage (recovery report §17) → freshness check red. Self-healed on DAS return.
   - Second, independent red window: the 2026-09-12 glob-delete incident wiped ALL six local snapshots → "No root snapshots found" until that night's btrbk run. Protections (btrfs-rescue tier) shipped same day.
   - **Current live state: GREEN** — exactly one snapshot `@.20260912T2300` (1 day old; `.snapshots` refills ~1/night because the glob-delete + rescue-tier change is still propagating through retention), rescue tier holds `@.rescue-20260913T020341`.
   - **Deliberately NOT softened to WARN:** the 2026-09-08/09 exit-4 doctrine targets CHRONICALLY failing units; this one is episodic and self-healing, and "no fresh root snapshot" is a genuine alarm (the glob-delete proves the value). No code change.

4. **`btrfs-verify-pool-backups` + the pool-tied remainder — the group label was CORRECT for these, and they are closed.** The sibling verifier is explicitly pool-gated (findmnt + received-backup freshness). Live: root receives through `@.20260912T2300` (fresh), `/data` leg empty → WARN-only per the documented EIO stance. The remaining pool-tied units of the 08-30 era (Immich ×2, Attic, Paperless ×4, Bank-Sync ×4 post-deploy fails; btrbk-data/pool mount-gate failures) were all verified DAS-class and documented self-healed on 2026-08-31 (`docs/status/2026-08-31_16-29_*` + AGENTS.md DAS-outage semantics). Notably, the DAS recovery EXPOSED two real non-DAS bugs hiding under the same group label (cv-backup broken-since-birth, paperless-exporter timer missing `Persistent`) — fixed separately; that is exactly the risk the TODO row called out.

5. **Verification:** `nix flake check --no-build` → all checks passed (aarch64-darwin omission warning expected per AGENTS); `nix fmt --no-update-lock-file -- --ci` → 0 changed; both fixed scripts exercised end-to-end per scenario above (not just syntax-checked).

## b) PARTIALLY DONE

1. **The exact 5-unit list is not recoverable from the source docs** — §d.3 says "the 5 failed units" while §f.1 says "4 pool-tied failed units" (3 named non-pool + 4 pool-tied ≠ 5). This triage therefore covers the three named units exhaustively + the pool-tied set as a class (with per-class evidence), which subsumes any exact split. The actionable content of the row is fully discharged either way.

## c) NOT STARTED (deliberately — smallest-correct-change)

1. **Website-repo `builtAt` stamping fix** — the 1980 sentinel lives in the larsartmann.com deploy pipeline, not SystemNix. Follow-up belongs there (verify-before-filing applies before touching that repo).
2. No Gatus/Discord surface for `website-deploy-monitor` (desktop-notification by design; onFailure alerting already wired for unit failures).
3. No retries/backoff added for cold-boot DNS races — the fixed degradation path (log-and-skip) is the designed behavior for a freshness monitor; the next hourly tick is the retry.

## d) TOTALLY FUCKED UP (own mistakes, this run)

1. **First verification one-liner had a wiring bug** — my multi-scenario runner passed the URL but not the body override, so the "HTML body" case silently re-fetched the live site and parsed the real sentinel instead. Caught on review of the output (case 2 printed "parsed OK"); re-ran with direct body substitution for the four content cases. The result was right; the first ROAD there was not.
2. Nothing else. No tree damage, no unrelated files touched, `systemctl`/`sudo` correctly not used (banned in sessions) — triage ran on config + live filesystem evidence + direct script execution.

## e) WHAT WE SHOULD IMPROVE

1. **Audit remaining `writeShellApplication` scripts for the dead-guard class** — "capture under errexit, guard after" is now proven in two modules and likely recurs; a targeted grep review of `$(curl`/`$(jq`/`$(date` captures followed by `[ -z` guards would close the class fleet-wide (candidate TODO).
2. **Pre-deploy failed-units warnings should name the EXPECTED set** — the 08-30 session had 9 known-failed units and still needed a session to ask "are these really all DAS?" A static allowlist-with-reasons (like `allowedUnits` in session-boot-audit) would turn the deploy warning into a diff, not a puzzle.

## f) Up to 20 things we should get done next

1. Fix `builtAt` stamping in the larsartmann.com website repo (the sentinel is the live signal this monitor exists to catch).
2. Fleet-wide dead-guard audit (§e.1).
3. Deploy the two fixes (`nix run .#deploy`) so the monitor's degraded-fetch path is live — until then a network transient can still fail the deployed unit one more time.
4. After deploy: confirm `website-deploy-monitor` takes the stale branch and fires its once-per-build desktop notification for the 1980 sentinel (expected behavior, not a bug).
5. Consider a Gatus liveness pattern for `website-deploy-monitor` journal staleness if desktop notifications prove too quiet (owner decision).

---

**Landing:** code changes rode daemon commit `e573995d` (modules/nixos/services/website-deploy-monitor.nix + platforms/nixos/system/scheduled-tasks.nix); the closing commit carrying the `Task-Queue-ID: 000001a098585992b853dc430d3d8200b4af` footer contains this report + the TODO_LIST closure.
