# Task `000001a0db75dfafa622748a00da7cf19ff9` — Full Status + Brutal Self-Review (5th dispatch of the closed @home-hermes 09-16 snapshot investigation)

**Date:** 2026-09-26 05:33 CEST · **Scope:** THIS task only (the `@home-hermes` 09-16 snapshot investigation, its five dispatches, and what this run noticed in passing). No unrelated research.
**Format note:** the skills' canonical output is a styled HTML dashboard (`status-report`/`brutal-self-review`); the user's explicit instruction (`.md` at this exact path) wins — honoring it and flagging the divergence per the skill's own rule.

**One-line context:** the queue re-dispatched an already-closed investigation for the 5th time. The item's verdict (**IN-SPEC — different effective retention; the queue premise misread the config**) has been stable across all five dispatches. This run re-verified every load-bearing leg first-hand, found and fixed a citation-drift defect the previous self-review pass left half-fixed, and self-harvested one new queue item.

---

## §0 — Direct answers to the three hard questions

**What did I forget?**
1. **I did not attempt a `btrbk dryrun` as an independent, falsifiable pre-check of tonight's prune prediction** — it would have turned "schedule() source-reading says 09-22 dies tonight" into "btrbk's own planner says so" before the 23:00 run. My sandbox cannot sudo (btrbk dryrun needs root for `btrfs subvolume list`), which is a partial excuse only — I could have tried and documented the attempt. Mitigating: the general "dryrun as observation proof" ask is already tracked (TODO_LIST:379); I endorse it instead of duplicating.
2. **I did not anticipate the auto-commit daemon race on my own commit**, despite AGENTS.md documenting the pattern extensively — my `git commit` lost to the daemon's `efc0a0e9` (05:22:01) and returned exit 1 "nothing to commit". Recovery was correct (verified the daemon staged ONLY my 3 files via `git show --stat`, then amend-forward to `e162c513`), but the failed cycle burned a full hook run (~40s) and one round of confusion.
3. **The prior dispatch (a32adc62, 05:09) forgot 2 of 4 stale refs** while executing its "citation fixes" — it corrected `257-258→255-256` and `336-340→336-341` but left `now 228`, `snapshots.nix:337` (both in the very same storage.md:16 row it edited) and TODO_LIST.md:52's `227`/`337` stale. This run found and fixed the remainder. Lesson recorded: when a citation-fix pass runs, sweep EVERY ref in the touched rows, not just the ones a diff highlighted.

**What could I have done better?**
- Run the citation sweep BEFORE writing my verification report rather than discovering the drift mid-verification (it cost a re-edit of both queue surfaces + a longer commit).
- Committed faster after staging — the daemon's sweep window is seconds-to-minutes; my report-then-commit order widened it. (Structural fix: §g3.)
- Checked daemon activity (`git log --since='-2 min'`) immediately before `git commit` — one command would have predicted the race.

**What could I still improve?**
- The run's marginal value was low by construction (5th verification of a stable verdict). A sharper protocol would have me verify closure + sweep citations + land a SHORT note, not a full report table — the full-report-per-refire pattern is spending commits on identical conclusions (see §e1).
- Nothing else material: the verdict's evidence chain (config → journal → inventories) is now verified first-hand by two independent dispatches (04-44 and this one) with identical results.

---

## a) FULLY DONE (verifiable, committed)

| # | Item | Evidence |
| - | ---- | -------- |
| a1 | **Re-dispatch protocol executed on the closed item** — closure confirmed on both queue surfaces (`TODO_LIST.md` + `docs/todo/storage.md`, both `[x]`, consistent, no lifecycle drift) instead of redoing the work | commit `e162c513`; `git log --grep=<task-id>` shows 4 footer-bearing commits (`3ada6e25`, `f237932c`, `a32adc62`, `e162c513`) |
| a2 | **Verdict re-verified FIRST-HAND (own commands, not prior citations):** live inventory 05:16 unchanged since 04:44 (`@` = 09-13, 09-20, 09-22..25; hermes = 09-16, 09-20, 09-22..25); hermes pool receives continuous 09-16→09-25; Sep 25 23:02:49 send parent = `@home-hermes.20260924T2300` (parent hold ruled out); 23:02:52 lockstep `subvolume delete` of `@.20260921` + `@home-hermes.20260921` (prune gap ruled out); missing locals (hermes 09-17, 09-18×3, 09-19, 09-21) all present pool-side | `ls /mnt/btrfs-root/.snapshots/`, `ls /mnt/pool/backups/root/`, `journalctl -u btrbk-root --since "2026-09-25 22:55"` |
| a3 | **Config legs re-read at current offsets:** root `snapshot_preserve = "3d 1w"` at snapshots.nix:231; hermes block 249-264 (only `target_preserve_min "7d"` / `target_preserve "14d 4w"`; inheritance sentence 255-256); forgejo `snapshot_preserve = "3d"` at :341 | `grep`/`sed`/`view` of snapshots.nix |
| a4 | **Citation drift found + fixed (4 stale refs in 2 queue surfaces):** `227→231` and `337→341` in TODO_LIST.md:52; `now 228→now 231` and `337→341` in storage.md:16. All OTHER cited refs in the task-family rows verified exact and untouched (255-256, 339, 336-341, 708-740, 774+) | in-file verification; diff in `e162c513` |
| a5 | **Run report landed** (docs/status/2026-09-26_05-21_…md) with the full verification table + the daemon-race mechanism (`7ea1c0e1` +4-line comment rewrite landed between ref determination and the prior fix pass) | commit `e162c513` |
| a6 | **All gates green:** gitleaks, eval-only `nix flake check`, `check-todo-system.sh` (run explicitly + via pre-commit, twice) | hook output on both commit attempts |
| a7 | **Daemon race handled per house discipline:** verified `efc0a0e9` staged exactly my 3 files (no foreign files absorbed), amended forward to `e162c513` with the footer — tree clean, no reset, no history damage | `git show --stat` before amend; `git status` clean after |
| a8 | **Task arc (all 5 dispatches) properly cross-referenced:** item `[x]` on both surfaces since run 1-2; [watch] falsifiable closure rows (storage.md:17-18) carry dates + predictions + reopen condition | file contents this session |

## b) PARTIALLY DONE

| # | Item | Works now | Open | Blocker | Effort |
| - | ---- | --------- | ---- | ------- | ------ |
| b1 | **Falsifiable closure of the investigation** ([watch] rows) | Verdict + predictions source-verified; mechanism confirmed in btrbk 0.32.7 source; live inventories match the model at two independent timestamps (04:44, 05:16) | The two predicted prunes have not HAPPENED yet: both `09-22` dailies due at tonight's (09-26) 23:00 run; weeklies `@home-hermes.20260916` + `@.20260913` due at 09-27 23:00 | Purely time-gated — correctly unverifiable at 05:16-05:33 CEST | S (a 23:00+ `ls` + journal check per night) |
| b2 | **Citation hygiene in the queue surfaces** | 4 stale refs fixed; remaining refs spot-verified exact | Systemic prevention not started — line-number refs rot on every parallel commit that shifts the cited file (3rd incident in this family). Queued as a new [ready] row this run (TODO_LIST queue + pipeline.md library) | None — agent-executable | S-M |
| b3 | **The investigation's §f follow-up backlog** (8 items from run 2 + this run's additions) | All land on tracked surfaces (storage.md:17-23, pipeline.md:165-168); none un-landed — swept this run | None of them EXECUTED — they belong to their own dispatches ([watch]/[blocked:user]/[decision]/[ready]) | Queue pacing + owner gates, by design | — |

## c) NOT STARTED (planned, no work done — none of it mine to start this dispatch)

| # | Item | Why not started | Still wanted? |
| - | ---- | --------------- | ------------- |
| c1 | Tonight's + tomorrow's prune confirmations ([watch] rows) | Time-gated (23:00 runs are in the future) | Yes — they are the item's falsifiable closure; if either snapshot survives its predicted run, the retention doctrine reopens |
| c2 | The citation-rot checker (just queued) | Brand-new row, not yet harvested into a dispatch | Yes — S-M, high value-per-cost given 3 incidents |
| c3 | Codifications adjacent to this task: re-dispatch protocol in CONTRIBUTING (TODO_LIST:55), spot-verify-config-claims at queueing (pipeline.md:167), re-fire counts in tq verify (pipeline.md:168), self-harvest codification (TODO_LIST:54) | Separate dispatches; each is one-sentence doc work blocked only on queue pacing | Yes — all four directly attack this task family's failure modes |
| c4 | §f storage-family backlog seen in passing (forgejo "(~9 snaps)" comment fix, explicit `snapshot_preserve` decision, hermes restore drill, btrbk transaction_log decision, btrfs-verify-snapshots comment) | [blocked:user]×2, [decision]×2, [ready]×1 — not this dispatch's contract | Yes — tracked with lifecycle tags |

## d) TOTALLY FUCKED UP

Nothing data-level is broken: no gate red, no test broken, no secret exposure, verdict stable across 5 dispatches, tree clean at `e162c513`. The honest fucked-up list is process-level:

| # | What's fucked | Severity | Root cause | Mitigation |
| - | ------------- | -------- | ---------- | ---------- |
| d1 | **5 dispatches (each: full report + commit + 2 hook cycles) spent on one closed S-size item.** Runs 3-5 each re-verified the same verdict and produced near-identical reports; the queue burned hours on zero new information | Medium (waste, not harm) | Dedup/closed-signal preflight exists as tracked rows (pipeline.md dedup-preflight, re-fire counts :168) but is NOT IMPLEMENTED at claim/dispatch time | Implement the preflight; until then, re-fires SHOULD degrade to "closure re-check + citation sweep + short note" (see §e1) |
| d2 | **The prior self-review's citation fix was itself half-done:** `a32adc62` ("citation fixes") corrected 2 refs while leaving 2 stale refs in the SAME storage.md row it edited + 2 in TODO_LIST.md:52 — the drift then looked "fixed" to any reader trusting the commit message | Low-Medium (misleading evidence trail, self-healed this run) | The fix pass diffed against a stale line-number model of the file instead of re-verifying every ref in the touched rows | Rule for future citation passes: re-verify EVERY `<file>:<N>` ref in the rows you touch against the live file, not just the ones your diff surfaced. Systemic: the new checker row (b2) |
| d3 | **Line-number citations in TODO/library rows are structurally fragile:** daemon commit `7ea1c0e1` (04:30) shifted snapshots.nix +4 lines BETWEEN the 04-26 run's ref writes and the 05:09 fix pass — every dispatch in the window cited lines that were already wrong when written | Low (each instance is a 1-line fix, but it recurs) | No automated validation of `<file>:<N>` refs anywhere in the gate stack | Queued this run: citation-rot checker (b2); alternative policy question in §g2 |
| d4 | **Auto-commit daemon races task commits** — my commit lost the race (exit 1, "nothing to commit"); the 05-05 dispatch analyzed the same class. Every task landing is one daemon sweep away from a heuristic-message commit that lacks the footer the queue cross-references | Medium (recurring friction; risk of a footer-less landing if a session doesn't notice) | The daemon commits independently of agent activity; no coordination/lock exists | House discipline works (verify staging → amend-forward) but is manual every time; design question in §g3 |

## e) WHAT WE SHOULD IMPROVE

1. **Degenerate re-fire protocol for dispatches ≥3 of a closed item:** closure re-check + live spot-check of ONE load-bearing leg + citation sweep + a ≤10-line landing note. Full reports only when something CHANGED. This run's new information (the citation drift) would still have been caught — it lives in the sweep.
2. **"Fix-everything-you-touch" rule for citation passes:** when correcting refs in a row, re-verify ALL refs in that row (and its queue/library twin) against the live file. d2 happened because the pass trusted its own diff.
3. **Self-harvest §f at authoring time** (already codified as a tracked row, TODO_LIST:54 — this run practiced it: 1 new row queued, everything else routed to tracked surfaces).
4. **Keep the queue one-liner and library entry byte-consistent on citations** — the drift-rule ("edit both") extends to line refs: TODO_LIST:52 and storage.md:16 now cite identical current numbers.
5. **Report-series hygiene:** this item now has 6 status docs (04-11, 04-26, 04-44, 05-05, 05-21, this one). When the [watch] rows close, a one-line CLOSED/SUPERSEDED-BY pointer per report (docs-health ANNOTATE style) would make the series read as one arc instead of six fragments.
6. **Tests/doctrine check (brutal-self-review Q11):** docs-only run — no product tests owed. The honest testing gap this run exposes is DOCS-as-artifact testing: nothing validates that cited line refs still point at cited content (the b2 row IS that test). Ghost systems (Q7): none — the [watch] rows are wired to concrete future checks, not dead text. Split brains (Q10): checked the queue/library pair, the AGENTS.md pinning-doctrine table ("`@home-hermes` same window"), and the hermes-section retention claims — all consistent post-fix. Scope creep (Q8): one new queue row total; no drive-by refactors; nothing removed (Q9: n/a).

## f) Next tasks (up to 50 — 32 solid; NEW = surfaced this run, TRACKED = endorsed with pointer. Feeds docs-health HARVEST; do not duplicate TRACKED rows at harvest time.)

| # | Task | Impact | Effort | Category | Status |
| - | ---- | ------ | ------ | -------- | ------ |
| 1 | After tonight's 23:00 run: confirm BOTH `09-22` dailies pruned from `.snapshots` | Critical | S | Verification | TRACKED (storage.md:17) |
| 2 | After 09-27 23:00 run: confirm `@home-hermes.20260916` + `@.20260913` weeklies pruned — if either survives, reopen the retention doctrine | Critical | S | Verification | TRACKED (storage.md:17) |
| 3 | Confirm `btrfs-verify-pool-backups` hermes freshness gate rides through the local weekly expiry; watch the FIRST pool-side hermes prune (~mid-Oct, `14d 4w`) | High | S | Verification | TRACKED (storage.md:18) |
| 4 | Implement the TODO-citation rot checker (`<file>:<N>` refs vs live content, ±5-line WARN / miss FAIL, wired into the todo gate) | High | S-M | Quality/tooling | NEW — queued this run (TODO_LIST queue + pipeline.md) |
| 5 | Codify the re-dispatch protocol in docs/CONTRIBUTING.md — WITH this run's amendments: citation sweep step, "degenerate form for re-fires ≥3", "expect the daemon to race your commit; verify staging before amend" | High | S | Documentation | TRACKED (TODO_LIST:55) + NEW amendments from this run |
| 6 | Codify "spot-verify config claims in queued items at queueing time" (would have prevented the original misread premise) | High | S | Documentation | TRACKED (pipeline.md:167) |
| 7 | Dedup/closed-signal preflight at DISPATCH time (not harvest time) — stop re-firing closed items | High | M | Feature | TRACKED (pipeline.md dedup-preflight rows; extended by this run's d1) |
| 8 | Surface re-fire counts in tq verify output | Medium | M | Feature | TRACKED (pipeline.md:168) |
| 9 | Pre-commit: skip the flake-check leg on docs-only staged diffs (would have sped up 3 of this family's 5 commits) | Medium | S | Quality | TRACKED (TODO_LIST:59, pipeline.md:165) |
| 10 | Classify tq verify failures before burning attempts (environmental / gate-slow / introduced) | High | M | Quality | TRACKED (TODO_LIST:57, pipeline.md:163) |
| 11 | Verify-gate triage runbook in CONTRIBUTING (binfmt → clean-HEAD check → nix-daemon → IO → then suspect the task) | Medium | S | Documentation | TRACKED (TODO_LIST:58, pipeline.md:164) |
| 12 | Codify "status reports self-harvest §f at authoring time" | Medium | S | Documentation | TRACKED (TODO_LIST:54) |
| 13 | Commit-msg hook: reject subjects >72 chars | Low | S | Quality | TRACKED (TODO_LIST:56) |
| 14 | DONE-note era-annotation convention ("superseded: <figure>") | Low | S | Documentation | TRACKED (pipeline.md:166) |
| 15 | Heal-attribution breadcrumbs for manual system heals | Medium | S | Quality | TRACKED (TODO_LIST:60) |
| 16 | `/data` corruption repair gate (the ONLY backup tier never completed pool-side; verification-first triage) | Critical | L | Bug | TRACKED (storage.md:24, blocked:user) |
| 17 | Mystery snapshot `data.20260905T2330` root-cause (root forensics) | Medium | S | Bug | TRACKED (storage.md:25, blocked:user) |
| 18 | Forgejo btrbk instance "(~9 snaps)" comment: verify live count (`sudo ls /mnt/hot/.snapshots`) then correct | Low | S | Cleanup | TRACKED (storage.md:19, blocked:user) |
| 19 | Owner decision: explicit `snapshot_preserve` on the `@home-hermes` block vs documented inheritance | Low | S | Decision | TRACKED (storage.md:20, decision) |
| 20 | Hermes restore drill: receive one `@home-hermes.*` into a scratch subvol + diff vs live (untested restore path = not a backup) | Medium | M | Verification | TRACKED (storage.md:21, blocked:user) |
| 21 | Owner decision: btrbk `transaction_log` persistence (retention-bounded) so prune/send forensics survive journal vacuum | Low | M | Decision | TRACKED (storage.md:22) |
| 22 | One comment block in `btrfs-verify-snapshots`: the gate checks NEWEST snapshot age per prefix only (11d mid-week weeklies are expected) | Low | S | Documentation | TRACKED (storage.md:23, ready — good next agent dispatch) |
| 23 | One-shot observation proof for the 2w-sharp model via read-only btrbk dryrun/info output (also the falsifiable pre-check I skipped this run) | Medium | S | Verification | TRACKED (TODO_LIST:379) |
| 24 | Extend calendar-anchor reconciliation to `/data` (14d 4w) + pool `services/*` (7d 4w) windows with live probes | Medium | S | Verification | TRACKED (TODO_LIST:380) |
| 25 | Owner call: is `3d 1w` the intended FINAL local retention on this historically-full box? | Medium | S | Decision | TRACKED (TODO_LIST:381) |
| 26 | Docker inactive-volumes policy (~206 volumes, 4.09 GB — forgotten state vs irreplaceable?) | Medium | M | Decision | TRACKED (TODO_LIST:382) |
| 27 | Refresh `/data` composition figures + adopt "(figures as of <date>)" convention for dated sizing prose | Low | S | Documentation | TRACKED (TODO_LIST:377) |
| 28 | [watch] Post-Sunday docker-prune verification (2 dangling images) | Low | S | Verification | TRACKED (TODO_LIST:378) |
| 29 | Fix borg-restore-drill fixture's borg stub dropping unknown flags as junk files in cwd | Medium | S | Bug | TRACKED (TODO_LIST:53) |
| 30 | Offsite Borg go-live (StorageBox hostname/username + host-key pin — owner-held inputs) | High | S | Decision+ops | TRACKED (AGENTS.md offsite-borg section; go-live gate owner-held) |
| 31 | After the [watch] rows close: annotate the 6-report series with CLOSED/SUPERSEDED-BY pointers (docs-health ANNOTATE, never rewrite) | Low | S | Documentation | NEW candidate (this run, §e5) |
| 32 | Daemon-race design pass: pause/lock for the auto-commit daemon during active agent commits (or ratify amend-forward as the permanent cost) | Medium | M | Feature/decision | NEW candidate — owned by §g3 answer |

(Items 31-32 are the only unqueued NEW candidates; both are noted here for harvest rather than auto-appended — 4 was appended because it is concretely agent-executable now, while 31 depends on the [watch] outcome and 32 on the §g3 decision.)

## g) Questions I cannot figure out myself

1. **Re-fire pacing intent:** Should the dedup/closed-signal preflight (once implemented) BLOCK re-dispatch of closed items entirely, or is periodic re-fire-as-verification deliberate redundancy you want kept at some cadence? Evidence I gathered: 5 dispatches on this one S-size item (4 after closure), runs 3-5 each adding only a citation fix or re-confirmation; the tracked rows (dedup preflight, re-fire counts) don't state the intended end state. The answer decides whether I build "stop re-firing" or "re-fire cheaply".
2. **Citation policy for queue surfaces:** line-number refs + the automated checker (queued, row 4) — or replace line refs with content-anchored refs (grep-able strings, rot-free, more verbose) everywhere in TODO_LIST.md + docs/todo/*.md? I verified both are mechanically feasible; the tradeoff (checker maintenance vs verbosity) is a style/ownership call nothing in the repo answers.
3. **Auto-commit daemon vs task commits:** is a coordination mechanism worth designing (daemon pause/lock while an agent holds the commit lock, or a tq-owned commit path), or is the current amend-forward discipline the accepted permanent cost? Three consecutive dispatches in this family hit the race (05-05 analyzed it, mine lived it); each costs one wasted hook cycle and carries the footer-less-landing risk if a session doesn't notice.

---

**Closing state:** tree clean at `e162c513` (this report + the queue row land in the next footer commit); 4 footer-bearing commits cross-reference the task; [watch] predictions pending tonight and tomorrow at 23:00.
