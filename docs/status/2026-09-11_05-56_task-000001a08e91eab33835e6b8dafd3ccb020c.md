# Status Report — Task 000001a08e91eab33835e6b8dafd3ccb020c (Google Sync DORMANT annotation)

**Date:** 2026-09-11 05:56
**Work item:** TODO_LIST P0 — "Google Sync go-live (user steps) or mark it DORMANT in AGENTS"
**Commit:** `9bcf79d9` (docs: mark Google Sync DORMANT in AGENTS with explicit go-live checklist)
**Scope discipline:** report covers ONLY this task's work plus things noticed in passing. No unrelated research.

> Format note: the status-report skill defaults to a styled HTML dashboard; the tasking
> explicitly requested Markdown at this path — user instruction wins (skill-sanctioned override).

---

## a) FULLY DONE

1. **AGENTS.md Google Sync section annotated DORMANT** — the section header now carries an
   explicit status banner: `STATUS: DORMANT (2026-08-31 finding, confirmed 2026-09-11) — NOT deployed`,
   stating `enable = false`, no units on evo-x2, "nothing below describes a running service",
   plus the full 5-step go-live checklist and a "do not trust this section as live" disclaimer.
   Evidence: commit `9bcf79d9`, diff +2 lines in `AGENTS.md` (Google Sync section, ~line 313).
2. **TODO_LIST.md P0 row closed** — the row is now `[x]` with the rationale: the agent-executable
   arm of the either/or (annotate DORMANT) is done; the go-live arm stays USER-owned (interactive
   Google OAuth + secret values an agent must never touch, per Critical Rules). Evidence: same commit.
3. **Build/test verification** — `nix flake check --no-build` passed (ran standalone AND via the
   pre-commit hook on both commit attempts); gitleaks passed; docs-only change, no `.nix` files touched.
   Evidence: pre-commit output "All validation checks passed" in both runs.
4. **Repo-level dormant-state verification (during this report)** — went beyond the 2026-08-31 doc
   finding: `platforms/nixos/system/configuration.nix` contains NO `services.google-sync` line at
   all (option defaults false), and all side-wiring is enable-gated inside the module:
   `services.backup-coordination.backups.google-sync` sits inside the `mkIf cfg.enable` block
   (`modules/nixos/services/google-sync.nix:307`), the Homepage tile guards on
   `config.services.google-sync.enable or false` (`modules/nixos/services/homepage.nix:53`), and the
   sops secret declaration is `lib.optionalAttrs (svcEnabled "google-sync")` (`modules/nixos/services/sops.nix:414`).
   DORMANT is structural, not just "nobody started the timer".
5. **Commit hygiene recovered after a daemon race** — the auto-commit daemon swept my staged docs
   into a heuristic commit (`3de29dd0`, "chore: auto-commit 2 changed file(s) (heuristic)") before my
   pathspec commit ran; I verified via `git show --stat` that the sweep contained ONLY my two files,
   then amended it into the proper message with the Task-Queue footer. Net: clean history, footer
   present, nothing foreign swept in or out. Final SHA: `9bcf79d9`.

## b) PARTIALLY DONE

1. **The work item's dual option "go-live OR mark DORMANT"** — DONE for the DORMANT arm; the go-live
   arm is untouched BY DESIGN (requires interactive OAuth consent screens and secret values).
   Remaining: all 5 user steps (see c). Blocker: none technical — pure user action. Effort: M-L user time
   (OAuth client setup + 3× `rclone authorize` + sops fill + deploy + 1.9 TB seed over hours/days).
2. **"Confirmed 2026-09-11" in the AGENTS banner** — partially honest: the 2026-09-11 confirmation is
   DOC-level (2026-08-31 finding re-checked + repo-level `enable = false`/gating verified today), NOT a
   fresh live `systemctl` probe — `systemctl` is blocked in this agent sandbox ("command is not allowed
   for security reasons"). What remains: one user-side command
   (`systemctl list-unit-files | grep google-sync`) to make the claim literally live-verified. Effort: S.

## c) NOT STARTED (scoped to this item)

1. **All 5 go-live steps** — deliberately not started by an agent: (1) Google Cloud OAuth client
   (Drive API, publishing status "In production"), (2) `rclone authorize "drive"` once per account
   (×3: gdrive, gdrive-shared, gwork), (3) fill the 3-remote placeholder in
   `platforms/nixos/secrets/google-sync.yaml` (interactive `sops`, never inline values), (4) flip
   `enable` + `nix run .#deploy`, (5) verify the ~1.9 TB seed. Waiting on: user decision whether/when.
   Still wanted? Yes (it is the off-site 3rd-copy leg candidate — TODO P0 "Off-site backup decision").
2. **`docs/services/google-sync.md` runbook** — does not exist (verified: no match in `docs/services/`).
   Every comparable service has one; the go-live checklist currently lives only in AGENTS + the
   `google-sync-config-check` ExecStartPre fail-fast message. Agent-doable now (S) — not part of the
   minimal work item, so not done (smallest-correct-change rule).
3. **Dedicated Gatus check for google-sync** — none exists (only banned-pattern comments reference it
   in `gatus-config.nix`); when live, freshness rides solely `backup_all_healthy` via backup-coordination
   (`maxAgeHours = 25` vs a 5-min interval — generous). Decision whether that suffices: NOT STARTED.
4. **TODO_LIST "Updated:" header round-note** — not appended for this task. Reason: the header is a
   shared line actively edited by parallel sessions today (2026-09-11 already carries multiple rounds);
   appending mid-race risks clobbering another session's edit. Left to docs-health HARVEST rounds.

## d) TOTALLY FUCKED UP

Nothing in the deliverable is broken — but radical honesty requires listing the real stumbles:

1. **Commit race fumble (severity: low, recovered in-session)** — my `git add && git commit -- <paths>`
   lost a race with the continuously-running auto-commit daemon: it committed my two staged files first
   (`3de29dd0`), so my pathspec commit found zero diff vs HEAD and exited 1 with "no changes added".
   Root cause: multi-command add+commit is inherently racy against the daemon. Mitigation applied:
   verified the sweep contained only my files, amended the daemon commit into a proper message with the
   footer. Cost: one wasted command + a background-job round trip. Prevention (for future queue tasks):
   single-command commit, immediately followed by `git log --oneline -1` verification.
2. **Mild overclaim risk in the banner wording (severity: low)** — "confirmed 2026-09-11" could be read
   as a live host probe; it is a doc + repo-eval confirmation (see b.2). If anyone treats it as
   live-verified without running the one-line systemctl check, the phrasing promised slightly more than
   it delivered. Fix available: run the probe, or soften the wording next time the section is touched.
3. **Nothing else.** No broken builds, no unrelated files touched, no secret values encountered or
   written, no scope creep. The working tree's other dirty files
   (`modules/nixos/services/system-health.nix`, `scripts/data-corruption-repair.sh`,
   `tests/test-scripts.nix`) belong to parallel sessions and were left strictly alone per Critical Rules.

## e) WHAT WE SHOULD IMPROVE

1. **Generalize the DORMANT/DISABLED banner pattern** — google-sync now has it, monitor365 already had
   it; a docs-health VERIFY sweep over AGENTS sections describing "ships disabled" modules without a
   status banner would prevent the next "reads as live" doc bug (this one lived 11 days).
2. **Date-stamp WHAT KIND of confirmation** — convention fix: "confirmed <date>" should say
   "doc finding" vs "live probe". Cheap wording discipline, prevents overclaim drift.
3. **Read-only host-state probes for agents** — `systemctl` (even `list-unit-files`) is blocked in the
   sandbox, so deployed-state claims (DORMANT/live, unit presence) can only be inferred from repo
   config. A sanctioned read-only probe script (e.g. `scripts/host-unit-inventory.sh`, allowlisted)
   would let queue tasks verify against reality, not documentation.
4. **Runbook-per-service before go-live, not after** — write `docs/services/google-sync.md` BEFORE the
   user starts OAuth so the checklist, seed expectations (48h first run, `backup_all_healthy` red until
   first success), and grace-tree checks live in the runbook; AGENTS becomes a pointer.
5. **Auto-commit daemon vs queue-task attribution** — heuristic daemon commits carry no Task-Queue ID
   and can sweep staged queue-task work mid-commit. Today's amend works, but a convention
   ("queue tasks amend immediately after any daemon-race detection") or a daemon-side skip for files
   touched by an active task would remove the race class entirely.
6. **Gatus coverage decision deferred is fine, but record it** — when google-sync goes live, decide
   explicitly whether `backup_all_healthy` (25h budget) is the intended sole signal or whether a direct
   `last_success` check is wanted; right now the absence of a dedicated check is undocumented.

## f) NEXT THINGS TO GET DONE (brainstorm — up to 50, ranked by impact; HARVEST input)

| #  | Task                                                                                                                                         | Impact | Effort | Category      |
| -- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------ | ------------- |
| 1  | USER: decide whether Google Sync go-live is wanted now or stays dormant (gates everything below; ties into Off-site backup P0)                | Critical | S    | Decision      |
| 2  | USER: create Google Cloud OAuth client (Drive API, publishing status "In production" — testing mode expires refresh tokens in 7 days)         | Critical | M    | Go-live step  |
| 3  | USER: `rclone authorize "drive"` for the gdrive (personal My Drive) account                                                                   | Critical | S    | Go-live step  |
| 4  | USER: `rclone authorize` for the shared-with-me remote (own remote, `shared_with_me = true`)                                                   | Critical | S    | Go-live step  |
| 5  | USER: `rclone authorize` for the gwork Workspace account (+ decide if `team_drive = <id>` is needed in its config section)                     | Critical | S    | Go-live step  |
| 6  | USER: fill all 3 remote sections in `platforms/nixos/secrets/google-sync.yaml` via interactive sops (never inline values)                      | Critical | S    | Go-live step  |
| 7  | Agent: write `docs/services/google-sync.md` runbook (go-live checklist, seed expectations, grace-tree checks, rollback) — pre-work for #2-#10   | High   | S     | Documentation |
| 8  | Agent: verify the sops scaffold's top-level key names offline (names are plaintext; sops-key-audit compatibility) — no decryption needed        | High   | S     | Quality       |
| 9  | Agent: check whether a VM test exists for the google-sync module (none found in passing); if not, add config-check placeholder fail-fast test   | High   | M     | Quality       |
| 10 | USER: decide seed-era cadence — keep 5-min `interval` from day one, or start with a longer interval until the 1.9 TB seed converges            | High   | S     | Decision      |
| 11 | Agent: add a pool free-space pre-check before enabling (1.9 TB seed onto the HDD pool — verify ≥2 TB free incl. deletion-grace headroom)       | High   | M     | Feature       |
| 12 | USER: flip `services.google-sync.enable = true` in configuration.nix + `nix run .#deploy`                                                      | Critical | S    | Go-live step  |
| 13 | USER+Agent: verify first seed run stays inside `TimeoutStartSec = 48h` and MemoryMax = 2G (`--fast-list` ~1KB/object × 500k+ files)            | High   | S     | Go-live step  |
| 14 | Verify `/var/lib/google-sync/last_success` appears → `backup_all_healthy` flips green (document that it is RED until first success)           | High   | S     | Go-live step  |
| 15 | Verify deletion-grace tree `/mnt/pool/backups/google-drive-deleted/<remote>/` is created OUTSIDE all three synced destinations                | High   | S     | Go-live step  |
| 16 | USER: decide Google-native `exportFormats` (pdf) list is still correct at go-live                                                              | Medium | S     | Decision      |
| 17 | Verify Homepage tile appears once enabled (gating confirmed at `homepage.nix:53`, render unverified)                                           | Medium | S     | Go-live step  |
| 18 | Decide gatus coverage at go-live: sole `backup_all_healthy` vs dedicated `last_success` check (see e.6)                                        | Medium | S     | Monitoring    |
| 19 | Check whether google-sync lands in system-health `monitoredServices` automatically or needs a manual entry at enable time                       | Medium | S     | Monitoring    |
| 20 | Review IO tiering: does the sync unit carry `ioTier.background`? (not verified in passing; pool is HDD but shares the USB link with backups)    | Medium | S     | Quality       |
| 21 | Confirm `google-sync-config-check` ExecStartPre fail-fast message points at the new runbook path (currently AGENTS/checklist pointer)          | Medium | S     | Cleanup       |
| 22 | USER: one-line live probe to upgrade the DORMANT claim from doc-confirmed to live-verified (`systemctl list-unit-files | grep google-sync`)     | Low    | S     | Verification  |
| 23 | On go-live: flip the AGENTS DORMANT banner to LIVE with date + close/move the TODO row via docs-health                                         | Medium | S     | Documentation |
| 24 | Sweep AGENTS for other sections that describe disabled-shipping services without a status banner (monitor365 has one; google-sync now does)     | Medium | M     | Documentation |
| 25 | Adopt wording convention: "confirmed <date>" must state doc-finding vs live-probe (AGENTS-wide)                                                | Low    | S     | Convention    |
| 26 | Consider a sanctioned read-only host-state probe script for agent sandbox use (e.g. unit inventory) — see e.3                                   | Medium | M     | Tooling       |
| 27 | Document the daemon-race amend pattern for queue tasks (single-command commit + immediate `git log` verify) in AGENTS git section               | Low    | S     | Convention    |
| 28 | At go-live, verify `Persistent = true` + `RandomizedDelaySec = 90s` timer behavior across a reboot during the multi-day seed                   | Low    | S     | Go-live step  |
| 29 | At go-live, watch the first deletion event end-to-end (remote delete → grace tree, not instant loss) on a scratch file                          | Medium | S     | Go-live step  |
| 30 | Re-check rclone upstream for `--drive-skip-checksum-gphotos` still being required at the pinned rclone version (AGENTS claims it is load-bearing) | Low    | S     | Quality       |

Deliberately NOT in this list: unrelated repo P0s (data corruption, flm reboot, key rotation) — they
already live in TODO_LIST and are out of this task's scope.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Do you actually want the Google Sync go-live to happen — and if so, roughly when?** The TODO row
   is now closed as DORMANT; whether I should invest in the runbook + pre-checks (#7-#11) now, or the
   whole service stays parked indefinitely, is a pure intent call. I searched TODO_LIST/AGENTS for a
   timeline signal; the only hint is that Google Photos/Drive is named as the off-site 3rd-copy leg in
   the Off-site backup P0, which suggests eventual go-live — but nothing confirms it.
2. **For `gwork`: is it a plain My Drive account, or does it need `team_drive = <id>` in its config
   section at setup time?** The module supports both; the answer changes the sops scaffold content you
   will fill. I cannot decrypt the sops file to inspect the placeholder shape (no age key in-sandbox),
   and even with it, the team-drive decision is yours.
3. **Should queue tasks get a sanctioned read-only host-probe path (e.g. an allowlisted
   `scripts/host-unit-inventory.sh`), or is doc-inferred deployed-state acceptable for DORMANT/LIVE
   claims?** Today `systemctl` (read-only!) was blocked in my sandbox, which is why the 2026-09-11
   "confirmed" is doc-level. If you want live-verified claims from agents, this needs your call
   (it is a sandbox/permissions decision, not something I can grant myself).

---

*Point-in-time snapshot — goes stale. Section (f) is docs-health HARVEST input; items 7-11 are the
agent-doable pre-work if go-live intent exists.*
