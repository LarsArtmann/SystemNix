# Task Report: task-000001a0d79daeca24808d2e967271007d42

**Build the /data damage-set inventory (single source of truth; consumes the AGENTS.md snapshot-pinning doctrine table)**

| Field        | Value                                                                                                          |
| ------------ | -------------------------------------------------------------------------------------------------------------- |
| Date         | 2026-09-25 10:18 CEST                                                                                          |
| Work commit  | `f6380065` — `docs: build the /data damage-set inventory as the single source of truth` (Task-Queue-ID footer)  |
| Files changed| `docs/services/data-damage-set.md` (new, 81 lines), `docs/services/jan.md` (2 links), `docs/todo/storage.md` (row closed), `TODO_LIST.md` (row closed) |
| Pushed       | **No** (per contract)                                                                                          |
| Verification | `scripts/check-todo-system.sh` OK; pre-commit suite green on the commit (gitleaks, trailing whitespace, `nix flake check` eval-only) |

## TL;DR

The queue item asked for a single-source-of-truth inventory of the /data corruption damage set whose
timeline CONSUMES (never duplicates) the AGENTS.md snapshot-pinning doctrine table. Delivered as
`docs/services/data-damage-set.md` — placed under `docs/services/` rather than inside
`docs/todo/storage.md` because the library's own house rules forbid system-state narratives there
("AGENTS.md and docs/services/* runbooks own state"); the TODO_LIST arrow
"→ docs/todo/storage.md" was read as the domain-library pointer (the convention every sibling row
uses), not the deliverable's file path. Content: root-cause + scrub-evidence table (2026-08-03
signature → 2026-09-11 gate-a PASS, gate-b formally pending), the full victim set (1 gone + 10 live
~49G, all re-downloadable), the decoupled repair-gate consequence (btrbk resume ~1 day vs scrub-clean
~4w), and a change protocol that registers the inventory as a live surface of jan.md's atomic doc
sweep. Both queue surfaces closed with DONE notes + pointers.

---

## Brutal Self-Review

### a) FULLY DONE

1. **Inventory doc created** — victims (gemma GGUF trashed 2026-09-12; inode 1331118 evidence-corrected gone; the 10 live T05-resolved files with root/ino, path, size), scrub evidence table, snapshot-pinning timeline, change protocol.
2. **Doctrine-table consumption honored** — the timeline section explicitly delegates pin windows to the AGENTS.md "Snapshot-pinning doctrine" table and carries ONLY the damage-set's position on them (snapshot coverage of the 10 files, the T06→T09 decoupling, the trash-copy pin); no window numbers duplicated.
3. **Cross-links landed** — jan.md RESOLVED bullet + recipe step 3 now point at the inventory; the recipe's atomic doc sweep formally includes it for future victims.
4. **Queue closure on both surfaces** — TODO_LIST.md row 45 `[x]` + DONE note; `docs/todo/storage.md` row `[x]` + strikethrough + pointer, per each file's conventions.
5. **Commit discipline** — the auto-commit daemon swept my 3 doc files mid-task; I `--stat`-verified it contained exactly my files, then amended forward with TODO_LIST.md included and the Task-Queue-ID footer (verified present via `git log --format=%B | grep -c`). Tree clean afterwards. Never pushed.

### b) PARTIALLY DONE

1. **Victim-set ground truth is doc-derived, not re-probed live.** Every row is transcribed from the 2026-09-11 T05 round (recorded in the storage P0 row) and the 2026-09-12 report — I did not re-run the `find /data -xdev -inum` walk or `stat` the 10 files this session. Defensible (the queue item was a docs consolidation; the walk is a user-runnable read-only op and the P0 row is the fresher authority), but the inventory inherits any staleness in its sources. Mitigation: the doc names its provenance per section.
2. **Gate (b) status is recorded as of 2026-09-11 evidence.** The scrub-mechanism fix has since deployed and scrubs may have completed since; a fresh `btrfs.prom` read would let the inventory say "gate b: delta X" instead of "pending". Left to the P0 row's own verification flow — updating it here would duplicate state the P0 row owns.
3. **The "~12 journal pairs vs 11 enumerated" reconciliation is stated, not resolved** — I carried the P0 row's "residual is journal-era noise" framing; nobody has actually explained the missing pair(s) from the retained journal.

### c) NOT STARTED (deliberately out of scope)

1. **The /data corruption repair itself** (T04–T08) — blocked:user, verification-first, untouched per scope discipline.
2. **Trash-purge policy for `/data/.Trash-1000`** — owner decision, referenced not made.
3. **GGUF re-download for gemma-4-31b-abliterated** — user-gated (source URL unknown; no URL guessing).
4. **The sibling storage rows seen in passing** — all queue-owned, none touched: flm↔doctrine cross-link, `docker system df` sizing refresh, root `@` retention anomaly reconcile, btrbk receive-freshness legs, offsite-Borg go-live chain, single-victim-recipe adjacent items already done by prior runs.
5. **No status-report rewrite of history** — `docs/status/**` archives left alone per the ANNOTATE-never-rewrite doctrine.

### d) TOTALLY FUCKED UP

**Nothing at category-d severity.** Near-misses, fully disclosed:

1. **First three edits failed "must read the file first"** (jan.md ×2, storage.md ×1) — I edited from bash-`sed`/grep context instead of the View tool. Recovered in one step each; no wrong content written. Wasteful, not harmful.
2. **Initial edit targets were written before the read-back verification loop existed in my flow** — had any of the three files drifted under a parallel session between my grep and my edit, the exact-match edit would have silently targeted stale context. The repo's multi-agent write discipline (content-pin before write) was applied to git state (`--stat` before amend) but only implicitly to file state (via the tool-enforced reads, which fortunately forced the check).

### e) WHAT WE SHOULD IMPROVE

1. **The inventory has no freshness tripwire.** It records gate-b as "pending" with a 2026-09-11 as-of; when the scrub delta finally lands, nothing reminds anyone this doc says "formally unproven". A one-line "re-verify this section at the next scrub" marker (or folding the as-of dates into a tiny header table) would make staleness visible.
2. **Victim rows should carry verification commands inline** (the `find -inum` / `stat -c %i` one-liners from T05) so the next person re-proving a row doesn't reconstruct them from the P0 row's prose.
3. **The `docs/services/data-damage-set.md` naming breaks the services-runbook convention lightly** — it is a state doc, not a service runbook. An alternative home (`docs/storage/` or a `docs/ops/` dir) would be cleaner, but creating a new docs top-level dir for one file is worse; noted for whoever grows the damage set.
4. **The doc-sweep recipe now has FOUR surfaces** (TODO row/library, runbook, AGENTS.md, inventory) but jan.md's step 3 lists them as "the TODO row/library entry, the owning service runbook, and AGENTS.md, plus the inventory" — the inventory is a bolt-on clause. If a fifth surface ever appears, restructure step 3 into an explicit checklist.
5. **Task-queue DONE notes on TODO_LIST rows are growing past one line** (mine included); the convention says one-liners. The DONE note + original ask + Source triad now runs 4+ lines per closed row, which is exactly the accretion the 2026-09-19 TODO split was meant to prevent. Consider pruning `[x]` rows to CHANGELOG at the next pass instead of annotating in place.

### f) NEXT THINGS (queue-owned; seen in passing, not re-verified — ordering is my judgment, the queue's gates decide)

1. Re-probe the 10 live victim rows against the live filesystem once (`stat -c %i` per path) and stamp the inventory with a fresh as-of date — closes my b-1 honesty gap. (storage)
2. When the first post-fix scrub completes, update gate (b) in the P0 row AND the inventory's evidence table in ONE commit (the inventory is now a doc-sweep surface). (storage, blocked:user)
3. Add the inline re-verification one-liners to the inventory's victim table (e-2). (storage, S)
4. Reconcile the "~12 pairs vs 11 enumerated" journal residual explicitly or downgrade the P0 row's "~12" wording. (storage, S)
5. flm AGENTS bullet ↔ snapshot-pinning doctrine cross-link (existing `[ready]` queue row). (storage)
6. `docker system df` sizing refresh for the doctrine table (existing row). (storage)
7. Root `@` retention anomaly reconcile (12-day weekly vs `3d 1w`) (existing row). (storage)
8. btrbk root pool-receive freshness: confirm the 23:00 self-heal window keeps age ≤3d through the next verify run. (storage `[watch]`)
9. golangci-lint storm-fallback (`~/tmp/go-lint` 1.8G) reclamation path (existing `[ready]` row). (storage)
10. MountPoint-vs-HM-symlink eval-time guard (existing `[ready]` row). (storage)
11. `discordsync-attachments-migrate` stub-fixture test BEFORE its triggering deploy (existing `[ready]` row). (storage)
12. browser-history dbBackup-ON VM coverage (existing `[ready]` row). (storage)
13. Pre-deploy §10 registry-gained backup-metric loan fixture (existing `[ready]` row). (storage)
14. Offsite-borg positive-render guard → shared pure lib extraction (existing `[ready]` row). (storage)
15. Pre-deploy §13 classifier eval-shape fixture (existing `[ready]` row). (storage)
16. Offsite-borg runtime FAIL-shape VM rehearsal (existing `[ready]` row). (storage)
17. Post-deploy: confirm the three HM fallback-cache symlinks + activation block landed (existing `[watch]` row). (storage)
18. Post-EIO-repair: `btrbk-data` cgroup `memory.peak` ≤ ~4G proof under a real full send (existing `[watch]` row). (storage)
19. Owner decisions pending in storage domain: 2-disk buildcache merge window, Borg go-live inputs, hot-db Phase-2 waves, `/data` repair sign-off, gemma re-download, trash-purge policy, archived/* eager-migrate, maxdepth-3 doctrine, BuildFlow quarantine ratification, go-build hot-tier relocation design. (all blocked:user/decision — listed once here, not re-enumerated per row)
20. Post-repair housekeeping this doc enables: after T06 deletion + scrub-clean, append the resolution rows here and STRIKE the live table (the inventory is the close-out surface). (storage, gated on #2)

### g) QUESTIONS (cannot figure out myself)

1. **Placement doctrine:** is `docs/services/data-damage-set.md` acceptable as the permanent home for a STATE doc inside the services dir (c) — or should the repo grow a `docs/ops/` (or similar) home for non-service state docs, and this file move there once the second resident appears?
2. **Live re-verify now or ride the P0 flow?** The 10 victim rows are doc-derived (b-1); a read-only `stat` sweep is user-runnable and would stamp fresh as-of dates. Do you want a follow-up queue item for that, or is the P0 row's own T06a flow the single sanctioned touchpoint for the victim set?
3. **DONE-note verbosity:** should the queue's `[x]` rows keep accumulating in-place DONE notes (my row runs ~4 lines), or do you want closed rows pruned to CHANGELOG at every pass as the house rule literally states — in which case my in-place annotations this run were convention-noncompliant and the next pass should trim them?

---

Nothing from THIS task's ask remains open. All follow-ups above are already tracked or newly foldable into the storage library; none was acted on this run (scope discipline).
