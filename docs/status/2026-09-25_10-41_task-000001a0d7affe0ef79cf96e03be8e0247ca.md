# Task Status Report — flm ↔ Snapshot-Pinning Doctrine Cross-Link

**Task-Queue-ID:** `000001a0d7affe0ef79cf96e03be8e0247ca`
**Generated:** 2026-09-25 10:41
**Scope:** one queue item from TODO_LIST.md "storage" — cross-link the flm AGENTS bullet to the snapshot-pinning doctrine table (one-line symmetry with jan.md).
**Format note:** the status-report skill defaults to a styled HTML dashboard; the tasking explicitly requested this Markdown path, so the user instruction wins and this report is Markdown.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | The queue item itself: the flm Package bullet's re-pull sentence in the repo AGENTS.md now carries the snapshot-pinning caveat (in-place `model.q4nx` overwrite pins old AND new weights through the ~4-5w /data window) and points at the BTRFS section's "Snapshot-pinning doctrine" table | AGENTS.md flm section, Package bullet — one-sentence append, zero other content touched |
| 2 | Symmetry goal achieved: jan.md already pointed at the doctrine table twice (disk-space cleanup bullet + repair recipe step 1); the flm bullet now does too. This was discoverability-only debt ("one-line asymmetry" per the source report §f2) and is now closed | jan.md:43, jan.md:49 vs AGENTS.md flm bullet |
| 3 | Queue closed in BOTH surfaces without drift: TODO_LIST.md row marked `[x]` with a DONE one-liner (queue convention: one ask + Source pointer preserved), and the library row in docs/todo/storage.md:66 struck through with a DONE 2026-09-25 note (library convention: `~~…~~ **DONE …**`) | TODO_LIST.md, docs/todo/storage.md |
| 4 | Verification gates all green: `scripts/check-todo-system.sh` → "OK: TODO queue/library structure clean"; pre-commit on the commit ran gitleaks (pass), trailing-whitespace (pass), `nix flake check` eval-only (pass) | commit 87cf9c6d hook output |
| 5 | Committed with the exact Task-Queue-ID footer, never pushed | `git log -1` — `Task-Queue-ID: 000001a0d7affe0ef79cf96e03be8e0247ca` |

## b) PARTIALLY DONE

| # | Item | Gap |
|---|------|-----|
| 1 | **Second flm re-pull mention not cross-linked.** The flm "Known crash (2026-08-21)" bullet also says "any future bump still needs the live-serve validation + one-time re-pull discipline (Q4_K weights hash-mismatch the v1.0.2 manifest)" — no doctrine pointer there either. The queue item asked for "the flm bullet" (singular) and the Package bullet is the canonical one (it describes the actual overwrite mechanics), so the letter of the item is satisfied — but the Known-crash bullet is a second discovery surface for the same trap. Judged out of scope for a one-line task; logged as follow-up (see f-#1). | AGENTS.md Known-crash bullet |
| 2 | **Commit had to be amended into shape.** The auto-commit daemon swept my three staged files into `chore: auto-commit 3 changed file(s) (heuristic)` between staging and my commit attempt; my commit then correctly found "nothing to commit" (exit 1) and I amended the daemon's HEAD into the proper message per the AGENTS.md daemon-race discipline. The end state is correct — but the initial `git commit` invocation itself was wasted motion that a `git log -1 --stat` check BEFORE attempting the commit would have avoided. | commit 87cf9c6d history |

## c) NOT STARTED

Nothing within this task's contract was left unstarted — the item is fully closed. Adjacent rows I saw in passing and deliberately did NOT touch (scope discipline, all already tracked in the queue):

| # | Item | Where it lives |
|---|------|----------------|
| 1 | Repair-recipe cross-link from the BTRFS /data-damage context (the mirror-image asymmetry of my task: the recipe lives in jan.md + the Jan AGENTS bullet, but the BTRFS section where a future corruption session starts has no pointer) | docs/todo/storage.md:130 — `[ready]`, already queued |
| 2 | Doctrine table `/data/docker` sizing refresh via `docker system df` | docs/todo/storage.md:67 |
| 3 | Root `@` retention anomaly reconciliation (12-day-old weekly vs `snapshot_preserve 3d 1w`) | docs/todo/storage.md:68 |
| 4 | Post-Borg-go-live offsite column for the doctrine table | docs/todo/storage.md:69 (`[watch]`) |
| 5 | Free-space triage order codification in the doctrine (`[decision]`, owner call) | docs/todo/storage.md:71 |

## d) TOTALLY FUCKED UP

**Nothing.** No broken state, no revert needed, no scope creep, no data risk. The one wobble (daemon race, section b-#2) was self-healed inside the run and is a process lesson, not damage. Honest nitpicks only:

- The wasted commit attempt (exit 1) is cosmetic noise in the shell transcript, zero repo impact.
- I did not write the report's section-(f) backlog items into TODO_LIST.md at run time — the contract for THIS task forbids appending items describing this task's work, and all genuinely new follow-ups surfaced only while writing this report (see f-#1/#2, which are new). Per the contract I may append them; flagging that I chose to leave them in this report for the HARVEST pass rather than growing the queue mid-run (see section g, question 1).

## e) WHAT WE SHOULD IMPROVE

| # | Improvement | Why |
|---|-------------|-----|
| 1 | **Check the daemon BEFORE committing, not after.** The multi-agent discipline says "verify WHAT the daemon actually staged" before amending — the same check applied a few seconds earlier (a cheap `git log -1 --stat`) would have skipped the doomed commit entirely. Make "pre-commit HEAD peek" part of the personal sequence for any task in this tree, not just the amend path. | The race is the NORM here (daemon commits ~every 10 min; queue tasks stage small diffs), so the check pays off nearly every run |
| 2 | **Asymmetry sweeps should grep the whole section, not just the named bullet.** The queue item named one bullet; a single `grep -n "re-pull" AGENTS.md` (which I ran for the jan.md comparison) already showed the SECOND unlinked mention. One extra edit would have closed both. Small tasks invite tunnel vision — the fix is mechanical: when a task is "add a pointer for X", grep X everywhere first, then decide scope explicitly. | Catches the b-#1 class for free |
| 3 | **Docs-only tasks still deserve the cheap gates.** I ran `check-todo-system.sh` because the queue files were edited — worth keeping as a reflex, since the queue's own conventions (struck-through library rows, DONE one-liners with Source) are exactly what that script guards. | Prevents the "edited two files, broke the third convention" class |
| 4 | **The discovery asymmetry this task fixed suggests a general pattern worth one audit:** every AGENTS.md service bullet that mentions a /data-resident artifact being overwritten/deleted in place (flm weights; Jan model deletes are already linked; ollama/gguf rows exist in the table but may lack inbound bullet links). A 15-minute grep pass could close all remaining inbound-pointer gaps in one sweep instead of one queue item per bullet. | Each one is a separate queue item today; a sweep amortizes the discovery cost |

## f) Up to 50 things we should get done next

Ranked roughly by impact; items 1-2 are new (surfaced by this run), the rest are already-queued work I saw in passing, restated here only so this report's HARVEST input is complete. New items are marked **(NEW)**.

**Direct follow-ups from this run:**
1. **(NEW)** Cross-link the flm "Known crash" bullet's re-pull discipline sentence to the doctrine table (second discovery surface, same one-line shape as this task).
2. **(NEW)** One-sweep audit: grep AGENTS.md service bullets for in-place overwrite/delete of /data-resident artifacts lacking a doctrine-table pointer; fix all in one commit (generalization of this task; subsumes #1 if run instead).
3. Repair-recipe cross-link from the BTRFS /data-damage context (storage.md:130 — the mirror-image of this task; a future corruption session starts at the BTRFS section).
4. Refresh the doctrine table's `/data/docker` row via `docker system df` (docker group, no sudo).
5. Reconcile the root `@` retention anomaly (12-day-old weekly vs `snapshot_preserve 3d 1w`) and restate the root window.
6. Codify the free-space triage order (nix-gc → cache → trash-empty → wait-out-window) in the doctrine — owner `[decision]`.
7. Post-Borg-go-live: add the offsite column to the doctrine table (`[watch]`, gated on go-live).

**Already-queued storage-domain work visible in docs/todo/storage.md (context of the doctrine table this task links to):**
8. Complete the `/data` damage-set repair decisions for the 10 live victims (~49G) — the inventory at docs/services/data-damage-set.md is the source of truth.
9. /data P0 EIO corruption repair (the btrbk-data leg stays dead until it — the doctrine table's /data window assumptions inherit this).
10. Offsite Borg go-live (owner inputs: StorageBox hostname/username + host-key pin) — unblocks #7 and the 3-2-1 posture.
11. Borg restore drill against the real repo (go-live checklist step 9; script already stand-in-proven).
12. Fold `crush-hot-db` interim mechanism into the ratified `services.hot-db` Phase-2 module (do not run both).
13. Single-source the four hand-kept buildcache fallback-reap surfaces (deploy.sh two loops, recovery step 2.5, HM activation block).
14. Provision the three newer fallback cache targets in `buildcache-init` (post-recovery symlinks dangle until next deploy).
15. Execute the 2-device btrfs buildcache merge at the next maintenance window (both SanDisks, script rewritten, module flip must ride together).
16. Root-context repro of the FSETID/sandbox setattr EPERM mechanism (P0 from the discordsync migration saga — mechanism still unresolved).
17. Measure `/var/lib/forgejo` post-mass-migration to project btrbk root-snapshot growth (needs forgejo user).
18. Decide the two Artmann-Minecraft transferred mirrors (DarkBlocks, DialogesWebInterface): delete or re-mirror into a forgejo org.
19. Clean the two stale forgejo `commit-graph.lock` files (needs forgejo user).

**flm/AI-stack adjacent (the bullet this task edits):**
20. Staged flm v1.0.3+ bump go-live: upstream issue is ELIGIBLE per the staged-bump gate — requires the live-serve validation + one-time re-pull discipline (the exact operation this task's caveat warns about).
21. The OWED reboot (clears the flm :52626 corpse, llama-rag D-state corpses, zram sizing) — run `pre-reboot-check` first.
22. llama-rag root-cause: the mid-load CPU spin reproduces on the PINNED 0.3.0 build — bisect ROCm runtime/kernel/GPU-state upstream of llama.cpp; unit-context soak required before any re-enable.
23. Wire a groq API key for CV's ChatService or disable the provider (the standing `/health` warn).

**Docs-system hygiene (the system this task operated in):**
24. HARVEST this report's section (f) into TODO_LIST.md / domain files per docs-health routing.
25. Annotate the two prior 2026-09-25 sweep reports (000001a0d56f… §f2 and 000001a0d686…) that this task closes into — docs-health ANNOTATE mode, so future readers see the closure at the source.

(Items beyond ~25 would be padding — the honest answer is that this run surfaced ~2 genuinely new items and the rest of the credible backlog already lives, better-formulated, in docs/todo/*.md. Stopping at 25 rather than inventing 25 more.)

## g) Questions I cannot figure out myself

1. **Should new follow-up items (f-#1, f-#2) be appended to TODO_LIST.md by the NEXT queue run (via HARVEST), or do you want follow-up discovery appended inline at commit time on every task?** My task contract said I MAY append new items but must never append items describing THIS task's work — f-#1 is arguably "this task's work, one bullet over", which is why I left it here. A standing rule would remove the ambiguity.
2. **For the generalization sweep (f-#2): is a single multi-bullet AGENTS.md pointer sweep acceptable as one queue item, or do you prefer one item per bullet for reviewability?** The queue's one-ask-per-item convention cuts against the sweep's efficiency; owner preference decides.
3. **The auto-commit daemon raced my staged commit within seconds of `git add`.** Is that expected cadence (heuristic batch every ~10 min, this one just landed mid-run), or should task-queue runs coordinate a daemon pause window while staging? I worked around it correctly this time; if the race is this tight every run, a documented "stage-and-commit in one command" convention would save every future run one wasted attempt.
