# Task 000001a0b1666334580cacecb0852d0eea69 — Forgejo upstream mirror-outage issue filing (verification run)

**Queue item:** "Forgejo upstream: file the 3 verified mirror-outage issues — dead-queue silence, TouchMirror masking failures, ENOENT aborts of clone credential helpers (verify against CURRENT upstream main first)." **Source:** `docs/status/archived/2026-08-22_08-52_forgejo-mirror-outage-diagnosis-monitoring-curl-hardening.md` §b2/§c.

**Outcome in one line:** the three bugs are re-verified against the CURRENT upstream `forgejo` branch (2026-09-18, code unchanged where it matters, zero duplicates found) and fully drafted, but the ISSUES THEMSELVES WERE NOT FILED — this machine has no Codeberg account/token, and that blocker was knowable from the 08-22 report's own open question g.2 before I started. Commit `e2ef0c4e` (amended into the daemon's auto-commit) carries the drafts + TODO BLOCKED annotation, with the `Task-Queue-ID` footer.

---

## a) FULLY DONE

1. **Bug 1 re-verified against current upstream — `TouchMirror` masks failures.** Fetched `services/mirror/mirror_pull.go` and `models/repo/mirror.go` from `codeberg.org/forgejo/forgejo` branch `forgejo` (2026-09-18). Confirmed verbatim: `SyncPullMirror` does `if !ok { repo_model.TouchMirror(ctx, m); return false }` and `TouchMirror` sets `m.UpdatedUnix = timeutil.TimeStampNow()` — the same value `runSync` sets on success. API `mirror_updated` therefore reports last ATTEMPT time, not last success. Code is identical to the v15.0.6 evidence in the 08-22 report. No fix landed upstream since.
2. **Bug 2 re-verified — wedged mirror queue is Trace-silent.** Fetched `services/mirror/mirror.go` and `services/mirror/queue.go` from current `forgejo` branch. Confirmed: the queue is `queue.CreateUniqueQueue(..., "mirror", ...)`; `Update()`'s cron push handles `queue.ErrAlreadyInQueue` with `log.Trace` and returns nil; `addMirrorToQueue` only Escalates non-AlreadyInQueue errors. A wedged/unconsumed queue produces zero journal output at `LEVEL = info`, exactly as the incident showed.
3. **Bug 3 re-verified — credential-helper temp-file failure aborts syncs, log-only.** Fetched `modules/git/command.go` from current `forgejo` branch. Confirmed `AddAuthCredentialHelperForRemote` still does `os.CreateTemp("", "forgejo-clone-credentials-")` and returns the raw error, and `runSync` still converts that into `log.Error + return nil, false` — **without** the `system_model.CreateRepositoryNotice` that the fetch-failure path a few lines below does get. The two suggested improvements (repository notice + error context with `os.TempDir()`) hold against current source.
4. **Duplicate sweep (verify-before-filing Gate 5) — all three clean.** Searched Codeberg issues (`/api/v1/repos/forgejo/forgejo/issues?q=...`, state=all): `TouchMirror` → 0 hits; `forgejo-clone-credentials` → 0 hits; "mirror queue stuck" → only unrelated results (no issue about unique-queue wedging or dedup-skip silence). Also noted in passing: adjacent-but-different mirror issues DO exist (#14273 push-mirror ref rollback, closed) — none overlap our three mechanisms.
5. **All three issue drafts written, voice-checked, persisted.** `docs/services/forgejo-upstream-issues.md` (188 lines): per-issue Problem with verbatim current-branch code quotes, the real-incident narrative, a concrete minimal suggestion, version line ("observed on 15.0.6; code identical on the current `forgejo` branch, checked 2026-09-18"), and an AI-drafted footer. Ran `github-voice`'s `check-draft.py --kind body-issue --ai-drafted`: **0 FAIL, 0 WARN** after adding the required attribution footers.
6. **TODO_LIST.md closed out honestly.** Item left UNCHECKED with a one-line BLOCKED reason naming the actual blocker (no Codeberg token; 08-22 report question g.2 never answered) and pointing at the drafts file.
7. **Committed with the queue footer, hooks green.** The auto-commit daemon raced me (commit `4483a449` batched exactly my two files); I verified `git show --stat HEAD` contained ONLY my files (the amend-safety rule), then amended into `e2ef0c4e` with a real message + `Task-Queue-ID: 000001a0b1666334580cacecb0852d0eea69` footer. Pre-commit ran: gitleaks pass, flake check (eval-only) pass. Not pushed. Working tree clean.

## b) PARTIALLY DONE

1. **The task itself: verification 100%, filing 0%.** The queue item says "file the 3 verified issues." I verified and drafted but could not submit: filing on codeberg.org needs a token, and no Codeberg credential exists anywhere on this machine (checked `tea`/`forgejo-cli` binaries, `~/.config/tea`, git config, `~/.git-credentials`, `~/.netrc`, git credential helper, sops file _names_ — the secrets are all service-scoped, none for a Codeberg account). Per contract #3 the item stays unchecked with the BLOCKED line. The drafts make the remaining work a single authenticated API call — but that remaining work is real and still open.
2. **Draft polish deferred to filing time.** Forgejo's issue culture (observed live in the repo's recent issues) favors the `problem:`/`bug:` title prefix convention; my draft titles do not carry it yet — I left titles descriptive rather than guessing the exact prefix convention, and flagged nothing in-file about it. Should be fixed at filing time (one-line edit per issue).
3. **The 08-22 report's own framing question stayed open.** Question g.2 ("file under your account? I need your go-ahead + account context") was the original blocker and remains exactly that; my run converted it from "unverified evidence awaiting approval" to "verified drafts awaiting credentials," which is progress but not closure.

## c) NOT STARTED

1. **Actually POSTing the three issues** to `POST /api/v1/repos/forgejo/forgejo/issues` (or the web UI) — blocked on token, see b.1.
2. **Cross-linking the three issues to each other** once filed (the drafts say "cross-link them when filing") and to the companion context — trivially done in the same API session.
3. **The follow-on forgejo hardening items from the same 08-22 report** (§f.10 VM test for DNS-gate boot ordering, §f.11 `forgejo-github-sync` mirror_updated freshness sensing, §f.12 post-deploy functional mirror probe) — these live in TODO_LIST line 461 ("Forgejo mirror monitoring depth") and were explicitly NOT this task's scope; untouched and still unchecked.
4. **Upstream-fix monitoring loop.** Nothing watches whether the filed issues get fixed upstream (the repo's own `ref=master`-pin discipline would want a probe when a fix lands). Not built; would belong in the backlog only after filing succeeds.

## d) TOTALLY FUCKED UP

1. **I should have checked for Codeberg credentials FIRST, before any verification work.** The 08-22 report I was told to source explicitly says, in its g-questions section: "needs the verify-before-filing outbound pass **+ a Codeberg account decision**." That sentence was in my context from minute one. I read the skill, fetched four upstream source files, ran the duplicate sweep, and wrote 188 lines of drafts — all correct work — before discovering the submission blocker that the source document had already flagged as unanswered. Cost: the whole session ran with a known-unanswerable question at its end. A 30-second credential probe at the start would have either (a) found a token and let me actually file, or (b) confirmed the block so I could have still done the verification work but reported the blocker structure immediately. The work wasn't wasted (the drafts were the deliverable's substance), but the sequencing was backwards: gate before grind.
2. **Minor: first TODO_LIST edit failed on a stale-read.** The auto-commit daemon re-committed mid-session and my `edit` hit the "read the file before editing" guard with shifted content; I re-read and re-located the line before retrying. No damage, one wasted round trip — but it's the same "file modified since read = another session is active" race AGENTS.md warns about, and I hadn't re-checked `git status` after my earlier `view`.
3. **Minor: the voice checker caught me, not my own review.** I shipped the drafts without the AI-attribution footer and only found out by running `check-draft.py` (1 FAIL). The fix was 30 seconds, but the miss was mine: I had just read the github-voice skill's rule ("bodies … Crush footer when Crush drafted") and still didn't apply it on the first pass.

## e) WHAT WE SHOULD IMPROVE

1. **Order the gates before the work, always.** For any "file/report to external project X" task, step 1 is "do I have credentials for X," not "is the diagnosis true." Verify-before-filing itself orders content-gates first and never mentions the credential gate — but the 08-22 report already encoded the lesson locally ("needs … a Codeberg account decision") and I skipped past it. Rule candidate for AGENTS.md: _external-submission tasks begin with an auth probe; content work proceeds only after the submission path is confirmed or the blocker is declared._
2. **The verify-before-filing skill has no credential/submit-ability gate.** All five gates are about the diagnosis; none asks "can you actually submit from this environment?" Worth proposing upstream (it's a local skill, `~/.agents/skills/verify-before-filing/`): a Gate 0 or a §7 anti-pattern "verified but unfiled is a deliverable only if the blocker is environmental and declared."
3. **Credential discovery should be a checklist, not an archaeology dig.** I probed `tea`, `~/.config/tea`, git config, credential files, env vars, and sops file names one at a time. A single `scripts/check-upstream-creds.sh` (host: token) would make the auth probe one command and would also serve every future upstream-filing queue item.
4. **Draft files should carry their own filing procedure.** My drafts doc says "filing is one API call" but doesn't embed the exact `curl`/`tea` command shape or the title-prefix convention. Next editor should paste the filing commands into the doc so the task needs zero reconstruction.
5. **Amend-into-daemon-commit worked as designed — keep proving it.** The `git show --stat HEAD` verification before amending is what made the footer land safely instead of absorbing a parallel session's files. This is the second consecutive session where the daemon raced an explicit commit; the discipline is holding, keep doing it every time.
6. **Full-text issue search on Codeberg is weak for duplicate sweeps.** The `q=` API search returned mostly-unrelated recent issues for my multi-word queries (only the exact-string `forgejo-clone-credentials` and `TouchMirror` searches were sharp). For future duplicate checks: prefer exact identifiers (function names, error strings) over symptom phrases, and search closed state explicitly (I did, but note it).

## f) NEXT UP TO 50

**P0 — finish this task (one credential away):**

1. **(USER) Create/locate a Codeberg account + API token with issue-write scope** — this is the only blocker; the account decision from the 08-22 report's g.2 folds into it.
2. Store the token (sops under a dedicated key, or user-run `tea login add`) — never in a doc; then re-run this queue item and POST all three issues from `docs/services/forgejo-upstream-issues.md`.
3. Add the `problem:`/`bug:` title prefixes per Forgejo convention at filing time.
4. Cross-link the three filed issues to each other + to the 08-22 incident context in a closing comment.
5. Mark the TODO_LIST item `[x]` with the three issue URLs.

**P1 — follow-ons from the same incident (already tracked in TODO_LIST line 461, untouched here):**

6. `forgejo-github-sync` should verify per-repo `mirror_updated` freshness and fail loudly on stalled mirrors (its "0 failed" was blind for 4 days).
7. post-deploy-check: functional forgejo mirror probe (API `mirror_updated` age < interval + slack).
8. VM test for forgejo DNS-gate boot ordering (dnsblockd-not-ready boot must not produce a PocketID-dead forgejo).
9. AGENTS.md forgejo mirror gotcha entry (dead queue / TouchMirror masking / restart heals) — the 08-22 report's f.22, still pending the dbPath-era rewrite.
10. Evaluate forgejo `[queue]` persistence/flush settings that survive crashes (08-22 §f.9).

**P2 — improvements noticed in passing (new, small):**

11. Propose the Gate 0 (submit-ability probe) addition to the local `verify-before-filing` skill.
12. Write `scripts/check-upstream-creds.sh` (host→token probe across tea/git-credentials/netrc/env/sops key names).
13. Embed exact filing commands into `docs/services/forgejo-upstream-issues.md` once a token exists (so the doc is self-executing).
14. Consider a tiny AGENTS.md rule: "external-filing queue items start with an auth probe."

**P3 — nothing else.** This task was deliberately narrow (docs + TODO only; no Nix surfaces touched, no build/test changes needed — pre-commit's eval-only flake check passed on the commit). Items 1-14 above are the complete follow-on set; padding to 50 would invent scope the queue's pacing gates should not pay for.

## g) QUESTIONS (cannot resolve myself)

1. **Do you have a Codeberg account, and may one be created/provisioned for upstream filings?** Everything else is done; this is the only gate. If yes: what token path/store do you want (sops key vs. `tea login`)?
2. **If no Codeberg account is wanted:** should the three drafts be filed through a different channel (e.g. via a maintainer contact, or the Forgejo forum/matrix), or shelved until the account decision flips? The drafts do not decay (code-verified 2026-09-18), but a fix upstream would.
3. **Filing etiquette call (yours, not mine):** Forgejo's recent issues include explicit "AI disclosure" lines on AI-drafted bodies; my drafts carry the disclosure + Crush footer. Confirm you want that disclosure kept as-is on the three filings, or prefer disclosure only when asked (the corpus suggests your recent filings like #14355 chose explicit disclosure — I matched that, but it's your name on it).
