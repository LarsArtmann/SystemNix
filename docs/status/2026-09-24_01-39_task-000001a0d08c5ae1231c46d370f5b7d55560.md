# Status Report — task 000001a0d08c5ae1231c46d370f5b7d55560 (3rd dispatch)

**When:** 2026-09-24 01:39 CEST · **Scope:** THIS task only (borg-restore-drill flake fixture) + what was noticed in passing
**Format note:** Skill default is a styled HTML dashboard; the tasking explicitly requested `.md` at this path — user instruction wins, override flagged here and NOT propagated back into the skill.

---

## a) FULLY DONE

| Item | Evidence |
|---|---|
| The task's deliverable — `checks.x86_64-linux.borg-restore-drill-fixture` in flake.nix — was already landed and committed by an earlier dispatch of this same task ID (`d7a54819`, report 01-27) | TODO_LIST.md line 33 `[x]`, docs/todo/storage.md:46 `[x]` |
| This dispatch verified the claim instead of trusting it: `nix build .#checks.x86_64-linux.borg-restore-drill-fixture` → green store path `bdz4gkd…` | build output, this session |
| Fixture coverage re-read and confirmed against the hardened drill script: root-gate die (+ FAIL record persisted), bogus `--local`, repo-without-config, PLACEHOLDER tripwire via fixture `BORG_ENV_FILE`, secrets-missing die, happy-path `--local` PASS incl. record content, `--verify-data` 1.x `--dry-run` fallback + `--archive` pin | flake.nix:1419-1562 |
| Tree left clean; queue item already `[x]` with correct DONE line; no bogus re-commit | `git status` empty |

## b) PARTIALLY DONE

- **Fixture coverage of the real-mode `/mnt/hot` mountpoint gate** — NOT asserted (documented sandbox limit: it sits behind `/run/secrets/*` existence checks, and `[ -e ]` is an unstubbable bash builtin). The secrets-missing die is asserted as its direct predecessor. Honest gap, correctly documented in the fixture header — but it means the 2026-09-24 hardening's flagship gate has zero automated proof.
- **This dispatch itself** — verification-only was the correct minimal action, but it produced no incremental value beyond the re-verification: the queue handed the same task ID a third time after it was already done and committed twice (reports 01-27 + 01-31 exist). The pacing/dispatch layer, not the work, is the partial part.

## c) NOT STARTED (open items seen in passing, storage section — not this task's work)

1. Fold the borg env-path-pin negative case into the drill fixture test (TODO_LIST line 41 — drifted `BORG_ENV_FILE` literal → `backup.nix` assertion must fire; the guard's only proof was a hand-run, ephemeral probe).
2. Fixture-negative for this very fixture: mutate a drill copy (e.g. drop the root gate) and assert the fixture FAILS — the repo's negative-test doctrine applied to behavioral script fixtures (TODO_LIST line 42, spawned from this task's own report §f6).
3. Persist a fixture test for the offsite-borg pre/post-deploy smoke blocks §13/§16 (pass/fail/skip branches) — the prior "fixture-tested" claim pointed at no committed artifact (TODO_LIST line 34).
4. Fix pre-deploy §13 silent-skip: any eval failure reads as "offsite-borg disabled" — fail loud on transient eval errors (line 35).
5. Assert the offsite-borg IO tier in §13 + §16 — the ioTier.background deliverable has no automated gate (line 36).
6. Persist the positive-path offsite-borg render probe as a committed eval-time check (line 37).
7. VM test for `platforms/nixos/system/backup.nix` before go-live — zero test coverage on the whole module (line 62).
8. Repo-wide sweep for hardcoded help-slicer ranges (`sed -n '2,<N>p'`) in `scripts/` (line 66).
9. Extend audit-textfile-tmp class B to `.sh` + `.md` surfaces — the borg drill escaped the scan (line 229).
10. Offsite-borg go-live inputs — owner-BLOCKED (StorageBox hostname/username, passphrase recovery-copy policy, exclusion exceptions; line 64/`storage.md:63`).

## d) TOTALLY FUCKED UP

Nothing broken by this task. Two process-level findings, honestly:

1. **Duplicate dispatch of a DONE task.** This task ID has now produced THREE reports (01-27, 01-31, 01-39) and the deliverable was committed once. The queue harvested the item into domain libraries and re-fired it anyway — dispatch should skip task IDs whose line is already `[x]` with the matching footer in `git log`.
2. **Report proliferation for one task.** Two reports for the same ID already existed before this one; a third adds zero information. The `close the loop` contract in the tasking is fine, but nothing checks "is there already a report with this Task-Queue-ID?" before writing one.

## e) WHAT WE SHOULD IMPROVE

- **Fixture drift surface is small but real:** the fixture copies `scripts/borg-restore-drill.sh` at build time (good — always tests current source) but asserts on EXACT output strings (`result           : PASS`, specific die messages). Any wording change in the drill breaks the fixture — that is intentional brittleness for a behavioral fixture, but it should be stated in the fixture header so a future drill rewording isn't read as a fixture bug.
- **The negative-mutation test (item c2) is the missing half of this fixture.** A behavioral fixture that only ever runs the real script cannot prove it would FAIL a broken script — exactly the phantom-green class this repo keeps documenting.
- **The mountpoint-gate coverage gap** has a cheap escape: let the drill accept an overridable `mountpoint` resolver (env var like the existing `BORG_ENV_FILE` fixture hook) or move the gate behind a stubbed binary — then the fixture can assert the gate directly instead of asserting its "direct predecessor".
- **Queue hygiene:** harvested items should carry the completing task ID; dispatch should dedupe on (item text, `[x]` state, footer-in-log).

## f) NEXT THINGS TO GET DONE (impact-ordered, from THIS task's orbit; most are already-queued items worth prioritizing)

1. Fixture-negative for `borg-restore-drill-fixture`: mutated drill copy must FAIL the fixture (TODO line 42) — proves the fixture tests the real script.
2. Fold the borg env-path-pin negative case into the drill fixture (TODO line 41) — `backup.nix` assertion currently has no committed regression artifact.
3. Make the drill's real-mode `mountpoint` gate fixture-reachable (stub hook), then assert it directly — closes the fixture's documented blind spot.
4. Persist §13/§16 offsite-borg smoke-block fixture (pass/fail/skip) — replaces an unverifiable prior claim.
5. Fix pre-deploy §13 silent-skip (eval error ≠ "disabled"; fail loud).
6. Assert offsite-borg IO tier (BE/6) in §13 eval + §16 deployed unit grep.
7. Commit the positive-path offsite-borg render probe as a flake check.
8. VM test for `backup.nix` before go-live (tripwire fires on PLACEHOLDER, job shape, `.last_success` marker, fake borg on PATH).
9. Repo-wide help-slicer sweep (`sed -n '2,<N>p'` truncation class).
10. Extend audit-textfile-tmp to `.sh`/`.md` surfaces.
11. De-dupe queue dispatch: skip task IDs already `[x]` with footer present in git log (process fix — would have saved this third dispatch).
12. One-report-per-task-ID check before writing status reports (report 01-27 + 01-31 + this one all cover the same ID).
13. Add the fixture-brittleness note (exact-string assertions are intentional) to the fixture header.
14. Owner decision still pending: offsite-borg go-live inputs (StorageBox creds + passphrase recovery-copy policy + exclusion exceptions) — the entire borg leg stays dormant behind this.
15. After first REAL borg run: archive-vs-exclude audit + irreplaceable-set sizing vs the BX21 trigger (already queued as `[watch]`).

(15 items — everything beyond this is ROADMAP territory and outside this task's orbit per the no-unrelated-research constraint.)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Why was this task ID re-dispatched twice after completion?** The queue's pacing gates decide when items run — is there a reason a `[x]`-marked item with a footer-matched commit was re-fired (re-verification policy? stale queue snapshot?), or is this a dispatch bug to fix in item f11?
2. **Should duplicate per-task reports be suppressed?** If a re-verification dispatch is intentional, should it append to the EXISTING report for that task ID instead of writing a new timestamped one (this ID now has three)?
3. **Is the mountpoint-gate sandbox gap acceptable to ship?** If yes, item f3 dies; if no, I need permission to add a fixture hook (env-var override for the mountpoint resolver) to `scripts/borg-restore-drill.sh` — that touches the hardened drill script, which the original task's "smallest correct change" contract deliberately did not do.
