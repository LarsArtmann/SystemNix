# Task 000001a0cd86bbfaedbf87be3d95691fe9ee — Status Report

**Date:** 2026-09-23 15:12
**Scope:** Fix EXACTLY one reviewer finding on `docs/services/offsite-borg-restore.md` (the full-restore step-2 sops re-render claims). No unrelated changes.
**Work commit:** 502db45d — "docs: correct sops re-render gating in Borg DR restore runbook"

---

## a) FULLY DONE

1. **Re-anchored the finding in the current tree.** The stale review SHA (`49677fd3`) was gone; located the verbatim anchor text (`re-render all three at the first rebuild, which is what makes step 3's borg access work`) at `docs/services/offsite-borg-restore.md` lines 104-106.
2. **Verified the finding's two factual claims before editing** (per repo doctrine: never encode an unverified external claim):
   - `services.offsite-borg.enable = false` in `platforms/nixos/system/configuration.nix` (dormant) — so a first rebuild from the freshly cloned flake renders NONE of the three borg secrets; the gate is real.
   - Step 3's env block (lines 111-113) rides `BORG_PASSPHRASE=<recovery-copy passphrase>` + `ssh -i ./restore-key`, i.e. out-of-band credentials, not `/run/secrets` — the misattribution was real.
3. **Fixed the sentence minimally** (9 insertions, 3 deletions, one paragraph): states the declarations are gated behind `services.offsite-borg.enable = true`, that nothing renders until the go-live checklist runs on the new host (StorageBox hostname/username + fresh `ssh-keyscan` pin re-supplied into `borg.yaml`), that the sops-rendered secrets serve the STEADY state (`borgbackup-job-hetzner`, `/run/secrets`-based drill block), and that step 3 itself rides the owner recovery copy + a manually provided key.
4. **Confirmed the earlier finding's fix was already in place** (lines 99-101 already correctly say the borg secrets do NOT ride in the archive) — no double-editing of that site.
5. **Gates passed:** pre-commit ran gitleaks (clean), trailing-whitespace, flake check (eval-only, green); docs-only change so no build/test surface touched. Committed with exactly ONE Task-Queue-ID footer (the fix ticket's id). Not pushed (per contract).

## b) PARTIALLY DONE

- Nothing — the finding was fully addressed; the task had a single deliverable and it is complete.

## c) NOT STARTED

- Nothing owed by THIS task. Follow-ups observed in passing are listed under (f); none belong to this run.

## d) TOTALLY FUCKED UP

- Nothing in this run. The inherited failure chain (two rejected attempts before this one) is the honest signal though: **both prior attempts "fixed" the sentence with plausible prose instead of verifying the described mechanism against the actual config** — first claiming the pin rides in the archive, then claiming a first rebuild re-renders the secrets. The fix that finally stuck took ~4 tool calls of verification (`grep` the anchor → view the step-3 env block → check the `mkSecrets` gating/enable flag in the finding itself against the tree). That verification should have been the FIRST step of the original work, not the third attempt.

## e) WHAT WE SHOULD IMPROVE

1. **DR-runbook sentences that describe mechanism need a verify-before-writing pass**: every "X makes Y work" claim in a restore runbook should be checked against the config that implements it (option gating, secret paths, unit names) before it is committed. A false claim in a DR doc is a 3-AM failure mode by definition — the repo's `verify-external-claims` skill exists for exactly this and applies to self-authored prose too.
2. **Self-consistency sweep for runbook edits**: finding #1 was already fixed in the same file (the archive claim), and the second reviewer still found a self-contradiction (dead-host column says re-pin via `ssh-keyscan`). After editing a runbook, grep the whole file for the touched concept (here: `borg_known_hosts`, `/run/secrets`, `re-render`) and reconcile every site in ONE pass — the contradiction classes cluster.
3. **Reviewer-finding fixes should re-run the finding's claims against the CURRENT tree, not the rejected diff's tree** — configs move (this one was already edited twice); anchors drift and line numbers lie (the repo's own doctrine: anchor by verbatim text, never line numbers — followed here, worked).

## f) Up to 50 things to get done next (only NEW items this task surfaced or directly related; most route to docs/todo/storage.md or services.md — queue gates decide)

Relates directly to the offsite-borg leg (all owner-gated or queue-gated, NOT this task's scope):

1. Re-read `docs/services/offsite-borg-restore.md` end-to-end for remaining mechanism claims (drill section, dead-host column, go-live checklist) and verify each against the tree — same class, one pass.
2. The go-live checklist should explicitly list "re-supply a FRESH host-key pin into `borg.yaml` via `ssh-keyscan -p 23`" as a step on the replacement host (the doc now implies it; make the checklist row carry it).
3. Add the drill's `/run/secrets`-based block a note that it requires `services.offsite-borg.enable = true` first (same gating fact, second site).
4. Consider a docs-only lint: grep DR runbooks for absolute claims of the shape "rides in the archive" / "comes back at rebuild" and flag them for verification (cheap tripwire against this exact class).
5. When the leg goes live: run `scripts/borg-restore-drill.sh` in real mode (go-live checklist step 9) and record the timed result in this doc.
6. Owner decision still open: StorageBox hostname/username + recovery-copy passphrase custody policy (docs/todo/storage.md row exists — keep it).

None of these are agent-executable *now* without owner inputs (1-4 are agent-executable; 5-6 are owner-gated) — items 1-4 are candidates for `docs/todo/storage.md` if the queue wants them.

## g) Questions I could NOT figure out myself (owner-decisions)

1. **Should the replacement host's `borg.yaml` re-pin use `ssh-keyscan` output directly, or do you want the pin re-derived from the StorageBox console's published host key?** (ssh-keyscan is TOFU-shaped; the runbook's dead-host column currently says ssh-keyscan — trust-on-first-use against a brand-new host is a known weak spot in DR, but the console key is only trustworthy if you record it at StorageBox signup.)
2. **Do you want the drill script hardened to REFUSE running against `/run/secrets` credentials when `services.offsite-borg.enable = false`** (i.e. fail loudly with the go-live pointer), or is the current behavior (out-of-band creds only for the small-subset drill) the intended split?
3. **Is the recovery-copy passphrase custody policy documented anywhere you trust** (password manager / paper), or should the go-live checklist add an explicit "owner confirms recovery copy exists and is retrievable" checkbox? The doc says "owner recovery copy" but nothing verifies it exists — same gap class as an untested backup.
