# Status Report: Task 000001a0d0b0f9f324a5513ee7dbc1c5b20a — Offsite-Borg §13/§16 Smoke Fixture

**Date:** 2026-09-24 02:22 CEST
**Scope:** THIS task only — persist a fixture test for the offsite-borg pre/post-deploy smoke blocks (pre-deploy §13 / post-deploy §16, pass/fail/skip branches). Task-Queue-ID `000001a0d0b0f9f324a5513ee7dbc1c5b20a`. Work commit: `84f7ab33` (plus daemon-captured logic commits `907b6bb8`, `9b2ac2c5`).
**Format note:** the user explicitly requested Markdown (`.md`), overriding the status-report skill's HTML default — one-off, not propagated to the skill.

---

## a) FULLY DONE

1. **§13/§16 logic extracted verbatim into `scripts/lib/offsite-borg-smoke.sh`** — two functions (`ob_pre_deploy`, `ob_post_deploy`), caller-injected verdict callbacks (`OB_PASS/OB_FAIL/OB_WARN/OB_SKIP`), sourced by BOTH gates. Byte-exact parity PROVEN programmatically: message sets and single-quoted grep/sed pattern sets diffed against `git show HEAD:` of the pre-extraction blocks via python `ascii()`-repr set comparison — zero differences (modulo the intentional variable renames `BORG_SVC→svc_json`, `_borg_unit→unit_path`, `_borg_prom→prom_path`). The extraction was forced: a fixture that re-implements the block would drift; a fixture that greps its own copy is the metrics-gate anti-pattern.
2. **Both gates rewired to the lib.** pre-deploy-check.sh §13: inline 23-line block → `ob_pre_deploy "$BORG_SVC"`; post-deploy-check.sh §16: 41-line block → `ob_post_deploy <unit> <prom>`; both source the lib with shellcheck annotations. Real-gate re-runs: pre-deploy `64 passed / 36 warn / 0 failed` with the §13 skip branch live; post-deploy §16 skip branch live (`SKIP Offsite Borg - not deployed`).
3. **Fixture test `scripts/test-offsite-borg-smoke.sh`** — 14 assertion branches through the SOURCED lib (never a copy):
   - pre: disabled→SKIP, fully-wired→3×PASS, empty-wiring→3×FAIL, plus three single-branch-FAIL isolations (tripwire / marker / env each missing → exactly that branch fails).
   - post: absent-unit→SKIP, happy path (stub executable tripwire + marker line + env line + prom row)→4×PASS, wrong-tripwire-pattern→FAIL, present-but-non-executable tripwire→FAIL, missing marker/env/prom→each isolated FAIL, prom-without-row→WARN (5-min tick pending semantics).
   - 14/14 green locally.
4. **Flake check `offsite-borg-smoke-selftest`** (flake.nix, next to `pre-deploy-metrics-selftest`/`post-deploy-pressure-selftest`) — stages the repo layout, copies the lib + test from THIS tree, runs through nix so the tested artifact is the sourced file. `nix build .#checks.x86_64-linux.offsite-borg-smoke-selftest` green; `nix flake check --no-build` green.
5. **Queue close-out** — TODO_LIST.md row `[x]` with DONE narrative; `docs/todo/storage.md` library row flipped to the DONE-strikethrough house pattern; AGENTS.md offsite-borg section updated (extraction + fixture + selftest documented).
6. **Committed with the exact Task-Queue-ID footer** (`84f7ab33`), footer byte-verified programmatically (`COMMIT_EDITMSG` equality check). Never pushed. Accidental `flake.lock` churn (mr-sync re-lock from the pre-commit `nix flake check`) reverted.

## b) PARTIALLY DONE

1. **Post-deploy gate is green only in the §16 section** — the full post-deploy run reports `FAIL: 6` (Bank-Sync, Forgejo Catppuccin theme asset / default theme / meta description, InboxClean). These are LIVE-SERVICE state from a parallel session's Forgejo theme work (undeployed assets) and service health — NOT touched by this task (my diff touches only the borg block + sourcing). Not fixed: out of scope, and per contract I don't drive-by-fix unrelated failures. Flagged, not diagnosed.
2. **The `shellcheck disable=SC2034` comment** on the fixture's `OB_PASS=...` line matches the house pattern (pre-deploy-check.sh documents the same indirection blind spot), but the fixture script has not been added to any shellcheck-coverage inventory (see §f).
3. **Lib header documentation** names the fixture and flake check but does not spell out the callback contract for future §13/§16 edits beyond one comment block — adequate, thin.

## c) NOT STARTED (observed in passing; all already queued by prior sessions — nothing new harvested for THIS task's follow-ups)

1. Pre-deploy §13 silent-skip (disabled vs transient eval error) — queued, `docs/todo/storage.md`.
2. IO-tier assertion in §13/§16 — queued.
3. Positive-path render probe (extendModules, enable=true) as a committed artifact — queued; my fixture does NOT cover it (it stubs the eval JSON; it never proves the real config renders tripwire/marker/env).
4. VM test for the offsite-borg module — queued (separate, larger).
5. Go-live itself — owner-held inputs, untouched (correctly).

## d) TOTALLY FUCKED UP (process, all self-inflicted and recovered; zero repo damage)

1. **The mechanical-extraction thrash — ~4 failed attempts before the clean hand-write.** I tried to generate `scripts/lib/offsite-borg-smoke.sh` via python transforms over the inline blocks (line-slice + string-replace). Three assertion failures from off-by-one slices, then one output with a stray `fi` at function scope (bash syntax error). Root cause compounded: I wrote assertions and slice bounds from memory of line numbers instead of re-printing the exact ranges each time. Fix was a hand-written lib + a byte-parity diff as the safety net — but I should have gone straight to hand-write-with-parity-check.
2. **The "font illusion" rabbit hole.** Repeatedly I believed I saw spelling variants (`offsite`/`offsite`, `Borg`/`Borg`, `svc_json`/`svc_json`) that turned out to be identical strings rendered ambiguously in my own context. I wasted several cycles and — worse — briefly hand-wrote a "corrected" lib from memory, which is exactly how real corruptions happen. The eventual discipline (python `ascii()`/`ord()` set-diffs against git HEAD) is the right permanent tool; I should have reached for it at the FIRST suspicion instead of eyeballing five more times.
3. **First commit attempt failed pre-commit** (shellcheck SC2034 on the fixture's `OB_*` bindings — used via indirection inside the sourced lib, invisible to shellcheck). Known house pattern, known annotation; I shipped without pre-running shellcheck on my own new script. One wasted gate cycle.
4. **"File modified since read" race on my own file** — between my write and my edit, the file changed under me (mod-time bump; likely the pre-commit trailing-whitespace pass during the aborted commit). I re-read before editing, which is the required behavior — but the sequence was self-inflicted (aborted commit left the tree mid-lint).
5. **`git commit` printed `fatal: unable to write new index file`** — commit landed (log + status verified clean afterward) but the index write error appeared. Transient (concurrent daemon activity likely). Recovered and verified; noted here because an unverified exit path here could have left a "committed but actually not" state — I checked, but only because the error forced me to.
6. **Daemon co-commit absorption risk, handled but lucky.** The auto-commit daemon captured my in-flight script edits (`907b6bb8`, `9b2ac2c5`) mid-task. My final commit was pathspec-scoped and the daemon commits were verified via `git show --stat` before proceeding — no amend-into-daemon-commit occurred. Correct per the multi-agent discipline, but I noticed the daemon sweep only when git status showed my "new" file as `M` — I should have checked `git log` at the FIRST anomaly, not after staging.

## e) WHAT WE SHOULD IMPROVE

1. **Pre-deploy §13's nix eval is a silent-skip single point of failure** — `nix eval ... || echo ""` conflates "service disabled" with "eval broke". My fixture can only test the disabled shape I feed it; the queued §d3 fix (fail loud on transient eval errors) is the real closure. My extraction did NOT change this behavior (deliberate — behavior-preserving), but the fixture now pins the current semantics, which makes the future fix a visible diff.
2. **Extracted-lib callbacks are string-named (`"$OB_PASS"`)** — works, but shellcheck can't follow it (SC2034 noise at every call site). A `declare -F` guard in the lib ("caller must define OB_PASS/...") would fail loud instead of `command not found` at first verdict.
3. **Fixture asserts event-class sequences, not message content** — the parity diff proves messages TODAY; the fixture would not catch a future message drift (it counts PASS/FAIL, doesn't compare text). Adding one message-content assertion per branch (grep the callback arg) would close that.
4. **My own tooling discipline**: for any future verbatim-extraction task, the recipe is — print exact ranges → hand-write → programmatic byte-parity diff → done. Not python codegen, not eyeballing. Write this down (it is now, here).
5. **Pre-run the pre-commit gates locally before committing** (shellcheck on new `.sh` files): one command, saves a full gate cycle.

## f) NEXT (up to 50; first ~6 are the direct continuations of THIS work, the rest are what I noticed in passing — brainstorm-grade, routing rigor applies before harvest)

**Direct follow-ups (already queued or natural children of this task):**
1. Fix pre-deploy §13 silent-skip: discriminate disabled vs transient eval error, fail loud on the latter (queued §d3; my lib extraction makes it a small, testable change — the fixture gains a "non-empty stderr/eval-error → FAIL" branch).
2. Add message-content assertions to the fixture (one per branch) so message drift fails the fixture, not just the parity diff I ran once (above, §e.3).
3. Assert the offsite-borg IO tier (BE/6) in §13/§16 — neither block checks it today (queued §d4; natural to land WITH the fixture now in place).
4. Positive-path render probe as a committed flake check (extendModules enable=true → tripwire/marker/borg-env/BE-6) — queued §f3; my fixture stubs the JSON, so this is the complementary real-render proof.
5. Fold the `borg-env` path-pin negative case into the drill fixture (queued; adjacent guard, same test family).
6. VM test for the offsite-borg module (tripwire on PLACEHOLDER, `.last_success` write; queued, M-sized).

**Noticed in passing (this session only):**
7. Post-deploy 6 FAILs (Bank-Sync, Forgejo ×3 theme checks, InboxClean) — verify the Forgejo trio resolves after the parallel session's theme deploy; investigate Bank-Sync/InboxClean state.
8. `git commit`'s transient "unable to write new index file" — if it recurs, look for daemon/lock contention on `.git/index.lock`.
9. The status-report skill's section-(f) default is 25, user asked 50 — no repo change needed; noted for harvest pacing.
10. Fixture test scripts (this one, `test-pre-deploy-metrics.sh`, `test-post-deploy-pressure.sh`) share a stub-callback + `expect` scaffolding shape — a tiny shared fixture helper could DRY them, but only if a 4th appears (3-uses rule).
11. `scripts/lib/offsite-borg-smoke.sh` could add a caller-contract guard (`declare -F OB_PASS || return 2`) — cheap fail-loud.
12. When §13/§16 next change semantically (items 1/3), the parity-diff recipe (byte set-diff old vs new messages/patterns) should be re-run as the review artifact — worth a tiny `scripts/` helper if it recurs a third time.

**Beyond this task's horizon (roadmap-grade, listed only because 50 was requested):**
13-50. I am deliberately NOT padding this list with 38 speculative items: everything genuinely actionable that I observed in passing is in 1-12. The honest backlog for this slice of the tree is those twelve; the rest of the queue (VM tests, go-live, §d3/§d4) is already owned by existing TODO rows. Inventing filler here would violate the harvest anti-pattern (unowned, vague items into the queue).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Should the §13 eval-side fixture stub at the `nix eval` boundary too?** My fixture calls `ob_pre_deploy` with fixture JSON; the `nix eval` invocation itself (the silent-skip vector, item 1 above) is untested. Testing it needs a stubbed `nix` on PATH inside the flake check — acceptable, or is that the §d3 fix's job (replace the eval with a fail-loud helper that gets its own negative test)?
2. **The Forgejo theme smoke FAILs in post-deploy — is the parallel session's theme work deployed-but-unverified, or undeployed?** I did not research it (out of scope); if it is deployed and still red, that is a real regression someone owns.
3. **Do you want the daemon's heuristic commits (`907b6bb8`, `9b2ac2c5` — my in-flight lib/gate edits) amended/rebased into the footered commit for a clean queue→commit mapping, or left as-is?** The queue's commit-derivation already cross-references `84f7ab33`; rewriting unpushed history for tidiness trades safety for cosmetics, so I left it — say the word if you want it cleaned.

---

*Point-in-time snapshot for task `000001a0d0b0f9f324a5513ee7dbc1c5b20a`. Items 1-6 of §f already have queue rows or are natural children of this task's artifacts; harvest with routing rigor.*
