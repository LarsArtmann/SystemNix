# Task-Queue Status Report — 000001a097f9c0f95f99780b7473695f7f0a

**Date:** 2026-09-13 02:52 CEST
**Work item:** SigNoz upstream trace-instrumentation gaps — flip `wiring = "upstream"` → enforced as each lands (6 binaries) — TODO_LIST.md, Priority 1
**Commit:** `c609f8a5` (signoz-coverage: flip dnsblockd to enforced, ratchet gap budget to 4)
**Scope discipline:** report covers THIS task's work only; passing observations explicitly labeled.

---

## Executive Summary

The work item's two spelled-out REMAINING actions were **both already satisfied elsewhere** — the row was a stale harvest:

- **dnsblockd** ("REMAINING: push + tag + relock (+ vendorHash), flip wiring"): the scheme-aware OTLP fix was already committed AND pushed upstream (`70ca2ea`, 2026-09-01, landed on origin via auto-commit `8aa005f`), SystemNix's lock has carried it since `94c9cb93` (2026-09-06), the deployed unit runs `dnsblockd-94c9cb9`, and **spans are flowing live right now** (`signoz_traces_reporting{service="dnsblockd"} 1`, last span 10 s old at read time). Nothing was owed on the push/tag/relock/vendorHash track. The only real gap was the registry entry, which this run flipped to `"config"` (enforced).
- **bank-sync** ("REMAINING: push + tag + relock, flip to env"): **already flipped 2026-08-31**, the same day the item was authored (registry comment; upstream `901978e`), spans reporting green live (span age 21 s at read time).

Parts (3)-(6) — overview, projects-management-automation, papdashboard, hermes — were re-verified as **never having emitted a span** (`last_span_age_seconds -1`); they need real upstream instrumentation first. Flipping them now would false-page the enforced budget. The row stays unchecked with a `— BLOCKED:` suffix per the contract (2 of 6 binaries closed).

No deploy was performed and none is required for correctness: the flip is registry truth-alignment (dnsblockd already emits), but the **running collector keeps the old registry until the next `nix run .#deploy`** — see b)2.

### Audit evidence (all from THIS run)

| # | Check | Result |
|---|-------|--------|
| 1 | `git log origin/master..HEAD` in dnsblockd | Exactly ONE unpushed commit (`20afd82`), and it is a docs-only strategy-report auto-commit — every code commit incl. the OTLP fix is on origin |
| 2 | `git grep WithInsecure 94c9cb93 -- internal/otel/otel.go` | 2 hits — the locked rev contains the scheme-aware fix (`70ca2ea`, 2026-09-01) |
| 3 | Deployed unit | `/etc/systemd/system/dnsblockd.service` ExecStart = `…-dnsblockd-94c9cb9/bin/dnsblockd serve` — the deployed binary IS the fixed lock rev |
| 4 | `git cat-file -e 1953055:cmd/bank-sync/tracing.go` + live metrics | bank-sync `tracing.go` in the locked rev (added 2026-07-10, OTLP export landed via `901978e`); `signoz_traces_reporting{service="bank-sync"} 1` |
| 5 | Live coverage textfile (`:9100/metrics` source) | `missing 3`, `upstream_gaps 5`, `over_threshold 0`, `scrape_errors 0` — dnsblockd and bank-sync both `reporting 1`; overview/PMA/papdashboard/hermes all `-1` (never seen) |
| 6 | Post-edit eval | `expected.dnsblockd = {serviceName "dnsblockd", wiring "config", maxAgeHours 26}`, `maxUpstreamGaps = 4` |
| 7 | Gates | `nix fmt --no-update-lock-file -- --ci`: 0 changed; `nix flake check --no-build` (the `.tq-verify` gate): all checks passed; pre-commit on the commit: gitleaks + flake check green |

---

## a) FULLY DONE

| # | Item | Proof |
|---|------|-------|
| 1 | dnsblockd registry flip `wiring = "upstream"` → `"config"` (enforced 26 h freshness via its live `otlp_endpoint` key, dns-blocker.nix) | Eval shows the flipped attrset; spans verified flowing live BEFORE the flip — the precondition the flip exists to enforce was already true |
| 2 | `maxUpstreamGaps` ratcheted 5 → 4 per the module's own doctrine ("ratchet DOWN as gaps close") so the next new silent-noop service trips the budget alert again | Eval = 4; `over_threshold` stays 0 |
| 3 | Stale comments corrected in `signoz-coverage.nix` (header gap-list + the old "flip after push + flake bump" registry comment) | Diff review; fmt clean |
| 4 | AGENTS.md stale passage replaced ("Scheme-aware fix in the dnsblockd checkout; pending push+tag+flake bump…" → RESOLVED narrative with commit hashes and the live-span proof) | Line 966 edit, diff reviewed |
| 5 | TODO_LIST row rewritten: parts 1-2 recorded DONE with evidence, parts 3-6 re-verified never-emitted, `— BLOCKED:` suffix added | Row 29 diff |
| 6 | bank-sync verification (no change needed — confirmed already enforced and reporting) | Evidence table rows 4-5 |
| 7 | Commit `c609f8a5` carries the `Task-Queue-ID:` footer; tree clean; nothing pushed (contract + repo convention: agent sessions never push — and there was nothing to push: the upstream fixes were already on origin) | `git log -1`, clean status |

## b) PARTIALLY DONE

| # | Item | State |
|---|------|-------|
| 1 | The work item as a whole | 2 of 6 binaries closed (the only two whose REMAINING steps were agent-executable). The other 4 are genuine upstream development (span sites / trace SDKs), not flips — the row stays unchecked with a BLOCKED suffix per the queue contract |
| 2 | The flip + ratchet are **inert on the live box until the next deploy** | `signoz-coverage-metrics` is a oneshot whose ExecStart embeds the registry JSON from a store path — the RUNNING collector still counts dnsblockd in `upstream_gaps` (live value 5, not 4) and treats it as non-enforced until the system generation switches. Safe in both directions (dnsblockd emits either way); it rides the next routine deploy, which several other pending items share |
| 3 | `signoz-coverage.nix` module docstring | The two resolved bullets were modernized, but the file still narrates itself as "evo-x2 truth as of 2026-08-31" in the registry preamble — deliberately left (historical context, modernizing per-flip would churn); listed as follow-up #17 |

## c) NOT STARTED

| # | Item |
|---|------|
| 1 | overview: real span sites (TracerProvider is wired, ZERO span sites — env var is a perfect noop) |
| 2 | projects-management-automation: same class |
| 3 | papdashboard: Go trace SDK (has OTel metrics only) |
| 4 | hermes: Python `opentelemetry-sdk` wiring into the agent runtime |
| 5 | The 4 corresponding flips + budget ratchet steps (4 → 3 → 2 → 1 → 0) |
| 6 | dnsblockd upstream release tag carrying the OTLP fix (latest tag `v0.7.1` predates it; SystemNix follows `ref=master` so it is not blocked, but any tag-pinning consumer is) |
| 7 | Verify the dnsblockd **/health wedge fix** state — see d)2/e)7: AGENTS.md still says "NOT pushed/deployed" for that fix, but the checkout is clean with only a docs commit unpushed, which makes that claim a second stale-premise candidate |

## d) TOTALLY FUCKED UP

Nothing in the landed state is broken. Two process incidents, both contained, reported honestly:

| # | Incident | Containment |
|---|----------|-------------|
| 1 | **Auto-commit daemon race**: after I finished edits + verification, the daemon swept all 3 files into a heuristic-message commit (`33f44d9e`, "chore: auto-commit 3 changed file(s)") before my pathspec commit could land — my queue footer would have been lost | Recovered by amending the message only, after verifying the commit contained EXACTLY my 3 files (27 insertions / 15 deletions, nothing foreign). Result: `c609f8a5` with the full message + footer. Lesson applied to future runs: commit immediately after the verify step, don't leave a verified tree idle |
| 2 | **Wrong investigation order (self-inflicted, no damage)**: I spent the first round auditing "push + tag + relock" logistics in the upstream repos before reading `signoz-coverage.nix` — where the registry comment had recorded the bank-sync flip (2026-08-31) all along. Reading the registry FIRST would have collapsed the whole audit. No wrong action resulted (the contract forbids pushes regardless), but the order was inefficient and the item's premises should have been suspected sooner — this repo has a documented stale-harvest class |

## e) WHAT WE SHOULD IMPROVE

1. **Stale-premise class, 3rd confirmed instance** (attic VM "deterministic RED", paperless email-smoke phantom-RED, now this row). TODO rows that encode "REMAINING: X" should carry a `state-verified <date>` stamp and the verification probe, so the next session re-verifies instead of executing stale steps.
2. **Dual bookkeeping**: the registry comments are the live truth (bank-sync flip recorded there on day one) while the TODO row lagged. Make registry comments canonical for wiring state; TODO rows should link to them rather than restate.
3. **Audit-derived work items should embed their flip gate** — e.g. "flip only when `signoz_traces_reporting{service=X} 1`" — so executors self-gate on evidence instead of trusting the row text.
4. **Daemon-race exposure**: any queue run whose commit must carry a footer is one daemon tick away from losing it. Either commit instantly post-verify as a hard habit, or (bigger) teach the daemon to skip files touched recently by an active queue session.
5. **Deploy-pending visibility**: my TODO row says DONE+FLIPPED (repo truth) but the running system still has gaps=5 until the next switch. The Hermes cron-fix row already carries a "goes live with the next deploy" annotation — that suffix should be standard for every config-landing row.
6. **`missing` conflates two failure shapes**: never-seen (`last_span_age -1`) and went-dark (>budget). A split (separate gauge or label) would let event-driven services be quiet without muddying dense-service outage semantics — directly relevant to the live `missing 3` finding below.

---

## f) Up to 50 things we should get done next

*(brainstorm per the skill contract — most are ROADMAP fuel; docs-health HARVEST applies routing rigor. Scoped to this task + what I noticed in passing.)*

**Trace-coverage tail (the item's actual remainder):**
1. overview: implement span sites upstream (http/cqrs boundaries), then flip wiring.
2. projects-management-automation: span sites (discovery scan, commit pipeline), then flip.
3. papdashboard: add the Go trace SDK next to the existing metrics registry, then flip.
4. hermes: wire `opentelemetry-sdk` into the agent runtime, then flip.
5. After each flip: ratchet `maxUpstreamGaps` 4 → 3 → 2 → 1 → 0 (each ratchet is its own conscious commit).
6. At 0 gaps: keep the budget check alive at 0 as a permanent silent-noop tripwire.
7. Deploy the current flip + ratchet (rides the next routine `nix run .#deploy`, shared with other pending items).
8. Split `signoz_traces_missing` into never-seen vs went-dark (separate gauge or `reason` label).
9. **Investigate the live `missing 3`** (file-and-image-renamer ×2 + gotenberg: no span in >40 days despite 720 h budgets) — genuinely quiet, or serviceName drift upstream? Passing observation: the Gatus "SigNoz Traces Coverage" check is RED on this RIGHT NOW (`[BODY] != pat(*\nsignoz_traces_missing [1-9]*)`), pre-dating my change and persisting after it — a live alert-fatigue source.
10. If the trio is genuinely quiet: either reclassify to `wiring = "upstream"` with a revisit trigger, or give event-driven entries never-seen-tolerant budget semantics.
11. dnsblockd upstream: cut a release tag carrying the OTLP fix (master-only today).
12. Decide whether SystemNix should tag-pin dnsblockd (qmd precedent) vs follow master — tradeoff: bad-master blast radius on every relock.
13. **Verify the dnsblockd /health wedge-fix state** (AGENTS.md "NOT pushed/deployed" vs a clean, fully-pushed checkout — second stale-premise candidate); if it landed, annotate/close TODO row 40 (the `/health` wedge row) and correct AGENTS.md.
14. dnsblockd repo hygiene: the unpushed `20afd82` is a daemon-auto-committed strategy doc — owner pushes it, or the daemon should exclude docs-only commits from upstream repos.
15. bank-sync has ZERO tags — decide a tagging scheme if any pinning consumer ever appears.
16. Split TODO row 29 into per-binary entries (or an inline checklist) so future flips close their piece without a BLOCKED suffix spanning finished parts.
17. Modernize the `signoz-coverage.nix` docstring's "2026-08-31 founding audit" framing once (not per-flip).
18. Quick verify: no SigNoz alert rule hardcodes the old gap budget of 5 (the metric is config-driven so this should be clean — one grep).
19. Post-deploy: confirm pre-deploy §10 / KNOWN_NEW_METRICS need no entry for the ratchet (metric names unchanged; verify once at the switch).
20. Standardize the "goes live with the next deploy" suffix for every config-landing TODO row (Hermes row precedent).
21. Consider a shorter freshness budget for dense always-on emitters (dnsblockd emits every few seconds; 26 h detection of a silent span-stop is generous — 6 h would catch the :9090-wedge class faster). Design question, not a bug.
22. When the 4 flips land, add each serviceName to the SigNoz Services-page onboarding checklist in the coverage module comment (keeps the "register EVERY service FULLY" rail self-documenting).
23. docs-health HARVEST for this report: route items 1-7 onto the existing row/ROADMAP, 8-10 + 13 as new TODO candidates, the rest as ROADMAP fuel.
24. Capture the "audit → land → flip" loop as a reusable runbook section in the coverage module comment, so the next gap closure is a checklist instead of an investigation.

*(Stopped at 24 honest items — the remaining TODO_LIST P0/P1 backlog is already tracked in TODO_LIST.md and restating it here would be padding, not signal.)*

## g) Questions I can NOT figure out myself

1. **For the 4 remaining gaps** (overview, PMA, papdashboard, hermes): do you want real upstream instrumentation (span sites / trace SDKs) queued as executable task-queue items per repo — or kept as passive gap-budget debt until you instrument them yourself? That decides whether this row spawns 4 implementation tasks or stays a watching brief.
2. **dnsblockd /health wedge fix**: AGENTS.md says "NOT pushed/deployed" (2026-09-06), but the checkout is clean with only a docs commit unpushed — is that fix on a different branch/worktree, or is AGENTS.md stale a second time? (Determines whether TODO row 40's fix-half is already done and whether the deployed `dnsblockd-94c9cb9` actually serves the cached /health.)
3. **The event-driven trio keeps "SigNoz Traces Coverage" red** (`missing 3`, >40 days silent): demote them to `wiring = "upstream"` (gap budget, non-paging), add a never-seen exemption, or leave them red as a nudge to actually exercise renames/office-conversions?

---

**Loop closed:** TODO_LIST row 29 updated (parts 1-2 DONE inline, BLOCKED suffix for 3-6); AGENTS.md corrected; commit `c609f8a5` carries the queue footer; verify gate (`nix flake check --no-build`) green; nothing pushed.
