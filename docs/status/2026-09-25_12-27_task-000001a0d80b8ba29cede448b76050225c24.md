# Status Report — Sweep Stale Root-Window Echoes (Task `000001a0d80b8ba29cede448b76050225c24`)

**Date:** 2026-09-25 12:27 CEST
**Task:** "Sweep the stale root-window echoes after the retention reconciliation (reserve T14 caveat "3d+1w", emergency-reserve doctrine-table row "root ≈ 1-2w", any other `3d 1w`/`1-2w` restatements)" — follow-up to the retention-reconciliation task `000001a0d7ddc4c5db6b3a8da3acdfb9a1d8` (§b1/b4/e1), which had restated the root `@` btrbk pin window to **2w sharp** (calendar-anchored weekly buckets) but only fixed the doctrine paragraph, leaving three echoes behind.
**Format note:** the status-report skill's canonical output is a styled HTML dashboard; the tasking prompt explicitly demanded a Markdown file at `docs/status/<ts>_task-<id>.md` — the user's instruction wins, so this is a flat .md (one-off override, not propagated into the skill).

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | **AGENTS.md fully aligned to the reconciled root window.** Three edits: (1) the emergency-reserve T14 caveat (~line 696) now reads "root pin window = 2w sharp — calendar-anchored, see the Snapshot-pinning doctrine above" (was "3d+1w retention"); (2) both doctrine-table rows (`/btrfs-emergency-reserve`, `~/projects` + rest of `/home`) changed from "root ≈ 1-2w" to "root = 2w sharp"; (3) the ClickHouse XFS finalize note (line ~969) changed from "root space frees as 3d+1w snapshots expire" to "root snapshots expire — pin window 2w sharp, calendar-anchored, see the BTRFS Snapshot-pinning doctrine" | commit `eb844911` (4 files, pre-commit gates green incl. `nix flake check`) |
| 2 | **`scripts/migrate-clickhouse-xfs.sh` runtime messages aligned (3 sites).** The sweep grep over live surfaces caught three user-facing messages still telling operators "3d+1w": the prepare-mode heredoc (line ~284), the pre-deletion-snapshot comment (line ~353), and the FINALIZE COMPLETE banner (line ~392-393). All now carry "root pin window = 2w sharp, calendar-anchored". The script was NOT in the task item's named scope (AGENTS.md + docs/todo + docs/services) — extended deliberately: these are live operational guidance that would mislead an operator mid-migration. Verified with `bash -n` + shellcheck (both pass) | same commit |
| 3 | **Sweep-verification grep is clean.** `grep -rn "3d 1w\|3d+1w\|1-2w"` across AGENTS.md + docs/todo + docs/services + scripts shows only: (a) live-config restatements of the ACTUAL `snapshot_preserve "3d 1w"` value (correct — that is the btrbk setting, not the pin window), (b) the "NOT \"3d+1w\" flat" negation in the Snapshots paragraph (correct by construction), and (c) archived status/planning reports (historical records, deliberately untouched) | session grep, post-edit |
| 4 | **Queue loop closed in both surfaces without drift.** TODO_LIST.md line 49 marked `[x]` with a DONE one-liner; the mirrored `docs/todo/storage.md` `[ready]` row marked `[x]` with a fuller DONE note; `bash scripts/check-todo-system.sh` → "OK: TODO queue/library structure clean" | same commit; check output in-session |
| 5 | **Commit hygiene survived two auto-commit-daemon races.** The daemon batched my three doc files into heuristic commit `0b8cc8e1` mid-commit, and — after a `git reset --soft` fold attempt — raced AGAIN into `c60b6dd9` containing all 4 files. Both daemon commits were verified (`git show --stat`) to contain ONLY my work before being folded forward; final state is ONE properly-messaged commit `eb844911` carrying all 4 files with the Task-Queue-ID footer, working tree clean, nothing pushed | `git log`/`git show --stat` in-session |

## b) PARTIALLY DONE

Nothing partial in the task itself — it is fully landed. Two adjacencies deliberately NOT done because they are separate queue items (listed here so the boundary is explicit, not forgotten):

1. **Calendar-anchor comment in `snapshots.nix`** (source report §f3) — the config still carries `snapshot_preserve = "3d 1w"` with no comment explaining that weeks start Sunday and weeklies live 14d. Already a queued `[ready]` item; NOT this task (smallest-correct-change contract).
2. **"Prune gaps ride guard/outage-killed nights" historical re-verify** (source report §g2) — queued, not this task.

## c) NOT STARTED

- **Retention-figure guard/lint.** During the sweep I noticed nothing ENFORCES the echo discipline: the previous session fixed one paragraph and missed two echoes; I fixed those and found three more in a script outside the named scope. The 11-51 report's §e1 lesson ("one-grep sweep on any figure change") is prose only. Nothing exists that would catch the NEXT stale figure. Proposed as a follow-up (harvested below).
- Everything else in the storage domain queue that predates this task (borg go-live owner inputs, /data EIO P0, hot-db fold, hermes-subvol retention verification, etc.) — untouched, correctly owned by its own items.

## d) TOTALLY FUCKED UP

Nothing is broken — but two process failures this run deserve naming honestly (severity: low, self-healed, no data loss):

1. **I lost two commit cycles to the auto-commit daemon and handled the first one sub-optimally.** My `git add && git commit` ran as a background shell (the tool auto-backgrounded it); while it sat queued, the daemon committed my staged doc files as `0b8cc8e1`. My first recovery (soft-reset + re-commit) was ALSO backgrounded and daemon-raced a second time (`c60b6dd9`). Root cause: the commit tool call crossing the daemon's ~10-min sweep window, twice, with slow foreground starvation from the pre-commit `nix flake check`. The house rule for exactly this case — PATHSPEC commit out of a shared dirty index — existed and I did not use it on the first attempt. Mitigation that finally worked: verify daemon-commit contents (`git show --stat`), amend-forward into the properly-messaged commit. Net damage: zero (both daemon commits contained exclusively my files), but three commit attempts where one clean pathspec commit would have done.
2. **The sweep verification ran AFTER the AGENTS.md edits, not before the commit.** I edited AGENTS.md first, then ran the confirm-grep — which is how the three script echoes were found at all. Had I committed immediately after the named-scope edits, the script echoes would have shipped stale and needed a second commit. The final state is correct; the order was luck-adjacent, not discipline: the item said "any other restatements", and the repo-wide grep that found the extras should have been step 1, not step 4.

## e) WHAT WE SHOULD IMPROVE

1. **Figure-change sweeps should be mechanical, not mnemonic.** A retention-window figure now appears in ≥6 live surfaces (doctrine paragraph, 2 table rows, T14 caveat, ClickHouse AGENTS note, migrate script messages, storage.md closure notes). The 11-51 §e1 lesson ("one-grep sweep") already failed once before this task re-learned it. Concrete fix: a tiny flake-check/pre-commit grep-gate over live surfaces (AGENTS.md, docs/todo, docs/services, scripts/) that fails on known-stale root-window restatements (`3d+1w` outside a negated/historical context is hard to lint; the workable gate is: fail on `root ≈ 1-2w` / `root ≈` patterns and on `3d+1w` in scripts/ + docs/services/), harvested below.
2. **Commit-before-daemon discipline for queue tasks.** When the working set is small and final (docs-only), run the commit as the FIRST action after the last edit, in a foreground tool call, with PATHSPEC (`git commit -m … -- <files>`), never letting `add` + `commit` straddle a background handoff. Worth one sentence in docs/CONTRIBUTING.md alongside the existing multi-agent write-discipline section.
3. **DONE notes that quote stale figures should self-annotate their era.** `docs/todo/storage.md:66` records the doctrine block "as landed" with "≈1-2w" — accurate history, but a future reader skimming DONE notes could re-cite the wrong figure. Cheap fix convention: when a closure note quotes a figure that a LATER item corrected, append "(superseded: <new figure>, see <item>)". Applied nowhere yet; convention proposal only.
4. **Scope-extension judgment was right but should be stated in the DONE note.** The script edits were outside the item's literal scope; the DONE note in TODO_LIST.md does not mention them. A one-word "(+ migrate script)" would make the queue's derived file-list self-explanatory. Minor.

## f) Up to 50 things we should get done next

Ranked by impact. Most are the source report's own sibling follow-ups (already queued — listed for completeness with their queue state) plus the new items this run surfaced. **HARVEST note:** new items marked `[NEW]` below are appended to `docs/todo/storage.md` as `[ready]` one-liners at authoring time (per the self-harvest rule); the rest already live in the queue and are NOT re-added.

| # | Task | Impact | Effort | Category | Queue state |
|---|------|--------|--------|----------|-------------|
| 1 | Add a calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix (weeks start Sunday; weeklies live 14d) | Medium | S | Documentation | queued `[ready]` |
| 2 | Re-verify whether the removed "prune gaps ride guard/outage-killed nights" framing tracked a real historical gap (05-32 report §f.41 evidence) | Medium | S-M | Quality | queued `[ready]` |
| 3 | Investigate the `@home-hermes` 09-16 snapshot (9d old vs `snapshot_preserve 3d`) — parent hold vs real prune gap | Medium | S-M | Quality | queued `[ready]` |
| 4 | `[NEW]` Add a grep-gate (flake check or pre-commit) that fails on stale root-window restatements (`root ≈ 1-2w`, `3d+1w` in scripts/ + docs/services/) so the next figure change cannot leave echoes | Medium | S-M | Quality | harvested this run |
| 5 | Drop unknown flags in the borg-restore-drill fixture's borg stub (junk file lands in cwd, breaks push-protection hook repo-wide) | High | S | Bug | queued `[ready]` |
| 6 | Fold the interim `crush-hot-db` mechanism into the ratified `services.hot-db` Phase-2 module (both exist; the interim is deployed) | High | M | Cleanup | queued (storage) |
| 7 | Buildcache fallback-surface single-sourcing: four hand-kept reap surfaces (deploy.sh ×2 loops, recovery step 2.5, HM activation block) — one source of truth | Medium | M | Cleanup | queued (storage) |
| 8 | `buildcache-init` should provision the three new fallback-cache targets (pnpm×2, cargo registry) — post-recovery symlinks dangle until next deploy | Medium | S | Bug | queued (storage) |
| 9 | Own-tools NVMe→pool migrations: browser-history dbBackup first-run verification after the pending deploy | High | S | Verification | queued (storage) |
| 10 | Offsite Borg go-live: owner supplies StorageBox hostname/username + host-key pin (the ONLY blocker for the dormant 3rd copy) | Critical | S (owner) | Decision | queued `[blocked:user]` |
| 11 | /data EIO inode P0: the nightly btrbk-data has aborted since Aug — repair or formally accept the un-replicated /data leg | Critical | L | Bug | queued (storage P0) |
| 12 | Hermes-subvol retention verification: confirm `@home-hermes` 2d/3d-1w pruning actually prunes (first receives landed 09-18) | Medium | S | Verification | queued (storage) |
| 13 | Empty the /data/.Trash-1000 victim (gemma GGUF) after window check — starts the /data pin clock on decision | Low | S | Cleanup | noted in data-damage-set.md |
| 14 | 2-device btrfs buildcache merge (both SanDisks → one fs at /mnt/buildcache): needs a maintenance window + module flip in the SAME change | Medium | L | Feature | queued (storage) |
| 15 | Rootfs `@home-hermes` bounded-retention check at next btrbk pass (14d 4w pool, 3d local) — verify no unbounded growth | Low | S | Verification | queued (storage) |
| 16 | Sweep `docs/services/*` runbooks for any other operational figures that drift when doctrine changes (e.g. window numbers quoted in jan.md/paperless.md recipes) — inventory, no edits | Low | S | Documentation | `[NEW]` harvested |
| 17 | Daemon-race commit discipline: add the PATHSPEC-commit-first sentence to docs/CONTRIBUTING.md multi-agent section | Low | S | Documentation | `[NEW]` harvested (pipeline-adjacent; route to pipeline.md if docs-health prefers) |
| 18 | Reconcile flake pin policy tail: remaining hard-rev pins with documented reasons (library-policy, go-auto-upgrade, overview, PMA) — re-probe upstream HEADs for drop-worthiness | Low | M | Maintenance | recurring (AGENTS pin-policy doctrine) |
| 19 | `preferStaticEmulators = true` follow-up (drops `/run/binfmt` from extra-sandbox-paths; closes the tmpfiles-cycle build-breakage class permanently) | Medium | S | Feature | noted in 2026-09-25 tmpfiles-cycle entry |
| 20 | Root-fs shadow-dir sweep after the Samsung-era migrations (the clickhouse finalize `--bind /` view pattern left shadowed originals on root `@`; verify all consumed, none straggle) | Medium | S | Verification | `[NEW]` harvested |
| 21 | SigNoz dashboard/panel for snapshot-pin windows (the doctrine table is prose; a panel reading `.snapshots` ages would catch future anomalies without manual `du`) | Low | M | Feature | ROADMAP fuel |
| 22 | Document the DONE-note era-annotation convention (e) in docs/CONTRIBUTING.md TODO-system section | Low | S | Documentation | `[NEW]` ROADMAP fuel |
| 23 | ChangeLOG prune pass: the `[x]` rows accumulated in TODO_LIST.md/storage.md (incl. this task's) must be pruned to CHANGELOG at the next docs-health pass | Low | S | Cleanup | standing convention |

(Items 24-50 withheld deliberately: the remaining storage-domain backlog lives in `docs/todo/storage.md` and is already harvested; padding this table with restatements would create a second, drifting list — the exact anti-pattern the TODO-system rules ban.)

## g) Questions (max 3)

1. **Historical-record policy boundary:** archived status/planning reports (e.g. `2026-08-22_02-38_clickhouse-xfs-migration.md`, five sites) state "3d+1w" as the root window as if currently true, with no era marker. I left them untouched per "historical records, never rewrite". Is that absolute — or should archival reports get a one-line top banner ("figures as of <date>; root window since reconciled to 2w sharp") via docs-health ANNOTATE when the stale figure could actively mislead a future reader?
2. **Daemon-race acceptance:** the auto-commit daemon batched my in-flight work twice this run (and has a documented history of it). Should the daemon skip files touched by a footer-bearing commit from a live queue task (it demonstrably can detect the pattern), or is the amend-forward dance accepted as permanent tax?
3. **Script-scope precedent:** I extended this sweep into `scripts/migrate-clickhouse-xfs.sh` (outside the item's literal AGENTS.md+docs scope) because its runtime messages would mislead operators mid-migration. Confirm this scope-extension judgment is the house default for "any other restatements" items — or should queue items be read hyper-literally with extras harvested as new rows instead?

---

**Handoff:** §f items marked `[NEW]`/harvested are appended to `docs/todo/storage.md` at authoring time; everything else already lives in the queue. This report is a point-in-time snapshot — figures cited are as of 2026-09-25 12:27 CEST, commit `eb844911`.
