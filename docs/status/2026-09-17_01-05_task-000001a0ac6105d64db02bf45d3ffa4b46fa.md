# Task Status Report — btrbk-data oom-kill containment

**Task-Queue-ID:** `000001a0ac6105d64db02bf45d3ffa4b46fa`
**Date:** 2026-09-17 01:05 CEST
**Work item:** TODO_LIST.md P1 — "btrbk-data oom-kill containment (2nd failure mode beside the EIO)" — nightly send died `oom-kill` (page cache charged to the unit cgroup, 20.6G peak). Source: `docs/status/2026-08-21_23-35_*` §e1/§f.9

---

## a) FULLY DONE

1. **Root-cause analysis of the incident class** — the 2026-08-21 `oom-kill` was systemd-oomd picking `btrbk-data.service` as the largest `/system.slice` consumer because the full-tree send charged ~20.6G of **page cache** to its cgroup. No code change was needed in btrbk itself; the fix is cgroup sizing.
2. **Fix implemented** in `platforms/nixos/system/snapshots.nix` (`btrbk-data` serviceConfig, ~line 360-377):
   - `MemoryHigh = "4G"` — the kernel reclaims the unit's **own** page cache early; the cgroup is bounded and the send is **throttled, never killed**. Deliberately NOT `MemoryMax`: a hard Max would re-introduce the exact kill class under a cache spike.
   - `OOMScoreAdjust = -250` — a restart-expensive nightly job (a killed send re-pays hours of QLC reads) drops out of oomd's preferred victims; fastflowlm (+300) remains the designated global-OOM sacrifice per house doctrine.
   - Both choices documented inline with the incident reference.
3. **Scope discipline** — applied to `btrbk-data` only (the incident unit). `btrbk-root` never exhibited the oom-kill failure (its historical failures were TimeoutStartSec, since fixed with 24h), so touching it would have been scope creep.
4. **Verification:**
   - `nix eval` of the evo-x2 `systemd.services.btrbk-data.serviceConfig` → `{"mh":"4G","oom":-250}` (both values land through the module merge).
   - `nix flake check --no-build` → all checks passed (the aarch64-darwin warning is expected per AGENTS.md).
5. **TODO_LIST.md closed out** — item marked `[x]` with the full DONE annotation (what was chosen, why MemoryHigh over MemoryMax, scope, verification, deploy requirement).
6. **Committed** with the required Task-Queue-ID footer. Note: the auto-commit daemon raced the explicit commit and landed `snapshots.nix` in its own heuristic commit (`1f314cd9`) before my pathspec commit (`c51cb4c1`) — both changes are in master; the queue cross-reference footer is on the TODO_LIST commit. Pre-commit hooks (gitleaks, flake check) passed.

## b) PARTIALLY DONE

1. **The fix is not DEPLOYED.** The systemd cgroup sizing only takes effect at the next `nix run .#deploy` (cgroup attributes are read when the unit (re)starts under the new generation). Until then, the running `btrbk-data` unit still carries the old (unbounded) serviceConfig — though the EIO repair gate means the expensive send doesn't run anyway, so exposure is near-zero until the /data corruption repair completes.
2. **The oom-kill fix cannot be live-verified yet.** The real proof (a full-tree send surviving under memory pressure without an oom-kill) is gated behind the /data EIO repair (T04-T08) — the gate deliberately fast-fails the send today. So the containment is config-verified, not incident-verified. This is correct sequencing, not a gap, but it belongs on the record.

## c) NOT STARTED

1. Nothing from this task's own scope was left undone. All follow-up items below were deliberately routed to the backlog (not started here, per contract).
2. Extending the same sizing to `btrbk-root`/`btrbk-pool` if they ever show the class — not started (no evidence they need it).

## d) TOTALLY FUCKED UP!

Nothing. No broken evals, no broken tests, no repo damage, no secret exposure, no daemon-race data loss. The only friction: the auto-commit daemon committed `snapshots.nix` seconds before my explicit commit, so the fix and the TODO close-out live in two adjacent commits instead of one. That is cosmetic (both are on master, the queue derives work from the footer + diffs) but slightly muddies history readability — a known, documented daemon behavior, not a mistake.

## e) WHAT WE SHOULD IMPROVE

1. **Commit-race timing:** for task-queue work, do the explicit commit IMMEDIATELY after the last edit, before writing TODO_LIST (the TODO edit is what gave the daemon its window — it committed the .nix file while I was annotating the list). Sequence: edit .nix → commit → edit TODO → commit. This keeps the whole task in ONE footer-carrying commit.
2. **The EIO repair gate now also masks the oom containment path:** when T04-T08 completes and the marker is created, the first full send will exercise BOTH the old failure modes at once (EIO healed, oom containment active but never proven). The repair runbook should include a post-send check of the unit's `memory.peak` to confirm the 4G MemoryHigh actually bounded the cache.
3. **Class-wide sweep consideration:** `btrbk-root` and `btrbk-pool` run the same `btrfs send` class (root sends are full-tree and go through the pool). If root ever lands in a full re-send (e.g. after a snapshot wipe — which happened 2026-09-12), it will charge the same page-cache profile with NO MemoryHigh. Cheap insurance, one-line each.
4. **TODO item hygiene:** the original item offered three fix strategies ("MemoryHigh + OOMScoreAdjust sizing, or split the send"); the sizing choice is now encoded, but a future reader of the archived source report won't know the split-the-send alternative was evaluated and rejected without reading this report. The DONE annotation covers it — keep this pattern.

## f) Up to 50 things we should get done next

Most of these are backlog fuel (TODO_LIST/ROADMAP routing), sorted roughly by impact. Items 1-10 are the direct neighborhood of this task; the rest are what I noticed in passing within this task's blast radius only (no unrelated research was done).

**Direct follow-ups (this task's neighborhood):**

1. **Deploy** the fix (`nix run .#deploy`) so `btrbk-data.service` actually carries MemoryHigh/OOMScoreAdjust — requires a quiet-IO window (deploy pressure gate was likely satisfiable at session time, but deploys are owner-paced).
2. **Post-first-real-send verification:** after T04-T08 clears the EIO gate, check `memory.peak` of the btrbk-data cgroup ≤ ~4G to prove the containment works under a real full send.
3. **Backlog: add `MemoryHigh = "4G"` + `OOMScoreAdjust = -250` to `btrbk-root.service`** (same full-tree send class; root re-sends are triggered by snapshot loss — the 2026-09-12 glob-delete incident shows that scenario is live).
4. **Backlog: `btrbk-pool.service` same sizing** (cheap, symmetric, pool sends snapshot all `services/*`).
5. **Backlog: eval-time assertion** that all `btrfs send`-carrying units carry a MemoryHigh (the class-guard pattern used elsewhere in the repo) — prevents future btrbk instances from shipping unbounded.
6. **Verify the Sep 17 23:00 btrbk-root window** (adjacent P1 item): Zone-6 guard trips have eaten two consecutive nightly root sends; the 3-day freshness verify FAILs Sep 19 if tonight's send is churn-stopped again.
7. **The /data corruption repair (T04-T08) remains the P0 that unlocks this task's real proof** — gate (b) scrub delta still formally unproven; T06a deletion needs user sign-off after it passes.

**Noticed in passing (adjacent, from this session only):**

8. Deploy-time grep: confirm post-deploy smoke covers `btrbk-data` unit-file diff (new keys appear in the deployed unit) — trivial addition if missing.
9. AGENTS.md BTRFS section: add one line noting btrbk units are MemoryHigh-bounded (2026-09-17) so future sessions don't re-derive the sizing debate.
10. When the flm corpse / :52626 wedge is finally cleared by the owed reboot, re-check that the memory-emergency-guard restore budget behavior hasn't changed the oomd victim landscape this fix assumes (btrbk at -250 assumes flm at +300 is still first victim).

11-50: **No further items generated.** The contract forbids scope creep and unrelated research; everything else legitimately next for this repo (llama-rag regression pin, Turso decision, Hetzner/Borg offsite leg, hermes subvol soak, crush-hot-db first migration verification, DNS `/health` decoupling, Zone-6 grace-window design, etc.) is ALREADY tracked in TODO_LIST.md — duplicating it here would create the exact split-brain the docs-health skill warns about. The TODO_LIST is the queue; this report should not shadow it.

## g) Questions I cannot figure out myself

1. **Should `btrbk-root` and `btrbk-pool` get the identical MemoryHigh/OOMScoreAdjust treatment now** (one deploy, closes the class for the 2026-09-12-style full-re-send scenario), or do you want to keep them untouched until one of them actually exhibits the 20.6G page-cache profile? (I kept to the contract's smallest-change rule and scoped to btrbk-data only; your call on pre-emptive hardening.)
2. **When the EIO gate comes off (post T04-T08), do you want the FIRST post-repair send run manually in a supervised quiet window** (so the oom containment gets a watched first exercise), or just let the nightly timer run it and check `memory.peak` the morning after?
3. **Is the auto-commit daemon racing explicit task commits acceptable history noise**, or should task-queue sessions adopt the edit→commit→edit→commit sequencing (fix first, TODO annotation second) as a standing convention? (The queue cross-reference works either way — the footer commit carries the TODO close-out — but one-commit-per-task reads better in git log.)

---

*Point-in-time snapshot. The authoritative queue is TODO_LIST.md; this report must not shadow it.*
