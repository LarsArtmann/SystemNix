# Task 000001a0b1666334580cacecb0852d0eea69 — Forgejo upstream mirror-outage issue filing

**Date:** 2026-09-18 00:42 CEST · **Scope:** TODO_LIST P1 item "Forgejo upstream: file the 3 verified
mirror-outage issues" · **Source incident:** `docs/status/archived/2026-08-22_08-52_*` §b2/§c
(14-day silent pull-mirror outage: dead queue, TouchMirror masking, credential-helper ENOENT aborts)

---

## a) FULLY DONE

1. **Three issue drafts persisted** (prior session today, commit `e2ef0c4e`, file `docs/services/forgejo-upstream-issues.md`): Issue 1 `TouchMirror` on failed sync advances `mirror.updated_unix` (masks outages from the API/UI); Issue 2 mirror-queue `ErrAlreadyInQueue` dedup-skip logged at `log.Trace` only (wedged queue = zero observable signal at default log level); Issue 3 `AddAuthCredentialHelperForRemote` temp-file failure aborts the whole sync with only a log line (no `CreateRepositoryNotice`, unlike the fetch-failure path). Each draft has problem, real-incident motivation, concrete suggestions, and version note.
2. **No-duplicate search on Codeberg** (prior session): `TouchMirror` → 0 hits, `forgejo-clone-credentials` → 0 hits, "mirror queue stuck" → only unrelated results (2026-09-18).
3. **Second-session independent re-verification (this run, commit `8f0183b1`)**: fetched the live upstream files from codeberg.org `forgejo` branch and confirmed every code claim first-hand:
   - `services/mirror/mirror_pull.go`: `SyncPullMirror` calls `repo_model.TouchMirror` on `runSync` failure; `runSync` sets `m.UpdatedUnix = timeutil.TimeStampNow()` on success; the `AddAuthCredentialHelperForRemote` error path returns `nil, false` with only `log.Error`, while the fetch-failure path a few lines below calls `system_model.CreateRepositoryNotice` — exactly as drafted.
   - `services/mirror/mirror.go`: `Update()` handler dedup-skips with `log.Trace("PullMirrors for %-v already queued for sync", repo)` — exactly as drafted.
   - `modules/git/command.go`: `os.CreateTemp("", "forgejo-clone-credentials-")` error propagates as a fatal sync abort — exactly as drafted.
4. **TODO_LIST loop closed correctly**: item stays unchecked with `— BLOCKED: no Codeberg account/token exists on this machine … filing is one API call once a token exists` (annotation already in place; verified coherent with reality this run).
5. **Doc blemish fixed (this run, same commit series)**: three stray `💘 Generated with Crush` commit-footer artifacts were embedded in the drafts doc body (daemon-era contamination); stripped.
6. **Repo hygiene**: pre-commit hooks green (gitleaks, flake check), tree clean, commits carry the `Task-Queue-ID` footer. Nothing pushed.

## b) PARTIALLY DONE

1. **The work item itself** — everything up to the last step is done; the actual filing (`POST /api/v1/repos/forgejo/forgejo/issues` × 3, issue-write token, or the Codeberg web UI) is blocked on a Codeberg account/token that does not exist on this machine. This is an account-creation decision only the user can make; no agent-side workaround exists (checked `env`, `~/.netrc`, `gh auth` — GitHub-only).
2. **Verification reproducibility** — the drafts cite the *branch* ("current `forgejo` branch, checked 2026-09-18"), not a pinned upstream commit SHA. The verification was done twice and holds today, but the citation is not replayable once upstream moves. Cheap fix, not yet done.

## c) NOT STARTED

1. **The filing itself** (blocked, see b1).
2. **Cross-linking the three issues to each other at filing time** (each draft says "cross-link them when filing") — inherently part of the blocked step.
3. **TODO_LIST line 461 "Forgejo mirror monitoring depth"** (per-repo `mirror_updated` freshness sensing, functional mirror probe, VM test) — separate queue item, deliberately untouched by this run; it is the *local* detection half of the same incident class.
4. **TODO_LIST line 471 stale `commit-graph.lock` removal on the forgejo repo** (user sudo) — flagged as pairing with these filings; untouched.

## d) TOTALLY FUCKED UP

Nothing. No broken state introduced; the only wrinkle is structural, not breakage:

- **Queue dispatch landed on an item a parallel session had already brought to its blocking wall earlier the same day** (drafts `e2ef0c4e` + status report `df06ee16` + BLOCKED annotation, all pre-existing at dispatch). This run's marginal contribution was the independent re-verification + re-verification record + footer-artifact cleanup. Not wasted (independent confirmation of the verify-before-filing gate is real value), but the queue has no dedup gate against same-day re-dispatch of already-blocked items.

## e) WHAT WE SHOULD IMPROVE

1. **Pin the upstream verification rev** in the drafts header (short SHA + date) so the claim is byte-reproducible instead of branch-floating.
2. **Make filing mechanical**: extract title+body per issue into a tiny filing script or three `curl` one-liners in the drafts doc (token read from `/run/secrets` — never inline), so once a token exists filing is one command, not copy-paste surgery.
3. **Queue hygiene**: blocked-on-external-credential items should be skippable (the annotation does this informally, but dispatch still picked it; a `BLOCKED:`-prefixed skip convention for the queue walker would avoid re-runs).
4. **Small process slip (this run)**: first edit attempt failed "must read the file before editing" — read-then-edit discipline should have preceded the edit call. Caught immediately, zero damage.
5. **Trusted the prior session's no-duplicate search without re-running it** — code claims were independently re-verified, the duplicate-search claim was not. Low risk (mechanisms are niche), but an asymmetric verification.

## f) NEXT (up to 50; focused on this task's orbit, ordered by impact)

1. **User creates a Codeberg account + issue-write token; store via sops; file the 3 issues** — the unblock.
2. Pin upstream rev in the drafts header (reproducible verification citation).
3. Add per-issue filing one-liners (curl + sops-rendered token path) to the drafts doc.
4. After filing: record issue URLs in the drafts doc + flip the TODO item to a "monitor upstream response" state.
5. TODO 461: sense per-repo `mirror_updated` freshness (dead-queue class invisible to error counters) — the local detection half.
6. TODO 461: functional mirror probe in post-deploy-check.
7. TODO 461: AGENTS.md gotcha entry (dead queue / TouchMirror masking / restart heals) once upstream triages.
8. TODO 461: VM test for forgejo DNS-gate boot ordering.
9. TODO 471: stale `commit-graph.lock` removal (user sudo, forgejo repo).
10. When upstream responds: draft follow-up comments in Lars's voice (github-voice skill, adapted to Codeberg).
11. If upstream fixes TouchMirror semantics: plan the local monitoring migration to the new freshness field (breaks `mirror_updated`-based checks by design).
12. Consider a textfile collector counting `AddAuthCredentialHelperForRemote Error` journal lines (would have caught issue 3 in hours, not days) — cheap, upstream-independent.
13. Sweep other docs for stray `💘 Generated with Crush` commit-footer artifacts inside doc bodies (same daemon-contamination class, found by accident here).
14. Add a pre-commit/audit guard rejecting commit-footer artifacts inside `docs/**` bodies (class prevention for #13).
15. Verify no OTHER issue drafts in `docs/` cite branch-floating upstream state without a rev pin (generalize #2).

*(Remaining items would be roadmap-padding; the queue's P0/P1 sections already carry the fleet-scale backlog — this report stays scoped per instruction.)*

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Do you want a Codeberg account created, and where should the issue-write token live?** (sops file + which consumers? This is the sole unblock for filing — everything else is done.)
2. **File under your personal Codeberg identity as-is?** The drafts are written first-person ("we ran a self-hosted Forgejo…") with real infrastructure details (134 mirrors, PrivateTmp, NixOS); confirm you're comfortable publishing those incident details under your name before I file.
3. **Branch-current vs rev-pinned verification citations** — when filing, should the issues cite the pinned upstream SHA at verification time (reproducible, but ages), or keep "current branch as of <date>" (fresher, not replayable)?
