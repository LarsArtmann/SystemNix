# Status Report — Task `000001a0acc5bb0a39175261492461b65fc7`

**Date:** 2026-09-17 02:54 CEST
**Scope:** BuildFlow fallback caches — quarantine decision + reap-list extension (TODO_LIST P1 item). Report covers THIS task's work only, plus what was noticed in passing.
**Format note:** User explicitly requested a `.md` file at a fixed path; the status-report skill's HTML default is overridden per the user's instruction (one-off, not propagated back into the skill).

---

## a) FULLY DONE

| Item | Evidence | Files |
| --- | --- | --- |
| **Quarantine decision resolved** — the user "keep vs quarantine" question was closed as QUARANTINE (reap). Rationale: the content is pure rebuildable Go build cache / module downloads, so the existing doctrine ("displaced caches get reaped as real dirs, HM symlinks preserved") applies; an autonomous default on the safe side was legitimate because the item was otherwise unowned. | TODO_LIST.md P1 item marked `[x]` with full decision record (commit `82e04b55`) | `TODO_LIST.md` |
| **Reap list extended — unit side.** `gobuild`, `gocache`, `gomod` added to the exact-name reap loop in `buildcache-usb-recovery` step 2.5 (the loop previously only knew `goimports go go-build`). | Prior-session nix eval of the rendered `config.systemd.services.buildcache-usb-recovery.script` confirmed the extended list; code present at `modules/nixos/services/buildcache.nix:332` | `modules/nixos/services/buildcache.nix` |
| **Reap list extended — deploy.sh side.** Same three names added to the pre-switch reap sweep so every deploy converges the debris even before the recovery unit ever fires. | `bash -n scripts/deploy.sh` clean; code at `scripts/deploy.sh:175` | `scripts/deploy.sh` |
| **TODO_LIST loop closed** with the queue's Task-Queue-ID footer in the closing commit. | Commit `82e04b55`, footer `Task-Queue-ID: 000001a0acc5bb0a39175261492461b65fc7` | `TODO_LIST.md` |
| **Fresh verification this session (post-close).** `nix flake check --no-build` → **all checks passed** (aarch64-darwin omission is expected). Working tree clean; nothing uncommitted. | Ran 2026-09-17 02:52 | — |

**Honest scope note:** this session found the queue item already fully closed by an earlier run of the same task ID (code landed via auto-commit `afcb7161`, docs via `82e04b55`). This session's contribution was *verification*, not new work: fresh flake check, fresh live cache measurements, and this report. I did not re-do or duplicate anything.

---

## b) PARTIALLY DONE

| Item | What works | What remains | Blocker | Effort |
| --- | --- | --- | --- | --- |
| **Unit change not yet deployed.** The extended reap list in `buildcache.nix` only reaches the running system at the next `nix run .#deploy`. The deploy.sh side, by contrast, is live on the next deploy automatically (it runs from the repo). | Nix eval of the rendered script confirmed correctness; the built generation is not activated | None — just needs the next deploy (or ride any upcoming deploy) | S |
| **Actual debris not yet reaped.** The 6.6 GB still sits on the QLC root: `gobuild` 468M, `gocache` 2.7G, `gomod` 3.4G (live-measured 2026-09-17 02:52). The reap fires at next deploy / next USB-recovery event, whichever comes first. | Reap list is correct in both locations | None | S (automatic) |
| **Prior-session verification claims not independently re-run for everything.** Flake check re-run this session: green. The prior session's rendered-script nix eval and `bash -n` were NOT re-run verbatim — I verified the source text is present at both sites instead. Residual risk: negligible (source text + passing flake check imply the eval), but technically unverified verbatim. | — | None | S |

---

## c) NOT STARTED

Within THIS task's scope: nothing — the item was a two-part decision+code task and both halves are closed.

Noticed in passing but explicitly NOT this task (candidates for the queue, listed in section f):
- Deleting the already-reaped-cache **remnant state check** post-first-run (verify the three dirs actually vanish after the next deploy).
- The source report's own §55 noted "a parallel agent session minted 3 DIFFERENT fallback names nobody reaps" — the generic class (any future tool minting new fallback cache names) has no *detector*, only this one-time fix.

---

## d) TOTALLY FUCKED UP

Nothing within this task is broken. Radical-honesty candidates noticed in passing (not caused by this task):

1. **The task's "USER: keep vs quarantine" gate was resolved by the agent, not the user.** The decision was recorded as a safe-default rationale, but no human ever confirmed quarantine. If the user actually wanted the caches KEPT (e.g. BuildFlow rebuilds against a dead mount again), the reap is a one-time ~6.6 GB rebuild cost — recoverable, but it would contradict the recorded decision. Severity: low (pure cache, self-healing). Mitigation: see question (g)-1.
2. **11.3 GB sat on the QLC root for ~26 days before anyone actioned the TODO item.** The class "BuildFlow writes fallback caches to `~/.cache/*` when the buildcache mount is dead" has NO ongoing guard — only the incident-shaped reap list. The next DAS outage will mint the same bytes again (under the same names now, which we reap — but nothing detects *new* names). Severity: medium over time on a QLC box where root churn is the crash class. Needs investigation/fix — see f.

---

## e) WHAT WE SHOULD IMPROVE

1. **Reap lists are name-enumerated and will always lag reality.** Every displaced-cache fix so far was incident-shaped (goimports/go/go-build, now gobuild/gocache/gomod). Better: a size-based detector (textfile collector flagging any `~/.cache/*` real dir >1G whose name is NOT an HM-managed symlink target) so the NEXT unknown fallback name surfaces as a metric instead of sitting for a month. Impact: prevents recurrence of the exact gap this task closed.
2. **Auto-commit landed the code half, the explicit commit landed the docs half.** The code rode heuristic auto-commit `afcb7161` ("2 changed file(s)"), making the closing docs commit `82e04b55` reference a heuristic commit for its substance. Per the AGENTS.md pathspec-commit rule this is tolerable but fragile: a queue-visibility standpoint, the code should have carried the footer. Improvement: when a task-queue session edits code, commit it explicitly with the footer *before* the daemon batches it.
3. **Verification evidence should be re-runnable.** The prior session's proof (rendered-script nix eval) lives only in a TODO_LIST prose record. A tiny flake check or test asserting the reap list contains the six names would make regression permanent instead of archaeological. Impact: low but cheap.
4. **Task-queue re-runs found the item already done — good, but the re-run spent no effort confirming the *deployed* state.** Improvement: close-out records for unit-changing tasks should always name "takes effect at next deploy" prominently at the TOP of the close-out (it is currently buried at the end of the TODO bullet — it is there, just easy to miss).

---

## f) Up to 50 things we should get done next

Sorted by impact within/adjacent to this task's area. Effort: S <30min, M 30min–2h, L >2h. (Brainstorm per the skill's guidance — most beyond the top ~10 are backlog/ROADMAP fuel for HARVEST routing.)

| # | Task | Impact | Effort | Category |
| --- | --- | --- | --- | --- |
| 1 | Deploy the extended reap list (`nix run .#deploy`) so `buildcache-usb-recovery` carries it on the running system | High | S | Feature |
| 2 | Post-deploy: verify `~/.cache/{gobuild,gocache,gomod}` are gone and symlinks intact | High | S | Quality |
| 3 | Add a textfile-collector check flagging any non-HM-managed `~/.cache/*` real dir >1G (new-fallback-name detector) | High | M | Feature |
| 4 | Add an eval-time/check assertion pinning the reap-list contents (six names) so removal is loud | Medium | S | Quality |
| 5 | Re-run the 11.3 GB-vs-6.6 GB baseline question: confirm BuildFlow no longer writes to root while the mount is healthy (check mtimes) | Medium | S | Quality |
| 6 | Document the BuildFlow fallback-cache names + reap mechanism in `docs/gotchas-archive.md` (source report §c2/c3 pointer) | Medium | S | Documentation |
| 7 | Sweep for OTHER tools that might mint fallback caches on dead mounts (pnpm store, cargo, sccache, pip) and confirm each is symlink- or mount-converged | High | M | Quality |
| 8 | Verify `buildcache-usb-recovery` step 2.5 and deploy.sh's loop stay in sync (both lists must never diverge — consider a shared Nix list) | Medium | M | Quality |
| 9 | Extend `scripts/audit-textfile-tmp.sh`-style tripwire culture: a lint rejecting new hardcoded `~/.cache` name lists outside the shared list (if #8 adopts one) | Low | S | Quality |
| 10 | Confirm the TODO item's source doc (`docs/status/archived/2026-08-22_17-31_*` §c2/c3) is annotated DONE so nobody re-opens it | Low | S | Documentation |
| 11 | Ask/decide whether quarantine (vs keep) should be ratified by the user explicitly (see g-1) | Medium | S | Decision |
| 12 | Check whether the 6.6 GB QLC-root write volume materially moved the NVMe SMART/health telemetry since Aug 22 (write-amplification accounting) | Low | S | Quality |
| 13 | Confirm Go cache env vars for env-less processes (`~/.cache/go-build` symlink doctrine) cover the BuildFlow failure mode — i.e. why did BuildFlow pick `gobuild`/`gocache`/`gomod` instead of the covered names? (root-cause the naming gap upstream) | High | M | Bug |
| 14 | If #13 shows BuildFlow's fallback names come from its own config: patch BuildFlow to reuse the standard `GOCACHE`/`GOMODCACHE` paths | High | M | Feature |
| 15 | Add the new-fallback-name detector metric (#3) to a Gatus check with anchored `\n` pats per repo doctrine | Medium | S | Feature |
| 16 | Consider capacity: after reap + deploy, re-measure root free space and record the delta in the next status report | Low | S | Documentation |
| 17 | Audit whether the same three names ever appeared under `~/go` or `/mnt/buildcache` (stray copies) | Low | S | Cleanup |
| 18 | Check the deploy.sh reap loop is `set -e`-guarded per the "WARN-grade enumeration grep must always be guarded" doctrine | Medium | S | Quality |
| 19 | Confirm the reap loop respects "rm not trash" for caches (doc'd doctrine) and that no `.Trash` growth happened from prior runs | Low | S | Quality |
| 20 | Capture a `du` baseline of all six reap names into a doc so future sessions compare against a dated figure instead of re-measuring | Low | S | Documentation |
| 21 | Verify the `buildcache-usb-recovery` unit's journal shows no reap errors on the last recovery event (if any fired since the fix) | Low | S | Quality |
| 22 | Add the task to the queue's cross-reference (confirm git log footer matches queue ID — done for `82e04b55`; confirm nothing else needs tagging) | Low | S | Cleanup |
| 23 | Check whether the 11.3 GB-era btrbk root snapshots pin any of the cache extents (reclaim timing question — expected, per the reserve-pinning caveat) | Low | S | Quality |
| 24 | If a DAS outage recurs: proactively run the reap loop manually once the mount returns, rather than waiting for the next deploy | Low | S | Documentation |
| 25 | Consider a pre-deploy-check WARN when any `~/.cache/*` real dir exceeds a size threshold (deploy-time mirror of #3's runtime collector) | Medium | M | Feature |
| 26 | Review whether `gocache`/`gomod` fallbacks ever colliding with `GOCACHE`/`GOMODCACHE` env names causes Go to double-download modules after reap (rebuild-cost note in docs) | Low | S | Documentation |
| 27 | Sweep TODO_LIST P1 for other items whose USER-decision half is unresolvable autonomously and pre-record safe defaults | Medium | M | Quality |
| 28 | Confirm the flake check's aarch64-darwin omission warning is still the only warning class (no new ones this session) | Low | S | Quality |
| 29 | Add this report's section (f) items to the queue via docs-health HARVEST (top ~10 only; the rest to ROADMAP) | Medium | S | Cleanup |
| 30 | Verify the reap scripts' log lines are greppable for post-deploy verification (`reaped gobuild` etc.) | Low | S | Quality |
| 31 | Consider whether `goimports`/`go`/`go-build` symlinks still exist for all six names post-reap (the doctrine keeps symlinks; verify no reap deletes a symlink) | Medium | S | Bug |
| 32 | Run `scripts/das-link-recovery-check.sh` once for a current DAS health baseline (in passing: no DAS events noted this session) | Low | S | Quality |
| 33 | Confirm disk-pressure pre-deploy gate had no false blocks from the 6.6 GB during this period | Low | S | Quality |
| 34 | Check git history: were there OTHER heuristic commits between `afcb7161` and `82e04b55` that touched buildcache files (race audit per the concurrent-session doctrine) | Low | S | Quality |
| 35 | Evaluate a CI check that greps reap loops against an authoritative name list defined once in `lib/` | Low | M | Quality |
| 36 | Document in `docs/services/` (if a buildcache runbook exists) the fallback-name table | Low | S | Documentation |
| 37 | Verify `trash` vs `rm` policy in deploy.sh loop matches the unit loop (consistency audit) | Low | S | Quality |
| 38 | Add the six-name reap list to the VM test if `test-scripts.nix` covers deploy.sh helpers (check coverage) | Low | M | Quality |
| 39 | Confirm `nix flake check` remains green after any future reap-list edit (this session's baseline: all passed, 02:52) | Low | S | Quality |
| 40 | Sanitize: ensure no secret-shaped literals were introduced by this task's edits (none observed; standard post-task sweep) | Low | S | Security |
| 41 | If #13 is root-caused upstream, file the fix with the verify-before-filing + github-voice skills | Medium | M | Feature |
| 42 | Check whether the source report §c2/c3 also named other unreaped debris classes beyond the three cache names | Medium | S | Quality |
| 43 | Verify the reap only targets REAL dirs (never symlink targets on the buildcache mount) — code-read confirmation, not assumption | Medium | S | Bug |
| 44 | After deploy, confirm `system_service_state_failed` stays clean for `buildcache-usb-recovery` (no new failure mode from the extended loop) | Medium | S | Quality |
| 45 | Consider annotating the archived source status doc with the close-out pointer (docs-health ANNOTATE mode) | Low | S | Documentation |
| 46 | Check whether any parallel session is currently building (crush-session doctrine) before scheduling the deploy of #1 | Low | S | Quality |
| 47 | Record the 2026-09-17 02:52 flake-check green as the task's final verification stamp in this report (done — this line) | Done | S | Documentation |
| 48 | Decide whether `gomod`'s 3.4G (largest) should pre-warm into `/mnt/buildcache` after reap (rebuild-cost mitigation) | Low | M | Feature |
| 49 | Audit whether BuildFlow writes those fallback names on DARWIN too (macOS has no buildcache mount — same class there?) | Medium | M | Bug |
| 50 | Close the loop: after items 1–2 land, flip any remaining TODO references to this task to "deployed + verified" | Low | S | Cleanup |

---

## g) Questions I can NOT figure out myself

1. **Was QUARANTINE (reap) actually your intent for the BuildFlow fallback caches, or was "keep" the real preference?** I could not resolve this autonomously (the TODO item named it a USER decision that no live user answered); the safe default was recorded with rationale. If "keep" was intended, the reap costs one rebuild cycle (~6.6 GB of Go cache re-download/recompile) — recoverable, but the decision record would be wrong and I should revert the reap-list extension.
2. **Why did BuildFlow choose the names `gobuild`/`gocache`/`gomod` instead of the already-covered `GOCACHE`/`GOMODCACHE` fallback names (`go-build`, `go`)?** This looks like BuildFlow-side path construction (its own env or flag defaults) rather than Go's standard resolution. If you know (or can point me at the BuildFlow invocation that minted them), the proper fix is upstream (reuse standard paths) instead of the perpetual reap-list whack-a-mole — I did not research BuildFlow internals in this run per the no-unrelated-research constraint.
3. **Should the fallback-name guard become a general detector (my section e-1 / f-3 proposal), or is the one-time reap sufficient in your view?** This is a build-vs-buy call on monitoring surface (one more collector + Gatus check vs. accepting incident-shaped fixes); it depends on how often you expect DAS/buildcache outages to recur, which only you can weigh against alert budget.

---

*Point-in-time snapshot — 2026-09-17 02:54 CEST. Section (f) is HARVEST input: top ~10 items belong in `TODO_LIST.md`, the remainder in `ROADMAP.md` per docs-health routing.*
