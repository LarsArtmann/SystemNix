# Status Report — Task 000001a08ea43a364ec0910b47ffaaa9f1cb (Off-site backup decision)

**Date:** 2026-09-11 06:31
**Task:** TODO_LIST P0 — "Off-site backup decision (3rd copy)" (flagged since 2026-06-25)
**Work commit:** `09d481b0` (docs: close offsite backup decision — Hetzner StorageBox + Borg chosen)
**Scope discipline:** docs-only change; no Nix eval surfaces touched beyond the pre-commit `nix flake check` gate (passed). Not pushed, per contract.

> **Format override note:** the status-report skill defaults to a styled HTML dashboard; this report is written as Markdown because the task instructions explicitly name a `.md` path. One-off override, not a new default.

---

## Context you need first (what I found vs. what I did)

The queue's work-item text ("Decide: Google Photos/Drive vs sdf rotation vs Hetzner StorageBox + Borg") pointed at a **stale TODO_LIST snapshot**: the literal item text no longer existed in `TODO_LIST.md` when I started. Two prior artifacts already existed in the shared tree:

1. `TODO_LIST.md:170` carried a **2026-09-05 DEFERRED** stance ("revisit when the user reopens it") from `docs/research/2026-09-05_btrfs-internet-sweep-vs-systemnix.md` §2.3.1.
2. `docs/research/hetzner-storagebox-borgbackup.md` had been **updated to DECIDED 2026-09-11** (Hetzner StorageBox BX11 + BorgBackup) by a parallel session, landed by the auto-commit daemon in `84c70af5` ("chore: auto-commit 2 changed file(s) (heuristic)") minutes before my session started.

The decision therefore already existed; what was missing was **closing the loop** — the TODO item still said DEFERRED and contradicted the research doc. My work: reconcile the contradiction, mark done, spawn the implementation follow-up, sync AGENTS.md. This matches the established convention (the Google Sync DORMANT item closed the same way on this queue, `60ea37c9`).

---

## a) FULLY DONE

| Item | Detail |
| --- | --- |
| **Decision reconciled and closed in TODO_LIST.md** | Old DEFERRED line replaced with `[x] DECIDED 2026-09-11: Hetzner StorageBox + Borg`, including the rejections (Google Photos/Drive: `google-sync` DORMANT + mirrors live data, not snapshots; sdf WOOACME rotation: drive FROZEN by user decision + manual cadence = unbounded RPO gap) and pointer to the rationale doc. Supersedes the 2026-09-05 stance explicitly. |
| **Implementation follow-up TODO created** | New unchecked item: implement the Borg leg per the blueprint (`platforms/nixos/system/backup.nix`, sops `borg_password`, port-23 SSH key provisioning, Gatus/backup-coordination registration) **including the 1 TB BX11 sizing caveat** — Borg job MUST exclude rebuildable trees (~700 G of /data ai/models/Steam/llamacpp) or the irreplaceable set needs the BX11→BX21 upgrade decision at ~800 G. The research doc asked for exactly this follow-up; it did not exist in TODO_LIST before. |
| **AGENTS.md synced (memory maintenance)** | BTRFS Backups section (line ~533) said the offsite leg "is the user's Google Photos/Drive (3-drive repurposing plan doc)" — now records the StorageBox+Borg decision with doc pointer + NOT-yet-deployed flag. Verified unique match (single occurrence) before replacing. |
| **Commit with queue cross-reference** | `09d481b0` carries the exact `Task-Queue-ID:` footer. Pre-commit hooks all green: gitleaks, trailing whitespace, Nix flake check (no staged .nix/.sh/.py). Pathspec-scoped commit (`-- TODO_LIST.md AGENTS.md`) per the shared-tree rule. Not pushed. |
| **Honest scope control** | No scope creep: I did NOT re-litigate the decision (it was already made with sound rationale), did NOT start the implementation, did NOT touch ROADMAP/FEATURES/history docs. Docs-only, 2 files, +3/−2 lines. |

## b) PARTIALLY DONE

| Item | Gap |
| --- | --- |
| **The 3-2-1 posture itself (the thing the TODO is actually about)** | The *decision* is done; the *protection* is not. Until the Borg leg deploys, the third copy does not exist — pool RAID1 still dies with the house. RPO remains bounded only by same-chassis btrbk snapshots. This is correctly tracked as the new TODO item, but it is worth being blunt: **marking the P0 done closes a paperwork loop, not a data-loss exposure.** The P0's spirit (data loss risk) is only retired when the first verified Borg archive lands offsite. |
| **Living-doc consistency sweep** | I fixed TODO_LIST + AGENTS.md only. I did NOT sweep `ROADMAP.md`, `FEATURES.md`, or `docs/CONTRIBUTING.md` for stale "offsite deferred / Google Drive is the 3rd copy" claims. Historical status reports are correctly left untouched (point-in-time doctrine), but living docs may still carry the old stance. Cheap follow-up (~5 min, docs-health VERIFY mode). |
| **Task provenance / aging metadata** | The queue item said "Flagged since 2026-06-25" — that date existed nowhere in the repo. My closed item records the decision date but the new implementation TODO does not carry the original flag date, so the aging clock effectively reset. Minor; matters only if the implementation item sits for months. |

## c) NOT STARTED (all correctly tracked, none forgotten into the void)

1. **Implement the offsite Borg leg** — module, sops secret, SSH key, monitoring. New TODO item; the single remaining real gap.
2. **User-side bootstrap steps** (agent cannot do these): Hetzner StorageBox credentials (uXXXX@<host>.your-storagebox.de), SSH-key upload to the box (port 23), `borg init` of the repo, passphrase → sops + a recovery copy decision (password manager).
3. **BX11→BX21 sizing trigger** — no monitoring exists for "irreplaceable set approaching 800 G"; the research doc names the threshold but nothing measures it.
4. **Living-docs stale-stance sweep** (ROADMAP/FEATURES) — listed in (b).
5. **Section (f) harvest into TODO_LIST/ROADMAP** — the skill notes section (f) is docs-health HARVEST input; I deliberately did not mass-insert 40 brainstorm items into TODO_LIST from this task (that is HARVEST's job with routing rigor, and would have been scope creep here).

## d) TOTALLY FUCKED UP

Nothing broke — but full honesty per the requested framing:

1. **I attempted an edit before reading the file with the View tool** (first `edit` on TODO_LIST was rejected by the tool with "you must read the file before editing"). Caught by tooling, zero damage, but it is a violation of the read-before-write rule that tooling had to catch. I had the line content from grep/sed, which is why I considered it read; the rule exists precisely so agents do not reason from grep fragments.
2. **I rewrote a TODO line without tracing its provenance first.** The queue item text and TODO_LIST line 170 were clearly different generations of the same item, and I closed it based on the research doc's DECIDED header + git log (`84c70af5`) without running `git log -S "Flagged since 2026-06-25"` to confirm the lineage chain. In a shared-tree repo with a bulk-committing daemon, confirming which session authored what is cheap insurance; I skipped it because the rationale content itself was verifiable and unambiguous. Acceptable risk, sloppy process.
3. **The decision record's commit message is generic.** `84c70af5` ("chore: auto-commit 2 changed file(s) (heuristic)") is the daemon's batch message carrying a strategically significant decision. Nothing to fix retroactively, but decisions of this weight deserve a pathspec commit with a real message — I did that for my part (`09d481b0`), the prior session did not. Not my damage to repair.
4. **Background-shell surprise on commit:** the commit exceeded the 60s foreground window (pre-commit `nix flake check` is slow) and moved to a background shell. Handled correctly (waited, verified hooks + sha), but I could have anticipated the flake-check latency from the repo's documented hook stack.

## e) WHAT WE SHOULD IMPROVE

1. **Decisions must atomically spawn implementation TODOs.** The research doc said "Follow-up (new TODO): implement per the blueprint" — and no TODO existed. A decide-without-schedule pattern is how "decided 2026-09-11" silently becomes "still not deployed 2027". Convention proposal: the session making a decision adds the follow-up item in the same commit.
2. **Queue items should anchor to canonical locations** (file:line or a stable slug), not full text snapshots — the text I received matched nothing in the file.
3. **Record first-flagged dates on TODO items** so aging metrics survive item rewrites (the 2026-06-25 provenance evaporated in the 2026-09-05 rewrite).
4. **Living-doc cross-checks for status words** (DEFERRED/DECIDED/DORMANT): docs-health VERIFY could grep TODO_LIST stances against the research docs they cite and flag contradictions — today nothing mechanically catches "doc says DECIDED, TODO says DEFERRED".
5. **When the Borg leg is implemented, wire it into the existing failure-pedigree layers from day one** (backup-coordination freshness, Gatus, `backup_ever_succeeded` metric from TODO_LIST, post-deploy smoke) — the cv-backup silent no-op and mail-relay phantom-green incidents both came from monitoring added late.
6. **Restore-path discipline:** the blueprint covers backup, not restore. An untested restore is Schrödinger's backup; the implementation TODO should include a periodic restore drill (even a small-subset one).

## f) Up to 50 things to get done next

Grouped, roughly impact-ordered within groups. Items marked *(noticed in passing)* were pre-existing TODO_LIST items I saw during this task — listed here for the HARVEST pass, not researched further.

**The offsite leg itself (from this task):**

1. **User: supply StorageBox credentials + activate BX11** (uXXXX, hostname; active service check) — hard prerequisite for everything below.
2. **User: upload the evo-x2 SSH public key to the StorageBox** (Hetzner web UI; port 23 is Borg-only there).
3. **User: decide passphrase recovery placement** (sops `borg_password` + password-manager copy) — one dead passphrase = one dead archive.
4. **Provision SSH connectivity** (`~/.ssh/config` host alias for the box, port 23, first-connection host-key pinning) + extend `networking.nix` knownHosts pin if reachable over the normal wire.
5. **Add `borg_password` to sops** (`platforms/nixos/secrets/`, `sops-key-audit.nix` will enforce presence once declared).
6. **Write `platforms/nixos/system/backup.nix`** per the blueprint (Borg wrapper, repo URL, compression, `repokey-blake2`).
7. **Define the exclusion list as data, not prose** — models (291 G), Steam content, llamacpp (92 G), HF caches out; home, services, pool backups, configs in. This is the sizing load-bearing artifact.
8. **Decide Steam save-game handling** — `compatdata` prefixes are the ONLY stateful Steam data (AGENTS.md); include the prefix saves, exclude the game blobs.
9. **Systemd timer + service** for the Borg job (staggered vs 01:00–03:00 backup window; `Persistent=true` per the eval-time-audit convention).
10. **Register in `services.backup-coordination.backups.<name>`** (directory, filePattern, maxAgeHours) → feeds `backup_all_healthy`.
11. **Gatus check** for the Borg job (freshness/failure; Discord alert) — every new backup producer gets monitored, cv-backup precedent.
12. **`backup_ever_succeeded` metric** integration (TODO_LIST item I noticed in passing) — a never-worked Borg repo must be indistinguishable-from-nothing-adjacent, per that item's rationale.
13. **`ioTier` for the Borg unit** — uplink + dedup reads should not fight interactive IO on the QLC NVMe (background tier; the send traffic is network-bound but chunking reads local files).
14. **Pre/post-deploy smoke** entries (repo reachable, last-archive age).
15. **Restore runbook** (`docs/services/` page): full restore + single-file restore + the "StorageBox is Borg-first-class" access notes.
16. **First-restore drill** after go-live (small subset, timed) — untested backup = no backup.
17. **Archive-size monitoring + BX21 upgrade trigger alert** at ~800 G irreplaceable-set (instant upgrade path documented in the research doc).
18. **Retention/prune policy** (e.g. keep-daily/weekly/monthly) — dedup makes generous retention nearly free; decide anyway before data accumulates.
19. **Bandwidth/RPO sanity note** — first seed upload of the irreplaceable set over the home uplink (RPO during seed = none; document the window).
20. **Eval-time guard consideration**: a sops key declared for a disabled-or-missing module should fail loudly (sops-key-audit already does the declared-vs-present half).
21. **`docs/services/` page for the offsite leg** (runbook + monitoring map + break-glass) — every service gets one.
22. **After go-live: re-verify `AGENTS.md` BTRFS Backups paragraph** ("NOT yet deployed" flag must flip) + update the TODO item to DONE.
23. **Living-docs sweep**: ROADMAP.md / FEATURES.md stale "offsite deferred" mentions (from section b).
24. **(Queue mechanics) point future queue items at canonical anchors** to prevent the stale-snapshot mismatch I hit.

**Backup/3-2-1 adjacent — noticed in passing, P0-relevant:**

25. *(noticed in passing)* **/data corruption repair** — the P0 above everything here: btrbk-data has NEVER completed pool-side (EIO inodes, 3 damage windows ~595G+); until repaired, the offsite leg has a hole in its source data too.
26. *(noticed in passing)* **Turso plan decision** — DiscordSync's cloud offsite copy stale since 2026-08-16 (`quota_exceeded` hourly); a second offsite gap in the same posture.
27. *(noticed in passing)* **`backup_ever_succeeded` metric + backup-catchup report** — the never-worked-backup detector this task's leg should ride.
28. *(noticed in passing)* **btrbk receive-freshness + pool-snapshot freshness gauges** in `backups.prom`.
29. *(noticed in passing)* **ClickHouse telemetry backup decision** (accept-as-rebuildable vs clickhouse-backup) — XFS telemetry currently has NO backup coverage.
30. *(noticed in passing)* **ClickHouse backup before the next SigNoz upgrade.**
31. *(noticed in passing)* **`backups/pixel6` into btrbk-pool snapshot set + backup-coordination freshness.**
32. *(noticed in passing)* **`SIGNAL_RECOVERY_KEY.txt` placement** (USER: password manager + delete).
33. *(noticed in passing)* **Browser-history DB backup** (folds into its pool migration).
34. *(noticed in passing)* **Own-tools NVMe→pool migrations**: discordsync + browser-history remain.
35. *(noticed in passing)* **NOCOW for Docker pg volumes on /data** (user-decided, standalone change NOW).
36. *(noticed in passing)* **Deploy.sh backup retention** (timestamped .bak, keep last 3).
37. *(noticed in passing)* **Post-DAS-recovery convergence watch (final leg)** — confirm tonight's root incremental + `backup_all_healthy` green; check bank-sync's 9-day Wise gap backfill.
38. *(noticed in passing)* **Service-completeness manifest audit** — every enabled service ⇒ Gatus + Homepage + backup registration + docs page (would mechanically catch future "backup producer without monitoring" gaps).
39. *(noticed in passing)* **Eval-time audits for unit-shape contracts** (cv-226 class; `Persistent=true` lint for backup timers — directly relevant to item 9).
40. *(noticed in passing)* **Google Sync go-live remains USER-owned** (DORMANT banner + 5-step checklist in AGENTS.md) — complementary photos/docs mirror, not a 3-2-1 leg.

**Fill-out candidates (41–50), noted in passing only — do NOT treat as commitments:**

41. *(noticed in passing)* Attic cache + CI token creation (`atticadm make-token`, push signoz-frontend + hermes tree).
42. *(noticed in passing)* Post-deploy-check: poll browser-history /health, drop explicit restart.
43. *(noticed in passing)* buildcache-gc observability metrics.
44. *(noticed in passing)* USB flap-counter metric + pre-deploy zombie-mount detector.
45. *(noticed in passing)* Identify residual post-deploy WARNs (null-byte line; File Renamer "0 operations").
46. *(noticed in passing)* Investigate Pocket ID SQLITE_BUSY restart bursts.
47. *(noticed in passing)* Flake-check guard: HTML `pat()` needles must start with `<`.
48. *(noticed in passing)* Derive AUTH_VHOSTS from caddy.nix / eval-time assert.
49. *(noticed in passing)* Centralize curl in `scripts/lib.sh` `fetch()`.
50. *(noticed in passing)* Extend mkOidcGate with optional diagnostic output.

Items 41–50 are ROADMAP fuel, not commitments — the docs-health HARVEST pass should apply routing rigor before any land in TODO_LIST proper.

## g) Questions I cannot figure out myself (max 3)

1. **StorageBox access:** is the BX11 service active and can you provide the box hostname + username (uXXXX)? Without credentials, the SSH-key upload (user step) and every implementation step below it stays blocked — the repo, key, and sops secret can otherwise all be prepared blind and converge later.
2. **Passphrase recovery:** where should the `borg_password` recovery copy live (password manager only, printed, or split)? sops holds the operational copy; I only need the *policy* — but a wrong answer here is unrecoverable data loss on the first dead disk.
3. **Exclusion-policy exception check:** is ANYTHING inside the "rebuildable" trees actually irreplaceable to you — e.g. Steam save games / Proton prefixes (confirmed stateful in AGENTS.md), personal model fine-tunes, curated llamacpp GGUFs you cannot re-download — that must be INCLUDED in the offsite set despite the 1 TB budget? This flips item 7/8 from assumption to fact before the first archive is written.

---

**Point-in-time snapshot.** Statuses reflect 2026-09-11 06:31 and the shared-tree state at commit `09d481b0`. Stale by design tomorrow — use docs-health ANNOTATE to correct, never rewrite.
