# Task 000001a0986f3cf12c588c100051d38e47f9 — KNOWN_NEW_METRICS retirement sweep + structural fix

Date: 2026-09-13

## Verdict: DONE

### 1. The 11-entry retirement sweep had NO residue

Every metric named in the TODO item was already retired from the manual
loan list before this task ran:

- `bank_sync_sync_errors_total` + `bank_sync_last_sync_timestamp_seconds`
  — retired 2026-08-31 ("DAS back" sweep; the pre-deploy comment block
  documents it).
- `system_dnsblockd_metrics_fresh` — retired in the same 2026-08-31 sweep.
- `discordsync_projection_dlq_legacy_unchanged` (+ `discordsync_turso_local_only_mode`)
  — retired in the same sweep.
- Later sweeps (2026-09-02, 09-03, 09-04/09-09, 09-09 cv_autoapply,
  09-12 pool-smart, 09-13 btrfs canary) kept the list empty.

`scripts/pre-deploy-check.sh` §10 carried `KNOWN_NEW_METRICS=""` at task
start. Verified by reading the loan-list history comments and the git
history of the sweep sessions.

### 2. Structural fix (the real remaining work) — implemented

**(a) Self-cleaning allowlist** (`scripts/lib/metrics-gate.sh`): the
`present` branch now checks whether the metric is still listed in
`KNOWN_NEW_METRICS`; if so it emits a WARN ("retire the stale loan entry")
instead of a silent PASS. Classification stays non-blocking — the gate's
job is to nag the retirement, since a stale loan under the same metric
name would mask a future genuine phantom regression.

**(b) Auto-derivation from the gatus-config diff**
(`scripts/pre-deploy-check.sh` §10): instead of a hand-maintained list,
metrics referenced by the TO-BE-DEPLOYED gatus config but ABSENT from the
RUNNING generation's config are auto-loaned WARN:

- Locator: nixpkgs' gatus module passes the config via the
  `GATUS_CONFIG_PATH` env var (`systemctl cat gatus`) — NOT
  `--config.file` (verified live against the deployed unit;
  `--config.file` is kept as a fallback). `tr -d '"'` strips the systemd
  Environment quoting.
- Same `extract_gatus_metrics` extractor runs against both the source
  `.nix` and the deployed YAML (the `\n` anchored patterns survive YAML
  double-quote round-trip as literal `\n` two-char escapes, so line-based
  grep extraction works on both — verified against the live store path).
- `comm -23` yields the auto-loan set; a warning lists it.

Live verification on this host: the derivation correctly auto-loans
exactly the 3 monitor365 metrics (`collector_events_collected`,
`cloud_sync_consecutive_failures`, `cloud_sync_upload_backlog_size`) —
referenced by the worktree gatus config but excluded from the deployed
config because monitor365 is disabled. No false loans, no false passes.

### 3. Tests

- `scripts/test-pre-deploy-metrics.sh`: new Fixture D2 proves the
  self-cleaning branch (listed+present → WARN retire; unlisted present →
  PASS) — 11 assertions green.
- Flake check `pre-deploy-metrics-selftest` (runs the fixture selftest
  through nix against the REAL sourced lib) — build green.
- `shellcheck` clean on `pre-deploy-check.sh` and the test script (the
  SC2148 on the shebang-less sourced lib and SC2034 fixture-env warnings
  in the test file are pre-existing).
- `bash -n` clean.

### Files changed

- `scripts/lib/metrics-gate.sh` — self-cleaning loan branch.
- `scripts/pre-deploy-check.sh` — §10 auto-derivation block.
- `scripts/test-pre-deploy-metrics.sh` — Fixture D2.
- `TODO_LIST.md` — item closed.
