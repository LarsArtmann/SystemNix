# Task 000001a0cd82283811924f618a21165fd52f — Status Report: borg-restore-drill env-path pin (verification run)

**Date:** 2026-09-23 14:24 CEST · **Branch:** master · **HEAD at report time:** `ae521c42` · **Work commit (prior run, same Task-Queue-ID):** `818d0ca4` (2026-09-23 13:58:27)

---

## TL;DR

The dispatched work item ("Pin the borg-restore-drill env path to the sops template (flake-wrap or eval-time literal assertion)") was **already fully implemented and closed by a prior run of this exact task** — commit `818d0ca4` carries the identical `Task-Queue-ID` footer, TODO_LIST.md:27 is `[x]` DONE, and docs/todo/storage.md:103 is `[x]` DONE with full detail. The dispatch was a stale-snapshot re-dispatch (the queue's snapshot predates the 13:58 closure commit).

This run therefore became an **independent re-verification** of the prior run's work. Verdict: the pin is correct, both probe directions reproduce, and the tree is green at HEAD. **No code changes were made by this run; no new work commit exists** (the Task-Queue-ID cross-reference already lives on `818d0ca4`). This report is the only artifact this run adds.

## Verification performed (both directions, independently reproduced)

The pin (`platforms/nixos/system/backup.nix:41-56, 167-182`): a **top-level unconditional `mkMerge` branch** asserts `lib.hasInfix drillEnvLiteral drillScript`, where `drillScript` is `builtins.readFile ../../../scripts/borg-restore-drill.sh` and `drillEnvLiteral` is `BORG_ENV_FILE="${BORG_ENV_FILE:-<config.sops.templates."borg-env".path, or the sops-nix default shape while dormant>}"`.

| Probe | Method | Result |
| --- | --- | --- |
| **Negative (drift fires)** | throwaway worktree at `818d0ca4`, sed-drifted the script literal to `/run/secrets/rendered/borg-env-DRIFT`, `nix eval --json .#nixosConfigurations.evo-x2.config.assertions --apply 'a: builtins.filter (x: !x.assertion) a'` | assertion FIRED with the exact message naming the expected literal and the fix |
| **Positive (real tree passes)** | same worktree, drift reverted, same eval | **0** failed assertions |
| **HEAD eval** | same filtered assertions eval at `c5a27b2e` (and later `ae521c42`-era tree) | `[]` — the pin passes in the live tree, including after the parallel lock bump |
| **`nix flake check --no-build` at HEAD** | rc captured correctly on re-run | **rc 0**, only the expected `aarch64-darwin` omission warnings |

Probe-technique notes (both documented in AGENTS.md, both hit once before remembering):

- `nix eval --json …config.assertions` **without** `--apply` crashes on this repo (forces ALL assertion messages; `oci-containers.nix` interpolates a null uid → `expected a set but found null`). The sanctioned probe is the `--apply filter` form, which forces only FAILED assertions' messages.
- Piping nix output through `tail` and reading `$?` reports the filter's rc, not the command's.

---

## Self-critique

### What did I forget?

1. **The rc-capture discipline I preach.** My first `nix flake check --no-build 2>&1 | tail -5; echo "FLAKE_CHECK_RC=$?"` printed `RC=0` from `tail` while the check had actually FAILED (`hindsight` extras eval error from the parallel session's lock bump). This is the exact AGENTS.md rule from the 2026-09-16 vendorHash-banner lesson ("always `set -o pipefail` or capture rc when piping build output through a filter"). Self-caught one step later, but it cost a false "check passed" moment.
2. **The canonical failed-assertions probe.** My first instinct was `nix eval --json …config.assertions`, which hit the documented null-message crash before I remembered the `--apply` filter form.
3. **Enumerating the second parallel commit's file list.** When HEAD moved to `c5a27b2e` (5 files), I never ran `git show --stat` on it — I treated "HEAD eval green" as covering my surfaces without confirming which files changed. It turned out moot (the HEAD assertions eval exercises the pin directly: the assertion forces `readFile` of the drill script and the literal construction), but the check itself was sloppy process.
4. **Nothing task-scoped was forgotten** — the queue loop was already closed by the prior run, so nothing was owed there.

### What could I have done better?

1. **Run the negative probe at HEAD too**, not only at `818d0ca4`. I discarded the worktree before HEAD stabilized; re-creating it at HEAD would have completed the full matrix (drift@HEAD × real@HEAD × drift@prior × real@prior). The assertion code is provably unchanged in between (HEAD eval green + prior-run commit stats), so the two-probe coverage is sound — but running the full matrix is the cleaner standard.
2. **Verify the third claimed direction**: the prior run's close-out claims "Template-path overrides AND script drift both fail eval." I reproduced script-drift only. The template-override direction is code-consistent (the literal derives from `config.sops.templates."borg-env".path`, so overriding the path changes the expected literal and must fire) but this run never executed that probe. It should be folded into the queued fixture test.
3. **Check the repo-health state before starting probes**, not mid-run: the parallel session's 14:09 lock bump (20 nodes, hermes-agent → `33a30fdd`) broke every eval for ~40 min and muddied my first two probe rounds. A quick `git log --since` + status check up front would have led me straight to the pre-bump worktree strategy.
4. **Set pipefail/capture-rc in the FIRST command**, not the fourth.

### What could I still improve?

1. Persist the negative-probe evidence as a standing fixture instead of hand-run probes (already queued as TODO_LIST.md:41 — the fold-in). Hand probes are ephemeral and, per the eval-cache trap doctrine, green results from identical derivations prove less than they feel like they do.
2. Standardize the throwaway-worktree naming (`/tmp/<topic>-neg`) — another session's `/tmp/borg-pin-neg` worktree was identifiable purely by name, which made multi-session attribution cheap. Keep the convention.
3. For re-dispatched/already-done tasks, converge on exactly this shape: verify, change nothing, one status report. No second work commit (the queue derives from footer commits; a duplicate would be noise).

---

## a) FULLY DONE

- **The work item itself** — closed by the prior run (`818d0ca4`, same Task-Queue-ID):
  - Eval-time literal assertion in `platforms/nixos/system/backup.nix`, in an **unconditional top-level `mkMerge` branch** (deliberate: an in-`mkIf` placement was proven blind — it passed flake check on a drifted script).
  - While the module is dormant the assertion pins the sops-nix default shape (`/run/secrets/rendered/borg-env`); once the template exists it pins the template's exact `.path`, so render-path changes are pinned too.
  - Drill default (`scripts/borg-restore-drill.sh:125`) and the assertion's expected literal agree byte-for-byte.
  - TODO_LIST.md:27 `[x]` DONE; docs/todo/storage.md:103 `[x]` DONE; AGENTS.md Offsite Borg section documents the pin + the "change the drill default and the template path together" rule; flake-wrap alternative rejected with rationale.
- **This run's re-verification** — all four probes green (table above).
- **Follow-up queued** by the prior run (TODO_LIST.md:41): fold the negative case into the borg-restore-drill fixture test.

## b) PARTIALLY DONE

- **Re-verification coverage**: 2 of the 3 claimed firing directions reproduced by this run (script drift fires; real tree passes). The template-path-override direction is claimed by the prior run and code-consistent, but not executed here → fold into the queued fixture test (item 1 in the next list).
- **Repo health at the post-bump lock**: eval + flake check green at HEAD, but the 14:09 lock wave (~20 input nodes incl. hermes-agent `33a30fdd`, bank-sync, buildflow, nixpkgs-darwin, signoz-src, PMA, …) has had **no full build sweep** from this run — that verification belongs to the bump session. Observed in passing: that bump transiently broke every evo-x2 eval with `Extra/group name 'hindsight' does not match either extra or dependency group` (the documented hermes-extras class — upstream `73c598e319` removed the extra; a consumer naming it fails uv2nix validation) and was self-healed by a follow-up commit ~40 min later.

## c) NOT STARTED (all pre-existing queue rows; this run appended nothing)

1. **Fold the borg env-path-pin negative case into the borg-restore-drill fixture test** (TODO_LIST.md:41) — the standing replacement for this run's hand probes; should also carry the template-path-override case.
2. **Flake fixture test for `scripts/borg-restore-drill.sh`** (PATH-stub borg, forgejo-scripts-fixture pattern) — TODO_LIST.md:33 / storage.md:46.
3. **Harden the drill**: real-mode `/mnt/hot` mountpoint gate, FAIL records from the top, flake-pinned borg, deep-integrity mode — TODO_LIST.md:31 / storage.md:44.
4. **Restore-runbook accuracy pass**: `borg break-lock` section, OnFailure Discord proof command, block-by-block env sweep, `borg mount` FUSE verify.
5. **Persist fixture test for the offsite-borg pre/post-deploy smoke blocks (§13/§16)**.
6. **Fix pre-deploy §13 silent-skip**: distinguish offsite-borg disabled from a transient eval error — fail loud on the latter.
7. **Real-repo restore drill** (go-live checklist step 9) — owner-gated on StorageBox go-live inputs (hostname/username, `ssh-keyscan -p 23` host-key pin).
8. **borg_password recovery-copy policy** — explicitly open in docs/todo/storage.md (owner decision).

## d) TOTALLY FUCKED UP

Honest ledger for this run — nothing repo-damaging, but three self-inflicted stumbles:

1. **False "check passed" moment** from the rc-after-`tail` mistake (detail above). If I had ended the session there, I would have reported a green flake check while the tree was actually red (parallel churn). Caught because the error text appeared in the same output I was reading — luck-adjacent, not discipline.
2. **One wasted eval round on the forced-assertions crash** — a documented trap I should have reached for correctly on the first try.
3. **One misattributed probe error**: the first /tmp worktree probe ran at the churn-broken HEAD (`9db22bce`), failed with the unrelated `oci-containers.nix` null error, and I spent a round treating it as possibly-mine before the trace showed the nixpkgs origin. ~2 min lost, zero wrong conclusions landed (everything was re-run correctly and no false claim made it into any file or commit).

Also honest: **the run itself arguably should not have existed** — the queue re-dispatched a closed item. The cost was bounded because I verified before writing anything.

## e) WHAT WE SHOULD IMPROVE

1. **Make the filtered-assertions probe the canonical one-liner** (AGENTS.md gotcha or docs/CONTRIBUTING): `nix eval --json .#nixosConfigurations.evo-x2.config.assertions --apply 'a: builtins.filter (x: !x.assertion) a'`. The raw `--json` form is documented as "latent" but the working form is written nowhere.
2. **Stop relying on hand-run negative probes for eval-time pins** — they are ephemeral, easy to misattribute across sessions, and eval-cache-trap-prone. The queued fixture test (c.1) closes this properly.
3. **Queue de-duplication**: closed rows should embed their Task-Queue-ID so a stale-snapshot re-dispatch is detectable by the next agent in one `grep` (queue-owner side; repo-side, the footer-on-commit convention already carries the mapping).
4. **Add a shell-audit pattern for rc-after-pipe** (`CMD | tail; RC=$?` shapes) — scripts/audit-shell-nullglob.sh precedent. I am at least the second person to trip this rule this quarter (the 2026-09-16 lesson exists for the same reason).
5. **Hermes extras pre-bump diff**: the hindsight break recurred today via the documented path (upstream extra removal vs consumer extras list). AGENTS.md carries the lesson; a tiny guard script ("diff `hermes.nix` extraDependencyGroups against the new rev's `[project.optional-dependencies]` before locking") would make it mechanical. Candidate queue item, upstream-adjacent.
6. **Multi-session churn etiquette**: 20-node lock bumps landing mid-verification are fine (concurrent sessions are expected here), but the bump session's follow-up loop took ~40 min during which every eval on the box was red. A one-line convention ("after a broad lock bump, expect eval-red until your heal commit lands — probe at pre-bump revs") would have saved me a round.

## f) NEXT (queue-ready, drawn only from this task + what I noticed in passing)

Pre-existing queued items restated in priority order (1-8), then new candidates from this run (9-18). Items 9+ are unchecked candidates for the queue's pacing gates — NOT appended to TODO_LIST.md by this run (append-only-on-discovery was satisfied: nothing new was discovered that isn't already queued except 10-12, flagged here for the queue owner).

1. Fold the env-path-pin negative case (script drift AND template-path override) into the borg-restore-drill flake fixture test.
2. Flake fixture test for `scripts/borg-restore-drill.sh` (PATH-stub borg: root-gate, bogus `--local`, placeholder-repo tripwire, missing-mountpoint die, happy-path PASS).
3. Drill hardening: `/mnt/hot` mountpoint gate in real mode, FAIL-record-from-top, flake-pinned borg binary, optional `borg extract -n --verify-data` deep-integrity mode.
4. Restore-runbook accuracy pass (`borg break-lock`, OnFailure Discord proof, env sweep, `borg mount` FUSE verify).
5. Persist fixture test for offsite-borg pre/post-deploy smoke §13/§16 (pass/fail/skip branches).
6. Pre-deploy §13 silent-skip fix (disabled vs transient eval error — fail loud on the latter).
7. Owner: StorageBox go-live inputs → flip `services.offsite-borg.enable` → go-live checklist → real-repo drill (step 9).
8. Owner: borg_password recovery-copy policy decision.
9. (candidate) Canonical filtered-assertions probe doc line (e.1).
10. (candidate) rc-after-pipe shell-audit pattern (e.4).
11. (candidate) Hermes extras pre-lock diff guard script (e.5).
12. (candidate) Queue closed-row Task-Queue-ID embedding for re-dispatch de-dup (e.3, queue-owner).
13. Phase 2 hot-DB waves (owner sudo windows; pocket-id/postgres/discordsync + docker data-root) — queued, storage.
14. MountPoint-vs-HM-symlink eval guard — queued, storage.
15. Pool+disk VM-test rebuild in a PSI-calm window — queued, storage.
16. Paperless DR completion: pg_restore drill into scratch cluster + dump-integrity gate — queued, storage.
17. Extend audit-textfile-tmp class B to `.sh`/`.md` surfaces — queued, pipeline (the borg drill escaped that scan).
18. End-to-end staged-file drill for the GOTOOLCHAIN guard block — queued, pipeline.

## g) QUESTIONS I cannot figure out myself (3)

1. **Queue semantics**: the item was re-dispatched ~30 min after a prior run closed it under the identical Task-Queue-ID. Is the harvest snapshot taken before the closure commit lands (a dispatch/close race), and can closed queue rows embed their Task-Queue-ID inline so future agents can detect a stale re-dispatch in one grep instead of full archaeology?
2. **The 14:09 lock wave**: ~20 input nodes moved at once (hermes-agent → `33a30fdd` among them) and broke all evals until a self-heal landed. Was that bump wave coordinated with the upstream hindsight-extra removal, and does its owner still owe a full build sweep at the new lock — or were eval + flake check the agreed verification gate for it?
3. **No-op verification runs**: when a re-dispatched task turns out already done and re-verified with zero changes, is the desired artifact (a) this status-report commit carrying the footer, (b) a silent no-op close, or (c) a no-op marker commit on the queue? The queue derives work from footer commits, so (a) keeps the cross-reference intact — but that is a queue-owner convention I cannot decide alone.

---

## State at close

- TODO_LIST.md:27 `[x]` DONE (closed by prior run; nothing to change).
- docs/todo/storage.md:103 `[x]` DONE, consistent with the verified implementation.
- This run's only artifact: this report. Commit carries the same `Task-Queue-ID` footer; nothing pushed.
- Tree left with only other sessions' dirty files (`AGENTS.md`, unstaged, parallel session) — untouched per multi-agent discipline.
- All throwaway state cleaned (`/tmp/borg-pin-drift` worktree removed; probe scratch under `/tmp` only).
