# Status Report — DMS Wallpaper Dangling-Pointer Incident + Self-Heal

**Date:** 2026-09-14 18:38 CEST
**Scope:** This session only (wallpaper incident diagnosis, restore, self-heal implementation, verification).
**Format note:** User explicitly requested `.md`; the status-report skill's canonical HTML format was overridden by user instruction.

---

## Session Narrative

User reported "What happened to my wallpapers!?" — blank desktop background.

**Root cause chain (verified):**

1. DMS (DankMaterialShell) stores the wallpaper as a **bare file path**, not a folder. Current pointer: `~/projects/wallpapers/cyberpunk-chinese-neon-cheongsam-character.jpeg`.
2. That file (plus `krea_4004.png`) was deleted from the working tree of the wallpapers repo.
3. The PMA auto-commit daemon **baked the deletion into commit `53fe554`** ("Remove deleted AI-generated wallpaper file", Sep 13 03:26) — a heuristic commit enshrining data loss.
4. `dms ipc call wallpaper get` still exits **0** with the dead path — the old `dms-wallpaper-init` guard (`if dms ipc call wallpaper get; then exit 0`) treated a dangling pointer as "user choice" and skipped seeding. Blank screen.

**Fixes applied this session:**

- Restored both files from git history (`git show 53fe554~1:<file>`), byte-verified (8,526,557 B jpeg + 978,799 B png).
- Re-set wallpaper from the protected declarative dir `~/.local/share/wallpapers` (store-backed from `wallpapers-src` input).
- Implemented **dangling-pointer self-heal** in `dms-wallpaper-init` (`platforms/nixos/desktop/niri-wrapped.nix`): at session start, exit only if the current wallpaper file EXISTS; missing/unset → log + re-seed random from collection.
- Updated AGENTS.md (self-heal bullet + testing lesson bullet).

**Verification performed:**

- Derivation builds (shellcheck passes inside `writeShellApplication`): store path `48j4kan0…-dms-wallpaper-init`, forced build via context-carrying `runCommand` (`cpsdpnks…`).
- Three-branch functional test with sed-injected stub `dms` (PATH shadowing cannot win — `runtimeInputs` dirs precede `$PATH` in the wrapper):
  - dangling path → logs "current wallpaper unusable (got: /home/does/not/exist.jpeg)" + calls `wallpaper set` ✅
  - empty reply → logs + re-seeds ✅
  - healthy existing path → exits 0, **no** set call ✅
- Healthy branch additionally verified live against the REAL running DMS (first stub attempt accidentally hit real DMS via runtimeInputs PATH — trace proves exit-0 on the then-current valid wallpaper).
- Full evo-x2 eval clean (the forcing eval evaluates the entire host config incl. HM).

---

## a) FULLY DONE

| # | Item                                                                                                                                                                                                                | Evidence                                                                                                                         | Scope                                                                          |
| - | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| 1 | Root-caused the blank wallpaper: DMS file-pointer + daemon-committed deletion (`53fe554`, Sep 13 03:26)                                                                                                             | `git log --diff-filter=D`, `git show 53fe554 --stat`                                                                             | `~/projects/wallpapers`                                                        |
| 2 | Restored `cyberpunk-chinese-neon-cheongsam-character.jpeg` + `krea_4004.png` from git history                                                                                                                       | `ls -la` post-restore, byte sizes match `53fe554~1` blobs                                                                        | `~/projects/wallpapers`                                                        |
| 3 | Wallpaper re-set from the protected store dir — cycling now derives from `~/.local/share/wallpapers`                                                                                                                | `dms ipc call wallpaper get` → `/home/lars/.local/share/wallpapers/purple-night-city-skyline-river-bridge-light-trails.jpg`      | live session                                                                   |
| 4 | Self-heal implemented: `dms-wallpaper-init` validates current wallpaper FILE existence at session start; dangling/unset → re-seed                                                                                   | drv `48j4kan0…` builds (shellcheck clean); evo-x2 eval clean                                                                     | `platforms/nixos/desktop/niri-wrapped.nix:120-133`, unit description line ~687 |
| 5 | All three logic branches functionally proven (stub `dms`, three scenarios above)                                                                                                                                    | ad-hoc harness output in session transcript                                                                                      | —                                                                              |
| 6 | AGENTS.md updated: self-heal behavior + the runtimeInputs-PATH-beats-stub testing lesson                                                                                                                            | AGENTS.md Quickshell section, 2 bullets                                                                                          | `AGENTS.md`                                                                    |
| 7 | Noticed + flagged the transient parallel-session breakage (`checks.x86_64-linux.paperless` red on `gatus-coverage-audit` option; `deploy-restart-audit.nix` dirty mid-session) — NOT touched, per shared-tree rules | session transcript; **re-verified clean at 18:38** — `nix flake check --no-build` now reports 0 errors (parallel session landed) | —                                                                              |

## b) PARTIALLY DONE

| # | Item                                                                                                                                                                                                             | Works now                             | Open gap                                                                                                                                                     | Blocker                                                                              | Effort |
| - | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ | ------ |
| 1 | **Self-heal guard is NOT DEPLOYED** — lives in the working tree only                                                                                                                                             | Code, build, tests, eval all green    | Needs `nix run .#deploy`; guard fires at next login after deploy                                                                                             | None anymore — flake check re-verified clean 18:38 (parallel session's red resolved) | S      |
| 2 | **User's preferred image still unprotected** — I set the purple-skyline from the safe dir, but the restored cyberpunk jpeg (possibly the user's actual favorite) lives only in deletable `~/projects/wallpapers` | Restoration + re-set done             | Adding it to the `wallpapers-src` collection (repo → flake input update → deploy) = recurrence-proofing                                                      | User preference unknown (see Question 1)                                             | S      |
| 3 | **No persisted regression test** for the three branches                                                                                                                                                          | Ad-hoc stub test proved logic once    | Repo-standard would be an HM eval-level test (precedent: `tests/test-niri-session-config.nix`) or a `tests/test-scripts.nix` fixture                         | None — just not written                                                              | M      |
| 4 | Wallpaper-set **persistence unverified across DMS restart**                                                                                                                                                      | `wallpaper get` answers correctly now | Earlier `settings.json` showed `wallpaperPath: null` (DMS state may live in `cache.json`/runtime); didn't re-check post-set whether a DMS restart retains it | None — one inspection command                                                        | S      |

## c) NOT STARTED

| # | Item                                                                                                                                                                                                               | Why not started                                                                                                                                           | Still wanted?                                                |
| - | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| 1 | **Mid-session deletion watcher** — the guard only runs at session start (`RemainAfterExit` oneshot, `WantedBy=graphical-session.target`); a wallpaper deleted mid-session still blanks the screen until next login | Session-start coverage was the agreed scope ("do it" = the two offered fixes); a timer re-running the (fast-exiting) script every N min is a cheap add-on | Optional — decide after deploy proves the session-start heal |
| 2 | Upstream guard idea: PMA/go-commit heuristic auto-commit should flag (not silently enshrine) deletion-only batches of binary assets — `53fe554` is the second-order data-loss vector                               | Out of session scope; belongs in the PMA repo                                                                                                             | Yes — tracked as next-task item                              |
| 3 | Sweep other repos for daemon-baked deletions around Sep 13 (same class as `53fe554`, incl. its `.config/metadata.yaml` edit which I flagged but did not inspect)                                                   | Out of session scope                                                                                                                                      | Yes                                                          |

## d) TOTALLY FUCKED UP

| # | What is broken                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Severity                                                                             | Root cause                                                                                               | Mitigation                                                                                                |
| - | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| 1 | **The incident itself was a silent data-deletion pipeline**: user-agnostic tooling deleted wallpaper files, and the auto-commit daemon's heuristic commit (`53fe554`) converted a working-tree deletion into permanent-looking history with no confirmation layer. DMS's file-anchored state turned it into a visible blank screen                                                                                                                                                                                               | Medium (data recovered this time; class remains open for ANY file in `~/projects/*`) | No deletion-confirmation layer in the daemon; DMS has no folder-authoritative mode                       | Self-heal (pending deploy) covers the wallpaper symptom only; the daemon class needs an upstream decision |
| 2 | **`dms-wallpaper-init`'s old healthy-check was a phantom-green for the dangling case** — `wallpaper get` exit 0 was treated as "user choice exists" when the pointed-to file was gone (blind-spot class: probing exit status instead of the asserted condition)                                                                                                                                                                                                                                                                  | Fixed in tree, **not yet deployed**                                                  | Exit-code probe vs. content/existence probe                                                              | The new file-existence check; same lesson family as the repo's phantom-green incidents                    |
| 3 | What I forgot / got wrong this session (self-critique): (a) I restored `krea_4004.png` and the cyberpunk jpeg **without asking whether the deletion was intentional cleanup** — restore is additive and reversible, but if it WAS deliberate housekeeping I un-did it silently; (b) I set a DIFFERENT wallpaper (purple skyline) than the one that vanished — possibly not what the user wants on screen; (c) I stopped short of deploy, so the user is still one mid-session deletion away from a blank screen until next login | Low-Medium                                                                           | Preference/intent unknowable from the tree (→ Questions 1-3); deploy deferred out of shared-tree caution | Explicit questions below; deploy is now unblocked and is next-task #1                                     |

## e) WHAT WE SHOULD IMPROVE

| # | Pattern / practice                                                                                                                    | Impact                                                               | Concrete fix                                                                                                                   |
| - | ------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| 1 | Wallpapers (and any personal media) living in `~/projects/*` repos are one daemon commit away from deletion                           | Recurring incident class                                             | Keep the authoritative collection in the `wallpapers-src`-backed dir; treat `~/projects/wallpapers` as scratch/staging         |
| 2 | Probe the CONDITION, not the exit code — third live incident of the phantom-green family this quarter                                 | Wasted incident time                                                 | The self-heal now checks file existence; codify "assert the state, not the signal" more loudly in AGENTS.md review checklist   |
| 3 | Ad-hoc branch tests die with the session                                                                                              | Regressions ship silently                                            | Persist the stub-`dms` three-branch test as a repo fixture (see b.3)                                                           |
| 4 | Testing hardened `writeShellApplication` scripts is awkward — PATH shadowing cannot beat `runtimeInputs`, so stubs need sed-injection | ~2 wasted tool rounds this session                                   | Add the sed-inject pattern as a tiny `scripts/lib/` helper or document in the testing gotchas                                  |
| 5 | Status-report skill says HTML, user workflow says `.md` (again)                                                                       | Spec/usage divergence every report                                   | This report is `.md` per explicit user instruction — if that's the standing preference, say so once and it becomes the default |
| 6 | Daemon heuristic commits swept MY file edits and the parallel session's mid-edit file into shared commits during this session         | Attribution blur; risk of deploying another session's half-done work | Already documented in Critical Rules; consider pathspec-commit awareness in the daemon itself (upstream PMA)                   |

## f) NEXT TASKS (session-derived, ranked; harvest input for TODO_LIST/ROADMAP)

| #     | Task                                                                                                                                                                                                                             | Impact   | Effort | Category      |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | ------------- |
| 1     | Deploy SystemNix (`nix run .#deploy`) — activates the wallpaper self-heal; flake check re-verified clean 18:38                                                                                                                   | Critical | S      | Feature       |
| 2     | Post-deploy verify: restart `dms-wallpaper-init` with a forced dead `wallpaperPath` → journal shows "unusable" + re-seed; confirm unit description updated                                                                       | High     | S      | Quality       |
| 3     | Answer Q1 (below), then add the chosen wallpaper(s) to the `wallpapers-src` collection → flake update → deploy                                                                                                                   | High     | S      | Feature       |
| 4     | Inspect `53fe554`'s `.config/metadata.yaml` hunk — confirm whether that edit was intentional companion cleanup                                                                                                                   | High     | S      | Bug           |
| 5     | Decide the fate of restored `krea_4004.png` (keep in repo or re-delete deliberately)                                                                                                                                             | Low      | S      | Cleanup       |
| 6     | Explicitly commit the two restored files in `~/projects/wallpapers` (daemon will sweep them otherwise)                                                                                                                           | Medium   | S      | Cleanup       |
| 7     | Verify DMS wallpaper persistence across a DMS/quickshell restart (re-check `settings.json`/`cache.json` post-set)                                                                                                                | Medium   | S      | Quality       |
| 8     | Persist the three-branch regression test (HM eval test or `tests/test-scripts.nix` fixture with stub-`dms` sed-injection)                                                                                                        | Medium   | M      | Quality       |
| 9     | Sweep sibling repos for daemon-baked deletion commits around Sep 13 (`git log --diff-filter=D` sweep, esp. binary assets)                                                                                                        | High     | M      | Bug           |
| 10    | Add the sed-inject stub helper to `scripts/lib/` + one-line AGENTS.md pointer                                                                                                                                                    | Low      | S      | Quality       |
| 11    | Update FEATURES.md wallpaper row + CHANGELOG entry for the self-heal                                                                                                                                                             | Low      | S      | Documentation |
| 12    | Upstream (PMA/go-commit): surface deletion-only heuristic batches for confirmation instead of silently committing them                                                                                                           | High     | M      | Feature       |
| 13    | Optional: idle timer re-running `dms-wallpaper-init` every 10 min for mid-session heal (cheap: script exits in <1s when healthy)                                                                                                 | Low      | S      | Feature       |
| 14    | Optional post-deploy smoke line: assert current wallpaper path exists on disk (one grep in `post-deploy-check.sh` desktop section)                                                                                               | Low      | S      | Quality       |
| 15    | Revisit DMS per-monitor wallpapers (`monitorWallpapers`) only if the user actually wants per-output images                                                                                                                       | Low      | S      | Feature       |
| 16-50 | Reserved — remaining capacity deliberately unused: a brainstorm padded to 50 would be ROADMAP fuel, not commitments. The 15 above are the complete actionable set derivable from THIS session without researching unrelated work | —        | —      | —             |

## g) QUESTIONS ONLY YOU CAN ANSWER

1. **Which wallpaper do you actually want as your default** — the purple-night skyline (currently set, from the protected dir), or the restored cyberpunk one? (Determines whether I add the cyberpunk jpeg to the `wallpapers-src` collection. I cannot infer preference from the tree; the old pointer says cyberpunk, my fix says skyline.)
2. **Was the deletion in `53fe554` intentional cleanup?** Did you (or a tool you ran) delete the cyberpunk jpeg + `krea_4004.png` from `~/projects/wallpapers` before Sep 13 03:26 — or should I treat the restore as pure recovery and hunt for what deleted them? (The commit itself is daemon-authored, so history cannot answer intent.)
3. **Deploy now, or wait?** The parallel session's red `checks.x86_64-linux.paperless` resolved at 18:38 and the tree is clean — do you want the self-heal deployed immediately, or is the other session still mid-arc and I should hold? (I cannot see the other session's plan; deploying now would activate it and sweep in anything the daemon has staged.)

---

_Point-in-time snapshot — goes stale. Section (f) is HARVEST input for `TODO_LIST.md`/`ROADMAP.md`. Report left uncommitted per Crush harness contract; the auto-commit daemon will pick it up._
