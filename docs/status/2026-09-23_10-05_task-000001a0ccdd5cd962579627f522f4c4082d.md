# Task 000001a0ccdd5cd962579627f522f4c4082d — Offsite Borg monitoring extras (THIRD dispatch)

**Date:** 2026-09-23 10:05 CEST · **Repo:** SystemNix @ `c460e39e` (31 ahead of origin, push held by policy) · **Scope:** re-verification of the already-completed monitoring-extras item + closing the one verification gap the 09-01 report left open. No unrelated research.

## TL;DR

This is the **third dispatch of the same task ID today** — the work was completed at 08:33 (`655e2af7`, footer attached), re-verified at 09:01 (its report `2026-09-23_09-01_…dd5c.md`), and patched at 09:30 (rendered-env-path fix, `f16f6cc5`). My run found nothing broken and added the one missing piece of evidence: the **positive-path render probe** (`enable = true` via `extendModules`) that the 09-01 report explicitly listed as never-run (§b2). It passes: BE/6 IO tier + all three wiring keys. Recorded as a third-pass VERIFY-NOTE in the storage.md row, committed as `c460e39e` (after amending a daemon-race capture). Two NEW findings surfaced in passing and are routed to the backlog (§f): the smoke blocks assert only 3 of the 4 deliverables (no IO-tier gate), and nixpkgs' override wrapper shape changed (`value` → `content`), which will bite every future probe author.

## Self-review (asked directly)

**What did I forget?**
1. I **never read the 09:30 report** (`2026-09-23_09-30_task-…d4ad.md` — the rendered-env-path fix on THIS deliverable chain). I read the 09:01 report in full, but my "no later regressions" claim rested on a commit filename-scan (`git log --name-only | sort -u`) plus the `f16f6cc5` diff-stat, not on the most recent report's own open-items list. The code-level verification was independent and sufficient, but the chain-state claim had an unread link in it.
2. I **repeated the ephemeral-verification anti-pattern** the 09-01 report named in its own §e.3: my positive-path probe left no committed artifact. Mitigated after the fact — the full command + output transcript is persisted in §a6 of THIS report — but the run order was wrong (persist-later instead of persist-first).
3. I **did not capture buildflow's findings summary** — I ran `buildflow -s nix-flake-check` and confirmed green from the telemetry `success:true` event plus the independent pre-commit flake gate, without reading the step's own findings/report lines. The verdict is solid (two independent gates), but "I read the tool's output" would have been the stronger claim.

**What could I have done better?**
1. **Commit within the same breath as the edit.** I checked the tree clean, made the storage.md edit, then paused — and the auto-commit daemon swept the file into heuristic `8a1c8eae` before my commit landed ("nothing to commit"). The AGENTS.md amend-forward discipline recovered it cleanly (`git show --stat` = exactly my 1-file edit → amend → `c460e39e`), but the race was avoidable: on this tree, stage+commit immediately after any write.
2. **Go straight to the priority-50 override in the probe.** The module row and AGENTS.md already say evo-x2 ships `services.offsite-borg.enable = false` from `configuration.nix`; my first probe attempt used a plain `true` and died on the conflicting-definition error. Attempt two died on the `content`-vs-`value` override shape (new nixpkgs behavior — genuinely undiscoverable in advance). Two of three attempts were spend on re-derivable facts.
3. **Run the cheap gates in parallel with the expensive one.** I launched buildflow first, then did greps while it ran — right shape, but `bash -n` and the deliverable greps could have been batched into the same message as the buildflow launch.

**What could I still improve (process, going forward)?**
1. **Front-load the bail signal.** A re-dispatched DONE item should be dismissible in one read. The storage.md row now carries the third-pass note (deliverables re-confirmed, probe closed) — future re-dispatches of this ID can bail from the row alone.
2. **Label the evidence class in every claim**: content-level (diff read), name-level (file list), or telemetry-level (success flag). My "no regressions" was name-level + one diff-stat; my buildflow green was telemetry-level + an independent gate. Both true, neither was the strongest available form.
3. **Persist-first for probes**: record command + output in the status report (or a fixture) in the same session that runs them.

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | All four deliverables re-confirmed in the current tree | `backup_ever_succeeded` metric: `modules/nixos/services/backup-coordination.nix:77`; IO tier: `platforms/nixos/system/backup.nix:263-264` (+ `ioTier` import :33); pre-deploy §13: `scripts/pre-deploy-check.sh:731-762`; post-deploy §16: `scripts/post-deploy-check.sh:1667-1721` |
| 2 | No later commit regressed any deliverable | `git log 655e2af7..HEAD --name-only` → only TODO_LIST.md churn among deliverable-adjacent files; `f16f6cc5` touched drill script + runbooks + queue docs only (diff-stat read) |
| 3 | Smoke scripts syntax-clean | `bash -n` on both scripts: OK |
| 4 | Flake evaluation green (two independent gates) | `buildflow -s nix-flake-check` → `success:true`, 3m27s (telemetry event); pre-commit hook "Nix flake check (eval-only, no builds)" passed on both commit attempts |
| 5 | **09-01 report's §b2 gap CLOSED — positive-path render probed green** | See §a6 transcript: `IOSchedulingClass=best-effort`, `IOSchedulingPriority=6`, golive-tripwire ExecStartPre, `.last_success` ExecStartPost, `/run/secrets/rendered/borg-env` EnvironmentFile — all four deliverables land on the unit with `enable = true` |
| 6 | Probe transcript persisted (command + output, verbatim) | See the block below this table |
| 7 | Third-pass VERIFY-NOTE recorded in the library row and queue loop confirmed closed | `docs/todo/storage.md:43` (third-dispatch sentence appended); `TODO_LIST.md:30` `[x]` closure verified accurate against code — no drift between row and code |
| 8 | Committed with the exact task footer; tree clean; not pushed | `c460e39e` = amended daemon capture `8a1c8eae` (verified 1 file = my edit before amending); footer is the message's last line |

**§a6 — probe transcript (the persisted artifact):**

Command (throwaway eval, never touches the tree — Critical-Rules safe-verification pattern; the priority-50 inline override is required because `configuration.nix` sets `enable = false` as a PLAIN value, and the wrapper field is `content`, not `value` — see §d3):

```nix
nix eval --impure --json --expr '
let
  f = builtins.getFlake (toString /home/lars/projects/SystemNix);
  sys = f.nixosConfigurations.evo-x2.extendModules {
    modules = [{ services.offsite-borg.enable = { _type = "override"; priority = 50; content = true; }; }];
  };
  svc = sys.config.systemd.services.borgbackup-job-hetzner.serviceConfig;
  plain = s: builtins.unsafeDiscardStringContext (toString s);
in {
  ioClass = plain svc.IOSchedulingClass;
  ioPrio = toString svc.IOSchedulingPriority;
  pre = plain svc.ExecStartPre;
  post = plain svc.ExecStartPost;
  envFile = plain svc.EnvironmentFile;
}'
```

Output (2026-09-23 ~09:55, at `d858e945`):

```json
{"envFile":"/run/secrets/rendered/borg-env","ioClass":"best-effort","ioPrio":"6","post":"/nix/store/xjl7p8dvyk2j53kqf7f43kdj4ypbxz7g-coreutils-9.11/bin/touch /var/lib/borg-offsite/.last_success","pre":"/nix/store/n07wyrycgx1qi3la46xvp8nlgpc64n7f-borg-offsite-golive-check/bin/borg-offsite-golive-check"}
```

## b) PARTIALLY DONE

| # | Item | Works now | Remains | Blocker | Effort |
|---|------|-----------|---------|---------|--------|
| 1 | Persisted positive-path verification | Transcript in §a6 (committed with this report) | No executed test artifact — the queued fixture-test row (storage.md:47) covers the §13/§16 smoke blocks but not the render probe; a cheap eval-time check would close it | None | S |
| 2 | buildflow evidence quality | `success:true` + independent pre-commit gate | The step's own findings summary was not read/captured | None | S |
| 3 | Runtime confirmation of `backup_ever_succeeded` in `backups.prom` | Code verified safe by construction this run: `MTIME` is ALWAYS numeric (`stat … \|\| echo 0`, else-branch `MTIME=0` — read at `backup-coordination.nix:57-66`), so the `[ … -ne 0 ]` test cannot error and the line always emits | First live tick after the next deploy (units absent while dormant — deploy-gated, unchanged since 09-01) | deploy-gated | S |

## c) NOT STARTED (noticed in passing; no new research)

All already tracked — listed for completeness with pointers, NOT re-queued:
- §13/§16 smoke-block fixture test (TODO_LIST:34, storage.md:47) — also the vehicle for §b1 above.
- Pre-deploy §13 silent-skip fix — fail loud on transient eval error (TODO_LIST:35, storage.md:48).
- Drill hardening: `/mnt/hot` mountpoint gate, FAIL-records-from-top, flake-pinned borg, deep-integrity mode (TODO_LIST:31, storage.md:44).
- Restore-runbook accuracy pass: `borg break-lock`, OnFailure Discord proof, block-by-block env sweep, `borg mount` FUSE verify (TODO_LIST:32, storage.md:45).
- Drill fixture test (TODO_LIST:33, storage.md:46).
- VM test for `platforms/nixos/system/backup.nix` (TODO_LIST:56).
- Drill env-path pin to the sops template (TODO_LIST:27).
- backup-catchup report script — the metric half landed; stamps-vs-OnCalendar + prom diff + btrbk dry-run remain (TODO_LIST:82, monitoring.md:15).
- Go-live inputs (user decision) + recovery-copy policy (TODO_LIST:58, storage.md:54).
- First-REAL-run watch row: archive-vs-exclude audit, BX21 trigger stamp, AGENTS.md header flip (storage.md:51).
- audit-textfile-tmp class B extension to .sh/.md (TODO_LIST:217, pipeline).

## d) TOTALLY FUCKED UP

Radical honesty — nothing runtime-broken (the chain is dormant and every gate is green), four process defects:

1. **I created a daemon-race window and then needed the recovery path.** Edit → pause → commit meant the auto-commit daemon captured my storage.md edit into heuristic `8a1c8eae` first; my commit attempt returned "nothing to commit" with hooks already running. Recovery was text-per-AGENTS.md (verify `--stat` = exactly my 1 file → amend-forward → `c460e39e`), but the window was my pacing error, not fate. Second occurrence class today on this repo (the 09-01 report's §e.2 described the same daemon behavior from the authoring side).
2. **Ephemeral verification, again — the exact anti-pattern the 09-01 report flagged.** My probe ran green and stayed unpersisted until this report. If a fourth dispatch arrives before this report is read, the probe would be re-run a fourth time. (Mitigated: §a6 transcript now carries it.)
3. **The unread link in the chain:** I verified code independently but cited chain-state ("no regressions") without reading the 09:30 report — the most recent document about these exact deliverables. Had that report found a defect in the smoke blocks, my name-level sweep would have missed it. Evidence-class lesson: a file-list sweep proves "files untouched", not "defects absent".
4. **Smoke blocks gate only 3 of the 4 deliverables — found only because I re-grepped for a word outside the block's own vocabulary.** My first grep (`offsite|borg`) could never see an `IOScheduling` line, and there is none: neither §13 nor §16 asserts the IO tier (verified: `grep -n IOScheduling` on both scripts → zero hits). The ioTier.background wiring — deliverable #2 of the original item — is enforced by NOTHING automated; it survives on code review + my probe. Every dispatch in this chain (implementer, 09-01 re-verifier, me until the second look) either missed or didn't mention this.

## e) WHAT WE SHOULD IMPROVE

1. **Queue verify-before-dispatch (third occurrence, same item, same day).** The harvester keeps dispatching `[x]`-closed rows. My row note now front-loads the bail, but the systemic fix is queue-side (skip DONE rows, or carry the source-report pointer). Feedback to the tq pool owner — not fixable from this repo.
2. **Persist-first convention for probes and one-off gates:** command + output land in the status report (or a fixture) in the same session. Cheap, kills the re-verification loop permanently.
3. **Commit-immediately on daemon-active trees:** stage + commit in the same tool-batch as the edit; treat any pause as a race window. The amend-forward discipline is the recovery, not the plan.
4. **Smoke-gate completeness check as a step, not a side-effect:** when a task's deliverables are N items, enumerate the gates per item and grep each gate's OWN vocabulary (the `IOScheduling` miss happened because the grep pattern came from the item name, not the wiring). A 4-deliverable × gates matrix would have caught §d4 in one pass.
5. **Evidence-class labeling in close-outs:** name-level / content-level / telemetry-level / executed — say which, so the next reader knows what a "verified" claim actually bought.
6. **nixpkgs module-system drift deserves a docs note** (see §f2): the override wrapper's inner field changed (`value` → `content`, error at `lib/modules.nix:1458`), and every AGENTS.md probe recipe that hand-rolls an override attrset is now stale.

## f) Next tasks (ranked; ★ = NEW this run, appended to TODO_LIST/docs/todo; rest tracked with pointers)

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | ★ Add an `IOSchedulingClass`/`IOSchedulingPriority` assert to pre-deploy §13 + post-deploy §16 borg blocks — the IO-tier deliverable currently has NO automated gate (§d4); §13 asserts via the eval'd serviceConfig, §16 greps the deployed unit file | High | S | Quality |
| 2 | ★ Document the nixpkgs override-shape change for probe authors: inline override attrsets are `{ _type = "override"; priority = 50; content = …; }` (inner field `content`, NOT the long-documented `value`; error `attribute 'content' missing` at `lib/modules.nix:1458`), and plain `enable = false` in configuration.nix forces external probes to priority ≤ 50. Lands in AGENTS.md gotchas or a pipeline todo row | Medium | S | Docs |
| 3 | ★ Persist the positive-path render probe as a committed artifact — cheapest shape is an eval-time flake check (extendModules + assert the four §a5 values), NOT the full VM test; folds with the §13/§16 fixture row if the owner prefers one vehicle | High | S | Quality |
| 4 | Fixture test for §13/§16 smoke blocks, pass/fail/skip branches (TRACKED — TODO_LIST:34, storage.md:47); fold #1/#3 into it if sensible | High | S | Quality |
| 5 | Fix pre-deploy §13 silent-skip: discriminate offsite-borg-disabled (attribute-missing eval error) from transient eval failure — fail loud on the latter (TRACKED — TODO_LIST:35, storage.md:48) | High | S | Bug |
| 6 | Drill hardening: real-mode `/mnt/hot` mountpoint gate, FAIL records from the top, flake-pinned borg, deep-integrity mode (TRACKED — TODO_LIST:31, storage.md:44) | High | M | Quality |
| 7 | Restore-runbook accuracy pass: `borg break-lock` section, OnFailure Discord proof command, block-by-block env sweep of BOTH runbooks, `borg mount` FUSE verify (TRACKED — TODO_LIST:32, storage.md:45) | Medium | M | Docs |
| 8 | Drill fixture test, PATH-stub borg (TRACKED — TODO_LIST:33, storage.md:46) | Medium | S | Quality |
| 9 | VM test for `backup.nix` before go-live: tripwire-on-PLACEHOLDER, job shape, `.last_success` write, fake borg on PATH (TRACKED — TODO_LIST:56); supersedes #3's coverage if the owner wants the heavy version only | High | M | Quality |
| 10 | Pin the drill's env path to the sops template — flake-wrap or eval-time literal assertion (TRACKED — TODO_LIST:27) | Medium | S | Quality |
| 11 | backup-catchup report script: stamps vs OnCalendar + prom diff + btrbk dry-run (TRACKED — TODO_LIST:82, monitoring.md:15) | Medium | M | Quality |
| 12 | First-tick runtime confirm: `backup_ever_succeeded{backup="offsite-borg"}` row lands in `backups.prom` after the next deploy (deploy-gated; carried on the storage row since 09-01) | Medium | S | Quality |
| 13 | Go-live inputs from owner: StorageBox hostname/username, `ssh-keyscan -p 23` host-key pin, recovery-copy policy (TRACKED — TODO_LIST:58, storage.md:54; BLOCKED on user) | High | — | Decision |
| 14 | After first REAL run: archive-vs-exclude audit, BX21-trigger size stamp, AGENTS.md DORMANT→live header flip (TRACKED — storage.md:51, watch) | Medium | S | Quality |
| 15 | Confirm next deploy carries clickhouse-db-backup + first 03:00 run (TRACKED from the 09-01 report's §f6 — still unverified as of this run) | High | S | Quality |
| 16 | Verify restic-app-dumps first nightly (was scheduled 05:45 today per the 09-01 report — unverified in passing; freshness row + `restic check`) | High | S | Quality |

## g) Questions I could NOT figure out myself

1. **Why does the queue re-dispatch `[x]`-closed rows?** This task ID completed at 08:33, was re-verified at 09:01, and dispatched a third time at ~09:50. Is the harvester's DONE-filter broken (tq pool config / go-taskqueue), or is repeated independent verification intentional policy? The answer decides whether I keep writing bail notes into rows or the fix belongs in the pool.
2. **What is the sanctioned persisted artifact for the positive path** — a cheap eval-time flake check (§f3, minutes), the queued §13/§16 fixture test (§f4, covers the smoke blocks), or the full backup.nix VM test (§f9, most realistic)? Three vehicles exist; building all three duplicates coverage, and the owner's weight preference decides.
3. **Should `services.offsite-borg.enable = false` in `configuration.nix` become `lib.mkDefault false`?** The plain value forces every external probe/override to priority ≤ 50 (§a6 command). `mkDefault` would let plain overrides win and removes a recurring trap — but the plain form may be deliberate, making the go-live flip a conscious configuration.nix edit. Owner call.
