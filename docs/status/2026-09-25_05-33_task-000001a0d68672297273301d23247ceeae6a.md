# Task Report: task-000001a0d68672297273301d23247ceeae6a — Re-Dispatch Diagnosis + Full Review

| Field | Value |
| ----- | ----- |
| Task | Codify the single-victim /data repair recipe (TODO storage row; source: archived 2026-09-12_03-54 §3/§6) |
| Date | 2026-09-25 05:33 |
| Run | **Attempt 2** — re-dispatch of an ALREADY-COMPLETE item; this run's product is the root-cause diagnosis, live re-verifications, two stale-premise row annotations, and this report |
| Companion | `docs/status/2026-09-25_05-14_…` (in-run record, attempt 1) and `docs/status/2026-09-25_05-19_…` (attempt-1 full self-review); both kept |
| Scope | THIS task's work + what was noticed in passing. No unrelated research. |

## TL;DR

The queue re-dispatched this task because **attempt 1 never closed**: it finished the work (footer commits landed 05:05–05:29) but its verify gate — `.tq-verify` = `nix flake check --no-build` — failed at 05:29:33 on the environmental `/run/binfmt` breakage (the boot-tmpfiles-cycle class documented in the stability row), so the queue recorded `task.failed` and re-claimed at 05:29:35. This run (attempt 2) verified the completed work surface-by-surface, re-proved the recipe's live-verified `dd` claim a **second** independent time, root-caused the re-dispatch from the tq facts log, and found the gate **GREEN** mid-run: `/run/binfmt` reappeared at 05:32 (environmental heal — I cannot sudo; no repo change landed; actor unknown) and the full `nix flake check --no-build` now passes at clean HEAD `1a4952df`. Stale gate rows (TODO_LIST 43 + pipeline.md) were annotated pairwise with the new evidence. No artifact work existed left to do; the item was and is complete. The run's honest finding: **the whole task queue's verify gate was — and after the next reboot may again be — dead on a boot-fragile environmental precondition, and the queue burns attempts doing no-op re-dos when that happens.**

## Why this run exists (root cause, from the tq facts log)

`tq show 000001a0d68672297273301d23247ceeae6a` (read-only) shows the full sequence:

| Time (2026-09-25) | Fact |
| --- | --- |
| 05:05:30 | task enqueued + claimed (worker-23073) — attempt 1 begins |
| 05:05–05:29 | attempt 1 does the work; footer commits land (`3ff7dfd7` recipe, `1a4952df` self-review) |
| 05:29:33 | `task.failed` attempt 1, stage `verify`: `agent verify failed ("nix flake check --no-build"): exit status 1 … error: getting attributes of path "/run/binfmt": No such file or directory` |
| 05:29:35 | task re-claimed (attempt 2 — this run) |
| ~05:32 | `/run/binfmt` REAPPEARS on the host (dir + aarch64 qemu symlink, mtime 05:32) — **not by me** (sudo is banned in my sandbox; no deploy in git log; flake.lock and HEAD unchanged). Most plausibly the documented manual heal (`sudo systemd-tmpfiles --create --remove --exclude-prefix=/dev`) run by the owner or a parallel session |
| 05:33+ | this run: full `nix flake check --no-build` → **"all checks passed!"** at clean HEAD |

Conclusion: the item was complete since attempt 1; only the gate's environmental death prevented closure. This attempt's verify will pass, and the task should close normally — but only because a lucky external heal landed mid-run.

## a) FULLY DONE

1. **The task itself (attempt 1, re-verified this run)** — recipe codified as `## Single-victim /data repair recipe` at `docs/services/jan.md` (3 steps: same-fs `trash` with `stat` verification → unmasked `dd if=<trashed> bs=4k count=1` re-verify on the TRASHED copy → atomic doc sweep across TODO row + runbook + AGENTS.md in one commit, `docs/status/**` exempt, re-download preconditions). Cross-links live from jan.md's RESOLVED bullet and AGENTS.md's corrupt-GGUF bullet.
2. **Queue closure verified in BOTH surfaces, no drift** — TODO_LIST row `[x]` DONE note + `docs/todo/storage.md` library row `[x]` with the full note; `scripts/check-todo-system.sh` green this run.
3. **Footer-bearing commits in history** — `3ff7dfd7` and `1a4952df`, both ending `Task-Queue-ID: 000001a0d68672297273301d23247ceeae6a`; tree clean at `1a4952df` before this run's edits.
4. **Healthy-side `dd` claim re-proven LIVE a second time (this run, independent of attempt 1)** — `dd if=…/gemma-4-26B-A4B-heretic-APEX-Balanced/mmproj.gguf bs=4k count=1`, run unmasked: `1+0 records in / 1+0 records out / 4096 bytes copied`, rc 0. The recipe's healthy-branch claim is now two-session-live; the damaged branch remains cited (no damaged file exists — inherent).
5. **Re-dispatch root-caused** — see the table above; the answer is environmental gate death, NOT a queue harvester bug and NOT incomplete work.
6. **Verify gate GREEN, this run's own evidence** — full `nix flake check --no-build` output ends `all checks passed!` (aarch64-darwin omission warning is the documented expected one).
7. **Stale-premise rows annotated pairwise (no-drift rule)** — TODO_LIST row 43 and `docs/todo/pipeline.md` row both now carry the dated UPDATE: gate green at clean HEAD, `/run/binfmt` environmental, keep open for post-reboot proof + the durable fix.
8. **Queue state untouched** — no `tq dismiss`/cancel/DB mutation (self-dealing); the task closes through the normal verify path on this attempt.
9. **Report written same-run** (this file) with the same Task-Queue-ID footer commit.

## b) PARTIALLY DONE

1. **The green gate is real but not durable.** The heal was environmental and boot-ephemeral; the ROOT fix (break the `hot-user-caches-nix-bootstrap` ↔ `systemd-tmpfiles-setup` boot cycle + tmpfiles-applied tripwire + `boot.binfmt.preferStaticEmulators` belt) is queued as the stability row (TODO_LIST 109) and unlanded. Next reboot plausibly re-kills `/run/binfmt`, re-dead the gate, and re-triggers this exact re-dispatch loop for every queue task.
2. **Heal attribution unknown.** The recovery is verified; its actor is not. Manual heals leave no breadcrumb (deploys do), so forensics cannot distinguish owner-heal / parallel-session-heal / nix-daemon restart side effect.
3. **Attempt-1's partially-done items remain partially done** (all queued, none lost): BTRFS-side cross-link of the recipe (discoverability for non-Jan victims); the daemon-race policy codification; the recipe's "in ONE commit" step-3 letter vs the ~10-min auto-commit daemon (attempt 1 itself needed 3 commits — the recipe text is unachievable as written under the daemon until the policy lands); jan.md GPU-table truncated command; trash-purge policy decision.
4. **The 05-19 report's g3 question is now moot but was never closed anywhere** — "is manual-guards + `--no-verify` acceptable while the flake-check gate is dead" — the gate is alive again, so the question evaporates; this report is the only place recording that. Question loops need a closure mechanism (see e6).
5. **Post-reboot proof is missing by construction** — my annotation says the gate deaths were environmental, but the boot-probe that would PROVE it (reboot → re-run the gate) hasn't happened; the row stays open pending it.

## c) NOT STARTED (deliberately; all queued or owner-gated)

Nothing from THIS task's ask remains open. Everything else seen in passing is tracked: see §f for the full carry-forward list. Highlights: /data damage-set inventory; BTRFS cross-link; /data corruption repair P0 (T04–T08, blocked:user); gemma re-download decision; offsite-Borg go-live chain; the durable `/run/binfmt` fix; phase-2 hot-DB waves.

## d) TOTALLY FUCKED UP (what I forgot; honest)

1. **What I nearly forgot: to ask WHY the dispatch existed.** My first instinct was verify-and-close as a no-op. The `tq show` probe (run second, almost skipped) is the difference between "duplicate dispatch, shrug" and a root-caused systemic finding. A no-op run without the probe would have shipped zero value and left the gate-death cycle invisible.
2. **The systemic fuckup is the queue's, not this run's: verify is gated on a boot-fragile environmental precondition, and gate deaths burn task attempts on no-op re-dos.** Attempt 2 re-did an already-complete item; with `maxAttempts=3`, one more such cycle would DLQ a DONE task. A one-command pre-dispatch check (grep the footer `Task-Queue-ID` in `git log` before dispatching) would have caught this class entirely.
3. **This run's success at close is partly luck of timing.** Had the external heal landed 3 minutes later, this attempt would have failed verify exactly like attempt 1, burned attempt 3, and DLQ'd a finished task. I cannot heal the system (sudo banned in the agent sandbox) and cannot touch `.tq-verify` (self-dealing) — the fix MUST be environmental (heal runbook) or structural (stability row).
4. **The recipe's own step 3 ("in ONE commit") was violated by its own authoring run and the text still says it.** Attempt 1 landed in 3 commits because the auto-commit daemon swept the edits. The recipe as written is unachievable under the daemon cadence; nobody has reconciled the text with reality yet (queued under the daemon-race policy row). A recipe that its first execution cannot follow is a defect in the recipe.
5. **Mooted questions don't get closed.** 05-19's g3 became obsolete within the hour and nothing tracked that; this report closes it narratively only. Same for any report question answered by later events — the loop relies on someone re-reading old reports.
6. **Category-d severity in the ARTIFACT: none.** No code, config, or service surface was touched; nothing removed; the landed recipe is accurate. The failures are process-shaped.

## e) WHAT WE SHOULD IMPROVE

1. **tq pre-dispatch satisfied-check** — before dispatching an item, grep `git log` for a commit whose footer carries this Task-Queue-ID (the payload already knows the ID); if found, skip dispatch or dispatch as verify-only. One command kills the no-op re-dispatch class.
2. **tq verify-failure classification** — when verify fails, re-run the gate at the PRE-attempt rev (or against a recorded clean baseline): pre-existing failure ⇒ route to a gate-dead holding state / alert, do NOT count against task attempts and do NOT blind-retry. This is the repo's clean-HEAD-baseline discipline, automated at the queue layer.
3. **Land the durable `/run/binfmt` fix** (stability row): break the boot ordering cycle + tmpfiles-applied boot tripwire + `boot.binfmt.preferStaticEmulators = true` (drops `/run/binfmt` from the sandbox paths entirely). Highest single leverage item in the queue right now: it unblocks the gate at every boot, fleet-wide, permanently.
4. **Gate-dead triage runbook line** (CONTRIBUTING, one paragraph): "verify failing across unrelated tasks? probe, in order: `/run/binfmt` exists → `nix flake check --no-build` at clean HEAD (pre-existing vs introduced) → nix-daemon restart (stale-fetch class) → only then suspect the task."
5. **Heal-attribution breadcrumb** — manual system heals should leave one journaled line (or a state-file stamp) so the next forensics round can attribute recoveries; deploys already do this implicitly.
6. **Question-loop closure** — when a report's g-question becomes moot or answered by events, the next touching run annotates the old row/report (this report does it for 05-19 g3; make it a habit, not a surprise).
7. **Reconcile the recipe's step-3 text with the daemon cadence** when the daemon-race policy lands (ONE-commit carve-out or pre-stage discipline), and update jan.md in the same change.
8. **Keep re-proving cheap live claims on every touch** — the `dd` re-prove cost milliseconds and upgraded the claim from "live once" to "live twice, independent runs". Do this for every inherited command-shape claim that is cheap to re-run.
9. **Considered and rejected:** automated presence-guards for the recipe section in flake checks (prose presence-guard = noise; unchanged from attempt-1's rejection); editing queue DB state to force-close the task (self-dealing, forbidden).

## f) NEXT THINGS (up to 50; NEW = appended/annotated this round, QUEUED = already tracked, read this run, untouched)

**NEW — surfaced by this run:**
1. Land the durable `/run/binfmt` fix (boot-cycle break + tmpfiles-applied tripwire + `preferStaticEmulators` belt) — stability row, TODO_LIST 109; the 05:32 heal is boot-ephemeral and the whole queue's verify dies again at the next reboot otherwise.
2. Post-reboot gate proof: after the next reboot, re-run `nix flake check --no-build` at clean HEAD; confirm the four failures were environmental (drv shapes binfmt/build-derived) and close or re-scope TODO row 43 + the pipeline row from that evidence.
3. tq pre-dispatch satisfied-check (footer-grep) to stop no-op re-dispatches of committed items (meta/pipeline).
4. tq verify-failure pre-existing-vs-introduced classification + gate-dead holding state instead of burning attempts (meta/pipeline).
5. Gate-dead triage runbook one-liner in CONTRIBUTING (`/run/binfmt` → clean-HEAD baseline → nix-daemon restart).
6. Heal-attribution breadcrumb convention for manual system heals.
7. Reconcile the recipe's "ONE commit" step-3 text with the daemon cadence when the daemon-race policy lands (jan.md edit rides that change).

**QUEUED — adjacent to this task, read this run, not re-verified:**
8. Cross-link the recipe from the AGENTS.md BTRFS /data-damage context (storage `[ready]`).
9. Build the /data damage-set inventory — consumes the recipe's victims; cites the snapshot-pinning doctrine table (storage `[ready]`).
10. Cross-link the flm AGENTS bullet to the snapshot-pinning doctrine table (storage `[ready]`).
11. Refresh the doctrine table's `/data/docker` sizing via `docker system df` (storage `[ready]`).
12. Reconcile the root `@` retention anomaly (12-day-old weekly vs `snapshot_preserve 3d 1w`) and restate the root window (storage `[ready]`).
13. Codify the daemon-race policy (amend vs land-on-top + ONE-commit carve-out) (pipeline `[ready]`).
14. Fix the jan.md Architecture-table GPU row (truncated command + unclosed backtick) (services `[ready]`).
15. `/data/.Trash-1000` purge policy (storage `[decision]`, owner).
16. Gemma-4-31b-abliterated re-download: want it back + source URL + expected size (owner; recipe step-3 precondition; exists nowhere since 2026-09-12).
17. Borg passphrase recovery copy off-host and proven to decrypt (storage `[decision]`).
18. Offsite Borg go-live inputs: StorageBox host/user + irreplaceable exceptions (owner).
19. Offsite Borg real-repo restore drill (go-live checklist step 9).
20. Post-go-live restore-drill cadence (decision).
21. First-real-offsite-run archive-vs-exclude audit + runbook sizing (watch).
22. Borg real-repo drill verify semantics: live-`cmp` vs baked manifest (decision).
23. Offsite repo append-only / ransomware posture (decision).
24. Replacement-host `borg_known_hosts` re-pin derivation (decision).
25. `borgbackup-job-hetzner` in Zone-6 `ioChurnUnits`: include-with-re-arm or document-why-not (decision).
26. Make the drill's real-mode mountpoint gate fixture-reachable (storage).
27. Drop unknown flags in the borg-restore-drill fixture's borg stub (storage).
28. Re-verify live BTRFS root chunk-unalloc; cite metrics, not frozen figures (watch).
29. /data corruption repair P0: scrub gate (b) + T04–T08 (storage, blocked:user) — the recipe's parent P0.
30. btrbk-data EIO inode disposition: repair-by-delete vs temporary Borg exclude (nightly leg failing since 2026-08-18; the 2026-09-24 window raised formally gating Borg go-live on it) (owner + storage).
31. Phase-2 hot-DB migration waves (Samsung tier) + fold `crush-hot-db` into the `hot-db` module (owner windows).
32. `hot-db` + `crush-hot-db` VM tests green on the current tree (quiet-IO window).
33. T14 hot-db tier monitoring: mount-presence metric + Gatus (storage).
34. `migrate-hot-db.sh` stub-fixture test before its first user window (storage).
35. Phase-2 plan doc T-task table refresh (storage).
36. Samsung boot-mirror activation + first reboot (storage).
37. Pool+disk quality remainder: `/mnt/hot` scrub family, commit-option assertion, shadow dirs, SLO (storage).
38. Own-tools NVMe→pool remainder: browser-history DB leg (deploy-gated Phase-2 wave).
39. Free-space triage order doctrine (owner precedence) (storage, decision).
40. flm re-pull headroom gate threshold (owner) (storage, decision).
41. When the flm bump lands: verify old-weights pin clock + log projected extent-free date (storage, watch).
42. Post-Borg-go-live: offsite column in the snapshot-pinning doctrine table (storage, watch).
43. `/mnt/pool` root-fs shadow directory cleanup (storage, blocked:user).
44. Formatter-exclusion regression guard (effective treefmt excludes) (pipeline `[ready]`).
45. Commit-msg hook leg: reject subjects >72 chars (pipeline).
46. Status-report self-harvest §f at authoring time — codify the discipline (pipeline).
47. ffprobe-sweep row dedupe in pixel6.md (prior round).
48. ClickHouse native-BACKUP restore drill (storage).
49. Repo-wide hardcoded help-slicer ranges sweep (storage); DR-runbook provenance checklist line in CONTRIBUTING (storage).
50. llama-rag mid-load CPU-spin root-cause bisect upstream of llama.cpp (ROCm runtime / kernel / GPU-state; the `nixpkgs-llama-rag` pin stays until root-caused) (watch, noticed in AGENTS).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Who/what restored `/run/binfmt` at ~05:32 today** (manual `systemd-tmpfiles` heal per the AGENTS runbook? a parallel session? a nix-daemon restart side effect?), and **when does the durable fix land** — the gate's life currently depends on an unattributed, boot-ephemeral heal; the next reboot re-kills `/run/binfmt`, re-deads every task's verify, and restarts the no-op re-dispatch cycle this report documents.
2. **Gemma-4-31b-abliterated re-download:** do you want it back at all, and from which source (URL + expected size)? It has existed nowhere since the 2026-09-12 trash; the recipe's step 3 requires the source recorded in jan.md before any re-download can be validated and imported. I will not guess URLs.
3. **`/data/.Trash-1000` purge policy:** leave-until-manual (current de facto), size cap, or periodic empty — noting that emptying the trash is what STARTS the ~4-5 week /data snapshot pin clock (before that, extents stay pinned regardless). Queued as `[decision]`; data-retention policy is yours to pick.
