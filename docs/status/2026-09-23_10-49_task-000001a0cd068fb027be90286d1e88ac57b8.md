# Status Report — task 000001a0cd068fb027be90286d1e88ac57b8 (review fix: restore-drill --help truncation)

**Date:** 2026-09-23 10:49 CEST
**Scope:** Reviewer-finding fix ONLY (low severity): `--help` output truncated mid-options in `scripts/borg-restore-drill.sh`.
**Parent task:** 000001a0cc8af769f497a7204704871623f0 (offsite Borg restore path — runbook + first timed drill; already DONE 2026-09-23, see `docs/todo/storage.md` line 41).
**Work commit:** `27c081e4` (`fix: restore-drill --help no longer truncates before the trailing sections`)
**Diff size:** 1 file, 1 line replaced.

---

## a) FULLY DONE

| Item | Evidence |
| ---- | -------- |
| Reviewer finding fixed: usage printer no longer slices a hardcoded `sed -n '2,30p'` range | `scripts/borg-restore-drill.sh:53` — the anchor line `grep '^#' "$0" \| sed -n '2,30p' \| sed 's/^# \{0,1\}//'` is GONE from the tree, replaced by `awk 'NR == 1 { next } !/^#/ { exit } { sub(/^# ?/, ""); print }' "$0"` which derives the block end from the first non-comment line |
| Full help verified live (the reviewer's exact repro) | `--help` now prints through `--keep`, the Phases section, and the Exit-codes section; exit code 2 (the designed usage exit) |
| Same prefix-stripping semantics preserved | old `sed 's/^# \{0,1\}//'` ≡ new `sub(/^# ?/, "")` — byte-identical help body, no formatting drift |
| Syntax gate | `bash -n scripts/borg-restore-drill.sh` → clean |
| Pre-commit gates (repo's own) | gitleaks PASS, trailing-whitespace PASS, shellcheck PASS on the changed script, eval-only `nix flake check` PASS |
| Script's own functional gate re-run | `--selftest` PASS end-to-end (connect+resolve 233 ms, extract 238 ms, verify 7 ms, TOTAL 478 ms; record `~/.local/state/borg-restore-drill/drill-20260923-104354.log`) |
| Dangling-commit disposition followed | `git cat-file -e f845d1e9…` → EXISTS; anchor text located at the anchor (script line 53) and judged on the CURRENT tree |
| No stale-doc fallout | checked during report prep: `docs/services/offsite-borg-restore.md` never quotes the `--help` output or the old sed range — no doc drift from this fix |
| Queue hygiene | exactly ONE Task-Queue-ID footer (the FIX ticket's id) in commit `27c081e4`; no second footer for the parent task; nothing pushed |

## b) PARTIALLY DONE

Nothing partial from THIS fix task — the diff is complete and verified.

Partially-done state that this fix *touches but does not own* (parent-task follow-ups, already queued):

- **Drill verification story** is real-mode-partial: selftest + local stand-in proven; the REAL-repo drill is deliberately parked as go-live checklist step 9 (`docs/services/offsite-borg.md`) pending owner-held inputs (StorageBox hostname/username, host-key pin).
- **Help-completeness is verified by eyeball + one live run**, not by a machine-checked assertion — the drill's fixture test (`docs/todo/storage.md` line 46) does not exist yet.

## c) NOT STARTED

- **Regression assertion for help completeness** — nothing pins "the usage block must contain `--keep` / `Phases` / `Exit codes:`" so this exact truncation class cannot silently return. Candidate to fold into the storage.md:46 flake fixture item (PATH-stub borg, forgejo-scripts-fixture pattern).
- **Repo-wide sweep for the same fragile idiom** (other scripts deriving help from a hardcoded `sed -n '2,Np'` range) — deliberately NOT done in this run (finding said fix EXACTLY this, no unrelated changes). Listed in §f.
- Everything else on the offsite-Borg go-live ladder (real run, smoke validation, quota observation) — untouched by design; owned by the queued go-live items, not this fix.

## d) TOTALLY FUCKED UP

Nothing. Tree clean at `27c081e4`, all gates green, single-line diff, no scope creep, no doc drift.

Two process blemishes worth recording honestly (neither damaged the tree):

1. **Edit-before-read rejection:** my first `edit` call was rejected by the tool ("must read the file before editing") because I had only `grep`ed the anchor, not `view`ed it. Cost: one wasted round-trip, zero impact. AGENTS.md/Crush rule is read-before-edit; I should have viewed first.
2. **The bug this fix addresses was originally MY implementation's bug** (the rejected commit `f845d1e9` was this task's own earlier work): the help printer was written with a hardcoded range and "verified" without actually running `--help` through the changed header assumptions. The reviewer found in one run what should have been caught in-session. Lesson below in §e.

## e) WHAT WE SHOULD IMPROVE

1. **Run the artifact, never trust the print path.** The truncation survived because the help text and the slicer were edited in different sessions/commits and nobody re-ran `--help` after the header grew past line 30. The fix session's own verification (running the actual command, comparing against the reviewer's repro) caught nothing new — because it ran AFTER the fix. In-session verification must include executing the thing whose output a reviewer will see.
2. **Derive, don't hardcode, structural ranges.** Any `sed -n '2,Np'`-style slice over a file section that can grow is a latent truncation. The awk idiom now in `usage()` (print comment lines until the first non-comment line) is robust to header edits; it should become the house pattern for script help printers.
3. **Machine-check human surfaces.** `--help` output has no test. A one-line assertion ("output contains `Exit codes:` and `--keep`") inside the already-planned drill fixture test would have made this finding impossible. Verification gates should cover exactly the artifacts reviewers consume.
4. **Read-before-edit, every time, even one-liners.** The rejected edit cost a round-trip and would have been a rule violation in a stricter harness. Grep locates; view authorizes.
5. **Review-finding disposition discipline worked — keep it.** Verifying the dangling commit exists (`cat-file -e`), locating the verbatim anchor, judging the CURRENT tree: this avoided both a stale-SHA wild goose chase and inventing a change if the anchor were gone. Worth keeping as the standard opening move for review-fix tasks.
6. **Scope discipline held but created known debt.** The minimal-scope contract was correct, but it means the same-idiom sweep and the help assertion are now open items — they must land via the queue, not drive-by.

## f) Up to 50 things we should get done next

Scoped to this task's domain (restore drill + offsite Borg leg + the untested-restore class it exists for). Items marked **[queued]** already have a queue row — listed here for completeness of THIS report's picture, not as new asks; the rest are harvest candidates (most are ROADMAP fuel). A longer list would be padding outside the domain this task touched.

**Drill script hardening (most already queued as storage.md:44/46):**

1. [queued] Real-mode `/mnt/hot` mountpoint gate — unmounted Samsung ⇒ borg cache `mkdir`s under the mountpoint onto the root fs (shadow-dir class).
2. [queued] Write a FAIL record even on early death — record setup sits after the env checks, so the likeliest real-world failures leave no record.
3. [queued] Resolve borg from THIS flake's locked nixpkgs (drill binary == job binary).
4. [queued] Optional `borg extract -n --verify-data` deep-integrity mode.
5. [queued] Drill-record retention note (tiny logs, unbounded count under `~/.local/state/borg-restore-drill/`).
6. [queued] Flake fixture test for the drill (PATH-stub borg): root-gate message, bogus `--local` die, placeholder-repo tripwire, missing-mountpoint die, happy-path PASS.
7. **[new harvest]** Fold a help-completeness assertion into item 6: `--help` must contain `--keep`, `Phases`, `Exit codes:` — pins THIS fix permanently.
8. **[new harvest]** Repo-wide sweep for other hardcoded help-slicer ranges (`sed -n '2,<N>p'` over comment headers) in `scripts/` — same truncation class, unknown blast radius.
9. **[new harvest]** Canonize the derive-from-first-non-comment awk idiom in `docs/CONTRIBUTING.md` script guidance so future help printers don't reintroduce the class.
10. [queued] Pin the drill's env path to the sops template (flake-wrap or eval-time literal assertion) — TODO_LIST line 27.

**Offsite Borg go-live ladder (owner-gated; the drill's real-repo mode rides these):**

11. Owner-held go-live inputs: StorageBox hostname/username (docs/todo/storage.md row).
12. Host-key pin: `ssh-keyscan -p 23` → sops `borg_known_hosts` (PLACEHOLDER = fail-closed today).
13. Owner recovery copy of the Borg repokey passphrase (policy deliberately open).
14. Flip `services.offsite-borg.enable = true` + first real nightly run.
15. Real-repo drill execution = go-live checklist step 9 (first true RTO numbers over the StorageBox SSH/WAN leg; local stand-in numbers — 233/238/7 ms this run — measure nothing of the WAN path).
16. Observe first-run duration vs `TimeoutStartSec=2d`; record WAN-seed evidence.
17. Verify the exclude set keeps the 1 TB BX11 quota headroom after the first real run.
18. Verify `.last_success` → `backup_healthy{backup="offsite-borg"}` → aggregate "All Backups Healthy" row goes green on the first success.
19. Verify the go-live tripwire LIVE once (PLACEHOLDER `BORG_REPO` ⇒ job fails with the checklist pointer) — currently only eval/smoke-shaped.
20. Validate pre-deploy §13 + post-deploy §16 smoke entries on the actual go-live deploy.
21. Verify prune (7d/4w/6m) behaves on StorageBox (quota accounting + prune exit codes over the WAN).
22. Monitor `/mnt/hot/borg` cache growth (multi-GB borg cache parked on the Samsung tier by design).
23. Confirm the `ioTier.background` mkForce actually applies to the running borgbackup job unit post-deploy (idle-class starvation was the reason it was forced).
24. Confirm `backup_ever_succeeded{backup="offsite-borg"}` distinguishes never-worked from stale after the first success.
25. [decision, queued] Post-go-live drill cadence: scheduled timer + OnFailure vs manual-only (storage.md:52).

**Restore-path design debt (same "untested restore is Schrödinger's backup" class):**

26. Cross-machine restore variant: current byte-verify does `cmp vs live files`, which is meaningless on a dead host — add a manifest-based verify mode (or document the live-cmp limitation) in the runbook.
27. [queued] Runbook accuracy pass: stale-lock recovery (`borg break-lock`), pre-staged OnFailure Discord delivery proof, block-by-block env sweep of BOTH borg runbooks against `sops.nix`/`backup.nix`, verify the `borg mount` FUSE path once (storage.md:45).
28. Drill selftest cleanup trap coverage: confirm the `/tmp` scratch is removed on FAILURE paths too, not only success (fixture-testable in item 6).
29. Machine-readable drill records (the record file is human log format today; a timings JSON line would let RTO trends be graphed later) — ROADMAP fuel.
30. [queued, adjacent] Twenty/manifest pg_dump restore drills (storage.md:28 item 13) — the same drill doctrine applied to the Docker-app dumps; listed because this task's whole premise is "dumps nobody restored are hopes, not backups".

Stopped at 30: the remaining storage-domain queue rows (data-corruption repair, hot-db waves, scrub family) are already faithfully queued and out of this task's passing-observation radius; padding this list to 50 would duplicate the queue without adding signal.

## g) Questions I can NOT figure out myself (max 3)

1. **Verify semantics for the real-repo drill (go-live step 9):** when restoring from the offsite repo, should the extracted bytes be compared against the LIVE evo-x2 files (current `cmp` design — only valid same-host) or against a manifest baked into the archive (proves the cross-machine/dead-host restore, which is the actual disaster case)? This changes the drill's design before the first real run, and it is an owner call on what "proven restore" must mean.
2. **Are drill records canonical RTO history or ephemeral diagnostics?** If `~/.local/state/borg-restore-drill/` logs are meant to be kept and referenced in the DR runbook (RTO trend evidence), they need retention/archival wiring (and maybe pool placement); if ephemeral, a simple prune note. I cannot infer the intent from the repo.
3. **Scheduling expectation for the first WAN seed:** `TimeoutStartSec=2d` implies the initial 1 TB-class upload may run past a nightly window. Do you want the seed kicked off in a specific quiet window (weekend / no-agent-session evening) at go-live, or is racing into the next day acceptable? This determines whether go-live needs a calendar entry or just the enable flip.

---

**Report format note:** written as `.md` per explicit instruction in the tasking message — the status-report skill's canonical default is a styled HTML dashboard; the user's explicit format choice wins for this run (one-off override, not propagated).

*Task-Queue-ID lineage: fix ticket `000001a0cd068fb027be90286d1e88ac57b8` descends from the review of parent task `000001a0cc8af769f497a7204704871623f0`.*
