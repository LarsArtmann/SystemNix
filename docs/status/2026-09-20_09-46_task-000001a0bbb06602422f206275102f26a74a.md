# Task 000001a0bbb06602422f206275102f26a74a — `services.hot-db` NixOS module (Samsung hot-DB tier, Phase 2 mechanism)

**Date:** 2026-09-20 09:46 CEST · **Agent session scope:** close-out + verification round of the `services.hot-db` task-queue item · **Branch:** master (unpushed, per contract)

---

## 0. What this task was

TODO_LIST storage section: "**`services.hot-db` NixOS module — the declarative mechanism for the Samsung hot-DB tier (Phase 2 of the ratified design)**" (plan doc `docs/planning/2026-09-14_13-27_SAMSUNG-PHASE2-HOT-DB-NATIVE-PARETO-PLAN.md`, tasks T3/T4/T7). The deliverable: a dormant NixOS module that mounts per-service BTRFS subvolumes from the Samsung TLC pool AT each service's existing dataDir, with nodatacow support, a pre-mount bootstrap oneshot, eval-time landmine assertions (btrbk must never see the hot tier), and anti-shadow consumer wiring.

## 1. State found at session start (IMPORTANT context)

When this session started (~09:28), the engineering was **already landed in-tree by an earlier run of the same queue task** (commits 438e7287 → 301ac271 → 5f04359c → 25945aa5, 08:xx–09:25, auto-commit-daemon messages) plus the TODO_LIST/docs/todo/storage.md item pruning and the CHANGELOG entry (commit af372dca, 09:27, same pruning batch). This session therefore had a different job than the queue text implied: **verify the landed state end-to-end and produce the missing footer-carrying commit** (no commit in the lineage carried `Task-Queue-ID: 000001a0bbb…`; the CHANGELOG entry cites it in prose only).

## a) FULLY DONE

1. **Module** (`modules/nixos/services/hot-db.nix`, 335 lines): `services.hot-db.{enable,device,toplevelMount,entries}` — per-entry generated `fileSystems` mount (`subvol=hot/<name>` on `/dev/disk/by-label/tlc`, `noatime nodiscard space_cache=v2 nofail`, `nodatacow` opt-in) AT the service dataDir; service configs untouched.
2. **Bootstrap oneshot** (`hot-db-bootstrap`): idempotent `btrfs subvolume create` + `chattr +C` per non-CoW entry, ordered via the entry mounts' Wants/Before (NOT `before local-fs.target` — ordering-cycle trap documented in-module), `ConditionPathIsMountPoint` on the toplevel, Wants-not-Requires so a detached Samsung degrades to consumer failure, never a dead boot.
3. **Eval-time guards**: btrbk LANDMINE assertion (recursively scans every btrbk settings string AND attr name for `hot/` or entry paths — one scheduled snapshot silently reverts nodatacow to CoW); duplicate-path / absolute-path / under-`/hot` validation as ALWAYS-ON `config.assertions`; unmanaged-entry WARNING; `systemd-escape --path`-correct mount unit names (`utils.escapeSystemdPath` — a dash in any dataDir would otherwise void the bootstrap ordering against the `\x2d`-escaped fstab unit).
4. **Anti-shadow consumer wiring**: `RequiresMountsFor` + `ConditionPathIsMountPoint` (mkAfter) on each declared `unit`/`extraUnits`, keyed by suffix-stripped unit names; entries-with-no-consumer WARN.
5. **Migration tooling**: `scripts/migrate-hot-db.sh prepare|finalize` (pressure gate, stop → ionice-idle rsync stage → deploy → drain staging → restart; clickhouse precedent) + flake app `migrate-hot-db`.
6. **Tests** (both GREEN, rebuilt FRESH this session at 09:33–09:35): `tests/test-hot-db.nix` VM test on a real btrfs disk (bootstrap-before-mount ordering, nodatacow EFFECT via inherited `+C` file flags — not `/proc/mounts`, which btrfs ≥6.x omits the option from; consumer write-through; idempotent re-bootstrap; anti-shadow condition-skip with zero shadow writes) and `tests/test-hot-db-assertions.nix` (8 negative/happy cases, pure eval).
7. **evo-x2 toplevel eval clean** (09:35, `/nix/store/940879f4…-nixos-system-evo-x2-…`); module confirmed DORMANT on the host as designed: `services.hot-db.enable = false`, `entries = {}`.
8. **Verification recorded + footer commit landed**: `c9dc44a6` (09:43) — CHANGELOG entry updated with the re-verification, commit message carries `Task-Queue-ID: 000001a0bbb06602422f206275102f26a74a`; pre-commit gitleaks + eval-only `nix flake check` passed. Full `nix flake check --no-build` re-run green at 09:44 ("all checks passed").
9. **Queue loop closed**: item was pruned from TODO_LIST.md + docs/todo/storage.md by af372dca per the file's own conventions (prune, never keep `[x]`); the follow-on waves remain correctly gated behind `[ready]` "Phase 2: hot DBs off the QLC root" (pocket-id → postgres → forgejo → dnsblockd/papdashboard).

## b) PARTIALLY DONE

1. **The whole Phase-2 rollout is partial BY DESIGN**: the mechanism ships dormant with ZERO entries; every actual migration wave (crush, pocket-id, postgres/immich/paperless, forgejo, docker data-root, dnsblockd/papdashboard) is still future owner-gated work. Nothing is live on evo-x2 from this module.
2. **The `crush-hot-db` interim module is NOT folded in** (storage.md `[watch]` item): it stays deployed at generation `bbb931a8` running beside the dormant Phase-2 module until its first ~42 GiB migration completes; the fold (delete interim module + deploy.sh entry, narrow ReadWritePaths) is a separate change. Two mechanisms for the same tier coexist in-tree right now — safe (one dormant) but documented debt.
3. **Deploy-restart-audit exemption defect in this task's lineage** — the module's `allowUnits = [ "hot-db-bootstrap" ]` plain assignment REPLACED the option's four upstream-plumbing exemptions; a parallel session fixed it at the config layer in `50f73871` (09:36, minutes after my green toplevel eval at 09:35 — `82d4fa4a` only carried the commit MESSAGE claiming the fix, its diff is docs-only). My re-verification pass therefore certified the toplevel eval one fix short of the final state — the 09:43 footer commit's pre-commit `nix flake check` did cover the final state, but my explicit CHANGELOG re-verification sentence was written before the last eval-layer fix landed. The claim is still true (evals were green at the time I ran them), but the sequencing was sloppy.
4. **`migrate-hot-db.sh` is fixture-untested in this session**: I did not run the script's prepare/finalize paths against a stub environment (the clickhouse precedent script it copies has its own history, but the hot-db variant was not re-exercised here). First real use is a user migration window.

## c) NOT STARTED (correctly out of scope, listed for completeness)

1. Any production entry enablement (all waves).
2. The fsync-pain measurement round (gatus/discordsync/browser-history/inboxclean/bank-sync) that decides who joins the tier (storage.md Phase-2 item).
3. Docker data-root move to the hot tier.
4. Monitoring wiring (T14: textfile mount-presence metric, Gatus checks, Samsung joins autoScrub/smartd) — module-level monitoring for the hot tier specifically; the Samsung disk itself is partially covered by nvme.prom/pool-smart but `/mnt/hot` mount presence has no dedicated check yet (crush-hot-db's nofail mount degrades silently by design).
5. T18 design-doc Rev-3 amendment refresh / doc harvest pass reflecting the landed module (AGENTS.md bullet exists and is current as of 2026-09-20; the plan doc's T-task table is not status-updated).
6. Any deploy (the tree is several commits ahead of the deployed generation; nothing in this task requires one).

## d) TOTALLY FUCKED UP (honest list)

1. **Nothing was destroyed or left broken.** But three near-misses / process failures worth recording:
2. **The queue's footer discipline failed for ~5 hours across the whole task lineage**: the earlier session(s) landed all the engineering but NO commit carried the footer until this session's `c9dc44a6`. Had this session not existed (or misread the situation as "already done, nothing to do"), the queue would never cross-reference the work. The task contract explicitly makes the footer the cross-reference mechanism.
3. **The prior session's CHANGELOG verification claim was written BEFORE its last test fix**: the CHANGELOG entry (af372dca, 09:27) asserts "VM test GREEN" while the final anti-shadow test rewrite (mount-stop + dependency-free consumer start to isolate the ConditionPathIsMountPoint layer) was still being iterated (25945aa5 09:24, af372dca 09:27 itself). The claim happened to hold, but "verified" was asserted on a test that was being rewritten in the same commit batch — exactly the "assert WHICH question your evidence answers" trap.
4. **My own re-verification certified a moving tree**: between my 09:35 toplevel eval and my 09:43 commit, a parallel session landed an eval-layer fix (82d4fa4a) for a defect THIS task's module introduced. I noticed it only because I checked the recent-commit log while writing this report — not because my verification design would have caught it. A close-out should snapshot the tree rev it verified (`git rev-parse HEAD`) and re-confirm nothing landed under it before declaring done. Also in passing: the queue-ID hex in commit messages appears to have tripped a gitleaks bare-hex false positive that the parallel session had to reword in 82d4fa4a — recurring cost of the footer format worth solving once in the scanner.

## e) WHAT WE SHOULD IMPROVE

1. **Footer-commit discipline**: the FIRST commit of a queue task should carry the footer, not the last. Amend-forward (or a tiny follow-up) is the recovery, but the daemon-raced amend-into-foreign-commit trap (2026-09-14 lesson) makes late footers risky — this session could not amend because the daemon had already stacked a foreign file on HEAD.
2. **Verification snapshots**: record `git rev-parse HEAD` inside any "verified" CHANGELOG/status claim so a later reader can tell exactly which tree the green applies to (I added the re-verification sentence but without a rev pin — would do it with one next time).
3. **Re-verify at commit time, not just at work time**: when a session's work spans >15 min on a shared tree, re-run the cheapest eval gate (`nix flake check --no-build`) immediately before the closing commit, not only at the start. The pre-commit hook saved me here by accident.
4. **The hot-db module should grow its own `path`-liveness metric + Gatus check when the first entry lands** (mount-present textfile, fail-closed) — currently the tier's failure mode (Samsung detached → consumers condition-skip) is silent by design and only visible as dead services.
5. **Fold plan for crush-hot-db should be written down as a checklist now** (while context is fresh): interim module delete + deploy.sh entry delete + ReadWritePaths narrowing + `crush.projects` attrset mapping — all in ONE change, never running both mechanisms live.
6. **`migrate-hot-db.sh` deserves a stub-fixture test** in `tests/` (PATH-injected mocks, forgejo-scripts pattern) before its first user-run window — an untested destructive script is the worst possible first contact.
7. **Gitleaks vs 40-hex queue IDs**: add a scoped allowlist or scanner lookahead for the `Task-Queue-ID:` line so every future footer commit doesn't need a manual FP reword.

## f) Up to 50 things to get done next (scoped to this task's domain; pacing via the queue)

**Wave enablement (owner-gated, in ratified order):**
1. Measure fsync pain on gatus/discordsync/browser-history/inboxclean/bank-sync SQLite dataDirs to decide tier membership (storage.md Phase-2 item).
2. Enable the first non-crush entry (pocket-id) in a user migration window via `migrate-hot-db.sh prepare|finalize`.
3. Postgres wave: immich + paperless DB dataDirs onto `hot/` subvols (CoW ON — integrity over latency, per entry `cow` semantics).
4. Forgejo dataDir wave.
5. dnsblockd + papdashboard wave.
6. Decide gatus `cow = true` (current design intent: integrity over latency) — confirm with a live fsync measurement, not intuition.
7. Docker data-root (`/data/docker`) move evaluation — separate decision: it is on `/data`, not QLC-root; may not need the hot tier at all.
8. `chattr +C` decision for `/mnt/hot/crush` dirs at fold time (storage.md `[decision]` item).

**Crush fold (one atomic change):**
9. Fold `crush-hot-db` into `services.hot-db.entries.crush.*` (tmpfiles L+ design per the plan doc's T5).
10. Delete interim module + its deploy.sh provisioner entry in the same change.
11. Narrow `ReadWritePaths` back to per-project dirs at fold time.
12. Map `crush.projects` attrset (276 dirs, nested depth 2).
13. Kill the first crush-hot-db migration blocker: owner staging decision (one-pass vs staged vs ioChurnUnits).
14. Run the first crush migration in a no-crush-session window.
15. PSI before/after measurement (`node_psi_io_some_avg60` vs the 40-60% baseline) after first crush wave.

**crush-hot-db hardening (existing `[ready]` items, unaffected by the fold):**
16. `onFailure` Discord route + `system-health.monitoredServices` entry for `crush-hot-db-migrate`; non-zero exit on per-dir failures.
17. Depth-cap tripwire (`.crush` deeper than maxdepth 3 → log/metric).
18. Per-project open-fd liveness guard (crush.db) so one idle session doesn't block all 276 dirs.
19. btrbk-does-not-snapshot-`/mnt/hot` confirmation (crush interim toplevel mount).
20. `archived/*` eager-vs-later migration decision (owner).

**hot-db module gaps:**
21. T14 monitoring wiring: hot-mount-presence textfile metric (fail-closed) + Gatus checks.
22. Samsung joins autoScrub + smartd coverage for `/mnt/hot` (partially covered by nvme.prom; mount-level check missing).
23. btrfs-health metrics coverage for the tlc filesystem (space/chunk health — currently invisible).
24. `migrate-hot-db.sh` stub-fixture test in `tests/`.
25. Eval-time assertion: hot entry paths must not collide with any OTHER service's dataDir without the `unit` wiring (unmanaged-entry WARN exists; consider promoting to assertion for enabled entries).
26. Consider `space_cache=v2` validation — confirm it's actually set on the tlv toplevel already (mixing v1/v2 across one FS is a mount-option footgun).
27. Bootstrap unit: add `MemoryMax` + explicit `TimeoutStartSec` (subvolume creates are fast, but the 3min global default is a wedge class on IO storms).
28. Capacity guard: eval-time or metric-level warning when `hot/` subvols approach Samsung capacity (the disk also hosts `/nix`).

**Verification debt from this close-out:**
29. Pin a tree rev into the CHANGELOG re-verification sentence (one-word edit, next touch).
30. Confirm post-82d4fa4a that the four upstream deploy-restart-audit exemptions still pass their negative tests (the fixer's own commit message asserts it; an independent re-run of the relevant check is cheap insurance).
31. Run the full `nix build .#checks.x86_64-linux.hot-db` once more at a quiescent moment after the next tree churn batch (moving-tree rule).

**Adjacent noticed-in-passing:**
32. The gitleaks FP reword for hex footer IDs (item 7 above) — scanner-side fix.
33. `/boot-mirror` activation + confirming reboot (separate deployed-but-inert work landed 2026-09-19, blocking the hot-tier deploy sequencing question).
34. The geometrikks status doc sitting staged-uncommitted at session end (parallel session's file — left untouched deliberately; owner should land or discard it).
35. llama-rag re-enable soak (`scripts/llama-rag-soak.sh`) — still disabled after freeze #5; unrelated but the biggest pending capability gap on the box.
36. `/data` corruption repair chain (T04→T09) — unchanged, still the top storage blocker.
37. Offsite Borg leg — unchanged, not started.
38. `services.hot-db` entries should get backup-coverage declarations when waves land (a nodatacow hot subvol is deliberately NOT btrbk-snapshotted — what restores each service's hot data? per-service dump jobs like pocket-id-backup need explicit registration review per wave).
39. Document the restore story per wave in `docs/services/<svc>.md` at wave time (hot tier = faster + less integrity; backups must compensate).
40. Rollback runbook for a hot entry (subvol → plain dir) — write it once, reference per wave.
41. Add `migrate-hot-db.sh` "verify" sub-step after finalize (consumer health + fsync spot-check) so the window closes on evidence.
42. Decide whether `services.hot-db.toplevelMount` should hard-require the hardware-configuration `/mnt/hot` entry at eval (cross-module assertion; currently trust-by-convention).
43. Samsung free-space visibility in the Phase-2 planning: `/nix` (~68G compressed) + hot subvols share one 1TB disk — size each wave before enabling.
44. Post-first-wave: update AGENTS.md hot-db section from "DORMANT" to deployed state (watch item exists for crush; same applies to hot-db).
45. Consider `Priority`/`ioTier` on `hot-db-bootstrap` (BFQ BE/6) — subvol creates are metadata-only but the box's IO doctrine says classify every unit.
46. Add hot-db entries to `backup-coordination` review per wave (some hot DBs may want MORE frequent dumps than their old QLC-era cadence, since the tier is unsnapshotted).
47. Sweep `pre-deploy-check.sh` for a hot-tier section once the first entry lands (subvol existence, mount state, fsync smoke).
48. Sweep `post-deploy-check.sh` likewise (consumer write-through probe per enabled entry).
49. Update the plan doc T-task table with completion status (T3/T4/T6/T7/T8 done; T5/T9-T15 pending) so the plan stops lying about progress.
50. Question-gated: if the queue produces more footer-only close-out commits, consider a `Task-Queue-ID:` allowlist pattern in `.gitleaksignore` (scoped to commit-message context — may not be expressible; see question 2).

## g) Questions I cannot answer myself

1. **Sequencing:** should the first crush-hot-db migration run BEFORE the Phase-2 fold (interim mechanism proves itself live first), or should we fold first and run the first-ever migration through `services.hot-db` directly (one mechanism, untested end-to-end live)? The storage.md items currently imply run-then-fold; I could not find an explicit owner decision.
2. **Gitleaks vs footer IDs:** commit-message-only 40-hex strings (every Task-Queue-ID footer) appear to trip a gitleaks bare-hex rule intermittently (the parallel session reworded one at 82d4fa4a). Is the intended fix a `.gitleaksignore` entry, a scanner lookahead, or should I leave footers as-is and eat the FPs? I could not reproduce the exact trip deterministically from this session.
3. **CoW defaults per wave:** the module defaults `cow = true` and the design says gatus stays CoW; is there an owner-ratified list of which future entries get `cow = false` (pocket-id? postgres? forgejo?) or is that decided per-wave via the fsync measurements? The plan doc says "nodatacow doctrine" but I found no per-service ratification.
