# Task Status Report — 000001a0d5f3f62584a64bc4cb7a6d1f8f83 (third dispatch)

**Date:** 2026-09-25 03:24 CEST · **Scope:** this task's work + what was noticed in passing (no unrelated research)
**Item:** "Fixture-negative for `borg-restore-drill-fixture`: mutate a drill copy (e.g. drop the root gate) and assert the fixture FAILS" (TODO_LIST storage section / docs/todo/storage.md)

**Dispatch lineage for this task ID:** (1) an earlier queue run implemented fixture case 8 and was crashed/orphaned into a heuristic daemon commit (no footer, no closure); (2) the 03-01 dispatch verified the code, closed the TODO rows, and put the work under a footer-bearing commit — but left the execution gap open (the machine-wide `/run/binfmt` sandbox breakage blocked every `nix build`); (3) **this dispatch** executed the full fixture harness standalone against the real script, narrowed the build-blocker root cause, cleaned up a junk file its own replay created, and completed the docs loop.

---

## a) FULLY DONE

1. **Prior dispatch's closure independently verified before acting.** Both closure commits (a55a9e7c, 03-01 report) carry the exact `Task-Queue-ID: 000001a0d5f3…` footer; TODO_LIST.md:42 and docs/todo/storage.md:117 are `[x]` with close-out notes; fixture case 8 (`mutation-negative`) is present in flake.nix's `checks.x86_64-linux.borg-restore-drill-fixture` with its three assertions (rc inversion, root-gate message ABSENT, env-file message PRESENT) and the sed-anchor rot guard. Without this check this dispatch would have re-executed a done item (the 03-01 report's own §d3 near-miss, one level deeper).
2. **Full-harness execution: all 8 runtime cases GREEN (rc 0), standalone, against the real committed `scripts/borg-restore-drill.sh`.** Method: render the fixture's build script out of flake.nix (dedent the 20-col Nix indent, strip `''`-string escapes, point the two store interpolations — `${pkgs.bash}` stub shebangs and the `cp ${./scripts/...}` source — at system equivalents), `bash` it with `out` exported. PASS marker: `root-gate, bogus-local, no-config, placeholder, secrets-missing, happy-local, verify-data, env-pin-drift, mutation-negative`. This is the 03-01 report's §e4 "maximal version" and answers its §f2: case 8 now has **in-harness execution proof**, not just the prior dispatch's hand replay of the one case — and cases 1–7 were re-proven against the CURRENT (hardened, mountpoint-gate) drill rather than the pre-hardening build from 2026-09-24.
3. **Eval-time env-pin-drift guards re-proven:** `nix eval .#checks.x86_64-linux.borg-restore-drill-fixture.drvPath` green — the `deepSeq pinGuards` block (control / script-drift / template-path override + anchor-rot throw) evaluates before `derivationStrict`, so a green drvPath proves all guards passed.
4. **Build blocker root cause narrowed one step further** (beyond the prior dispatch's §b3): `/run/binfmt` sits in the daemon's **BASE** `sandbox-paths` (`nix show-config`), not only `extra-sandbox-paths` — so an `--option extra-sandbox-paths …` client override is ineffective even conceptually; and `trusted-users = root` closes the non-trusted-client override path entirely. The only fixes are root-side (`mkdir -p /run/binfmt`, reboot-ephemeral since /run is tmpfs) or a reboot. My tooling can do neither (`sudo` and `systemctl` blocked).
5. **Junk `--help` removed; push-protection pre-commit leg restored to passing.** The prior dispatch removed a junk root file named `--help` hours ago (a55a9e7c, "blocking commits repo-wide"); the daemon re-committed a NEW one (26ea9fc7) — see §d1 for the embarrassing part: this one was MY replay's artifact. After removal, the push-protection audit leg PASSES (gitleaks leg passed both times; the flake leg remains dead on `/run/binfmt`, unrelated to file contents — no staged `.nix` files).
6. **Docs loop completed:** the 03-01 report's §b1 annotated inline (per docs-health ANNOTATE convention) with the execution evidence and the narrowed root cause; CHANGELOG bullet added for case 8 (the 03-01 dispatch closed the TODO rows but never logged the fixture addition); AGENTS.md offsite-borg paragraph extended with the mutation-negative case + the standalone-replay evidence.
7. **Recurrence queued:** the stub flag-handling weakness that produced the junk file is now a `[ready]` row in both TODO_LIST.md (storage section) and docs/todo/storage.md (no-drift rule, edited together).
8. **Honesty repair:** the 03-01 report's §b1 claim "UNKNOWN whether the fixture derivation has EVER run green" was wrong — the 7-case-era fixture DID build PASS on 2026-09-24 (store path `bdz4gkd…`, docs/status/2026-09-24_01-27_…md). I had that evidence in view during verification and missed it in my first annotation pass; the inline CORRECTION is included in this commit (see §d3).
9. **All landed as one footer-bearing commit; tree clean; nothing pushed.**

## b) PARTIALLY DONE

1. **The literal `nix build .#checks.x86_64-linux.borg-restore-drill-fixture` has still never realized the case-8-era drv.** Behavioral proof is complete (a2); derivation-execution proof is not. After the `/run/binfmt` host fix this is a one-command close — it is the ONLY remaining verification gap for this item.
2. **The standalone replay proves behavior, not Nix wiring.** The replay executes the fixture's script logic faithfully, but cannot prove the sandbox-level facts the derivation would: `nativeBuildInputs` resolution, the build user's env, `out` pointing at the derivation output, sandbox PATH construction. Those remain eval-argued (drv-level) only until a real build runs. A reviewer should treat a2 as strong-not-complete evidence.
3. **The stub flag-handling fix is queued, not implemented** — deliberately: the queue item's contract is the mutation-negative verification, and the stub guard is a distinct (small) change that deserves its own dispatch with its own fixture-negative run.

## c) NOT STARTED (owned elsewhere / queued; only what this run noticed)

1. The `/run/binfmt` host fix itself (root `mkdir -p /run/binfmt`, tmpfiles rule, or reboot) — the box's #1 blocker since 2026-09-25 early morning; owned by whoever holds sudo. Every sandboxed build, the pre-commit flake leg, `negative-test-lints.sh`, and VM tests are dead until then.
2. Mutation-negative coverage for the repo's OTHER behavioral script fixtures — browser-history-probe-purge, forgejo-scripts-fixture, migrate-forgejo-subvol-fixture (03-01 report §c3/f4; doctrine extension, nothing queued yet).
3. The storage-domain rows seen in passing while working in docs/todo/storage.md — single-victim /data repair recipe, /data damage-set inventory, `docker system df` sizing refresh, root `@` retention anomaly, flm↔snapshot-pinning cross-link — all already queued, none touched (scope discipline).
4. TODO row 43 (pipeline): `checks.disko-layout` eval failure — same breakage family as the sandbox death but a distinct shape, pipeline-owned.
5. Owner decision whether `boot.binfmt.emulatedSystems` is earning its keep (the sandbox-paths bind is the failure surface; if no aarch64 builds happen outside rare VM tests, dropping it removes the class).

## d) TOTALLY FUCKED UP

1. **My replay polluted the repo and re-broke commits repo-wide — the second occurrence of this exact class in hours, and this one is MINE.** Chain: I invoked the rendered harness from the repo ROOT → the drill's borg2-capability probe (`borg extract --help`, part of the `--verify-data` path) hit the fixture stub's `*)` catch-all → the stub treated `--help` as an extraction PATH and wrote a junk file named `--help` into its CWD → the auto-commit daemon committed it (26ea9fc7, 03:15) → the push-protection pre-commit leg failed for every hook-respecting committer until I removed it in my task commit. The scratch-cwd discipline existed and I didn't apply it: one `cd "$(mktemp -d)"` before the replay prevents the entire chain. The prior dispatch removed the previous `--help` with an explicit "this was blocking commits repo-wide" note — I read that note hours before recreating the file.
2. **Three rendering iterations for the standalone replay, each failure knowable up front.** (i) Missed that nix's `''`-string escape also strips quotes before a bare `$` — the harness `run()` line `"''$@"` rendered in my copy as literal `''` + `"$@"`, handing the drill a two-quote argument that hit the usage branch (rc 2); (ii) missed that NixOS has no `/bin/bash` — my stub shebang substitution pointed there and every stub invocation died rc 126 "bad interpreter"; (iii) only the third render ran. ~10 minutes and two failed runs spent on escape rules I should have derived before the first attempt.
3. **Annotation without claim-auditing.** I appended execution evidence to the 03-01 report's §b1 without checking the truth of the sentences around it — including the factually-wrong "EVER run green" claim (a2 of THIS report). Caught it while writing THIS report; the inline CORRECTION rides the same commit. That is exactly the repo's "verify-external-claims" doctrine applied to my own neighbor text, and I skipped it once.
4. **Amend pathspec fumble:** re-ran the amend including `--help` as a pathspec after the file was already deleted — git rejects pathspecs for absent files; one wasted retry. Trivial, but it is the kind of thing that hits harder when the tree is shared.
5. **Observed, not mine, but the asymmetry got worse:** the daemon committed the junk file through NO gate at 03:15 while my hook-gated commits were being blocked — first by that very file's presence, then by the dead flake leg. The gate gates only the committers that respect it (03-01 report §d2, re-confirmed live).

## e) WHAT WE SHOULD IMPROVE

1. **Persist the sandbox-down verification recipe.** The render-extract-and-run pipeline (dedent N indent → strip `''` escapes → substitute store interpolations → export `out` → scratch-cwd run) turned a dead build sandbox into full behavioral proof — but it exists only in this report's prose and cost three fumbles to get right. A small documented script (or an AGENTS.md snippet) means the next sandbox outage doesn't re-derive it, and reviewers get a standard form of "harness executed standalone" evidence.
2. **Scratch-cwd doctrine for ANY local harness/fixture execution — one AGENTS.md line.** Fixtures write files relative to CWD (that is their job); repo roots are the worst possible CWD for that. This is the structural fix for §d1's class; the queued stub fix is only the local fix.
3. **Push-protection audit invocation robustness (two stacked bugs, visible in this run's hook output):** the audit passed the junk filename to grep unquoted-after-dashes, so grep parsed `--help` as a FLAG — printing its usage text and exiting 2 — and the audit treated exit-2 as a MATCH for every pattern in its list, emitting a wall of grep usage dumps plus one FAIL line per pattern. Fix: pass paths as `./$name` (or use `grep --`), and treat grep exit ≥2 as an AUDIT ERROR (fail loud, name the file), never as a hit.
4. **Daemon junk tripwire (carried from 03-01 §e3/f15, re-motivated by recurrence):** a heuristic committer should reject (or at least flag) tracked files whose names begin with `-`. Both occurrences of the junk class were daemon-committed.
5. **Claim-audit before annotation:** when annotating a prior report, check each neighboring claim's truth, not just the one being resolved (the "EVER" miss, §d3).
6. **Verify-before-done for fixture work means "harness executed", not "eval + hand-replay of the interesting case".** The 03-01 dispatch's case-8-only replay was sufficient but not maximal (its own §e4 admission); this run's full replay also re-proved cases 1–7 against the hardened drill — the pre-hardening 2026-09-24 build no longer exercises today's script. The full-harness replay is the template for future fixture verification when builds are down.
7. **Queue dedup preflight should check closed rows.** This task ID was dispatched a THIRD time after its TODO rows were `[x]` and its footer commits existed. The 2026-09-24 window-closeout already logged "the queue re-fired 4 closed task IDs 7 times"; this adds another instance. Each re-fire costs a full verification pass.

## f) NEXT (impact-sorted; provenance in parens)

1. **Fix `/run/binfmt`** — root `mkdir -p /run/binfmt` (reboot-ephemeral; durable fix = tmpfiles rule or the binfmt module) or reboot. Unblocks every sandboxed build, the pre-commit flake leg, negative-test-lints, and VM tests. (carried 03-01 f1; still #1)
2. **After 1: `nix build .#checks.x86_64-linux.borg-restore-drill-fixture`** — the first realization of the case-8-era drv; closes this item's last verification gap (b1). (carried 03-01 f2, sharpened by this run)
3. **Implement the queued borg-stub flag-handling fix** — dash-prefixed tokens shift-and-continue; only non-dash tokens collect as extraction paths. (queued this run, docs/todo/storage.md)
4. **Harden the push-protection audit invocation** — `./`-prefixed paths / `grep --`; exit ≥2 = audit ERROR naming the file, never a "hit". (new this run, §e3)
5. **Daemon junk-file tripwire** — reject tracked filenames beginning with `-` at commit time. (carried 03-01 e3/f15)
6. **Persist the sandbox-down verification recipe** as a script or AGENTS.md snippet. (new this run, §e1)
7. **AGENTS.md one-liner: run local harnesses from scratch cwds.** (new this run, §e2)
8. **Queue dedup preflight reads closure state** — suppress re-fires of IDs with `[x]` rows + footer commits. (queue-level, §e7)
9. **Mutation-negative cases for the other behavioral fixtures** — browser-history-probe-purge, forgejo-scripts-fixture, migrate-forgejo-subvol-fixture. (carried 03-01 f4)
10. **Owner decision: keep or drop `boot.binfmt.emulatedSystems`** — the failing bind is its sandbox-paths output; drop removes the failure class if aarch64 emulation is unused. (carried 03-01 f14)
11. **TODO row 43 (pipeline): `checks.disko-layout` eval failure** — pipeline-owned, same `/run/binfmt`-family breakage. (queue)
12. **Storage rows in passing** (all queue-owned, listed for completeness only): single-victim /data repair recipe; /data damage-set inventory; `docker system df` sizing refresh; root `@` retention anomaly reconcile; flm↔snapshot-pinning cross-link. (queue)

Items stop at 12: everything else this run touched is either already queued (the storage library owns it) or would require researching systems this task never opened — the honest cap, per the instruction's "based on THIS task's work".

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **`/run/binfmt` (carried from the 03-01 dispatch, still unanswered, still the box's #1 blocker):** may someone with sudo run `mkdir -p /run/binfmt` (reboot-ephemeral — if that unblocks builds, the durable fix is a tmpfiles rule or a module change), or is a reboot preferred? My tooling has neither `sudo` nor `systemctl`; the binfmt_misc interpreter registration itself is healthy — only the bind-source directory is missing.
2. **Queue dedup:** this task ID was dispatched a third time while its closure (footer commits + `[x]` TODO rows) was already in the tree at dispatch time. Is the dedup preflight actually reading closure state, and can re-fires of closed IDs be suppressed at enqueue? Three dispatches of one item is the failure mode the 2026-09-24 closeout already quantified (4 closed IDs re-fired 7 times).
3. **Daemon hook policy (carried 03-01 §g2, re-observed live):** the daemon committed the junk `--help` through no gate at 03:15 while hook-respecting commits were blocked on the same tree — first by that very file, then by the dead flake leg. Is daemon bypass intentional? If yes, should the answer to "the pre-commit gate is dead" always read "dead for hook-respecting committers only"; if no, what enforces the daemon running hooks?
