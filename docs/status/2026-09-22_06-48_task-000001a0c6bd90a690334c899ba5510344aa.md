# Task 000001a0c6bd90a690334c899ba5510344aa — GOTOOLCHAIN guard review-fix + status snapshot

**Date:** 2026-09-22 06:48 CEST · **Work commit:** `744f3882` (unpushed, by contract) ·
**Fixes reviewer finding on:** `b4eda274` (the parent task's pre-commit GOTOOLCHAIN narrowing) ·
**Branch:** master · **Format note:** Markdown per explicit instruction (status-report skill default is styled HTML — user instruction wins).

---

## Work performed

The reviewer's finding, reproduced first: the `b4eda274` narrowing changed the pre-commit
GOTOOLCHAIN guard predicate to `GOTOOLCHAIN[[:space:]]*=[[:space:]]*"auto"|GOTOOLCHAIN=auto`,
which silently stopped matching the spaced unquoted nix assignment `GOTOOLCHAIN = auto;`
(probe-confirmed: unquoted MISSED, quoted/bare CAUGHT, fish override + `local` correctly pass)
— and nothing pinned the guard's shapes, so the drift was invisible.

Fix shipped exactly as the reviewer specified, plus the promised selftest:

1. `.githooks/pre-commit` — predicate extended to
   `GOTOOLCHAIN[[:space:]]*=[[:space:]]*("auto"|auto)` (catches quoted nix, unquoted nix, bare
   env form; the redundant second alternation was absorbed). Comment updated.
2. `scripts/test-gotoolchain-guard.sh` (new, executable) — extracts the LIVE predicate and
   off-exclusion from the hook itself (extraction fails loudly if the guard is renamed/moved —
   doubles as a drift tripwire) and pins the boundary:
   - must CAUGHT: `GOTOOLCHAIN = "auto";` · `GOTOOLCHAIN = auto;` · `GOTOOLCHAIN=auto`
   - must PASS: `set -gx GOTOOLCHAIN auto` (fish FP) · `GOTOOLCHAIN = "local";` · off-marker line
3. Wired into `.githooks/pre-commit` (before the guard, `audit-serviceconfig-merge` precedent)
   and `.github/workflows/nix-check.yml` (new "GOTOOLCHAIN guard selftest" step).
4. TODO_LIST row "Negative-test the pre-commit GOTOOLCHAIN guard" marked `[x]` with a DONE
   annotation (`docs/todo/pipeline.md` has no library entry for it — verified, no drift to fix).

**Verification:** selftest green; mutation-tested against the OLD predicate in a scratch tree
(fails as required — the selftest is real); reviewer's exact probe shapes re-verified CAUGHT;
`shellcheck --severity=warning` clean on both shell files; all five pre-commit fast gates green
(serviceconfig-merge selftest+scan, check-todo-system, push-protection selftest+scan,
shell-nullglob, textfile-tmp); `nix fmt --no-update-lock-file -- --ci` → 0 changed; live-tree FP
sweep clean. Mid-session the auto-commit daemon captured the 4 in-flight files into a heuristic
commit (`a1d58712`); contents verified exact and amended forward into `744f3882` with the fix
footer (hook gates green on the amend: gitleaks OK, `nix flake check --no-build` OK).

---

## a) FULLY DONE

| Item | Evidence |
| --- | --- |
| Reviewer finding reproduced and fixed (predicate extension, unquoted nix shape caught) | `744f3882`; probe table above |
| Promised negative-test/selftest shipped with all 6 pinned shapes (3 catch + 3 pass) | `scripts/test-gotoolchain-guard.sh`; `SELFTEST OK` output |
| Selftest proven able to detect its own regression (mutation against old predicate) | scratch-tree run: `SELFTEST FAIL: guard missed a violation shape: GOTOOLCHAIN = auto;` |
| Selftest wired into pre-commit AND CI (house pattern for hook-guard scanners) | `.githooks/pre-commit` selftest block; `nix-check.yml` "GOTOOLCHAIN guard selftest" step |
| Shellcheck + syntax + formatter clean on all touched files | `shellcheck --severity=warning` rc 0; `bash -n` rc 0; fmt `--ci` 0 changed |
| Queue loop closed without drift (no pipeline.md library entry exists — checked) | TODO_LIST `[x]` row; `grep GOTOOLCHAIN docs/todo/pipeline.md` → empty |
| Dangling-commit disposition honored (`b4eda274` verified present; anchor text located) | `git cat-file -e` OK; anchor at hook line 192 pre-fix |

## b) PARTIALLY DONE

1. **The selftest pins the guard's PATTERNS, not its full per-file pipeline.** The `violates()`
   helper mirrors the extracted greps but the hook's surrounding plumbing (`[ -f "$f" ]`, the
   NUL-delimited loop, exit propagation) was never exercised END-TO-END with an actually staged
   violating `.nix` file this session (an amend commit skips the staged-file guards when the
   index carries no `.nix` diff). The plumbing is pre-existing and unchanged by this fix, so the
   residual risk is low — but the honest statement is: patterns proven, plumbing unexercised.
   Effort to close: S (one staged-file drill, or fold the drill into a harness).
2. **The off-exclusion fixture is synthetic.** `GOTOOLCHAIN = auto; # off` pins that the
   exclusion grep still exists, but NO live tree line uses an off marker; the historical
   file-level off semantics (abe346cf) were narrowed to line-level in `3482718a` without anyone
   noticing until this selftest was written. The exclusion's real-world contract is now
   lineage-documented but not domain-decided (see g).
3. **Multi-line nix assignments stay invisible** (`GOTOOLCHAIN =\n  "auto";` is valid nix the
   line-based grep can never see). Same limitation as the old guard; accepted, now documented.

## c) NOT STARTED

- **Nothing from the reviewer finding** — both expected deliverables (predicate fix, selftest)
  shipped. The adjacent same-family work discovered in passing was deliberately NOT touched
  (fix-exactly-this-finding contract); it is itemized in (d)/(f):
  - CI hygiene step + `scripts/check-flake-inputs.sh` still carry the OLD bare grep (d2/d3).
  - End-to-end staged-file drill for the guard block (b1).
- Already-queued storage items untouched by design (PSI-gated hot-db VM-test rebuild;
  mountPoint-vs-HM-symlink eval guard) — still open in TODO_LIST.

## d) TOTALLY FUCKED UP (repo state discovered in passing — none of it introduced by this task)

1. **CI has been RED on master for ≥6 consecutive pushes (all of 2026-09-21), with TWO stacked
   independent failures masking each other.** Verified via `gh run list` / `gh run view
   35617574247`: (i) the main `nix-check` job dies at **"Run statix linter"** —
   `modules/nixos/services/hot-user-caches.nix:101` `device = cfg.device;` trips statix
   `[04] Assignment instead of inherit` — and every later gate (deadnix, fmt, eval, templ,
   nullglob, textfile, push-protection, serviceConfig-merge, **and "Flake input hygiene"**) is
   SKIPPED behind it; (ii) all VM-test jobs (boot, miniflux, searxng, port-uniqueness,
   caddy-auth-patterns) die with `Failed to fetch git repository
   'ssh://git@github.com/LarsArtmann/branching-flow'` — the branching-flow deploy-key/ssh-agent
   wiring documented in AGENTS.md is not working in the VM jobs. Consequence: CI currently
   provides ZERO signal on master, and the newest red (item 2) is invisible behind the older one.
2. **The fish-line GOTOOLCHAIN FP is still LIVE in the CI hygiene step and
   `scripts/check-flake-inputs.sh` — the exact class `b4eda274` fixed in the pre-commit guard.**
   Both still grep the bare `GOTOOLCHAIN.*auto` with only the `off` exclusion; home.nix's fish
   lines (`set -gx GOTOOLCHAIN auto`, landed in `9be027c8`) trip both — locally PROVEN today:
   `bash scripts/check-flake-inputs.sh` → rc 1, and the CI inline grep returns the two lines.
   Latent in CI only because statix blocks the job before the hygiene step runs. The
   2026-09-16 status report's claim "excluded the fish session-guard from the purity check
   (pre-existing false positive on master, live-verified cleared)" is CONTRADICTED by live
   evidence — the exclusion never landed or was later lost.
3. **`scripts/check-flake-inputs.sh` is an ORPHAN and a split-brain duplicate.** Referenced
   NOWHERE (not in `.githooks/`, `.github/`, or `flake.nix`); the CI "Flake input hygiene" step
   is a hand-inlined COPY of its logic. Three copies of the same predicate now exist
   (pre-commit hook, CI inline step, orphan script) and they have ALREADY diverged (the fixed
   one vs two stale ones). This is precisely the second-source-of-truth class this fix's
   selftest exists to prevent.
4. **A status-doc claim rotted into "verified"** (see d2) — the same 24h class already queued
   as the `verify-status-citations.sh` idea, now with a third concrete victim.

## e) WHAT WE SHOULD IMPROVE

1. **One predicate, extracted everywhere.** The GOTOOLCHAIN purity rule now has one
   source-of-truth predicate (the hook) and one selftest — but CI and the orphan script still
   hold stale copies. House fix shape: make the CI step (or a shared `scripts/lib/` fragment)
   consume the hook's predicate the same way the selftest does, then delete the orphan or make
   it the single consumer. Every future guard fix should follow this shape by default.
2. **Fix runs should SMOKE the sibling gates of whatever they touch.** This fix session ran the
   five fast gates for its changed files but not `check-flake-inputs.sh` — one command would
   have surfaced the orphan-red (d2/d3) a day earlier, in the fix run's own close-out instead
   of a status report. Cost of the habit: seconds.
3. **"Live-verified" claims need an expiry.** d2's 09-16 claim was presumably true when written.
   Any status-report claim of a live green should carry the command that proves it, so the next
   session can re-run instead of trusting prose (pairs with the already-queued
   verify-status-citations item, which covers SHAs; this extends it to command claims).
4. **Daemon-commit amend-forward worked as designed but adds a review step every session**
   (verify `git show --stat` before amending). No change needed — just noting the discipline
   held and is worth keeping in the report templates as a checklist line.

## f) Up to 50 things to get done next (ranked; ★ = NEW discovery from this session, HARVEST
targets; "(queued)" = already in TODO_LIST, listed here only for the ranked picture)

| # | Task | Impact | Effort | Category |
| --- | --- | --- | --- | --- |
| 1 ★ | Fix statix finding `hot-user-caches.nix:101` (`device = inherit (cfg).device;` shape) — unblocks ALL skipped gates in the main CI job | Critical | S | Bug |
| 2 ★ | Fix branching-flow ssh fetch in CI VM-test jobs (deploy-key/ssh-agent wiring per the AGENTS.md recipe — key exists as `NIX_DEPLOY_KEY_BRANCHING_FLOW`; VM jobs apparently don't load it) | Critical | M | Bug |
| 3 ★ | Converge the CI "Flake input hygiene" step onto the hook's narrowed predicate + fish-shape pass (reuse the selftest's extraction approach or a `scripts/lib` fragment) | High | S | Quality |
| 4 ★ | Resolve the orphaned `scripts/check-flake-inputs.sh`: delete it, or make CI call it instead of the inline duplicate (single source of truth) | High | S | Cleanup |
| 5 ★ | Annotate the 2026-09-16_16-36 status report's "fish guard excluded, live-verified cleared" claim as contradicted (docs-health ANNOTATE, inline correction) | Medium | S | Documentation |
| 6 | End-to-end staged-file drill for the GOTOOLCHAIN guard block (stage a violating fixture, run the hook, expect exit 1, unstage — daemon-race-safe) — closes the b1 residual | Medium | S | Quality |
| 7 | Decide the off-exclusion contract: is `off` a still-supported escape hatch (then give it a sanctioned line shape and a live fixture) or dead semantics to drop from all three greps | Medium | S | Decision |
| 8 | Document the multi-line-assignment limitation of all line-based GOTOOLCHAIN greps (hook comment + CONTRIBUTING guard inventory) | Low | S | Documentation |
| 9 (queued) | PSI-gated rebuild of hot-db/crush-hot-db VM tests (io avg10 gate <20%) | High | M | Quality |
| 10 (queued) | mountPoint-vs-HM-symlink eval guard (the go-build automount class) | High | M | Quality |
| 11 (queued) | `scripts/verify-status-citations.sh` (bare-SHA citation verification + docs-health gate) — extend scope to command claims per e3 | Medium | M | Quality |
| 12 (queued) | Queue dedup preflight for closeout/review-fix dispatches (owner-gated) | Medium | S | Decision |
| 13 (queued) | Formatter-exclusion regression guard (flake check asserting `docs/**/*.html` in effective treefmt excludes) | Medium | S | Quality |
| 14 (queued) | Re-splice the formatter-inflated disk-layout HTML (7.9 MB → ~3.6 MB) + verify-html-diagrams | Medium | S | Cleanup |
| 15 (queued) | restic-app-dumps first-run deploy gate (module landed, deploy-pending) | Medium | S | Feature |
| 16 (queued) | Owed evo-x2 reboot (flm :52626 corpse since 09-07/09-11; llama-rag D-state corpses) — run `nix run .#pre-reboot-check` first | High | M | Stability |
| 17 ★ | Once CI is green again, sweep `gh run list` for the FIRST red run and date exactly when master CI went dark (known ≥09-21; earlier boundary unknown) | Medium | S | Documentation |
| 18 ★ | Add a CI-awareness tripwire: a daily scheduled run (or queue-item check) that PAGES when `nix-check.yml` has ≥3 consecutive red pushes — CI dark went unnoticed for ≥1 day | High | S | Feature |
| 19 ★ | Check whether `nix-check.yml`'s VM jobs also miss OTHER deploy keys (CV, go-cqrs-lite) the way branching-flow does — same recipe, same audit | Medium | S | Bug |
| 20 ★ | Add the fish session-override lines to the selftest's PASS fixtures? No — they're already pinned; instead: assert the selftest FAILS if the hook's guard block is deleted entirely (drift tripwire currently only covers pattern extraction, not block presence) | Low | S | Quality |
| 21 | Sweep `docs/status/2026-09-22_*.md` backlog lists for items already satisfied by this fix (the two closeout reports list the GOTOOLCHAIN selftest as backlog item 6/4) — annotate or prune | Low | S | Documentation |
| 22 | Give every hook-guard scanner the same `--selftest` contract (audit-shell-nullglob and audit-textfile-tmp have none today — they are scanners without prove-it-still-detects tests, the exact gap that let the GOTOOLCHAIN narrowing slip) | High | M | Quality |
| 23 | Consider a `scripts/lib/guard-patterns.sh` shared library for purity predicates (GOTOOLCHAIN, tarball, ref=master) consumed by hook + CI + scripts | Medium | M | Quality |
| 24 (queued) | Hot-db Phase-2 fold: `crush-hot-db` into `services.hot-db` (interim mechanism doctrine) | Medium | L | Cleanup |
| 25 (queued) | llama-rag root-cause (unit-context spin on pinned build; soak under real units ≥10 min before any re-enable) | High | L | Bug |
| 26 (queued) | Hetzner StorageBox + BorgBackup offsite leg (blueprint exists, not deployed) | High | L | Feature |
| 27 (queued) | Resend domain verification → mail-relay/pocket-id delivery probes | Medium | S | Feature |
| 28 (queued) | history purge push-time re-filter (if the held decision ever flips) | Low | M | Security |
| 29 ★ | Add `scripts/check-flake-inputs.sh` (or its successor) to pre-commit so the purity rule is enforced locally too, not just in the (currently red) CI | Medium | S | Quality |
| 30 ★ | After CI heals: verify the "GOTOOLCHAIN guard selftest" step passes in the REAL runner environment (grep behaviors can differ on runner images — e.g. locale/ERE; locally proven only) | Medium | S | Quality |
| 31 | Pre-deploy-check §-audit: does any deploy gate reference `check-flake-inputs.sh` (orphans often have forgotten callers in deploy scripts) | Low | S | Cleanup |
| 32 (queued) | Pocket ID CIMD stance pick (UNSET vs explicit `[]`) on the next docs pass | Low | S | Decision |
| 33 (queued) | groq key decision for CV ChatService (`warn` overall /health owner decision) | Medium | S | Decision |
| 34 (queued) | Forgejo `Artmann-Minecraft` transferred mirrors: delete or re-mirror (owner decision recorded 09-18) | Low | S | Decision |
| 35 ★ | Extend `negative-test-lints.sh` doctrine doc to mention hook-guard selftests (it currently claims the flake-check lane; the hook lane now exists alongside) | Low | S | Documentation |
| 36 | `boot-mirror-sync` verified green since 09-18? (in passing: no complaints seen; confirm the diff gate still passes post-784/785 churn) | Low | S | Quality |
| 37 ★ | Statix pre-commit coverage gap: statix runs in CI repo-wide but the hook only lints STAGED files — the `hot-user-caches.nix` lint aged on master unnoticed; consider running `statix check` on the whole tree in pre-commit for NEW modules (or accept CI-as-arbiter) | Medium | S | Quality |
| 38 | After CI heals, re-baseline the "known red checks" list in CONTRIBUTING (AGENTS.md mentions the historical cv check red; today's real red set differs) | Low | S | Documentation |
| 39 ★ | The `1m02s` CI failures suggest some jobs fail fast at eval — inventory WHICH jobs fail at which step across the last 20 runs and turn it into a table in docs (this session only sampled 1 run deeply) | Low | M | Documentation |
| 40 | Rotate the report backlogs: harvest THIS section into TODO_LIST/docs/todo (docs-health HARVEST) — items 1-8 + 17-23 + 29-31 + 35 + 37 + 39 are the new-discovery set; the rest are already tracked | High | S | Process |

*(50 was the ceiling; 40 concrete items beat 10 padded ones — items 41-50 would have been
rephrasings of the above.)*

## g) Up to 3 questions I can NOT figure out myself

1. **Is GitHub CI on SystemNix expected to be live signal or known-dark?** Six consecutive red
   pushes on 2026-09-21 drew zero visible reaction (unlike CV's hosted-minutes death, which is
   documented). If CI is signal: should I dispatch the statix one-liner (#1) and the
   branching-flow VM-auth fix (#2) immediately? If known-dark: which failure class convinced
   you to stop watching, and should the workflows be paused/scheduled instead of failing
   silently on every push?
2. **The GOTOOLCHAIN `off` exclusion: dead relic or supported escape hatch?** No tree line has
   used an off marker in months (probe), the original file-level semantics were silently
   narrowed to line-level in `3482718a`, and my selftest now pins a synthetic shape. If `off`
   is dead, three greps lose an exclusion; if alive, it needs a sanctioned line shape and a
   live fixture. Which is it?
3. **Should the fish session-override (`set -gx GOTOOLCHAIN auto`) get a declarative exemption
   marker** (e.g. a trailing `# session-only` tag that all GOTOOLCHAIN scanners exclude), so
   future guard work can't re-litigate the FP — or do you prefer the rule to stay "hook
   predicate is the single source, everything else derives from it" (then the marker is
   unnecessary and the answer to #2 probably converges with it)?

---

**Pending loop-closure:** section (f)'s ★ items are HARVEST targets (docs-health → TODO_LIST /
docs/todo), not yet harvested — this report only records them per the dispatch scope.
