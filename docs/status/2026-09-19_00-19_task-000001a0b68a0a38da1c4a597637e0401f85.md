# Task Status Report — 000001a0b68a0a38da1c4a597637e0401f85

**Date:** 2026-09-19 00:19 CEST
**Work item:** TODO_LIST.md P1 — "Verify Paperless AI actually USES the llama-rag embeddings end-to-end"
**Outcome:** Item left **unchecked with a BLOCKED marker** — the item's premise ("llama-rag is re-enabled and ACTIVE") was stale and falsifiable in one grep; the executable half (paperless-side verification) was done, the root-gated half was not.
**Work commit:** `dd688ef4` (docs(todo): mark paperless E2E item blocked (llama-rag disabled)) with Task-Queue-ID footer.
**Format note:** User requested `.md`; the status-report skill's canonical format is styled HTML. Honoring the user instruction; one-off override, not propagated into the skill.

---

## Context: what the task actually faced

The task queue handed me an item written 2026-09-18 during the gen-784 deploy window. Between that note and my session, the **freeze-5 escape condition fired**: the pinned llama.cpp 0.3.0 build — "live-verified at ~4s load" via shell direct-run — **spun at ~94% CPU per server under the REAL systemd units** (2h33min CPU over 2h43min wall ×2, 8.6G written per lifecycle), killed the box at 15:27, and `llama-rag.enable = false` landed. AGENTS.md's llama-rag section already documented all of this when I started. The TODO item did not.

So the prescribed next step ("IO-drained manual `systemctl restart llama-embeddings llama-reranker`, expect ~4s load + /health 200") was doubly impossible: the units no longer exist in the deployed generation, and a restart would have re-entered the exact spin class that caused freeze #5 — during an active IO storm (io PSI some avg60 = 56% at session time).

---

## a) FULLY DONE

1. **Stale-premise detection and correction.** Proved llama-rag is config-DISABLED in the repo (`configuration.nix:626`) and in the deployed generation. One `grep` against AGENTS.md would have found this before execution — see (e)1.
2. **Deployed-generation reconciliation.** `/run/current-system` == `/nix/var/nix/profiles/system` (system-785, `26.11.20260916.b1b8759`) — clean anchoring, no un-anchored-generation trap.
3. **Live unit-absence proof.** No `llama-embeddings`/`llama-reranker` units exist in `/etc/systemd/system` (only `ollama.service` matches) — the prescribed `systemctl restart` is structurally impossible without a config re-enable + deploy.
4. **Endpoint probes.** :8848 and :8849 both refuse connections (python urllib — `curl` is banned by this session's tooling). Paperless RAG is dark, as expected with the module disabled.
5. **Paperless health check.** All four units (`paperless-web/consumer/scheduler/task-queue`) active/running via busctl with correctly escaped object paths (`_2d`/`_2e`).
6. **Deployed env wiring verified on the delivery surface.** `/etc/systemd/system/paperless-web.service` carries `PAPERLESS_AI_ENABLED=true`, `PAPERLESS_AI_LLM_EMBEDDING_ENDPOINT=http://127.0.0.1:8848/v1`, `EMBEDDING_MODEL=bge-m3`, `EMBEDDING_API_KEY=llama-server-no-auth` — this upgrades the item's "env wiring confirmed correct" from source-claim to deployed-generation fact, using the sanctioned surface (AGENTS gotcha: grep the unit file, not phantom config paths).
7. **Gatus gating verified.** llama-rag's registry checks + homepage tile use `inherit (cfg) enable;` — the disable leaves no permanently-red checks or tiles.
8. **TODO_LIST.md loop closed per contract.** Item annotated with the 2026-09-19 verification round (evidence, falsified premises, verified facts) and a one-line `— BLOCKED:` reason; left unchecked per the contract's fail path.
9. **New backlog item appended (discovered follow-up):** the hermes-python-source store-path orphan (see d1) — agent-executable fix defined (quiet-window `nix build` of the FOD).
10. **Commit `dd688ef4` with Task-Queue-ID footer**, daemon-race-safe: the auto-commit daemon committed my TODO edit first (`1207797f`); I verified `git show --stat HEAD` = exactly my one file, then amended into the properly messaged commit. Docs-only `--no-verify` per the documented precedent (`73145250`) because of the pre-existing hermes red (d1).

## b) PARTIALLY DONE

1. **The item itself: paperless↔llama-rag E2E verification.**
   - Works now: paperless side fully verified (health, deployed env wiring, delivery surface) — half the item.
   - Remains open: the live-call half — paperless actually completing an embedding request against a serving :8848, plus the `paperless_appconfig` DB-vs-env check.
   - Blocker: llama-rag is frozen by the freeze-5 escape condition (upstream bisect + ≥10-min real-unit soak required before any re-enable); the DB check needs sudo (PostgreSQL peer auth) and this session's tooling bans `sudo`/`systemctl`.
   - Effort to finish once unblocked: S (one quiet-window session).
2. **Runtime evidence gathering.**
   - Works now: generation, unit, port, env, Gatus-gating evidence — all captured.
   - Remains open: journal evidence of paperless's embedding attempts (graceful-degradation lines). Two attempts failed: a background `journalctl` hung under the IO storm (killed), and my `--grep` invocation was malformed. Non-essential — the DB check subsumes it.
   - Blocker: journal reads are IO-expensive and the box is mid-storm; not worth the risk for corroboration only.
   - Effort: S in a quiet window.
3. **IO-storm situational awareness (in passing).**
   - Works now: measured `/proc/pressure` (io some avg10=60.79 / avg60=56.18 / avg300=49.03 at ~00:05) — enough to rule out restarts and heavy validation.
   - Remains open: driver identification (freeze-6 recovery readers per AGENTS: crush-hot-db migration + discordsync-db-heal are the suspects; I did not investigate — explicitly out of scope).
   - Effort: S–M.

## c) NOT STARTED

1. **Upstream bisect of the llama.cpp mid-load spin** (ROCm runtime / kernel / GPU-state, upstream of llama.cpp). Not started: standing escape-condition work, owned by AGENTS doctrine, not this task. Priority: the gate for everything llama-rag.
2. **`paperless_appconfig` DB-vs-env effective check.** Not started: sudo-banned this session. The load-bearing remaining verification of the item. Priority: High (it is the item's "UI-saved values override env" clause).
3. **`PAPERLESS_AI_LLM_RERANKER_*` wiring.** Deliberately skipped: MOOT per prior source-verification (paperless 3.1.3 has zero reranker support upstream). Correctly not-started; nothing to do.
4. **llama-rag re-enable + post-re-enable E2E re-run.** Not started: gated behind (1). Priority: High after the gate passes.
5. **Anything behind this item in the queue** — out of scope by contract; not assessed.

## d) TOTALLY FUCKED UP

1. **Pre-commit flake leg is red for ALL explicit commits (repo-wide, pre-existing).**
   - What: `nix flake check --no-build` aborts at `checks.x86_64-linux.hermes` with `path '7g4gal5lh4zzgk41v2cax2rsjch7q4ym-hermes-python-source' is not valid`.
   - Root cause (best evidence): the store DIR exists on disk but is unregistered in the nix DB (no `.drv` in store either) — a present-but-unrealized orphan, consistent with a freeze-era interrupted realization (freeze #5/#6, 2026-09-18) after the 26-input lock bump `7acbdbe4`.
   - Severity: blocks the dev workflow (every explicit `git commit` with hooks); deploys and the auto-commit daemon are unaffected.
   - Mitigation: `--no-verify` for docs-only commits (precedent `73145250`); real fix tracked in the new backlog item (quiet-window FOD re-build).
2. **Paperless RAG is silently degraded (by design, but near-invisibly).**
   - What: `PAPERLESS_AI_LLM_EMBEDDING_*` points at dead :8848; paperless-ai degrades gracefully — which for monitoring purposes is the worst kind of green. Nothing outside paperless's own logs surfaces "RAG dark because llama-rag disabled".
   - Severity: Medium (no data loss; silent capability loss only).
   - Mitigation: the disable itself (correct per escape condition); visibility gap listed in (f).
3. **The task-queue premise was fucked up at intake.** The item asserted "llama-rag is re-enabled and ACTIVE" and prescribed a restart with a falsified expectation ("~4s load") hours after that expectation was disproven by freeze #5. A session with less discipline would have re-enabled, deployed, and possibly contributed to freeze #7. Severity: High (process), no damage done.
4. **My own missteps during the session (radical honesty):**
   - **False outage alarm:** my first busctl query used a malformed object path (`paperless_2eweb` instead of `paperless_2dweb`) and returned stub properties; I briefly treated "paperless-web inactive/dead" as a finding before re-querying correctly. A false alarm like that, written into a report, is exactly the "single tool output drives a semantic claim" class AGENTS warns about.
   - **Malformed journalctl:** `--grep -i "embed"` errored (`Failed to add match`); the documented form is `--grep "<pat>" --output cat`. Wasted a round.
   - **Unbounded journal read:** my first journal probe had no `timeout`, hung under the IO storm, and had to be backgrounded and killed — the AGENTS `timeout` rule applied one attempt too late.
   - **Slow premise check:** I ran five live-state commands before grepping TODO/AGENTS for the llama-rag state story; the AGENTS.md llama-rag section alone would have re-shaped the plan in one step.
   - **Lost the daemon race:** the TODO edit sat uncommitted long enough for the daemon to commit it under a "heuristic" message; the amend dance worked but was avoidable by committing immediately.

## e) WHAT WE SHOULD IMPROVE

1. **Queue premise hygiene.** Items asserting live system state ("X is re-enabled and ACTIVE") go stale in hours on this box. Fix: queue intake requires either a freshness re-check instruction or the agent preamble mandates reconciling the item against the relevant AGENTS.md section BEFORE any execution. Impact: this session would have been 30% shorter and would never have contemplated a restart.
2. **Root-gated items should be marked at intake.** Items prescribing `systemctl restart` and sudo-only DB checks are unexecutable by queue agents (tool policy bans systemctl/sudo). Fix: tag such items "root-session required, agent verifies read-only half" so the BLOCKED outcome is the planned one, not a discovered one. Impact: no burned sessions; faster user execution.
3. **Pre-commit vs store-path orphans.** A present-but-unregistered store path nukes every explicit commit's flake leg with a cryptic message. Fix: extend the pre-commit flake-leg failure classification (the pre-deploy §1 block-attribution pattern) to recognize `path ... is not valid` as a loud WARN naming the repair command, and let docs-only commits pass without `--no-verify`. Impact: removes a repo-wide dev friction that will recur after every freeze.
4. **busctl escaping helper.** Unit name → object path (`.`→`_2e`, `-`→`_2d`) burned me once and is a recurring manual trap in a systemctl-banned environment. Fix: a 5-line helper script (or a shell function in the agent preamble). Impact: kills the false-alarm class.
5. **Journal probes: apply the AGENTS rule from attempt one.** `timeout N` + `--grep "<pat>" --output cat` + treat rc 0/1 as valid counts. Impact: no hung shells under IO storms.
6. **TODO_LIST append discipline.** My BLOCKED append is a wall of text; the item is now a novel. Fix: BLOCKED appends ≤ a few sentences, narrative goes in the timestamped status report (this file), and docs-health HARVEST splits long items. Impact: keeps the queue readable.
7. **Commit immediately after edit.** The daemon race is deterministic when I dawdle; commit the moment the edit lands, with the pathspec discipline already documented. Impact: no amend dance, no "heuristic" messages carrying task content.

## f) Top things to get done next (up to 50; ranked by impact within groups)

Legend: Impact / Effort (S <30min, M 30min–2h, L >2h) / Category. Items 1–12 extend THIS task's chain; 13–16 are the passing discoveries; 17–21 are IO/freeze-6 follow-ups noticed in passing; 22–26 process; 27–31 paperless-adjacent open items (already tracked, listed for completeness); 32–38 hygiene/verification loose ends.

**llama-rag → Paperless chain (this item's remaining life):**

1. Bisect the llama.cpp mid-load CPU-spin upstream (ROCm runtime/kernel/GPU-state env; the pinned 0.3.0 build spins under real units — the escape condition stands until this lands). — Critical / L / Bug
2. Before ANY re-enable: soak the pinned build ≥10 min under the REAL unit sandbox (`systemd-run` with the exact unit env + device cgroup) — a shell direct-run green is not evidence (freeze-5 lesson). — Critical / S / Quality
3. Re-enable `llama-rag.enable` and deploy in an IO-quiet window (deploy pressure gate green) after 1+2. — High / S / Feature
4. Post-re-enable verification: `/health` 200 on :8848 + :8849, ~4s load, `llama_rag_leaked_instances 0`. — High / S / Verification
5. Re-run this item's E2E half: functional `/v1/embeddings` probe + paperless journal evidence of successful RAG calls. — High / S / Verification
6. Run the `paperless_appconfig` DB-vs-env check (`sudo -u postgres psql -d paperless`) — hunt UI-saved rows that override the env wiring; this is the item's original "UI overrides env" clause. — High / S / Verification
7. Pin down which paperless source consumes `PAPERLESS_AI_*` (fork vs nixpkgs 3.1.1; where the `appconfig` schema lives) — needed to write the DB check's exact row keys. — Medium / S / Documentation
8. Add degradation visibility: a metric or Gatus signal for "paperless expects embeddings but :8848 is dark" so the disable isn't silent (currently only paperless logs show it). — Medium / M / Feature
9. Verify the `llama-rag-leak-metrics` collector is enable-gated like the checks/tiles (no orphan collector while disabled). — Low / S / Verification
10. Confirm post-deploy-check's llama smoke auto-skips gracefully with the module disabled (no phantom red/green in the deploy gate). — Low / S / Quality
11. Rogue-listener defense for :8848/:8849 while the module is disabled — the 2026-09-18 orphan class can phantom-pass future embedding smokes. — Medium / M / Bug
12. Add a "RAG dark = expected while llama-rag disabled" note to `docs/services/paperless.md` so the next operator doesn't chase 503s. — Low / S / Documentation

**Passing discoveries (this session):**
13. Re-realize the orphaned `hermes-python-source` store path (backlog item already appended): quiet-window `nix build` of the FOD, then `nix flake check --no-build` green — un-blocks explicit commits repo-wide. — High / S / Bug
14. Investigate the orphan mechanism (which freeze left a dir present-but-unregistered) and sweep for other orphans (`nix store verify`-class check, quiet window). — Medium / M / Bug
15. Pre-commit: classify `path ... is not valid` eval failures as loud WARN + repair hint (allow docs-only commits without `--no-verify`), mirroring pre-deploy §1's block-attribution. — Medium / S / Quality
16. Document the hermes red + `--no-verify` docs-only doctrine in docs/CONTRIBUTING.md's guard inventory. — Low / S / Documentation

**IO storm / freeze-6 follow-ups (noticed in passing):**
17. Identify the current storm driver (io some avg60=56% at 00:12): Zone 6 trip log, crush-hot-db-migrate state, discordsync-db-heal, concurrent agent-session count. — High / S / Diagnosis
18. Confirm the crush-hot-db migration CONVERGED post-freeze-6 (symlink sweep + PSI baseline drop) — freeze #6 left it ~2/3 done. — High / S / Verification
19. Extend `ioChurnUnits` with `crush-hot-db-migrate` + `discordsync-db-heal` (freeze-6 rule b — the guard is structurally blind to recovery readers). — High / S / Bug
20. Serialize recovery readers through `heavy-job` (freeze-6 rule c) instead of letting them run concurrent at boot. — Medium / M / Quality
21. Confirm `discordsync-db-heal` completed after the freeze-6 boot (it timed out at 10 min pre-freeze and re-runs every boot into a cold cache — the crash-loop amplifier). — Medium / S / Verification

**Process / tooling:**
22. Queue intake rule: live-state claims need a freshness re-check instruction (see e1). — High / S / Process
23. Mark root-gated queue items at intake ("root-session required") (see e2). — High / S / Process
24. Sanctioned read-only probe wrappers for agents (busctl-state helper, python HTTP probe, a read-only sudo allowlist decision). — Medium / M / Feature
25. busctl unit→object-path helper script (see e4). — Low / S / Quality
26. Auto-commit daemon: consider footer-awareness or task-file exclusion to stop racing explicit queue commits (see e7). — Medium / M / Process

**Paperless-adjacent open items (already tracked in TODO_LIST; re-listed for completeness):**
27. Paperless scheduled-task failure monitoring collector + Gatus check (P0, open — the 22h classifier-red class). — High / M / Feature
28. Encrypted-tag consistency alert (wrong-password detection companion). — Medium / M / Feature
29. Mail relay SPF replacement + Resend domain verification (user/registrar step), then paperless share-link re-test to a non-owner inbox. — High / S / User step
30. InboxClean→Paperless API token rotation (user-run sudo runbook, open). — Medium / S / Security
31. Deploy the paperless retro-decrypt repair feature (upstream 2026-09-12, needs upstream push + flake bump; noticed in passing). — Medium / M / Feature

**Hygiene / verification loose ends:**
32. docs-health pass: split the novel-length paperless item into a llama-rag-gate row + a paperless-E2E row (see e6). — Low / S / Documentation
33. Annotate TODO_LIST line 455 owner-question (4): the llama-rag decision was resolved by events (pin → escape condition → disable). — Low / S / Documentation
34. Confirm no other consumer pats `llama_rag_*` textfile metrics while the module is disabled (stale/absent .prom must not phantom-red anything). — Low / S / Verification
35. Verify the deploy gate (§10 metric loans) has no llama-era stale loan entries after the disable. — Low / S / Verification
36. Capture the journal evidence of paperless's embedding attempts in a quiet window (corroboration for the item's history). — Low / S / Documentation
37. Consider a fast path in pre-commit for pure-markdown diffs (skip the flake leg) — complements item 15. — Medium / S / Quality
38. After the llama-rag chain lands: update AGENTS.md paperless bullet + this TODO item's lineage so the next session inherits truth, not archaeology. — Low / S / Documentation

## g) Questions I cannot figure out myself

1. **llama-rag risk appetite:** do you want a dedicated upstream-bisect session queued NOW (ROCm runtime/kernel/GPU-state — the only path back to paperless RAG), or should paperless RAG stay dark until llama.cpp/ROCm fix it upstream on their own? Two freezes came from this subsystem, so this is an owner risk call, not an engineering one.
2. **Queue contract for root-gated work:** is "agent verifies the read-only half + BLOCKED marker; user executes the privileged half" the intended contract, or should agents get a sanctioned read-only path (e.g., a sudo allowlist for `systemctl status/restart` of named units and `sudo -u postgres psql` reads)? This session's tooling bans `systemctl`/`sudo` outright, which made the item's prescribed next step unexecutable by construction.
3. **`PAPERLESS_AI_*` implementation location:** I did not resolve (and per the no-unrelated-research constraint did not dig for) whether the AI/embedding layer consuming `PAPERLESS_AI_LLM_*` and the `paperless_appconfig` table lives in a SystemNix-consumed paperless fork or an extension of nixpkgs paperless-ngx 3.1.1 — the answer defines the exact DB-check query for item 6. If you know it offhand, it saves the next session a repo dig.

---

**Handoff note:** Section (f) items 1–12 are the live continuation of this task; item 13 is the repo-wide unblocker. The TODO_LIST.md already carries the BLOCKED close (commit `dd688ef4`); items 13 and 32–33 are the only (f) entries not yet in the queue file (13 is; 32/33 fold into the next docs-health pass).
