# Status Report + Self-Review — Task `000001a0da9592025fcb2a6ded5f35c8fefa` (run 2, re-dispatch)

**Date:** 2026-09-26 00:23 CEST
**Work item:** "Re-verify whether the removed 'prune gaps ride guard/outage-killed nights' doctrine framing tracked a real historical gap (read the 05-32 sweep report §f.41 evidence; separate entry if yes, misdiagnosis note if no)" → `docs/todo/storage.md`
**Nature of THIS run:** SECOND dispatch of the same Task-Queue-ID. Run 1 completed the item ~9 hours earlier (commits `37869731` + `7467e2e2`, both footer-bearing; report `docs/status/2026-09-26_00-16_task-…fefa.md`; verdict MISDIAGNOSIS; both queue surfaces `[x]`). This run executed the re-dispatch verification protocol (`docs/todo/pipeline.md:159`) and landed this footer-bearing report — nothing else changed in the repo.
**Prior reports in this chain:** `2026-09-26_00-16_task-…fefa.md` (run 1 — the report this run verifies), `2026-09-25_11-51_task-…3acdfb9a1d8.md` §g2 (origin ask), `2026-09-25_12-27_task-…0225c24.md` (harvest that queued the item), `2026-09-25_00-20`/`00-39_task-…ebd1.md` (the sweep session that wrote the framing), `archived/2026-09-11_05-32_task-…d28d84088.md` §f.41 (the origin of the whole doctrine chain).
**Format note:** the status-report skill's canonical output is a styled HTML dashboard; this dispatch explicitly named a `.md` path — user instruction wins (flagged per the skill's override rule; same as every prior report on this chain). The self-review questions (brutal-self-review skill) are folded into sections d/e/g here rather than written as a separate `docs/reviews/` HTML — one report was requested at one path.

---

## Executive summary

The queue re-dispatched an already-completed task (re-fire class: `docs/todo/pipeline.md:157` — its own text cites 4 task IDs fired 7 times in the 09-23/24 window; this is another live instance). The re-dispatch verification protocol (`pipeline.md:159`) was executed end-to-end instead of blindly redoing or blindly skipping: **(1)** footer commits + queue-surface closure verified (`37869731`/`7467e2e2` footer-bearing; `docs/todo/storage.md:14` and `TODO_LIST.md:50` both `[x]`, no drift); **(2)** ONE load-bearing claim of run 1 spot-checked live — the three-commit provenance chain (`dbe48a7d` → `50c8adb6` → `22a4e88f`) exists with matching dates, messages, and file footprints; **(3)** run 1's §f swept — every item is `[folded]` or `[queued]`, nothing un-landed (unlike the 00-39 re-fire, which found 5 orphans behind a closed `[x]`); **(4)** this footer-bearing report is the attributable landing. **The verdict stands unchanged: MISDIAGNOSIS — the framing tracked no real historical prune gap; no separate incident entry needed.** The only candidate real over-retention in the inventory remains the `@home-hermes` 09-16 snapshot, which already has its own queued row. Zero code touched; the repo is not broken.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| a1 | **Work item confirmed closed on BOTH queue surfaces, no drift** (house rule: one-liner and library entry move together): `docs/todo/storage.md:14` carries the full MISDIAGNOSIS note including the `@home-hermes` cross-reference; `TODO_LIST.md:50` carries the compact `[x]` version + pointer. Both read as consistent with each other and with run 1's report. | `view` of both surfaces this run; run 1's a6 (`check-todo-system.sh` green ×2) |
| a2 | **Footer commits verified under the exact Task-Queue-ID:** `37869731` ("close prune-gaps-framing re-verify — misdiagnosis, no separate entry") and `7467e2e2` ("status report for the prune-gaps-framing re-verify task") both end in `Task-Queue-ID: 000001a0da9592025fcb2a6ded5f35c8fefa`; working tree clean at HEAD `7467e2e2` when this run started. | `git log --format=%B` on both commits; `git status --short` empty |
| a3 | **Protocol step 2 — live spot-check of ONE load-bearing claim (independently of run 1's report):** the provenance chain run 1 built its verdict on is real. `dbe48a7d` (09-25 00:09, "document snapshot-pinning doctrine for large deletable trees") touched AGENTS.md + TODO_LIST + jan.md + storage.md — the framing's birth; `50c8adb6` (00:21, "task status report + self-review for the pinning-doctrine sweep") — the self-review correction; `22a4e88f` (11:44, "reconcile root @ snapshot retention — weekly buckets are calendar-anchored, pin window is 2w sharp") — exactly 3 files, +3/−3, the removal. Dates, messages, and footprints all match run 1's a1 narrative. | `git log -1` ×3 + `git show --stat` on `dbe48a7d`/`22a4e88f`, run live this session |
| a4 | **Protocol step 3 — run 1's §f swept for un-landed follow-ups: NONE.** f1 `[folded]` onto `pipeline.md:155` (landed by run 1), f2 fix leads `[queued]` on the same row, f3/f4 `[queued]` (`TODO_LIST:52`/`:51`), f5 `[optional]` by explicit judgment. This re-fire is verification-only by evidence, not by luck. | `2026-09-26_00-16_task-…fefa.md` §f read in full this run |
| a5 | **Protocol step 4 — footer-bearing landing:** this report, committed with the task footer, so the dispatch is attributable in `git log` (the queue derives completion from the footer). | this file + commit |
| a6 | **Repo left unbroken:** turn 1 changed nothing; turn 2 adds docs-only markdown. No build surface, no unit, no eval surface touched; no tests owed. | `git status` before/after; diff scope |

## b) PARTIALLY DONE

| # | Item | What's done | What's missing | Effort |
|---|------|-------------|----------------|--------|
| b1 | Turn-1 verification depth | Bookkeeping verification (footers, `[x]` marks, surface consistency) was complete in turn 1. | Protocol step 2 (live evidence spot-check) was skipped in turn 1 and executed only in turn 2 because this message demanded a report. See d3. | done as of this report |
| b2 | The `§f.41` origin-report leg | Run 1 read it in full (`archived/2026-09-11_05-32_…:167-169` — the deletable-files sweep ask, no prune-gap evidence) and its resolution is quoted verbatim in run 1's a2. | THIS run took that reading on trust rather than re-opening the file — the load-bearing leg (framing provenance, a3) was spot-checked instead. Deliberate scope call for a verification-only dispatch; flagged per the "answer the question the item asked" rule. | S (optional) |
| b3 | The tracked flake-check gate row (`pipeline.md:155`) | Run 1 appended the RECURRENCE 2026-09-26 evidence (fifth intermittent shape + the `ix80s6aj` shape outliving the heal). | Fix leads unchanged and owner-gated: the sudo `nix-daemon` restart probe and the deploy/reboot wave carrying `af87ce31`+`d827f478`. Nothing agent-side remains. | owner-gated |

## c) NOT STARTED

Nothing within the work item's contract was left unstarted — the item was already closed before this dispatch, and this run's protocol obligations are complete.

| # | Item | Why not started |
|---|------|-----------------|
| c1 | Sibling queued storage items — snapshots.nix calendar-anchor comment (`TODO_LIST:51`), `@home-hermes` 09-16 snapshot probe (`TODO_LIST:52`) | Separate dispatches with their own rows; touching them here is scope creep |
| c2 | New queue-row appends from this run | The one genuinely new-looking finding (dispatch-time dedup) turned out ALREADY tracked (`pipeline.md:157` Queue dedup preflight, with its 4-IDs/7-fires evidence) — dedupe won per the harvest rule; this run is filed as another data point on that row via this report, not as a duplicate row |
| c3 | Any attempt at the gate's fix leads (nix-daemon restart, deploy/reboot wave) | Owner-gated (sudo / deploy); queued on `pipeline.md:155`, pointed at, not attempted |

## d) TOTALLY FUCKED UP

Nothing shipped is broken (zero code touched; surfaces consistent; footer verified). Four process failures, all admitted:

- **d1 — THE RE-DISPATCH ITSELF (queue-side, the headline).** A completed, closed, footer-committed, reported task was re-fired ~9 hours later. The cost this time was one wasted agent run; the RISK was a second agent redoing archaeology, double-committing, or drifting the closed entries — avoided only because turn 1 grepped before acting. The damning part: the fix (dedup preflight) has existed as a `[ready]` queue row (`pipeline.md:157`) since 09-25 with SEVEN prior fires cited, and it still exists only as a row. The systemic failure is not that re-dispatches happen; it is that the countermeasure keeps not becoming code.
- **d2 — Skill-load-first, FIFTH consecutive session on this chain.** Turn 1's verb was literally "re-verify … evidence" — docs-health VERIFY territory — and I grepped/viewed/verified without loading any matching skill first. It is the least-harm instance on the chain (zero edits), but the confession count increments again and the structural lever (prior run's §g2) is still parked. Four prior sessions confessed the same miss; I am the fifth.
- **d3 — Turn-1 verification was a rubber-stamp pattern.** I verified the closure MARKERS (footer, `[x]`, file entries) without touching the verdict's EVIDENCE — exactly the gap protocol step 2 exists to close. If run 1's archaeology had been fabricated, turn 1 would have stamped it clean. The spot-check happened only because the report demand forced a second pass. A verification protocol that runs when convenient is not a protocol.
- **d4 — Wrong-tool first, twice.** qmd MCP `get` failed on two repo files (`docs/todo/storage.md`, `TODO_LIST.md` — the MCP's collections cover `cv` only; repo docs were never indexed) before falling back to `view`. Trivial noise, but I reached for the search surface before checking its scope.

## e) WHAT WE SHOULD IMPROVE

1. **Promote the dedup preflight from queue row to tq code.** `pipeline.md:157` already specifies it: skip dispatch when the row is `[x]` AND a footer-bearing commit exists AND the `docs/status/*_task-<ID>.md` report exists — plus its own hard-won caveat (the "follow-ups harvested" leg / self-harvest norm so a naive skip cannot orphan §f work). Every additional re-fire is another confession with zero structural change.
2. **Make the live spot-check the FIRST action of any suspected re-dispatch**, not a report-time afterthought. One `git log`/`git show` command, seconds of cost — it is the difference between "useful re-fire" and "dead queue churn" (the 00-39 precedent proved the value; this run proves the default ordering is wrong).
3. **Skill activation before the first tool call** — same structural question, now five confessions deep on one chain (crush-config hook that surfaces matching-skill names; owner lever, see §g2).
4. **Pointer precision in queue rows** (run 1's e5, re-costed by this dispatch): cite the evidence-BEARING report, not the origin ask — "05-32 §f.41" sent this run looking for a file before run 1's resolution was read. One hop each dispatch, forever, until the rows are reworded.
5. **qmd MCP scope honesty:** either index SystemNix repo docs as a collection or stop treating it as a repo-file search surface in this repo's sessions; a search tool that cannot see the searched files costs a round-trip every time it is tried first.

## f) Up to 50 things we should get done next

**No new rows were appended to the queue by this run** — every item below is `[queued]`/`[folded]`/observed on an existing tracked surface (HARVEST dedupes, never duplicates). Sources: `TODO_LIST.md` rows read this run, `docs/todo/storage.md:15-20`, `docs/todo/pipeline.md:155/157/158/159`, and run 1's report. The queue holds **283 unchecked items** total; this is the slice visible from THIS task's reads, roughly impact-ordered.

| # | Task | Cat | Where tracked |
|---|------|-----|---------------|
| 1 | Implement the queue dedup preflight as tq code (skip on `[x]` + footer-commit + report-file; keep the follow-ups-harvested caveat) — this run is re-fire #8+ on the class | Bug | pipeline.md:157 |
| 2 | Owner deploy + reboot wave carrying `af87ce31`+`d827f478` (tmpfiles boot-cycle durable fixes) — unblocks the gate row, the `/run/binfmt` class, and the hot-db cascade | Bug | pipeline.md:155, TODO_LIST:109-area stability row |
| 3 | `sudo systemctl restart nix-daemon` probe for the `ix80s6aj…-source` stale-fetch-cache shape (outlived the binfmt heal; pacing question open since run 1 §g1) | Bug | pipeline.md:155 |
| 4 | `@home-hermes` 09-16 snapshot investigation — the ONLY candidate REAL over-retention left; if real, it is the incident the removed framing gestured at | Quality | TODO_LIST:52 / storage.md:16 |
| 5 | Calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix — the authoring-time fix for this entire framing episode | Docs | TODO_LIST:51 / storage.md:15 |
| 6 | Codify "status reports self-harvest §f at authoring time" (one sentence, AGENTS.md TODO-system section) | Docs | pipeline.md:158 |
| 7 | Codify the re-dispatch verification protocol in docs/CONTRIBUTING.md (this run is its second live execution; write down what worked) | Docs | pipeline.md:159 |
| 8 | Commit-msg hook leg: reject subjects >72 chars | Quality | TODO_LIST:56 |
| 9 | Classify tq verify failures before burning attempts (environmental vs introduced) | Quality | TODO_LIST:57 |
| 10 | Gate-triage runbook paragraph in docs/CONTRIBUTING.md (binfmt → clean-HEAD check → nix-daemon → IO pressure) | Docs | TODO_LIST:58 |
| 11 | Heal-attribution breadcrumb convention for manual system heals | Quality | TODO_LIST:59 |
| 12 | Dedupe the ffprobe-sweep rows in docs/todo/pixel6.md | Docs | TODO_LIST:60 |
| 13 | Crush-DB migration: baseline + follow-through | Quality | TODO_LIST:61 |
| 14 | Rebuild `hot-db` + `crush-hot-db` VM tests green on the current tree (quiet-IO window) | Bug | TODO_LIST:62 |
| 15 | Samsung 2nd boot disk (boot-mirror): activation + reboot (landed 2026-09-19, not live) | Infra | TODO_LIST:63 |
| 16 | T14 hot-db tier monitoring: mount-presence metric (fail-closed) + Gatus for `/mnt/hot` | Infra | TODO_LIST:64 |
| 17 | `migrate-hot-db.sh` stub-fixture test BEFORE its first user migration window | Quality | TODO_LIST:65 |
| 18 | Update the Phase-2 plan doc's T-task table with completion status | Docs | TODO_LIST:66 |
| 19 | buildcache-init must provision the new fallback targets (pnpm-cache, pnpm-state, cargo/registry) | Bug | TODO_LIST:69 |
| 20 | Single-source the env-less cache reap inventories (four hand-kept loops) | Quality | TODO_LIST:70 |
| 21 | Restic repo post-deploy proof chain: first run + `restic check` + restore smoke + dedup ratio | Quality | TODO_LIST:71 |
| 22 | Paperless DR completion: pg_restore drill + dump-integrity gate | Quality | TODO_LIST:72 |
| 23 | Stub/fixture test for the destructive `discordsync-attachments-migrate` oneshot BEFORE the deploy that triggers it | Quality | TODO_LIST:73 |
| 24 | Converge `~/.npm` (103M stale) + give `~/tmp/go-lint` (1.8G live) a reclamation path | Quality | TODO_LIST:74 |
| 25 | VM test for the offsite-borg module BEFORE go-live (zero coverage on backup.nix) | Quality | TODO_LIST:77 |
| 26 | storage.md cache-sweep lineage annotations | Docs | TODO_LIST:78 |
| 27 | ClickHouse native-BACKUP restore drill: script + runbook section | Quality | TODO_LIST:80 |
| 28 | Repo-wide sweep for hardcoded help-slicer ranges in `scripts/` | Quality | TODO_LIST:81 |
| 29 | DR-runbook provenance checklist line in docs/CONTRIBUTING.md | Docs | TODO_LIST:82 |
| 30 | Extract the offsite-borg positive-render guard into a shared pure lib function | Quality | TODO_LIST:86 |
| 31 | Pin the pre-deploy §13 classifier's eval shapes in a committed fixture | Quality | TODO_LIST:87 |
| 32 | VM rehearsal of offsite-borg runtime FAIL shapes (mock sops, tripwire + PLACEHOLDER) | Quality | TODO_LIST:88 |
| 33 | Fix §16's enable-blind SKIP (`ob_post_deploy` claims not-deployed without reading the enable flag) | Bug | TODO_LIST:89 |
| 34 | Locate the EIO /data inode's PATH (root 256, ino 1331118) before the offsite go-live flip | Quality | TODO_LIST:90 |
| 35 | `borg-restore-drill-fixture` unknown-flag drop in the borg stub (junk paths) | Bug | TODO_LIST:53 |
| 36 | `[blocked:user]` Phase-2 hot DBs off the QLC root (pocket-id/postgres/discordsync waves) | Infra | TODO_LIST:20 |
| 37 | `[blocked:user]` /data corruption repair: gate (b) scrub delta still unrun; keep btrbk-data failing as the tripwire | Infra | storage.md:17 |
| 38 | `[blocked:user]` Mystery snapshot `data.20260905T2330` root-cause (root forensics) | Quality | storage.md:18 |
| 39 | `[blocked:user]` Deploy authority: queue-fired vs user-manual `nix run .#deploy` | Decision | TODO_LIST:75 |
| 40 | `[blocked:user]` Restic repo's offsite role: local dedup only vs THE offsite leg | Decision | TODO_LIST:76 |
| 41 | `[blocked:user]` Offsite-borg go-live: recovery-copy policy gate (passphrase custody) | Decision | TODO_LIST:79 |
| 42 | `[blocked:user]` ClickHouse dump third-copy placement | Decision | TODO_LIST:83 |
| 43 | `[blocked:user]` Borg real-repo drill verify semantics (live-files vs manifest comparison) | Decision | TODO_LIST:84 |
| 44 | `[blocked:user]` Borg passphrase recovery copy (off-host + proven to decrypt) | Decision | TODO_LIST:85 |
| 45 | `[watch]` Post-DAS-recovery convergence final confirmation — its stated windows (Sep 17-19) have passed; needs a fresh re-check round or a formal close decision, not silent aging | Quality | storage.md:20 |
| 46 | `[folded]` `borg-restore-drill-fixture` intermittent toFile-source eval (fifth gate shape) — stays on the gate row per one-ask-per-row; split only if pacing demands | Bug | pipeline.md:155 |
| 47 | Keep recording gate-flap recurrence evidence on the tracked row (per its own bypass doctrine) until the fix lands | Docs | pipeline.md:155 |
| 48 | `[optional]` qmd MCP scope: index SystemNix docs or stop offering it for repo files (d4) — tiny; file only if it recurs | Tooling | this report §e5 |
| 49 | `[optional]` Reword evidence-pointers in queue rows to the evidence-bearing report (e4/e5 class) — sweep opportunistically on touch, not as its own dispatch | Docs | this report §e4 |
| 50 | `[optional]` Live corroboration of the MISDIAGNOSIS verdict (`.snapshots` listing + btrbk journal since 09-12) — run 1 judged it optional (provenance, not retention, was the question); still optional | Quality | run 1 §f5 |

## g) Questions I can NOT figure out myself

1. **When does the dedup preflight stop being a queue row?** `pipeline.md:157` has now accumulated eight-plus live re-fires across its evidence base (4 IDs / 7 fires in one window, this run included). Is the intended owner tq-upstream (go-taskqueue) or queue-config-side, and should it get a deadline or priority bump — or is each wasted verification-only run considered acceptable pricing for the "follow-ups harvested" safety it preserves? I cannot implement it from this repo either way.
2. **Skill-load-first, structurally (same question run 1 asked, now 5 confessions deep):** do you want a structural lever pulled (e.g., a crush-config hook that surfaces matching-skill names before the first tool call), or does it stay per-session discipline? I can change my own behavior within a session but cannot install my own guardrails — the lever lives in the crush-config repo.
3. **The `[watch]` Post-DAS convergence row (storage.md:20):** its confirmation windows (Sep 17-19 btrbk-root sends and the 3-day freshness gate) have long passed. Should it be re-fired as a fresh verification dispatch (one read-only re-check would close it), or is it superseded by the newer backup-health rows and should be formally closed? I did not re-check it this run (outside this task's scope), so the row is aging without a decision.
