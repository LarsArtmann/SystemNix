# Task Report: task-000001a0d6d4440f02adfdb7dddfd259e173 — Gate-Fix Verification + Stale-Row Correction

| Field | Value |
| ----- | ----- |
| Task | Fix the pre-existing `checks.disko-layout` eval failure / dead pre-commit flake-check gate (TODO_LIST storage row; library: pipeline.md) |
| Date | 2026-09-25 06:45 |
| Run | **Re-dispatch verification + close-out** — the item's ask (fix the gate) resolved as environmental + an already-landed in-tree durable fix; this run's product is fresh green-gate evidence, the deployed-state proof, stale-instruction removal, and cross-row annotations |
| Scope | THIS task's work only; the boot-cycle fix itself belongs to the stability row (TODO_LIST 109) and was NOT re-implemented here |

## TL;DR

The gate is **GREEN at HEAD `97711fdd`** — this run's own full `nix flake check --no-build --keep-going` ended `all checks passed!` (rc=0), the exact check that died three times since 2026-09-24. The deaths were environmental (the `hot-user-caches-nix-bootstrap` ↔ `systemd-tmpfiles-setup` boot-ordering cycle leaving `/run/binfmt` missing after the 2026-09-24 22:35 boot; manually healed 05:32), **not** a repo defect — so the row's original ask has no code to fix. The durable fixes are **already landed in-tree but UNDEPLOYED**: `af87ce31` 03:10 (bootstrap re-anchored to the on-demand `.mount` + `tests/test-hot-user-caches.nix` tmpfiles boot tripwire) and `d827f478` 04:28 (`binfmt-sandbox-dir` mkdir belt). The deployed host still runs **gen 797 with the OLD automount-anchored wiring** (read from `/run/current-system` unit text, 06:37), so a reboot BEFORE the next deploy re-manifests the cycle and re-kills the gate. The row therefore stays open with a BLOCKED marker: post-reboot proof needs an owner deploy + reboot, neither agent-runnable. Also removed the row's stale "every commit needs `--no-verify`" instruction — a false premise flagged as the bypass-teaching class by the 06:03 report.

## a) FULLY DONE

1. **Fresh same-session gate proof** — `nix flake check --no-build --keep-going` at HEAD (rc=0, "all checks passed!"; only the documented aarch64-darwin omission warning). Third green data point today (00:33-ish per 05:33 report, 05:33, 06:34 this run) and the first recorded by THIS task's ID.
2. **Deployed-state probe (the decisive new fact)** — `/run/current-system/etc/systemd/system/hot-user-caches-nix-bootstrap.service` still carries `Before=/WantedBy=home-lars-.cache-nix.automount`; profile = `system-797-link`. The fix commits postdate the boot (03:10/04:28 today vs 22:35 yesterday) and no deploy has anchored since. Consequence: the green gate is boot-ephemeral until a deploy lands — quantified in the row's BLOCKED line.
3. **Durable-fix inventory located and verified in-tree** — `af87ce31` (4 files: hot-user-caches.nix, deploy.sh, tests/default.nix, test-hot-user-caches.nix) and `d827f478` (boot.nix `binfmt-sandbox-dir`, gated to the same binfmt-enabled condition). No new work invented; the stability row (TODO 109) already owns what remains (quiet-window VM-test run, deploy, reboot-verify) and is now annotated so its next dispatch starts from facts.
4. **Stale false-premise instruction removed** — pipeline.md's "**every commit needs `--no-verify`**" replaced with the corrected consequence text + pointer to the 06:03 report that flagged the class; agents currently read that line as license to bypass a healthy gate.
5. **Re-check one-liner added to the row** (`ls /run/binfmt && nix flake check --no-build`) per the time-sensitive-row convention (06:03 §f1) — the next agent confirms-or-closes in one command.
6. **Pairwise annotations, no drift** — TODO_LIST 43 ↔ pipeline.md; TODO_LIST 112 ↔ stability.md; this report.
7. **Queue state untouched** — no tq DB mutation; the task closes through the normal verify path.

## b) PARTIALLY DONE

1. **The item itself** — everything agent-side is done (verify, classify, annotate); the named residual "post-reboot proof" is owner-gated by construction (deploy + reboot). Left unchecked with `— BLOCKED:` per contract rather than closed, honoring the row's own keep-open instruction.
2. **disko-layout isolation at BUILD level** — the check evals green (drvPath + full flake check), but the full VM rehearsal was deliberately NOT run: IO PSI some avg10 was 38% at run start (above the 20% deploy-gate floor; VM tests are heavy-job territory). Eval-level evidence + the formatter/hermes checks passing under `--keep-going` is sufficient for the gate claim; the VM-boot proof rides the post-deploy window (tracked on the row).

## c) NOT STARTED (deliberately; owner-gated or other rows' work)

1. Owner deploy carrying `af87ce31` + `d827f478`, then reboot, then the post-reboot probe (gate green on a FRESH boot without any manual heal — that is the proof the environmental attribution is complete).
2. `tests/test-hot-user-caches.nix` VM run in a quiet window (stability row 109's remaining leg).

## d) TOTALLY FUCKED UP (what I forgot; honest)

1. **First PSI probe was sloppy** — I grepped `/proc/pressure/io` for the string "psi" (always empty; the files contain `avg10=…` lines), got a blank reading, and nearly proceeded on it. Corrected to `cat` immediately. A wrong-direction "PSI is zero" here could have licensed a heavy VM build under a 38% storm.
2. **HEAD moved under me mid-run** (28a727d1 → 97711fdd, a parallel session's self-review commit landing between my check start and finish). The gate green was earned at ~28a727d1 and the delta is docs-only, but the row citation names `97711fdd` post-hoc — the pre-commit hook re-running the check on MY commit is what makes the citation honest.
3. **The heal's actor is still unknown** (third report in a row to say so) — nothing this run can do about it; the breadcrumb-convention row (stability.md) remains the fix.

## e) WHAT WE SHOULD IMPROVE

1. Queue rows that assert a live environmental state should lead with the re-check command, not bury it at the end (applied here; convention already queued in pipeline.md).
2. When a row's fix lands in-tree via daemon "heuristic" commits while the row still says "unlanded", the next dispatch burns time re-diagnosing — the pairwise UPDATE pattern used here (fix-commit SHAs + undeployed state) is the cheap cure.

## f) NEXT THINGS (NEW = this run; QUEUED = already tracked)

1. QUEUED — owner deploy + reboot + post-reboot gate probe (this row's BLOCKED residual).
2. QUEUED — `tests/test-hot-user-caches.nix` quiet-window VM run (stability row 109).
3. QUEUED — heal-attribution breadcrumb convention (stability.md).
4. QUEUED — gate-triage runbook + verify-failure classification rows (pipeline.md, from the 05:33 attempt arc).

Nothing NEW worth queueing: every follow-up found this run is already owned by an open row (checked before writing).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Who runs the deploy?** The gate's health past the next reboot depends on `af87ce31` + `d827f478` reaching `/run/current-system`; IO PSI is currently above the deploy gate, so this is a quiet-window owner action. Until then, if `/run/binfmt` dies again: `sudo systemd-tmpfiles --create --remove --exclude-prefix=/dev` (documented manual heal).
