# Task 000001a0c618c3adc95abca642eadda2dd3c — Restic repo on pool for app dumps (dedup)

**Date:** 2026-09-22 01:00 CEST
**Queue item:** TODO_LIST.md → storage → "restic repo on pool for app dumps (dedup)" (plan T17, `docs/planning/archived/2026-08-17_14-41_data-corruption-recovery-pool-completion-master-plan.md` §backlog)
**Commits:** `49cfb914` (code: module + configuration.nix + deploy.sh, daemon-swept then verified), `36a946d2` (docs close-out, carries the Task-Queue-ID footer — queue-ID-remap precedent)

---

## a) FULLY DONE

1. **Module `modules/nixos/services/restic-app-dumps.nix`** — flake-parts wrapper (auto-discovered by filename), `services.restic-app-dumps.enable` + `paths` option (default: the 10 app-dump dirs — cv, forgejo, geometrikks, inboxclean, manifest, miniflux, paperless, pocket-id, signal-backups, twenty).
2. **Restic repo via the nixpkgs module** — `services.restic.backups.app-dumps`: local repo at `/mnt/pool/backups/restic-app-dumps`, `initialize = true` (a lost pool re-initializes on the first post-recreate run — the repo dir itself needs NO bootstrap oneshot, restic init mkdirs), `--tag app-dumps`, bounded retention `--keep-daily 14 --keep-weekly 8` (the pool carries btrbk root receives FOREVER; this layer must not grow unbounded on top).
3. **Timer** — nightly 05:45 + 10m jitter, `Persistent` (staggered after geometrikks 05:15; the nixpkgs module stamps `X-OnlyManualStart` + `restartIfChanged = false` on its own).
4. **Password bootstrap** — `restic-app-dumps-setup` oneshot (`wantedBy = multi-user.target` + `RemainAfterExit`, tq-storage-dir shape so deploy.sh's is-enabled provisioner gate passes): creates `/var/lib/restic-app-dumps/password` once (44-char base64, umask 077 → 0600). Deliberately NO sops (searxng-secret-key pattern): the repo is pool-local until the offsite Borg leg exists — losing the host loses the repo either way; documented in AGENTS.md.
5. **deploy.sh provisioner entry** — `restic-app-dumps-setup` added to the restart loop (the name matches the deploy-restart-audit `-setup` converger pattern, so this wiring is eval-ENFORCED, not convention).
6. **Monitoring, no new surface** — `ExecStartPost` touches `/mnt/pool/backups/restic-app-dumps/.last_success` ONLY when backup + forget + prune all succeed; integration registry entry (`vHost.layer = "none"`, no port/subdomain — pool-side infra) fans the `backup = { directory, filePattern = ".last_success", maxAgeHours = 25 }` row into backup-coordination → `backup_healthy{backup="restic-app-dumps"}` + the existing aggregate "All Backups Healthy" Gatus check. Plus OnFailure (`notify-failure@%n`) on both units.
7. **Resource shape** — ioTier.background (BE/6), `MemoryMax = 4G`, `TimeoutStartSec = 12h` (first seed is tens of GB onto the HDD pool; global 3min default would have killed it), `unitConfig.RequiresMountsFor = [ "/mnt/pool" ]` (DAS outage fails the unit loudly instead of writing to a root-fs shadow dir — the 226 class).
8. **Verification** — `nix eval` of the evo-x2 toplevel drv clean; `nix flake check --no-build` all-passed (this also exercised deploy-restart-audit, systemd-shape-audit, mount-gating-audit, gatus-pattern-lint, sops-key-audit over the new module); rendered units spot-checked via `nix eval --json` (ExecStartPost marker, MemoryMax, TimeoutStartSec, IOSchedulingPriority 6, timer OnCalendar/Persistent/RandomizedDelaySec, backup-coordination row); `nix fmt -- --ci` 0 changed; `bash -n scripts/deploy.sh` OK; pre-commit hook green (gitleaks + eval-only flake check) on both commits.
9. **Queue + docs close-out** — TODO_LIST.md item `[x]` with run note; `docs/todo/storage.md` library row `[x]` with the full engineering summary; one AGENTS.md bullet in the HDD Backup Pool section (repo shape, password location, exclusions); follow-up VM-test item appended to queue + library (allowed new-item route).
10. **Excluded by design (documented)** — `root`/`data` (btrbk-owned btrfs receives), `forgejo-subvol`/`github-voice-corpus`/`pixel6` (extend `services.restic-app-dumps.paths` if wanted).

## b) PARTIALLY DONE

1. **Live verification is deploy-gated** — the module is eval-verified only. First real run (password creation → `restic init` → first backup → prune → marker), `restic check`, and one restore drill have NOT happened. Per house doctrine (a verification close-out must answer the question the item asked): the dedup QUESTION ("forgejo zips share ~0 extents — how much does chunk-level dedup actually recover across nightly zips?") is UNANSWERED until the first two nightly runs and a `compsize`/`du` comparison of the repo vs the source dirs.
2. **The daemon/commit shape** — the code rode a heuristic daemon commit (`49cfb914`) because the daemon swept it mid-session; my amend attempt lost a race to the daemon (see d.1). The Task-Queue-ID footer lives on the docs commit (`36a946d2`), cross-referencing via the established queue-ID-remap precedent — functional, but the code commit itself does not carry the footer.

## c) NOT STARTED

1. **VM test** (`tests/test-restic-app-dumps.nix`) — queued as a new `[ready]` item: password-bootstrap idempotence, `restic init` on a missing repo, marker only on success (failure-path negative), backup-coordination row picks up the marker.
2. **Offsite leg for the restic repo** — the repo is pool-local (same-chassis HDD RAID1). The item that would actually close the ransomware/dual-failure gap is the Hetzner Borg leg (separate queue item, untouched).
3. **SigNoz dashboard panel / dedicated Gatus check for restic internals** (repo size, snapshot count, prune outcomes) — deliberately skipped for smallest-correct-change; backup-coordination freshness + OnFailure is the monitoring floor.

## d) TOTALLY FUCKED UP

1. **Lost the amend race to the auto-commit daemon** — my first `git commit --amend` (turning the daemon's `49cfb914` into a properly-messaged commit with the footer) failed `fatal: cannot lock ref 'HEAD': is at 1a7613e6 but expected 49cfb914`: the daemon committed my staged docs between my `git status` check and the amend. Recovered correctly (verified `1a7613e6` contents = exactly my 3 docs files, then amended THAT forward into `36a946d2` with the footer), but the net effect is a two-commit split where one clean commit was the intent. LESSON (already in AGENTS.md, now re-learned live): between "verify what the daemon staged" and "amend", the daemon can commit AGAIN — do the content check and the amend in ONE atomic command, and re-verify HEAD's sha inside the same command chain.
2. **Minor: `docs/todo/storage.md` append landed at file END, not in the Prioritized section** — the new VM-test library entry was appended via `cat >>` (fastest, safe against parallel edits) and sits in the tail section instead of next to the restic row it belongs to. Cosmetically wrong placement, still discoverable (explicit `**Source:**` back-pointer). The TODO_LIST queue one-liner is also at the very bottom rather than in the storage section — same tradeoff.

## e) WHAT WE SHOULD IMPROVE

1. **Amend-after-daemon must be atomic** — house rule update candidate for AGENTS.md multi-agent section: never spread `git show --stat HEAD` (verify) and `git commit --amend` (mutate) across two tool calls; chain them `git show --stat HEAD | <review> && git commit --amend …` or accept the remap precedent from the start.
2. **The T17-class "plan T17" pointer was archaeology** — the queue item pointed at "plan T17" which resolves to a one-line backlog row in an August master plan; a fresh implementer had to reconstruct scope (which dirs? retention? password custody?) from AGENTS.md conventions. Queue items for infra work should carry the 3-4 design decisions inline (they were all inferable here, but it cost several greps).
3. **Eval-only verification is the floor, not the bar** — every "deploy-pending" module accumulates the same gap (first-run behavior, negative paths). The VM-test follow-up pattern is right; consider making "new backup/backup-adjacent module ⇒ committed VM test in the same run" the default rather than a queued follow-up.

## f) NEXT (up to 50, ordered by impact)

1. **Deploy** (user sudo window) so the module stops being paper — first run creates password + repo + first snapshot.
2. **After first run: `restic check` + one-file restore drill** — prove the repo is a restore path, not just bytes written.
3. **Measure the dedup win** — `du`/`compsize` on the restic repo after ≥3 nightly runs vs source dirs; validates the item's premise (forgejo zips) or falsifies it (deflate streams may dedup poorly — in which case reconsider retention).
4. **VM test** (already queued): `tests/test-restic-app-dumps.nix` incl. the marker-on-failure negative.
5. **Watch the aggregate Gatus check for a false "All Backups Healthy" regression**: restic's 25h freshness gate vs the 05:45+10m schedule has ~5h slack — fine normally, but a skipped run (guard Zone-6 churn-stop during the 05:45 window) stays green until 06:55 next day; acceptable, but confirm the alert latency is understood.
6. **Confirm the 05:45 slot doesn't collide with pool-heavy neighbors** (geometrikks 05:15 dump, pool SMART collector every 5m, btrbk-pool-clean 23:50) — first weeks of journal watch.
7. **`restic forget --prune` runtime under MemoryMax 4G**: if the repo grows past ~200G, prune's index rebuild may need more headroom — revisit ceiling on first prune.
8. **Consider `--exclude` hardening**: none of the 10 dump dirs should contain symlinks into the restic repo (self-inclusion loop) — restic refuses repo-inside-source, but an explicit sanity note in the module header is cheap.
9. **Bandwidth shaping**: first seed at BE/6 on the single USB DAS link — if it lands during the morning guard window, consider a `heavy-job` wrap or a later OnCalendar (user call).
10. **Decide whether `signal-backups` belongs** — it rode the default 10-dir list without a per-service dump unit owning it; confirm it is an app-dump dir and not a live data dir (a `ls` I did NOT run — see g.3).
11. Same confirm for `inboxclean` (its dump dir exists on the pool; the papersync ledger lives inside the service dataDir — what exactly is in `/mnt/pool/backups/inboxclean`?).
12. **Offsite Borg leg** (existing queue item) — the restic repo increases the value of pool-local data and therefore the offsite gap.
13. **SigNoz panel** for `backup_age_hours{backup="restic-app-dumps"}` trend on the storage dashboard (cheap addition to the existing provisioner spec).
14. **CHANGELOG.md entry** for the restic module when it goes live (the `[x]` rows prune there at the next pass).
15. **`docs/services/` runbook stub** — every service has one (`docs/services/restic-app-dumps.md`: password recovery (it cannot be recovered — re-init), restore recipe, manual run commands). Skipped this run; queue it.
16. **Eval-time guard idea**: reject `services.restic.backups.*` entries whose repository path sits under a `nofail` mount without `RequiresMountsFor` (the mount-gating audit only covers ReadWritePaths today — this unit is ungated but I added RequiresMountsFor by hand; generalize it).
17. **Daemon-race hardening** (from e.1) — AGENTS.md one-liner.
18. **Restic wrapper CLI** on PATH (`restic-app-dumps snapshots|restore` wrapper like the nixpkgs module's `createWrapper`) — manual ops today require hand-building env vars; `createWrapper = true` (module default true?) gives a free `restic-app-dumps` command — VERIFY the option and flip if off.
19. **Prune jitter** — backup + prune in one unit means a big prune stretches the 12h ceiling; consider splitting prune to its own weekly timer if first prune exceeds ~30min.
20. **Retention cross-check with forgejo's own 7d zip retention**: restic keeps daily-14 of what forgejo keeps 7 of — the dedup value lives in the overlap; if forgejo retention ever drops below ~3 days the dedup premise weakens. Note for the next forgejo-backup touch.
21. **Add the repo dir to `pool-subvols-ensure`-style declarative creation** if that queued item (storage.md #5 of the quality row) lands — restic init currently owns dir creation implicitly.
22. **restic cache location**: module defaults `CacheDirectory = restic-backups-app-dumps` on the ROOT fs (`/var/cache`) — small (index cache), but on this QLC box every dir is guilty until measured; check size after first run, relocate to `/mnt/hot` if >1G.
23. **fsync/corruption posture**: the pool is btrfs RAID1 + zstd — restic's own integrity (check) is the second line; schedule a monthly `restic check --read-data-subset` if the repo becomes load-bearing.
24. **Documentation of the 25h freshness semantics for the marker** — backup-coordination's find uses mtime; `touch` per run is right; confirm no prune pass rewrites `.last_success` mtime (it doesn't touch the repo root file — verified by construction, re-verify live).
25. **Consider `inhibitsSleep = true`** on the backup if desktop suspend could interrupt a long seed (systemd-inhibit; the box is a desktop — first seed at 05:45 while the user sleeps is fine, but a manual re-run mid-session might get suspended mid-write; restic resumes cleanly, note only).
26. **Queue-item hygiene**: the "plan T17" back-pointer can now be updated to point at this run's report (done in storage.md row — the master-plan T17 row itself remains stale; a one-line "landed 2026-09-22" edit there is a trivial follow-up).
27. **`backup-coordination` comment in configuration.nix** says "All backup freshness rows moved to their owning modules" — mine lives in the module too (consistent), no drift. No action; recorded as verified.
28. **Cross-check `restic` in `lib/images.nix`/port registry** — no port, no image; the port-registry audit passed, confirming no accidental literals. No action.
29. **Long-term**: if the dedup win is large, consider pointing the offsite Borg job AT the restic repo (one dedup layer feeds both) vs Borg re-dedup of the same sources — design question for the Hetzner item, not this one.
30. **Everything else already queued in storage.md** (data corruption repair, hot-db Phase 2, ClickHouse backup coverage) remains untouched and higher-priority than items 13-29 of this list.

(30 items — the tail beyond that would be padding; the queue gates decide.)

## g) QUESTIONS I COULD NOT ANSWER MYSELF

1. **Is `signal-backups` an app-dump dir (safe to mirror into restic) or live data?** It exists under `/mnt/pool/backups` but no owning dump unit is visible in configuration.nix from this session; if it is a live copy of Signal export state, mirroring it is still harmless, but the freshness row semantics differ. I could not determine its writer without more pool-side inspection (sudo needed for some paths).
2. **Same for `inboxclean`:** is `/mnt/pool/backups/inboxclean` populated by a unit I should cross-reference (its own freshness row may already exist via the integration registry), or a leftover from bring-up? Restic mirrors it either way; the question is whether two freshness rows now race on the same dir (harmless) or the dir is stale garbage being enshrined (needs a decision).
3. **Should the restic repo password be promoted to sops anyway?** I chose machine-local (pool-local repo, no sops key addition without host identity — agents cannot add keys to existing sops files). If you ever plan to copy the repo offsite WITHOUT the password file (e.g., restic over rclone to the StorageBox instead of the planned Borg leg), the password must move to sops BEFORE the first real dependence on the repo. One-line answer decides whether I queue the sops migration.

---

**Verification claim discipline (per house rule):** everything marked "done" above is eval-verified or committed; nothing is live-verified. The item's actual QUESTION (dedup recovery on forgejo zips) is explicitly open until run 3 + measurement (f.3).
