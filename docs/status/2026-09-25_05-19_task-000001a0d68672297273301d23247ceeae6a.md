# Task Report: task-000001a0d68672297273301d23247ceeae6a — Full Self-Review

| Field | Value |
| ----- | ----- |
| Task  | Codify the single-victim /data repair recipe (TODO storage row; source: archived 2026-09-12_03-54 §3/§6) |
| Date  | 2026-09-25 05:19 |
| Scope | THIS task's work + what was noticed in passing while doing it. No unrelated research. |
| Companion | In-run record: `docs/status/2026-09-25_05-14_task-000001a0d68672297273301d23247ceeae6a.md` (kept; this report is the full review, not a replacement) |

## TL;DR

The task itself is done and verified: the 3-step /data single-victim repair recipe is codified in jan.md, cross-linked from jan.md's RESOLVED bullet and AGENTS.md's corrupt-GGUF bullet, both queue surfaces closed, and the healthy-side `dd` claim LIVE-verified this round (4096 bytes, rc 0, stamped into jan.md). The honest failures are process-shaped: (1) the daemon-race question was hit for the SECOND time (after 2026-09-12 g-2) and resolved by private judgment again because no policy exists — and my landing could not satisfy my own recipe's "ONE commit" rule under the daemon; (2) the recipe is only discoverable from the Jan surfaces, not from the BTRFS section where a future /data-corruption session would start; (3) I nearly wrote a false claim into this report (CHANGELOG "lost" the gemma row) and caught it only by re-grepping with different terms. All findings are either fixed in this round or queued.

## a) FULLY DONE

1. **Recipe codified** — new `## Single-victim /data repair recipe` section at the end of `docs/services/jan.md`: (1) same-filesystem `trash` (rename, zero copy, `stat`-verified both paths; snapshot-pinning caveat: the trashed copy frees NOTHING until the /data window expires); (2) `dd if=<trashed> bs=4k count=1` header re-verify on the TRASHED copy (damaged ⇒ I/O error os error 5 + `0+0 records in`; healthy ⇒ exactly 4096 bytes), run UNMASKED (the `| tail` pipeline-masking trap, taken from the 09-12 report's own §d1 near-miss); (3) atomic doc sweep — `grep -rn "<filename>" --include='*.md'`, update every live surface (TODO row, service runbook, AGENTS.md) in ONE commit; `docs/status/**` archives exempt; re-download preconditions (recorded source URL + expected size + pre-import header validation).
2. **Cross-links live** — jan.md's RESOLVED bullet ("Repair procedure for the NEXT victim: the recipe below") and AGENTS.md's Jan-section "Corrupt GGUF RESOLVED" bullet both point at the recipe; the grep over `*.md` confirmed those are the only two LIVE surfaces (TODO_LIST was pruned; status reports are frozen archives).
3. **Queue closed without drift** — TODO_LIST row `[x]` with DONE note + docs/todo/storage.md library row `[x]` with the full note, edited in the same change.
4. **Footer-bearing commit landed, never pushed** — `Task-Queue-ID:` footer exact; attribution preserved via the repo's established landing-on-top pattern.
5. **Pre-existing gate proven before bypassing** — baseline `nix flake check --no-build` at clean HEAD (`4f3f552f`, BEFORE any edit) failed with the documented `/run/binfmt` shape (the pipeline queue row's "SECOND instance observed 2026-09-25" class), so the `--no-verify` is proven-narrow to that dead leg; gitleaks (staged tree, hook-exact invocation), check-todo-system, push-protection, and identity guards all run manually green.
6. **Daemon-race contents verified before deciding** — `e660e882` = exactly my jan.md edit (+9/−1), `1d54d169` = exactly my three one-line edits; nothing foreign absorbed into either daemon commit; tree clean after.
7. **Healthy-side claim upgraded from cited to LIVE-verified (this review round)** — `dd bs=4k count=1` on a real healthy jan GGUF (`gemma-4-26B-A4B-heretic-APEX-Balanced/mmproj.gguf`): `1+0 records in / 1+0 records out / 4096 bytes copied`, rc 0; jan.md step 2 now carries the live stamp with the date. This closes the biggest verify-external-claims gap the first pass left open.
8. **§f sweep of the source report executed** — found one un-landed follow-up behind the closed work (the trash-purge policy, 09-12 §c3) and queued it as a `[decision]` library row.

## b) PARTIALLY DONE

1. **Recipe discoverability for NON-Jan victims** — the recipe lives in jan.md + the Jan-section AGENTS bullet only. A future /data-corruption session that starts at the AGENTS.md BTRFS section (snapshot-pinning doctrine, the P0 corruption row) finds no pointer. It is a one-liner I judged out of the "smallest correct change" scope in-run — on reflection that judgment was too tight; it is now queued (`[ready]`, storage) rather than landed.
2. **My own landing violated the recipe's step-3 letter** — "update every surface in ONE commit" did not happen: the daemon swept the content edits into two heuristic commits before my footer-bearing commit landed (three commits total). Content is intact and attributable, but the recipe as written is UNACHIEVABLE under the ~10-min daemon cadence without a carve-out or a pre-stage-and-commit-fast discipline — a flaw in the recipe text I did not notice until this review.
3. **Damaged-side shapes remain cited, not live-proven** — the I/O-error / `0+0 records in` branch rests on the 09-12 session's live evidence; no damaged file exists to re-probe (expected and fine), but the asymmetry with the now-live healthy side should be stated, not just inherited. Stated here; the recipe cites the original evidence date implicitly via the section intro.
4. **The AGENTS.md pointer partially RESTATES the recipe** (compact summary + pointer) rather than only pointing — the established house pattern (every AGENTS bullet summarizes its runbook), but it is a mild split-brain surface: if jan.md's recipe changes shape, the AGENTS summary can drift. Canonical home declared as jan.md; drift risk acknowledged, accepted per house pattern.
5. **In-run status report was thin** — the 05-14 report covers done/verification but not §a–g depth; this report supersedes it for review purposes.

## c) NOT STARTED

1. **Everything the recipe ENABLES but does not include** — deliberately untouched, separately queued or owner-gated: the /data damage-set inventory, the /data corruption repair P0 (scrub gate (b), T04–T08), the GGUF re-download (user-gated), the trash-purge policy decision (now queued). Nothing from THIS task's ask remains open.

## d) TOTALLY FUCKED UP

1. **The daemon-race question was punted twice by two different sessions.** The 2026-09-12 report asked it openly (g-2: amend the verified-single-file daemon HEAD, or land a separate footer-bearing commit on top?) — no policy was ever written. This run hit the identical fork and resolved it OPPOSITELY (09-12 amended; 09-25 landed on top) by private judgment. Two sessions, two different unwritten answers, zero repo record = a systemic process failure, not an individual one. Now queued for codification (pipeline `[ready]`), which is the least it deserves.
2. **Near-miss false claim in THIS report, caught pre-write** — my first CHANGELOG grep (`gemma-4-31b|single-victim`) found nothing and I almost wrote "the 2026-09-12 DONE row was pruned without landing in CHANGELOG". A second grep with looser terms found the row (CHANGELOG line 81 words it "the corrupt /data gemma GGUF trashed", docs-health 2026-09-14 batch). The near-miss is exactly the stale-harvest/false-negative class this repo documents repeatedly; had I trusted the first grep, this report would have contained a lie. No false claim shipped.
3. **Category-d severity in the ARTIFACT: none.** The recipe text is accurate against the source evidence; no code, no config, no service surface was touched; nothing was removed. The failures above are process and discoverability, not damage.

## e) WHAT WE SHOULD IMPROVE

1. **Codify the daemon-race policy** (queued, pipeline): pick amend-verified-single-file vs landing-on-top, write it into CONTRIBUTING/AGENTS, and reconcile every "in ONE commit" docs rule with the daemon cadence (explicit carve-out or pre-stage discipline). This also fixes the recipe's step-3 tension.
2. **Add the BTRFS-side cross-link** (queued, storage): one line near the snapshot-pinning doctrine / P0 corruption row pointing at the jan.md recipe.
3. **Fix the jan.md Architecture-table GPU row** (queued, services): the cell's verify command is truncated mid-pipeline with an unclosed backtick (ends at `pgrep -x llama-server`; the Verification section has the full command) — noticed in passing, one-line repair.
4. **Stamp cited claims with their verification provenance at authoring time** — done this round for the dd healthy-side (live 2026-09-25); the general habit: any command shape inherited from another session's report gets either a live re-probe or an explicit "cited from <report>, not re-verified" marker.
5. **Keep the pre-change-baseline discipline for every gate bypass** — proving the flake-check failure at clean HEAD BEFORE editing is what turned a suspicious `--no-verify` into a proven-narrow one. It worked exactly as the queue row intended; keep it mandatory.
6. **Grep for a negative claim with at least two term-choices before asserting absence** — the CHANGELOG near-miss rule (§d2). Absence claims ("X is not queued/recorded") need a synonym sweep, not one grep.
7. **Considered and rejected:** a flake check asserting jan.md contains the recipe section (presence guard). A doc recipe has no meaningful automated test beyond presence, and presence-guards on prose are low-value noise — the atomic-sweep discipline in the recipe itself is the real guard. Not queued.

## f) NEXT THINGS (up to 50; NEW = appended to the queue this round, QUEUED = already tracked and read this run, untouched/unre-verified)

**NEW — appended this round (queue row + library entry, no drift):**
1. Codify the daemon-race policy for task-queue agents (amend vs landing-on-top + the ONE-commit carve-out) — pipeline `[ready]`.
2. Cross-link the single-victim /data repair recipe from the AGENTS.md BTRFS /data-damage context — storage `[ready]`.
3. Fix the jan.md Architecture-table GPU row (truncated command + unclosed backtick) — services `[ready]`.
4. Trash-purge policy for `/data/.Trash-1000` (owner blessing of "leave it") — storage `[decision]`, library-only (surfaced from the 09-12 report's §c3 §f-sweep).

**QUEUED — adjacent to this task, read this run, NOT re-verified:**

5. Build the /data damage-set inventory (storage) — consumes the recipe's victims.
6. /data corruption repair P0: scrub gate (b) + T04–T08 (storage, blocked:user) — the bounded-damage parent of the whole recipe.
7. Cross-link the flm AGENTS bullet to the snapshot-pinning table (storage) — the same one-line-asymmetry class as NEW-2.
8. Refresh the doctrine table's /data/docker sizing via `docker system df` (storage).
9. Reconcile the root `@` retention anomaly (12-day-old weekly) and restate the root window (storage).
10. Drop unknown flags in the borg-restore-drill fixture's borg stub (storage).
11. Fix the pre-existing flake-check gate failures (`/run/binfmt`/treefmt class) — the leg this run had to bypass (pipeline).
12. Codify the re-dispatch verification protocol in CONTRIBUTING (pipeline).
13. Codify "status reports self-harvest §f at authoring time" (pipeline) — the discipline that surfaced NEW-4.
14. Commit-msg hook leg: reject subjects >72 chars (pipeline).
15. Dedupe the ffprobe-sweep rows in pixel6.md.
16. ClickHouse native-BACKUP restore drill (storage).
17. Repo-wide hardcoded help-slicer ranges sweep (storage).
18. DR-runbook provenance checklist line in CONTRIBUTING (storage).
19. Offsite Borg go-live inputs: StorageBox host/user + passphrase recovery policy + irreplaceable exceptions (storage, owner).
20. Post-go-live restore-drill cadence (storage, decision).
21. After the first real offsite run: archive-vs-exclude audit + runbook sizing (storage, watch).
22. Borg real-repo drill verify semantics: live-`cmp` vs baked manifest (storage, decision).
23. Borg passphrase recovery copy: is it off-host and proven? (storage, decision).
24. Offsite repo append-only posture / ransomware resistance (storage, decision).
25. Replacement-host `borg_known_hosts` re-pin derivation (storage, decision).
26. `borgbackup-job-hetzner` in Zone-6 ioChurnUnits: include-with-re-arm or document-why-not (storage, decision).
27. Make the drill's real-mode mountpoint gate fixture-reachable (storage).
28. Re-verify live BTRFS root chunk-unalloc; cite metrics, not frozen figures (storage, watch).
29. Phase 2: hot DBs off the QLC root — migration waves (storage, owner windows).
30. Pool + disk-domain quality remainder: /mnt/hot scrub family, commit-option assertion, shadow dirs, SLO (storage).
31. Own-tools NVMe→pool migrations: discordsync + browser-history (storage, deploy-gated).
32. Crush-DB migration baseline + Zone-6 follow-through (storage).
33. Rebuild `hot-db` + `crush-hot-db` VM tests green on the current tree (storage, quiet-IO window).
34. Samsung 2nd boot disk (boot-mirror): activation + reboot pending (storage).
35. T14 hot-db tier monitoring: mount-presence metric + Gatus (storage).
36. `migrate-hot-db.sh` stub-fixture test before its first user window (storage).
37. Update the Phase-2 plan doc's T-task table (storage).
38. Free-space triage order doctrine (owner precedence) (storage, decision).
39. flm re-pull headroom gate (owner threshold) (storage, decision).
40. When the flm bump lands: verify old-weights pin clock + projected free date (storage, watch).
41. Post-Borg-go-live: offsite column in the snapshot-pinning doctrine table (storage, watch).
42. Clean the `/mnt/pool` root-fs shadow directory (storage, blocked:user).
43. GGUF re-download decision: `gemma-4-31b-abliterated` exists NOWHERE since the 2026-09-12 deletion — want it back, and from which source? (owner; the recipe's step-3 precondition sits user-gated; not currently a queue row anywhere — flagged here for the owner to resolve or dismiss).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Daemon-race policy:** when the auto-commit daemon sweeps an agent's edits before its commit, is the sanctioned pattern (a) amend the daemon HEAD after verifying it contains exactly your single logical change (the 09-12 choice), or (b) leave the daemon commits alone and land a separate footer-bearing commit on top (my 09-25 choice)? Both have precedent in this repo's history; you own the daemon; I queued the codification either way.
2. **Gemma re-download:** do you want `gemma-4-31b-abliterated-Q8_0` back at all (it has existed nowhere since the 2026-09-12 trash), and if yes, from which source? I will not guess URLs; the recipe's step 3 requires the source URL + expected size recorded in jan.md before any re-download can be validated and imported.
3. **Docs-only gate bypass:** is manual-guards + `--no-verify` (current practice, twice-precedented, proven-narrow via the clean-HEAD baseline) acceptable as the standing pattern while the flake-check gate is dead — or should the hook grow a docs-only fast path (skip only the flake leg when no `.nix`/`.sh`/`.py` is staged) so agents stop bypassing gitleaks-by-association? The fast path is implementable, but it changes the hook's fail-closed contract, so it is your call.
