# Status Report — Task Queue Item `000001a0d13bb97f785bd705387c539b4235` (dispatch #3)

**Date:** 2026-09-24 05:36 CEST
**Work item:** "Persist the offsite-borg positive-path render probe (eval-time flake check: BE/6 + tripwire/marker/borg-env on enable=true)" (TODO_LIST:37 → docs/todo/storage.md:51, Source §f3)
**Repo:** `/home/lars/projects/SystemNix`, tree clean at start and at report time.
**Dispatch chain for this ONE item:** #1 implemented + committed (`14fc6816`, 04:45, footer ✓) → #2 verification-only + report (`3d8a8031`, 05:05 report) → **#3 this run: verification-only again, report only.** Three dispatches, one item, two of them pure re-verification. The queue re-fired a closed item twice in an hour.

---

## Executive summary

This dispatch arrived to find the item **already implemented, committed, closed on both queue surfaces, and verified once** by dispatches #1/#2 of the same task ID. This run added three things dispatch #2 did not do: (1) proved the check is **registered** in the fleet checks attrset (`nix eval .#checks.x86_64-linux --apply attrNames` lists both `offsite-borg-positive-render` and `offsite-borg-smoke-selftest` — the 05:05 run built the attr directly, which forces the asserts but not the registration path a misnamed/mis-system check would need), (2) **re-proved drift sensitivity** with a fresh throwaway probe (priority-49 `idle` override renders through and throws on the replicated assert — consistent with the 05:05 finding, second independent confirmation), and (3) established the **triple-dispatch pattern** with exact evidence, which materially strengthens the already-queued queue-dedup rows (TODO_LIST:197/200/291). No tree changes; the only artifact is this report.

## Self-review: what I forgot / could have done better / can still improve

- **Forgot — nothing in the item itself.** Verification covered existence, build-green, registration, drift sensitivity, and closure consistency (queue row, library row, AGENTS.md, footer commits, prior report).
- **Near-miss worth naming:** my first move this run was to append a NEW "queue dispatch dedup" item to the backlog — the task contract authorizes that. Grep-first saved it: the gap is **already triple-queued** (TODO_LIST:197, :200, :291). Appending would have been exactly the duplicate-row noise the queue suffers from. Lesson generalized: on ANY follow-up append, grep TODO_LIST + the target library file for the concept BEFORE writing, not just for the exact phrasing (my item would have said "re-fire", the existing row says "re-firing" — a phrasing-level grep catches it, a title-match would not).
- **Could have done better — read the same-ID sibling report FIRST.** I planned and ran verification from the work item + AGENTS.md alone, then read the 05:05 report while writing this one. Had I read it first, I would have known the positive build + drift probe were already done at 05:05 and skipped straight to my actual deltas (registration proof, dispatch-pattern evidence). Cost: ~2 redundant tool calls; no wrong conclusions. Lesson: same-task-ID re-dispatch ⇒ the prior report is the fastest ground truth — read before planning.
- **Could have done better — verification was item-scoped.** I did not run a full `nix flake check --no-build`. Deliberate: the item-scoped build + registration eval is sufficient evidence here, and the 05:05 report documents a known INTERMITTENT `checks.disko-layout` stale-drv eval failure in the fleet gate (pre-existing, not this item's) — running the fleet gate now would risk attributing someone else's flake to this item.
- **Eval-cache honesty (same caveat as 05:05):** the positive build's store path (`c9nn18w5…`) is indistinguishable from an eval-cache replay — a green positive build alone has no teeth. The drift probe (throw at priority-49) is what makes the verification mean something; ran it fresh this run.
- **Deliberate non-fix, flagged not edited:** TODO_LIST:291's evidence is now stale ("3+ same-day duplicates on 09-21/22") and its library entry appears MISSING from docs/todo/pipeline.md (queue↔library drift; grep for "dedup preflight/already-closed/repeat dispatch" in pipeline.md returns nothing). I chose not to edit another item's row mid-flight (multi-agent write discipline; parallel sessions own the pipeline section) — flagged as f22/f23 instead.
- **Sequential when parallelizable:** the `date` call, registration eval, and drift probe were independent; ran mostly sequentially. Trivial latency, noted for honesty.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| a1 | The work item itself: `checks.x86_64-linux.offsite-borg-positive-render` — `extendModules` render of evo-x2 with `services.offsite-borg.enable = lib.mkOverride 50 true` (configuration.nix's plain `enable = false` forces probe overrides to ≤50) + `builtins.deepSeq` eval asserts on the rendered `borgbackup-job-hetzner.serviceConfig` | Commit `14fc6816` (dispatch #1); flake.nix:1936-1964 |
| a2 | The four go-live deliverables asserted at eval time: `IOSchedulingClass == "best-effort"` + priority `6` (ioTier.background mkForce intact), ExecStartPre carries `borg-offsite-golive-check` (PLACEHOLDER tripwire), ExecStartPost carries `/var/lib/borg-offsite/.last_success` (freshness marker), `EnvironmentFile == sops.templates."borg-env".path` (drift on either side fails) | flake.nix:1951-1957 |
| a3 | Exec-line asserts route through a list-normalizing helper (`execLines`) — a future nixpkgs-side list merge of Exec* entries still matches | flake.nix:1947-1950 |
| a4 | **This run — build green:** `nix build .#checks.x86_64-linux.offsite-borg-positive-render` → `/nix/store/c9nn18w5v83a08m367cmli1r7wcrf90k-offsite-borg-positive-render-check` | This run, 05:36 |
| a5 | **This run — registration proof:** both offsite checks present in the fleet checks attrset (`offsite-borg-positive-render`, `offsite-borg-smoke-selftest`) — NEW vs dispatch #2, which only built the attr directly | `nix eval .#checks.x86_64-linux --apply builtins.attrNames` |
| a6 | **This run — drift sensitivity re-proven (2nd independent confirmation):** throwaway `extendModules` with `enable` + `IOSchedulingClass = "idle"` both at priority 49 → the replicated assert THROWS while evaluating `((svc).IOSchedulingClass == "best-effort")` — the render is genuinely read, not constant | `nix eval --impure` probe, this run |
| a7 | Loop closed on every surface, verified consistent this run: TODO_LIST:37 `[x]` with DONE narrative; docs/todo/storage.md:51 `[x] [ready]` with DONE narrative (no queue↔library drift); AGENTS.md offsite-borg section documents the check as permanent (incl. the priority-50 vs `<50` drift rule); both chain commits end in the exact Task-Queue-ID footer; dispatch-#2 report exists | Grepped/read this run |

## b) PARTIALLY DONE

| # | Item | Gap |
|---|------|-----|
| b1 | Contract step 4 ("commit your changes") — **no new commit exists for this dispatch.** Nothing changed, and empty commits are forbidden. The queue's footer cross-reference for this task ID is satisfied by dispatches #1/#2 (`14fc6816`, `3d8a8031`), not by this run. This is the second consecutive dispatch with this shape — for the queue bookkeeping, a verification-only run contributes zero footer commits by design | Systemic: see f20/f21 |
| b2 | Verification depth: standalone build + registration + drift probe done; **full `nix flake check --no-build` NOT run** (item-scoped choice; fleet gate has a pre-existing intermittent `disko-layout` stale-drv eval failure per the 05:05 report — running it now invites misattribution) | Accepted, documented |
| b3 | Evidence hygiene: TODO_LIST:291's duplicate-dispatch count is stale and its pointed-to library entry is absent from docs/todo/pipeline.md; row :200's count ("8 repeat dispatches") predates today's triple. Not edited (cross-item, parallel-session risk) — flagged f22/f23 | Flagged, not fixed |

## c) NOT STARTED (within this item's scope)

Nothing — the item is complete and permanently gated. Adjacent work is deliberately untouched (it lives in its own queue rows): guard lib-extraction (TODO_LIST:71), VM rehearsal of FAIL shapes (:73), backup.nix VM test (:62), CONTRIBUTING guard-doctrine note (:302), and the entire owner-gated go-live chain (StorageBox inputs, host-key pin, enable flip, first seed, real-repo drill).

## d) TOTALLY FUCKED UP

Nothing — no breakage, no regressions, tree untouched and clean, no secrets anywhere near the report. The honest worst finding is systemic, not this run's: **the queue burned a third dispatch on a closed item within one hour** (04:45 → 05:05 → 05:36), each re-fire costing a full session + report. That failure mode is real but already owned by queued rows (f20-f23). Second systemic near-miss (caught in-process, zero damage): this session nearly appended a duplicate dedup backlog item before grepping — the concept-level grep-first discipline is now written down above so the next session doesn't need the lucky catch.

## e) WHAT WE SHOULD IMPROVE

1. **Same-ID re-dispatch protocol:** on receiving a task ID that already has a `docs/status/*_task-<ID>.md` report, read that report BEFORE planning — it is ground truth for what's done and what the last verifier already covered. (Would have saved this run's redundant positive-build + drift-probe.)
2. **Concept-level grep before any backlog append** (exact-phrase grep misses reworded duplicates; happened live this run).
3. **Queue harness dedup preflight** — already queued (TODO_LIST:197/200/291); today's evidence strengthens the case: harvest should skip items whose queue row is `[x]` AND whose task-ID footer commits already exist.
4. **Done-signal semantics for verification-only runs** (TODO_LIST:200): a run that changes nothing produces no footer commit; the queue needs a non-commit closure signal it can consume (e.g. matching the `docs/status/*_task-<ID>.md` report file itself as the done marker).
5. **Fix the row↔library drift** for TODO_LIST:291 (missing pipeline.md entry) while touching that area next.
6. **Keep the drift-probe habit for eval-cache-prone checks:** any check whose green store path is content-stable needs one throwaway negative probe per verification to mean anything (negative-test-convention doctrine, applied to positive checks).
7. **Replica-probe limitation is known and queued** (TODO_LIST:71): the inline deepSeq guard can only be probed by replication; the lib extraction makes probes drive the REAL guard code. Until then, every verification carries the "guard LOGIC weakening passes both probe and build" residual — only diff review catches that class.

## f) UP TO 50 THINGS TO GET DONE NEXT

*Direct descendants of this item (offsite-borg verification chain):*

1. Extract the positive-render guard into a shared pure lib fn `(svc, envTemplate) → asserts` so throwaway probes drive the REAL guard (queued, TODO_LIST:71).
2. VM rehearsal of offsite-borg runtime FAIL shapes (mock sops, tripwire gates start with checklist pointer) (queued, :73).
3. VM test for `platforms/nixos/system/backup.nix` — the only backup module with zero coverage (queued, :62).
4. CONTRIBUTING "Eval-Time Guards" doctrine note: inline deepSeq vs shared-lib guards, which pattern, how to probe each (queued, :302).
5. After (1) lands: re-point this report chain's probe recipe + AGENTS.md §a6-class note at the shared lib.
6. When nixpkgs' borgbackup module ever merges Exec lists, confirm the `execLines` helper still matches (flake check fires automatically — just read the failure if it ever comes).
7. Runbook micro-doc: "positive-render check is permanent — no manual re-run needed on module touches" (AGENTS.md already says it; runbook one-liner optional).
8. Confirm `offsite-borg-smoke-selftest` still green at next full flake check (adjacent sibling check, not re-run this run).

*Go-live chain (owner-gated; each is a real dispatch when unlocked):*

9. Owner: paste StorageBox hostname/username into sops `borg-env` (go-live gate inputs, docs/todo/storage.md row).
10. Owner/user: `ssh-keyscan -p 23` → pin `borg_known_hosts` (PLACEHOLDER = fail-closed today).
11. Owner: hold a recovery copy of the borg passphrase (policy row open in docs/todo/storage.md).
12. Flip `services.offsite-borg.enable = true` + `nix run .#deploy` (first enabled deploy exercises pre-deploy §13 PASS branch live).
13. Watch the first WAN seed end-to-end (TimeoutStartSec=2d sized for it); confirm `.last_success` + `backup_healthy{backup="offsite-borg"}` + aggregate "All Backups Healthy" green.
14. Post-deploy §16 PASS branch exercises on the same deploy (tripwire executable, marker, prom row, IO-tier greps in the deployed unit).
15. Real-repo restore drill (`scripts/borg-restore-drill.sh` real mode) after first seed — go-live checklist step 9.
16. Verify prune ladder on the StorageBox (7d/4w/6m) after the first monthly cycle; project repo growth vs the BX11 1 TB budget.
17. Owner decision: append-only/ransomware-resistant posture for the offsite repo (restricted authorized_keys on StorageBox) — tradeoff: restores then need the unrestricted key kept offline.
18. Owner decision: restic repo's offsite role — local dedup only vs replicated leg (blocked row :61).
19. Owner decision: ClickHouse dump third-copy placement (blocked row :68).
20. Confirm OnFailure→Discord delivery for `borgbackup-job-hetzner` (`monitored = true` + "Offsite Borg Job Service" registry check wired 09-24; first real failure is the proof — no synthetic break suggested).
21. Observe one full monthly cycle: prune/compact timing vs the 06:30 slot, WAN-seed vs nightly-incremental duration delta.

*Queue/pipeline systemic (noticed in passing; the direct cause of this triple dispatch):*

22. Queue dedup preflight in the harvester: skip `[x]` rows with existing footer commits (queued :291 — evidence NOW: 3 dispatches of ONE item, 2 verification-only, within 51 minutes).
23. Done-signal semantics decision (queued :200): what terminates re-dispatch for a no-commit verification run — concrete proposal: treat `docs/status/*_task-<ID>.md` existence as the done marker.
24. Update TODO_LIST:291's stale evidence line (currently "3+ same-day duplicates on 09-21/22") with today's same-ID triple dispatch.
25. Backfill the MISSING library entry for TODO_LIST:291 in docs/todo/pipeline.md (queue↔library drift, found this run).
26. Triage the intermittent `checks.disko-layout` stale-store-drv eval failure in the fleet pre-commit gate (05:05 report; also why this run skipped the full flake check).
27. Measure `nix flake check --no-build` wall time on this box — if the fleet gate is minutes, document an expected-duration line so intermittent hangs are distinguishable from slowness.
28. Sweep for other same-ID multi-dispatch chains in docs/status/ (row :200 cites 8 repeats in the 09-19 window; a one-liner `ls docs/status | sed 's/_task-.*//' | sort | uniq -d`-style audit would quantify the current total for the owner decision).

*Small hardening/polish observed in passing (low priority, correct homes noted):*

29. Consider a one-line comment on the `offsite-borg-positive-render` check noting its registration is proven via the checks attrset (this run's a5) — optional, the attrNames proof is reproducible on demand.
30. When (1) lands, add the drift-probe one-liner to the drill/probe docs so future verifiers copy the working `nix eval --impure` probe instead of re-deriving it.

*(Stopped at 30 — the remaining slots would be padding; every real successor of this item plus its systemic causes is listed.)*

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Done-signal for verification-only dispatches:** when a task ID re-fires on an already-closed item and the run changes nothing (no footer commit possible), what should the queue consume as closure — harvest-time `[x]`+footer-commit preflight, or the `docs/status/*_task-<ID>.md` report file as the done marker? (TODO_LIST:200 asks the general form; today's triple dispatch is the concrete case.)
2. **Offsite posture:** do you want the StorageBox borg repo hardened with an append-only/restricted authorized_keys command (ransomware resistance), accepting that restores then require an unrestricted key held offline? This changes the go-live runbook (key provisioning + drill prerequisites) and is cheapest to decide BEFORE the first seed.
3. **Scope of the fleet-gate flake:** the intermittent `checks.disko-layout` stale-store-drv eval failure blocks full `nix flake check` runs unpredictably (per the 05:05 report). Is fixing that class a queued pipeline item I should pick up next, or accepted noise to be documented and routed around?

---

*Report-only run: zero tree changes besides this file. Verification evidence in a4-a6 is independently reproducible via the commands quoted above.*
