# Task 000001a08e0d2a27ecfc327f9e89de947c99 — /data Corruption Repair, T05 Round — Self-Review + Full Status

**Session:** 2026-09-11 ~04:05–04:49 CEST (agent, task queue)
**Item:** TODO_LIST P0 "/data corruption repair (1.3MB, 22 extents …)"
**Work commit:** `0ef51388` (TODO_LIST.md only) — prior same-queue rounds: `6fc970dd` (scrub-mechanism fix), `07d65ffa` (gate-a verification round)
**Verification:** gitleaks PASS, `nix flake check` PASS on both commits (the miniflux-VM-test pre-commit failure noted in `07d65ffa` did NOT recur — it is fixed or flaky; flagging, not investigated, out of scope).

---

## a) FULLY DONE

1. **T05 corrupt-file mapping — COMPLETE and self-verified.** Every journal-derived bad inode from the 2026-09-11 verification round resolved to exactly one live path, each pair re-confirmed by `stat -c %i`:
   | inode | path (relative to /data) | size |
   |---|---|---|
   | 2114533 | `ai/models/image/perfectdeliberate_v90.safetensors` | 6.9 G |
   | 2608101 | `ai/models/image/illustrij_v21_diffusers/tokenizer/tokenizer.json` | 3.6 M |
   | 4020751 | `ai/models/image/sana-1.6b/tokenizer/tokenizer.model` | 4.2 M |
   | 4995089 | `ai/models/image/ernie-image/pe/model.safetensors` | 7.7 G |
   | 1389858 | `ai/models/jan/llamacpp/models/qwen3.6-27b-aggressive/mmproj-f16.gguf` | 0.9 G |
   | 1389877 | `ai/models/jan/llamacpp/models/qwen3.6-27b-aggressive/…Q4_K_P.gguf` | 17.5 G |
   | 4971282 | `ai/models/jan/llamacpp/models/llmfan46/gemma-4-26B-…/mmproj.gguf` | 1.2 G |
   | 2114473 | `ai/cache/huggingface/hub/models--Tongyi-MAI--Z-Image-Turbo/blobs/31349551…` | 1.7 M |
   | 2114488 | `…/blobs/95facd59…` | 10.0 G |
   | 2114494 | `…/blobs/aba4e37a…` | 4.7 G |
   10 files, ~49G total. **All re-downloadable AI artifacts; zero unique user data; the monitor365 DuckDB is NOT among them** (T04 safety copy is unaffected by these files but stays plan-mandatory before destructive steps).
2. **Evidence correction — headline inode 1331118 is GONE.** Full live `/data` walk: absent. Newest snapshot walk (`data.20260910T2330/ai`): absent. The "recurring in nightly sends for 5+ weeks" signal rode stale-era parent chains. The nightly FULL send now dies on the **10 LIVE corrupt files** (all present in the newest snapshot — a full send reads every extent, which is exactly the 2026-09-05 failure shape: EIO at 258G/639G read).
3. **Snapshot pinning timeline mapped** (35 snapshots, `data.20260721T2330` → `data.20260910T2330`, gap 08-21→08-31 = the DAS outage): 7/10 corrupt files present in EVERY snapshot; the 3 Z-Image-Turbo blobs appear from `20260906T2330` on. This quantifies the exact extent-pinning horizon the work item's "mind 14d+4w snapshot pinning" clause pointed at.
4. **Repair recipe refined and recorded** (TODO_LIST): all 10 files are `lars:users 644` → deletion needs NO root, only the T06a sign-off. Delete live files → the 23:30 snapshot is clean → btrbk-data full-send of THAT snapshot re-establishes the pool seed with **zero snapshot surgery** (old snapshots keep rollback value; corrupt extents stay pinned until natural expiry). Scrub-green (T08) additionally waits ~4w. **The btrbk-resume gate and the scrub-clean gate DECOUPLE** — the original plan text implied strict T08→T09 ordering; the refined sequence resumes backups ~1 day after deletion instead of ~4 weeks.
5. **State re-verified live:** deployed `/etc/systemd/system/btrfs-scrub-data.service` ExecStart still points at the bare (directory) store path — the `lib.getExe` fix from `6fc970dd` is in the repo but NOT deployed (`/run/current-system` = Sep-5-era `c043004` build). btrfs.prom confirms `/data` scrub status 3 / errors_total 0 (freeze-killed Aug-31 run; `/` also 3) — **no completed scrub since Aug 31, bounded hypothesis formally still unproven, deletion correctly stays gated behind it.**
6. **Loop closed per contract:** TODO_LIST item updated in place (round appended, BLOCKED reason corrected — the old text wrongly claimed deletion needs root), header line refreshed, item left `[ ]` (correct — the work item is not done), committed `0ef51388` with the Task-Queue-ID footer. Not pushed (per contract).

## b) PARTIALLY DONE

1. **The work item itself** — verification-first gate: (a) PASS (prior round), (b) mechanism fixed but scrub NOT yet run (needs deploy + root). Repair (T06a) not executed (gated on sign-off + gate b). Correctly left `[ ]` + BLOCKED.
2. **T05 "physical addr ↔ fiemap join"** — the master plan's T05 also mentions a physical-address join against kernel csum-error physical ranges. I resolved the journal-derived inode list only; the kernel physical addresses were not retrievable (journalctl as this user returned nothing for `csum failed` since 2026-08-01 — rotated or permission-filtered). The inode→path mapping is the load-bearing half for T06; the fiemap join would only sharpen extent-level confidence, and the scrub delta (gate b) supersedes it as the boundedness proof.
3. **T04 DuckDB safety copy** — feasibility asserted but not executed, deliberately: 54G copy under live IO PSI some avg10 26–33% violates the quiet-window doctrine the repo itself enforces (scrubGuard/balance Guard 0 trip at ≥20%). It is also not on the critical path until T06a sign-off exists.

## c) NOT STARTED (all root-only or sign-off-gated; sequenced in TODO_LIST)

- Deploy of the `lib.getExe` scrub fix (`nix run .#deploy`; blocked: sudo in agent session + pressure gate at session time).
- Gate (b): completed `/data` scrub in a quiet window + before/after error delta vs the 1,351,271 baseline.
- T06a: user sign-off per file → delete the 10 files (user-runnable!).
- T04 safety copy (do before/with T06a).
- T07 low-risk `btrfs check` window; T08 scrub-clean 0-csum (post-expiry); T09 btrbk-data resume; T11 green sweep; T14 reserve re-provision.

## d) TOTALLY FUCKED UP — honest list

1. **First edit attempt failed** ("read the file before editing") — I edited TODO_LIST.md from grep output without a View first. Cost: one wasted round trip. Self-inflicted, zero blast radius.
2. **Commit raced the auto-commit daemon.** The daemon committed my staged change as `5bca669c "chore: auto-commit 1 changed file(s)"` between my `git add` and my `git commit`, so my commit found a clean tree and exited 1. I then **amended the daemon's commit** (`0ef51388`) with the proper message. This is the right repair for THIS instance (the amended commit contained exactly my one file — verified via `--stat` before amending), but amending daemon commits is generally risky: had the daemon batched a parallel session's file in, I would have swallowed foreign work into my message. I verified first; still, a cleaner reflex would have been `git commit --amend` ONLY after re-checking `show --stat` (done) AND considering a follow-up empty-ish note commit instead (rejected: no-empty-commits rule). Acceptable, but worth remembering.
3. **Nothing else broke.** No repo breakage, no unrelated file touched, no secret touched, no sudo attempted twice, no destructive action taken.

## e) WHAT WE SHOULD IMPROVE (process / repo observations from this task)

1. **The P0 item is a monolith.** Three sessions have now appended rounds to one TODO line; the actionable state lives in prose. Once gate (b) passes, split into sub-rows (T04…T11) so per-file sign-off has checkbox granularity.
2. **"ROOT-ONLY" claims in the item were wrong** (deletion is user-runnable) — cost the previous session a false blocker. Verification rounds should re-test ownership/mode assumptions each round instead of inheriting them.
3. **1331118 stale evidence survived two rounds** of "verification" because nobody re-ran the cheap find. The 2026-09-11 round said "headline inode 1331118 recurring … still present" — presence of the *journal signature* was conflated with presence of the *file*. Rule: evidence items carry a re-verify timestamp or they rot silently.
4. **The scrub-mechanism fix sat undeployed for ~30 minutes** with the repo claiming "unblocked" — a fix that requires a deploy is not "unblocked", it is "deploy-gated". TODO wording now says so.
5. **journalctl access as the agent user is a black box** — empty output with rc=0 is indistinguishable from "no permission". Worth a one-line note in AGENTS.md (I did not add it — docs churn beyond the item; candidate for the next docs-health pass).
6. **btrfs.prom `scrub_status 3` for `/` with errors 0 and duration 0** is a confusing telemetry state (interrupted-by-never-started vs interrupted-mid-run are not distinguishable). Minor collector improvement candidate.
7. **The "miniflux VM test pre-commit failure"** noted in `07d65ffa` did not reproduce; if it is flaky it will burn a future deploy round. Triage it (cheap: run the VM test twice).

## f) NEXT 50 (ordered; ~1–20 are the actual repair chain, the rest are in-passing candidates — do NOT treat as one work order)

**Repair chain (critical path):**
1. Deploy the `lib.getExe` scrub fix (quiet window; `nix run .#deploy`).
2. Confirm all three scrub units execute (journal "deferring/cleaning" lines, not 203/EXEC).
3. Run/await completed `/data` scrub outside IO-storm windows.
4. Read the scrub error delta vs 1,351,271 → close gate (b): delta ≈ 0 ⇒ bounded; growing ⇒ STOP, hardware-first triage reopens the item.
5. User sign-off on the 10-file T05 table (now trivial: all re-downloadable, ~49G, listed with sizes).
6. T04: safety-copy monitor365 DuckDBs (31G + 23G) to `/mnt/pool/archive/monitor365-nvme-safety/` + checksums (quiet window).
7. Delete the 10 files (user-runnable; `rm`-equivalent semantics needed — extent freeing, NOT trash: trash keeps extents allocated on /data and snapshots would pin the trash tree too).
8. Verify `ai/cache/huggingface/hub/models--Tongyi-MAI--Z-Image-Turbo/` snapshots/refs survive blob deletion gracefully (HF cache will re-download on demand; check no dangling-lock files).
9. Wait for the 23:30 snapshot; confirm it contains none of the 10 paths.
10. Let btrbk-data's next run full-send the clean snapshot; monitor journal past the old EIO point (~258G read mark) and past the oom-kill cliff (~20G cgroup).
11. If the send oom-kills again: raise `btrbk-data` MemoryMax (20.6G observed peak) BEFORE declaring failure.
12. After pool receive completes: `btrfs-verify-pool-backups` green, `backup_all_healthy=1`.
13. Restore the `btrfs-verify-pool-backups` /data branch from WARN-only to hard-FAIL (was downgraded 2026-09-09 as the deploy exit-4 containment — restore after the repair).
14. Schedule T07: docker-down window, `btrfs check --mode=low-risk` read-only.
15. T08: re-scrub after ~4w snapshot expiry; confirm 0 csum FS-wide.
16. Consider accelerating expiry: prune the pre-08-21 `/data/.snapshots` (user decision — they are the only /data history; the 7 pinned files are re-downloadable so pruning is defensible but is history deletion).
17. T09 done → T11 final green sweep (Gatus all-green backup tier).
18. T14: re-provision `/btrfs-emergency-reserve` + freshness semantics.
19. Update AGENTS.md HDD-pool section + master plan doc with the repair outcome + decision record.
20. Move the P0 row to CHANGELOG per docs-health contract.

**In-passing improvements (observed this session, not acted on):**
21. Split the P0 item into per-step checkbox rows after gate (b).
22. Add ownership/mode re-verification to every blocked-round template.
23. Note journalctl-as-user rc=0-blackout in AGENTS.md.
24. Distinguish "never scrubbed" from "interrupted" in the btrfs.prom collector.
25. Triage the flaky miniflux VM test (`tests/test-miniflux.nix`).
26. The Jan model tree at `/data/ai/models/jan/llamacpp/models/` holds a corrupt 17.5G qwen3.6-27b-aggressive — after deletion, check Jan's model list doesn't reference it (or re-pull if the user still wants it).
27. `perfectdeliberate_v90.safetensors` / `ernie-image/pe/model.safetensors`: decide re-pull vs permanent removal (disk relief ~15G).
28. Z-Image-Turbo HF cache: three corrupt blobs → `huggingface-cli download` re-pull or delete the whole cache entry.
29. `/data/docker` + `/data/containers` are unreadable to the user walk — note in the runbook that a future full-tree corruption sweep needs root (or accept: wrong era).
30. 35 `/data/.snapshots` on a space-tight QLC NVMe — re-audit whether 14d+4w is still the right retention post-repair.
31. The find walk for inode 1331118 covered snapshots' `ai/` only — a final post-deletion scrub is the authoritative confirmation it is truly gone (extents freed after expiry).
32. Add the T05 inode→path table (with sizes) to the master plan doc — it currently plans the mapping but has no results slot.
33. The `?rev=`-pin/lock-churn PMA class: nothing to do here, but the queueID-footer pattern worked well — consider adding it to CONTRIBUTING.md for task-queue work generally.
34. `07d65ffa`'s claim "no completed scrubs since Aug 31" was verified again this session — after the deploy, add a Gatus check that scrubs COMPLETE weekly (a perpetually-deferred scrub currently only shows via `btrfs_scrub_status` staleness; make it explicit).
35. Consider a small "corrupt-file inventory" textfile metric (count of known-bad files still present) so deletion progress is dashboard-visible.
36. When T06a executes, run the deletion BEFORE 23:00 to catch the same-day snapshot.
37. If IO PSI is high at deletion time, delete anyway (metadata op, negligible IO) — do not conflate with the copy-class quiet-window rule.
38. btrbk-data's first successful receive will be ~600G — schedule it AWAY from the Sunday scrub window (marker-gate P1 row already covers nightly-read-burn; keep honoring it).
39. After the seed lands, spot-check a received subvol (`btrfs receive` checksum trust is implicit; compare `btrfs filesystem du` sample).
40. Post-repair: re-enable oom-kill watch on btrbk-data for one week (the second failure mode) before declaring T11.
41. AGENTS.md "CV Server" section unrelated; ignore. (Self-check: no drive-by doc edits made this session — confirmed.)
42. The `find -xdev` trick (btrfs nested subvols share st_dev so one walk covers all) is worth a gotcha line — it silently contradicts the usual "-xdev stops at subvol boundaries" intuition.
43. Re-verify after T08 that `/` (root fs, same physical device p8-era confusion) never carried corruption — the discovery doc asked this in 2026-08 and it was never answered.
44. Confirm no OTHER snapshots exist outside `/data/.snapshots` pinning the extents (e.g., pool-side received copies — pool has zero received /data subvols today, so clean).
45. When the repair completes, the P1 "marker-gate" nightly-read-burn containment row can be retired — chain them in TODO_LIST.
46. Check whether anything on the system actively READS the corrupt files (Jan/flm model lists, HF loaders) and logs EIO — after deletion those errors vanish; before, they are free confirmation signals.
47. The 0x8941f998 zero-page signature: document it in the master plan as the definitive shrink-wipe fingerprint for any future recurrence.
48. `btrfs scrub status` raw output (not just prom) for the delta gate should be captured to a status doc — the prom only keeps the last run's summary.
49. Post-repair, re-run `nix run .#post-deploy-check` and attach output to the closing status doc.
50. Consider making the task-queue worker convention (read AGENTS → work item → verify → TODO_LIST close-loop → commit with Task-Queue-ID) an explicit CONTRIBUTING.md section — it worked cleanly across three independent sessions on this item.

## g) QUESTIONS I CANNOT ANSWER MYSELF

1. **T06a sign-off granularity:** do you want to sign off the 10-file table as a batch (it is 100% re-downloadable artifacts), or do you still require per-file confirmation as the master plan's T06a literally states? And for the three big still-wanted models (qwen3.6-27b-aggressive 17.5G, perfectdeliberate 6.9G, ernie-image 7.7G): re-pull after deletion, or permanently reclaim the space?
2. **Snapshot retention trade-off:** the 7 pre-08-21 snapshots pin ~half the corrupt extents for up to 4 more weeks. Accept the natural expiry (zero risk, backups resume ~1 day after deletion anyway), or may a sudo session prune the pre-repair snapshots early to reach scrub-green faster (costs /data rollback history)?
3. **Is the qwen3.6-27b-aggressive Jan model still in use?** Jan's live model set per AGENTS.md is the router at `/data/ai/models/jan`; if this tree is a stale duplicate of a model you no longer load, its deletion is pure disk relief and its re-pull should be skipped even if you re-pull the others.

---

**Bottom line:** the repair's hardest unknown (what exactly is corrupted) is now fully answered and self-verified; everything remaining is sequencing, one root-run scrub, and one sign-off. The verification-first order was preserved; nothing destructive was done; the repo is clean and both commits passed all hooks.
