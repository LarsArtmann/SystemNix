# Task Status Report — 000001a0d1898ba3e48695371bc62baac77d

**Date:** 2026-09-24 06:03 CEST
**Task:** "Document the nixpkgs override-shape change (inline overrides now need `content`, not `value`) + the plain-`enable=false` conflict class for probe authors" → docs/todo/pipeline.md (Source: 2026-09-23_10-05 report §f2)
**Scope:** Task-scoped report per run instruction — no unrelated research; project-wide state is owned by the standing status/harvest chain.
**Format note:** The status-report skill's canonical output is styled HTML; the run instruction explicitly mandated this `.md` path, so the override is honored here and flagged per the skill's divergence rule (not propagated into the skill).

**Work commit:** `16719c10` "Document extendModules probe-override pitfalls in AGENTS.md" (3 files: AGENTS.md +1 bullet, TODO_LIST.md row 38 `[x]`, docs/todo/pipeline.md row 154 `[x]`), Task-Queue-ID footer, unpushed, full pre-commit suite green.

---

## a) FULLY DONE

1. **The queue item, end-to-end.** Durable gotcha landed in AGENTS.md, Non-Obvious Gotchas → Nix & Nixpkgs, directly after the adjacent `nix eval …drvPath does NOT check NixOS assertions` bullet. Covers both traps: (1) the module-system override wrapper's inner field is `content` (`mkOverride = priority: content: { _type = "override"; inherit priority content; }`) so the long-documented `{ _type = "override"; priority = 50; value = true; }` literal dies `attribute 'content' missing` at `lib/modules.nix:1458`; (2) plain values carry priority 100 (`defaultOverridePriority`), so a plain `enable = false` in configuration.nix conflicts with a probe's plain `enable = true`, forcing probe overrides to priority ≤ 50 (and < 50 to beat an existing `mkForce`). Evidence: commit `16719c10`; `scripts/check-todo-system.sh` rc=0; hook gitleaks + `nix flake check --no-build` green.
2. **Claims verified BEFORE writing (verify-external-claims discipline).** Both error shapes were reproduced live against the locked nixpkgs (26.11.20260923.4975466) via throwaway impure `extendModules` evals that never touched the tree: (a) value-shaped override → `error: attribute 'content' missing` at `lib/modules.nix:1458:75` (the `strip` function reads `def.value.content`); (b) plain `enable = true` vs configuration.nix plain `false` → `The option 'services.offsite-borg.enable' has conflicting definition values … Use lib.mkForce value or lib.mkDefault value to change the priority`. Source-level facts read from the locked nixpkgs tree: `mkOverride` at modules.nix:1598, `mkForce = mkOverride 50` at :1607, `defaultOverridePriority = 100`.
3. **Source chain fully traced before acting.** Four reports read: `2026-09-23_10-05_task-…ccdd5cd9.md` §f2/§d (origin), `2026-09-24_04-02_task-…56328c.md` §d3 (fresh live evidence of the trap pair), `2026-09-24_05-05_task-…785bd70.md` §c1/§f1 (established the knowledge already existed in the row — the task was the queue-work, not the knowledge), `2026-09-24_05-36_task-…85bd70.md` §a1/§a6 (the committed `offsite-borg-positive-render` probe recipe, incl. the <50-to-beat-mkForce nuance).
4. **Queue loop closed on both ends.** TODO_LIST.md:38 and docs/todo/pipeline.md:154 flipped to `[x]` with "— DONE 2026-09-24 (task queue)" completion notes pointing at the AGENTS.md landing spot; the repo's `check-todo-system.sh` (pre-commit queue-structure guard) passes.
5. **Daemon-race handled per multi-agent doctrine.** The auto-commit daemon heuristic-committed exactly my 3 files as `439c1bdc` mid-run; verified `git show --stat` (no foreign files absorbed), then amended forward (never reset) into `16719c10` with a properly-messaged body; footer verified as the literal last line; unpushed state verified via `git merge-base` against origin/master before amending.

## b) PARTIALLY DONE

1. **Probe-author recipe coverage across docs.** What works: AGENTS.md now carries the complete gotcha (session-loaded, durable). What remains: docs/CONTRIBUTING.md "Eval-Time Guards" — the tutorial-style home for guard/probe authors — does not yet teach the recipe (mkOverride 50, throwaway-eval template, priority ladder). Blocker: none — deliberately untouched; owned by the queued §f9 row (pipeline.md:156). This report recommends pairing the two in one edit (see f2). Effort: S.

Nothing else in this task's scope is partial; the landed state is complete and verified.

## c) NOT STARTED

(Things noticed in passing, deliberately not started — scope discipline; each is routed in section f.)

1. **CONTRIBUTING.md recipe** — queued (§f9 row), untouched this run.
2. **Stale-row close-out for pipeline.md:155** (disko-layout / `--no-verify` claim) — my commit produced fresh contradicting evidence (see d1), but closing another session's queue row is not this task's work; routed in f1.
3. **`lib.mkDefault false` for `services.offsite-borg.enable`** — the root-cause fix for the conflict class (probes with plain `true` would win; no ≤50 override needed). Open owner decision since 2026-09-23_10-05 §d3-3; unanswered; not queued as a row.

## d) TOTALLY FUCKED UP

1. **A queue row is actively teaching a verification-gate BYPASS on a false premise (noticed via this task's own commit).** docs/todo/pipeline.md:155 claims `nix flake check --no-build` fails on `checks.disko-layout` (`path …disko-samsung-tlc-vm.nix.drv is not valid`) and therefore "every commit needs `--no-verify` (eval gate, gitleaks, formatting all skipped fleet-wide)". This run's commit at ~05:56 ran the hook's literal `nix flake check --no-build` (verified: .githooks/pre-commit:356) and it PASSED — no `--no-verify` used, gitleaks and formatting ran. As of now the row's instruction is false and harmful: any agent following it skips gitleaks/format/eval gates for nothing. Severity: medium-high (quality-gate erosion; not data loss). Root cause: a time-sensitive "live <date>" claim with no cheap re-verify step attached. Mitigation: one command re-check, then close/update the row (S) — routed f1.
2. **Nothing shipped by this task is broken.** The landed bullet was source-verified and error-shape-verified before writing; the commit passed the full hook suite; queue structure checks pass.
3. **Honesty ledger (near-miss, not shipped damage):** my scripted two-branch edit of the two done-rows dropped the bold title from the pipeline.md row (branch bug: offset past `len(alt)` without re-adding the title text). Caught by immediate line-diff verification, fixed within one tool call, never committed. Classified under (e) as a process lesson, stated here because the (d) section should know it existed.

## e) WHAT WE SHOULD IMPROVE

**Direct answers first:**

- **What did I forget?** (1) The AGENTS.md bullet does not embed the two throwaway-eval reproduce one-liners — a future reader must open the report chain to re-run the proof (the house style cites the incident, not always the command; the 09-24_05-36 §a6 recipe holds them). (2) I carried the "<50 needed to beat an existing mkForce" nuance from 05-36 §a6 rather than re-proving it (low risk — it is repo-recorded evidence — but not independently re-verified this run). (3) During the work run I appended no new TODO_LIST items; the disko-layout staleness only became provable during this report's verification, so it lands here instead.
- **What could I have done better?** The dual-branch python edit for the two done-rows was over-engineered — one replace-branch, or the edit tool per file, would not have had a branch to get wrong (d3 near-miss). And the work run could have ended with a 30-second `nix flake check --no-build` status note, which would have caught the d1 staleness a few hours earlier.

**Pattern-level improvements:**

1. **Time-sensitive queue rows need re-verify commands.** Rows asserting "live <date>" states (eval failures, endpoint down, store-path breakage) should carry a one-line re-check command so the next agent confirms-or-closes in one step. The disko-layout row would have self-corrected hours ago. Impact: prevents false gate-bypass doctrine (d1 class). Fix: convention + backfill the worst offenders.
2. **Destination ambiguity for documentation tasks.** The TODO_LIST arrow said "→ docs/todo/pipeline.md" while the in-row text said "AGENTS.md gotchas or here" — each session re-derives the landing spot. Convention fix: durable knowledge lands in session-loaded files (AGENTS.md / CONTRIBUTING.md); todo rows track the task and get pruned to CHANGELOG on harvest, so knowledge in a row is knowledge with an expiry date. State it once in the TODO-system rules.
3. **Knowledge must never live only in a todo row.** Same point from the other side: 05-05 §c1 noted the technical content already sat in the row — which harvest would eventually prune to CHANGELOG. Rows should hold one ask + Source pointer; the payload belongs in durable docs.
4. **`check-todo-system.sh` could validate close-out format.** `[x]` rows should be required to carry a machine-parsable "DONE <date> (task queue)" note (TODO_LIST:37 precedent), so harvest parsing stays uniform. Effort S, extends an existing selftesting guard.
5. **Queue re-dispatch of already-satisfied work.** This item's chain shows the pattern three times (the 09-23 triple dispatch; 05-05 §c1 explicitly noting the knowledge existed; this run re-executing a row whose content was drafted). A content-presence or dedup gate before dispatch would save whole runs. Harness-level — needs the owner (see g2).
6. **Commit-attribution convention for queue commits.** Repo precedent (all prior `--grep Task-Queue-ID` commits) ends with the bare Task-Queue-ID footer; the default commit convention otherwise appends Crush attribution lines. The contract's "ending in this exact footer line" makes bare-footer the only compliant reading; ratify it explicitly so future sessions don't re-litigate (g3).
7. **AGENTS.md gotchas-section bloat.** The flat Non-Obvious Gotchas list keeps growing (this run added one more bullet). Discoverability degrades with every entry. Owner-gated idea: curated index at top + long-tail topics split into docs/ files. Large surface; not urgent.

## f) TOP NEXT TASKS (ranked; up to 50 allowed — 13 honest ones delivered)

Per the run instruction ("do not research unrelated stuff"), items below come only from this task's work and what it surfaced in passing. Padding to 50 from an un-researched scope would manufacture items — the skill's own guidance says larger N is a brainstorm with extra harvest rigor. NEW = needs harvest routing; EXISTING = already queued, listed for ordering/pairing only.

| # | Task | Impact | Effort | Category | Status |
|---|------|--------|--------|----------|--------|
| 1 | Re-check `checks.disko-layout` once (`nix flake check --no-build`) and close/update stale row pipeline.md:155, deleting the "every commit needs --no-verify" instruction — this run's commit empirically passed that exact check at ~05:56 | High | S | Documentation | NEW |
| 2 | Execute the queued §f9 CONTRIBUTING.md "Eval-Time Guards" note and fold in the override-shape recipe (mkOverride 50 template, throwaway-eval command, priority ladder: ≤50 beats plain-100, <50 beats mkForce) | Medium | S | Documentation | EXISTING (+fold) |
| 3 | Owner decision: `services.offsite-borg.enable = false` → `lib.mkDefault false` to defuse the plain-100 probe-conflict class at the root (pro/contra framed in 2026-09-23_10-05 §d3-3) | Medium | S | Decision | NEW |
| 4 | Add one-line re-verify commands to queue rows carrying time-sensitive "live <date>" claims (start with the storage/pipeline rows this chain touched) | Medium | M | Process | NEW |
| 5 | Queue-harness dedup: prevent dispatch of items whose content already exists in the row (evidence: 05-05 §c1; this run) — needs owner input (g2) | Medium | M | Process | NEW |
| 6 | Extend `check-todo-system.sh`: `[x]` rows must carry a "DONE <date> (task queue)" close-out note (machine-parsable, TODO_LIST:37 format) | Low | S | Quality | NEW |
| 7 | Ratify + document the queue-commit attribution convention (bare Task-Queue-ID footer as the last line, superseding the default attribution footer) | Low | S | Process | NEW |
| 8 | Document the destination convention for documentation-tasks: durable knowledge → AGENTS.md/CONTRIBUTING.md; todo rows track tasks only (resolves the "→ pipeline.md" vs "AGENTS.md or here" ambiguity) | Low | S | Documentation | NEW |
| 9 | Chain carry-over audit (from this task's source reports): confirm 04-02 report's prior-chain d1 (§16 enable-blind SKIP) and d3 (borg job ↔ Zone-6 `ioChurnUnits` decision) are queued somewhere or harvest them; d2's /data-EIO root is already covered by the queued storage.md:13 row — verify the borg-run angle isn't a distinct gap | Medium | S | Process | NEW |
| 10 | Self-contain the AGENTS.md gotcha: append the two throwaway-eval reproduce commands (or point at the 09-24_05-36 §a6 recipe) so the proof is re-runnable without the report chain | Low | S | Documentation | NEW |
| 11 | Execute the queued "Enumerate the 2026-09-22/23 nixpkgs-move FOD casualties" row — premise re-confirmed live this run: locked nixpkgs now 26.11.20260923.4975466 | Medium | M | Quality | EXISTING |
| 12 | Run docs-health HARVEST on this report's (f) section into TODO_LIST/ROADMAP with the NEW/EXISTING tags above | Medium | S | Process | NEW |
| 13 | Owner-gated (large): restructure AGENTS.md Non-Obvious Gotchas (curated index / topic split) before the flat list becomes unsearchable | Low | L | Documentation | NEW |

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Should `services.offsite-borg.enable = false` in configuration.nix become `lib.mkDefault false`?** I tried to answer it myself: both stances are framed in 2026-09-23_10-05 §d3-3 (mkDefault kills the whole probe-conflict class; plain form keeps the go-live flip a conscious edit) and the row stays unanswered. It is a semantics/ownership call only you can make — it decides whether future probes need `mkOverride 50` forever.
2. **Is re-dispatch of already-satisfied queue items expected pacing, or a dedup gap?** Evidence: the same-ID triple dispatch on 2026-09-23 (04:45 impl + two verify-only re-runs), 05-05 §c1 noting this item's knowledge was already drafted, and this run executing that row anyway. I cannot see the queue harness's dedup internals from inside the repo — if it is a gap, tell me where to route the fix (f5).
3. **Which commit-footer convention governs queue-dispatched commits?** Repo precedent (every prior `Task-Queue-ID` commit) ends with the bare footer; the default commit convention appends Crush attribution lines after it — the two are incompatible with "ending in this exact footer line". I followed repo precedent; confirm it is ratified (f7) so this stops being a per-run judgment call.

---

**Handoff:** Section (f) items tagged NEW are the harvest input for docs-health HARVEST → TODO_LIST.md / ROADMAP.md; EXISTING rows are pointers, not duplicates. Wait for further instructions.
