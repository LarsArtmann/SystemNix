# Task 000001a0d80b8ba29cede448b76050225c24 — Root-Window Echo Sweep (verification-only close-out)

**Date:** 2026-09-25 12:56 CEST
**Work item (TODO_LIST.md, storage):** "Sweep the stale root-window echoes after the retention reconciliation (reserve T14 caveat `3d+1w`, emergency-reserve doctrine-table row `root ≈ 1-2w`, any other `3d 1w`/`1-2w` restatements)" (Source: `2026-09-25_11-51_task-000001a0d7ddc4c5db6b3a8da3acdfb9a1d8.md` §b1/b4/e1)

---

## Verdict up front

**The task was already done before this session started.** Commit `eb844911` ("align stale root pin-window echoes to the reconciled 2w-sharp figure") plus its status report `docs/status/2026-09-25_12-27_task-000001a0d80b8ba29cede448b76050225c24.md` landed the full sweep earlier today under THIS SAME task ID, and TODO_LIST.md:49 / docs/todo/storage.md:13 were already marked `[x]` with DONE notes. This session performed an **independent re-verification** of that sweep and found it complete and correct. **Zero code/doc changes were needed; no work commit exists because there was nothing to commit.**

---

## a) FULLY DONE

1. **Independent grep sweep re-run from scratch** (not trusting the DONE note): `3d+1w`, `3d 1w`, `1-2w`, `root ≈ 1-2w`, `≈ 1-2w` across AGENTS.md, docs/services/, docs/todo/, scripts/, platforms/, modules/.
   - **AGENTS.md** confirmed aligned at all three echo sites:
     - Snapshot-pinning doctrine paragraph (line ~657): root `@` = **2w sharp** with full calendar-anchor mechanics (days start 00:00, weeks start Sunday, `1w` = first-of-week of the TWO calendar weeks covered, 14d weekly lifetime; "12-day-old weekly was IN-SPEC, not a prune gap").
     - Doctrine table rows (lines ~667-668): `/btrfs-emergency-reserve` and `~/projects`+rest-of-`/home` both read `root = 2w sharp`.
     - Emergency-reserve T14 caveat (line ~696): "root pin window = 2w sharp — calendar-anchored, see the Snapshot-pinning doctrine above".
     - Bonus site also verified: ClickHouse XFS migration note (line ~969) — "pin window 2w sharp, calendar-anchored".
   - **Remaining hits are all LEGITIMATE:** the literal `snapshot_preserve 3d 1w` config restatements (those ARE the live values; the doctrine negation "NOT \"3d+1w\" flat" at line 655) and historical archived status/planning reports (`docs/status/2026-08-*`, `docs/planning/*`), which are dated snapshots and must NOT be retroactively edited.
2. **Queue-closure state verified:** TODO_LIST.md:49 marked `[x]` with an accurate DONE summary; docs/todo/storage.md:13 carries the domain-library DONE note; the 12-27 status report exists and is cited as Source by the follow-up grep-gate item (storage.md:136).
3. **Working tree verified clean** (`git status` empty) — nothing to commit for the work itself; repo not left broken.

## b) PARTIALLY DONE

Nothing. The sweep was binary: either echoes remain or they don't. They don't.

## c) NOT STARTED

Nothing for this task. (The related but separate follow-up items — `snapshots.nix` calendar-anchor comment, the stale-restatement grep-gate — are already queued as their own `[ready]` rows in docs/todo/storage.md:15/:136 and were deliberately NOT touched: scope discipline.)

## d) TOTALLY FUCKED UP

1. **Wasted a tool call on qmd for the source report** (`docs/status/2026-09-25_11-51_…` — "Document not found"; it lives in the SystemNix tree, not the qmd-indexed cv collection). Cost: one round trip, recovered immediately via `ls docs/status/`. Trivial, but the reflex to search the knowledge base before checking the local filesystem was wrong for a repo-relative path I was explicitly handed.
2. **No self-deception found, but worth stating plainly:** this session's only real output is a verification record. The honest close-out is "already done, independently confirmed" — NOT a restatement of the prior session's work as if it were mine. Everything in section (a) above is `eb844911`'s content; this session's contribution is the second pair of eyes and this report.

## e) WHAT WE SHOULD IMPROVE

1. **Queue dedup for same-ID re-dispatch:** this task ID was dispatched twice (12:27 execution + this ~12:5x run). The tq pool should skip tasks whose queue row is already `[x]` with a DONE note and a same-ID status report — the check is one grep of TODO_LIST.md before starting work. This session did check repo state first, which is why nothing was damaged, but the dispatch shouldn't have happened.
2. **DONE-note discovery ordering:** check the queue row's closure state BEFORE deep-reading the source report. (I did check `git status` + the report first; the `[x]` state emerged from the grep sweep. Cheap ordering fix: one `grep <task-fragment> TODO_LIST.md` as step 1 for every dispatch.)
3. **Verification-only sessions still deserve a report** — that convention held here. But the report should be the ONLY artifact; no empty "chore: no changes" commits should ever be manufactured. None was.

## f) NEXT 50 (observed-in-passing; near items first — most already exist as queued rows and are listed here only as confirmation, not duplicates to harvest)

Within this task's blast radius (top of list = do first):
1. **Add the calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix** — weeks start Sunday, weeklies live 14d (already queued: storage.md:15, Source §f3 of the 11-51 report).
2. **Add a grep-gate (flake check or pre-commit) failing on stale root-window restatements** — `root ≈ 1-2w` anywhere live; `3d+1w` in scripts/ + docs/services/ + docs/todo with negation/DONE-note context rules (already queued: storage.md:136). This session's sweep is the canonical hit-list for its test fixtures.
3. **Queue dedup: skip dispatch of tasks already `[x]` with a same-ID report** ( tq-side; this incident is the motivating case).
4. **Extend the same calendar-anchor reconciliation decision to `/data` and pool `services/*` windows** — open owner question from the 11-51 report (its §"questions" #3); approximations are currently declared consistent-but-unreconciled.

Noticed in passing during the sweep (pre-existing queue rows confirmed still open, or new small observations):
5. Bank statement decryption retro-repair (`inboxclean paperless --backfill --decrypt-repair`) still needs upstream push + flake bump (AGENTS.md InboxClean section).
6. Pocket ID / Mail Relay go-live: verify `larsartmann.cloud` in Resend, then non-owner delivery probe (AGENTS.md Mail Relay go-live).
7. CV lock hold: do NOT bump `cv` input until upstream drops the go ≥ 1.27.1 floor or adopts `go_1_27` (AGENTS.md CV branch-ref governed hold).
8. Hermes Forgejo write-token scope-marker upgrade already landed per AGENTS.md — verify the collaborator sweep converged on a live boot (new-repo grant path unverified since).
9. `boot.binfmt.preferStaticEmulators = true` candidate (drops `/run/binfmt` from extra-sandbox-paths, kills the whole tmpfiles-cycle class) — noted in AGENTS.md as "follow-up worth doing", not queued.
10. `buildcache-init` does not provision the three 2026-09-22 fallback cache targets (`~/.cache/pnpm`, `~/.local/state/pnpm`, `~/.cargo/registry`) — post-recovery symlinks dangle until next deploy (AGENTS.md buildcache section, "queued").
11. Single-source the four hand-kept fallback-cache reap surfaces (deploy.sh ×2 loops, recovery step 2.5, HM activation block) — AGENTS.md marks this "queued".
12. Root-context repro of the FSETID/sandbox setattr EPERM mechanism (discordsync attachments class) — AGENTS.md marks "P0, docs/status/2026-09-22_14-35".
13. `crush-hot-db` → `hot-db` Phase-2 module fold still pending (AGENTS.md crush-hot-db: "separate queue item"; both modules in-tree).
14. Zero-byte git object corruption: consider `core.fsync` coverage audit beyond loose-object,index after the 09-15 incident fix (AGENTS.md git section — fsync landed; a periodic `git fsck` cron is NOT noted anywhere as existing).
15. Wise SCA / post-approval watch: if RFC3339 `statement_coverage` values reappear post-SCA-approval, dump goroutines (bank-sync section watch item).
16. Forgejo mirror: `commit-graph.lock` cleanup on 2 live mirrors needs the forgejo user (AGENTS.md, cosmetic).
17. Forgejo mirror: post-mass-migration `/var/lib/forgejo` size measurement for btrbk projection (AGENTS.md residual).
18. Forgejo transferred-repo decision (DarkBlocks, DialogesWebInterface): delete or re-mirror into a forgejo org (owner decision, AGENTS.md).
19. Offsite Borg go-live: owner inputs (StorageBox hostname/user + host-key pin) still pending; drill is stand-in-proven (AGENTS.md Offsite Borg).
20. Restic app-dumps: `forgejo-subvol`/`github-voice-corpus`/`pixel6` exclusion review — extend `paths` if wanted (AGENTS.md).
21. groq key for CV ChatService: wire a sops key or disable the provider (owner decision flagged in AGENTS.md CV section).
22. Forced full CV re-scoring stays manual by design — confirm still the owner stance after funnel automation matured (docs/services/cv.md).
23. `nixpkgs-llama-rag` pin stays until the ROCm-runtime/kernel-side root cause of the mid-load spin is bisected (AGENTS.md llama-rag; spin reproduced on the pinned build, so the pin alone is NOT the fix).
24. llama-rag re-enable requires a REAL-unit soak (systemd-run with exact sandbox, ≥10 min) — direct-run greens are disqualified (freeze-5 lesson, AGENTS.md).
25. flm v1.0.3 staged bump: still held at v1.0.2 pending the owed reboot + live-serve validation + one-time weight re-pull discipline (AGENTS.md fastflowlm).
26. The owed REBOOT (flm :52626 corpse from 2026-09-07 boot still pinned) — pre-reboot-check then power-cycle (also clears D-state corpses; NIC-vanish risk means power-cycle, not warm reset).
27. Post-reboot: verify `/run/booted-system` == `/run/current-system` (crash-reboot doctrine).
28. SigNoz exemplar persistence: revisit only when upstream ships it (AGENTS.md, deliberate revisit trigger).
29. CIMD revisit trigger unchanged: only when an MCP-style OAuth consumer appears against Pocket ID (AGENTS.md, decision recorded).
30. Crush minimax re-enable decision (Token Plan exhausted; sops key still rendered) — owner decision whenever the plan refreshes.
31. Context7 key rotation still the standing item from the leak incident (rotation is the real fix; purge push held).
32. `docs/todo/storage.md:136` grep-gate fixture list should include the archived-report allowlist boundary this sweep validated (feeds item 2).
33. Hermes workspace-layout decision stays DEFERRED on `@` — revisit trigger lives in docs/todo/services.md (confirmed unchanged).
34. Google-sync DORMANT go-live checklist unchanged (OAuth + `rclone authorize` + sops fill + enable) — user sequence, untouched.
35. Miniflux `disableLocalAuth` go-live gate: still requires one proven live SSO login before flipping (AGENTS.md).
36. GeoMetrikks MaxMind/CARTO keys ship EMPTY (geo-degraded banner) — user-gated paste (docs/todo/services.md).
37. Health-hub fetch-load decision (merge-on-read request volume per remote) — adding remotes is one line but cadence options are parked in TODO_LIST.
38. Architecture-catalog go-live sequence (setup-forgejo.sh → first CI run → sops token → deploy) still owner-gated with checks RED by design.
39. Samsung `/data` repair recipe: single-victim doc sweep doctrine worked (gemma GGUF) — no action, recorded as healthy pattern.
40. TV PCM-not-bitstream lesson (2026-09-25, smart-audio section) — already landed in AGENTS.md, nothing pending.
41. `@home-hermes` btrbk leg: confirm the 09-18 catch-up chain continues nightly (first receives landed; guard kills were healed — one more fresh `btrbk clean`-free night confirms).
42. `pma` go-commit lock-drift guard: upstream CI guard tracked in docs/todo/upstream.md — confirm it landed before the next PMA input bump.
43. `discordsync` Turso decision still PENDING (upgrade plan vs permanent local-only; Gatus check deliberately red) — user decision item.
44. Immich cross-archive (DiscordSync `immich.enable`) key paste still pending (PLACEHOLDER-inert).
45. `qmd` tag v2.8.3 current — no bump; re-check on next sweep.
46. Shell: nothing new — the `LC_ALL=C comm` / herestring-grep lessons from AGENTS.md applied cleanly to this session's greps.
47. Consider recording "docs-only task = verification-report-only, no synthetic commit" as an explicit tq contract line (this session followed it implicitly).
48. The `eb844911` commit message and DONE notes read accurately — no doc debt found in the closure trail (positive finding, keep the pattern).
49. `snapshots.nix` retention numbers cited in the doctrine (3d 1w / 14d 4w / 7d 4w / 3d) were cross-checked against AGENTS.md quotes only, not against the live file this session — the grep-gate (item 2) is the durable guard; a one-time live `grep snapshot_preserve snapshots.nix` would close that residual.
50. Emergency-reserve "periodic rewrite timer" stays NOT-adopted by recorded decision — do not re-suggest (AGENTS.md T14).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Queue dispatch policy:** should the tq pool skip tasks whose TODO_LIST row is already `[x]` with a same-ID status report (this task was dispatched twice within ~30 minutes), or was the double dispatch intentional (independent verification pass)? If intentional, should verification-only runs get a distinct task type so the queue's derived-stats don't count them as work?
2. **Reconciliation scope (carried from the 11-51 report, still owner-open):** extend the calendar-anchor reconciliation to `/data` (14d 4w) and pool `services/*` (7d 4w) windows now, or is root-only the accepted scope?
3. **Grep-gate shape for item 2/storage.md:136:** flake check (eval-time, fleet-wide, slower) vs pre-commit (fast, staged-only)? The stale figures are prose in .md files — pre-commit doesn't gate .md content today, and a flake check reading docs at eval time adds `readFile` purity concerns. Which layer do you want?

---

**Footer note:** no work commit accompanies this report because the sweep was already landed by `eb844911` and this session changed nothing. Verification evidence: grep sweep clean across all live surfaces; `git status` clean at close-out.
