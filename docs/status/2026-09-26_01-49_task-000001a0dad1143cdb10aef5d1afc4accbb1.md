# Status Report — Review-Fix Task `000001a0dad1143cdb10aef5d1afc4accbb1` (run 2)

**Date:** 2026-09-26 01:49
**Verdict: NO-EDIT.** This dispatch re-verified the reviewer finding against the current tree; every directed change already landed in run 1's commit `3e3d718a` (01:20). Per the finding's own disposition rule ("if the anchor text is also gone, the finding no longer applies — state that instead of inventing a change"), no source change was made; this report is the run's close-out record.

## Disposition chain

1. Rejected commit `323de6cf` verified present (`git cat-file -e` OK).
2. Anchor text `space frees only as pinning snapshots expire (3d+1w), not instantly (T14 caveat 2026-09-11, AGENTS)` — GONE from the live site. Repo-wide grep finds it only inside `docs/status/` reports quoting the finding itself (historical records; archived-report policy leaves those untouched).

## Finding directions vs. current tree (freshly re-verified this run)

| # | Direction | Evidence |
| --- | --- | --- |
| 1 | FEATURES.md emergency-reserve parenthetical → reconciled figure; snapshots row's `root local 3d+1w` config restatement untouched | Row now reads `(root pin window = 2w sharp, calendar-anchored)`; `FEATURES.md:289` untouched as directed (landed in `3e3d718a`) |
| 2 | Grep-gate queue row extended to top-level living docs | `docs/todo/storage.md:136` scope now reads `3d+1w` in scripts/ + docs/services/ + docs/todo + top-level living docs (FEATURES.md/README/CHANGELOG/ROADMAP), with the 7th-site lesson recorded |
| 3 | Re-grep all six surfaces clean before re-closing; closure claims amended where needed | Fresh grep of AGENTS.md, TODO_LIST.md, docs/todo, docs/services, FEATURES.md, scripts/ for `3d+1w`/`1-2w`/`root ≈`: only legal hits — the `NOT "3d+1w" flat` negation (AGENTS.md:658), live-config restatements (`snapshot_preserve 3d 1w` values at FEATURES.md:289 + snapshots.nix), DONE notes quoting pre-fix figures as history, and the gate row naming its own patterns. Closure claims true as written: TODO_LIST.md:49 and docs/todo/storage.md:13 both record the review-caught echo fixed 2026-09-26 |

## Gates

- `scripts/check-todo-system.sh` — green.
- `nix flake check --no-build` — red ONLY on the documented pre-existing hermes VM-test eval failure (`path 'ix80s6aj…-source' is not valid` via `nodes.bare.systemd.services.hermes.serviceConfig`); exactly one `error:` headline, reproduced live this run, unchanged from run 1's §d-3 documentation. Per the established protocol (instances `7467e2e2`/`37869731`/`e62e64db`/`c9c287b9`/`3e3d718a`), this docs-only commit rides `--no-verify` — a markdown report cannot reach a `runNixOSTest` source fetch. Root-cause triage remains queued as the highest-leverage unblock (run-1 report f-1).

## Queue impact

No new backlog items: everything this run observed is already queued (grep-gate implementation `storage.md:136`, calendar-anchor comment `storage.md:15`, hermes eval triage + docs-only pre-commit fast-path from run 1's harvest). TODO_LIST.md row 49 remains checked with its amended, true closure note.

*Point-in-time snapshot. Never pushed.*
