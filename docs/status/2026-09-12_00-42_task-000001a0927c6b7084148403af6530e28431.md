# Task-Queue Status Report — 000001a0927c6b7084148403af6530e28431

**Date:** 2026-09-12 00:42 CEST
**Work item:** Hermes v0.21.0 cron scheduler ERRORS in production (`systemd-run --user --scope` failures) — TODO_LIST.md, Priority 0
**Scope discipline:** report covers THIS task's work only; passing observations explicitly labeled.

---

## Executive Summary

The fix itself was **already landed earlier today by the fixing session** (module wiring + VM test + TODO closure + AGENTS.md documentation). This queue run's real work was **independent re-verification** and **filling the queue-contract gap**: no commit carried the `Task-Queue-ID` footer (the fixing session's work rode heuristic auto-daemon commits). Verification passed on all gates; the footer commit `9502c138` now cross-references the queue. The fix is **not yet live** — deployed system still runs the pre-fix generation; live cron errors stop only on the next deploy.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | Module wiring verified at all four pieces | `modules/nixos/services/hermes.nix`: `linger = true` + pinned `uid = 975` (:514-519), `after`/`wants user@975.service` (:565-571), `XDG_RUNTIME_DIR=/run/user/975` (:616), tmpfiles `L+ /bin/true → coreutils` (:552) |
| 2 | VM regression test §9 confirmed present and green | `tests/test-hermes.nix` runs the EXACT upstream probe (`systemd-run --user --scope /bin/true` as hermes with env) + no-env negative control; `checks.x86_64-linux.hermes` → `vm-test-run-hermes` store path built successfully |
| 3 | Eval gate green | `nix flake check --no-build` → "all checks passed" (includes eval-time assertions) |
| 4 | Companion lock rev confirmed | flake.lock `nodes["root"].inputs["hermes-agent"]` → `79445a496c86a19332ad786494b8384d2167e2d0` (github, 2026-09-05) — matches the identified "macOS parent-death watchdog" commit (#103172), ~20 desktop-churn commits past verified `d3630f85`, nothing server-relevant |
| 5 | TODO_LIST loop closed | Item already `[x]` by fixing session; this run appended the re-verification line (gates run + lock confirmation + queue ID) |
| 6 | Queue-contract gap filled | Commit `9502c138aa94800080a2320eaf5956bccfd2cd22` carries the `Task-Queue-ID` footer — the fixing session left none (its work rode `chore: auto-commit N file(s)` heuristic commits) |
| 7 | Pre-commit hooks passed at commit | gitleaks clean, `nix flake check` green inside the hook |

## b) PARTIALLY DONE

| # | Item | State |
|---|------|-------|
| 1 | Independent live-journal verification | **Could not re-run**: this sandbox sets `no-new-privileges` (sudo unavailable), and the system journal is root/adm-readable only. Relied on the fixing session's documented AGENTS.md claims (journal clean of new ERROR classes; Discord 429 slash-sync noise gone since ~09-08; remaining 429s = zai/minimax LLM Token-Plan limits). The item's "journal-check after" was done by the fixing session, not independently by this run. |
| 2 | VM test execution | Green but **store-cached**, not freshly executed this run. Valid (deterministic derivation, clean tree at HEAD — any input change would re-run), but the TODO re-verification line says "VM test green" without the "(cached)" qualifier — a small precision gap in the artifact. |
| 3 | Fix go-live | The fix is committed but the **deployed generation predates it**; live cron errors continue until the next `nix run .#deploy`. Documented ("goes live with the next deploy") but not executed — out of a repo-side queue item's remit, yet the item's real-world goal is only met after deploy. |

## c) NOT STARTED

1. **Deploy** (`nix run .#deploy`) — the actual go-live step.
2. **Post-deploy verification** — cron-error absence in hermes journal, `/bin/true` present on live system, deployed unit carrying `XDG_RUNTIME_DIR`, first real cron fire landing in `user@975.slice`.
3. **Upstream filing** — correctly not started: the module-side wiring fully resolves the failure (upstream's `systemd-run` fail-closed design is deliberate; nothing broken upstream to file). No `verify-before-filing` trigger exists.

## d) TOTALLY FUCKED UP

Nothing catastrophic — repo left green, no reverts, no scope creep. Honest misses, in order of embarrassment:

1. **Tripped a gotcha that was already in my own context.** I fumbled 3 jq queries against flake.lock (`.root.inputs[...]` on a lock whose top-level `root` is a node-key STRING, not a mapping) before landing on `nodes["root"].inputs` — and AGENTS.md literally documents this exact trap ("walk `nodes[root].inputs`... never a bare `nodes[<input>]` read"). ~3 wasted tool calls rediscovering documented knowledge.
2. **Predictable sudo failure.** Attempted `sudo -n journalctl` inside a no-new-privileges sandbox — 1 wasted call. Reasonable due diligence (second-strategy rule), but the environment constraint was knowable upfront.
3. **Git index.lock collision** with the auto-commit daemon on first commit attempt — handled correctly (waited for lock clear, never deleted the lock), 1 retry. Not damage, just friction inherent to the shared-tree doctrine.

## e) WHAT WE SHOULD IMPROVE

1. **Consult in-context gotchas before empirically rediscovering them.** The lock-structure trap cost 3 calls; the AGENTS.md Non-Obvious Gotchas section is the first place to check when a command form looks unusual.
2. **State cache-vs-fresh test evidence explicitly in committed artifacts.** "VM test green" should have read "VM test green (store-cached for HEAD)" — precision in verification claims is the difference between evidence and vibes.
3. **Task-queue runs should commit their own work early**, not rely on the auto-daemon: heuristic daemon commits cannot carry `Task-Queue-ID` footers, so the queue↔git cross-reference silently never existed for the fixing session. Backfilling (what this run did) works but the audit trail has a hole between fix-landing and footer-commit.
4. **Runbook note for sandboxed agents:** verification steps requiring root journal access should carry a documented fallback ("rely on AGENTS.md-documented check" or "requires privileged runbook step") so the boundary is explicit rather than discovered per-run.

## f) Things we should get done next

Task-critical (the fix only counts when live):

1. **Run `nix run .#deploy`** — puts the hermes cron fix into the booted generation; the single highest-impact next step.
2. **Post-deploy: confirm `Restart-safe cron worker dispatch failed` is absent** from the hermes journal (was ~1,302 errors over the outage; every-few-minutes cadence pre-fix).
3. **Post-deploy: verify `/bin/true` exists on the live system** (tmpfiles rule applied) and survives a reboot.
4. **Post-deploy: verify the deployed `hermes.service` unit carries `XDG_RUNTIME_DIR=/run/user/975`** and `After=/Wants=user@975.service`.
5. **Post-deploy: observe one REAL cron fire** landing as a scope in `user@975.slice` (outside the gateway cgroup — that IS the restart-safety), not just the availability probe passing.
6. **Confirm lars' imperative linger is untouched** after a deploy (declarative `users.manageLingering` must only touch the declared hermes user).
7. **Watch hermes/system-health metrics for new classes** post-deploy: `system_service_restart_churn`, `system_service_memory_over_threshold` (scoped workers carry `MemoryMax = min(gateway limit, half RAM, 4G)` — first live data point).
8. **Verify the 60s probe-failure TTL cache can't mask a first-boot ordering miss** (ordering `after user@975.service` should make this belt-not-load-bearing — confirm once on a cold boot).
9. **Convert the cache-green to a fresh-green:** re-run `checks.x86_64-linux.hermes` uncached once in a low-pressure window, for untainted go-live evidence.
10. **Next hermes-agent input bump:** §9 re-runs automatically, but re-read the cron-dispatch code path at the new rev (~20 commits of churn already sit past verified `d3630f85` at `79445a496`).

Noticed in passing (pre-existing TODO_LIST P0 items, unchanged by this task):

11. **btrbk-data marker-gate fast-fail** until the EIO repair — stop the nightly ~258G / 3h12m doomed read burn (~8TB/month of pointless QLC reads).
12. **Paperless `PAPERLESS_EMAIL_HOST` smoke bug** — real config gap, red on EVERY deploy since 2026-09-02; trace `paperless.nix` relay gating vs the deployed unit env.
13. **Paperless scheduled-task failure monitoring** + encrypted-tag consistency alert (the 22h "Train Classifier" red was UI-only).
14. **Textfile-collector fixed-name `.tmp` audit** (the niri EACCES class) + stale `btrfs-compression.prom.tmp` check.
15. **Mystery snapshot `data.20260905T2330` root-cause forensics** (Aug-18 dir mtime, zero audit trail).

Repo hygiene noticed in passing:

16. **`stdenv.isDarwin`/`stdenv.isLinux` deprecation warnings** spam every eval/build output (seen in flake check output this run) — a sweep to `stdenv.hostPlatform.isDarwin/isLinux` would quiet the noise; find the source module(s) first.
17. **Queue-footer discipline:** consider a task-queue convention where each run's FIRST commit is its own work commit with the footer, keeping the auto-daemon's heuristic commits out of the cross-reference path.
18. **flake.lock inspection ergonomics:** a tiny `scripts/` helper (or docs snippet) for "print node for input X handling the string-root quirk" would have saved this run 3 calls — candidates: nothing exists today beyond the AGENTS.md prose.

(18 items — "up to 50" treated as a ceiling, not a quota; padding with low-value items would bury the deploy step, which is the only one that matters this week.)

## g) Questions I cannot figure out myself

1. **When should the deploy run?** The fix sits ready, but deploy timing on this box is yours (pressure gates, movie-night overlay rules, the owed flm reboot all interact). Is there a preferred window, and should it ride the same deploy as other pending P0 fixes?
2. **Is store-cached VM-test green acceptable go-live evidence,** or do you want a fresh uncached `checks.x86_64-linux.hermes` run (item f.9) before the deploy as a matter of policy?
3. **If hermes ever lands on a second host,** the pinned `uid = 975` conflicts fail the eval loudly by design. Do you want a per-host uid override option added preemptively, or is loud-eval-failure the desired contract?

---

*Point-in-time snapshot. Live cron errors continue until the next deploy — treat section f.1 as the action item.*
