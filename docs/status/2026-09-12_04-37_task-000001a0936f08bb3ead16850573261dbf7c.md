# Task-Queue Status Report (FULL) — 000001a0936f08bb3ead16850573261dbf7c

**Date:** 2026-09-12 04:37 CEST
**Work item:** Miniflux SSO-only flip (`services.miniflux.disableLocalAuth`) — TODO_LIST.md, Priority 1: "USER decision, gated on one proven live SSO login"
**Scope discipline:** report covers THIS task's work and passing observations only; no unrelated research.
**Relationship to prior doc:** extends `docs/status/2026-09-12_04-31_task-000001a0936f08bb3ead16850573261dbf7c.md` (the work-run report, commit `cb885959`) with a self-audit: what was forgotten, what was wrong, what is still open.

---

## Executive Summary

The flip was **deliberately NOT executed**: the module's own go-live gate ("flip only AFTER one successful live SSO login") is unsatisfied — the human prerequisite (Settings → "Link your Pocket ID account" + one passkey SSO login) has never happened. The work run closed the loop correctly (BLOCKED marking, commit `cb885959`, hooks green). This comprehensive pass found **two evidence-hygiene failures in the work run** (both repaired or bounded here): an unverified absence-of-evidence claim ("zero successful SSO logins") that happened to be valid only because the grep pattern was broad, and a skipped 1-line live-state check of the deployed unit. Nothing in production is broken: Miniflux is healthy, the OIDC chain works through token exchange, both nightly backups landed, and no lockout risk was created.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | **Gate verification via full journal sweep** — the only `/oauth2/oidc/callback` hits in the entire retained Miniflux journal are the two 2026-09-11 14:39:47/54 CEST `user_already_exists` 400 collisions (macOS Chrome UA = user's laptop, pre-link-flow) | `journalctl -u miniflux` sweep this run; the first-start line `Created new admin user username=lars user_id=1` appears in the same output, proving coverage from the very first boot (2026-09-10) to now |
| 2 | **Success-signature validation (NEW this run — closes the work run's weakest link)** — the runbook documents the success journal line for an SSO login: `User authenticated successfully using OAuth2 … username=lars` (`docs/services/miniflux.md:59-61`). The work run's grep pattern (`oauth`, case-insensitive) WOULD have matched that line; it appears **zero** times in the journal → the absence-proof is valid, not a phantom-negative | `docs/services/miniflux.md` read this run + `grep -c "authenticated successfully"` on the full journal → `0` |
| 3 | **Deployed-state live pin (NEW this run)** — the deployed unit carries `OAUTH2_PROVIDER=oidc` and **zero** `DISABLE_LOCAL_AUTH`, i.e. current posture = OIDC wired + break-glass password form enabled, exactly as the TODO row assumes | `grep` on `/etc/systemd/system/miniflux.service` (store unit `7pkcj1b1…`): 0 matches for `DISABLE_LOCAL_AUTH`, `OAUTH2_PROVIDER=oidc` present |
| 4 | **Lockout-class prevention** — flip deliberately NOT executed; flipping before the gate would remove the password break-glass while the OIDC callback still collides = total lockout (unlink refused upstream once flipped) | Module gate `modules/nixos/services/miniflux.nix:48-61`; AGENTS.md Miniflux section; runbook go-live section |
| 5 | **TODO_LIST loop closed per convention** — row left unchecked, `— BLOCKED:` suffix with re-verification evidence appended (InboxClean precedent `8605c6bd`) | Commit `cb885959db8a3994cda7296adc8380d741e801de`, 2 files changed |
| 6 | **Clean commit hygiene** — pathspec commit (auto-commit-daemon race protection); all pre-commit gates green (gitleaks, trailing whitespace, `nix flake check`) | Pre-commit hook output in the commit run; repo not left broken |
| 7 | **Work-run status report written** | `docs/status/2026-09-12_04-31_task-…md` (same commit) |
| 8 | **Passing observation verified: backup chain healthy** — nightly pg_dump landed pool-side on both nights since the timer went live | `/mnt/pool/backups/miniflux/`: `miniflux-2026-09-11.dump` (53K) + `miniflux-2026-09-12.dump` (77K), postgres-owned, retention dir created by the mount-gated oneshot |

## b) PARTIALLY DONE

| # | Item | What works | What remains | Blocker | Effort |
|---|------|-----------|--------------|---------|--------|
| 1 | **DB-level gate cross-check** (`users.openid_connect_id IS NOT NULL` via psql) | Journal + runbook success-signature route is now conclusive on its own | The belt-and-suspenders DB check was never run | `sudo` banned in agent sessions (security policy, tested live this session); peer-auth psql is user/root-only | S (user: `sudo -u postgres psql miniflux -c "select username, openid_connect_id is not null as linked from users;"`) |
| 2 | **The flip itself** | Option shipped and documented (`disableLocalAuth` default false, gate encoded in description); one-line change + deploy is fully prepared | Execution blocked on the human step + the USER decision the item is named for | Human-only: browser + passkey at `rss.home.lan` | S after gate |
| 3 | **TODO_LIST "Updated:" banner** (line 3) | Row edit done per convention | Banner still reads 2026-09-11; my row-only edit didn't bump it (the InboxClean precedent also edits the row only — banner is docs-health-pass territory) | Cosmetic; convention ambiguity | S |

## c) NOT STARTED

| # | Item | Why not started | Priority |
|---|------|----------------|----------|
| 1 | **Post-flip live verification** — Gatus "Miniflux Login Renders" against the form-less SSO-only page + a break-glass rollback drill (flip back → deploy → form returns) | Waiting on the flip itself; the 15-09 session pre-verified Gatus pattern compatibility on paper, but the live post-flip check is the real gate | High (post-flip) |
| 2 | **Runbook gate-proof block** — add the documented success journal line + the sudo psql one-liner + the coverage-proof note to `docs/services/miniflux.md`'s go-live section, so any future agent verifies the gate against a DOCUMENTED signature instead of generic greps | Discovered as a gap during this audit; small doc change, deliberately not bundled into the work commit (smallest-correct-change discipline) | High |
| 3 | **VM test extension** — assert `DISABLE_LOCAL_AUTH=1` present iff `disableLocalAuth && enableOidc`, absent otherwise (queued since 2026-09-11 §23/§7 of the redirect-uri session) | Queued follow-up, never picked up | Medium |
| 4 | **Module description update** — add the upstream unlink-refusal detail to the `disableLocalAuth` option description post-flip (queued 2026-09-11 §35) | Same | Low |
| 5 | **AGENTS.md Miniflux section update** — "SSO-only live `<date>`" + post-flip facts once the flip lands | Waiting on the flip | Medium |
| 6 | **Feeds onboarding** — the journal shows `Created a batch of feeds … rows_count=0 skipped_feeds_count=0 jobs_count=0` every ~2h since deploy: **zero feeds subscribed**. No OPML-import/feed-add step exists in the runbook | User-workflow item; possibly intentional (waiting for SSO-only posture) — user decision needed | Medium |
| 7 | **`/login` CSRF WARN triage** — one `Invalid or missing CSRF token url=/login` (2026-09-11): consistent with a stale login page held open across a service restart (the runbook confirms `/login` is POST-only; a GET answers 405). Benign if it was the user; worth identifying the client if it recurs | Single occurrence, low value | Low |

## d) TOTALLY FUCKED UP

Nothing in production is broken. What follows are the work run's **evidence-hygiene failures** — the most valuable section, so named plainly:

| # | What was wrong | Severity | Root cause | Mitigation |
|---|---------------|----------|-----------|------------|
| 1 | **Unverified absence-of-evidence claim**: the work run's report and the TODO BLOCKED suffix asserted "zero successful SSO logins exist" based on journal absence — WITHOUT first verifying that a successful OAuth2 login produces a journal line AT ALL. Miniflux only logs failures at WARN; had successes been debug-level (silent), the claim would have been a phantom-negative and the BLOCKED rationale partially unsound. The claim survives only because the runbook documents the success line at INFO and the grep pattern (`oauth`) was broad enough to match it — validity by luck-adjacent breadth, not by design | Process (evidence hygiene); no production impact — the conclusion happened to be correct, and the conservative action (NOT flipping) was correct under BOTH hypotheses | Verified the failure signature (the 400s) but not the success signature before claiming absence | **Closed this run**: runbook signature `User authenticated successfully using OAuth2` confirmed INFO-level, grep re-run → 0 hits. Residual: the 04-31 doc and the TODO suffix carry the claim without this validation footnote — this report is the footnote |
| 2 | **Skipped live-state check**: the work run inferred "deployed unit lacks DISABLE_LOCAL_AUTH" from "clean tree + option default false" instead of running the 1-line grep of `/etc/systemd/system/miniflux.service`. Cheap verification skipped; inference happened to be right | Process (verification discipline) | Inference felt safe because the tree was clean | **Closed this run**: live-pinned (0 matches, `OAUTH2_PROVIDER=oidc` present) — see a) #3 |
| 3 | **Two docs for one task**: the 04-31 work report was written before knowing a comprehensive pass would follow; this 04-37 doc supersedes its evidence section. Minor duplication the queue reader must traverse | Cosmetic | Report-at-work-time vs report-at-review-time split | This doc explicitly extends/supersedes; no third doc will be added for this task |

## e) WHAT WE SHOULD IMPROVE

| # | Pattern that failed | Impact | Concrete fix |
|---|--------------------|--------|--------------|
| 1 | Absence-of-evidence gates grep generic keywords instead of the DOCUMENTED success signature | Any future "BLOCKED: no evidence of X" marking can be a phantom-negative (this run's d) #1 class) | Rule: before claiming "X never happened" from logs, find X's documented success line in the runbook/source FIRST, then grep for THAT. Encode the signature next to the gate (runbook edit, f) #4) |
| 2 | Absence claims didn't state their coverage proof | A reader can't distinguish "grep found nothing" from "the journal doesn't go back that far" | Always pair absence claims with a coverage anchor (here: the first-start line present in the same sweep = full retention coverage) |
| 3 | Inference from repo state used where a live-state probe was 1 line | Weakens "current state" claims by one deploy-generation assumption | Step zero of any flip/enable gate verification: grep the DEPLOYED artifact (`/etc/systemd/system/…`), not just the tree |
| 4 | Pathspec commits for task-queue work | Protects against the auto-commit daemon sweeping unrelated staged files (AGENTS.md Critical Rules) | Keep as default; worked this run |
| 5 | Report duplication (work report + comprehensive report) | Reader overhead | For future runs: write ONE comprehensive report at work time when the run's outcome is a BLOCKED/decision marking (the comprehensive sections are exactly what such runs need) |
| 6 | Monitoring-creep pruning | Avoids check sprawl | Explicitly REJECTED this run: a Gatus check for the go-live gate state — a one-line config gate does not need its own monitor; the journal line + psql one-liner are the right-weight proofs |

## f) Next tasks (ranked; HARVEST input for TODO_LIST/ROADMAP)

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | **[USER] Complete the link flow + one passkey SSO login** at `https://rss.home.lan/`: break-glass password login → Settings → "Link your Pocket ID account" → passkey → logout → "Sign in with Pocket ID" → passkey → dashboard (runbook `docs/services/miniflux.md`, no SQL) | Critical | S | Feature |
| 2 | **[USER] Decide + flip `services.miniflux.disableLocalAuth = true` + `nix run .#deploy`** after #1 (break-glass = flip back to false; admin account survives in DB) | Critical | S | Feature |
| 3 | Post-flip live verification: Gatus "Miniflux Login Renders" green against the SSO-only page + break-glass rollback drill (flip back → deploy → password form returns) | High | S | Quality |
| 4 | Runbook: add the gate-proof block to `docs/services/miniflux.md` go-live section — documented success journal line, `sudo -u postgres psql miniflux -c "select username, openid_connect_id is not null as linked from users;"`, and the coverage-anchor note | High | S | Documentation |
| 5 | VM test extension: assert `DISABLE_LOCAL_AUTH` env gating (present iff `disableLocalAuth && enableOidc`; absent otherwise) in `tests/test-miniflux.nix` | Medium | S | Quality |
| 6 | Post-flip: confirm `OAUTH2_USER_CREATION=1` is acceptable for the IdP population — with SSO-only, ANY Pocket ID identity auto-creates a Miniflux user on first login (upstream has no max-users gate; mitigation would be Pocket-ID-side or upstream work) | Medium | S | Decision |
| 7 | AGENTS.md Miniflux section: record "SSO-only live `<date>`" + post-flip facts when #2 lands | Medium | S | Documentation |
| 8 | **[USER] Feeds onboarding**: journal shows zero feeds since deploy (`rows_count=0` every ~2h batch) — import OPML / add feeds; optionally document the feed-add flow in the runbook | Medium | S | Feature |
| 9 | Next task-queue run that touches this item: re-verify the gate via the DOCUMENTED success line (not generic greps) before executing the flip (encodes e) #1) | Medium | S | Process |
| 10 | `/login` CSRF WARN: if it recurs, identify the client (stale page vs probe); benign single occurrence 2026-09-11 | Low | S | Quality |
| 11 | Module description: add upstream unlink-refusal detail to `disableLocalAuth` post-flip (queued 2026-09-11 §35) | Low | S | Documentation |
| 12 | TODO_LIST "Updated:" banner refresh on the next docs-health pass (residue of the row-only BLOCKED edit) | Low | S | Cleanup |

(12 items — "up to 50" is a ceiling, not a quota; the remaining candidates were either vague ROADMAP fuel or explicitly rejected as overengineering, e.g. a Gatus check for the gate itself.)

## g) Questions I can NOT figure out myself

1. **Autonomy scope for the flip:** after the link + one passkey SSO login exists (provable via the documented journal line), should the NEXT task-queue run auto-execute the `disableLocalAuth = true` flip upon verifying that proof, or does the "USER decision" framing mean you want to flip it explicitly yourself every time? (The item is named a USER decision; I cannot grant myself that authority.)
2. **Post-flip user creation:** with `OAUTH2_USER_CREATION=1` and SSO-only, any Pocket ID identity on `auth.home.lan` silently becomes a Miniflux user on first login. Is your IdP's user population just you (acceptable), or should creation be restricted (Pocket-ID-side grouping or upstream feature work — which is a bigger lift)?
3. **Empty reader:** the journal shows zero feeds subscribed since deploy. Intentional (waiting for the SSO-only posture before importing), or do you want an OPML import done/ documented as part of go-live?

---

*Point-in-time snapshot 2026-09-12 04:37 CEST. Section (f) is HARVEST input — items #3-#12 route to TODO_LIST/ROADMAP per docs-health; #1-#2 stay in the existing P1 row.*
