# Status Report — Task 000001a0db313588314c503cdffbc48896b9

**Date:** 2026-09-26 03:33 CEST · **Branch:** master (unpushed) · **Work commit:** `5befa617` ("docs: add calendar-anchor comment to root snapshot retention", 3 files, +10/−2)
**Task:** Add a calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix (weeks start Sunday; weeklies live 14d) and close the matching queue rows.
**Scope discipline:** comment-only change to one config file + queue closure. No drive-by refactors. All evidence below is from THIS run; §f items are drawn from files read during this run plus what I noticed in passing — no unrelated research.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | **Calendar-anchor comment added at the authoring site** — `platforms/nixos/system/snapshots.nix` root instance settings block now documents: retention buckets are CALENDAR-anchored (btrbk 0.32.7 `schedule()`, source-verified 2026-09-25), days start 00:00, weeks start Sunday (`preserve_day_of_week` default), `"3d"` keeps all snapshots from the last 3 day-buckets, `"1w"` keeps the FIRST snapshot of each of the TWO calendar weeks covered, a Sunday-dated weekly lives exactly 14 days, root pin window = 2w sharp, NOT "3d+1w" (~10d) flat | `git show 5befa617 -- platforms/nixos/system/snapshots.nix` (8 inserted lines, comment block above `snapshot_preserve` lines); figures cross-checked against AGENTS.md Snapshot-pinning doctrine + the 11-51 reconciliation report before writing |
| 2 | **Queue row closed in TODO_LIST.md** — item marked `[x]` with DONE note and date, following the sibling convention (`~~title~~` + DONE + pointer) | `git show 5befa617 -- TODO_LIST.md` |
| 3 | **Domain library row closed in docs/todo/storage.md** with the same DONE note — the "queue one-liners and library entries must not drift" rule honored (both edited in the same commit) | `git show 5befa617 -- docs/todo/storage.md` |
| 4 | **Verification suite green:** `nix flake check --no-build` (all checks passed, aarch64-darwin omission expected), `nix fmt --no-update-lock-file -- --ci` (0 changed — formatting clean, zero lock writes), `bash scripts/check-todo-system.sh` (OK), and the full pre-commit hook chain on the final commit (gitleaks, deadnix, statix, treefmt, eval-only flake check — all SUCCESS) | Tool output this run; hook output visible in the amend's transcript |
| 5 | **Commit hygiene:** single properly-messaged commit `5befa617` carrying the exact `Task-Queue-ID:` footer; working tree clean after; nothing pushed | `git log --oneline -2`, `git status` (clean) |

## b) PARTIALLY DONE

Nothing in the deliverable is partial. Two honest "partial-by-design" notes:

| # | Item | State |
|---|------|-------|
| 1 | **CHANGELOG entry for this change** — deliberately skipped: the change is one comment + queue closure; AGENTS.md assigns `[x]`-row pruning to CHANGELOG to the harvest pass. But the sibling reconciliation task (2026-09-25) DID get an immediate CHANGELOG entry from its own session, so the convention is ambiguous. Flagged as §g Q2. | Not done, awaiting convention call |
| 2 | **§f next-step list** — I capped it at ~25 honest items rather than padding to 50. Per the status-report skill, a larger N is a brainstorm (ROADMAP fuel), not a commitment list; fabricating filler items would be worse than a shorter list. | ~25 real items, sorted by impact |

## c) NOT STARTED (deliberately out of scope, still queued — verified present in the queue this run)

| # | Item | Where it lives |
|---|------|----------------|
| 1 | Investigate the `@home-hermes` 09-16 snapshot (9 days old vs `snapshot_preserve 3d`, no weekly in its config) — the one live over-retention candidate | TODO_LIST storage section, right below this task's row; docs/todo/storage.md:16 |
| 2 | Grep-gate for stale root-window restatements (`root ≈ 1-2w`, `3d+1w` out of context) | TODO_LIST:366; docs/todo/storage.md:136 |
| 3 | One-shot observation proof of the 2w-sharp model (read-only btrbk dryrun/info vs live `.snapshots`) — closes the derivation-only gap | TODO_LIST:371 |
| 4 | Extend the calendar-anchor model to `/data` (14d 4w) + pool `services/*` (7d 4w) | TODO_LIST:372 — BLOCKED (owner question, asked 3×) |
| 5 | Final-retention-config owner decision (is 2w-sharp pin intended, or should it tighten?) | TODO_LIST:373 — BLOCKED (owner) |
| 6 | `snapshot_preserve` widening 3d→7d (DAS-outage rollback-window lesson) | docs/todo/storage.md:24 — blocked:user |

## d) TOTALLY FUCKED UP

**Nothing at the content level.** The deliverable is correct, verified, and committed once, cleanly. The honest fumble is process-level:

| # | What | Detail | Damage |
|---|------|--------|--------|
| 1 | **Lost the commit race to the auto-commit daemon THREE times**, burning 3 background shells and ~7 minutes | Attempt 1: `git add` + `git commit` in one command — the daemon committed snapshots.nix (`1720448a`) and the TODO files (`de3c54e4`) between my edits and the commit. Attempt 2: `git reset --soft` to the base + recommit (squashing the two daemon commits, both verified mine-only first) — the daemon raced AGAIN, committing all three files (`66ca2821`) between my reset and my commit. Attempt 3 finally used the documented pattern: verify `git show --stat`, then `git commit --amend` the unpushed HEAD → `5befa617`. | Zero content damage (all three daemon commits contained exactly my files, verified each time; all history unpushed; no forced anything). Cost: noise, time, and 3 throwaway reflog entries. |

The irony is recorded: the correct pattern was in AGENTS.md the whole time ("when the daemon commits your in-flight work: verify its contents with `git show --stat`, then `git commit --amend` the unpushed HEAD"). I reached for reset+squash first because my work was split across TWO daemon commits; the daemon then merged them for me mid-race, making amend the right move anyway. For tiny 3-file queue tasks, **stage everything first, then let the daemon commit, then amend** is the race-free sequence — see §e.

## e) WHAT WE SHOULD IMPROVE (self-review: what I forgot / could do better / could still improve)

1. **Commit sequencing on daemon-active trees.** A single `git add A B C && git commit` is a race window, not an atomic act, when the daemon commits every ~10 min (observed twice within minutes this run). Improvement: for queue tasks, either (a) stage and immediately amend whatever the daemon produces, or (b) accept the daemon commit and amend it — never reset+recommit. Worth one sentence in AGENTS.md's multi-agent discipline section (queued, §f item).
2. **I re-derived a documented pattern from scratch.** The amend-forward doctrine was already written down; my first instinct was a soft-reset squash. Reading the gotcha BEFORE the first commit attempt would have saved two races. General lesson: on this repo, check AGENTS.md's git section before any commit that isn't instantaneous.
3. **CHANGELOG ambiguity left unresolved (see §b/§g).** Not a defect, but I made a silent judgment call; it should be an explicit convention.
4. **Split-brain inventory (brutal-review question 10):** the calendar-anchor doctrine now intentionally lives in FOUR places: AGENTS.md doctrine table, snapshots.nix comment (new), the 11-51 reconciliation report, and CHANGELOG. This is the intended "why at the authoring site" pattern, but it means any future retention change must now touch the comment too. The queued grep-gate (item 2 in §c) is the guard that makes this survivable — one more reason to prioritize it. My new comment's exact shape (quoted `"3d"`/`"1w"` on separate lines, `"3d+1w"` only inside the NOT-negation) should become a legal-context fixture for that gate so it doesn't false-positive on the very anchor it protects.
5. **Ghost systems (question 7): none created.** No code paths, no unwired units. The change is inert documentation in a comment block.
6. **Tests (question 11): no new tests needed and none added — correct for a comment-only change.** Negative-testing a comment would be over-engineering. The change IS guarded by existing layers: pre-commit flake check (eval), treefmt (format), check-todo-system.sh (queue/library consistency), gitleaks (scan). What tests CANNOT catch is doctrinal drift between comment and reality — that is exactly what the §c items 2–3 (grep-gate + observation proof) are for.
7. **Did I lie anywhere? No.** Every claim above has a command or file reference. The one soft spot: §f items beyond ~15 rely on queue files I read this run, not fresh verification — they are pointers, not findings.
8. **Noticed in passing, not acted on (correctly):** `tests/test-hot-db-assertions.nix:81` and `platforms/nixos/system/btrfs-rescue.nix:27` also carry `snapshot_preserve` literals; the `/data` (266-267) and pool `services/*` (295-296) retention blocks in snapshots.nix have NO calendar-anchor comments. Touching any of these was outside the task contract — all routed to §f.

## f) TOP THINGS TO GET DONE NEXT (brainstorm, sorted by impact; S/M = effort)

| # | Task | Effort | Why it matters | Source |
|---|------|--------|----------------|--------|
| 1 | Prioritize the grep-gate for stale root-window restatements (flake check or pre-commit) — patterns `root ≈ 1-2w`, `3d+1w` out of context; allowlist: negations, DONE notes, live-config restatements | M | Four doctrine surfaces now exist; this gate is the only thing keeping them from drifting again (the original wrong-figure episode cost ~6 sessions) | TODO_LIST:366, noticed in passing this run |
| 2 | Seed a grep-gate fixture for the NEW snapshots.nix comment shape (quoted `"3d"`/`"1w"` split across lines; `"3d+1w"` only in the NOT-negation) so the gate stays legal on its own anchor | S | The gate's context rules were spec'd before this comment existed | This run |
| 3 | One-shot observation proof of the 2w-sharp model: `btrbk dryrun`/`info` output vs live `.snapshots` inventory, read-only | S | Turns the hand-derived calendar math (now also asserted in a config comment) into printed observation | TODO_LIST:371 |
| 4 | Investigate `@home-hermes` 09-16 snapshot (9d old vs `3d`, no weekly in config): send-parent hold vs prune gap | S-M | The only live over-retention candidate; my comment makes the expected behavior for `@` explicit, making any deviation there more visible by contrast | TODO_LIST:52, storage.md:16 |
| 5 | Owner decision: extend calendar-anchor comments to the `/data` (`14d 4w`) and pool `services/*` (`7d 4w`) blocks in snapshots.nix now, or wait for TODO_LIST:372's model-extension answer | S | Same misreading class exists for those tiers; two-line comments each | This run, §g Q1 |
| 6 | Owner decision: is `3d 1w` the FINAL retention config, or should it tighten? (If it changes: snapshots.nix comment, test-hot-db-assertions.nix:81 fixture, and gate fixtures must move in ONE commit) | S (decision) | Decides whether the figure this ticket family chased changes again | TODO_LIST:373 (asked repeatedly) |
| 7 | If retention ever changes: update snapshots.nix comment + `tests/test-hot-db-assertions.nix:81` fixture together — add this pairing to the grep-gate's fixture list | S | Fixture and config currently share the literal; silent split-brain risk on any future bump | Noticed this run |
| 8 | Document the daemon-race commit pattern for queue runners in AGENTS.md multi-agent discipline: "stage all files → if the daemon commits first, verify `git show --stat` contains only your files → amend with the real message" | S | This run burned 3 commit attempts; the pattern is derivable but not written as a sequence | This run, §e.1 |
| 9 | Codify "status reports self-harvest §f at authoring time" (one sentence in AGENTS.md TODO-system section) | S | Already queued; this report's §f is again only entombed here until harvest runs | TODO_LIST:54 |
| 10 | CHANGELOG convention call: immediate entry for tiny queue-fixed doc items vs harvest-time-only (see §g Q2) | S | Kills the ambiguity that made me skip it this run | This run |
| 11 | `/data` corruption repair (the btrbk-data blocker; verification-first gate a) SMART = PASS already) — the pool backup tier has NEVER completed for /data | L | Biggest standing data-risk item in the storage domain | storage.md:17 |
| 12 | Mystery snapshot `data.20260905T2330` root-cause (root forensics; rename/restore signature, zero audit trail) | M | Unexplained snapshot appearing outside timer cadence; companion gotcha (never hand-rename snapshots) already documented | storage.md:18 |
| 13 | Owner decision: `snapshot_preserve` widening 3d→7d local (DAS-outage lesson: rollback window collapsed to zero while pool was down) | S (decision) | Directly interacts with items 5-6; same figure | storage.md:24 |
| 14 | After the grep-gate lands: sweep docs/status + docs/planning for stale `1-2w`/`3d+1w` echoes and confirm the allowlist covers historical reports without editing them | S | Historical reports stay untouched by design; the gate must know that | TODO_LIST:366 follow-up |
| 15 | Consider annotating `snapshot_preserve_min = "2d"` with its semantic asymmetry (min floor is AGE-based, unlike the calendar-anchored preserve buckets) | S | Subtle asymmetry an operator could misread the same way the `1w` figure was misread | Noticed this run while writing the comment |
| 16 | Cross-check btrfs-rescue.nix:27's `snapshot_preserve 3d 1w` mention stays in sync if retention changes (comment context, legal as live-config restatement) | S | Same literal, different file; belongs on the gate's allowlist inventory | Noticed in passing |
| 17 | When TODO_LIST:372 (extend the 2w-sharp model to /data + pool) is answered: reconcile those windows in AGENTS.md the same way (they currently carry "≈" approximations) | M | "Consistent with the model" is what the root window claimed before it was reconciled | TODO_LIST:372 |
| 18 | Fold this report's §f into TODO_LIST.md via docs-health HARVEST (do not leave it entombed) | S | The skill's explicit post-report rule; only actionable items, correctly routed | Status-report skill contract |

Items 19-25 (lower priority, from the same queue files, listed for completeness):
| # | Task | Source |
|---|------|--------|
| 19 | Drop unknown flags in the borg-restore-drill fixture's borg stub (junk file lands in cwd, broke push-protection hook twice) | TODO_LIST:53 |
| 20 | Root `@` snapshot set: verify the calendar-anchor comment's "Sunday-dated weekly" claim against the next 2-3 real weekly snapshots (passive observation, zero cost) | This run |
| 21 | If grep-gate lands with fixtures: run it through `scripts/negative-test-lints.sh` per house convention | TODO_LIST:366 spec |
| 22 | Harvest-pass: prune the now-3 DONE rows in docs/todo/storage.md storage section to CHANGELOG (mine + the two 09-25 siblings) | AGENTS.md TODO rules |
| 23 | Consider whether FEATURES.md's snapshots row should mention the comment anchor (it restates the live config legally; likely no-op — one-line check) | Noticed in passing |
| 24 | When the retention final-config decision (item 6) lands: re-run the 11-51 report's 6-site grep to confirm zero stale echoes, then close the ticket family | TODO_LIST:373 follow-up |
| 25 | Candidate ROADMAP fuel (not a task): a tiny `btrbk explain-retention` helper that prints the calendar-anchor math for a given preserve string — would have prevented this whole ticket family | This run |

I stopped at 25 rather than padding to 50: the queue files I legitimately read this run contain ~18 open storage-domain items, and the rest would be fabricated filler. The status-report skill explicitly rules that a larger N is brainstorm, not commitment.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Scope call:** Should the `/data` (`14d 4w`) and pool `services/*` (`7d 4w`) retention blocks in snapshots.nix get matching calendar-anchor comments NOW (same two-line pattern, same misreading class), or does that wait for the owner's answer on TODO_LIST:372 (extend the 2w-sharp model to those tiers)? I cannot decide whether to extend an owner-gated scope preemptively.
2. **Convention call:** For tiny queue-fixed doc/config items like this one, is an immediate CHANGELOG entry expected (precedent: the 09-25 reconciliation session added one), or is the `[x]`+DONE note in the queue files sufficient until the harvest pass prunes to CHANGELOG (the AGENTS.md reading I followed)? The two conventions conflict for this change size.
3. **Fixture coupling:** Is `tests/test-hot-db-assertions.nix:81`'s `snapshot_preserve = "3d 1w"` a live-tracking fixture (must follow any retention change in the same commit) or a fixed test constant (retention changes may leave it)? Deciding requires reading the test's intent, and if retention ever changes (TODO_LIST:373), getting this wrong is a broken gate either way.

---

**Self-check against the brutal-review questions:** forgot nothing in-scope (§e.1-3 are process, not deliverable); nothing stupid added; no lies; no ghost systems; one intended (now guarded) doctrine-restatement surface added; tests correctly unchanged for a comment. The ticket family's remaining risk lives entirely in §f items 1-6.
