# Task 000001a0c64b1e5ab9970fc9fccba6a66e3f — Status + Self-Review

**When:** 2026-09-22 04:30 · **Repo:** SystemNix @ `master` (work commit `564484c2`) · **Scope:** the TODO queue item *"Own-tools NVMe→pool migrations: discordsync + browser-history remain"* (`docs/todo/storage.md` rows 31/32) — verification + queue closure of the 2026-09-21 re-scoped legs (discordsync = BLOBs only, browser-history = DB backup leg).

**Verdict up front:** The agent-side work is **complete and eval-verified, but runtime-zero**. Both legs' config landed in-tree (authored by the parallel 2026-09-21/22 sessions; my run verified and closed it). Nothing is deployed: the running generation (`system-792`, `dj5k7ykb`, nixpkgs-20260919 era) predates all of it, and the box sat in a **98% io-PSI storm** for the whole run, which correctly gated off deploys and VM tests. Every "done" claim below is **tree-true, not runtime-true** until the next `nix run .#deploy`.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| a1 | Item located + correctly scoped: honored the 2026-09-21 re-scope note (discordsync → BLOBs/attachments only; browser-history → backup leg only; DBs stay NVMe/hot-tier per Phase-2 verdicts) | `TODO_LIST.md` queue row; `docs/todo/storage.md` before my edit |
| a2 | Discordsync pool-leg config verified complete in-tree: `attachmentsDir` option, `ATTACHMENT_STORAGE_PATH` env (mkForce), service `RequiresMountsFor`/`ReadWritePaths` pool gating, mount-gated `discordsync-attachments-dir` leaf creator (CAP_CHOWN+FOWNER+DAC_OVERRIDE, mount-root ReadWritePaths), one-time STATIC `discordsync-attachments-migrate` oneshot (stop → `rsync -aHAX` → checksum verify → `rm -rf` source → restart; `ConditionPathIsDirectory` skip on later runs) | `modules/nixos/services/discordsync.nix:234,367-375,381-384,447,455-505,507-590` |
| a3 | Browser-history backup-leg config verified complete in-tree: `dbBackup` option (default OFF), `browser-history-backup-dir` mount-gated leaf (provisioner-restarted), `browser-history-backup` oneshot (online `sqlite3 .backup`, 14d retention, `RequiresMountsFor backupDir`, `CAP_DAC_READ_SEARCH` for the 0700 DynamicUser StateDirectory — cv-backup silent-no-op class handled), 02:15 Persistent timer, integration-registry `backup` row (maxAge 25h) gated on `dbBackup.enable` | `modules/nixos/services/browser-history.nix:201-213,639-725,779-785` |
| a4 | Host opt-ins present: `discordsync.attachmentsDir = "/mnt/pool/services/discordsync/attachments"`, `browser-history.dbBackup.enable = true` | `platforms/nixos/system/configuration.nix:503,522` |
| a5 | deploy.sh knows all three units: `discordsync-attachments-dir` + `browser-history-backup-dir` in the provisioner restart loop; `discordsync-attachments-migrate` in the dedicated `--no-block` block (static-unit is-enabled-gate trap avoided) | `scripts/deploy.sh:467,488-491,707-709` |
| a6 | Eval verification: `nix flake check --no-build` → **ALL checks passed** (includes every eval-time audit: deploy-restart-audit, mount-gating, gatus-pattern-lint, sops-key-audit, systemd-shape; aarch64-darwin omission is the documented expected warning) | run at HEAD `93d439f3`, 04:1x |
| a7 | Targeted evals of rendered evo-x2 config: migrate unit `Type=oneshot`; `discordsync.serviceConfig.ReadWritePaths = ["/var/lib/discordsync","/mnt/pool/services/discordsync/attachments"]`; `browser-history-backup.timerConfig.OnCalendar = "*-*-* 02:15:00"`; backup unit `CapabilityBoundingSet = "CAP_DAC_READ_SEARCH"`; `backup-coordination.backups.browser-history.maxAgeHours = 25` | `nix eval .#nixosConfigurations.evo-x2...` outputs |
| a8 | Live-state ground truth established (no guessing): deployed gen `system-792` → `dj5k7ykb…26.11.20260919.20b1ddd`, profile ANCHORED (`/run/current-system` == profile target); zero new units in `/etc/systemd/system`; `/mnt/pool/services/discordsync/attachments` and `/mnt/pool/backups/browser-history` do not exist → the work is correctly **[blocked:deploy]**, not "done" | `readlink`, `ls /etc/systemd/system`, `ls` pool paths |
| a9 | Queue/library closed in sync per the TODO-system contract: `TODO_LIST.md` migration row left unchecked + `— BLOCKED:` reason; browser-history-backup row marked `[x]` code-done; `docs/todo/storage.md` row 31 → `[blocked:deploy]` with post-deploy completion checks, row 32 → `[x]` code-landed; `check-todo-system.sh` → "OK: TODO queue/library structure clean" | commit `564484c2` |
| a10 | Committed correctly under the multi-agent discipline: PATHSPEC commit (`-- TODO_LIST.md docs/todo/storage.md`), pre-commit hooks green (gitleaks, eval-only flake check), exact `Task-Queue-ID:` footer, **not pushed** | commit `564484c2` |
| a11 | Correctly did NOT deploy and did NOT run VM tests: io PSI some avg10 = **98.5%** (avg60 98.7%) at session time — the deploy pressure gate refuses by design, VM tests are house-PSI-gated, and a switch would have shipped parallel sessions' in-flight tree | `/proc/pressure/io` at 04:0x |

## b) PARTIALLY DONE

| # | Item | What works | What remains | Blocker | Effort |
|---|------|-----------|--------------|---------|--------|
| b1 | **The work item itself (the migrations)** | 100% of config; eval-verified; deploy wiring proven | The actual data movement (~40 GB attachments off NVMe) and the first backup run: 0% | One `nix run .#deploy` — refused by the pressure gate during the storm; also carries the whole parallel-session batch | S (the deploy) + M (verification) |
| b2 | **Verification depth** | Eval-shape checks (a6/a7) all green | **No test executes either new code path.** `grep -rn "dbBackup\|attachments" tests/*.nix` → zero hits; `tests/test-browser-history.nix` exercises the default-OFF path only; there is no `test-discordsync.nix` at all. My verification proved the units RENDER, not that they WORK | Test coverage gap (see d1/d2) | M |
| b3 | **Deploy-day alert behavior** | Traced during this review: `backup-coordination` emits fail-closed — a never-run backup yields `backup_healthy{backup="browser-history"} 0` → `backup_all_healthy 0` → Gatus "All Backups Healthy" **red from the deploy until the first 02:15 run (≤ ~24h)** (`backup-coordination.nix:61-77,159-167`) | Whether pre-deploy §10 cleanly auto-loans the new metric series for this specific diff is an unverified assumption (the 2026-09-13 auto-derive mechanism should handle it; I did not run the §10 fixture) | None — but see g3 | S |
| b4 | **Closure semantics of queue row 24** | Marked `[x]` code-done (restic-row-30 precedent: deploy-gated first run noted in the body) | Defensible but ambiguous: "done" here means *implemented*, not *running* — the storage row and body say so explicitly | House semantics for `[x]`-with-deploy-gate are convention, not written rule (see e4) | — |

## c) NOT STARTED

Scoped to THIS task's chain (the wider storage domain stays tracked in its own `docs/todo/storage.md` rows — not re-listed here):

1. **The ~40 GB attachments migration run** — blocked:deploy (b1). Nothing has moved; `/var/lib/discordsync/attachments` is untouched.
2. **First `browser-history-backup` run** — blocked:deploy; unit + timer absent from the host (a8).
3. **Post-deploy completion checks** (written into storage row 31): pool leaf exists with correct owner/mode; source dir gone; discordsync healthy on the pool path; day-1 dump file; `backup_all_healthy` green.
4. **dbBackup-ON-path VM coverage** — zero coverage today; not filed until this review (now appended to storage.md backlog).
5. **Stub/fixture test for `discordsync-attachments-migrate`** — the destructive script has never been executed anywhere; not filed until this review (now appended; see d1).
6. **Discordsync DB leg (Samsung hot tier)** — deliberately out of scope: Phase-2 hot-db wave, `[blocked:user]` sudo window. This task's re-scope explicitly excludes it.

## d) TOTALLY FUCKED UP

1. **An untested destructive script auto-fires at the next deploy.** `discordsync-attachments-migrate` stops the service, rsyncs ~40 GB, and `rm -rf`s the source — and it has **never been executed anywhere**: no fixture, no VM run, no manual dry-run. It starts no-block the moment anyone deploys. Mitigations are real (checksum-verify gate before removal; source kept on every failure branch; `ConditionPathIsDirectory` makes re-runs idempotent; OnFailure pages), but this is exactly the anti-pattern the repo's own doctrine names — `docs/todo/storage.md` row 25: *"an untested destructive script is the worst first contact"* (written for `migrate-hot-db.sh`) — and that doctrine was **not applied to this script** by the landing sessions. **Severity: high (data-adjacent, mitigated). Root cause: module-shape review treated as verification. Workaround: fixture-test before the deploy (filed, f-group-2).**
2. **My review had the same blind spot.** I verified what the landing sessions BUILT, not whether it was TESTED. The contract explicitly allowed appending follow-up queue items — I used that permission **zero times**, when at least two legit rows existed (d1's fixture test; the dbBackup VM coverage). I only surfaced both gaps in THIS self-review. That is the run's real process failure: confirmation-shaped verification. A verification pass should adversarially ask "what would break this and when would we find out" — the untested-destructive-script question is precisely that, and I didn't ask it.
3. **Deploy debt is compounding.** The running system is a whole batch behind the tree: this task + restic-app-dumps + crush-hot-db guard fixes + hot-db anti-shadow fix + HM cache symlinks + boot-mirror sync + more are all runtime-absent. Each storm-day that the deploy is refused makes the NEXT deploy bigger — bigger blast radius, more exit-4 surface (the known cv-perms / db-heal / hot-user-caches classes), harder attribution when something regresses. All `[x]`/"done" marks describe unbuilt state until the switch lands. Not caused by this task; massively amplified by it being stuck.
4. **The box is in the freeze-prone regime right now.** io PSI some avg10 98.5% / avg60 98.7% (memory fine: ~3%, MemAvailable ~45 GiB; zram ~88% full, noticed in passing). Everything heavy is correctly gated — but the storm itself is the standing freeze risk, and it is what turned a "verify and close" run into a "verify, close, and wait" run.
5. **Guaranteed post-deploy red window (minor but certain).** Per b3: "All Backups Healthy" will be red up to ~24h after the deploy unless the owner hand-starts the first backup. Discord noise is guaranteed; §10 auto-loan should keep the DEPLOY unblocked (unverified assumption).

## e) WHAT WE SHOULD IMPROVE

1. **Add a mandatory test-coverage step to queue-run verification.** When a run verifies landed-by-others code, it must grep `tests/` for the new option/unit names and FILE the gap if zero hits — not just eval-shape-check. (Would have caught d1/d2 during the run, not in a post-hoc review.)
2. **Generalize the row-25 destructive-script doctrine.** ANY oneshot whose script `rm -rf`s a source tree must ship with a stub/fixture test in the SAME change (forgejo-scripts-fixture / PATH-stub pattern). Enforce mechanically: a pre-commit grep-guard for `rm -rf` inside systemd `script =` bodies without a referencing fixture check would have caught this class at commit time.
3. **Make deploy debt visible.** `[blocked:deploy]` rows should carry a one-line batch inventory ("rides the deploy with: X, Y, Z") so whoever fires the switch knows the blast radius before stc runs — today that knowledge is scattered across AGENTS.md and status reports.
4. **Write down the `[x]`-with-deploy-gate semantics once** (TODO-system house rules): "code-complete + first-run-deploy-gated" may be marked `[x]` with the gate noted (restic precedent), vs. migrations that ARE the run stay open. I re-derived this mid-run; it should be a rule, not a judgment call.
5. **Give deploy-gated first runs an owner affordance.** Post-deploy, `sudo systemctl start browser-history-backup discordsync-attachments-migrate` kills the red window and gets the migration done in a chosen window instead of deploy-time no-block. Document the one-liners in the rows/runbooks.
6. **Sweep smoke surfaces when adding deploy-converging units** (the PapDashboard-merge rule, generalized): add post-deploy-check entries for new units in the same change, or state why not.
7. **My own probe hygiene:** two of my eval probes failed on guessed attribute names (`ATTACHMENT_DIR`, `serviceConfig.User`) before I read the module shape. Read first, probe second — cost two round trips.

## f) TOP THINGS TO GET DONE NEXT (brainstorm for HARVEST; impact/effort/category per item)

**Group 1 — this task's completion chain (all behind ONE deploy):**

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | Fixture-test `discordsync-attachments-migrate` (stop/copy/verify/rm/restart + all 4 failure branches keep source) BEFORE the deploy that triggers it — PATH-stub pattern | Critical | M | Quality |
| 2 | When io PSI some avg10 < 20% AND no Zone-6 trip in the last 60 min: `nix run .#deploy` (carries the whole batch; expect §1 `--keep-going` enumeration if anything stacked) | Critical | M | Ops |
| 3 | Verify deploy anchored post-switch: `readlink /nix/var/nix/profiles/system` == `/run/current-system` (exit-4/anchor doctrine) before trusting any smoke verdict | High | S | Ops |
| 4 | Watch `discordsync-attachments-migrate` to completion (hours on the shared USB link): journal must end "verified identical, source removed" | Critical | S | Verify |
| 5 | Confirm pool leaf: `/mnt/pool/services/discordsync/attachments` exists, `discordsync:discordsync` 2770 | High | S | Verify |
| 6 | Confirm discordsync.service active with `ATTACHMENT_STORAGE_PATH` on the pool path; capture works; `/api/health/backup` DLQ depth sane after restart | High | S | Verify |
| 7 | If migrate fails mid-way: source is kept by design — fix cause, re-run `sudo systemctl start discordsync-attachments-migrate` (idempotent); document this re-run one-liner in `docs/services/discordsync.md` | Medium | S | Docs |
| 8 | Confirm `browser-history-backup-dir` ran + day-1 dump `browser-history-db-*.sqlite` exists in `/mnt/pool/backups/browser-history` | High | S | Verify |
| 9 | Confirm `backup_all_healthy 1` by 02:15+1d; if §10 loaned the new series pre-deploy, retire the loan entry | Medium | S | Verify |
| 10 | Owner option to kill the red window: hand-start `browser-history-backup` right after the switch (see g3) | Medium | S | Ops |
| 11 | Verify the REST of the batch post-switch (restic-app-dumps-setup, hot-db/crush-hot-db fixes, HM cache symlinks, boot-mirror-sync, …) — batch inventory before the deploy | High | M | Verify |
| 12 | Measure freed NVMe space post-migration (~40G expected; remember snapshot pinning delays the reclaim) + record migration timing in `docs/services/discordsync.md` | Medium | S | Docs |
| 13 | Flip AGENTS.md discordsync/browser-history sections from "deploy-pending" to live state after verification (the established pattern) | Medium | S | Docs |
| 14 | `nix run .#pre-reboot-check` before ANY reboot of the new generation | Critical | S | Ops |

**Group 2 — test-coverage gaps found this run (filed to storage.md backlog):**

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 15 | Extend `tests/test-browser-history.nix` with a dbBackup-enabled variant (pool disk via `emptyDiskImages`, test-cv pattern): backup-dir leaf, day-1 dump, registry row → `backup_healthy{backup="browser-history"} 1` from the collector | High | M | Quality |
| 16 | Fixture test for `discordsync-attachments-dir` leaf creator (owner/mode; can fold into #1's harness) | Medium | S | Quality |
| 17 | Assert the `attachmentsDir = null` legacy shape still renders (tmpfiles in-state subdir) in a VM/eval test — protects pool-less hosts | Medium | S | Quality |
| 18 | Extend the pre-deploy §10 fixture with a backup-coordination-emitted series to prove the auto-loan handles registry-gained `backup_healthy` labels | Medium | M | Quality |
| 19 | Add post-deploy-check smoke lines for the two new units (or record why not) | Medium | S | Quality |

**Group 3 — noticed in passing (deploy-pending batch / storage domain, already tracked in rows):**

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 20 | storage row 64: rebuild `hot-db` + `crush-hot-db` VM tests green on the current tree (heavy-job, PSI-gated < 20%) | High | M | Quality |
| 21 | storage row 25: `migrate-hot-db.sh` stub-fixture test BEFORE its first user window (same doctrine as f1) | High | M | Quality |
| 22 | storage row 75: confirm the next 23:00 btrbk-root send lands (2-day WARN / 3-day FAIL boundary live) | Critical | S | Verify |
| 23 | storage row 30: restic first run + `restic check`/restore smoke (deploy-gated with f2) | High | M | Verify |
| 24 | storage row 87: restic VM test first green RUN (PSI-gated) | Medium | M | Quality |
| 25 | storage row 82: post-deploy confirm the 3 HM cache symlinks + `migrate-buildcache-fallback-caches` activation | Medium | S | Verify |
| 26 | storage row 81: `~/tmp/go-lint` 1.8G reclamation path (extend buildcache-gc or move fallback to tmpfs) | Medium | M | Cleanup |
| 27 | boot-mirror: `nix run .#boot-mirror-activate` (owner sudo) + pre-reboot-check §11 flip | Medium | S | Ops |
| 28 | Expect the known exit-4 classes on the batch deploy (cv perms, db-heal timeout, hot-user-caches) — fix-forward, re-run | Medium | M | Ops |
| 29 | docs-health HARVEST of this report's (f) into TODO_LIST/ROADMAP (this file is a timestamped tombstone otherwise) | Medium | S | Docs |
| 30 | Prune the `[x]` rows (storage 32, restic 30/87, TODO row 24) to CHANGELOG at the next docs-health pass | Low | S | Docs |
| 31 | Discordsync DB → Samsung hot tier stays Phase-2 `[blocked:user]` — no action now; keep the re-scope note intact | Low | S | Docs |
| 32 | Zram fill was ~88% during the run (noticed) — glance at the ZRAM Fill trend vs the 50%-sized device before the deploy window | Low | S | Verify |

**Group 4 — process improvements from (e), as trackable items:**

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 33 | Codify the test-coverage grep step into the queue-run playbook/contract text | Medium | S | Process |
| 34 | Generalize row-25 doctrine: fixture required in the same change as any `rm -rf`-source migration oneshot; consider a pre-commit grep-guard | Medium | M | Process |
| 35 | Batch-inventory line on `[blocked:deploy]` rows | Low | S | Process |
| 36 | Write the `[x]`-with-deploy-gate semantics into the TODO-system house rules | Low | S | Docs |
| 37 | Owner-affordance one-liners (hand-start first run) into the affected rows/runbooks | Low | S | Docs |

*(37 items — the guide's note applies: above ~25 this is a brainstorm; HARVEST must route with extra rigor. Items 1–14 and the two filed rows are the real commitable work; the rest is Roadmap fuel or already-tracked rows.)*

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Who fires the batch deploy, and under what rule?** I chose not to deploy (98% io storm + the switch would carry every parallel session's in-flight tree). But `[blocked:deploy]` rows are piling up across sessions and each storm-day grows the blast radius. Should queue runs auto-deploy when calm (io avg10 < 20%, no Zone-6 trip in 60 min), or is the switch always owner-triggered? I can decide the *gate*; I cannot decide the *authority*.
2. **May the ~40 GB attachments migration ride ANY deploy, or must it be pinned to an away/night window?** The unit currently starts no-block at every deploy (deploy.sh no-block block). It runs ioTier.background on the shared USB HDD link for hours and stops discordsync during the copy. Only you know the household usage tolerance — if it must be night-gated, the right change is a start-gate (env/manual) rather than deploy-triggered.
3. **Is a ≤24h red "All Backups Healthy" window after the deploy acceptable?** Confirmed fail-closed behavior: until the first 02:15 run, `backup_all_healthy 0` → Gatus/Discord red. The alternative is an owner hand-start right after the switch (`sudo systemctl start browser-history-backup`). Alert-noise tolerance is your call, not mine.

---
*Report format note: user explicitly requested `.md` at this path — the status-report skill's HTML default was overridden per its own escape clause. Sections follow the user's a–g shape (50-cap f, 3-cap g).*
