# Task 000001a0b68a0a7d3cf616a18addd70234ff — Status Report (Review Fix)

**Date:** 2026-09-19 00:35 CEST
**Work item:** Review finding on the browser-history registration-gate verification (task 000001a0b21d7d821da36f61711e46394735) — "Close-out headline contradicts its own live evidence: gate did NOT hold in prod."
**Outcome:** DONE — both contradictory headline sites corrected; item stays `[x]` with an accurate outcome statement. Commit `0e65881f` (the fix itself; this report rides the same Task-Queue-ID footer).

---

## a) FULLY DONE

1. **Dangling-commit disposition performed correctly.** `git cat-file -e 7c07de71…` → the rejected change (the 03-56 status report) still exists in history; the anchor text `**DONE 2026-09-18 … NO chain work needed — the gate is live in the deployed binary**` was located live in the CURRENT tree (`TODO_LIST.md:40`), so the finding applies as filed. No re-anchoring gymnastics needed.
2. **The finding was verified against the evidence before touching anything.** Re-read the full status report (`docs/status/2026-09-18_03-56_task-000001a0b21d7d821da36f61711e46394735.md`): §a.1 records the first probe `POST /auth/register` → **201** while real users existed and the read-model count was 0. The reviewer is right: the deployed gate accepted ONE unauthorized registration in prod — the item's core question ("is the gate closed LIVE?") has the answer NO in effect, and the old headline asserted the opposite in the same breath.
3. **TODO_LIST.md:40 headline corrected (the anchored site).** The close-out now reads: "verification complete, NO chain work needed — the gate CODE is live in the deployed binary, but the effective close FAILED live: the first probe returned 201, i.e. the deployed gate accepted ONE unauthorized registration in prod because the read-model count was 0 (count-gap finding below; closed only by the backlogged upstream count-gap fix)". Item KEPT `[x]` per the reviewer's first option: the text now accurately states the outcome (the VERIFICATION work item is complete; the gate-code liveness claim is narrowed to what the scratch-run actually proved; the prod bypass is stated as a failure, not a footnote).
4. **The status report's Outcome headline corrected too.** The reviewer's finding explicitly named BOTH documents ("The status report and TODO_LIST both record that…"), so fixing only TODO_LIST would have left the contradiction standing at the second site. The report's Outcome line now carries an inline `**CORRECTED 2026-09-19**` annotation explaining the original headline error, referencing §a.1's own evidence and §e.2's count-gap finding — following the docs-health ANNOTATE convention (inline resolution, not appendix-only). §a.1's body was left untouched: it honestly records the 201 already; only the interpretive headline lied.
5. **Swept for the same claim class elsewhere** — `grep -rn "gate is live|gate verified live"` over all `.md`: only the two fixed sites remained; `FEATURES.md:526`'s hit is an unrelated startup-speed claim, deliberately not touched (scope discipline).
6. **Committed cleanly.** Pathspec commit (`git commit -- TODO_LIST.md docs/status/…`) per the concurrent-agents rule — exactly 2 files changed, 2 insertions(+), 2 deletions(-), tree clean afterwards. Pre-commit gates green: gitleaks (no secrets), trailing-whitespace, `nix flake check` eval-only. Single footer `Task-Queue-ID: 000001a0b68a0a7d3cf616a18addd70234ff`, not pushed.
7. **Backlog hygiene respected.** The count-gap fix (TODO line 43) and probe-user cleanup (line 42) already existed from the original task — no new items were needed and none were duplicated. Nothing in this fix spawned new work.

## b) PARTIALLY DONE

1. **The underlying security gap itself is NOT closed** — deliberately out of scope. The deployed browser-history still accepts one ghost registration after every read-model rebuild until the upstream cqrs-htmx count-gap fix lands and propagates (TODO line 43). This run fixed the DOCUMENTATION lie, not the gate. The honest state after this commit: gate code live, effective close open by exactly one slot, fix backlogged.
2. **§a.1's item title ("Gate live-verified in the deployed binary") left as-is** in the 03-56 report. It is defensible as "the gate was probed live" and its body states the 201 plainly, but a hyper-literal reader could still trip on it. Judged below the headline threshold; called out here so the judgment is visible.

## c) NOT STARTED

1. Nothing from the review finding itself — it was a one-site text correction and both sites are done.
2. All queued follow-ups from the original task stand untouched (probe-user deletion, count-gap fix, import-gate propagation, OAuth2 live test, CSRF smoke recipe). Correctly not started: the reviewer's disposition explicitly forbids unrelated changes, and the queue owns pacing.

## d) TOTALLY FUCKED UP (this run's honest self-review)

1. **The original close-out (previous run) wrote the headline its own evidence refuted** — this finding is 100% a self-inflicted documentation failure. The 03-56 report's §a.1 sat right next to the 201 fact and the §e.2 count-gap finding, yet the headline (and the TODO close-out copied from it) said "the gate is live". The failure mode: pattern-matching the expected outcome ("binary gates → verification passed → DONE, gate live") instead of reading the prod result against the item's ACTUAL question ("is the gate closed?"). The scratch-run proof (fresh DB: user1 201 → user2 403) is evidence about the BINARY; the prod probe is evidence about the SYSTEM — the headline conflated them, and the count-gap is exactly the variable that separates the two. This is the same phantom-green class the repo keeps documenting (fixture ≠ prod truth; green where it can't see), committed as prose instead of a check.
2. **A reviewer had to catch it.** The self-review section (§e) of the original report had the count-gap finding IN HAND and still framed it as "the gate is live, one more CAN slip through" — understating a live, already-exploited bypass as a hypothetical future risk. The correct framing was available in the same session and was not used. Rule going forward: when a verification probe MUTATES prod state, the mutation is not a side note — it IS the finding, and the close-out must lead with it.
3. **Minor: the first `edit` attempt failed** ("you must read the file before editing") because I grepped the line instead of viewing it first — one wasted round trip, self-corrected by viewing then editing. Trivial, but it's exactly the discipline the critical rules spell out.

## e) WHAT WE SHOULD IMPROVE

1. **Close-out headline rule:** a DONE line must state the outcome OF THE THING THE ITEM ASKED, not the outcome of a sub-proof. "Binary gates on a fresh DB" does not answer "is the gate closed in prod". When a live probe's first result contradicts the expected invariant, the item's verdict is that contradiction — everything else is commentary. Worth a line in AGENTS.md's cross-cutting lessons if this class recurs (it is adjacent to the existing "assert WHICH entity served it" rule: assert WHICH question your evidence answers).
2. **Security findings discovered mid-verification should be worded at the strength they were observed.** "Was 0 at probe time" + "one more CAN slip through" described an ACTIVE bypass (it had already let one through, during the probe itself) in future-tense hypothetical language. Strength-of-claim discipline: observed-once = happened, not "can".
3. **Docs-only review fixes are cheap — the expensive part was upstream.** The queue worked as designed (work → review → finding → fix), and the fix was 2 lines. The residual risk is that the 03-56 report is already cross-referenced (commit `e033d4d4` cited inside itself); annotation rather than rewrite was the right call so the correction is itself auditable.
4. **The count-gap + import-hole pair (TODO lines 43/44) is the remaining real exposure** — two bypass classes on one MaxUsers invariant, both queued. Until then the deployment accepts up to one ghost user per read-model rebuild; the Caddy-fencing idea (03-56 §f.47) remains the cheap defense-in-depth bridge if the queue wants one.

## f) Up to 50 things we should get done next

_(Agent-executable backlog candidates. Items 1-6 already EXIST in TODO_LIST.md from the original task — listed here for cross-reference only, NOT re-appended.)_

1. _(exists, TODO line 42)_ Delete prod probe user `probe-gate@example.com` (root-only SQL: read-model row + `user.registered` journal events, keep `lars@larsartmann.cloud`; verify `browser_history_user_count` and a fresh 403).
2. _(exists, TODO line 43)_ Upstream cqrs-htmx count-gap fix: gate counts from the authoritative store (or assert Count() ≥ 1 post-hydration) + regression test on a hydrated multi-user read model.
3. _(exists, TODO line 44)_ Propagate the import-gate fix to browser-history: tag cqrs-htmx `68a2cd68` chain → goModules probe → SystemNix bump → live JSON-import-over-cap rejection.
4. Post-count-gap-fix: re-run the EXACT live verification from 03-56 and re-close TODO line 40 a second time — this time the first probe must 403 on prod (that is the acceptance criterion the original close-out should have used).
5. Add the CSRF cookie+header dance as a post-deploy smoke for the registration gate (03-56 §f.4) — today a csrf-wall 403 is indistinguishable from a closed gate; the smoke would have caught the prod bypass class only with the dance, but the DANCE is what makes future gate probes trustworthy at all.
6. When rewriting close-outs after upstream fixes: derive the headline from the acceptance criterion, and paste the probe transcript line that proves it (the 201-vs-403 line IS the verdict).
7. Sweep historical status reports for the same headline-vs-evidence pattern (a claim sentence followed by a contradicting data point) — cheap grep pass over `docs/status/archived/` for "verified live|DONE" near "first attempt|first probe|201". This report's own §d.1 pattern may have siblings.
8. Gatus check candidate (03-56 §f.5, still unqueued as a check): alert if `browser_history_user_count` DECREASES or exceeds its pre-probe baseline — turns the count-gap's exploitation signal into paging rather than a docs line.
9. Consider the Caddy loopback/LAN fence on `/auth/register` as an interim mitigation while items 2/3 sit in the queue (03-56 §f.47/§g.3) — it converts the count-gap from "one ghost user per rebuild" to "zero from off-box".
10. Docs-health pass candidate: when a close-out gets corrected by review (this commit), check whether any OTHER doc citing the wrong conclusion needs the same annotation (the 03-56 report cites commit `e033d4d4`; TODO line 40 was the only other consumer — verified).

_(Items 11-50: noticed-in-passing from the original task's §f list, unchanged and un-researched — the queue's pacing decision, not this task's:)_

11. Mail relay: verify `larsartmann.cloud` in Resend (SPF replace of the `-all` lockdown + DKIM/MX).
12. Pocket ID "Send test email" re-click after the SMTP fix deploy.
13. The owed REBOOT (flm :52626 zombie corpse, llama D-state pile) — `nix run .#pre-reboot-check` first.
14. crush-hot-db first-migration verification (symlink sweep + io PSI baseline).
15. InboxClean token rotation (user-run).
16. Paperless AI ↔ llama-rag embeddings E2E check at the next deploy that re-enables llama-rag.
17. llama-embeddings/reranker `/health` verification post llama-rag re-enable (still disabled since freeze-5).
18. flm v1.0.3 staged go-live after the reboot.
19. Monitor365 re-enable owner decision (private wireguard-collector crate).
20. Turso plan decision for DiscordSync (upgrade vs permanent local-only).
21. Hetzner StorageBox + BorgBackup offsite leg implementation.
22. Per-service BTRFS subvol doctrine Phase-2 `services.hot-db` (fold crush-hot-db in when it lands).
23. `@nix` dead-subvol deletion on the QLC disk (safe after the reboot).
24. groq API key decision for CV ChatService (`/health` overall at `warn`).
25. CV upstream: adopt `go_1_27` to lift the branch-ref-governed hold.
26. CV goModules re-probe when upstream moves.
27. Paperless retro-decrypt repair deploy (upstream 2026-09-12, not yet deployed).
28. Miniflux `disableLocalAuth` go-live gate: prove one SSO login, then flip.
29. browser-history macOS agent: must use a bh_ DB token (env-token path invisible to agent-activity monitor).
30. signoz-src / signoz-collector-src bump = migration-review task.
31. SigNoz exemplar persistence revisit when upstream ships it.
32. dnsblockd release tag carrying the cached-health fix.
33. `clickhouse-backup` follow-up (telemetry has no backup coverage).
34. SigNoz read-only zombie ClickHouse tables (~10 GiB) — human DROP TABLE decision.
35. DiscordSync DLQ poison events: keep documented, no purge.
36. Hermes workspace layout decision (deferred).
37. Hermes private-repo PAT paste if hermes-git workflows need it.
38. `workload-admission` adoption sweep for repo-tree scans.
39. Fixed-tmp textfile collector conversion for the two ALLOWLISTED units (on touch).
40. btrbk offsite/retention review at ~50% pool usage.
41. NixOS VM test for the CSRF-then-gate layering.
42. `browser_history_user_count` onto a SigNoz dashboard.
43. Runbook entry for the browser-history gate-verification recipe.
44. Trim `docs/status/` noise; archive this report per convention.
45. Review whether the 5 bring-up users in the prod browser-history DB are all wanted (owner decision; interacts with the count-gap).
46. Queue pacing: count-gap fix (item 2) BEFORE any multi-user feature work on browser-history.
47. Caddy fence (see item 9) — same idea, queue-priority decision.
48. Verify `/auth/import` unauthenticated-unreachable on the deployed reverse-proxy path.
49. Feed the "CSRF sits in front of the gate" fact into the AGENTS.md browser-history section.
50. After items 2+3 land upstream: bump, re-verify, re-close (same as item 4 — queue decides whether they stay separate).

## g) Questions I can NOT figure out myself

1. **For the acceptance re-run (§f.4): should the re-verification wait for the count-gap fix, or is a post-probe-user-cleanup re-run (TODO line 42 first, then re-probe) acceptable as the interim "closed" proof?** After deleting `probe-gate@example.com`, the count may return to 0 — meaning the FIRST probe of a re-run would 201 AGAIN (another prod mutation, another debris user). Re-probing before the fix may be structurally incapable of producing a clean "closed" verdict; should the item instead accept "closed = first-probe-403" only post-fix, leaving prod unprobed until then?
2. **Do you want the Caddy fence (§f.9/47) as an interim mitigation, or is the LAN trust model acceptable for a single-slot bypass?** It is a one-block Caddy change plus an eval-time vHost consideration, but it changes the threat model for local tools (the agent's own probes would need the fence's allowlist or localhost exemption).
3. **Should the corrected 03-56 report be left annotated (current state) or is a `docs/status/archived/` move preferred now that its headline is corrected?** The ANNOTATE convention resolved numbered items inline, but the report's own §f.44 flagged archiving; the correction makes it a slightly better historical record, and I don't know your archival trigger (age? queue completion? next report on the same topic?).

---

_Point-in-time snapshot, 2026-09-19 00:35. Fix commit `0e65881f`; review finding anchored to commit `7c07de71` (alive, anchor re-located in current tree). Task-Queue-ID cross-reference: `000001a0b68a0a7d3cf616a18addd70234ff`._
