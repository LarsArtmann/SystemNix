# Status Report — Task 000001a08ef20c1a4e464fb20c81f52bd3c5 (2nd dispatch)

**Date:** 2026-09-11 08:23 CEST
**Task (TODO_LIST P0):** Reboot into kernel 7.2.2 + FastFlowLM v1.0.3 retry (USER-gated)
**Dispatch:** 2nd (queue re-dispatch ~20 min after the 08:02 first dispatch)
**Work commit:** `6841a50b` (status addendum + static loader validation + footer cross-reference)
**Format note:** the status-report skill's canonical format is HTML; this report is Markdown because the dispatch explicitly specified a `.md` path — user instruction wins, flagged per the skill's own rule. The brutal-self-review skill (triggered by "what did you forget / what could you improve") is folded in here instead of its separate `docs/reviews/*.html` output for the same reason.

---

## TL;DR

The item is **blocked on exactly one human action**: the session-killing reboot that releases the unreapable `flm-real` zombie (PID 443303) still holding `:52626`. Everything agent-executable was already done by the 08:02 first dispatch; this 2nd dispatch re-verified all of it (unchanged), added one genuinely new validation (all `flm-real` DT_NEEDED resolve under the fixed wrapper's loader path), and closed the queue's cross-reference gap (first dispatch's changes rode a footerless auto-commit). Honest critique: a 2nd dispatch of a USER-gated item inevitably re-verifies ~80% of what dispatch 1 verified — that is queue-process cost, not work. New findings noticed in passing: the memory-emergency-guard's restore budget appears **spent for today (3/3)** with **17 doomed-start bind failures since 07:50**, meaning any deploy before the reboot carries the documented exit-4 risk; `/data` is at **90% (108G free)** — the 21.6 GB Q4_K pull fits but tightens it; the boot-default is **not agent-verifiable** (loader.conf is sudo-only) and the last SAFE-TO-REBOOT verdict predates gen 765 (03:20 today); and the post-reboot runbook lives **only inside a TODO row** — no durable runbook doc exists.

---

## The three headline questions

### 1. What did you forget?

- **The boot-default is unverified for the CURRENT generation.** The Sep-9 18:35 `pre-reboot-check` "SAFE TO REBOOT" verdict was for gen 764. Gen 765 (`96dz4wvc`, today 03:20) landed afterwards; I verified `/run/current-system` == 765 but did **not** (and mostly **cannot**) verify the loader default points at an entry whose `init=` exists on the live Samsung store — `cat /boot/loader/loader.conf` is permission-denied for agents. The 2026-09-07 stuck-boot incident is exactly this class. The runbook's post-reboot step 1 catches a wrong boot only AFTER the user has already rebooted into it.
- **A durable home for the runbook.** The full post-reboot go-live sequence exists ONLY as a TODO_LIST row (line 13). TODO rows get rewritten every harvest cycle; if a harvest pass rewords that row before the reboot, the post-reboot session loses the runbook. There is no `docs/services/fastflowlm.md`.
- **The memory-emergency-guard restore budget.** I did not check it during the work dispatch. The journal shows restores #14–#16 today with the latest showing budget **3/3** — the daily cap (`maxRestoresPerDay = 3`) appears spent, the doomed socket is re-armed, and **17 bind-failures since 07:50** prove the client-triggered doomed-start churn is live. Practical consequence I failed to surface during the work dispatch: **any deploy between now and the reboot risks the flm-unit exit-4 class** (doomed-but-restarting unit exit-4s activations without any unit-file change).
- **`/data` headroom context.** The first dispatch recorded "108G free — fits". True, but `/data` is at **90%**; the Q4_K pull drops that to ~88%. Fine, but worth saying out loud given the box's ENOSPC history.
- **A live CLI smoke of the staged wrapper.** I stopped at static `ldd`. A `timeout 10 flm --version` (or `--help`) run would have added "the wrapper executes and the CLI starts" — the one runtime signal obtainable without the reboot. Small, safe, skipped.

### 2. What could you have done better?

- **Push back on the re-dispatch itself.** Dispatch 2 of a USER-gated item has almost no executable surface. The correct move was a *cheap* re-verification (state changed since 08:02? no → done), not a full re-audit. I re-ran roughly 80% of dispatch 1's checks. That is token/process waste the queue pays for on every re-dispatch cycle.
- **Verify rather than trust the first dispatch's claims verbatim.** I did re-verify the big ones (corpse, version, staged store path, wrapper content) — good — but I took "flake check green" and "build-verified" from the first dispatch's report without re-running them in the work dispatch. They were true (the pre-commit hook ran `nix flake check` at my commit, which retroactively covered it), but the verification order was luck, not discipline.
- **Check the guard/journal state proactively.** The restore-budget and bind-failure-count facts took 30 seconds to gather when I finally ran them (for THIS report). They materially affect "is a deploy safe right now" — which is exactly the decision surface a pre-reboot dispatch owns.
- **Mark the ldd check's limits explicitly in the work commit.** The addendum says dlopen-time enumeration "remains the only live-only gate" — correct — but `ldd` only proves DT_NEEDED resolution; XRT's driver plugin (`libxrt_driver_xdna.so.2`) is dlopen'd at runtime by `libxrt_core`, so the fix's *actual* mechanism (multiarch dir on LD_LIBRARY_PATH enabling that dlopen) is still inferred, not proven. The wording is fine; the epistemic status could have been crisper.

### 3. What could you still improve?

- **Make the static loader check a flake check.** "all DT_NEEDED resolve under the wrapper's LD_LIBRARY_PATH + `libxrt_driver_xdna.so.2` exists in the multiarch dir" is mechanically checkable at build time in `pkgs/fastflowlm.nix` (it already has layout self-checks). Then every future XRT bump fails at build instead of at first serve. Not done — smallest correct change won this dispatch, but it is the highest-value hardening left in repo-space.
- **Write `docs/services/fastflowlm.md`** and move the go-live runbook there (TODO row keeps a pointer). Guards against harvest rewrites and gives the post-reboot session a stable target.
- **Add a "verified-at" marker convention for USER-gated queue items** so dispatch N+1 can cheaply confirm "state unchanged since T" instead of re-auditing.
- **Reword TODO:211** ("flm v1.0.3 retry gated on the 7.2.2 reboot") — the kernel premise is dead; the gate is the corpse-clearing reboot + wrapper validation. Stale wording will mislead a future harvest.
- **Decide v1.0.3 vs v1.0.5 before the reboot** (see questions) — flm itself now nags "current v1.0.2, latest v1.0.5" in the journal; if releases 1.0.3–1.0.5 are truly weights-only, staging v1.0.5 directly could save a second 21.6 GB re-pull later, at the cost of re-verifying the wrapper/layout against the newer tree.

---

## a) FULLY DONE

1. **Live state re-verification (2nd dispatch, ~08:14–08:23):** boot 2026-09-07 kernel 7.2.3; corpse PID 443303 (`Zsl <defunct>` + `Xsl`, PPid 1) still LISTENs on :52626; `fastflowlm.service` still dies `bind: Address already in use` (17× since 07:50); llama :8848/:8849 UP; deployed unit still v1.0.2 (`l7bpnd399…`); `/run/current-system` = `96dz4wvc` (gen 765); tree clean.
2. **Staged v1.0.3 intact:** store path `4q6gk3c5…-fastflowlm-1.0.3` present; wrapper script re-inspected — `XILINX_XRT=$out`, `LD_LIBRARY_PATH` carries BOTH `$out/lib` and `$out/lib/x86_64-linux-gnu`, comment documents the broken-prefix probe + dlopen fallback rationale.
3. **NEW: static loader validation** — under the wrapper's exact `LD_LIBRARY_PATH`, `ldd $out/flm-real` resolves every DT_NEEDED (zero "not found"); XRT 2.25.00 chain incl. `libxrt_core.so.2` and the NPU driver plugin `libxrt_driver_xdna.so.2` present in the multiarch dir. (Limits honestly stated: dlopen-time enumeration remains live-only.)
4. **Kernel-premise falsification** (dispatch 1, upheld): amdxdna byte-identical v7.2→v7.2.3, 0 files changed — "reboot into 7.2.2 fixes enumeration" is dead; the retry rides the wrapper fix alone.
5. **TODO_LIST.md:13 closure state:** unchecked + full 7-step post-reboot runbook + `BLOCKED: user reboot required — the :52626 corpse is SIGKILL-immune and survives everything except a reboot` tail (contract-conform; untouched this dispatch).
6. **Upstream-issue decision** (dispatch 1, upheld): NOT filed — fails verify-before-filing Gates 1+4 (override unvalidated, kernel premise dead). Correct re-evaluation trigger: live strace only if post-fix enumeration still fails.
7. **Deliberate non-deploy decision** (dispatch 1, upheld and *strengthened* by this dispatch's guard-budget finding): pre-reboot activation of v1.0.3 gains nothing while the corpse pins :52626, and the doomed-restarting-unit exit-4 class makes pre-reboot deploys actively risky today.
8. **Footer cross-reference:** `6841a50b` carries `Task-Queue-ID: 000001a08ef20c1a4e464fb20c81f52bd3c5` (dispatch 1's changes had ridden a footerless auto-commit).
9. **Gates:** gitleaks + `nix flake check` green at commit (pre-commit hook output verified).

## b) PARTIALLY DONE

1. **The task itself.** The core action (reboot) is USER-gated and pending; the retry/validation/pull/revert chain is fully runbooked but unexecuted. This is the maximum possible completion for an agent without the user action.
2. **Wrapper-fix validation.** Static half done (DT_NEEDED + plugin presence). Runtime half (dlopen of the xdna plugin → NPU enumeration → serve) impossible until the corpse clears.
3. **Module retune.** 40G/32G MemoryMax deliberately kept (sized for Q4_1's 21.6G/26G-peak); Q4_K size unknown until the pull; runbook gates any retune on measured post-pull RSS. Smoke 480s timeout carried over on the same reasoning.
4. **Pre-reboot safety verification.** `/run/current-system` verified; loader default NOT agent-verifiable (sudo-only) and `pre-reboot-check` has not been re-run since gen 765 landed. The runbook's step 1 is post-reboot; a pre-reboot `nix run .#pre-reboot-check` run (self-elevating, therefore USER) is the missing half.
5. **AGENTS.md currency.** The flm section accurately reflects today's state (staged/owed-reboot) but still contains the pre-falsification-era framing in places (e.g. the item title in TODO history, TODO:211 wording).

## c) NOT STARTED (all correctly gated behind the reboot unless noted)

1. `nix run .#deploy` activating v1.0.3 (runbook step 2).
2. One-time `flm pull qwen3.6-moe:35b-a3b` — Q4_K re-pull, ~21.6 GB (step 3).
3. Live `flm serve` enumeration validation + :52625 `/v1/models` + llama functional probes (step 4).
4. Revert to v1.0.2 if enumeration fails (step 5) — decision point, not failure.
5. Module retune if Q4_K peak RSS > 32G (step 6).
6. Upstream XRT issue with strace evidence — ONLY if post-fix enumeration still fails (step 7).
7. PMA / papdashboard / paperless-ai consumer recovery verification (implicitly post-reboot).
8. Q4_K size + cold-load-time recording into AGENTS.md (needs the pull).
9. Static-loader-check-as-flake-check hardening (repo-side, *could* start now — consciously deferred as out of the smallest-change scope).
10. `docs/services/fastflowlm.md` runbook doc (same).

## d) TOTALLY FUCKED UP

Nothing in this dispatch broke anything — but honesty requires listing what is actually fucked up around this task:

1. **The AI stack has been dark for 4+ days** (since 2026-09-07) on a failure mode whose ONLY fix is a reboot that kills every session — including the tq pool agents and the user's desktop. The blocking dependency is one human decision, and no amount of agent dispatches changes that.
2. **The queue keeps dispatching a USER-gated item to autonomous agents.** Dispatch 2 added ~20% new value over dispatch 1. Dispatch 3 (unless the state changes) will add less. The item needs a "parked until user signals reboot" state, not re-dispatches.
3. **The doomed-start churn is self-sustaining right now:** guard restore budget 3/3 spent, socket re-armed, every PMA/enricher client connection triggers a backend start that dies `EADDRINUSE` in <1s — and that same churn is a loaded gun for any deploy's exit-4 path until the reboot.
4. **The runbook's single point of failure is a TODO row.** One harvest rewrite away from the post-reboot session flying blind (it would partially recover from AGENTS.md, but slowly).
5. **`/data` at 90%** before a 21.6 GB pull on the box with an ENOSPC crash history. Not a blocker (108G free) — but nobody should discover the headroom math during the pull.

## e) WHAT WE SHOULD IMPROVE

1. **Queue contract for USER-gated items:** add a parking state ("awaiting human action — do not re-dispatch without state change") so re-dispatches carry new information.
2. **Promote runbooks out of TODO rows** into `docs/services/*.md`; rows hold pointers only.
3. **Build-time loader validation for flm** (ldd-equivalent + plugin presence in the package's own checks) — converts the live-only failure class into a build failure.
4. **Agent-verifiable boot-safety proxy:** since loader.conf is sudo-only, make `pre-reboot-check` runnable without elevation for the read-only parts it can check (or emit a world-readable summary file at deploy time), so agents can answer "is the boot default still sane" without sudo.
5. **Surface guard-restore budget in monitoring:** `memory_emergency_guard_restored_total` exists; a "budget spent + socket re-armed + recent EADDRINUSE" composite gauge would make the deploy-risk state visible instead of journal-archaeology.
6. **Version-target policy for flm bumps:** decide (owner) whether staged-retry targets `latest` or `known-layout`, since every major retry costs a 21.6 GB re-pull if the manifest changes.
7. **Honest epistemic labels in evidence:** "static resolution proven; dlopen path inferred; enumeration unproven" beats "wrapper fix landed".

## f) 50 things we should get done next

*Sorted roughly by impact; the first ~21 are the go-live chain (owner = post-reboot session unless marked USER).*

1. **USER: run `nix run .#pre-reboot-check`, then reboot** — releases the :52626 corpse; verdict is stale (predates gen 765).
2. Post-reboot: verify booted==current==profile (rollback-generation class; expected gen 765 `96dz4wvc` or newer).
3. `nix run .#deploy` — activates v1.0.3 toplevel; corpse guards should stay silent.
4. Verify clean activation: 0 failed units, profile bumped, corpse guards silent.
5. Re-verify `/data` free space, then `flm pull qwen3.6-moe:35b-a3b` (Q4_K, ~21.6 GB).
6. Live `flm serve` validation — watch for `No such device with index '0'` (enumeration).
7. Validate `:52625/v1/models` through the public socket (480s budget; cold load).
8. Validate llama :8848/:8849 `/health` + embeddings/rerank functional probes.
9. `nix run .#post-deploy-check` full pass post-go-live.
10. Measure Q4_K peak RSS → retune MemoryMax 40G/32G ONLY if >32G observed.
11. If enumeration fails: capture live strace (openat on /dev/accel*, libxrt paths) BEFORE reverting.
12. If enumeration fails: revert package to v1.0.2 + deploy + verify consumers recover.
13. Only after a strace-evidenced post-fix failure: file the upstream XRT issue (verify-before-filing gates now satisfiable).
14. Update TODO_LIST.md:13 — check off, remove BLOCKED tail, record go-live evidence.
15. Update AGENTS.md flm section — deployed version, validation results, Q4_K size/cold-load numbers.
16. Verify PMA commit LLM path recovered (journal `duration=10-20s`, no heuristic-fallback lines).
17. Verify papdashboard insight enricher recovered (insights flowing, no cold-load timeouts).
18. Verify paperless-ai RAG path (embedding endpoint) functional again.
19. Confirm corpse fully gone: no `Zsl`/`Xsl` flm-real threads; :52626 owned by the live flm process.
20. Confirm system failed-units count back to baseline (the ~92 flm-corpse-class failures clear).
21. Confirm Gatus flm service-state/start-limit checks green; SigNoz journald echo (flm prompts/logs) flowing.
22. Write `docs/services/fastflowlm.md`; move the go-live/rollback runbook there; TODO row keeps a pointer.
23. Encode the static loader validation (ldd-equivalent + `libxrt_driver_xdna.so.2` presence) as build-time self-checks in `pkgs/fastflowlm.nix`.
24. Attempt a timeout-bounded `flm --version`/`--help` of the staged wrapper (pre-reboot "CLI starts" evidence; skipped this dispatch).
25. Decide v1.0.3 vs v1.0.5 staging target before the reboot (see question g2; flm nags latest=v1.0.5).
26. Reword TODO:211 — the "7.2.2 reboot" gate is dead; the gate is corpse-reboot + wrapper validation.
27. After v1.0.x proven: delete obsolete hand-install leftovers (`~/.local/share/fastflowlm/`, `~/.local/bin/flm`, `~/.bashrc` LD exports) if still present.
28. FastFlowLM smoke: assert model NAME in `/v1/models` (existing TODO).
29. Add the flm idle-check unit test (existing TODO, second half).
30. flm smoke PSI-skip for contention-stretched cold loads (27–43 min class).
31. Add the 3 gotcha entries from the fastflowlm 203-fix session (existing TODO).
32. Resume flm upstream release watch with corrected framing (latest now v1.0.5; weights-only family).
33. Post-reboot: verify 2026-09-05 sysctls actually landed (`vm.page-cluster`, `vm.watermark_boost_factor` — boot-gated TODO).
34. Post-reboot: re-baseline zram steady-state fill + guard zone thresholds on the 124.3G total (TODO:75).
35. Remove `/boot/loader/loader.conf.bak-stuckboot` (TODO:141 leftover).
36. Decide `p0ccbqj5` gcroot disposition post-reboot (TODO:141).
37. Start the Samsung `/nix` soak clock; schedule old `@nix` deletion after soak (TODO:141).
38. P1: corpse-aware memory-emergency-guard restore (skip restore when backend journal shows recent EADDRINUSE).
39. pre-reboot-check candidate §10: gcroots/profiles symlink resolution check (TODO:141).
40. Until the reboot: treat any non-urgent deploy as exit-4-risky (doomed-restarting-unit class); defer or accept the documented re-run-on-exit-4 path.
41. Add a composite monitor: guard budget spent + flm socket re-armed + recent EADDRINUSE = "deploy-risk" state (make it a metric, not journal archaeology).
42. Investigate the load-average 8–15 baseline noticed this dispatch (corpse churn vs something else).
43. Sanity-check the "48 users" uptime oddity (tmux/tq session accumulation?).
44. Keep the `/mnt/pool/backups/data` zero-received-backups P0 (the /data EIO stance) tracked — noticed in passing, unchanged.
45. Plan the Q4_K pull bandwidth/time budget (owner: what link does the 21.6 GB ride, and when).
46. Post-go-live: prune the AGENTS.md flm section (it has accumulated three eras of framing; docs-health sweep candidate).
47. Record actual Q4_K model size + cold-load time in AGENTS.md (validates the 480s smoke assumption with data).
48. Add the "verified-at" marker convention for USER-gated queue items (cheap re-dispatch verification).
49. Post-go-live: sweep for other docs still claiming "kernel 7.2.2 fixes enumeration" (TODO history, archived reports get annotations, not rewrites).
50. Close the tq queue loop: mark the item done after go-live and check no other queue item pins stale flm assumptions.

## g) Questions I cannot figure out myself

1. **When do you plan to reboot, and should the queue park this item until then?** Every re-dispatch until the reboot is ~80% repeated verification. If you signal "reboot done" (or the item detects a new boot), dispatch N+1 executes the runbook in one pass.
2. **Version target: staged v1.0.3 as-is, or bump to v1.0.5 first?** flm itself reports latest=v1.0.5, and 1.0.3–1.0.5 are described as weights-only — if their manifests pin the same Q4_K weights, staging v1.0.5 now saves a potential second 21.6 GB re-pull later. Cost of bumping: re-verifying wrapper/layout/tarball hash against the newer tree before the reboot. I can research the release notes if you want, but the risk appetite (known-and-staged vs latest-and-reverified) is yours.
3. **If enumeration STILL fails post-fix: after strace + revert, do you want v1.0.2 re-pinned long-term** (shelve v1.0.x until upstream fixes XRT 2.25), **or keep retrying each new release?** Each retry costs a 21.6 GB pull, an outage window, and a revert path — I cannot weigh that for you.

---

*Point-in-time snapshot (2026-09-11 08:23). ANNOTATE, never rewrite, when bringing it current.*

Task-Queue-ID: 000001a08ef20c1a4e464fb20c81f52bd3c5
