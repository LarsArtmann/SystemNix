# Task Report: task-000001a093815838131ca53b4231759e5d68

**Paperless statement decryption go-live + duplicate cleanup + retro-decrypt repair — independent verification + runbook re-test (third trap found and fixed)**

| Field | Value |
| --- | --- |
| Date | 2026-09-12 ~06:15 CEST |
| Task | TODO_LIST P1 item (paperless decrypt go-live / duplicate prune / retro-decrypt repair) |
| Outcome | **BLOCKED (unchanged), verification COMPLETE, runbook defect #3 found + fixed.** An earlier run of this same queue ID landed the upstream engineering (InboxClean `--backfill --decrypt-repair` @ `7aa5c3f`, commit carries this Task-Queue-ID) and fixed runbook traps #1-2 (`INBOXCLEAN_CONFIG`, unit `PATH`). This run re-verified every claim against reality and found the env reconstruction STILL broken: the CLI's global config gate dies `config.api_key_required` before dispatch without `LLM_PROVIDER`, and the runbook's `INBOXCLEAN_TOKEN_FILE` is not a knob the code reads. |
| Work commit | SystemNix docs commit (AGENTS.md, docs/services/paperless.md, TODO_LIST.md, this report) |
| Pushed | **No** (per contract) |

## 0. Claims re-verified (2026-09-12 06:00-06:15 CEST)

| Claim (TODO item) | Verdict | Evidence |
| --- | --- | --- |
| Engineering complete, InboxClean master @ 7aa5c3f | TRUE | `7aa5c3f` is ancestor of InboxClean HEAD `cb3496c`; full `--decrypt-repair` implementation present (`cmd/inboxclean/paperless_command.go`, spec + CHANGELOG in the commit) |
| Deployed c65c797 lacks the flag | TRUE | flake.lock `nodes[inboxclean].locked.rev = c65c7973…`; input URL `github:LarsArtmann/InboxClean?ref=master` |
| Upstream push still pending | TRUE | `git log origin/master..master` = 10+ commits; agent sessions never push |
| sops password still PLACEHOLDER (user-gated) | UNCHANGED (by design untestable without sudo) | part 1 remains a user step; nothing in the interim suggests it was filled |
| Runbook env reconstruction correct | **FALSE — fixed this run** | see §1 |

## 1. New defect: the reconstruction failed its first real dispatch test

The queue contract says "TEST the runbook env reconstruction first". Doing exactly that against the deployed binary:

1. `inboxclean paperless --help` (no env) → **hard fail** `[rejection:config.api_key_required]` — `internal/config` defaults `LLMProvider` to `openai` (`defaultLLMProvider = ProviderOpenAI`); the gate validates BEFORE command dispatch, so even `--help` dies.
2. The runbook extracted only `INBOXCLEAN_CONFIG|DB_PATH` + `PATH` + paperless vars — no `LLM_PROVIDER`. The deployed unit sets `Environment=LLM_PROVIDER=ollama`, but `sudo` scrubs env and `env` only sets what it is given → **the user's prune run would have died at the gate, never reaching paperless.**
3. Differential test with `LLM_PROVIDER=ollama`: help prints; `paperless` subcommand dispatches (fails later with `paperless: not configured` — expected without the token).
4. Second defect in the same block: `INBOXCLEAN_TOKEN_FILE=/var/lib/inboxclean/token.json` — **zero hits in the InboxClean Go source**; the real knobs are `GMAIL_CREDENTIALS_FILE`/`GMAIL_TOKEN_FILE` (both `Environment=` lines in the deployed unit). The main account's source re-fetch builds its client from `cfg.Gmail` (`cmd/inboxclean/main.go:604` → `gmail.NewClient(ctx, accounts[0].CredentialsFile, accounts[0].TokenFile)`); the TOML covers only the extra `work` account. Without these, `--decrypt-repair` on main-account rows could not re-fetch sources.
5. Corrected reconstruction (all five vars extracted from `/etc/systemd/system/inboxclean-sync.service`) gate-tested: passes config validation, stops exactly at `config.paperless_url_without_token` — the root-owned `PAPERLESS_TOKEN` the user's `sudo grep` supplies. That is the correct stopping point for an agent test (sudo banned; the token is the user's).

Fix landed in `docs/services/paperless.md` (grep alternation extended to `GMAIL_CREDENTIALS_FILE|GMAIL_TOKEN_FILE|INBOXCLEAN_CONFIG|DB_PATH|LLM_PROVIDER`, inert line removed, comment explains each var) + AGENTS.md retro-repair bullet + TODO_LIST blocked line.

## 2. What was NOT done, and why (unchanged blockers)

1. **Part 1 (sops password)** — human-only value + interactive sops edit.
2. **Part 2 (`--backfill --prune`) + part 3 live runs** — user-gated per the TODO item's own current wording ("parts 1-2 + the live repair runs USER-gated"): live-host runs need `sudo -u inboxclean` + the root-owned paperless token; the repair additionally needs the real password AND the upstream flag (push + tag + `nix flake lock --update-input inboxclean` + deploy first). An interim `git+file?rev=` pin + live deploy was considered (go-taskqueue precedent) and rejected: the repair cannot run until the user fills the password anyway, so a live-system deploy buys nothing today while adding deploy churn + a later flip-back; the previous run's push-first sequencing stands.
3. **The 2 duplicates were not independently re-enumerated** — paperless API and the ledger DB both sit behind root-owned auth/files; the dry-run preview (which the user runs first) is the designed enumeration surface.

## 3. Self-review notes

- The prior run's "extraction one-liners live-verified against the deployed unit" claim was true for EXTRACTION (the greps match the unit) but was never a dispatch test — the LLM gate fires only when the binary actually runs. Lesson: "verified the extraction" ≠ "verified the command" — a runbook command is only verified when its failure surface is the expected one.
- Verification of the upstream "full suite green" claim was NOT re-run (would mean building/testing the InboxClean tree again hours after the authoring run); the feature presence, scope, and spec/changelog were verified at source level instead. Residual risk: none for this run's changes (docs-only); the suite claim matters only at relock time, when CI/pre-deploy gates re-prove it anyway.
- Scope held: no flake bump, no deploy, no InboxClean code changes, no TODO_LIST harvest beyond this item.

*Point-in-time snapshot — 2026-09-12. Blocked state re-verified; report per repo task-queue convention.*
