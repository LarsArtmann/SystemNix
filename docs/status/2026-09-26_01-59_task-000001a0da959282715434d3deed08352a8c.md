# Task 000001a0da959282715434d3deed08352a8c — root-window finding closure (run 4)

**Date:** 2026-09-26 01:59
**Finding (medium):** "Doctrine root-window restatement left internal contradictions in AGENTS.md at close-out" — anchored to the doctrine-table row `\| /btrfs-emergency-reserve (10G) … root ≈ 1-2w …`; rejected change `58cb44ae`.
**Verdict: NO-EDIT CLOSURE (4th consecutive run, independently re-verified).** Dangling commit verified present (`git cat-file -e` OK); it is the ORIGINAL task's report+harvest commit, not a doctrine edit. The anchor text is gone from every live surface and every root-window echo in the finding's scopes reads the reconciled **2w sharp** figure. Per the finding's own instruction ("if all echoes are already aligned on master, close this finding with no further edits"), zero content edits were made.

## a. Verification evidence (fresh greps this run)

| Check | Result |
| --- | --- |
| Anchor `root ≈ 1-2w` in AGENTS.md / docs/services / docs/todo / TODO_LIST.md / FEATURES.md | ZERO hits (rc=1) |
| AGENTS.md :670 + :671 doctrine-table rows | both `root = 2w sharp` (aligned; anchor row healed by `eb844911`) |
| AGENTS.md :660 doctrine paragraph | `root @ (3d 1w, min 2d) = **2w sharp**` (aligned; `3d 1w` there is the config restatement) |
| AGENTS.md :699 T14 caveat | `root pin window = 2w sharp — calendar-anchored` (aligned) |
| AGENTS.md :972 ClickHouse finalize note | `pin window 2w sharp, calendar-anchored` (aligned) |
| AGENTS.md :658 | legal: live-config restatement `snapshot_preserve 3d 1w` + the `NOT "3d+1w" flat` negation |
| docs/services/ | ZERO hits for all four patterns |
| docs/todo/ (7 hits) | all legal: DONE notes quoting pre-fix figures as history (storage.md:13/14/66/72), queue rows quoting the live config value or the grep-gate's own patterns (storage.md:15/136), convention-motivator row (pipeline.md:166) |
| Unicode variants (`1–2w`, `≈1-2w`, `≈ 1-2w`, `~1-2w`, `1-2 w`, `1-2 weeks`, `3d + 1w`) in the 3 scopes | only the same legal DONE-note/queue-row hits above |

## b. Disposition

The contradiction the finding describes existed transiently between `22a4e88f` (paragraph-only restatement) and `eb844911` (echo alignment, same day 2026-09-25). Master has carried the fully reconciled state since `eb844911`; runs 1-3 of this ticket (`docs/status/2026-09-26_01-09/01-35/01-38_…`) each independently confirmed the same no-edit verdict. Nothing to change; no new queue items (the remaining lineage items — grep-gate `storage.md:136`, calendar-anchor comment `storage.md:15`, era-annotation convention `pipeline.md:166` — are already queued).
