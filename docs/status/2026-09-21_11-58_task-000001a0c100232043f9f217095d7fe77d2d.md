# Review-Fix Session Report — task 000001a0c100232043f9f217095d7fe77d2d

**Date:** 2026-09-21 11:58
**Task:** Address reviewer finding (medium) on the `services.hot-db` shared-consumer-unit wiring.
**Verdict:** Finding ALREADY RESOLVED on this tree — zero code changes made, zero new commits needed.

---

## a) FULLY DONE

- **Anchor verification.** The finding's verbatim anchor `// lib.listToAttrs (` is GONE from `modules/nixos/services/hot-db.nix`. The rejected-change commit `067cbe97` (a docs close-out report, not the wiring itself) still exists; the wiring was re-anchored to the current tree per the ticket's disposition rules.
- **Fix confirmed present.** `modules/nixos/services/hot-db.nix:317-347` (landed in daemon commit `9cce5bfd`, 2026-09-21 11:17) groups consumer wiring PER UNIT with `lib.zipAttrsWith (_: lib.concatLists)` over per-entry `{unit = [path];}` fragments, emitting `unitConfig.RequiresMountsFor = paths` + `ConditionPathIsMountPoint = lib.mkAfter paths`. Two entries sharing one unit now wire BOTH paths. The comment block documents the listToAttrs keep-last trap it replaced.
- **Requested test exists.** `tests/test-hot-db-assertions.nix` case `shared-unit-entries-wire-all-paths` (line 170) adds a second entry (`/var/lib/other`) onto the SAME `mydb.service` and asserts BOTH paths appear in BOTH unitConfig attributes (order-insensitive via sort).
- **Gates green.** `nix build .#checks.x86_64-linux.hot-db-assertions .#checks.x86_64-linux.hot-db` — both pass (assertions eval + the full VM test).
- **Working tree clean at end.** Nothing to commit for the fix itself; this report is the only new file.

## b) PARTIALLY DONE

- Nothing in scope. The fix ticket's deliverable was binary (fix applied or finding obsolete); it landed in the "obsolete — already fixed" branch.

## c) NOT STARTED

- Nothing. The ticket explicitly forbids scope creep; no follow-up work was seeded into TODO_LIST.md because none surfaced.

## d) TOTALLY FUCKED UP

- **Nothing broken.** Honest self-critique: the FIRST check invocation was sloppy — I guessed the check attr path (`.#checks.x86_64-linux.hot-db-assertions-negative-test`, wrong nesting) before listing `checks` attrNames. One wasted eval, no damage.
- **Process note:** the fix I was asked to make had landed minutes earlier in daemon commit `9cce5bfd` (a batched heuristic auto-commit carrying FIVE unrelated files incl. a 200k-line HTML formatter blast). The parallel-session race worked in my favor here, but I verified content, not authorship — correct per the multi-agent discipline, worth stating explicitly.

## e) WHAT WE SHOULD IMPROVE

1. **Reviewer-finding dispositions should cite WHERE the fix landed.** The finding was filed against commit `067cbe97`, but the actual remediation rode an anonymous heuristic daemon commit (`9cce5bfd`) with no reference back to the finding. The queue's review→fix loop is only as good as the commit trail; a `fix-review:`-prefixed message convention would let reviewers self-close findings by grep.
2. **Daemon batch commits keep burying substantive fixes.** `9cce5bfd` carried the hot-db review fix + an fsync-bench change + a 200k-line formatter-inflated HTML diff in ONE commit. Finding "what fixed my finding" required `git show --stat` archaeology. The amend-forward discipline exists but is not applied to daemon batches.
3. **The 200k-line HTML diff** in `9cce5bfd` (disk-layout report re-inflated by a formatter) is the exact regression class AGENTS.md's "treat inline bundles as GENERATED content — never format these files" rule exists to stop. It landed AGAIN today.

## f) Up to 50 things to do next (queue-seeded judgment, NOT appended to TODO_LIST by this session — this ticket's charter is fix-only)

1. Grep-reconcile the reviewer-finding backlog against `9cce5bfd`/`c09d4867`/`6cc114df` daemon batches — other findings may also be silently already-fixed.
2. Re-verify the formatter-inflated disk-layout HTML against `bash scripts/verify-html-diagrams.sh` and re-splice the compact bundle if inflated (the 2026-09-20 class recurring 2026-09-21).
3. Add `git log --grep "fix-review"` convention docs to CONTRIBUTING.md.
4. Audit whether hot-db.nix's `ConditionPathIsMountPoint = lib.mkAfter paths` ordering is intentional vs plain list (mkAfter inside unitConfig lists is unusual — one-line review).
5. Confirm the shared-unit fix shape survives `extraUnits` (the code wires `[e.unit] ++ e.extraUnits`; no test case exercises extraUnits collision with unit — cheap fixture case).
6. Sweep for other `// lib.listToAttrs` consumers-unit merge patterns anywhere in modules/ (same class, one grep).
7. Extend test-hot-db-assertions with an extraUnits-shared case (pair with #5).
8. Record the "finding already fixed by daemon commit" disposition pattern in the tq pool docs so future agents don't re-apply fixes.
9. Check whether any other reviewer finding this window (2026-09-20/21) references pre-fix anchors and needs the same re-anchoring disposition.
10. Consider an eval-time assertion in hot-db.nix rejecting duplicate (unit,path) entries across entries — makes the keep-last class impossible rather than fixed.
11. Verify the hot-db VM test actually boots a two-entries-one-unit config (the assertions test covers eval; the VM test fixture may still be single-entry).
12. Run a full `nix flake check --no-build` once the tree is quiescent — my gates covered the two hot-db checks only, per the minimal-scope contract.

## g) Questions I cannot resolve myself

1. **Was the `9cce5bfd` daemon-batch fix authored by a parallel queue session that ALSO received this finding?** If yes, the fix task and this ticket are duplicates in the queue and the pacing gate should dedupe.
2. **Should the 200k-line re-inflated HTML diff in `9cce5bfd` be reverted/re-spliced?** It matches the documented formatter-incident class, but the owning session may have re-generated it deliberately (SUPERSEDED-banner flow).
3. **Is `mkAfter` on `ConditionPathIsMountPoint` semantically intended?** (List priority inside unitConfig affects merge order with upstream/module defaults — if deliberate, a one-line comment would prevent future "cleanup".)

---
**Task-Queue-ID footer lives on the commit carrying this file.** No code commits were made by this session (nothing to fix); this report is the sole deliverable.
