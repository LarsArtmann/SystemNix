# Status Report — Task `000001a0d6d4440f02adfdb7dddfd259e173` (disko-layout gate work item)

**Date:** 2026-09-25 09:52 CEST
**Scope:** This run ONLY — the TODO_LIST "storage" work item about the pre-existing `checks.disko-layout` / `treefmt.drv is not valid` pre-commit flake-check gate deaths. No unrelated research per instruction.
**Verdict:** NO-OP RUN (correctly). The work item was already fully closed by a prior task-queue run; this run verified that state and declined to duplicate, suppress, or re-open anything.

---

## a) FULLY DONE

| Item | Evidence |
| --- | --- |
| **Work item already closed by prior run** — durable fixes IN-TREE, item marked `[x]` with full DONE note | TODO_LIST.md:43 — fix chain `af87ce31` (bootstrap → `.mount` anchoring + `tests/test-hot-user-caches.nix` tmpfiles tripwire), `d827f478` (`binfmt-sandbox-dir` belt), plus the flake.nix formatter override whose eval no longer coerces the treefmt package output. Gate verified GREEN 5× on 2026-09-25 (05:33, 06:34, ~07:14, ~07:17 pre-commit-verified, 06:55) at rev `4435ddb8`. **UPDATE 09:53: a 6th green — the pre-commit hook of THIS report's own commit ran the full `nix flake check --no-build` successfully at the final tree.** |
| **This run's state verification** — tree delta since the last verified-green tree is docs-only | Commits since: `ee0d4abe` (TODO_LIST.md + docs/todo/pipeline.md, 2 lines) and `749f8a19` (AGENTS.md, 1 line). Zero `.nix` changes ⇒ eval-safety of the gate is unchanged; no re-verification needed by construction. |
| **Residual correctly routed, not lost** — post-reboot gate proof is owner-gated and tracked | TODO_LIST.md:112 (`[blocked:deploy]` stability row, host still on gen 797, fixes UNDEPLOYED) + the pipeline row carries the same pointer. Nothing about the residual lives only in a status doc. |
| **Environmental heal still holding at probe time** | `ls /run/binfmt` → present (aarch64-linux) at 09:51, uptime 11:16h — the 05:32 heal has survived the whole boot. |

## b) PARTIALLY DONE

| Item | Gap |
| --- | --- |
| **Live gate re-verification this run** | Skipped BY DOCTRINE, not by neglect: io PSI some avg60 = 89.9% (avg10 = 90.6%) at 09:51 — the repo's IO-storm skip rule (freeze-#5 / attempt-2 precedent) forbids running full `nix flake check` mid-storm. Combined with the docs-only delta, a re-run would prove nothing new; but strictly, this run adds NO fresh green of its own. The count stays 5 greens from the earlier sessions. **UPDATE 09:53: the report commit's pre-commit hook produced a 6th green, see §a.** |
| **Contract step 5 (append follow-ups)** | Deliberately empty: every discovered follow-up (deploy, reboot-verify, quiet-window VM test, gate-triage runbook, heal-attribution breadcrumb, formatter-exclusion guard) is ALREADY an unchecked row (112, 54, 55, 308). Duplicating them would violate the one-per-line queue hygiene. |

## c) NOT STARTED

- **Deploy of the durable fixes** (`af87ce31`/`d827f478` wiring): host still boots gen 797 with the OLD automount-anchored `hot-user-caches-nix-bootstrap`. Owner-gated (quiet-window VM test → `nix run .#deploy` → reboot-verify). NOT this run's scope; unchanged.
- **Post-reboot gate proof** (`ls /run/binfmt && nix flake check --no-build` after the deploy + reboot): pending on the above.
- **Quiet-window VM test** of the tmpfiles tripwire (`tests/test-hot-user-caches.nix`): queued in row 112; the IO storm makes it un-runnable today.

## d) TOTALLY FUCKED UP

Nothing in THIS run. The honest negative findings:

- **Zero commits made by this run.** The queue harvests work via commits + the Task-Queue-ID footer; a no-op run leaves no trail except this report. That is the correct outcome for an already-done item, but the queue's accounting must tolerate it (footer lands on this report's commit instead).
- **Prior-run observation carried forward (NOT this run's damage):** the root cause of the gate deaths was a boot-ordering cycle that silently deleted `/run/binfmt` for ~2 days — a machine-wide sandbox breakage that looked like repo breakage and cost multiple agent sessions. The in-tree fixes are landed but UNDEPLOYED, so a reboot before the owner deploy RE-MANIFESTS the breakage. That is the standing hazard, not a defect of this run.

## e) WHAT WE SHOULD IMPROVE

1. **Queue dedup at dispatch time:** this item was handed to a fresh run even though a prior run had closed it `[x]` the same morning. The dispatcher (or the tq pool) should skip/annotate items already `[x]` — a pre-flight `grep` of TODO_LIST for the item's key phrase would have saved the run.
2. **`[x]`-with-residual convention is ambiguous:** line 43 is `[x]` but explicitly carries an open, owner-gated residual. The queue contract ("mark done or append BLOCKED") has no third state. A `[x] … RESIDUAL: <pointer to open row>` convention is what line 43 improvised — codify it so harvests don't re-open or mis-skip such items.
3. **Probe-before-run protocol:** this run's first two commands (git log + TODO grep) resolved the no-op in <30s. That check should be a documented first step for every queue run (content-pin discipline already covers write races; this covers ALREADY-DONE races).
4. **IO-storm skip rule should be machine-readable:** the doctrine ("skip full flake check when PSI avg60 high") lives in prose + incident reports. A one-liner probe (read `/proc/pressure/io`, compare threshold) in the task preamble would make the skip decision uniform instead of judgment-based.
5. **Doc-only delta detection could be automated:** "gate safety unchanged because delta is docs-only" was reasoned by hand (`git show --stat`). A cheap check script (`git diff --name-only <verified-rev>..HEAD | grep -v '\.md$'`) would make the claim mechanical.

## f) Up to 50 things we should get done next

Scoped honestly: items 1–8 come directly from THIS task's trail; 9–25 are adjacent items observed in passing on the same TODO surface (already tracked — listed here for the harvest view, NOT to be duplicated into the queue); 26+ are flagged as ROADMAP fuel, not commitments.

**Directly from this work item's trail (agent-executable):**
1. Deploy the durable fixes (gen 797 → current tree) in a quiet window — the ONLY action that closes the root cause on the host. *(owner-gated: deploy decision)*
2. Run `tests/test-hot-user-caches.nix` VM test in a quiet window (IO PSI < 20%) before the deploy.
3. After deploy + reboot: `ls /run/binfmt && nix flake check --no-build` — the post-reboot gate proof that retires the `[blocked:deploy]` flag on rows 43/112.
4. Gate-triage runbook paragraph in docs/CONTRIBUTING.md (row 54: /run/binfmt → clean-HEAD check → nix-daemon restart → IO-pressure budget → only then suspect the task).
5. Heal-attribution breadcrumb convention (row 55: manual system heals leave a journaled line/state stamp — the 05:32 `/run/binfmt` heal actor is still unknown).
6. Formatter-exclusion regression guard: flake check asserting `docs/**/*.html` in the effective treefmt excludes (row 308).
7. Root-context repro of the FSETID/sandbox setattr EPERM mechanism (queued P0 in docs/status/2026-09-22_14-35; observed in passing on the AGENTS.md surface).
8. Add the PSI-threshold probe + docs-only-delta check as a shared pre-run preamble script for queue agents (new item this report proposes — harvest decision needed).

**Already-tracked adjacent rows seen in passing (do NOT re-add; listed for completeness):**
9. boot-mirror §11 / pre-reboot-check audit trail upkeep.
10. Offsite Borg go-live (owner-held StorageBox inputs, row in docs/todo/storage.md).
11. `hot-db` Phase-2 fold of the interim crush-hot-db mechanism.
12. llama-rag root-cause bisect (ROCm/kernel/GPU-state, upstream of llama.cpp; `nixpkgs-llama-rag` pin retained).
13. FastFlowLM staged v1.0.3 go-live (owe-reboot candidate fix for the :52626 corpse class).
14. The owed REBOOT itself (multiple corpse/anchor classes accumulate until it).
15. Two transferred Forgejo mirrors (Artmann-Minecraft org): owner decision delete vs re-mirror.
16. forgejo commit-graph.lock cleanup on the two alive mirrors (needs forgejo user).
17. Groq API key wiring for CV ChatService (owner decision: wire key or disable provider).
18. CV BRANCH-REF GOVERNED HOLD: upstream go_1_27 floor fix + lock probe discipline.
19. Resend domain verification (`larsartmann.cloud`) — final mail-relay/pocket-id go-live step. *(user-gated)*
20. Geometry/`dns-local.nix` deletion plan (T05/T06) hardening the catalog cross-check into an assertion.
21. Architecture-catalog go-live sequence (owner-gated setup-forgejo.sh → token → deploy).
22. `services.deploy-restart-audit.allowUnits` justification sweep (any unpruned entries).
23. Niri-session-manager upstream hazards (restore-on-every-start) — upstream issue/PR candidate.
24. Context7 key rotation (still-live residue from the 2026-08-18 incident table). *(user-gated)*
25. History-purge decision revisit (push held; rotation-only stance) — user decision, parked.

**ROADMAP fuel (26+): brainstorm only, no queue commitment**
26. Machine-readable task-queue pre-flight contract (done-check + IO-probe + delta-class) as a repo script.
27. A third TODO state (`[x] RESIDUAL`) formalized in the queue conventions + `scripts/check-todo-system.sh`.
28. Continuous PSI telemetry into the queue's pacing gate (avoid dispatching heavy tasks mid-storm).
29. Simplify the 5-layer prevention pipeline docs into a single decision tree for agents.
30. Audit for other gates that die "indistinguishably from a crash" without a verdict (the §10 rc-1 class) — grep for un-guarded enumerations under `set -e`.
31. Inventory of "environmental vs structural" gate-failure discriminators as a CONTRIBUTING table.
32. Periodic re-verification rota for gates verified green only N times (drift detection).
33. Consider `boot.binfmt.preferStaticEmulators = true` to remove `/run/binfmt` from extra-sandbox-paths entirely (already noted as follow-up in AGENTS.md — promote to a queued item after the deploy proves the ordering fix).
34. Deduplicate the stale-root-owned-prom / textfile-collector class guards into one shared test helper.
35. Post-deploy smoke: generic "phantom-green on a port whose owning unit is failed" guard (llama-rag 2026-09-18 lesson, generalizable).
36. Extend `ioChurnUnits` review rota — any new resumable heavy reader must be added (freeze-#6 rule).
37. Annotate `docs/status/2026-09-25_00-20_*` (§d3/b3) with the final 5-green count so the narrative is self-contained.
38. Sweep docs/status for open "pending" claims older than 7d and route each to TODO or close.
39. ChangeLog entry for the disko-layout gate saga (rotation-doctrine style summary).
40. Check whether `checks.disko-layout` specifically (vs the whole gate) has its own residual flakiness independent of `/run/binfmt` — one clean-HEAD targeted build post-deploy.
41. `nix fmt` exclusion guard test #6's negative case (formatter config extraction break) — verify it still fires after the treefmt override refactor.
42. Review whether the `binfmt-sandbox-dir` belt tmpfiles unit itself needs the DefaultDependencies=false treatment (it must run before builds, not before mounts — verify its ordering).
43. Confirm the AGENTS.md one-line addition in `749f8a19` (external session) didn't contradict this task's docs. *(in passing: it did not — 1-line heuristic commit, content unrelated to the gate.)*
44. Add a "no-op run" report convention so no-op task runs still produce a harvestable record (this report is the prototype).
45. Cross-check tq pool budget accounting for no-op runs (did this run consume quota?).
46. Fold row 43's residual note into row 112 once the deploy lands (single-source the post-reboot proof).
47. Post-reboot: re-run the full 8-case borg-restore-drill-fixture harness through nix (its 2026-09-25 green was standalone-only due to the same `/run/binfmt` sandbox breakage).
48. Similarly re-verify any other fixtures that were "hand-verified" during the sandbox-broken window.
49. Consider CI-side early warning: a scheduled clean-HEAD `nix flake check --no-build` would have caught the gate death in hours, not days, when it was environmental.
50. Write the "when a gate dies for ALL agents, treat it as FLEET incident, not per-task noise" rule into CONTRIBUTING (the meta-lesson of this item).

## g) Up to 3 questions I can NOT figure out myself

1. **Who/what healed `/run/binfmt` at 05:32 on 2026-09-25?** The heal-attribution row (55) exists, but if YOU know the actor (manual `systemd-tmpfiles --create`, another session, a reboot?), it closes the breadcrumb question immediately and tells us whether the heal mechanism is repeatable before the deploy lands.
2. **Do you want the durable fixes deployed today despite the recurring IO storms, or held for a deliberate quiet window?** The host re-manifests the boot-ordering cycle on every reboot until the deploy lands; the queue cannot decide deploy timing (owner-gated), but your intent determines whether items 1–3 above get scheduled aggressively or left `[blocked:deploy]`.
3. **Should no-op runs on already-`[x]` items be suppressed at dispatch (preferred) or reported like this?** If dispatch-time dedup is wanted, I can draft the tq/queue-side check as a follow-up queue item — but it's a workflow-policy call, not an agent-executable one.

---

*Point-in-time snapshot at 2026-09-25 09:52 CEST, rev `749f8a19`. Sections (f) items 8 and 26–29, 44–46, 49 are the only NEW harvest candidates; the rest are already tracked and must NOT be duplicated into TODO_LIST.*
