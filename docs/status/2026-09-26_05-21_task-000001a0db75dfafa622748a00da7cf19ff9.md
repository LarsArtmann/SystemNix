# Task `000001a0db75dfafa622748a00da7cf19ff9` — @home-hermes 09-16 snapshot investigation (re-fire verification #5)

**Date:** 2026-09-26 05:21 CEST · **Scope:** THIS task only (fifth dispatch of the closed `@home-hermes` 09-16 snapshot investigation; prior runs 04-11, 04-26, 04-44, 05-05). No unrelated research.

The queue re-dispatched the already-closed investigation. The re-dispatch protocol was executed instead of redoing the work: **the verdict stands, and this run independently re-verified every load-bearing leg FIRST-HAND** (own `ls`/`journalctl`/`grep` output, not citations of prior runs), plus found and fixed one new defect: a daemon-race citation drift in the two queue surfaces.

## Verdict re-confirmed: IN-SPEC — different effective retention

`@home-hermes` sets only POOL-side retention (`target_preserve_min "7d"` / `target_preserve "14d 4w"`, snapshots.nix:261-262); LOCAL retention inherits the root instance's volume-level `snapshot_preserve = "3d 1w"` (snapshots.nix:231, inheritance sentence 255-256). 09-16 is the previous calendar week's first-of-week snapshot — in-spec under the calendar-anchored `1w` window.

## Independent verification table

| # | Check | Result | Evidence (this run's own commands) |
| - | ----- | ------ | ---------------------------------- |
| a1 | Footer commits + queue-surface closure | ✓ 3 footer-bearing commits (`3ada6e25`, `f237932c`, `a32adc62`); `TODO_LIST.md` + `docs/todo/storage.md` both `[x]`, consistent, no lifecycle drift | `git log --grep=<task-id> --oneline`; view of both surfaces |
| a2 | Live snapshot inventory 05:16 CEST | ✓ unchanged since the 04:44 run's check: `@` = 09-13, 09-20, 09-22..25; `@home-hermes` = 09-16, 09-20, 09-22..25 (4 dailies + 2 weeklies each) — no early prune, no drift | `ls -1 /mnt/btrfs-root/.snapshots/` |
| a3 | Pool receives current | ✓ hermes receives continuous 09-16→09-25 (incl. the three 09-18 catch-up receives); `@` current through 09-25 | `ls -1 /mnt/pool/backups/root/` |
| a4 | Parent hold ruled out (first-hand) | ✓ Sep 25 23:02:49 send parent = `@home-hermes.20260924T2300`, not 09-16; nothing older needed as send parent | `journalctl -u btrbk-root --since "2026-09-25 22:55"` |
| a5 | Prune gap ruled out (first-hand) | ✓ same journal second (23:02:52): lockstep `subvolume delete` of BOTH `@.20260921T2300` and `@home-hermes.20260921T2300`; locals missing (hermes 09-17, 09-18×3, 09-19, 09-21) all present pool-side — delete phase converging in lockstep | journal + local-vs-pool set comparison |
| a6 | Config legs re-read at current line numbers | ✓ root `snapshot_preserve = "3d 1w"` at :231; hermes inheritance comment at :255-256; forgejo `snapshot_preserve = "3d"` at :341 | `grep`/`sed` over snapshots.nix |
| a7 | Time-gated predictions | correctly unverifiable before 23:00 — `09-22` dailies predicted to die at tonight's (09-26) 23:00 run, weeklies `09-16`+`09-13` at 09-27 23:00 ([watch] rows in storage.md stand unchanged) | current time 05:16-05:21 CEST |

## b1 — Citation drift found + fixed (this run's one defect)

`7ea1c0e1` (daemon, 04:30:32) rewrote the calendar-anchor comment in snapshots.nix (+4 lines), landing BETWEEN the 04-26 run's ref determinations and the 05:09 citation-fix pass (`a32adc62`) — so three refs in the two queue surfaces predate the shift and point 4 lines high:

| Surface | Stale | Actual (verified this run) |
| ------- | ----- | -------------------------- |
| storage.md:16 | "root `snapshot_preserve` now 228" | 231 |
| storage.md:16 | forgejo "snapshots.nix:337" | 341 |
| TODO_LIST.md:52 | "snapshots.nix:227 + block comment" / ":337" | 231 / 341 |

All four numbers corrected in place (the `a32adc62` precedent). The other cited refs in the task-family rows verified EXACT and left untouched: 255-256, 339 ("~9 snaps"), 336-341, 708-740 (`check_freshness()` at 709), 774+ (`btrfs-verify-snapshots` unit). Mechanism note: daemon commits that touch the cited file between a dispatch's read and its citation-fix pass are the recurrence window — same family as the multi-agent content-pin rule.

## §f sweep

All §f follow-ups from prior runs land on tracked surfaces ([watch] rows 17-18, forgejo-comment fix 19, explicit-retain decision 20, restore drill 21, transaction_log 22, verify-snapshots comment 23 in storage.md; spot-verify codification in pipeline.md). Nothing un-landed; no new queue rows appended — nothing from this run's work describes new follow-up work beyond what the [watch] rows already track.

## Next checks (owned by the [watch] rows, not this run)

1. After the 2026-09-26 23:00 run: both `09-22` dailies pruned.
2. After the 2026-09-27 23:00 run: `@home-hermes.20260916` + `@.20260913` weeklies pruned — if either survives, the doctrine reopens.
