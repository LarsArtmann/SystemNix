# Task Report — 000001a0c70ff761f6779056239de6ae76c9 (review-fix: restic repo password 0644)

**Written:** 2026-09-22 07:40 CEST · **Repo:** SystemNix @ `master` (unpushed ahead 37+) · **Branch state:** clean tree

**Ticket:** Reviewer rejection (high) of the restic-app-dumps work (original commit `49cfb914`, task `…eadda2dd3c`). Finding: the repo password file was created 0644 while four surfaces claimed 0600. **Format note:** the status-report skill's canonical output is a styled HTML dashboard; the tasking explicitly named a `.md` path, so this report is Markdown (explicit user instruction wins; flagged here per the skill's override rule, not propagated back as a default).

---

## Disposition summary (the fix itself)

| Item | Evidence |
| --- | --- |
| Rejected commit `49cfb914` still exists | `git cat-file -e` → EXISTS; anchor text live in tree |
| Fix applied | `modules/nixos/services/restic-app-dumps.nix:107` — `UMask = "0077";` added to the `restic-app-dumps-setup` serviceConfig (reviewer's preferred single-spot fix; restic repo data is ciphertext, nothing else changed) |
| Work commit | `6251c198` (+1 line), exactly one footer `Task-Queue-ID: 000001a0c70ff761f6779056239de6ae76c9`, pre-commit hooks green (gitleaks, flake check) |
| Rendered-unit verification | `nix eval …systemd.services.restic-app-dumps-setup.serviceConfig.UMask` → `0077` on the full evo-x2 config |
| Gates | `nix flake check --no-build` all green; `nix fmt --no-update-lock-file -- --ci` 0 changed |
| VM runtime proof | **DEFERRED** — IO PSI some avg10 = **99%** at verify time; the repo's own doctrine (deploy pressure gate, freeze-#5/#6 rules, "VM RUNS PSI-gated" queue convention) forbids a VM build in that state. The queued VM test (`tests/test-restic-app-dumps.nix`, asserts `mode == "600"`) becomes satisfiable by this fix but still has never executed |

**Daemon-race handling (in-passing, done right):** the auto-commit daemon committed my in-flight edit as heuristic `ff6ede23`; per the multi-agent doctrine I verified `git show --stat` held exactly my one file and the commit was unpushed, then amended into `6251c198` with the proper message.

---

## a) FULLY DONE

1. **The reviewer finding is fixed, minimally, at the mechanism level.** One line (`UMask = "0077"`) on the setup unit; the password file now lands 0600 at creation, the script comment ("0600 (umask 077)"), the AGENTS.md searxng-pattern claim, and the VM test's `assert mode == "600"` are all TRUE simultaneously. Commit `6251c198`, evidence: `nix eval` renders `0077` into the deployed unit shape.
2. **Eval gates pass on the shared tree.** `nix flake check --no-build` ("all checks passed", expected aarch64-darwin omission only) and the locked formatter in `--ci` mode (0 files changed) both green after the edit — the tree was left buildable for the parallel sessions.
3. **Commit hygiene held under the daemon race.** Pathspec-verified amend (contents inspected first, per the 2026-09-14 footer-fragmentation lesson); exactly one Task-Queue-ID footer (the fix ticket's — no duplicate footer for the parent task, per the fix contract); nothing pushed.
4. **Live-host exposure audit (done during this report, see §d for why it was late):** `/var/lib/restic-app-dumps/` does not exist on the running host — the module is deploy-pending (running generation predates it), so the 0644 hole **never materialized**: the finding was preventive, not an active exposure. Severity context the original disposition should have stated.
5. **Sibling-class sweep (in-passing):** grepped every `modules/nixos/**.nix` for secret-file creation (`urandom`, `openssl rand`, `chmod 600/640`). 14 hits: searxng, browser-history, cv, dns-blocker, forgejo-scripts, hermes, pocket-id (640, deliberate), gatus-config all carry explicit `chmod` after create; signoz (`signoz.nix:750-751`) creates root-password then `chmod 400` in the next statement (microsecond 0644 window only); hermes/ai-stack/storage-collector carry deliberate UMask overrides with comments. **restic-app-dumps was the only umask-less secret writer** — the class has no second live instance.
6. **btrbk interaction checked (in-passing):** `btrbk.instances."pool"` snapshots `services/*` subvolumes only (`snapshots.nix:298-315`) — the restic repo under `/mnt/pool/backups/restic-app-dumps` is NOT pool-snapshotted, so no double-storage amplification, and equally no snapshot tier covers the repo itself (documented "losing the host loses the repo" trade stands).

## b) PARTIALLY DONE

1. **Runtime verification of the fix (eval-true, runtime-zero).** What works: rendered unit carries `UMask=0077`; VM test source asserts the matching `600`; module evals green on evo-x2. What remains: the VM test has **never executed** (written under an IO storm, PSI-gated ever since), and the module + fix reach the host only with the queued batch deploy. Blocker: IO PSI some avg10 62–99% for the entire session window (third consecutive report flagging this). Effort to finish: M (one calm-window VM run + the batch deploy).
2. **The four contradicted surfaces are now consistent — but two are only transitively fixed.** The script comment and AGENTS.md claims are true again via the unit UMask; the two status reports (`01-16`, `05-07`) that narrate "umask 077 → 0600" stay as-written per the point-in-time annotate doctrine (no rewrite). Remaining open: a future reader of the standalone script sees "(umask 077)" and looks for a `umask` call in the script — none exists; the mechanism lives on the unit. Effort: S (one comment line on next touch). This is the exact falsified-comment class this repo keeps burning on (`db2b44c0` precedent, same day).
3. **Post-deploy verification plan exists but is scattered.** The 04-30 report queued "verify restic-app-dumps-setup post-switch" (batch inventory item 11); the 06-48 report queued it as item 15 ("first-run deploy gate"). Not yet consolidated with the new mode assertion (password 0600 + StateDirectory mode) into one deploy checklist entry. Effort: S.

## c) NOT STARTED

1. **CHANGELOG row for the security fix** — the repo logs notable fixes in `CHANGELOG.md`; the original module landed with a full entry, the review-fix did not add one (the 2026-09-22 entries fold in same-day corrections, precedent exists). Planned, not started; S.
2. **Deploy-time mode assertion** — `post-deploy-check.sh` has no check that `/var/lib/restic-app-dumps/password` is 0600 (or that the unit carries UMask). The VM test owns the assertion but its first run is gated; a one-line gated smoke (`stat -c %a` when the unit exists) would catch the class at every deploy. Not started; S.
3. **Secret-creation convention standardization** — three coexisting mechanisms (unit UMask, chmod-after-create, sops templates) with no documented house rule; signoz's create-then-chmod-400 leaves a race window; restic's hole happened precisely because the author picked "neither". Not started; M + a convention decision (see §g Q2).
4. **Eval-time audit for the class** — an `audit` module (the `audit-shell-nullglob` / `textfile-tmp` precedent, self-tested via `negative-test-lints.sh`) that rejects any unit ExecStart redirect into a secret-looking path (`*password*`, `*secret*`, `*token*`) without `UMask = "0077"` or an adjacent `chmod`. Would have caught the original hole at `nix flake check` time. Not started; M–L.
5. **Restic repo observability** — SigNoz panel for `backup_age_hours{backup="restic-app-dumps"}` (queued in the 01-16 report, item 13) and a dedup-ratio measurement after ≥3 nightly runs (ROADMAP trigger already written). Not started; S / M.
6. **Runbook stub** — `docs/services/restic-app-dumps.md` (password recovery semantics = re-init only, restore recipe, manual run commands); every other service has one. Queued in the 01-16 report item 15, not started; S.

## d) TOTALLY FUCKED UP

Nothing in this run is broken or data-loss-class; the honest fuck-up inventory, ranked by how much each cost:

1. **The original module shipped a security claim that was never true** (reviewer-caught, this run's reason to exist): the author wrote "0600 (umask 077)" into the script and "searxng-secret-key pattern" into AGENTS.md without ever rendering the unit — `harden{}` sets no UMask, systemd defaults 0022, so the claim was false from the first eval. Class: **wrote the claim, not the mechanism** — the same failure shape as the phantom-green and stale-comment classes this repo has repeatedly documented. Mitigation: fixed at `6251c198`; prevention is §e1/§c4. Severity: high-as-found, **zero runtime exposure** (module undeployed — verified this report).
2. **My disposition accepted the finding's exposure framing without probing the live host.** The reviewer wrote "Every local account and non-root service can read the password" — true only counterfactually; no such file existed on evo-x2. I fixed the code (correct either way) but stated no severity context, and only ran the `ls /var/lib/restic-app-dumps` probe during THIS report. Cost: the fix commit message implies an active hole. Lesson: dispositions against security findings should separate "hole in shipped config" from "hole reachable at runtime" — one `ls` answers it.
3. **I deferred the sibling-class grep out of scope discipline.** "Update nothing else" was obeyed literally — but verifying blast radius is not updating anything. The 30-second grep (done now, §a5) would have let the work commit state "restic was the only instance" instead of leaving that question open for the reviewer's next pass. It came back clean, so no harm materialized — this time.
4. **The fix rides a batch that is now three reports deep in deploy debt.** restic-app-dumps, paperless-db-backup, discordsync attachments, browser-history dbBackup, cache symlinks, hot-db fixes, boot-mirror sync: all landed, all runtime-zero, deploy refused by the pressure gate all window (io avg10 62–99%). Every storm-day compounds the exit-4 surface. This is pre-existing and tracked, but it is the single biggest gap between the tree and reality — and my fix only exists at eval level until it clears.
5. **Minor, self-inflicted:** the script comment now attributes the mechanism to a umask that lives one layer up (see §b2); no CHANGELOG row (§c1). Both deliberately left per "update nothing else" — but both are now owed follow-ups, which is the real cost of strict minimalism: residue.

## e) WHAT WE SHOULD IMPROVE

1. **Verify security/runtime claims at the rendered-unit level at authoring time.** One `nix eval` of the relevant `serviceConfig` (or the built unit file) would have caught the 0644 hole before commit, not at review. Cheap, mechanical, generalizes to every "runs as X, mode Y" claim in this repo.
2. **Scope freezes should freeze WRITES, not VERIFIES.** When a reviewer says "update nothing else", the correct reading is: fix one thing, then still sweep the class and report findings — the sweep is what turns a one-line fix into a closed class (§a5). Codify: every security-shaped review fix ends with a sibling grep + a one-line "no other instances" statement in the commit or report.
3. **Standardize one secret-creation pattern.** Unit-level `UMask = "0077"` is strictly superior to chmod-after-create (no race window, survives refactors that move the redirect, self-documents in the unit). Migrate the chmod-after-create sites opportunistically on touch; document the convention (and the pocket-id 640 / hermes 0026 exceptions) in `docs/CONTRIBUTING.md`.
4. **Make the class eval-time-impossible.** An `audit`-family check (redirect into secret-looking path ⇒ require UMask or adjacent chmod) with the existing `negative-test-lints.sh` harness would have rejected the original module at `nix flake check`. The repo already runs nine audits of this shape; this is the same pattern applied to a class that just cost a review cycle.
5. **Deploy-time mode assertions for secret files.** Post-deploy smoke asserting landed modes (600/640/400 per file) converts any future regression of this class into a red smoke within one deploy, independent of VM-test scheduling.
6. **Reviewer-finding disposition template:** (a) anchor live? (b) commit exists? (c) runtime exposure real or preventive? (d) class siblings? (e) fix at mechanism level. (a)+(b) were done this run; (c)+(d) were late — a checklist makes them automatic.

## f) Things we should get done next (ranked; ≤50 — grounded, not padded)

Impact: Critical/High/Medium/Low · Effort: S <30min, M 30min–2h, L >2h. Items marked **[already queued]** exist in TODO_LIST/docs — HARVEST should dedupe, not duplicate. None of the NEW items describe the fix task's own work; they are residue/follow-ups discovered in passing.

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | Fire the queued batch deploy in the first calm window (io avg10 <20%, no Zone-6 trip in 60 min) — carries this fix + ~10 landed features still runtime-zero **[already queued, TODO_LIST "storage"]** | Critical | M | Feature |
| 2 | First green VM run of `tests/test-restic-app-dumps.nix` (PSI-gated since written; now validates the mode-600 assertion end-to-end) **[already queued]** | High | M | Quality |
| 3 | Consolidate the scattered post-deploy restic checklist into ONE entry: unit active, password exists **and is 0600**, StateDirectory mode, repo initialized, first backup + `.last_success`, backup-coordination row green (merges 04-30 item 11 + 06-48 item 15 + this run's mode check) | High | S | Quality |
| 4 | Add a gated post-deploy smoke: password file mode == 600 when the setup unit exists (catches the class every deploy, independent of VM scheduling) | High | S | Quality |
| 5 | Reword the one-line mechanism comment in `restic-app-dumps.nix` ("umask 077" → "unit UMask 0077") on next touch, killing the falsified-comment drift at `restic-app-dumps.nix:34` | Medium | S | Cleanup |
| 6 | Add a one-line CHANGELOG entry for the 0600 security fix (fold into next docs pass) | Medium | S | Documentation |
| 7 | Eval-time audit: reject units redirecting into secret-looking paths without UMask/chmod (self-tested via `negative-test-lints.sh`; `audit-*-nix` precedent) | High | M–L | Quality |
| 8 | Document the house secret-creation convention (UMask=0077 default; chmod exceptions pocket-id 640 / hermes 0026 / signoz 400) in `docs/CONTRIBUTING.md` | Medium | S | Documentation |
| 9 | Migrate chmod-after-create sites to unit UMask on touch (searxng, browser-history, cv, dns-blocker, forgejo-scripts, hermes env, signoz root-password — opportunistic, one per touch) | Medium | M | Cleanup |
| 10 | `docs/services/restic-app-dumps.md` runbook stub (re-init semantics, restore recipe, manual commands) **[already queued, 01-16 item 15]** | Medium | S | Documentation |
| 11 | Verify `createWrapper` semantics for the nixpkgs restic module; wire a `restic-app-dumps` ops CLI if trivially available **[already queued, 01-16 item 18]** | Medium | S | Feature |
| 12 | Decide restic repo's offsite role vs the Hetzner Borg leg (replicate restic repo, or Borg supersedes; decides whether the 0600 password becomes a cross-host secret → sops migration) **[already queued as decision]** | High | S | Decision |
| 13 | Owner decision: extend `services.restic-app-dumps.paths` with `forgejo-subvol` / `github-voice-corpus` / `pixel6` dumps or keep excluded **[already queued]** | Low | S | Decision |
| 14 | SigNoz panel for `backup_age_hours{backup="restic-app-dumps"}` on the storage dashboard **[already queued, 01-16 item 13]** | Low | S | Feature |
| 15 | Measure dedup ratio + repo size after ≥3 nightly runs; feed the ROADMAP retention-trigger decision **[already queued, ROADMAP]** | Medium | S | Verify |
| 16 | Post-deploy: confirm systemd applied UMask to the StateDirectory itself (0700 vs 0755 — systemd version-dependent; one `stat` settles it; file is safe either way) | Low | S | Verify |
| 17 | Watch the first `--keep-daily 14 --keep-weekly 8` prune for pool-growth sanity (first prune runs the morning after deploy) | Medium | S | Verify |
| 18 | Post-deploy: verify backup-coordination emits `backup_healthy{backup="restic-app-dumps"} 1` on the live host (VM asserts it; live confirm owed) | Medium | S | Verify |
| 19 | Watch item: `storage-collector.nix` UMask `mkForce "0022"` loosens upstream 0077 — re-verify the tmp+rename justification still holds on next upstream bump | Low | S | Watch |
| 20 | Reconcile the three secret-creation mechanism sites into the audit from #7 (grep surface already mapped in this report, §a5) | Medium | M | Quality |
| 21 | Fold the "verify claims at rendered-unit level" rule into `docs/CONTRIBUTING.md`'s verification section (one sentence + the `nix eval` one-liner pattern) | Medium | S | Documentation |
| 22 | Add §c/§d disposition checklist (5 steps, §e6) to the review-fix workflow notes in CONTRIBUTING or the tq skill notes | Low | S | Documentation |
| 23 | Keep pressure-gate discipline: do NOT force the deploy (`DEPLOY_FORCE_PRESSURE`) for this fix — no runtime exposure exists (verified); the queued calm-window path is correct | High | S | Decision-confirmed |
| 24 | After batch deploy: re-run `nix eval` parity for the UMask key on the DEPLOYED unit (`systemctl cat restic-app-dumps-setup`) — closes the eval-vs-runtime gap for this specific key | Medium | S | Verify |
| 25 | Consider a `lib/systemd.nix` default `UMask = mkDefault "0077"` for harden'd units writing to StateDirectory — wide blast radius, needs a survey of units that legitimately create world/group-readable files first | Medium | L | Feature |
| 26 | Sweep status reports that narrate "umask 077 → 0600" for annotate-mode corrections ONLY if a future doc-health pass flags them (otherwise leave per point-in-time doctrine) | Low | S | Documentation |
| 27 | Queue the restic first-run expectation into pre-deploy §10's known-exit-4 watchlist for the batch deploy (the unit is a converger; it is already in deploy.sh's provisioner list — verify it fires post-switch) | Medium | S | Verify |
| 28 | Extend #24 into a generic "post-deploy rendered-unit parity" helper script (diffs `nix eval` of N critical keys vs `systemctl cat` output) if #24 proves useful twice | Low | M | Feature |
| 29 | Update FEATURES.md restic row (🟡 LANDED, deploy pending) only AFTER the batch deploy — no state change warranted by this fix alone | Low | S | Documentation |
| 30 | When the module first deploys: record the actual password-file mode + StateDirectory mode + first-backup timestamp in the next status report (closes this ticket's runtime loop completely) | Medium | S | Verify |

*(30 items — the remaining 20 slots of "up to 50" are deliberately unfilled: everything else this session touched is either already queued under the IDs cited above or would be fabricated scope. Better 30 grounded than 50 padded.)*

**HARVEST note:** items 3–9, 16–18, 21–22, 24, 27, 29, 30 are NEW (not yet in TODO_LIST); the rest cite their existing queue home. Item 12/13 are `[decision]`-class and belong in the domain library, not the dispatch queue.

## g) Questions I cannot figure out myself

1. **When the Hetzner Borg offsite leg lands, does `restic-app-dumps` get replicated offsite (making the machine-local 0600 password a cross-host credential that must move to sops), or does Borg supersede it for offsite entirely?** The "NO sops — pool-local" design decision, the password location, and the §f item 12 scope all fork on this, and it is a pure owner call about the backup architecture's end state.
2. **Should the house convention be "unit `UMask=0077` everywhere" (my recommendation — no race window, survives refactors), accepting the migration cost across ~8 modules, or is chmod-after-create acceptable as documented style with only the eval-time audit (#7) as the guard?** I can build either; the style call with repo-wide review cost is yours.
3. **Is there any window where you would accept running the never-executed VM test suite (restic + the other PSI-gated tests from this batch) under moderate PSI (say avg10 20–40%), or does it stay hard-gated to calm windows?** The queue gates say calm-only; if a weekend-maintenance exception exists, three reports of "runtime-zero, eval-true" debt could clear in one morning — your pacing call, I cannot derive it from the doctrine.

---

*Verification anchors: fix commit `6251c198` (footer `000001a0c70ff761f6779056239de6ae76c9`); rejected parent `49cfb914`; daemon-race pre-amend sha `ff6ede23`; rendered-unit eval `UMask=0077`; sibling sweep 14 grep hits (restic sole umask-less writer); live-host probe: `/var/lib/restic-app-dumps` absent (module deploy-pending, no runtime exposure ever materialized).*
