# Status Report — Fix Task `000001a0cd068f79239e5320030b96d20ad4`

**Date:** 2026-09-23 09:30 CEST
**Scope:** The reviewer-finding fix on the offsite-borg restore-drill work (parent task `000001a0cc8af769f497a7204704871623f0`, commit `f845d1e9` → finding → fix commit `f16f6cc5`), plus what was noticed in passing. No unrelated research.
**Format note:** The status-report skill defaults to a styled HTML dashboard; the task explicitly requested `.md` at this path — honoring the explicit instruction, one-off override, not propagated as a default.

---

## Direct answers first

### What did you forget?

1. **The reviewer's "ideally" clause.** The finding suggested interpolating
   `config.sops.templates."borg-env".path` via a flake-wrapped script. I
   implemented only the minimal literal correction and then failed to backlog
   the "ideally" — it existed only in the reviewer's text until this report.
   The drill still hand-writes a path that is *correct today* but unpinned
   from the template declaration. Fixed by this report's commit: a `[ready]`
   backlog row now exists in `docs/todo/storage.md` + the TODO_LIST storage
   queue section.
2. **A direct live probe of the render path.** I verified the
   `/run/secrets/rendered/` claim by doctrine cross-check (the audit script's
   own class-B text, the AGENTS.md mail-relay incident record, the module's
   interpolation at `platforms/nixos/system/backup.nix:147`) and the
   reviewer's stated live verification — but I never ran the 5-second host
   probe myself (`systemctl cat gatus | grep -m1 EnvironmentFile`). The
   conclusion was right; the evidence chain was secondhand.
3. **(Original run, surfaced by the review):** the sibling runbook
   `docs/services/offsite-borg.md:78` carried the identical pre-existing
   literal while the original commit message claimed the port-23 fix was "now
   coherent in backup.nix, both runbooks, and the drill". A coherence claim
   was made without a same-class sweep of the sibling surface.

### What could you have done better?

1. **In the original task:** run the banned-literal sweep (`grep -rn
   'secrets-rendered'` over scripts/ + docs/, the same one this fix ran)
   *before* claiming the artifacts coherent. The sweep takes seconds and
   would have caught all three sites in one commit — the reviewer finding
   would not exist.
2. **In the fix task:** verify the third site (offsite-borg.md:78) was the
   *last* one by also grepping the drill script's own docs/usage text and any
   rendered-script surfaces — I did the repo-wide grep first thing, so this
   was covered, but the habit should be explicit: sweep → fix → re-sweep,
   evidenced in the close-out (I did re-sweep: 0 remaining matches).
3. **Scope discipline vs. durability tension:** "fix EXACTLY this finding"
   correctly stopped me from flake-wrapping the drill (that changes the
   documented invocation surface in two runbooks + AGENTS.md). The right
   resolution was: minimal fix + immediate backlog — I did the first half and
   only remembered the second half when writing this report. The lesson is
   not "do more in the fix", it is "the backlog append is part of the fix".

### What could you still improve?

1. Guard coverage must follow artifact surface growth: `.sh` tools and
   runbook docs now embed infra paths, but `audit-textfile-tmp.sh` class B
   scans only `.nix` under `modules/ platforms/ tests/ lib/`. The 2026-09-06
   sweep report already carried this as follow-up item 29 (".sh files") and
   it sat 17 days — the recurrence was predictable and predictable
   recurrences are process bugs, not bad luck.
2. The daemon-race amend flow worked exactly as AGENTS.md prescribes
   (verify `git show --stat HEAD` → amend-forward), but it succeeded because
   the heuristic commit contained exactly my 3 files. The vigilance is
   manual every time; the queued "daemon-beat-precommit-hook" incident item
   is the systemic thread.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | Finding disposition: reviewed commit verified present, anchor text located live | `git cat-file -e f845d1e9` OK; anchor `BORG_ENV_FILE="${BORG_ENV_FILE:-/run/secrets-rendered/borg-env}"` at `scripts/borg-restore-drill.sh:122` |
| 2 | Repo-wide literal triage: 25 `secrets-rendered` matches classified; exactly 3 in-scope sites identified; all others sanctioned (incident docs, guard comments documenting the ban, the audit script itself, fixture comments) | `grep -rn 'secrets-rendered'` output reviewed line-by-line |
| 3 | Fix 1 — drill real-mode default corrected to `/run/secrets/rendered/borg-env` | commit `f16f6cc5`, `scripts/borg-restore-drill.sh` 1 line |
| 4 | Fix 2 — same-host restore source line corrected | commit `f16f6cc5`, `docs/services/offsite-borg-restore.md:62` |
| 5 | Fix 3 — pre-existing literal in the ad-hoc borg env block swept | commit `f16f6cc5`, `docs/services/offsite-borg.md:78` |
| 6 | Module-side path already correct — no Nix change needed (the job unit interpolates the template's `.path`; only the drill/docs hand-wrote it) | `platforms/nixos/system/backup.nix:147` `envPath = config.sops.templates."borg-env".path` |
| 7 | Verification battery: 0 remaining `secrets-rendered` in the 3 files; `bash -n` OK; `shellcheck --severity=warning` OK (matching the pre-commit gate); `audit-textfile-tmp.sh` 236 files fail=0; pre-commit full battery green (gitleaks, eval-only `nix flake check`) | command outputs in session; hook log on commit |
| 8 | Backlog: scanner-scope gap filed — extend class B to `scripts/*.sh` + `docs/**/*.md` | `docs/todo/pipeline.md` `[ready]` row + TODO_LIST `### pipeline` queue line (in commit `f16f6cc5`) |
| 9 | Commit hygiene: exact one-line Task-Queue-ID footer (fix ticket only, no parent footer), tree clean after, nothing pushed | `git log -1 --format=%(trailors)` shows `Task-Queue-ID: 000001a0cd068f79239e5320030b96d20ad4`; `git status` clean |

## b) PARTIALLY DONE

| # | Item | Works now | Remains open | Blocker | Effort |
|---|------|-----------|--------------|---------|--------|
| 1 | Reviewer's "ideally interpolate the env path" | Literal corrected; module side interpolates properly | Drill still hand-writes the path — correct today, silently drift-prone if the template ever moves | None — backlog row appended this run | S–M |
| 2 | Offsite-borg restore path (parent item, marked DONE) | Runbook + drill exist; `--selftest` and `--local` modes proven PASS ×2 (2026-09-23, 281/295 ms connect, 326/283 ms extract) | Real-repo drill (go-live step 9) never executed; bare-metal path and FUSE path never exercised anywhere | Owner go-live inputs (StorageBox hostname/username + host-key pin) | S once unblocked |
| 3 | Scanner hardening for the escaped class | Gap precisely diagnosed and backlogged with design notes (prose allowlist needed for incident docs) | Extension not implemented — deliberately out of the fix's scope | None; queue pacing | M |

## c) NOT STARTED

| # | Item | Why | Still wanted? |
|---|------|-----|---------------|
| 1 | Real-repo timed restore drill (go-live checklist step 9) | Blocked on owner-held StorageBox inputs | Yes — it is the item's whole point ("an untested restore is Schrödinger's backup") |
| 2 | Full bare-metal restore rehearsal (dead-host runbook section) | No local stand-in variant exists; needs the real repo or a purpose-built rehearsal | Yes — High |
| 3 | `borg mount` / FUSE verification (`allow_other`-capable env) | Never attempted | Yes — Medium |
| 4 | `audit-textfile-tmp.sh` class-B extension to `.sh`/`.md` | Backlogged this run, not implemented | Yes — High |
| 5 | Drill env-path pinning (flake-wrap or eval-time literal assertion) | Backlogged this run, not implemented | Yes — Medium |
| 6 | Drill hardening batch: real-mode `/mnt/hot` mountpoint gate, FAIL records from the top, flake-pinned borg, deep-integrity mode | Already queued (TODO_LIST storage section, Source: parent task's status report) — untouched by this fix | Yes — High |

## d) TOTALLY FUCKED UP

Nothing in **this fix run** is broken — the fix is minimal, verified, and
committed. The genuinely fucked-up things are the root causes this finding
exposed:

1. **The original commit shipped a restore path that could never work as
   written, under a coherence claim.** Two new artifacts (drill script,
   restore runbook) hardcoded the banned `/run/secrets-rendered` literal, and
   a third pre-existing instance sat one line above text the original commit
   itself edited in `docs/services/offsite-borg.md` — while the commit
   message claimed the fix was "now coherent in … both runbooks and the
   drill". Severity at go-live: the documented same-host restore does `source
   /run/secrets-rendered/borg-env`, bash continues with `BORG_REPO` unset on
   the nonexistent file, and borg dies with a confusing no-repository error
   mid-incident. Root cause: `.sh`/`.md` surfaces are invisible to
   `audit-textfile-tmp.sh` class B (scan roots: `.nix` under modules/
   platforms/ tests/ lib/ only). Mitigation: all three sites fixed in
   `f16f6cc5`; scanner extension backlogged.
2. **The recurrence was a 17-day-old open follow-up.** The 2026-09-06
   mail-relay sweep status report already listed "extend audit to `.sh`
   files" as item 29. It was never harvested into the todo system (the tq
   pool harvests only TODO_LIST.md), so the exact miss it predicted happened.
   Severity: process, not code — but this is the second time an entombed
   status-doc follow-up resurfaced as a reviewer finding.
3. **The auto-commit daemon raced the fix mid-run** (heuristic commit
   `2903fbc2` swept the 3 file edits while the backlog files were being
   edited). Handled per AGENTS.md discipline: contents verified as exactly
   the 3 in-flight files (nothing foreign absorbed), amend-forward to
   `f16f6cc5`. Not damage — but the race window is real and the safeguard is
   manual vigilance every time.

## e) WHAT WE SHOULD IMPROVE

1. **Sweep siblings before claiming coherence** — any commit whose message
   claims "X is now consistent across A, B, and C" must include the same-class
   grep over A, B, and C in the same change. Seconds of grep vs. a full
   review round-trip (this finding cost one).
2. **Live-probe infra claims when a 5-second probe exists** — doctrine
   cross-checks are strong but secondhand; the fixture-trap class
   (`email_states`, mock-sops default) has repeatedly shown "docs agree" is
   weaker than "host says".
3. **Harvest status-doc follow-ups at write time** — follow-up items
   numbered inside status reports are entombed by construction; the queue
   only reads TODO_LIST.md. Candidate convention: every status-report
   follow-up either lands in a domain todo file in the same commit or is
   explicitly marked "report-only".
4. **Guard scope tracks artifact surface** — when a new artifact class
   starts embedding infra paths (drill scripts, runbooks), the corresponding
   lint/audit either gains the surface or the artifact interpolates the value
   (single source of truth). Both escape hatches exist; pick one at
   write time, not at review time.
5. **Backlog the "ideally" immediately** — reviewer suggestions beyond the
   minimal fix are queue input, not report trivia. This one lived only in
   the finding text for the length of the fix run.

## f) Up to 50 things we should get done next

Grounded in this task's domain (offsite-borg leg) + noticed in passing.
Items marked **[queued]** already exist in TODO_LIST/domain files (no
duplication — listed here for the ranked view); **[new]** items from this
run are or will be in the todo system; the rest are report-only brainstorm
for HARVEST to route. Impact / Effort (S <30min, M 30min–2h, L >2h) / Category.

### Offsite-borg leg (the task's domain; steps 1–9 = go-live checklist order)

| # | Task | Impact | Effort | Category | Status |
|---|------|--------|--------|----------|--------|
| 1 | Go-live steps 1–4: store borg_password recovery copy off-host, enable StorageBox SSH, paste the ed25519 pubkey, fill `borg_repo` + `borg_known_hosts` via sops | Critical | S | Feature (owner) | existing checklist |
| 2 | Flip `services.offsite-borg.enable` + deploy (tripwire fails loudly on surviving placeholders) | Critical | S | Feature | existing checklist |
| 3 | First run + WAN seed watch (`TimeoutStartSec=2d`; `journalctl -f borgbackup-job-hetzner`) | Critical | S+wait | Feature | existing checklist |
| 4 | Verify chain: `borg list`, `.last_success`, `backup_healthy{backup="offsite-borg"} 1`, `backup_ever_succeeded`, "All Backups Healthy", post-deploy §16 | Critical | S | Quality | existing checklist |
| 5 | **Real-repo timed restore drill** (step 9) + record timings in the restore runbook | Critical | S | Quality | existing checklist |
| 6 | Sizing measurement ("This archive" + `borg info` vs BX11 1 TB; BX21 trigger ~800 G) | High | S | Feature | existing checklist |
| 7 | Bare-metal restore rehearsal (dead-host section) — build a local-stand-in variant so this is exercisable pre-go-live | High | M | Quality | [new] |
| 8 | `borg mount`/FUSE verification (`allow_other`) | Medium | S | Quality | [new] |
| 9 | Drill hardening batch: real-mode `/mnt/hot` gate, FAIL-from-top, flake-pinned borg, deep-integrity mode | High | M | Quality | [queued] (TODO_LIST storage) |
| 10 | Pin drill `BORG_ENV_FILE` to the sops template: flake-wrap (interpolated at build time) or eval-time literal assertion | Medium | S–M | Quality | [new — backlogged this run] |
| 11 | Update AGENTS.md offsite-borg section to live state after go-live (currently "deployed DORMANT") | Medium | S | Documentation | [new] |
| 12 | Decide + record the borg_password recovery-copy policy/location (open row in docs/todo/storage.md) | High | S | Decision (owner) | existing row |

### Scanner / tooling (the class this finding exposed)

| # | Task | Impact | Effort | Category | Status |
|---|------|--------|--------|----------|--------|
| 13 | Implement class-B extension: `scripts/*.sh` + `docs/**/*.md` into `audit-textfile-tmp.sh`, with a prose allowlist for incident-documenting docs | High | M | Quality | [new — backlogged this run] |
| 14 | Negative-test the extended scanner via `scripts/negative-test-lints.sh` (mutation must FAIL) | High | S | Quality | [new] |
| 15 | Decide whether class A (fixed-tmp writes) also gains `.sh` coverage | Medium | S | Quality | [new] |
| 16 | Convention: status-report follow-up items harvested into domain todos at write time (closes the item-29 entombment class) | Medium | S | Process | [new] |
| 17 | Sweep `scripts/*.sh` for other hand-written `/run/secrets/...`-class paths that should interpolate (the drill hardcodes `/run/secrets/borg_password` etc. — correct per sops-nix, but confirm the pattern repo-wide once) | Medium | S | Quality | [new] |

### Noticed in passing (mostly already queued — pointers, not duplicates)

| # | Task | Impact | Effort | Category | Status |
|---|------|--------|--------|----------|--------|
| 18 | Restic app-dumps first-run proof chain — the first nightly run fired **this morning 05:45**; verify repo + `restic check` + one-file restore smoke + dedup ratio | High | S | Quality | [queued] (TODO_LIST storage) |
| 19 | ClickHouse DB backup first run (code landed 2026-09-23, deploy-gated) — deploy + verify backup dir exists before any SigNoz upgrade | High | S | Quality | [queued] |
| 20 | Own-tools NVMe→pool migrations (discordsync attachments + browser-history DB) need one PSI-quiet deploy | High | S | Feature | [queued, was PSI-blocked] |
| 21 | Phase-2 fold: crush-hot-db interim mechanism into the dormant `services.hot-db` module (AGENTS.md flags the fold pending; do not run both) | Medium | M | Cleanup | [queued] |
| 22 | 2-disk buildcache btrfs merge (maintenance window, module flip + reformat must ride together) | Medium | M | Feature | [queued, blocked:user window] |
| 23 | docs-health HARVEST of this report's non-duplicated items into TODO_LIST/ROADMAP | Medium | S | Process | due now-ish |
| 24 | Daemon-race mitigation (queued incident-filing item): pre-commit re-checks `git status` after its steps | Medium | S | Process | [queued] (TODO_LIST pipeline) |

Deliberately NOT listed: anything requiring research beyond this task's
surfaces — the queue's pacing gates own prioritization.

## g) Questions I cannot figure out myself

1. **Drill invocation convention:** keep `sudo bash scripts/borg-restore-drill.sh`
   (plain repo script, as both runbooks + AGENTS.md document today) or
   converge drills on flake apps (`nix run .#borg-restore-drill`)? This
   decides item 10's shape: a plain script can only get an eval-time literal
   assertion (grep-pinning a script — the fragile-guard pattern), while a
   flake wrap interpolates `config.sops.templates."borg-env".path` at build
   time (durable, but changes the documented invocation surface). I tried to
   pick myself and could not: both are defensible, and the invocation
   convention is yours to set.
2. **Real-repo drill timing:** strictly inside the go-live window after step
   7 verification, or do you want a real-mode dry-run *before* go-live
   (placeholder host → expected loud SSH failure) to prove the real-mode
   wiring (env parsing, cache/cache-dir wiring, root gate) ahead of the real
   credentials? It changes the go-live checklist ordering.
3. **Where should the borg_password recovery copy live** (password-manager
   entry name / printed copy / other)? Go-live step 1 blocks on it, the
   runbook mandates owner-held, and docs/todo/storage.md carries the policy
   as open — only you can answer it.

---

## Verification evidence appendix

| Check | Command | Result |
|-------|---------|--------|
| Reviewed commit present | `git cat-file -e f845d1e9...` | OK |
| Anchor located | grep for verbatim `BORG_ENV_FILE="${BORG_ENV_FILE:-/run/secrets-rendered/borg-env}"` | `scripts/borg-restore-drill.sh:122` |
| Repo-wide literal triage | `grep -rn 'secrets-rendered'` | 25 matches; 3 in-scope, rest sanctioned |
| Post-fix re-sweep (3 files) | `grep -c secrets-rendered <3 files>` | 0 / 0 / 0 |
| Script syntax | `bash -n scripts/borg-restore-drill.sh` | OK |
| Lint (gate parity) | `shellcheck --severity=warning scripts/borg-restore-drill.sh` | OK |
| Audit | `bash scripts/audit-textfile-tmp.sh` | 236 files scanned, fail=0 |
| Commit gates | pre-commit: gitleaks, eval-only `nix flake check` | all green |
| Commit | `git log -1 f16f6cc5` | 5 files, +5/−3, exact footer, tree clean, not pushed |

**Lineage:** parent task `000001a0cc8a` (commit `f845d1e9`) → reviewer
finding (high) → this fix (`f16f6cc5`, footer
`000001a0cd068f79239e5320030b96d20ad4`) → this report (same footer).
