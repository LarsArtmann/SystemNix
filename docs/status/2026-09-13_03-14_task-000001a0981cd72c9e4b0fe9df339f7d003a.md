# Status: task-queue run 000001a0981cd72c9e4b0fe9df339f7d003a — reviewer finding (red-era nixpkgs pin misquoted as e5bdc4a) — verified, annotated, closed — 2026-09-13 03:14

Task: re-review of the attic-closure work (rejected commit `a4d1470d`, work queue item `…97a2c784`). Finding: the superseded §a.4 parenthetical quoted the red-era nixpkgs pin as `e5bdc4a` (actually the Aug-16-era pin); expected fix: cite `34ab99075` in both the status doc and the TODO_LIST row. **Outcome: finding verified and already fixed in content; this dispatch independently re-verified the evidence, annotated the report, and landed the queue footer.** Format note: this report is Markdown per the dispatch's explicit `.md` instruction (the status-report skill's canonical default is styled HTML — one-off override, not propagated).

---

## Direct answers to the three opening questions

- **What did I forget?** The pin-claim verification step. The original pass-1 work wrote "the nixpkgs pin at the red observation (`e5bdc4a`)" from memory instead of running the one command that settles it (`git show <rev>:flake.lock`). The repo already documented this exact discipline (AGENTS "Status reports are point-in-time, re-verify"; the 09-12 stale-harvest lesson) and I repeated the class anyway. Second thing forgotten: the file's abbreviated-SHA style — I initially pasted the full 40-char SHA, which gitleaks' built-in `sourcegraph-access-token` rule matches, blocking the commit.
- **What could I have done better?** (1) Check for duplicate dispatches FIRST — the identical finding was already fixed by the parallel review-fix run `9e972a5f` (02:27, queue `…29157b`); one grep of `docs/status/` for the finding text would have revealed that before I started. (2) Commit immediately after a markdown-only edit, then run gates — my first commit attempt failed with "nothing to commit" because I ran a multi-minute gate in between and the auto-commit daemon captured the file footer-less. (3) My first abbreviation edit still embedded the full SHA inside its own replacement text; it took a second edit to actually remove it.
- **What could I still improve?** Encode the lessons so they cannot recur: an AGENTS.md rule against 40-char hex literals in tracked docs (they trip the whole-index gitleaks scan), the `git show <rev>:flake.lock` gate as a hard pre-write step for any "the lock pinned X at time T" claim, and a duplicate-dispatch check in my queue-start routine. All three are in section (f).

---

## a) FULLY DONE

1. **Reviewer's evidence independently re-verified before touching anything.** `git show 094d383d:flake.lock` → zero occurrences of `e5bdc4a`; walked the lock per the AGENTS node-key-mapping gotcha (top-level `root` is a node-key STRING, not a mapping): `nodes[root].inputs.nixpkgs.locked.rev` = `34ab99075ac4f7e40cf037eef32cb1c360bb85e9`. `e5bdc4a` confirmed as the Aug-16-era pin (AGENTS DAS section: `26.11.20260816.e5bdc4a`). Reviewer finding confirmed correct on all points, including the adjacent "lock moved to `c043004` only on 2026-09-05, after the red" claim.
2. **Discovery that the content fix already existed.** The parallel review-fix run `9e972a5f` (2026-09-13 02:27, queue `000001a0981cd729157b22ac6724d6f6b34d`) had already rewritten BOTH targets: status doc §a.4 now reads "the nixpkgs lock went `34ab99075` (Sep-4 tree, `git show 094d383d:flake.lock`) → `c043004d1` (current; moved 2026-09-05)" and TODO_LIST row 28 carries the same correction. Both files grep clean of `e5bdc4a` as a pin citation. Re-applying the fix would have been a no-op; skipping it and re-verifying instead was the correct minimal action.
3. **Closure annotation written into the artifact this task produced.** `docs/status/2026-09-13_00-19_task-…97a2c784.md` §a.4 gained a PIN-CITATION annotation (reviewer finding quoted, evidence re-verified, correction attributed to `9e972a5f`) + verification-table row 8 recording the exact commands (`git show 094d383d:flake.lock` grep + the jq node-key walk) and results.
4. **Gitleaks trap found and defused before it poisoned the repo.** The hook exports the ENTIRE index (`git checkout-index -a`) and scans it — a full 40-char hex SHA in ANY tracked doc would have blocked every future commit repo-wide. Fixed by citing the pin abbreviated (`34ab99075`, matching the file's style and the finding's own wording), with the abbreviation rationale recorded in the doc. Post-fix: gitleaks clean on the file and on the full exported index.
5. **Footer landed despite two daemon races.** The auto-commit daemon captured my edits twice footer-less (`138f8b81` while a gate ran; `7ea4e5f6` while the hook's full flake check ran). Final state: commit `120ada36` (amended HEAD, unpushed) carries the full message + exact footer `Task-Queue-ID: 000001a0981cd72c9e4b0fe9df339f7d003a`; tree clean; only the status doc touched.
6. **Gates green on the final tree.** During the interrupted hook run AND verified after: gitleaks on exported index (PASS), trailing whitespace (PASS), full `nix flake check` (PASS — all checks; markdown-only change, no `.nix` touched). Markdown-only `--no-verify` per repo precedent `73145250`, reason documented in the commit message.
7. **TODO_LIST loop state confirmed correct without re-editing:** row 28 is `[x]`, cites the correct pin, links the 00-19 report. The finding did not require a TODO_LIST change (the row's pin sentence was already correct post-`9e972a5f`).

## b) PARTIALLY DONE

1. **Duplicate-dispatch coordination is implicit, not recorded queue-side.** This dispatch and `…29157b` covered overlapping findings from the same review of `a4d1470d`; I annotated the report (§a.4 + row 8) but the QUEUE itself has no marker that two items resolved one finding class. Left implicit because queue-side tooling is outside this repo.
2. **The "never paste 40-char hex into tracked docs" lesson is applied but not yet codified.** Applied in this doc + the commit message; not yet in AGENTS.md (section f, item 1) — deliberately not done here: AGENTS.md edits were outside the finding's scope and the tree had two parallel sessions active.
3. **Root-cause hygiene from the original pass-1 error.** The retraction chain (a4d1470d → 9e972a5f → this annotation) documents WHAT was wrong and the correct evidence, but no prevention layer exists for "memory-sourced pin claims" (see b.2 of the 00-19 report and f items 6/14).

## c) NOT STARTED (follow-up candidates, deliberately not done here — smallest-correct-change contract)

1. AGENTS.md rule: no full-length hex literals (commit SHAs, drv hashes beyond the established `…`-abbreviated style) in tracked docs — gitleaks scans the whole index on every commit, so one violation blocks ALL commits repo-wide. Place under Critical Rules or a new "Docs" gotcha.
2. Repo-wide audit: `git grep -E '[0-9a-f]{40}'` over tracked files to find OTHER docs carrying full SHAs (noticed in passing: `docs/status/2026-09-12_00-42…`, `2026-09-12_00-59…`, `2026-08-18_21-15…`, an archived Hyperland doc matched the generic regex — unverified whether they match the actual gitleaks rule; commits currently pass, so probably not, but the margin is untested). Abbreviate or allowlist findings.
3. `.gitleaks.toml` decision: bounded allowlist for SHA-shaped strings quoted in docs (e.g. backtick-delimited 40-hex) vs. abbreviation-only policy — tradeoff: allowlist weakens secret detection heuristics; abbreviation costs nothing but must be followed.
4. Queue-start duplicate check as personal doctrine: before executing a review-fix item, grep `docs/status/` for the finding's distinctive quote (this dispatch's fix was 95% done before I started).
5. Harvest this report's section (f) into TODO_LIST/ROADMAP per docs-health HARVEST (the skill explicitly requires the loop close there, not here).

## d) TOTALLY FUCKED UP!

1. **The pass-1 fabrication-class error (the reason this finding exists).** The original work (before `a4d1470d`) cited the red-era pin from memory. In a report that quotes drv hashes to 6+ chars, a memory-sourced pin claim is precision theater — and it was caught by the reviewer, costing a full review round-trip. This is the second confirmed instance of the stale-harvest/uncited-claim class in this task family (09-12 paperless closure was the first).
2. **Race loss #1 — "nothing to commit".** I ran the fast gate BETWEEN editing and committing; the daemon committed `138f8b81` in the window and my footer commit found a clean tree. Wasted one cycle; the fix (commit first, gates after, amend on race) only landed after the SECOND race.
3. **Race loss #2 — hook-runtime window.** The first amend ran the hook's FULL flake check (multi-minute), giving the daemon a window to commit `7ea4e5f6` mid-hook. The amend failed with a ref-lock error AFTER all gates had passed. For markdown-only commits with a known-clean staged set, the correct sequence was: stage → `--no-verify` amend (sub-second) → gates already run manually. Documented; both losses were self-inflicted sequencing errors, not daemon misbehavior (AGENTS explicitly documents the daemon's bypass-hooks behavior).
4. **Sloppy edit sequencing.** My first attempt to remove the full SHA wrote a replacement that STILL contained the full SHA (inside a parenthetical). Caught it myself one step later, but a careful single edit would have avoided a second diff cycle on a file another session could touch at any moment.

## e) WHAT WE SHOULD IMPROVE!

1. **Verification-before-write for factual claims** — make `git show <rev>:flake.lock` (or domain equivalent) a MANDATORY pre-write step for any "X was the state at time T" sentence; the cost is seconds, the failure class is reviewer-trust-destroying.
2. **Markdown-only commit fast path** — for markdown-only changes with no eval surface: write → stage → commit `--no-verify` immediately → run gates → amend if needed. The daemon's seconds-cadence makes any multi-minute gap a race.
3. **Gitleaks-vs-hex-shape awareness** — the hook scans the whole index; treat full-length hex literals in tracked docs as repo-wide commit blockers. Abbreviated forms + a "full form in the session, abbreviated in the tree" convention are strictly better.
4. **Duplicate-dispatch detection at queue start** — one grep for the finding's distinctive text across `docs/status/` before starting work.
5. **Codify the lessons (AGENTS.md)** — pin-claim gate, hex-literal rule, daemon-race protocol; all three are cross-session liabilities, currently only scattered in status reports and commit messages.

## f) Things we should get done next (brainstorm — most items are ROADMAP fuel; ~1–12 are harvest-grade, the rest opportunistic)

1. AGENTS.md: add the "no full-length hex literals in tracked docs (gitleaks whole-index scan)" rule (from c.1).
2. AGENTS.md: add the pin-claim verification gate ("any 'lock pinned X at time T' gets `git show <rev>:flake.lock` first") to Cross-Cutting Lessons (from e.1).
3. AGENTS.md: document the markdown-only daemon-race commit protocol (write → commit fast → gates → amend) next to the existing daemon paragraph (from e.2).
4. Audit tracked files for gitleaks-rule-matching hex strings (`git grep -E '[0-9a-f]{40}'` + the rule's actual regex — I inferred the shape from the block message, never read the rule definition) (from c.2).
5. `.gitleaks.toml` policy decision: SHA allowlist vs abbreviation-only (from c.3).
6. Queue/dispatcher: dedupe or cross-reference identical reviewer findings filed as separate queue items (this dispatch duplicated `…29157b`'s fix scope) (from b.1).
7. docs-health HARVEST: pull harvest-grade items from this report into TODO_LIST/ROADMAP (skill's closing mandate).
8. Add the verify-before-harvest dispatcher rule (re-run the cited check before any "red since X" row enters the queue) — already proposed in the 00-19 report §g.1, still unbuilt; this finding is a third data point (pin claim, same family).
9. Small script or flake check: lint status docs for full-length hex literals (mechanical version of item 1, catches the class at pre-commit instead of by convention).
10. Confirm the other full-hex-matching docs (2026-09-12 task reports, papdashboard 08-18 report) actually pass the gitleaks rule shape — if yes, record WHY in the audit result so the safe shape is documented (from c.2).
11. Personal-doctrine checklist update: duplicate-dispatch grep at queue start (from e.4).
12. Annotate the 02-29 report with a one-liner that its finding class also spawned this duplicate dispatch `…d003a` (cross-reference completeness, 2 minutes).
13. Consider extending the pre-commit hook with a markdown-only fast path (skip the multi-minute flake check when the staged diff contains no evaluable file types) — reduces the race window for ALL future doc commits (observed cost this session: two races, ~10 min).
14. Same hook: consider whether the full-index gitleaks export could additionally print WHICH file matched (it did print file/line for my doc — good; keep) — no change needed, recorded for context.
15. `.cache/gatus-src` + `.cache/signoz-src` fixture keys trip directory-level gitleaks scans (gitignored, never in the index — verified); add a note to the scanner docs so a future `--no-git` full-directory run isn't mistaken for a repo leak (noticed in passing during the block diagnosis).
16. The 00-19 report §b.1 root-cause gap (why the Sep-4 red happened) remains open by design (L effort, low value) — unchanged verdict; listed so it is not lost.
17. The latent `atticd-storage-dir` ReadWritePaths wart (00-19 §c.1) remains open by design — unchanged.
18. The "verify-before-harvest" prevention layer (00-19 §c.2) remains unbuilt — folded into item 8.
19. Queue hygiene: when a dispatch's expected fix is already present in-tree, is a "verified already-fixed + footer commit" closure (what this run did) the sanctioned pattern, or should such dispatches return a distinct verdict to the queue? (Queue contract question — also §g.)
20. Cross-session tree etiquette worked as designed this session (parallel signoz-coverage work landed `c609f8a5` mid-window with no conflicts); no action — recorded as a positive control for the concurrent-sessions doctrine.
21. Status-report skill default (styled HTML) vs repo task-report convention (Markdown, sections a–g) diverge; if these queue task reports become the norm, consider a skill variant or a template so format stops being a per-dispatch decision.
22. When annotating existing reports, prefer ONE consolidated annotation block per finding (done here) over scattered edits — keep as convention.
23. Evidence tables: adopt "abbreviated in tree, full form recoverable via the cited command" as the standing style (applied in row 8; generalize).
24. If the queue keeps re-dispatching reviewer findings after a parallel run fixed them, consider a lightweight "claim" step (write a one-line intent note into the report early) so a duplicate dispatch at least discovers the sibling run in seconds — cheaper than the post-hoc discovery path.
25. AGENTS.md's DAS section remains the authoritative `e5bdc4a` reference (Aug-16 pin) — verified consistent this session; no action, listed to close the loop on the reviewer's context citation.
26. Post-commit hook prints deploy hint on every commit; harmless, no action.
27. Consider recording in docs-health conventions that status-report section (f) items above ~N=12 are brainstorm-grade and must not be auto-harvested into TODO_LIST wholesale (skill already warns; reinforce in HARVEST).
28. For future review-fix dispatches: quote the reviewer's finding verbatim in the report header (done here) — keeps the evidence chain self-contained without re-reading the queue.
29. The work commit's message embeds the full SHA-abbreviated forms correctly; no action.
30. Optional: a `git alias`-style helper for the lock-walk one-liner (`jq '.root as $r | .nodes[$r].inputs.nixpkgs…'`) since the node-key-string gotcha cost a failed jq invocation this session — convenience only.

## g) QUESTIONS (cannot figure out myself)

1. **Queue contract:** when a dispatched finding's expected fix is already present in-tree (fixed by a parallel queue item), should the dispatch close as "verified already-fixed" (this run's pattern) with its own footer commit, or is a distinct verdict/reporting path expected so the queue can dedupe? I chose the former because the contract demands a footer commit either way.
2. **Policy:** full 40-char commit SHAs in tracked docs — abbreviation-only (current, safe, slightly lossy in-tree), or a bounded gitleaks allowlist for SHA-shaped strings (convenient, marginally weakens the secret scan)? My recommendation: abbreviation-only; full forms live in git history and session logs anyway.
3. **Hook design:** is a markdown-only fast path in the pre-commit hook (skip the multi-minute full `nix flake check` when no evaluable file is staged) acceptable to the repo's risk posture? It would have prevented both daemon races this session; the tradeoff is that a stale `.nix`-affecting state could hide behind a "markdown-only" diff — mitigatable by the existing fast `nix flake check --no-build` fallback.
