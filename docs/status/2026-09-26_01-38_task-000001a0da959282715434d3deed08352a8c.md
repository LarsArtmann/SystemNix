# Task 000001a0da959282715434d3deed08352a8c — Status Report (run 3: comprehensive)

**Date:** 2026-09-26 01:38 CEST
**Ticket:** reviewer fix task for finding "Doctrine root-window restatement left internal contradictions in AGENTS.md at close-out" (medium), rejected change `58cb44ae` (original task `000001a0d7ddc4c5db6b3a8da3acdfb9a1d8` close-out).
**This dispatch = runs 2+3:** run 2 (re-verification report, commit `9c175c70`, 01:35) + run 3 (this report). Run 1 = `docs/status/2026-09-26_01-09_…same-ticket…md`, commit `c9c287b9` (no-edit verdict).
**Verdict (unchanged, now fully evidence-backed):** the finding no longer applies — anchor text gone from master; every root-window echo in the finding's scopes (AGENTS.md, docs/todo, docs/services) plus the adjacent scopes (FEATURES.md, scripts/) reads the reconciled **2w sharp** figure. Zero content edits made in this dispatch; the finding's own instruction ("if all echoes are already aligned on master, close this finding with no further edits") was followed.

---

## a) FULLY DONE

| # | Item | Evidence (all fresh THIS dispatch unless marked) |
|---|------|--------------------------------------------------|
| 1 | Rejected-commit disposition protocol executed: `git cat-file -e 58cb44ae…` → EXISTS; `git merge-base --is-ancestor` → on master (not rebased away) | Session command output, run 2 |
| 2 | Anchor-text check: `root ≈ 1-2w` in the reserve doctrine-table row is GONE from master; AGENTS.md:670 reads `root = 2w sharp` | Grep over AGENTS.md + docs/ + TODO_LIST.md + FEATURES.md; anchor hits only inside historical status reports (legal) |
| 3 | Four-pattern echo sweep (`3d 1w`, `3d+1w`, `1-2w`, `2w sharp`) over the finding's three scopes: AGENTS.md (:658 legal config restatement + negation, :660 paragraph, :670/:671 rows, :699 T14 caveat, :972 ClickHouse note — all reconciled), docs/todo/ (only DONE notes + queue rows quoting patterns as history/gate fixtures — legal), docs/services/ (zero hits) | Session greps, run 2 |
| 4 | Adjacent-scope verification, re-run fresh (closing run 2's cited-not-run gap): FEATURES.md:291 aligned to `root pin window = 2w sharp, calendar-anchored` (sibling fix `3e3d718a`); `scripts/migrate-clickhouse-xfs.sh` banner lines 392-394 carry `2w sharp, calendar-anchored`; line 404's "1-2 weeks" classified LEGAL (soak-period advice for the recovery snapshot, not a pin-window restatement) | Session grep + file read, run 3 |
| 5 | Unicode-variant blind spot closed: en-dash/spaced variants (`1–2w`, `≈1-2w`, `≈ 1-2w`, `~1-2w`, `1-2 w`) grepped over ALL live surfaces (AGENTS.md, docs non-archived, TODO_LIST, FEATURES, README, CHANGELOG, ROADMAP, scripts/, modules/, platforms/) — ZERO en-dash hits anywhere; remaining `≈1-2w` hits are DONE notes/queue rows (legal) | Session grep, run 3 |
| 6 | Gate status verified with an actual run: `nix flake check --no-build` red ONLY on the pre-existing hermes VM-test eval failure (`error: path 'ix80s6aj697xcrzlnlc074wz6zd0avf4-source' is not valid`) — identical to the breakage documented in `c9c287b9`'s message; unrelated to this docs-only diff | Session command, run 2 |
| 7 | Run-2 re-verification report written and committed: `docs/status/2026-09-26_01-35_task-…md`, commit `9c175c70`, pathspec-scoped (1 file), `Task-Queue-ID: 000001a0da959282715434d3deed08352a8c` footer present, not pushed | `git show 9c175c70` |
| 8 | TODO_LIST loop: nothing to mark — the lineage's original queue item is already `[x]` (TODO_LIST.md:48 with DONE note); fix tickets carry no TODO_LIST row. Contract #3 satisfied vacuously | TODO_LIST.md:48-51 |

## b) PARTIALLY DONE

| Item | What works | What remains | Blocker | Effort |
|------|-----------|--------------|---------|--------|
| Finding-closure completeness | Named scopes + FEATURES + scripts all fresh-verified aligned | One adjacent surface never checked by ANY run in this lineage: whether macOS/darwin docs carry retention prose with a stale root window (run-1 report item 47 judged it "likely N/A" but never looked) | None — one grep on the darwin half of `platforms/` | S |
| Stale-figure grep-gate | Fully specced (storage.md:136): patterns, context rules (negation / DONE notes / config restatements), scope extended to top-level living docs after the FEATURES.md 7th-site catch; fixture set = the 7 found sites | Not implemented (no flake check / pre-commit leg exists yet); pattern list should now ALSO include the unicode variants (see e1#2) | None — pure implementation work, `[ready]` | S-M |

## c) NOT STARTED

| Item | Why not started | Priority |
|------|----------------|----------|
| Implement the grep-gate (storage.md:136) | Queued `[ready]`; out of this fix ticket's "exactly this finding" scope | High — it is the durable fix for the failure class this whole ticket family exists because of |
| Calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix (storage.md:15, TODO_LIST:51) | Queued `[ready]`; authoring-time fix for the original wrong-figure episode | Medium |
| DONE-note era-annotation convention (pipeline.md:166) + annotate storage.md:66's landed-history `≈1-2w` | Queued `[ready]`; needs the CONTRIBUTING codification decision | Low-Med |
| Owner decisions: root-@ retention retune intent; archived-report era-banner policy; queue semantics for footered no-edit closures (see g) | Owner-only; asked twice before (12-27 §79, 01-09 §owner), unanswered | Medium (they gate (c) items 1-3's final shape) |

## d) TOTALLY FUCKED UP

Nothing from THIS dispatch — zero content edits were made, so nothing could break. Radical-honesty inventory of the surrounding state:

1. **The pre-commit flake-check leg is RED for every session on this box** (pre-existing, not mine): the hermes VM test eval dies `error: path 'ix80s6aj…-source' is not valid` (source absent from store, cannot be realized standalone). Re-observed live this dispatch. Severity: blocks the repo's own verification gate for all docs AND code commits → forces the documented `--no-verify` protocol (`7467e2e2`/`37869731`/`e62e64db`/`c9c287b9`), i.e. the gate that exists to catch breakage is currently bypassed per-commit. Mitigation in place (protocol + fresh triage note in each commit message); a durable triage is NOT queued anywhere I could find — that gap is item 1 in (f).
2. **Master briefly carried a self-contradicting doctrine** (the original sin this ticket family exists for): `22a4e88f` shipped "2w sharp" in the doctrine paragraph while two table rows + the T14 caveat in the SAME section said "1-2w"/"3d+1w", for ~hours, healed by `eb844911`. Root cause: a numeric figure lives in ≥6 prose surfaces and no mechanical gate exists. Not fucked-up anymore — listed because (d) is worthless without it.
3. **Three status reports now exist for one fix ticket** (01-09, 01-35, this file) because the queue re-served the ticket after a footered no-edit closure. Not broken, but report-proliferation noise with no cross-links between the three (fix: (f) item 4).

## e) WHAT WE SHOULD IMPROVE

**e1. What did I forget?**
1. In run 2 I cited `scripts/migrate-clickhouse-xfs.sh` alignment and the FEATURES.md fix from PRIOR-run reports under the label "verified in passing" instead of re-running the greps myself — a commit-message-evidence-rule violation in spirit (a claim of "X is aligned" should rest on a run in THIS session or name its source run). Run 3 closed it: fresh grep + file read (a.4). The fresh run also surfaced line 404's "1-2 weeks", which run 2's report would have missed had it been a real echo.
2. The en-dash/unicode variant class (`1–2w`) was invisible to ALL greps in this lineage — the finding's four patterns and both sweeps were ASCII-hyphen-only. A unicode-variant echo would have sailed through three sweeps and two reviews. Closed this run (a.5, clean), but the future gate must own this class (b).
3. Nothing else in the finding's scope: the three named scopes, the anchor, and the dangling-commit protocol were all covered in run 2.

**e2. What could I have done better?**
1. Evidence labeling: every verification-table row should say "fresh this run" vs "cited from <report/commit>". Run 2's table blurred the two; this report fixes the convention (a: all rows labeled).
2. RC-capture hygiene: my `nix flake check … | tail -25; echo RC=$?` printed tail's rc (0) next to a failing eval — misleading to a skimmer. The verdict rested on the captured error body (sound), but the command should have been `set -o pipefail` or rc-captured directly.
3. Report economy: run 2's report + this run 3 report could have been ONE comprehensive report if I had anticipated the dispatch shape; alternatively run 2 could have been skipped entirely (run 1's verdict + footer was already on master). Three reports for one ticket is the cost of not knowing whether the queue registered `c9c287b9`.
4. Cross-linking: none of the three run reports links the others with supersession context (fix in (f)).

**e3. What could I still improve (process/design)?**
1. **Make the figure-sweep mechanical** — the grep-gate (b) is the fix; prose "one-grep sweep" lessons have now failed three times in this lineage (11-51 §e1, the FEATURES miss, the unicode class).
2. **Evidence provenance as a report convention** — fold "fresh vs cited" into the status-report skill's section-quality guide so every future report inherits it (per the skill's own feedback-loop rule: improvements recurring in 2+ reports become skills).
3. **No-edit closure artifact** — define what a no-edit fix closure must produce (e.g. a report with a canonical DISPOSITION header the queue can grep) so footered closures are recognized and tickets don't re-dispatch.
4. **Gate pattern hygiene** — when the grep-gate lands, seed its pattern list from this lineage's full history: `root ≈ 1-2w`, `3d+1w` (scripts/docs/services/docs/todo/top-level living docs), PLUS unicode/spaced variants (`1–2w`, `≈1-2w`), with the three context rules already specced (negation, DONE notes, config restatements).

## f) Up to 50 things we should get done next (ranked; based ONLY on this task's work + noticed in passing)

| # | Task | Impact | Effort | Category | Queue status |
|---|------|--------|--------|----------|--------------|
| 1 | Durably triage the hermes VM-test eval red (`source … not valid`) so the pre-commit flake-check leg can be re-armed and the `--no-verify` docs-only protocol retired | High | M | Bug | NOT queued — new |
| 2 | Implement the stale-figure grep-gate per storage.md:136 + unicode/spaced variants from e3.4, fixture set = the 7 lineage sites + the legal-context negative cases | High | S-M | Quality | `[ready]` (storage.md:136) — extend pattern list |
| 3 | Add calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix (weeks start Sunday; weeklies live 14d) | Medium | S | Documentation | `[ready]` (storage.md:15) |
| 4 | Cross-link the three run reports of this ticket (01-09 / 01-35 / 01-38) with one-line supersession headers so a future reader lands on the current verdict, not run 1's | Low | S | Documentation | NOT queued — new |
| 5 | Check darwin-side docs/config for retention prose carrying a stale root window (`grep -rn "3d 1w\|1-2w" platforms/darwin/`) — last unchecked adjacent surface | Low | S | Verification | NOT queued — new |
| 6 | Codify the DONE-note era-annotation convention in CONTRIBUTING (TODO-system section) and annotate storage.md:66's `≈1-2w` landed-history note | Low-Med | S | Documentation | `[ready]` (pipeline.md:166) |
| 7 | Nice-to-have: `migrate-clickhouse-xfs.sh` line 404's soak advice could cross-reference the 2w-sharp drain explanation (lines 392-394) so the two adjacent statements read as one doctrine | Low | S | Documentation | NOT queued — new |
| 8 | Owner decision: is `snapshot_preserve 3d 1w` (→ 2w-sharp pin) the intended final config, or should retention tighten now the semantics are source-proven? Decides whether this figure changes again (and the gate needs round 2) | Medium | S | Decision | owner-gated |
| 9 | Owner decision: archived-report era-banner policy (never-rewrite absolute vs one-line ANNOTATE banner for actively-misleading figures; 5+ archived reports state 3d+1w as current truth) | Medium | S | Decision | owner-gated |
| 10 | Queue/owner decision: define the no-edit-closure artifact so footered closures stop re-dispatching (see g1) | Medium | S | Process | owner-gated |
| 11 | Fold "fresh vs cited" evidence labeling into the status-report skill's section-quality guide (crush-config repo, not SystemNix) | Low | S | Process | cross-repo — new |

(Stopped at 11: the instruction says up to 50, but everything else I could name would be unrelated research or filler — this ticket's blast radius is documentation-only and items 1-11 cover every gap it exposed or noticed.)

## g) Three questions I cannot figure out myself

1. **Queue semantics:** run 1 closed this ticket with a committed report carrying the exact `Task-Queue-ID` footer (`c9c287b9`), yet the ticket re-dispatched to me. What does the queue actually match on to mark a fix ticket resolved — is a no-edit closure expected to produce a specific artifact/keyword (which run 1 lacked), or is re-dispatch after a footered closure a queue bug worth filing? (Tried: verified the footer byte-for-byte in `c9c287b9`'s message and grepped the report for disposition language; I cannot see the queue's matcher.)
2. **Retention intent:** now that the root `@` pin window is source-proven at **2w sharp** (every deleted NVMe byte pinned 14 days, calendar-anchored), is `snapshot_preserve 3d 1w` + `preserve_min 2d` the intended long-term tradeoff (max rollback depth, slow space recovery on a historically-full box), or should it tighten? This decides whether the figure this entire ticket family chased is even the final figure.
3. **Historical-text policy:** is "never rewrite archived reports" absolute, or do dated reports/planning docs that state `3d+1w` as the currently-true root window (5+ of them) get a one-line top era banner via docs-health ANNOTATE? Asked 2026-09-25 12-27 §79 and in run 1's report; still unanswered, and it gates whether the grep-gate needs an archived-scope exception forever.

---

## Verification appendix (commands and results, this dispatch)

| Check | Command | Result |
|-------|---------|--------|
| Dangling commit | `git cat-file -e 58cb44ae…` / `git merge-base --is-ancestor … HEAD` | EXISTS / ANCESTOR-OF-HEAD |
| Anchor gone | `grep -rn "root ≈ 1-2w" AGENTS.md docs/ TODO_LIST.md FEATURES.md` | Only historical status-report hits |
| Four patterns, three scopes | grep over AGENTS.md, docs/services/, docs/todo/ | All echoes reconciled; legal contexts only |
| Unicode variants | `grep -rn "1–2w\|1-2 w\|≈1-2w\|≈ 1-2w\|~1-2w\|~ 1-2w"` over all live surfaces | Zero en-dash hits; remaining hits legal (DONE notes/queue rows) |
| Scripts adjacency | `grep` + read `scripts/migrate-clickhouse-xfs.sh:390-419` | Banner carries 2w sharp; line 404 = legal soak advice |
| Gate | `nix flake check --no-build` | Red only on pre-existing hermes eval failure (documented, unrelated) |
| Commit | `git commit -- docs/status/2026-09-26_01-35_…md` | `9c175c70`, 1 file, footer present, not pushed |
