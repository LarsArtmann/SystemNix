# Task 000001a0c7c71490118972242781061faaa9 — Offsite Borg Leg (Verification Pass)

**Date:** 2026-09-22 12:42 CEST
**Work item:** TODO_LIST.md §storage — "Implement offsite Borg leg to Hetzner StorageBox"
**Session type:** Verification / close-out dispatch (implementation was already landed by a prior session of the same queue item; this run confirmed it end-to-end rather than duplicating work)

---

## a) FULLY DONE

### Offsite Borg leg to Hetzner StorageBox — implemented, verified, closed

The item is **complete**. This session's verification evidence:

| Check | Result |
|---|---|
| `nix flake check --no-build` | **All checks passed** (rc 0) |
| Dormant eval (`enable = false`) | Zero units leak into the generation — no job, no timer, no dir unit, no freshness row |
| Enabled-shape eval (throwaway `extendModules` + `lib.mkForce` probe, per Critical Rules) | Job materializes: paths `/etc`, `/home/lars`, `/data`, `/mnt/pool/backups`, `/mnt/pool/services/immich`, `/mnt/pool/services/paperless`; 06:30 daily `Persistent`; `auto,zstd,9`; prune 7d/4w/6m; timer `borgbackup-job-hetzner` present |
| Go-live gate | `repo = PLACEHOLDER@go-live.invalid:…` — fail-closed tripwire ExecStartPre aborts with the checklist pointer while owner inputs (hostname/username) are missing; nothing can half-deploy against a fake host |
| Mount-gated cache dir | `borg-offsite-dir` oneshot `wantedBy = multi-user.target` (deploy.sh provisioner-restartable), Borg caches on the Samsung `/mnt/hot/borg` per the emergency-reserve doctrine |
| Backup-coordination | `offsite-borg` freshness row present → `backup_healthy{backup="offsite-borg"}` + aggregate "All Backups Healthy" Gatus check |
| Sops secret | `platforms/nixos/secrets/borg.yaml` tracked in git (commit `1c7e85bc` fixed it being gitignored); password is a real random value, owner must hold a recovery copy (policy open in docs/todo/storage.md) |
| Runbook | `docs/services/offsite-borg.md` — go-live checklist + recovery-copy policy |
| TODO_LIST.md loop | Item already marked `[x]` "DONE 2026-09-22" (commit `aae1b75a`) |

**Deliberately NOT done (by design):** deploy. The leg ships dormant (`enable = false`, configuration.nix:1089) — go-live is owner-gated on the Hetzner StorageBox hostname/username + `ssh-keyscan -p 23` host-key pin into `borg_known_hosts`. No user input arrived, so activating it would have violated the fail-closed gate's purpose.

### Session hygiene

- No `git reset`/`checkout`/`rm`; no tree edits to verify (throwaway eval expressions only, per the Critical Rules pattern).
- First `mkForce`-free extendModules probe hit a same-priority conflict (plain `false` in configuration.nix) — recognized as a probe artifact, not a repo bug; re-probed correctly.
- Working tree left clean; no empty commits (the prior session's commits carry the work).

---

## b) PARTIALLY DONE

1. **Verification depth on the enabled shape.** I confirmed the job/timer/dir/freshness surfaces materialize, but did not build the enabled toplevel (only eval). A full `nix build` of the enabled toplevel would catch a class eval cannot (e.g. unit-text rendering issues in `services.borgbackup.jobs`). Low residual risk — the job rides the stock nixpkgs borgbackup module — but it is the one probe above eval depth I skipped.
2. **Offsite Borg restore path** (TODO_LIST line 28, separate queue item): runbook + first timed restore drill — not started, correctly left to its own item.

---

## c) NOT STARTED (tracked, agent-executable)

- **Restore drill** (TODO_LIST §storage line 28): `borg extract` runbook + one timed restore of a small subtree against a scratch repo. The single most valuable next step for the leg — a backup you have never restored is Schrödinger's backup.
- **Restic↔Borg DR-architecture decision** (TODO_LIST line 48): owner-gated (`[blocked:user]`-shaped) — decides whether forgejo-subvol/github-voice-corpus/pixel6 join restic `paths` and how the two repos interact. Not mine to decide.

---

## d) TOTALLY FUCKED UP

**Nothing in this task's scope.** Two honest near-misses worth recording:

1. **The queue re-dispatched an already-completed item.** The implementing session (same Task-Queue-ID) landed the module, runbook, sops file, deploy.sh wiring, and closed the TODO item — then a second dispatch hit the queue. I detected this within two tool calls (git log + TODO_LIST grep) and switched to verification instead of re-implementing (which would have produced conflicting edits against a clean, committed state). Not damage, but the queue's dedup or the prior session's close-out message didn't prevent the re-dispatch.
2. **First extendModules probe failed noisily** on the plain-`false` priority conflict. Caught and corrected in one step; no consequence.

---

## e) WHAT WE SHOULD IMPROVE

1. **No VM test for `backup.nix`.** Every other backup-producing service has one (test-cv steps 9-10 caught the cv-backup silent no-op; test-miniflux asserts the pg_dump chain). Offsite-borg has zero test coverage — the go-live tripwire, the excludes, and the `.last_success` marker freshness wiring are verified only by eval + code review. A minimal VM test (fake borg on PATH, assert tripwire fires on PLACEHOLDER, assert job shape, assert marker write) would close that gap before go-live, which is exactly when debugging is most expensive.
2. **No pre-deploy/post-deploy hook.** Other backup legs have deploy.sh smoke coverage (§12 mail relay, §13 pool flags). Offsite-borg will be invisible at deploy time until the first 06:30 run fails or succeeds silently. A post-deploy smoke asserting the unit's EnvironmentFile override + tripwire behavior (dormant: expect the PLACEHOLDER failure path to be absent since the timer isn't enabled; enabled: expect tripwire OR success marker) would match the house pattern.
3. **TODO_LIST dispatch didn't dedup.** See (d)1. Either the prior session should have moved the item to a "done" section the harvester skips (it did mark `[x]` — the harness dispatched from a stale snapshot), or the queue needs a done-item guard. Not fixable in-repo; noting for the queue owner.
4. **Recovery-copy policy is still open.** The borg passphrase is a real random value in sops; if the host dies AND sops dies, the offsite repo is dead weight. The runbook flags it; the owner decision (password manager / paper copy) is pending in docs/todo/storage.md. This should block go-live, not trail it.

---

## f) UP TO 50 THINGS WE SHOULD GET DONE NEXT

Sorted roughly by impact. Items 1-3 are Borg-leg-adjacent (this task's neighborhood); the rest are things noticed in passing during verification. Items already tracked in TODO_LIST.md keep their routing; new ones (marked **NEW**) may be appended to the queue by a HARVEST pass.

**Offsite Borg leg:**
1. **NEW** — VM test for `backup.nix`: go-live tripwire fires on PLACEHOLDER repo, job shape (paths/prune/compression), `.last_success` marker write, sops template wiring. (before go-live)
2. **NEW** — Post-deploy smoke for offsite-borg in `scripts/post-deploy-check.sh`: unit-env override present; tripwire-or-marker verdict, matching the §12/§13 house pattern.
3. Restore drill runbook + first timed restore (already queued, TODO_LIST line 28) — do this immediately after go-live.
4. **NEW** — Verify Hetzner StorageBox SSH: after the owner supplies host/user, pin the host key via `ssh-keyscan -p 23` into sops `borg_known_hosts`, then a one-shot `borg list` probe before enabling the timer.
5. **NEW** — Borg repo size projection: compute expected archive size given the excludes (the docs list ~152 MB git-history diet, pool receives excluded, /data model trees excluded) against the BX11 1 TB quota — catches a "first seed exceeds the box" surprise before it happens at 06:30.
6. **NEW** — borg cache dir health: after first real run, confirm `/mnt/hot/borg` cache sizing and that `borg-offsite-dir`'s nofail degradation ("crush-hot-db precedent") behaves on a `/mnt/hot`-absent boot.
7. **NEW** — offsite-borg in `system-health.extraMonitoredServices`? Currently freshness rides backup-coordination only; a failed-unit signal (OnFailure → Discord exists, but systemd-state metric) should be checked for parity with other backup units once enabled.

**Noticed in passing (from AGENTS.md/queue context — verify before harvesting, several are already tracked):**
8. llama-rag mid-load CPU-spin regression — root cause suspected in ROCm runtime/kernel/GPU-state, NOT the llama.cpp version; unit-context soak required before any re-enable (escape condition already fired 2026-09-18). Highest-stability item on the box.
9. flm v1.0.2 corpse pinning :52626 — the owed reboot clears it; run `nix run .#pre-reboot-check` first (boot-mirror §11 now grades the mirror).
10. Crash-recovery readers (`crush-hot-db-migrate`, `discordsync-db-heal`) belong in the guard's `ioChurnUnits` (freeze-6 finding) — check whether landed; if not, small high-value fix.
11. Restic app-dumps (`modules/nixos/services/restic-app-dumps.nix`, deploy-pending) — verify first run + freshness row after next deploy.
12. DiscordSync attachments pool migration (deploy-pending) — one-time `discordsync-attachments-migrate` runs via the dedicated no-block block; verify post-deploy.
13. Buildcache 2-device btrfs merge (decided, awaiting window) — `scripts/buildcache-btrfs-convert.sh` + module flip must land in the SAME window.
14. Hot-db Phase-2 fold: crush-hot-db interim mechanism vs `services.hot-db` — do not run both; fold is a tracked queue item.
15. Samsung boot-mirror: confirm first `Linux Boot Manager (Samsung)`-first boot + pre-reboot-check §11 grading live.
16. Mail relay: confirm "Verified" in Resend dashboard, then the non-owner delivery probe (paperless share / forgejo notification) — the last user-gated go-live step.
17. Pocket ID: re-click "Send test email" post SMTP fix (2026-09-17) if not yet confirmed.
18. CV branch-ref governed hold: probe `nix build github:LarsArtmann/CV/master#default.goModules` periodically for the go_1_27 upstream fix.
19. Browser-history UPSTREAM REGRESSION HOLD (2026-09-17, `f3561fd8` SQLITE_READONLY) — re-check upstream for the checkpoint-write fix, then re-lift the lock hold.
20. Forgejo transferred-mirror decision (DarkBlocks, DialogesWebInterface → Artmann-Minecraft org): delete or re-mirror into a forgejo org — owner decision.
21. Forgejo `/var/lib/forgejo` size measurement post first mass migration (root-snapshot growth projection) — needs forgejo user.
22. Forgejo golangci-lint + DynamicMinecraftNetwork `commit-graph.lock` cleanup (needs forgejo user).
23. GeoMetrikks go-live: paste MaxMind/CARTO keys into sops (user-gated); verify UI post first deploy.
24. Health-hub deploy-pending — first remote (CV) federation; verify build rev stamp matches lock post-deploy.
25. `buildcache-init` should provision the three new fallback-cache targets (pnpm/pnpm-state/cargo-registry) — post-recovery symlinks dangle until next deploy; queued.
26. Single-source the four-hand-kept buildcache fallback reap surfaces (deploy.sh ×2 loops, recovery step 2.5, HM activation block) — queued.
27. btrbk /data EIO inode repair (docs/todo/storage.md P0) — the data backup leg has been failing since August; the corruption repair is the blocker.
28. ClickHouse backup coverage (btrbk excludes it; clickhouse-backup follow-up in docs/todo/storage.md).
29. `/data` repair decision affects btrbk-data; after repair, confirm the first full data send lands pool-side.
30. Immich cross-archive key paste (discordsync-immich.yaml PLACEHOLDER → real key, user-gated).
31. niri freeze-4/5 structural fix: route repo-tree scans through `heavy-job` (workload-admission) — audit for remaining raw scan entry points.
32. PMA commit-blackout guard: confirm the vendored go-commit rev ≥ `22f0e4c` + `OPENAI_BASE_URL` chain still intact after any PMA input bump (standing post-bump check).
33. Offsite Borg password recovery-copy policy decision (owner) — should gate go-live.
34. SignoZ traces wiring gaps: overview/papdashboard/hermes need real upstream instrumentation before coverage flips from `upstream` to `env` (tracked).
35. `email_state` fixture drift class: audit other upstream-fixture-derived monitors for fixture≠prod-truth patterns (the InboxClean lesson generalized).
36. Gatus coverage sweep: any service added since 2026-09-15 without a registry `checks` entry? (Every service must be monitored — silent failures unacceptable.)
37. Nix daemon stale-fetch cache: after the next multi-input bump wave, consider restarting nix-daemon if same-rev evals diverge (2026-09-02 machine finding).
38. sops-nix `buildGo125Module` alias shim (overlays/shared.nix): drop when sops-nix > `13616fff` lands — check upstream periodically.
39. playwright shim drop (django-polymorphic checkInputs strip + d2 browsers-chromium preset): drop when nixpkgs repairs playwright.
40. libdisplay-info shim: drop when niri-flake unpins `libdisplay-info_0_2`.
41. utoipa-swagger-ui overlay: drop when upstream fixes `fs::copy`.
42. SigNoz exemplar persistence: revisit drilldown when upstream ships exemplar storage (tracked, low priority).
43. Check whether new-metric loan entries in pre-deploy §10 have stale manual `KNOWN_NEW_METRICS` rows to retire (the gate auto-WARNs on these — read the last deploy log).
44. Hermes workspace layout decision: still deferred on `@`, revisit trigger in docs/todo/services.md.
45. Google Sync go-live checklist (OAuth runbook, sops scaffold, enable) — user-gated, dormant.
46. Old SQLite paperless export: recover-or-delete decision still pending (user).
47. `/tmp/.Trash-1000` rule adherence audit: confirm no recent `trash`-under-/tmp usage landed (the 35G incident class).
48. TOML/TODO hygiene: run `bash scripts/check-todo-system.sh` to confirm queue one-liners vs library entries haven't drifted (recent harvest waves make this cheap insurance).
49. Docs-health ANNOTATE pass on the 2026-09-22 11:31 self-review report if any of its premises changed after this verification run (they didn't — its claims match what I verified).
50. After go-live: add offsite-borg to the weekly ops checklist rotation (freshness check in "All Backups Healthy" covers it, but a manual monthly `borg list` sanity probe is cheap insurance against silent repo drift).

*(Items 1, 2, 4, 5, 6, 7 are NEW and agent-executable; the rest either exist in the queue/domain files or are owner-gated notes. HARVEST should apply the usual routing rigor — most of 8-50 are ROADMAP or already-tracked, only 1-2 + 4-7 merit fresh queue rows.)*

---

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Should the offsite-borg go-live block on the recovery-copy policy?** The passphrase is a real random sops value; if you lose both the host and the sops store, the repo is dead. Do you want the recovery copy (password manager / paper) recorded as a hard go-live prerequisite, or accept the risk and go live first?
2. **Queue re-dispatch policy:** this item was dispatched twice (implementation session + this verification session). Was that intentional (independent verification pass) or a queue bug I should note for the harness owner? If intentional, should the close-out message convention change so re-dispatches detect prior completion faster (e.g. the `[x]` marker already did — the dispatch snapshot was just stale)?
3. **Borg retention vs BX11 quota:** the job prunes 7d/4w/6m, but the first WAN seed is unbounded until the first prune. Do you want a `--dry-run` sizing estimate (item 5) before go-live, or is the 1 TB headroom comfortable enough to just let the first run happen?
