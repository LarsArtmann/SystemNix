# Task 000001a0c373465b2a59cd41196ec4d45014 — Paperless PG-level dump: comprehensive status report

**Date:** 2026-09-21 12:29 CEST
**Work item:** TODO_LIST.md → storage: "Paperless PG-level dump (or ratify exporter-only RPO) before the postgres hot-db wave"
**Commits:** `9546e7a1` (module + deploy.sh, daemon-committed mid-flight, amended-context in the footer commit's body) + `e9407432` (docs, carries the `Task-Queue-ID:` footer). Unpushed, tree clean, hooks green.

---

## a) FULLY DONE

1. **The pg_dump leg chosen and wired (the item's core ask).** `modules/nixos/services/paperless.nix` grew a nightly `pg_dump --format=custom` job, miniflux-backup pattern verbatim:
   - `paperless-db-backup.timer` — 02:00 + 10min jitter, `Persistent = true` (the paperless-exporter 2026-08-31 boot-catch-up lesson applied up front).
   - `paperless-db-backup.service` — oneshot, `User = postgres` (peer auth), `pg_dump --format=custom` of `cfg.settings.PAPERLESS_DBNAME or "paperless"`, 14d retention (`find -mtime +14 -delete`), `ioTier.background`, `RequiresMountsFor` on the pool leaf (detached DAS = clean dependency failure, never 226/NAMESPACE), house `startLimitBurst 5/300s` + `serviceOneshotDefaults`.
   - `paperless-db-backup-dir` — mount-gated cv-backup-dir-pattern oneshot (`ReadWritePaths` on the MOUNT ROOT, `CAP_CHOWN CAP_FOWNER CAP_DAC_OVERRIDE`) creating `/mnt/pool/backups/paperless` owned `postgres:postgres` — the 226-class and the silent-DAC no-op class both pre-empted.
2. **Deploy wiring.** `paperless-db-backup-dir` added to `scripts/deploy.sh`'s provisioner restart loop — `deploy-restart-audit.nix` caught the omission on the first `nix flake check` exactly as designed; fixed before any deploy attempt.
3. **Monitoring.** New `paperless-db` integration-registry entry fans the dump into backup-coordination (`directory` + `filePattern paperless-db-*.dump` + `maxAgeHours 25`) — separate from the existing `paperless` exporter-manifest entry, so the two DR surfaces alert independently.
4. **Queue closure.** TODO_LIST queue row pruned (house rule: done rows never persist); `docs/todo/storage.md` entry marked `[x] ~~…~~ DONE` with the decision + wiring summary and the original Source pointer.
5. **Docs.** CHANGELOG `[Unreleased]/Added` entry (DEPLOY REQUIRED) + a one-line AGENTS.md Paperless-section bullet so the next session sees the DR shape without digging.
6. **Verification.** `nix flake check --no-build` green end-to-end (including the deploy-restart-audit trip); pre-commit hooks on the amend re-ran gitleaks + flake check green. One eval error caught and fixed mid-flight (`cfg.database.name` — the option does not exist in this nixpkgs module; switched to the rendered `cfg.settings.PAPERLESS_DBNAME or "paperless"`).

## b) PARTIALLY DONE

7. **The commit topology is split.** The daemon auto-committed paperless.nix + deploy.sh as `9546e7a1` (heuristic message) while the footer commit `e9407432` carries only the docs. The footer commit's body cross-references `9546e7a1`, but a queue/tooling scan keying ONLY on the footer commit's diff would see docs-without-code. A `--soft` fold of both into one commit was the clean shape; the absolute never-`git reset` rule (not even `--soft`) made me amend-forward instead. Acceptable, worth a house-rule carve-out someday.
8. **Runtime verification is inherently undeployed.** Everything is eval+fixture-level green; the actual first dump, dir-oneshot convergence, and backup-coordination freshness flip can only be proven by the next deploy (`nix run .#deploy`, DEPLOY REQUIRED) plus one timer tick. I did not deploy — no permission signal in the contract and deploys are shared-box events.
9. **AGENTS.md coverage is one sentence**, not a full subsection — deliberately minimal per the no-scope-creep contract; the module comments + CHANGELOG carry the detail.

## c) NOT STARTED

10. **Deploy** (owner/queue-gated): first activation, provisioner restart of the dir oneshot, first 02:00 dump.
11. **Restore drill**: a `pg_restore` proof that the custom-format dump actually restores into a scratch cluster (mirrors the queued twenty/manifest drill; nothing analogous exists for paperless).
12. **VM-test regression**: `tests/test-paperless.nix` asserts the exporter; no step asserts the db-backup units (dir-oneshot creates the leaf, dumper writes a dump, backup-coordination entry renders). Immich/miniflux VM coverage precedent exists for cv-backup (test-cv steps 9-10) — paperless-db has none.

## d) TOTALLY FUCKED UP (honest list)

13. **One real edit bug, caught by me, not by tooling-first**: while adding the `paperless-db` registry entry I mangled the adjacent `oidc = {` line (my old_string ate the block opener), leaving a syntactically-valid-but-broken attrset. `nix flake check` caught it (`attribute 'name' missing` — misleading error, the real cause was the eaten line). Self-inflicted, fixed in the next edit, zero collateral — but the failure mode was sloppy anchor discipline on an edit I'd planned as an insertion, not a replacement.
14. **`nix fmt --no-update-lock-file -- --ci` is NOT dry-run on this tree**: despite the AGENTS bullet treating `--ci` as "checks formatting", it REFORMATTED three foreign files (the two never-format multi-MB HTML bundles — the exact 2026-09-20 inflation class — plus a parallel session's `scripts/fsync-bench.sh`). I caught it from the error output and `git restore`d all three before commit, so nothing landed — but I ran a tree-wide formatter knowing the parallel-session rule and the never-format-bundles doctrine. Should have scoped the check to my files only (or skipped it: pre-commit already validates staged nix files).
15. **First `nix flake check` attempt consumed the known-good order**: I ran the check before adding the deploy.sh provisioner entry, so the run died on `deploy-restart-audit` instead of batching all findings. The Critical-Rule "--keep-going FIRST to enumerate ALL root failures" exists for exactly this; I found the audit error only because it was the sole failure.

## e) WHAT WE SHOULD IMPROVE

16. **Fold `git reset --soft` allowance for daemon-commit folding** — the amend-only workaround produces split commit topologies on every long-running session (see §7).
17. **Make `--ci` formatter runs scoped-by-default** (path args), or wrap them, so agents stop re-learning the tree-wide-reformat footgun (§14).
18. **Paperless DB-name should be derivable from a single source** — `cfg.settings.PAPERLESS_DBNAME or "paperless"` duplicates the nixpkgs default; if the nixpkgs module ever exposes `database.name`, switch to it.
19. **Backup-coordination could support two files-entries per service natively** — today two surfaces require a second synthetic registry entry (`paperless-db`); a `backups = [ … ]` list option would remove the synthetic entry.
20. **The 02:00 slot** sits 30 min after the exporter's 01:30; both write the pool — fine on HDD RAID1, but if the pool ever hosts a contention-sensitive job, revisit staggering.

## f) Up to 50 next things (queue-ready, no ordering implied)

Paperless-adjacent (highest value first):
1. Deploy the generation carrying the dump; verify the dir oneshot converged + first dump landed + backup-coordination green (DEPLOY REQUIRED follow-through).
2. `pg_restore` drill: restore the first nightly dump into a scratch cluster and assert document counts (prove the DR path, not just the artifact).
3. VM-test step for `paperless-db-backup` in `tests/test-paperless.nix` (test-cv steps 9-10 pattern).
4. Add a post-deploy smoke entry: dump file exists + newer than 25h (or the collector check owns it — pick one, avoid double coverage).
5. Sweep other createLocally PG consumers for the same gap (twenty? manifest? — the queued twenty/manifest pg_dump restore drill item already exists; extend it to enumerate ALL PG DBs vs those with pg_dump jobs, as a one-shot audit script).
6. Extend the postgres hot-db wave runbook to reference the paperless dump as the DR path (docs/status 2026-09-21 §e cross-link).
7. Consider `pg_dump --jobs` / checksum verification (dump integrity is currently trust-the-exit-code).
8. paperless-exporter `RandomizedDelaySec` (already open inside "Pool + disk-domain quality" (8)) — now interacts with the 02:00 slot, worth closing together.

Noticed-in-passing (from this task's context only):
9. `bank-sync-stub` `getExe` deprecation warning fires on every flake check (set `meta.mainProgram`).
10. `scripts/fsync-bench.sh` (parallel session) currently fails the tree-wide formatter — owner will trip on it too.
11. The two disk-layout HTML bundles still fail `--ci` formatting by design — consider a treefmt ignore entry so agents stop nearly-destroying them.
12. TODO_LIST still carries "Browser-history DB backup" unchecked — same gap shape as this task; likely the next identical miniflux-pattern clone.
13. The `paperless-db` synthetic registry entry has `vHost.layer = "none"` + no subdomain — confirm dns-local assertion doesn't complain when the exporter entry and this one share the service (it didn't here; noting for the two-entries-per-service pattern's next user).

(Stopped at 13: the remaining backlog belongs to other domains' queues; padding to 50 would be research, not reporting.)

## g) Questions I cannot answer myself

1. **Deploy window**: should I (or the next queue dispatch) run `nix run .#deploy` for this generation, or does it ride the next multi-feature deploy? The dump is inert until activated.
2. **Restore-drill scope**: do you want paperless's `pg_restore` proof as a one-time manual drill (runbook entry) or an automated periodic check (dump → scratch restore in a VM test)?
3. **Commit-folding rule**: is a `--soft`-fold exception for folding daemon heuristic commits into a footer commit acceptable, or should the footer commit always ride the LAST commit with cross-references in the body (what I did)?
