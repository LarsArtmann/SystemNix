# Task 000001a09d42534bad3f7090259ce94cd5b9 — Reviewer-fix status report (cmdguard sweep omission)

**Date:** 2026-09-14 03:08 CEST
**Parent work:** commit `1483bde0` (InvokeNamed sweep, task `000001a09d06…`)
**Fix commit:** `449ef806` (this repo's HEAD at report time)
**Scope:** exactly one reviewer finding (low severity), docs-only. No code changes.

---

## What was asked and what was done

The reviewer found the sweep report (`docs/status/archived/2026-09-14_00-00_task-000001a09d06ce7945994b3bef63af3ff419.md`) claimed all 16 repos with `do.InvokeNamed`/`do.MustInvokeNamed` call sites were swept, but a 17th existed: `cmdguard` (`pkg/cmdguard/v4/scope.go:194` — `do.InvokeNamed[T]` inside its own generic wrapper, paired with its own `do.ProvideNamed[T]` in the same file, same `T` on both sides → clean passthrough, not a trap).

Done:

- Verified the finding independently before editing (grep at the exact file:line; confirmed cmdguard is a real local checkout under `~/projects`).
- Added the cmdguard row to the sweep table with the clean-verdict rationale.
- Corrected the heading count to "all 17 CLEAN".
- Conclusion unchanged: ZERO live instances of the samber/do `InvokeNamed[interface]` trap.
- Committed with the required `Task-Queue-ID: 000001a09d42534bad3f7090259ce94cd5b9` footer. Not pushed.

## a) FULLY DONE

1. The reviewer finding itself — cmdguard added, count corrected, verdict justified (commit `449ef806`).
2. Independent verification of the finding's premise before acting (read the actual call site; did not paste the reviewer's claim into the report unverified).
3. TODO_LIST loop closure: parent task already closed by the original sweep; this fix required no TODO_LIST edit (finding was reviewer-initiated, not a queue item).
4. Queue cross-reference: both the original work commit and this fix carry the correct Task-Queue-ID footers.
5. No scope creep: zero unrelated file changes; `git status` clean after commit.

## b) PARTIALLY DONE

1. **Hook-leg coverage:** the pre-commit hook's `nix flake check` leg ran and failed on the KNOWN pre-existing red `checks.x86_64-linux.cv` (documented in AGENTS.md 2026-09-13: cv.nix sets `oidc.enabled = true` with empty client_secret by design; the VM fixture seeds no `CV_OIDC_CLIENT_SECRET`). While that red stands, the hook's flake-check leg exercises nothing — a docs commit carrying an accidental `.nix` change would land unevaluated. I followed the documented `--no-verify` precedent (with the reason in the commit message), but the fixture itself remains unfixed (tracked in TODO_LIST, not this task).
2. **The report's "Method" section** still says the sweep was driven by grepping every Go file under `~/projects` — accurate, but it does not enumerate HOW the repo list was bounded (e.g. `.git` presence, LarsArtmann ownership check). The cmdguard miss likely came from that boundary being implicit. Improving the method description was arguably in scope for a "completeness/honesty fix"; I did the minimal table+count fix only.

## c) NOT STARTED (out of scope by instruction, listed for completeness)

1. Fixing the cv VM-test fixture (the red that blocks every pre-commit flake check).
2. A repo-generic CI analyzer for `InvokeNamed[interface]` pairs (the report's own "future hardening" note).
3. Any cmdguard-side guard comment/CI analyzer (upstream repo; the passthrough is clean, so nothing to fix — but the branching-flow `doanalyzerv2` precedent could be adopted there).

## d) TOTALLY FUCKED UP (own mistakes, ranked)

1. **The auto-commit daemon raced me and won.** I edited the report, then ran `git add` + `git commit` — by which time the daemon had already committed my half-finished edit as `chore: auto-commit 1 changed file(s) (heuristic)` with a meaningless message. AGENTS.md warns about exactly this ("the daemon races explicit commits; re-check `git status --short` immediately before `git add`"). Recovery was an `--amend` into the proper message — but the tree was briefly in a state where my work existed only under a junk commit subject. Check `git status` BEFORE editing next time, and commit immediately after the edit in the same breath.
2. **Two wasted full hook runs (~5 min each).** I first attempted a plain commit, watched the hook run gitleaks + the ENTIRE `nix flake check` (all packages, all modules, three VM test builds) fail on the known cv red, then did the same dance again on the `--amend`. The cv red was documented in AGENTS.md the whole time — I should have gone straight to `--no-verify` for a docs-only commit and said so up front.
3. **A failed multiedit on the first pass.** My second edit's `old_string` used `||` instead of the actual `|` table-cell prefix, so 1 of 2 edits silently failed. Caught immediately (the tool reported it), but a careful read of the file before composing edits would have made it zero passes.
4. Minor: my first `multiedit` also embedded the `||` typo in the NEW row content, meaning the tool would have written a broken table row had the edit matched. Sloppy string assembly under haste.

## e) WHAT WE SHOULD IMPROVE (process-level)

1. **AGENTS.md pre-commit red list should be checked FIRST** for any commit, not after a failed hook run — the known-red list (currently: `checks.x86_64-linux.cv`) turns a 10-minute hook cycle into a decision that takes 10 seconds.
2. **Daemon-race discipline:** commit small docs fixes immediately after the edit, not after composing a longer message; or stage+pathspec-commit in one command to shrink the race window.
3. **The sweep report's method section should encode the repo-listing boundary** (how "LarsArtmann Go repo under ~/projects" was decided) so a future sweep cannot repeat the same omission class.
4. The reviewer found cmdguard via grep over `~/projects`; the original sweep used the same tool and missed it — worth a one-line look at WHERE the two greps diverged (likely a trailing-newline/module-marker filter) before the next sweep trusts either.

## f) Up to 50 next items (concrete, mostly small)

Sweep-adjacent (this task's domain):
1. Fix the cv VM-test fixture (seed `CV_OIDC_CLIENT_SECRET` or neuter the OIDC gate in the test) so `checks.x86_64-linux.cv` goes green and the pre-commit flake-check leg becomes meaningful again.
2. Diff the original sweep's grep command against the reviewer's to find why cmdguard was missed; record the corrected method in the sweep report.
3. Add cmdguard to the sweep report's residual-risk mitigation note if it adopts branching-flow's `doanalyzerv2`.
4. Stand up the repo-generic CI analyzer for `InvokeNamed[interface]` pairs across LarsArtmann Go repos (the report's own "possible future hardening").
5. Add a regression test to cmdguard upstream pinning the same-T passthrough (so a future refactor to interface-T fails CI loudly).
6. Re-run the full sweep with the corrected method and re-verify the count 17 (one grep, minutes).

Repo hygiene observed in passing:
7. `git branch --unset-upstream` — the working repo reports "upstream is gone"; stale origin config.
8. The auto-commit daemon's "heuristic" message class produced junk history again (this task); consider a daemon-side pathspec/dedup guard or a min-message-quality gate.
9. Two half-built VM-test artifacts from the aborted hook runs remain in the store; harmless, but `nix store gc` will reclaim them at the next window.

(Items 1–2 are the high-leverage ones; 3–6 are cheap; the rest is hygiene. Stopped at the items genuinely noticed during THIS task — no speculative backlog padding.)

## g) Questions I cannot answer myself

1. Should the sweep report's repo-listing method be normative (written into CONTRIBUTING/AGENTS as a checklist) so future queue sweeps inherit the corrected boundary — or is a one-off report amendment enough?
2. Is fixing the cv VM-test fixture acceptable as a drive-by in a future task's commit (it unblocks every pre-commit flake check repo-wide), or does it need its own queue item with upstream coordination (the fixture lives next to cv.nix's deliberate empty-secret design)?
3. For the auto-commit daemon race: is amending the daemon's heuristic commit (what I did) acceptable to you as standing practice, or would you rather I leave the junk commit and stack my proper commit on top to preserve history exactly?

---

**Bottom line:** the finding is fixed exactly as specified, minimally, with the conclusion (zero live traps) unchanged. The work is small; the honest assessment is that the execution cost ~3x what it should have (hook runs + daemon race), and the two lessons (check the known-red list first; beat the daemon to the commit) are the transferable ones.
