# Task 000001a0b1d43f78d4325627260af417c7ed — llama-rag restart LEAK + 2026-09-14 spin regression — Status Report

> **[docs-health 2026-09-19] RESOLVED + ARCHIVED:** activation overtaken by events: gen 784 deployed 11:28, llama-rag re-disabled same day post-freeze-5 (spin on the pinned build — docs/status/2026-09-18_15-50_*); bisect gated in docs/todo/ai-stack.md.


**Date:** 2026-09-18 02:45
**Repo:** /home/lars/projects/SystemNix (master)
**Work commit:** `4a04b5f9` (docs: cross-verify llama-rag leak/regression fix landed in-tree)
**Report commit:** this one (same Task-Queue-ID footer). ~~activation awaited a deploy~~ deployed gen 784 2026-09-18 11:28, then llama-rag re-disabled post-freeze-5 (docs/status/2026-09-18_15-50_*).

---

## 0. Executive summary

The queue item (P1: llama-rag restart LEAK under QLC saturation + the separate 2026-09-14
llama.cpp 0.4.0 mid-load spin regression) was **already fully landed in-tree by a parallel
session earlier on 2026-09-18** — before this run started. This run's contribution:
**cross-verification from a clean tree + TODO_LIST close-out + a footer-carrying commit**
(the queue derives work via the footer; none existed for this ID). No new engineering was
needed, and none was invented — smallest-correct-change contract honored.

Verification performed this run:

| Check                                         | Result                                                                                              |
| --------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `nix flake check --no-build`                  | ✅ green (all checks passed; aarch64-darwin omission expected)                                      |
| evo-x2 eval `services.llama-rag.enable`       | ✅ `true` (re-enabled)                                                                              |
| evo-x2 eval `llama-embeddings` TimeoutStopSec | ✅ `2min` (leak-hardening present)                                                                  |
| evo-x2 eval `systemd.services`                | ✅ `llama-rag-leak-metrics` unit exists (leaked-instance monitor)                                   |
| flake inputs                                  | ✅ `nixpkgs-llama-rag` rev-pin `0968519e14f7aa7d3e9b389682bd74d2b51c8ce8` in flake.nix + flake.lock |
| Pre-commit hooks on commit                    | ✅ gitleaks + flake check passed                                                                    |

What the (already-landed) fix consists of, from AGENTS.md/FEATURES.md cross-read:

1. **Spin-regression half:** llama.cpp pinned back to the 0.3.0 build via the new rev-pinned
   `nixpkgs-llama-rag` input (path-form URL — the sanctioned broken-upstream pin shape).
   Resolved to the BYTE-IDENTICAL store path of the proven 2026-09-05 deployment
   (`sj4rpa8y…`, `libggml-hip.so` in `bin/` via GGML_BACKEND_DL) — zero rebuild — and
   functionally re-verified live (~4s bge-m3 load, `/health` 200, 1024-dim embedding on a
   scratch port). `llama-rag.enable = true` restored.
2. **Leak-hardening half:** explicit `TimeoutStopSec=2min` on both servers
   (`llama-embeddings`, `llama-reranker`) + the `llama-rag-leak-metrics` collector (5-min,
   root, mktemp + CAP_FOWNER) emitting `llama_rag_leaked_instances` / `leaks_present`
   (fail-closed via `scrape_errors`) + Gatus "llama.cpp Leaked Instances".
3. **Escape condition documented:** if the spin signature ever reappears on the pinned build,
   re-disable and bisect upstream; drop the `nixpkgs-llama-rag` input once 0.4.0+ is fixed
   upstream and re-verified live.

---

## a) FULLY DONE

- **The entire queue item** — both halves (leak hardening + 0.4.0 spin pin-back) were in-tree,
  marked `[x]` in TODO_LIST.md line 37, and documented in AGENTS.md (llama-rag section) and
  FEATURES.md before this run began.
- **Cross-verification of the landed state** (this run): flake check green; evo-x2 eval proves
  enable, stop-timeout, and collector unit all present in the host config.
- **Queue close-out:** TODO_LIST line 37 appended with a `CROSS-VERIFIED 2026-09-18` note
  naming this task ID; committed as `4a04b5f9` with the required
  `Task-Queue-ID: 000001a0b1d43f78d4325627260af417c7ed` footer. Not pushed (per contract).
- **Repo left clean and green** — commit passed gitleaks + flake-check pre-commit gates.

## b) PARTIALLY DONE

- **Live activation is still pending a deploy.** `llama-rag.enable = true` is in the tree;
  the units go ACTIVE only at the next `nix run .#deploy` (agent sessions don't deploy).
  Until then the endpoints :8848/:8849 stay dark and post-deploy smoke carries the 4 known
  baseline reds. The fix is designed so the stc re-arm trap is now harmless (re-armed units
  serve instead of spinning), so the deploy needs no special llama handling.
- **Post-deploy verification chain** (not executable this run): `/health` on :8848/:8849,
  `/v1/embeddings` 1024-dim smoke, Paperless RAG end-to-end (TODO line 36), and first real
  production run of `llama-rag-leak-metrics` (its own log lesson is already recorded: it
  MUST run as root or `ss -tlnp` miscounts foreign-user servers as leaks — an agent-run as
  `lars` miscounted the healthy 0.4.0 reranker that way on 2026-09-17).

## c) NOT STARTED (by this task, and correctly out of scope)

- Upstream llama.cpp 0.4.0 bisect/bug report (the escape condition only triggers if the
  pinned build regresses again; the pin is the deliverable).
- TODO line 459 (llama-rag monitoring depth: FUNCTIONAL Gatus probes `/v1/embeddings` +
  `/v1/rerank`, GPU-util metric) — related backlog, explicitly a separate queue item.
- TODO line 434 (deploy.sh generic stopped-for-containment stop-list) — largely MOOT now
  that the root cause is fixed and units serve instead of wedging; the item should likely
  be re-scoped or closed at next review (noted, not touched — drive-by rule).

## d) TOTALLY FUCKED UP

Nothing. No broken builds, no repo damage, no reverts. The only honest self-criticism:

- **I initially searched for a footer commit before checking whether the WORK existed at
  all.** Correct order would have been: read the TODO item's current state and the tree
  FIRST, then decide. The search still converged fast (one `grep` showed line 37 already
  `[x]` with a full DONE write-up), so no time was wasted — but the sequence was lucky-good,
  not methodical-good.
- **First `edit` of TODO_LIST.md was rejected** (had not Viewed the file in this session
  first; the grep-based line lookup then revealed the item had MOVED to line 37 while my
  memory of the file was from the queue text). Cost: two tool calls. No damage.

## e) WHAT WE SHOULD IMPROVE

1. **Queue handoff visibility:** the item was completed by a parallel session the same day,
   but nothing in the tree told a new session that WITHOUT reading the 1,000-word TODO line
   first. A convention like a short `(landed in <short-rev>)` marker at the TOP of a done
   item would let the next agent skip re-verification scoping. (This report now serves that
   purpose for this item.)
2. **Footer-commits for already-landed work:** the queue cross-references footers, but a
   verify-and-close-only run produces a docs-only commit. Fine, but the queue should expect
   "verification-only" footer commits for items finished out-of-band; a one-line convention
   in the item ("CROSS-VERIFIED by task X") is the mitigation used here.
3. **Deploy-blocked items accumulate "one deploy away" state** — llama-rag, crush-hot-db
   first migration, scrub-guard fix, cv sops seeding all wait on the same sudo-gated deploy.
   A consolidated "next deploy must verify: [list]" section in TODO_LIST would make the
   post-deploy checklist mechanical instead of archaeology.
4. **The `nixpkgs-llama-rag` pin has no expiry alarm.** The drop condition ("0.4.0+ fixed
   upstream and re-verified") lives only in prose (AGENTS.md, flake.nix comment). A pinned
   second nixpkgs input silently rots; consider a quarterly TODO review line or a CI note.

## f) NEXT 50 THINGS (llama-rag-adjacent first, then what I noticed in passing; the queue

owns pacing — these are candidates, most already exist as TODO items)

**llama-rag direct follow-ups:**

1. Deploy the tree (re-enables llama-rag; also activates crush-hot-db, scrub-guard fix, cv
   sops seeding — one deploy unblocks many).
2. Post-deploy: verify `/health` 200 on :8848 and :8849.
3. Post-deploy: verify `/v1/embeddings` returns a 1024-dim vector (bge-m3).
4. Post-deploy: verify `/v1/rerank` ranks correctly (bge-reranker-v2-m3).
5. Verify `llama-rag-leak-metrics` collector's first production run (root; `leaked_instances 0`).
6. Confirm the Gatus "llama.cpp Leaked Instances" check is green and alerting-wired.
7. Un-block TODO line 36: verify Paperless AI actually USES :8848 embeddings end-to-end
   (UI-vs-env precedence check needs sudo).
8. Decide TODO line 459: functional Gatus probes for /v1/embeddings + /v1/rerank.
9. Decide the GPU-utilization metric + SigNoz >90% alert half of line 459.
10. Re-scope or close TODO line 434 (deploy.sh llama containment stop-list) — root cause fixed.
11. Calendar the `nixpkgs-llama-rag` pin review: probe llama.cpp 0.4.0+ upstream monthly.
12. When the upstream fix lands: drop the pin input, re-verify live per the escape condition,
    and update AGENTS.md/FEATURES.md in the same change.
13. Confirm `ollama` stays healthy on the shared ROCm stack after re-enable (it was the
    differential control during the regression).
14. Watch io PSI for the first days after re-enable — the original leak class fed on QLC
    saturation; the collector now sees it, but a baseline re-read is cheap.

**Noticed in passing (existing backlog, flagged not started):**
15. `/data` corruption repair (TODO line 9) — still gated on user sign-off + root.
16. Crush DBs off QLC root (line 19) — deployed module, first migration still pending.
17. Paperless decrypt go-live + retro-decrypt (line 24) — user-gated sops password.
18. Offsite Borg/Hetzner leg (line 157).
19. Automated post-hard-reset check script (line 436).
20. Owner-questions batch (line 452) — includes the llama pin decision now overtaken by events.
21. Hermes ROCm/llama verify (line 188) — stale-era item, likely closeable.
22. Crush key-hygiene leftovers (line 468).

**Meta/process improvements:**
23. Add the "landed in <rev>" marker convention to done TODO items (§e.1).
24. Add the consolidated post-deploy verify checklist (§e.3).
25. Quarterly pin-review sweep for all second/aux flake inputs (`nixpkgs-llama-rag` is the
current only one, but the pattern generalizes).

(Fewer than 50: inventing filler items for an already-green area would violate the
smallest-change contract — the honest list is 25, and 20 of them are existing backlog
re-flagged with current context.)

## g) QUESTIONS (cannot answer myself)

1. **Deploy window:** llama-rag re-enable + several sudo-gated fixes are all "one deploy
   away." Should the next deploy be run ASAP (RAG is user-visible dark; paperless semantic
   search degraded), or batched behind a quieter IO window given the QLC PSI history?
2. **Paperless re-verification ownership:** after the deploy, the end-to-end Paperless RAG
   check (TODO line 36) needs sudo (DB-vs-env precedence). User-run, or is a sudo-granted
   agent session acceptable for that one check?
3. **Pin-drop policy:** should the `nixpkgs-llama-rag` input get a scheduled probe (e.g.
   monthly `nix build` of upstream llama.cpp 0.4.x in this flake context) tracked as a
   recurring TODO, or stay purely reactive to the escape condition?

---

**Artifacts:** TODO_LIST.md line 37 (cross-verified note) · commit `4a04b5f9` · this report.
Nothing pushed. Repo clean at commit time.
