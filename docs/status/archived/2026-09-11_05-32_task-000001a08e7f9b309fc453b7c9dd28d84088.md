# Status Report — Task-Queue Item 000001a08e7f9b309fc453b7c9dd28d84088

**Date:** 2026-09-11 05:32 CEST
**Scope:** P0 TODO item "BTRFS root chunk-unalloc — gate CLEARED 2026-09-02" (T14: emergency-reserve snapshot pinning)
**Commit:** `a84f8701` (`docs: document emergency-reserve snapshot pinning, close T14 TODO`)
**Report format:** Markdown — explicit user instruction wins over the skill's HTML default (flagged per skill contract).

---

## Work Item (verbatim from TODO_LIST.md)

The reserve file's extents are snapshot-pinned — either a periodic rewrite timer or a
documented pinning caveat in AGENTS.md (plan T14). Re-check unalloc >10G stays the safety
gate before any space-hungry op.

---

## a) FULLY DONE

1. **T14 decision made and documented** — chose the "documented pinning caveat" option over
   the "periodic rewrite timer" option. Rationale recorded in AGENTS.md itself: a rewrite
   timer is pure CoW churn (10 GiB rewritten per cycle) for near-zero benefit while
   chunk-unalloc (~222 GiB as of 2026-09-02) is the real headroom; the reserve is the
   LAST-ditch margin, not the first.
2. **AGENTS.md updated** — BTRFS "Emergency reserve" bullet (line ~553) now carries the
   caveat: extents are pinned by daily btrbk root snapshots; `rm` frees the 10 GiB only as
   pinning snapshots expire (3d+1w retention), not instantly; "instant" holds only when the
   newest snapshots don't yet reference the file's extents (true at cold provision, weaker
   at steady state). The `unalloc >10G` re-check is explicitly retained as the safety gate
   before any space-hungry op.
3. **TODO_LIST.md P0 item closed** — flipped to `[x]` with a DONE-2026-09-11 note pointing
   at the AGENTS.md caveat and recording the timer-rejection rationale (file's own
   conventions followed: keep history in the line, no deletion).
4. **Verification ran clean** — `nix fmt --no-update-lock-file -- --ci AGENTS.md TODO_LIST.md`
   (0 changed, no lock churn); `nix flake check --no-build` → `all checks passed!`;
   pre-commit hooks green on the amended commit (gitleaks, flake check).
5. **Commit hygiene** — the auto-commit daemon batched my two files first (`4b77eb3a`);
   I amended it into a proper message (`a84f8701`) with the required
   `Task-Queue-ID: 000001a08e7f9b309fc453b7c9dd28d84088` footer. Working tree left clean.
   Not pushed (per contract).

## b) PARTIALLY DONE

Nothing within the item's scope — it was a two-file documentation closure and both halves
(AGENTS.md caveat + TODO close) landed. The parent /data corruption repair item (T04-T08)
is untouched by design (separate task, different queue ID).

## c) NOT STARTED

- **No runtime verification of the live reserve state.** The caveat is now documented, but I
  did not SSH/`sudo`-probe the actual box (does `/btrfs-emergency-reserve` exist right now?
  what does `btrfs_emergency_reserve_present` report? what is CURRENT chunk-unalloc, vs the
  222.41 GiB figure from 2026-09-02 — nine days stale at write time?). The docs task did not
  require it; a live unalloc re-read would have made the AGENTS caveat cite a fresh number.
- **No Gatus/metrics cross-check** that the reserve-present alerting is currently green
  (passing observation only).
- **HARVEST into TODO_LIST/ROADMAP** of section (f) below — not done; this report is the
  input for a later docs-health HARVEST pass.

## d) TOTALLY FUCKED UP

Nothing. Small, well-scoped task; no code, no config, no live-system interaction — nothing
to break. Honest near-misses worth naming (they belong in (e), not here): the auto-commit
daemon raced me into the commit (my `git status` came back empty, briefly confusing), and
the first `edit` attempt failed on the read-before-edit guard — both self-corrected in one
round, zero cost beyond a tool call.

## e) WHAT WE SHOULD IMPROVE

1. **The 222.41 GiB "gate CLEARED" number is 9 days stale** — the TODO line and now the
   AGENTS caveat both cite it. A cheap periodic metric already exists conceptually
   (`btrfs-health-metrics` every 5 min); docs should cite the metric, not a frozen figure.
2. **T14's "either timer or caveat" was an A/B decision with no ADR-style trace.** I put the
   rationale inline in AGENTS.md (good enough here), but for future plan-T decisions, a
   one-line decision record in the status doc source would survive AGENTS.md pruning.
3. **Race with the auto-commit daemon is routine now** — it committed my work mid-task.
   The amend worked because the batch contained exactly my two files; on a busier tree the
   daemon batch could sweep other sessions' files into "my" commit and an amend would be
   wrong. A queue-work convention (commit fast after verification, before the daemon's
   next tick) would remove the gamble.
4. **Status-report skill defaults to HTML; queue contracts here ask for .md** — recurring
   divergence, flagged each time. Either the queue template or the skill default should
   yield permanently.

## f) Up to 50 things we should get done next

(Brainstorm, per HARVEST routing rules — most belong in TODO_LIST/ROADMAP, only the top
few are actionable-now. Ordered by impact.)

**From this task's direct blast radius (P0-adjacent):**
1. Re-verify live chunk-unalloc on evo-x2 (nine-day-old 222.41 GiB figure) and record the
   current value + date wherever it is cited.
2. Add `btrfs_emergency_reserve_{present,bytes}` + current unalloc to one existing Gatus
   check body-assertion (if not already asserted) so docs never need manual re-checks.
3. Delete T14 entirely from TODO_LIST (it is `[x]` but still carries the full old narrative —
   trim at next docs-health pass per file conventions).
4. Add a one-line pointer from AGENTS.md "Emergency reserve" to the T14 source status doc
   (`docs/status/2026-08-31_20-02_disk-review…md` §f.3/7) for the forensics trail.
5. Decide whether the auto-commit daemon should skip files with an open task-queue ID
   footer pattern (eliminate the mid-task commit race class).

**Neighboring P0 items observed in passing (untouched, by design):**
6. /data corruption repair T04-T08 (SMART gate (a) already PASSED in the 2026-09-11
   verification round noted in TODO_LIST; scrub-delta gate (b) still pending — the ONLY
   backup tier that has never completed pool-side).
7. Google Sync go-live or mark DORMANT (module built, ships disabled, AGENTS reads as-if-live).
8. Off-site backup 3rd-copy decision (Photos/Drive vs sdf vault rotation vs Hetzner+Borg).
9. Persistent nag: rotate leaked/stale API keys (Resend REVOKED → Pocket ID email broken;
   Context7 verified live; Synthetic assumed live).
10. Reboot into kernel 7.2.2 + FastFlowLM v1.0.3 retry (AI stack DOWN on NPU wedge since
    2026-09-03; ONLY a reboot recovers; also clears the :52626 zombie-socket corpse class).
11. fastflowlm socket re-arm churn: corpse-aware restore skip in memory-emergency-guard
    (P1 candidate already flagged in AGENTS).
12. mail-relay go-live tail: verify `larsartmann.cloud` in Resend (Domains → SPF/DKIM), then
    re-run the runbook test send; null-sender DSN rejection (550 Invalid from) stays accepted.
13. Pocket ID email: same new Resend key into `pocket-id.yaml` (shared with #12's key gen).
14. Bank-sync RFC3339 phantom-writer watch: after SCA approval, watch for new RFC3339
    statement_coverage values; goroutine dump if the writer returns.
15. Wise SCA re-approval runbook readiness (~every 90 days; next window post-2026-09-06 pause).
16. ClickHouse XFS: `clickhouse-backup` follow-up (telemetry has NO backup coverage).
17. ClickHouse READ-ONLY zombie log tables (`trace_log_14..18`, etc., ~10 GiB) need human
    `DROP TABLE` decisions.
18. immich/monitor365 stale uid 966:956 re-chown when monitor365 is re-enabled.
19. Monitor365 re-enable owner decision (publish crate / public repo / vendor).
20. inboxclean-decrypt go-live: paste the real Polish-bank PDF password via sops (placeholder
    is inert by design — the gap is visible via the `encrypted` tag until then).
21. Hermes GitHub read-only PAT go-live (`hermes-github-token.yaml` still placeholder).
22. CV `pipeline.evaluation.min_day_rate` owner value decision (upstream proposed 600).
23. flm v1.0.3 validation artifacts: file the XRT-2.25/kernel-7.2.0 upstream issue
    (verify-before-filing) after the reboot test.
24. CI darkness: `NIX_GITHUB_RO_TOKEN` fine-grained PAT secret must land for the 32
    private-repo `github:`-type lock nodes (120+ dark runs).
25. tq-agent-pool: flip flake input from interim `git+file?rev=` to
    `github:…?ref=master` once go-taskqueue pushes (CI cannot fetch git+file).
26. go-output upstream: cut v0.37.1 for the re-tagged-release lock-vs-proxy misalignment.
27. dnsblockd upstream health-cache fix (2026-09-06, implemented unpushed): push + tag +
    SystemNix flake bump.
28. bank-sync tolerant-read + V10 migration (2026-09-06, committed local, UNPUSHED): push +
    `nix flake lock --update-input bank-sync` + deploy.
29. templ-components: ensure all templ projects regenerate `*_templ.go` before commits
    (recurrent hook-failure class; consider a shared pre-commit helper).
30. DMS config split-brain: consider upstreaming a "declarative keys win" merge mode so
    runtime UI changes stop being silently ephemeral.
31. niri-session-manager upstream hazards (restore-on-every-start; no same-pid dedupe) —
    file issues with the 152-terminal storm evidence.
32. mm_stat-based zram alert recalibration: the 97%-fill-steady-state incident showed
    fill-% alone is meaningless without headroom context — Gatus check already combined-
    gated; audit for other single-gauge alerts with the same trap.
33. docs-health HARVEST pass on this report's section (f) + the two remaining `[ ]` P0s.
34. AGENTS.md size: the evo-x2 sections are enormous (crush/Hermes/InboxClean alone dwarf
    the flake architecture section) — split per-service runbooks fully into docs/services/
    and leave 3-line pointers, or fresh-session context cost keeps growing.
35. Rotate the `AKIAIOSFODNN7EXAMPLE`-class allowlist review (scanner allowlist audit,
    cheap annual hygiene).
36. Purge decision revisit trigger: user held the history push indefinitely; once Context7 +
    Resend + Synthetic keys are all rotated, re-surface the "history is inert, close the
    purge runbook" cleanup.
37. `sdf` WOOACME drive: still FROZEN by user decision; when touched, re-validate SMART and
    consider the vault-rotation role (#8).
38. hp printers/etc. not relevant — skip list intentionally ends with repo-hygiene items:
39. Add an eval-time guard for the `StartLimitBurst`-in-serviceConfig class (AGENTS says
    "no eval-time guard yet" — a known, cheap audit gap).
40. `scripts/audit-shell-nullglob.sh` and `audit-textfile-tmp.sh` cover .nix inline text;
    store-path scripts remain invisible — consider a runtime-journal tripwire for exit-127
    classes in oneshots.
41. Snapshot-pinning doctrine: the reserve got its caveat today; do a one-pass sweep for
    OTHER large intentionally-deletable files (flm weights re-pull staging, jan model tree)
    and document their pinning behavior before someone learns it during an ENOSPC.
42. btrbk retention on root receives is FOREVER — revisit at ~50% pool usage (trigger
    already documented; add the pool-% metric to a Gatus reminder so the trigger fires).
43. VM test flakiness budget: test-cv/test-pool-recovery now mount real disks; consider a
    CI timeout policy so a D-state-simulating test cannot wedge a run.
44. `git-filter-repo` purge runbook: keep the replacements file OUT of any committed path
    when (if) the purge decision flips — it currently lives only in AGENTS text, fine, but
    the runbook should note it must never be committed.
45. Sweeper for TODO_LIST items whose "Source" status docs are >60 days old with no
    annotation (staleness detector — cheap script).
46. Consider a `qmd collection add docs` so status reports become semantically searchable
    (qmd MCP is wired but the docs corpus was never onboarded).
47. `docs/CONTRIBUTING.md` — confirm the module template still matches the flake-parts
    wrapper shape (the bare-module silent-noop trap deserves a template-level guard).
48. Add the `email_state`-class fixture-vs-prod check to the test-writing checklist
    (mock-sops default path agreed with a prod bug once already).
49. Review whether `atticd-storage-dir`-pattern oneshots all got into deploy.sh's restart
    list (bank-sync lesson: stc never restarts inactive oneshots).
50. Celebrate: the reserve caveat now exists, T14 is closed, and the box has ~222 GiB of
    breathing room — nothing on fire from this task.

## g) Questions I can NOT figure out myself (up to 3)

1. **Is the live chunk-unalloc still comfortably above the 10G gate today (2026-09-11)?**
   The docs cite 222.41 GiB from 2026-09-02; I deliberately did not touch the live system
   for a docs task. If it has collapsed (heavy flm re-pull era, balance churn), the AGENTS
   caveat's "last-ditch margin" framing deserves an immediate re-check — one
   `sudo btrfs filesystem usage /` answers it.
2. **Do you want T14's full narrative line TRIMMED from TODO_LIST now** (per your convention
   of trimming closed items at the next docs-health pass), or left in place as history
   until then? I followed the mark-`[x]`-keep-narrative pattern; a global convention
   decision would stop me guessing per item.
3. **Should the auto-commit daemon learn to skip task-queue work commits** (footer pattern
   `Task-Queue-ID:`), or is fast-commit-before-daemon-tick an acceptable convention? This
   is a workflow-ownership call I cannot make for you; the race cost me one amend this time
   and could sweep foreign files into a queue commit on a busier tree.

---

*Point-in-time snapshot — generated 2026-09-11 05:32 CEST for queue task
000001a08e7f9b309fc453b7c9dd28d84088. Work commit: `a84f8701`.*

Task-Queue-ID: 000001a08e7f9b309fc453b7c9dd28d84088
