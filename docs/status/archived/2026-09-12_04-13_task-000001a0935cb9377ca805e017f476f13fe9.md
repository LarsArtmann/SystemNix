# Status Report — Task `000001a0935cb9377ca805e017f476f13fe9`

**InboxClean `main` OAuth re-consent — Gmail sync DEAD since 2026-09-04**

- **Written:** 2026-09-12 04:13 CEST, autonomous tq-pool agent session
- **Scope:** exactly this queue item + what was noticed in passing (per task contract). No unrelated research.
- **Work commit:** `8605c6bd` (TODO_LIST.md blocked-note + verified state); report commit: this one
- **Verdict: BLOCKED — human-only steps.** Diagnosis, live-state verification, and loop closure are DONE; the fix itself structurally cannot be executed by an agent session on this machine (Google Cloud Console flip + interactive Google browser login + `sudo -u inboxclean`).

---

## 0. Verified live state (2026-09-12 04:00–04:12 CEST)

| Signal | Value | Source |
| --- | --- | --- |
| `main` account | `auth_expired`; `invalid_grant` ("Token has been expired or revoked") every 30-min tick; unit exit 1 + OnFailure firing; `start_history_id=5152620` frozen since Sep 04 07:34 | `journalctl -u inboxclean-sync`, `/health` |
| `work` account | `connected`; cursor ADVANCING (→5173367 at Sep 12 04:03); `token-work.json` mtime Sep 12 04:07 (live refresh); **ZERO** `token_revoked` lines since Sep 04 | journal + `stat` |
| `token.json` (main) mtime | 2026-09-04 07:34:39 = last successful refresh before death | `stat` |
| Paperless `gmail` tag demote PATCH | FAILING since Sep 06 06:55 (80×), `[rejection:paperless.client_error]` — **under the CONNECTED `work` account** | journal |
| `/health` overall status | `"status": "ok"` while `main` has been dead 8 days | `fetch http://127.0.0.1:8099/health` |
| Journal noise floor | 105 `token_revoked` lines in the last ~2 days (≈1/tick; bounded) | journal |

---

## a) FULLY DONE

1. **Contract compliance, end to end:** read AGENTS.md first; smallest-change discipline (docs-only, zero code touched); pathspec-scoped commit; Task-Queue-ID footer; never pushed; `.crushrc`/`crush.json`/`.tq-verify` untouched.
2. **Live diagnosis re-verified before acting** (table above) — every premise of the TODO item checked against current reality; the item was 8 days stale in places (see §d-4).
3. **Blocker root-caused at SOURCE level:** InboxClean `cmd/inboxclean/commands.go` — the auth flow prints an auth URL, waits for the human to complete Google login in a browser, then waits for the pasted redirect URL on stdin. No device-code flow, no localhost-redirect listener (`grep` for DeviceAuth/oob/OOB: zero hits). Combined with: `sudo` banned in agent sessions (tool policy) and token files owned by the `inboxclean` service user. Conclusion: 0% of the fix is agent-executable — proven, not assumed.
4. **`work`-account risk dispositioned with evidence:** never revoked, actively refreshing, cursor advancing. The item's "may be affected next" fear is unfounded *today* (but see §d-2/§d-3 for the caveat I under-reported).
5. **New contradiction captured in the queue item:** the `gmail` tag AUTO→NONE demote PATCH fails at the PAPERLESS side (`client_error`) under the CONNECTED account since Sep 06 — contradicting the item's causal claim that the dead `main` token blocks that PATCH.
6. **TODO_LIST loop closed per contract:** left unchecked + `— BLOCKED: <one-line reason>` + verified state + the new finding.

## b) PARTIALLY DONE

1. **The work item itself** — diagnosis 100%, fix 0%. From the queue's perspective this is a blocked outcome, not a completion; the service remains dead until a human acts.
2. **Impact-claim correction** — flagged the demote-PATCH contradiction but did NOT root-cause the actual Paperless rejection (needs the HTTP status from paperless/inboxclean debug output; the claim's origin session asserted it without visible evidence).
3. **"work may be affected next"** — proven not-yet-affected, but no watch exists for the case where the console is STILL in Testing (work's current grant would then also carry the 7-day bomb; its issuance date is unknowable from file mtimes because refreshes rewrite the file).

## c) NOT STARTED

1. Google Cloud Console consent-screen flip to "In production" (human-only; possibly already done — §g Q1).
2. The `main` re-consent itself (human-only; exact command in §f item 2).
3. Post-re-consent verification (health `connected`, sync green, cursor > 5152620).
4. **Stale-historyId risk assessment** — after re-consent, main retries with `startHistoryId=5152620`, now 8 days old; Gmail history windows can expire and the client's fallback (full re-scan vs retry-loop) is unverified. Found while writing THIS report; was on nobody's list before.
5. Papersync backlog coverage for `main` (8 days of unarchived statements — does papersync rescan the blind window, or only new mail?).
6. Upstream InboxClean issue for the paperless tag-PATCH rejection (verify-before-filing: HTTP status first, §f-8/10).

## d) TOTALLY FUCKED UP

Brutally honest; nothing fabricated to look self-critical:

1. **The service has been dead 8 days and will stay dead until a human acts — and the task was queued to an agent structurally incapable of performing it.** That is a queue-routing failure, not an effort failure: any task whose minimal execution requires a Google web login + sudo + browser consent should be tagged HUMAN at enqueue and never reach an unsupervised agent pool. One session was spent discovering what a capability tag would have said instantly.
2. **My first (queue-facing) report over-assured on `work`** ("never revoked and actively refreshing — only `main` needs re-consent"). True today, but I omitted the watch item: if the consent screen is still in Testing, work's CURRENT grant dies 7 days after ITS issuance too, and I cannot date that issuance. An unflagged caveat is a future surprise with my name on it.
3. **An unexplained contradiction I noticed and did not resolve:** AGENTS.md predicted work's death "Sep 05 evening" (7-day bomb; grant issued Aug 29 ~19:00). Work is alive Sep 12 — 14 days past its recorded grant date. Either work was silently re-consented, the production flip happened before Aug 29 19:00 (which contradicts main's Aug 28 grant dying exactly on schedule), or Google's expiry is not the clean 7 days the narrative assumes. I reported "work is fine" without surfacing this. Corrected here.
4. **The TODO item's causal claim was encoded unverified and I only appended a note instead of correcting it inline:** "dead main token blocks the `gmail` tag AUTO→NONE self-heal PATCH" — that PATCH demonstrably fails at the Paperless side under the CONNECTED account. The claim came from the 2026-09-06 session and became queue doctrine without re-verification. This is the same class as the `email_states` fixture trap: point-in-time session claims treated as current truth.
5. **`/health` lies by design and I documented the lie instead of escalating it as its own defect:** `"status": "ok"` with one account dead for 8 days, and no Gatus body-pattern for `auth_expired`. The OnFailure Discord path is the only signal. Out of scope for this item, but it is the exact phantom-green class this repo keeps killing — and it is live RIGHT NOW.
6. **The TODO item grew longer, not shorter** (~90 words added) — necessary for honest loop closure, but it worsens the AGENTS.md §290 size-diet problem and needs a trim the moment the human steps land.
7. **Not fucked up, for the record:** nothing was destroyed or reverted; no code changed; repo gates green on commit (gitleaks, trailing whitespace, nix flake check); no split brain created — the queue-item note deliberately duplicates AGENTS.md state until the fix lands (trim scheduled in §f-23).

## e) WHAT WE SHOULD IMPROVE

1. **Capability-aware queue routing** — tag items requiring browser / sops-write / sudo / vendor consoles as HUMAN at enqueue time; agents skip with a one-line reason instead of discovering it empirically.
2. **Headless consent path upstream (highest leverage)** — device-code flow or localhost-redirect listener in `inboxclean auth` would have made this entire task agent-executable. Every future 7-day bomb becomes a closed ticket instead of a blocked queue item.
3. **Health honesty** — `/health` overall should be `degraded` (not `ok`) when any account is `auth_expired`; Gatus should body-assert `auth_expired` absence. Presence-based `connected` reporting was the 2026-09-04 lesson; the aggregate status is the same lie one level up.
4. **Token-liveness metric** — textfile gauge from `token*.json` mtime age (no refresh activity in N hours = dead token even if present). Cheap, covers both accounts, closes the presence-vs-validity gap from OUTSIDE the app (works even if upstream never fixes health).
5. **Verify causal claims before they become queue doctrine** — second instance of this class this month. Rule: an item's stated impact chain gets one live probe before it is trusted as the reason for the task.
6. **Correct AGENTS.md's InboxClean narrative with today's evidence** — work never revoked (prediction falsified), health now reports `auth_expired` (the phantom-`connected` half of TODO 237 looks fixed upstream — verify, annotate, close; don't re-litigate).
7. **Trim dense queue items on resolution** — blocked-notes should be stripped when the human step lands, not left to accrete.

## f) Up to 50 things we should get done next

Pri: P0 now / P1 this week / P2 soon / P3 backlog. Owner: USER / AGENT / UPSTREAM.
Items 1–10 are the critical path. Honest stop at 40: the remaining 10 slots would be padding, and invented work is worse than a short list ("up to 50" is a ceiling, not a quota).

**Critical path (unblock `main`):**

1. P0 USER — Check the Cloud Console consent screen publishing status (APIs & Services → OAuth consent screen). Work's survival suggests it may already be "In production" — verify BEFORE anything else (§g Q1).
2. P0 USER — Re-consent `main` (AGENTS.md runbook; `main` needs NO `INBOXCLEAN_CONFIG` — that env is only required for extra accounts like `work`):
   `sudo -u inboxclean GMAIL_CREDENTIALS_FILE=/var/lib/inboxclean/credentials.json GMAIL_TOKEN_FILE=/var/lib/inboxclean/token.json DB_PATH=/var/lib/inboxclean/inboxclean.db inboxclean auth --account main`
   (open the printed URL, log in AS the main mailbox user, paste the redirect URL back).
3. P0 USER — Verify: `/health` → `main: connected`; next sync tick green; cursor advancing past 5152620.
4. P0 AGENT — Watch for the stale-historyId mode (§c-4): if the sync fails post-consent with a history/404-class rejection, confirm InboxClean falls back to a full re-scan instead of retry-looping an expired historyId.
5. P0 USER — Calendar reminder: if `main` dies again exactly ~7 days after re-consent (≈ Sep 19 if done today), the console is NOT actually in production → flip + re-consent again. This converts the §d-3 contradiction into a decisive experiment.
6. P0 USER — Confirm statement archiving resumes for `main`: journal shows a "Paperless-ngx attachment sync (account main)" section with >0 scanned. Anything mailed to `main` during Sep 04–12 is unarchived; if papersync's window misses the blind period, trigger its backfill path.
7. P1 AGENT — Re-check the `gmail` tag demote PATCH after re-consent: expected to STILL fail (it fails under connected `work` too) — at that point the old causal claim is dead and the record gets corrected, not merely annotated.
8. P1 AGENT — Root-cause the Paperless-side rejection: obtain the HTTP status (paperless journal / inboxclean debug) for `update tag "gmail"`. Hypotheses: paperless 3.1.1 tag-update contract change, API-token scope, papersync PATCH payload bug. No code changes before the status is known.
9. P1 USER — Pragmatic unblock independent of upstream: set the `gmail` tag's matching algorithm to NONE once in the Paperless UI (Settings → Tags). Kills the recurring demote attempt and the classifier exposure in one click.
10. P1 UPSTREAM — File the InboxClean issue for the tag-PATCH rejection (verify-before-filing discipline: only after §f-8 yields the real status; include repro + paperless version).

**Hardening (monitoring / honesty):**

11. P1 UPSTREAM — `inboxclean auth` headless path (device code or localhost-redirect listener) — makes this task class agent-executable forever.
12. P1 AGENT — Gatus body assertion that `/health` never contains `auth_expired` (or upstream: aggregate `status: degraded`) — the `status: ok` lie is live right now.
13. P1 AGENT — Token-liveness textfile metric from `token*.json` mtime age (fail-closed emission, per repo doctrine); Gatus pattern; covers both accounts from outside the app.
14. P1 USER — Confirm OnFailure Discord alerts actually fired during Sep 04–12 (8 days of exit-1 every 30 min should have been loud). If the channel is silent, the alert ROUTE is the next ghost to hunt — an outage that doesn't alert is worse than the outage.
15. P2 UPSTREAM — `/health` should validate refresh tokens, not presence (TODO 237 first half): `auth_expired` detection exists now; verify coverage of ALL dead states, then annotate/close 237.
16. P2 UPSTREAM — Dashboard should surface a persistent re-auth action card for `auth_expired` (TODO 237 second half; journal text already says the right thing — check the UI matches).
17. P2 USER — Post-consent, verify `/inbox` renders `main`'s tab and recent messages (functional outcome, not just health JSON).
18. P2 AGENT — Watch `inboxclean.db` growth/WAL during main's 8-day catch-up (event-store flood; the 2026-08-16 404k-locked-errors class lives in this codebase's history).
19. P2 AGENT — Token-liveness alert should distinguish "no mail = no refresh" from "refresh broken" (idle-period false positives) before it ships — calibrate against work's quiet periods.
20. P3 AGENT — Record the resolved noise floor: ≈52 `token_revoked` lines/day while dead → post-fix journal should drop to ~0; residue means a third token path or retry loop is still broken.

**Related queued / noticed in passing:**

21. P2 USER — Rotate the InboxClean→Paperless API token (user-chosen 2026-09-03, all steps sudo; TODO_LIST line 46) — batch it with §f-24's sops edits in one sitting.
22. P2 USER — Paperless statement decryption go-live: fill `inboxclean-decrypt.yaml` with the bank PDF password (TODO 27 step 1) — without it, resumed `main` archiving still lands encrypted statements as-is with the `encrypted` tag.
23. P2 AGENT — Trim the TODO item to its post-fix residual once the human steps land (remove the BLOCKED note; §d-6).
24. P2 AGENT — docs-health HARVEST on this report's §f: route P1/P2 items into TODO_LIST with owners (section f belongs in TODO_LIST, not entombed in a timestamped file).
25. P2 AGENT — Annotate AGENTS.md InboxClean section: work never revoked (prediction falsified), health `auth_expired` improvement, demote-PATCH paperless-side failure.
26. P2 USER — Decide agent sops-write capability (TODO 288) — THIS task is the poster child: with the user age-key as recipient, several BLOCKED classes become agent-executable (browser/Console steps stay human regardless).
27. P2 AGENT — Queue convention proposal: HUMAN tag at enqueue for browser/sudo/console-gated items (prevents this task's routing failure class permanently).
28. P3 AGENT — Cross-check other queued human-gated items (Mail Relay go-live, Miniflux SSO flip, Hermes PAT) for the same routing problem and tag them HUMAN.
29. P3 AGENT — AGENTS.md size diet (TODO 290) — this incident added more InboxClean evidence; the diet keeps slipping; schedule it explicitly.
30. P3 UPSTREAM — papersync should escalate a persistently-failing tag ensure to the dashboard (currently a per-sync WARN line only, invisible to non-journal readers).
31. P3 UPSTREAM — `inboxclean auth` should print WHICH token file it will write BEFORE waiting on stdin (accidental-run safety for multi-account boxes).
32. P3 AGENT — Add the auth runbook's `main` variant (no `INBOXCLEAN_CONFIG` needed) next to the `work`-variant trap note in docs so the human step is fully copy-paste.
33. P3 USER — Verify nothing was LOST in the blind window: messages Gmail-side-processed during Sep 04–12 must appear as history entries on resume; spot-check a few known emails in the dashboard after re-consent.
34. P3 AGENT — Confirm the Gatus "InboxClean Projections Ready" (`email_state`) check stayed green through the outage — if green for 8 days while the product was dead, document the check's true scope (projection liveness ≠ ingest health).
35. P3 USER — Hard-refresh the dashboard after re-consent (confirm the UI drops the dead-state cache; AGENTS.md says reload reconnects automatically — verify the claim).
36. P3 AGENT — If §f-14 shows Discord silence: audit the `onFailure` wiring (module routing + webhook sops key) as its own incident — monitoring ghost during a real outage is the worst class in this repo.
37. P3 AGENT — Post-incident: add the stale-historyId caveat + work-grant contradiction to AGENTS.md in one pass, then archive this report per docs-health conventions.
38. P3 UPSTREAM — Consider a `doctor` subcommand check for token grant age vs consent-screen status (best-effort warning: "token issued <7d ago and app possibly in Testing").
39. P3 AGENT — Verify `inboxclean-sync` timer semantics held through boot/DAS churn this week (Persistent catch-up; cadence looked clean today — confirm nothing stacked).
40. P3 AGENT — Re-read `/health` over the following week for the `dead_letters: 0` invariant (capture-DLQ discipline) and confirm both accounts age past 7 days post-consent.

## g) Questions I cannot figure out myself

1. **Is the Google Cloud Console consent screen already "In production"?** Work's survival past its predicted Sep-05 death suggests yes — but that contradicts main's on-schedule Sep-04 death, so I cannot infer it reliably. It decides whether §f-1 is already done AND whether main's NEW token will survive.
2. **Did you re-consent `work` at some point after Sep 5 — or why did work never hit the 7-day bomb despite the AGENTS.md prediction?** This determines the §f-5 bomb-watch design and whether AGENTS.md's expiry narrative needs correcting.
3. **For the `gmail` tag demote PATCH failing Paperless-side:** file the upstream issue after root-cause (§f-8/10), or just set the tag's matching to NONE once in the Paperless UI (§f-9) and close the class? The second is a 30-second user action; the first protects every future tag.

---

*Point-in-time snapshot — 2026-09-12 04:13 CEST. Written per the status-report skill with the user-mandated Markdown format override (skill default is HTML).*
