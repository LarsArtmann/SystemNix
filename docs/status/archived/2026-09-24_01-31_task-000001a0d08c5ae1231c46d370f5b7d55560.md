# Task 000001a0d08c5ae1231c46d370f5b7d55560 — Status Report (second snapshot)

> **RESOLVED + ARCHIVED (2026-09-24 docs-health pass):** this task's work is fully landed and closed on both queue surfaces (TODO_LIST `[x]` + docs/todo/storage.md DONE row; commit chain verified in the 2026-09-24 06:45 window closeout). This file is a superseded same-ID dispatch report; the canonical report for this task ID is `docs/status/2026-09-24_01-39_task-000001a0d08c5ae1231c46d370f5b7d55560.md`. ~~Open dispatch report for a closed task.~~ resolved + archived at the 06:45 pass.

**Date:** 2026-09-24 01:31 CEST
**Scope:** Work item "Flake fixture test for `scripts/borg-restore-drill.sh` (PATH-stub borg)" from TODO_LIST.md storage section. This is the SECOND report for this task ID (first: `2026-09-24_01-27_…`, committed 6298b07d), written after a re-verification pass. **Format note:** user explicitly requested `.md`; the status-report skill's HTML default was overridden per the user instruction (one-off, not propagated).

**What this second pass is for:** the first report closed the loop and documented findings, but its (f)-list lived ONLY in a timestamped file — the exact "entombed in a status report" anti-pattern the repo's own docs-health doctrine warns about. This pass (1) re-verified the deliverable from the committed tree, (2) harvested the top findings into TODO_LIST.md / docs/todo where the queue can actually pick them up, and (3) answers the self-review questions honestly.

---

## Re-verification (delta since the 01:27 report)

- `checks.x86_64-linux.borg-restore-drill-fixture` rebuilt green from the COMMITTED tree (`/nix/store/bdz4gkdy…-borg-restore-drill-fixture`), not just from the dirty worktree it was authored in.
- Working tree was clean before this pass; commit `d7a54819` carries the Task-Queue-ID footer; TODO_LIST.md:33 marked `[x] DONE 2026-09-24`.
- No code changes this pass. Changes: this report + 3 harvested backlog rows (see §c/"forgot").

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | **Fixture check landed and committed**: `checks.x86_64-linux.borg-restore-drill-fixture` (forgejo-scripts-fixture pattern) runs the REAL `scripts/borg-restore-drill.sh` in the sandbox with a triple stub — `nix` (the drill resolves borg via `nix build --expr`, so a plain "PATH-stub borg" per the queue item's literal wording would never shadow), `borg` (behavioral), `id` (fakes root for the env-file die paths) | Build PASS re-verified this pass from the committed tree; commit `d7a54819` |
| 2 | **Seven behaviors asserted under `set -e`**: root-gate die WITH FAIL record persisted; bogus `--local` die; repo-without-config die; PLACEHOLDER-repo tripwire (fixture `BORG_ENV_FILE`); `/run/secrets/borg_password missing` die (the mountpoint gate's asserted direct predecessor); happy-path `--local` PASS incl. byte-verified subset + per-run PASS record; `--verify-data` 1.x `--dry-run` fallback + `--archive` pin | fixture build output lists all seven scenario names |
| 3 | **Queue + library closed in sync**: TODO_LIST.md:33 and docs/todo/storage.md:46 both `[x]` with the honest sandbox-coverage note | diff in `d7a54819` |
| 4 | **Pre-existing gate breakage proven, not guessed**: the pre-commit flake-check failure (`checks.disko-layout` → `disko-samsung-tlc-vm.nix.drv is not valid`) reproduced byte-identical on a `git worktree` baseline at pre-change HEAD (same drv hash `4xv74d17…`) | baseline run in the 01:27 session |
| 5 | **Commit discipline**: footer on the close-out commit; `--no-verify` use documented with its pre-existing-failure justification (precedent `73145250`) | git log |
| 6 | **NEW this pass — findings harvested into the backlog** (the first report's biggest gap, see §e): fixture-negative row (TODO_LIST + storage.md), disko-layout gate outage row (TODO_LIST + pipeline.md [ready]) | this pass's diff |

## b) PARTIALLY DONE

1. **Real-mode coverage stops at the secrets-existence die — the `/mnt/hot` mountpoint gate is NOT directly asserted.** The gate sits behind three `[ -e /run/secrets/* ]` checks (bash builtins, unstubbable), so it is unreachable in the nix sandbox. Mitigated honestly (predecessor die asserted, gap documented in check comment + both TODO rows); only a VM test (`runNixOSTest` seeding the secret files + a real `/mnt/hot`) can close it — queued.
2. **The stub `borg` is behavioral, not the real binary** — the fixture proves die/plumbing paths, not borg semantics. The live `--selftest` against real borg (last PASS 2026-09-24 00:29) is the semantics gate. Deliberate split, but the fixture alone is NOT sufficient evidence that a real restore works — the go-live real-repo drill (checklist step 9) remains the true proof.
3. **The pre-commit gate is partially dead** (see d1): gitleaks and the eval gate both skip on every `--no-verify` commit fleet-wide until the disko-layout fix lands.

## c) NOT STARTED

1. **TODO_LIST:41** — folding the borg env-path-pin negative case (drifted `BORG_ENV_FILE` literal → `backup.nix` eval assertion must fire) into this fixture. Distinct queued dispatch; deliberately untouched (no scope creep).
2. **The `checks.disko-layout` eval failure itself** — diagnosed and now QUEUED this pass, but not fixed (out of this task's scope; it predates the task).
3. **No `CHANGELOG.md` entry for the fixture** — judgment call (test-only addition; the script's entry exists from 2026-09-23). Still open as a judgment, not an omission with consequences.
4. **VM test for the real-mode drill path** — queued-shaped, not yet queued as its own row (named in storage.md:46's sandbox note; fold into a future dispatch).

## d) TOTALLY FUCKED UP

1. **The repo's pre-commit gate is broken at HEAD — and my run is the first to trip over it.** `checks.disko-layout` fails eval with `disko-samsung-tlc-vm.nix.drv is not valid`, proven pre-existing. **Consequence: every agent's commit now uses `--no-verify`, silently skipping gitleaks + the eval gate + formatting for the whole fleet.** Not caused by this task, but every `--no-verify` commit widens the unguarded window. NOW QUEUED (pipeline.md [ready], this pass) — at the 01:27 report it was only documented, which was the real mistake (see e1).
2. **The first report's (f)-list was entombed**: 18 next-items sat in a timestamped file nobody harvests, while the harness contract explicitly permits appending new queue items. The top items stayed unpickable for ~4 minutes — trivial in wall-clock, but the pattern (report-then-wait instead of harvest-now) is the exact anti-pattern the skill file warns about. Fixed this pass for the top 2 items.
3. **Nothing else.** No broken tests, no data risk, no secret exposure, no tree damage from this task.

## e) WHAT WE SHOULD IMPROVE

1. **Harvest-while-you-are-there**: when a status report produces `[ready]`-shaped findings, the SAME session should append them to TODO_LIST/docs/todo — not leave them for a future docs-health HARVEST pass that may read the report weeks later. This pass did it retroactively; do it inline next time.
2. **Queue rows should describe intent, not mechanism.** "PATH-stub borg" was wrong the moment the drill gained `nix build` resolution; a stub-borg-only implementation would have passed nothing. The fixture discovered the triple-stub need pre-build, which is the system working — but the queue wording invited a wasted cycle.
3. **Fixtures need fixture-negatives.** The drill fixture asserts the script's failure paths, but nothing asserts the FIXTURE fails when the script regresses (a fixture that silently stops exercising the real script would phantom-green). The repo applies this doctrine to lints (`negative-test-lints.sh`) but not yet to behavioral script fixtures — queued this pass.
4. **Formatter runs vs parallel sessions**: in the authoring session, `nix fmt -- --ci` reformatted a parallel session's brand-new file and the daemon batched it into a shared commit. Held per doctrine (flagged, not reverted), but format checks should be scoped or preceded by a foreign-dirty-file check — the 2026-08-27 837-line reformat trap.
5. **`--no-verify` escapes should always run gitleaks manually** on the staged tree (10 s) so the secret gate survives the eval-gate outage. Standing habit going forward.
6. **Two status reports for one task ID is redundancy**, not diligence: the second pass only added harvest + re-verification. If re-verification is the goal, do it BEFORE the first report commits.

## f) Up to 50 things we should get done next

*Brainstorm, not commitments; sorted by impact. Items 1–3 directly enabled/unblocked by THIS task. The top items are now QUEUED (this pass); the rest are harvest fuel. Deliberately NOT padding to 50 — the honest actionable count from this task's scope is 18; unrelated repo-wide wishes would violate the "do not research unrelated stuff" constraint.*

1. **Fix the pre-existing `checks.disko-layout` eval failure** — QUEUED this pass (pipeline.md [ready] + TODO_LIST). Re-arms the pre-commit eval gate + gitleaks for the whole fleet; add the eval-guard negative test after.
2. **Fixture-negative for `borg-restore-drill-fixture`** (mutate a drill copy → assert the fixture FAILS) — QUEUED this pass (storage.md [ready] + TODO_LIST).
3. **Fold the env-path-pin negative case into the fixture** (TODO_LIST:41, pre-existing queue row; now trivially adjacent since the fixture exists).
4. **VM-test the drill's real-mode path**: seed `/run/secrets/{borg_password,borg_ssh_key,borg_known_hosts}` + rendered env + real `/mnt/hot`, stub only the network — reaches the mountpoint gate the sandbox cannot.
5. **Persist the offsite-borg §13/§16 deploy-smoke fixture** (TODO_LIST:34) — the "fixture-tested" claim still points at no committed artifact.
6. **Fix pre-deploy §13 silent-skip** (TODO_LIST:35): distinguish offsite-borg disabled from a transient eval error.
7. **Assert the offsite-borg IO tier in the smoke blocks** (TODO_LIST:36) — ioTier.background has no automated gate.
8. **Positive-path render probe for offsite-borg** (TODO_LIST:37): eval-time flake check for BE/6 + tripwire/marker/borg-env on enable=true.
9. Re-run `scripts/borg-restore-drill.sh --selftest` (plain + `--verify-data`) post-fixture to reconfirm real-borg semantics independently of the stub.
10. Audit other `scripts/*.sh` that call `nix` at runtime for the same stub-the-resolution-chain trap (`grep -l 'nix build' scripts/`).
11. Prove shellcheck covers `scripts/*.sh` in pre-commit (TODO_LIST:39).
12. Sweep the source report's (§f.6-adjacent) findings list — confirm every item from the 2026-09-24 accuracy reports is closed or queued.
13. Consider promoting the drill to a flake app (`nix run .#borg-restore-drill`) — parked since 2026-09-23.
14. StorageBox go-live stays owner-gated (hostname/username + host-key pin) — unchanged by the fixture.
15. Real-repo drill (go-live checklist step 9) after go-live; append the timing row to the restore runbook.
16. Confirm CI (`nix-check.yml`) red window during the disko-layout outage and whether any input bumps were blocked meanwhile.
17. deadnix-style cleanup of the fixture's unused `nativeBuildInputs` (gnused is no longer needed — PATH injection not used).
18. `tq`-harvest sanity: rows 1–2 are `[ready]`-shaped and should be picked up; verify the harvest actually sees them.

## g) Questions I can NOT figure out myself

1. **Was the `checks.disko-layout` outage known/already owned?** I proved it pre-existing and queued it this pass — but if another session is already on it, the new pipeline.md row should be deduplicated. If NOT owned: it is the fleet's top priority (every commit currently skips gitleaks).
2. **Is the stub-borg fixture acceptable as the standing regression gate, or do you require the deeper VM test (f4) before the drill counts as "fixture-tested"?** Only you can weigh the un-asserted mountpoint gate against the restore path's risk profile.
3. **Should the go-live real-repo drill (checklist step 9) be allowed to run against the REAL StorageBox repo before the first production backup lands**, as a final go-live validation, or strictly after? The runbook currently says "step 9 of go-live" without pinning the order relative to the first nightly job.
