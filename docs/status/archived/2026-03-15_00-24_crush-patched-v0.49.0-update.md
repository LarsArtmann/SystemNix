# Status Report: Crush-Patched v0.49.0 Update

**Date:** 2026-03-15_00-24
**Session Goal:** Update crush-patched to latest version (v0.49.0)
**Status:** BUILD SUCCESSFUL - Awaiting patch decision and system apply

---

## Executive Summary

Successfully updated `crush-patched` from v0.47.2 to v0.49.0. The build compiles and runs correctly with the new version. However, the custom grep UI patch (PR #2070) was **NOT merged upstream** and is currently commented out pending user decision.

---

## A) FULLY DONE ✅

1. **Fixed update script URL bug** (`pkgs/update-crush-patched.sh`)
   - Issue: Double "v" prefix causing `vv0.49.0` in GitHub URLs
   - Root cause: Version stored without "v" but script added it again
   - Fix: Strip "v" from detected tag, add it back only for GitHub URL construction
   - Status: **VERIFIED WORKING**

2. **Updated Go from 1.26.0 to 1.26.1** (multiple files)
   - Files: `flake.nix`, `pkgs/go-1.26.nix`, `platforms/darwin/default.nix`
   - Hash: `sha256-MXIpPQSyCdwRRGmOe6E/BHf2uoxf/QvmbCD9vJeF37s=`
   - Reason: crush v0.49.0's go.mod requires `go >= 1.26.1`
   - Status: **VERIFIED WORKING**

3. **Fixed vendor directory inconsistency** (`pkgs/crush-patched/package.nix`)
   - Issue: `postUnpack = "rm -rf vendor"` not working with buildGo126Module
   - Attempts: postUnpack, postPatch, preBuild all failed
   - Solution: Added `GOFLAGS = "-mod=mod"` to force module mode
   - Vendor hash: `sha256-xakV5Alm3EwDk5VkSINxJM1C3uF492QzA+BQkqZ6qB4=`
   - Status: **VERIFIED WORKING** - Build succeeds

4. **Changed to buildGo126Module** (`pkgs/crush-patched/package.nix`)
   - Ensures Go 1.26.1 is used for building crush
   - Status: **VERIFIED WORKING**

5. **Build verification**
   - Command: `nix build .#crush-patched`
   - Output: `./result/bin/crush --version` → `crush version v0.49.0`
   - Status: **VERIFIED WORKING**

---

## B) PARTIALLY DONE ⚠️

1. **Custom patch (grep-show-search-params)**
   - **Status:** Patch is **NOT merged** in upstream v0.49.0
   - **Verified:** `git merge-base --is-ancestor e4aa1742 v0.49.0` returned false
   - **Current state:** Patch commented out in package.nix
   - **Decision needed:** Re-apply patch or proceed without it

2. **Update script improvements**
   - Script now executable (`chmod +x`)
   - URL bug fixed
   - **Not tested end-to-end** - only verified URL construction logic

---

## C) NOT STARTED ❌

1. **System apply** - `just switch` not executed
2. **Final verification** - crush not tested in actual usage
3. **Git commit** - Changes not committed yet
4. **Patch rebase** - If user wants patch, need to test if it applies cleanly to v0.49.0

---

## D) TOTALLY FUCKED UP 💥

1. **Initial vendor directory approaches failed**
   - `postUnpack`, `postPatch`, `preBuild` approaches all failed
   - Root cause: buildGo126Module timing differences
   - Fix: GOFLAGS approach succeeded

2. **proxyVendor = true caused GOPROXY=off errors**
   - Wrong solution for the problem
   - Reverted and used GOFLAGS instead

---

## E) WHAT WE SHOULD IMPROVE 📈

1. **Update script testing** - Run full end-to-end test of update script
2. **Patch management** - Create a patches/ directory with version-specific patches
3. **Build documentation** - Document the GOFLAGS = "-mod=mod" workaround
4. **Automated verification** - Add post-build version check to flake
5. **Vendor hash automation** - Script should auto-detect vendor hash

---

## F) Top #25 Things to Get Done Next

### Immediate (Today)

1. **Decide on patch** - Re-apply grep-show-search-params patch or proceed without?
2. **Test patch application** - If yes, test if patch applies cleanly to v0.49.0
3. **Update patch hash** - If patch applies, calculate new hash
4. **Run `just switch`** - Apply system changes
5. **Verify crush works** - Test actual crush functionality

### Short Term (This Week)

6. **Test update script end-to-end** - Verify ./update-crush-patched.sh works
7. **Commit changes** - Create detailed commit message
8. **Clean up sublime-text changes** - These are unrelated, handle separately
9. **Document GOFLAGS workaround** - Add to AGENTS.md or wiki
10. **Create patch management strategy** - Version-specific patches directory

### Medium Term (This Month)

11. **Automate vendor hash detection** - Improve update script
12. **Add pre-commit check** - Verify crush builds in CI
13. **Monitor crush releases** - Set up release notifications
14. **Review PR #2070 status** - Check if/when it will be merged
15. **Consider contributing** - Help merge patch upstream

### Long Term

16. **Evaluate nixpkgs crush package** - Could we use upstream package?
17. **Reduce custom patches** - Work with upstream more
18. **Improve build reproducibility** - Pin all dependencies
19. **Add update automation** - Auto-update via flake
20. **Document build process** - Wiki page for crush-patched

### Nice to Have

21. **Create crush development shell** - For local development
22. **Add build caching** - Speed up rebuilds
23. **Create test suite** - Verify crush functionality
24. **Monitor Go versions** - Alert when new Go available
25. **Improve error messages** - Better build failure diagnostics

---

## G) Top #1 Question I Cannot Figure Out Myself 🤔

**Should we re-apply the grep-show-search-params patch (PR #2070) to v0.49.0?**

Context:

- Patch adds: Show grep search parameters in pending state (pattern, path, include, literal mode)
- User experience: Better visibility of what grep is doing while running
- PR status: Open, not merged, commit e4aa1742699db27c2ccd5e9c2b9f4d0948870581
- Version check: Patch NOT in v0.49.0 (verified via git merge-base)
- Risk: Patch may not apply cleanly to v0.49.0, may need rebase

Options:

1. **Re-apply patch** - Better UX, but maintenance burden
2. **Proceed without** - Simpler, but lose feature until upstream merges
3. **Rebase patch** - Create v0.49.0-specific patch version

**I need user input on this decision before proceeding.**

---

## Files Modified

| File                             | Change                                    | Status            |
| -------------------------------- | ----------------------------------------- | ----------------- |
| `pkgs/crush-patched/package.nix` | v0.47.2→0.49.0, buildGo126Module, GOFLAGS | ✅                |
| `pkgs/go-1.26.nix`               | Go 1.26.0→1.26.1                          | ✅                |
| `flake.nix`                      | Go 1.26.0→1.26.1 (2 overlays)             | ✅                |
| `platforms/darwin/default.nix`   | Go 1.26.0→1.26.1                          | ✅                |
| `pkgs/update-crush-patched.sh`   | Fixed URL bug, made executable            | ✅                |
| `dotfiles/sublime-text/*`        | Unrelated changes                         | ⚠️ Separate commit |

---

## Build Verification

```bash
$ nix build .#crush-patched
# Success - no errors

$ ./result/bin/crush --version
crush version v0.49.0
```

---

## Next Immediate Steps

1. **User decision required:** Re-apply patch or proceed without?
2. If patch: Test patch application, update hash if needed
3. Run `just switch` to apply system changes
4. Verify crush functionality
5. Commit all changes with detailed message

---

**Generated:** 2026-03-15_00-24
**Author:** Crush AI Assistant
**Session:** Crush-Patched v0.49.0 Update
