# Status Report — Task 000001a08ea43a364ec0910b47ffaaa9f1cb (Off-site backup decision)

**Date:** 2026-09-11 06:50
**Task:** TODO_LIST P0 — "Off-site backup decision (3rd copy)" (flagged since 2026-06-25)
**Session type:** SECOND dispatch of the same queue item. The first session (earlier today, commits `84c70af5` → `09d481b0` → `91e387fd`) made the decision and closed the paperwork. My dispatch found the item already complete; my work became **independent verification + closure-completion of the gaps the first session itself had flagged**.
**Commits this session:** living-doc sync (`ROADMAP.md` + `FEATURES.md` stale offsite stances → DECIDED) + this report. No work commit of my own exists for the closure itself — see (d)2.
**Scope discipline:** docs-only. `nix flake check --no-build` re-verified green this session ("all checks passed!"; the aarch64-darwin omission is the documented expected warning). Working tree clean before my edits. Not pushed, per contract.

> **Format override note:** the status-report skill defaults to a styled HTML dashboard; this report is Markdown because the task instructions explicitly name a `.md` path. One-off override, not a new default. This is the SECOND report for this Task-Queue-ID (first: `docs/status/2026-09-11_06-31_task-…md` from the decision session) — kept separate as point-in-time snapshots; the ANNOTATE pass may cross-link them.

---

## Context you need first

The queue handed me the raw item text ("Decide: Google Photos/Drive vs sdf rotation vs Hetzner StorageBox + Borg") — a **stale snapshot**. The literal TODO line no longer existed: line 170 already read `[x] … DECIDED 2026-09-11 (task queue): Hetzner StorageBox + Borg`, closed by the first session minutes earlier under the same Task-Queue-ID. I verified the closure end-to-end instead of redoing it, found and fixed the one verified remaining doc gap (stale "decide whether…" stances in ROADMAP/FEATURES), and wrote this report.

**The blunt headline, unchanged from the first report and still true: the DECISION is done, the PROTECTION is not.** Until the Borg leg deploys, the third copy does not exist and the pool still dies with the house. Marking the P0 `[x]` closed a paperwork loop, not a data-loss exposure.

## a) FULLY DONE

| Item | Evidence |
| --- | --- |
| **Decision made and recorded (first session, verified by me)** | Hetzner StorageBox BX11 + BorgBackup (repokey-blake2, port 23). Rejections recorded with reasons: Google Photos/Drive (`google-sync` DORMANT + mirrors live data, not snapshots); sdf WOOACME rotation (drive FROZEN by user decision, unbounded RPO). Blueprint: `docs/research/hetzner-storagebox-borgbackup.md` (committed `84c70af5`). |
| **TODO_LIST loop closed per file convention** | `TODO_LIST.md:170` `[x]` with full rationale + explicit "NOT yet deployed" pointer; implementation follow-up spawned as its own unchecked item at `TODO_LIST.md:171` (module, sops `borg_password`, port-23 key, monitoring, BX11 1 TB sizing caveat). Commit `09d481b0` carries the exact Task-Queue-ID footer. `[x]`-in-place is this file's convention (3 such markers present). |
| **AGENTS.md memory synced** | BTRFS Backups section records the DECIDED stance + NOT-yet-deployed flag (same commit). Verified present in the loaded context this session. |
| **My verification pass (this session)** | Independent re-check of every closure artifact: TODO_LIST lines 170–171, AGENTS.md paragraph, research doc on disk + tracked, `Task-Queue-ID` footer present on BOTH task commits (`09d481b0`, `91e387fd`), working tree clean, `nix flake check --no-build` → "all checks passed!". |
| **Living-doc stale-stance sweep (this session — the gap the first session flagged and left open)** | `ROADMAP.md:15` ("decide whether that satisfies 3-2-1…") and `FEATURES.md:279/508/516` ("true off-site still open" / "Off-site backup still missing" / "decide whether 3-2-1 is satisfied") all contradicted the DECIDED decision. All four lines now record DECIDED + rejected alternatives + not-yet-deployed + TODO_LIST pointer. Fixed on sight under the AGENTS.md trivial-doc-staleness owner permission — this task's exact topic, ~4 lines. |
| **No-change discipline on the duplicate dispatch** | Nothing was re-edited, no empty commit created, the first session's work was not reverted or duplicated. TQ_RESULT pointed at the existing closure commit `09d481b0`. |

## b) PARTIALLY DONE

| Item | Works | Remaining gap | Effort |
| --- | --- | --- | --- |
| **The 3-2-1 posture itself** | Decision + blueprint + tracked TODO exist; pool (2nd copy) live since 2026-08-17 | The entire implementation (see (c)1). No offsite byte has flowed. Blocker: user-side StorageBox credentials. | L |
| **Documentation consistency for the decision** | TODO_LIST + AGENTS + ROADMAP + FEATURES all now agree (this session completed the sweep) | Historical status reports intentionally still carry the old DEFERRED stance — correct per point-in-time doctrine; docs-health ANNOTATE may resolve them later. The first report (`06-31`) predates my ROADMAP/FEATURES fix — its (b)2 item is now done and could be annotated. | S |
| **My verification depth** | Presence, consistency, commit footers, eval gate — all verified | I did NOT re-review the research doc's full body during the work session (existence + commit shape only), and did not read the first session's status report until writing THIS one — which means I initially declared the task "fully complete" while the first report itself listed open follow-ups. Reading same-ID artifacts should have been step one. | S |
| **Aging metadata** | Decision date recorded | The "flagged since 2026-06-25" provenance lives only in FEATURES/ROADMAP rows now; the implementation TODO at line 171 carries no first-flagged date, so its aging clock starts at 2026-09-11. | S |

## c) NOT STARTED

1. **Implement the offsite Borg leg** — `platforms/nixos/system/backup.nix`, sops `borg_password`, port-23 SSH key provisioning, systemd timer, backup-coordination + Gatus registration, exclusion list. The single real remaining gap; fully specified in the blueprint. Blocked on: user StorageBox credentials (Q1).
2. **User-side bootstrap** — activate/verify BX11 service, upload SSH key (Hetzner web UI), `borg init`, passphrase into sops + a recovery copy (Q2).
3. **Sizing guard** — nothing measures the "irreplaceable set approaching 800 G" BX21-upgrade trigger the blueprint names.
4. **Restore drill + runbook** — blueprint covers backup only; an untested restore is Schrödinger's backup.
5. **ANNOTATE pass over the two same-ID status reports** (mark the first report's (b)/(c) items resolved inline) — optional, low value, listed for completeness.
6. **Section (f) harvest into TODO_LIST/ROADMAP** — deliberately not mass-inserted by either session (HARVEST's job with routing rigor).

## d) TOTALLY FUCKED UP

Nothing broke and no data is at risk from this session — but full honesty:

1. **The queue re-dispatched a completed item and I initially almost treated it as fresh work.** I found the prior closure only because I checked `git log` for the Task-Queue-ID early (which the AGENTS.md shared-tree rules pushed me to do). A worker who skipped that check would have re-made the "decision", clobbered the first session's richer closed line with a thinner one, and created a conflicting second closure. **The system survived because of process luck, not guardrails.** The queue (or the worker contract) needs an idempotency rule: grep git log for the Task-Queue-ID before touching anything.
2. **I have no work commit of my own for the closure** — my TQ_RESULT cited another session's commit (`09d481b0`) as the work commit. Honest and correct (committing nothing was right; empty commits are banned), but it means the queue's mechanical gate now cross-references a commit whose author session is not me. Intrinsic wrinkle of duplicate dispatch, worth recording so nobody reads the git history as one session's work.
3. **I verified presence, not provenance, on the first pass.** I confirmed artifacts existed and were consistent, but did not open the first session's status report until writing this one. Its (d) section lists process slips from the first session (edit-before-view rejected by tooling, provenance not traced with `git log -S`) — information that should have shaped my verification targets. Cost: my first verification pass was shallower than it could have been; the living-doc sweep gap sat open for ~20 extra minutes until this session.
4. **"Build and tests" verification for a docs-only task is inherently weak, and I should have said so more plainly in my work report.** `nix flake check` validates the flake, not the docs; nothing in the repo mechanically verifies that a decision record's content is TRUE (e.g. that BX11 was "already purchased"). I verified internal consistency (doc ↔ doc ↔ commit), which is the strongest check available for prose — but the purchase claim itself rests on the first session having had it right.
5. **Minor:** my first command batch put a long-running `nix flake check` in the foreground and it backgrounded unexpectedly; handled correctly (waited, read output), but the repo's documented hook stack (pre-commit runs the same check) made the latency foreseeable.

## e) WHAT WE SHOULD IMPROVE

1. **Queue dispatch must be idempotent per Task-Queue-ID.** Cheap rule: before doing the work, `git log --grep=<Task-Queue-ID>`; if hits exist, downgrade to verification + loop-closing (exactly what this session did, but by luck-of-process rather than contract). Would have saved one full session today.
2. **Queue items should anchor to canonical locations** (file + stable slug), not full-text snapshots — both dispatches of this item carried text that matched nothing in the tree.
3. **Same-ID artifacts are part of the input, not history.** When a Task-Queue-ID has a prior status report, reading it should be step one of verification — it carries the author's own gap list, which is the cheapest work-queue available.
4. **Decisions must atomically spawn implementation TODOs + sweep living docs** (first-session improvement #1, still unadopted as convention): one commit = decision + TODO + AGENTS/ROADMAP/FEATURES sync. Today it took two sessions and a re-dispatch to get there.
5. **Record first-flagged dates on TODO items** so aging survives rewrites (2026-06-25 provenance again).
6. **Mechanically catch stance contradictions**: a docs-health VERIFY check grepping TODO_LIST/ROADMAP/FEATURES status words (DECIDED/DEFERRED/DORMANT) against the docs they cite would have caught both the DEFERRED-vs-DECIDED split (first session) and the ROADMAP/FEATURES staleness (this session) automatically.
7. **When the Borg leg lands, wire monitoring from day one** (backup-coordination freshness, Gatus, `backup_ever_succeeded`, post-deploy smoke, restore drill) — the cv-backup silent-no-op and mail-relay phantom-green classes both came from monitoring added late.

## f) Up to 50 things to get done next

Grouped, impact-ordered. Items 1–24 carry forward unchanged from the first report for THIS task (still open — I re-verified TODO_LIST state this session); items marked *(noticed in passing)* are pre-existing items I saw in TODO_LIST (lines 159–183) and AGENTS.md during this session, listed for HARVEST, not researched further.

**The offsite leg itself (this task's follow-up):**

1. **User: supply StorageBox credentials + confirm BX11 active** — hard prerequisite for everything below.
2. **User: upload evo-x2 SSH public key to the StorageBox** (port 23 is Borg-only there).
3. **User: decide passphrase recovery placement** (sops operational copy + password-manager recovery copy).
4. Provision SSH connectivity (host alias, port 23, host-key pinning; extend `networking.nix` knownHosts if applicable).
5. Add `borg_password` to sops (`sops-key-audit.nix` enforces presence once declared).
6. Write `platforms/nixos/system/backup.nix` per the blueprint (wrapper, repo URL, compression, repokey-blake2).
7. Define the exclusion list as data, not prose — models (291 G), Steam blobs, llamacpp (92 G), HF caches out; home, services, pool backups, configs in. The sizing load-bearing artifact.
8. Decide Steam save-game handling (compatdata prefixes are the ONLY stateful Steam data — include saves, exclude game blobs).
9. Systemd timer + service (staggered into the 01:00–03:00 backup window; `Persistent=true`).
10. Register in `services.backup-coordination.backups.<name>` → feeds `backup_all_healthy`.
11. Gatus check (freshness/failure, Discord alert) — every backup producer gets monitored, cv-backup precedent.
12. `backup_ever_succeeded` integration — a never-worked repo must be loud, not invisible.
13. `ioTier` for the Borg unit (background tier — chunking reads are local NVMe work).
14. Pre/post-deploy smoke entries (repo reachable, last-archive age).
15. Restore runbook (`docs/services/` page): full restore + single-file + StorageBox access notes.
16. First-restore drill after go-live (small subset, timed).
17. Archive-size monitoring + BX21 upgrade alert at ~800 G irreplaceable set.
18. Retention/prune policy (dedup makes generous retention nearly free; decide before data accumulates).
19. Bandwidth/RPO note for the first seed upload over the home uplink.
20. `docs/services/` runbook page for the offsite leg.
21. After go-live: flip AGENTS.md "NOT yet deployed" flag + close TODO 171 + update FEATURES row.
22. Eval-time guard idea: backup producer without backup-coordination/Gatus registration should fail eval (service-completeness class).
23. (Done this session, listed for the record) living-doc sweep — ROADMAP/FEATURES synced.
24. (Queue mechanics) anchor future queue items to canonical locations; add the git-log idempotency check to the worker contract.

**Backup/3-2-1 adjacent — the bigger P0 underneath (noticed in passing, AGENTS.md):**

25. **/data EIO corruption repair** — btrbk-data has NEVER completed pool-side; until repaired the offsite leg inherits a hole in its source data.
26. **The owed reboot** — clears the flm :52626 corpse, the D-state llama-server pile, applies the zram 50% sizing; run `nix run .#pre-reboot-check` first (2026-09-07 stuck-boot class).
27. **Turso plan decision** — DiscordSync's cloud offsite copy stale since 2026-08-16 (`quota_exceeded` hourly).
28. `backup_ever_succeeded` metric + backup-catchup report (the detector item 12 rides).
29. btrbk receive-freshness + pool-snapshot freshness gauges in `backups.prom`.
30. ClickHouse telemetry backup decision (currently NO backup coverage; decide before next SigNoz upgrade).
31. `backups/pixel6` into btrbk-pool snapshot set + freshness registration.
32. `SIGNAL_RECOVERY_KEY.txt` placement (user: password manager, then delete).
33. Browser-history DB backup (folds into its pool migration, TODO_LIST:168).
34. Own-tools NVMe→pool migrations: discordsync + browser-history remain.
35. NOCOW for Docker pg volumes on /data (user-decided 2026-09-05, standalone change NOW — TODO_LIST:172).
36. deploy.sh backup retention (timestamped .bak, keep last 3).
37. Post-DAS-recovery convergence watch — confirm root incremental + `backup_all_healthy` green; bank-sync 9-day Wise gap backfill.
38. Service-completeness manifest audit (every enabled service ⇒ Gatus + Homepage + backup registration + docs page).
39. Eval-time audit: `Persistent=true` lint for backup timers (directly relevant to item 9).
40. Google Sync go-live remains USER-owned (DORMANT banner + 5-step checklist) — complementary mirror, not a 3-2-1 leg.

**Fill-out candidates 41–50 (noticed in passing, TODO_LIST:159–183 — ROADMAP fuel, not commitments):**

41. deploy.sh lock-wait on activation contention (poll/serialize instead of aborting post-build).
42. Flake-check guard: HTML `pat()` needles must start with `<`.
43. Derive AUTH_VHOSTS from caddy.nix (or eval-time assert).
44. Centralize curl in `scripts/lib.sh` `fetch()` helper.
45. Investigate Pocket ID SQLITE_BUSY restart bursts (recurring, never root-caused).
46. Identify residual post-deploy WARNs (null-byte line ~233; File Renamer "0 operations").
47. USB flap-counter metric + pre-deploy zombie-mount detector.
48. buildcache-gc observability metrics (`buildcache_gc_last_success_timestamp` + watermark-nuke alert).
49. post-deploy-check: poll browser-history /health + drop the explicit restart (4.5-min false-FAIL window obsolete).
50. Attic cache + CI token (`atticadm make-token`; push signoz-frontend + hermes uv2nix tree that CI rebuilds daily).

## g) Questions I cannot figure out myself (max 3 — carried forward, still open, still the blockers)

1. **StorageBox access:** is the BX11 service active, and what are the box hostname + username (uXXXX)? Without credentials the SSH-key upload and every implementation step stays blocked (the repo, key, and sops secret can otherwise all be prepared blind and converge later). *Tried: repo, blueprint, sops scaffold — nothing holds credentials; only the user can supply them.*
2. **Passphrase recovery policy:** where should the `borg_password` recovery copy live (password manager only, printed, split)? sops holds the operational copy; I need the policy — a wrong answer here is unrecoverable data loss on the first dead disk. *Tried: blueprint lists the open question; no repo policy exists.*
3. **Exclusion-policy exceptions:** is ANYTHING inside the "rebuildable" trees actually irreplaceable to you (Steam save games/Proton prefixes, personal fine-tunes, curated GGUFs that cannot be re-downloaded) that must be INCLUDED despite the 1 TB budget? This flips the exclusion list from assumption to fact before the first archive is written. *Tried: AGENTS.md confirms compatdata is stateful and NFS HPRE cloud status is unverified — only the user knows the value judgment.*

---

**Point-in-time snapshot.** Reflects 2026-09-11 06:50 and the shared-tree state of this session's commit. Stale by design tomorrow — use docs-health ANNOTATE to correct, never rewrite. Second report for this Task-Queue-ID; see also `docs/status/2026-09-11_06-31_task-000001a08ea43a364ec0910b47ffaaa9f1cb.md`.
