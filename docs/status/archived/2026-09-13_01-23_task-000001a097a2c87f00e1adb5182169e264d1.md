# Status Report — task 000001a097a2c87f00e1adb5182169e264d1 (InboxClean queue-ID remap, re-dispatch run)

- **Date:** 2026-09-13 01:23 CEST
- **Scope:** THIS task only — the reviewer finding "Wrong Task-Queue-ID footer" against commits `8605c6bd` / `005d1023` (the original InboxClean `main` OAuth re-consent work, first enqueued as `000001a0935cb9377ca805e017f476f13fe9`). Items from `TODO_LIST.md`/`AGENTS.md` noticed **in passing** are clearly marked; no unrelated research was done.
- **Format note:** the status-report skill defaults to styled HTML; the task instruction explicitly mandates `.md` for task-queue reports — honored. This is the second consecutive task-queue run with that override; if it is now the norm, the skill default should follow (see §e).

---

## Direct answers to the three headline questions

**What did I forget?**
Nothing inside the finding's required outcome — every mandated artifact exists and verifies (footer, greppable mapping, clean tree, gates green). What I missed *procedurally*: (1) my first edit attempt was fired before a `View` of the file (tool rejected it; I had only grep output — the rule exists precisely because grep output is not the file), costing one wasted round trip; (2) I did not anticipate the pre-commit hook's full `nix flake check` runtime, so the commit unexpectedly went to background (handled correctly via `job_output`, but predictable); (3) I noticed only afterward that the prior run of this same dispatch (d23e47ae) had *not* filed a status report, unlike the attic re-dispatch run (5298471b) — this report exists only because the queue re-ran the item.

**What could I have done better?**
Determined the *canonical-ID authority question* (§g Q1) instead of just fixing footers: the reviewer mandated `000001a0936f08…`, this dispatch mandates `000001a097a2c8…` — two different "canonical" IDs for one item within hours. I recorded the full chain (the correct minimal move) but did not flag the systemic cause loudly enough in the work commit message. Also: the work commit could have anticipated §f's harvest note.

**What could I still improve?**
Everything in §e, most importantly stopping the ID-churn class at the source (queue-side) rather than growing the remap line every re-dispatch, and treating `View`-before-edit as unconditional even when I "already have the content".

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | **Push-state verified before choosing fix path** — `git merge-base --is-ancestor` proves `8605c6bd` and `005d1023` are on `origin/master`, so rewording their footers would fork public history (agent force-push forbidden). This validates the reviewer's own fallback: record the mapping. | This session, commands logged below |
| 2 | **Prior-run fix verified, not duplicated** — found d23e47ae (the first run of this dispatch) already carried the mandated footer `Task-Queue-ID: 000001a097a2c87f00e1adb5182169e264d1` and the TODO_LIST remap note; did not re-do its work. | `git show d23e47ae` |
| 3 | **Mapping chain completed** — TODO_LIST.md line 25 remap note now records all three IDs ever used for this item (`000001a0935cb9377…` first-enqueue → `000001a0936f08db2ea47b01b7299ca8930e` reviewer-canonical → `000001a097a2c87f00e1adb5182169e264d1` re-dispatch). One-sentence change, no scope creep. | commit `b884c353` |
| 4 | **Commit carries the exact mandated footer as its final line** — `grep -c '^Task-Queue-ID: 000001a097a2c87f00e1adb5182169e264d1$'` on the message body = 1, and it is the last line. | `git log -1 --format=%B` |
| 5 | **Cross-reference resolvable for all three IDs** — `git log --grep` returns the remap commits for each ID (canonical ID: d23e47ae + b884c353; stale ID: 8605c6bd, 005d1023, d23e47ae, b884c353; re-dispatch ID: d23e47ae, b884c353). The finding's core complaint ("git log --grep for the mandated ID returns nothing") is dead. | grep outputs this session |
| 6 | **Repo gates green** — pre-commit ran gitleaks (pass), trailing-whitespace (pass), Nix flake check (pass). Tree clean after commit; nothing pushed (6 commits ahead of origin, per doctrine). | pre-commit output, `git status -sb` |
| 7 | **TQ_RESULT emitted for the work run** — `{"files_changed": ["TODO_LIST.md"], "commit_sha": "b884c353"}`. | prior message |

## b) PARTIALLY DONE

| Item | What works | What remains | Blocker | Effort |
|------|-----------|--------------|---------|--------|
| **Finding closure (this task)** | Mapping fully recorded in two consumable places (git log via commit messages/footers; TODO_LIST.md note) and verified greppable. | It is a *workaround by design*: the stale footers on pushed commits 8605c6bd/005d1023 persist forever. Closure now depends on the queue actually consuming the mapping (see §g Q2 — I cannot see the queue's matcher). | Queue semantics unknown to me | S |
| **Underlying InboxClean `main` OAuth re-consent** (the item this queue ID belongs to) | State re-verified 2026-09-12 by a prior run: `work` never revoked, actively refreshing; only `main` needs re-consent; cursor frozen at 5152620 since 2026-09-04. | Google Cloud Console consent-screen flip + browser re-consent + `sudo -u inboxclean` auth run — all human-only steps. PLUS the `gmail` tag demote PATCH failing since Sep 06 with `[rejection:paperless.client_error]` under the **connected** `work` account — possibly an independent Paperless-side bug the re-consent will NOT fix. | Human-only (USER) | USER-gated |

## c) NOT STARTED

Nothing remains unstarted *within this finding's scope*. From passing observations (not researched this session, flagged for HARVEST): queue-side stable item IDs, a per-dispatch status-report convention, and the whole USER-gated Priority-1 chain (InboxClean re-consent, Miniflux SSO flip, paperless decrypt go-live) — see §f.

## d) TOTALLY FUCKED UP

Radical honesty, scoped to what this session can actually claim:

1. **One work item, three queue IDs, three dispatch runs.** The finding I just closed is itself evidence of the systemic bug: the original run landed (8605c6bd/005d1023, stale ID), the reviewer re-enqueued under a new canonical ID (000001a0936f08…), a re-dispatch run fixed it (d23e47ae) but its completion apparently never reached the queue, so the queue re-ran the finding again under a *fourth-generation* ID (000001a097a2c8…) — this run. Severity: process-level waste (each duplicate costs a full agent run + a commit); no data or service damage. Root cause: the queue mints a fresh ID per re-enqueue and/or loses TQ_RESULT. Mitigation so far: the growing remap line — which is a Band-Aid that must be extended on every future re-enqueue (§e).
2. **Two "canonical" IDs within hours.** The reviewer's finding text mandates `000001a0936f08db2ea47b01b7299ca8930e`; this dispatch's contract mandates `000001a097a2c87f00e1adb5182169e264d1`. Both are now recorded, but which one the queue keys on is unknowable from inside the repo (§g Q1/Q2).
3. **Minor, self-caught:** premature `edit` without `View` (one rejected call). Fixed by following the rule; zero repo impact. Listed here because "small stupid things done anyway" belong in this section.

Not fucked up: the repository, the InboxClean service beyond its already-known BLOCKED state, git history (no rewrites attempted), and nothing user-facing.

## e) WHAT WE SHOULD IMPROVE

| # | Improvement | Impact | Concrete fix |
|---|-------------|--------|--------------|
| 1 | Queue mints a fresh ID per re-enqueue → unbounded remap chains and duplicate full runs | High (every re-dispatch of a reviewed item) | Queue-side: stable item ID across re-enqueues; supersede instead of re-mint. This is the root cause of this task existing twice. |
| 2 | Lost TQ_RESULT → whole finding re-runs | High | Queue-side: record/re-attach dispatch completions; a re-dispatch should carry "prior run state" in the prompt. |
| 3 | Dispatch prompt carries only the newest ID | Medium | Include the prior-ID chain (or a pointer to the remap note) in re-dispatch prompts so agents don't reconstruct it from finding text + git archaeology. |
| 4 | Remap knowledge is prose, not doctrine | Medium | One AGENTS.md line: "task-commit footers on PUSHED commits are immutable — verify pushed-state (`merge-base --is-ancestor`) before ever considering a reword; record old→new ID mappings in TODO_LIST.md + a remap commit." |
| 5 | Inconsistent status-report convention for re-dispatch runs | Low | Rule: every task-queue run files `docs/status/<ts>_task-<id>.md` (the attic run did; this item's first review-fix run didn't). |
| 6 | Skill default vs actual practice | Low | status-report skill: make `.md` the documented default for task-queue reports (HTML stays for interactive reviews) — this is the second override in a row. |
| 7 | My own execution | Low | Unconditional View-before-edit; pre-commit runtime budgeting (the hook runs full `nix flake check` — expect minutes, run commits in background deliberately). |

## f) Up to 50 things to get done next

Ranked by impact. **Source legend:** `[T]` = this task; `[TL]` = TODO_LIST.md line read in passing; `[A]` = AGENTS.md passage read in passing. Items beyond ~#25 are brainstorm/ROADMAP fuel per the status-report skill — docs-health HARVEST must route accordingly, and anything marked USER/agent-push-banned cannot be done by an agent session.

| # | Task | Impact | Effort | Category | Source |
|---|------|--------|--------|----------|--------|
| 1 | HARVEST this report's §f into TODO_LIST.md / ROADMAP.md (docs-health) | High | S | Documentation | [T] |
| 2 | USER: flip Google Cloud Console consent screen to "In production" (FIRST, before re-consent — tokens issued under Testing carry the 7-day bomb even after the flip) | Critical | S | USER-gated | [TL] |
| 3 | USER: browser Google login re-consent for InboxClean `main` (runbook in AGENTS InboxClean section) | Critical | M | USER-gated | [TL] |
| 4 | Post-consent agent verification: `journalctl -u inboxclean-sync` green + `/health` shows both accounts `connected` (token-PRESENCE caveat applies) | High | S | Verification | [TL] |
| 5 | Root-cause the `gmail` tag demote PATCH failure (`[rejection:paperless.client_error]` since Sep 06 under the CONNECTED `work` account) — re-check after re-consent | High | M | Bug | [TL] |
| 6 | USER: fill bank-statement PDF password in sops (`inboxclean-decrypt.yaml`) | Critical | S | USER-gated | [TL] |
| 7 | Land the retro-decrypt repair: upstream push + tag + `nix flake lock --update-input inboxclean` + deploy (deployed c65c797 lacks `--decrypt-repair`) | High | M | Deployment | [TL] |
| 8 | USER: run `--backfill --prune` + `--backfill --decrypt-repair` (dry-run first) per corrected runbook | High | S | USER-gated | [TL] |
| 9 | Deploy the pending Hermes cron-scheduler fix (live cron errors continue until next `nix run .#deploy`) | Critical | S | Deployment | [TL] |
| 10 | USER: Miniflux "Link your Pocket ID account" + one live SSO login, then `disableLocalAuth` flip + deploy | High | M | USER-gated | [TL] |
| 11 | Queue-side: stable item ID across re-enqueues (root fix for this task's duplicate-run class) | High | M | Process | [T] |
| 12 | Queue-side: record/re-attach TQ_RESULT so a lost completion doesn't re-dispatch | High | M | Process | [T] |
| 13 | Dispatch prompts: embed prior-ID chain for re-enqueued items | Medium | S | Process | [T] |
| 14 | USER: verify `larsartmann.cloud` in Resend (SPF/DKIM) → re-run the mail-relay test send (550-bounce class) | Critical | S | USER-gated | [A] |
| 15 | USER-timed: the OWED reboot — clears the flm corpse pinning :52626 (EADDRINUSE since 2026-09-07) | High | USER | Operations | [A] |
| 16 | Run `nix run .#pre-reboot-check` before #15 | High | S | Operations | [A] |
| 17 | Post-reboot: live-validate staged FastFlowLM v1.0.3 (`flm serve`); REVERT to v1.0.2 if NPU enumeration still fails | High | M | Verification | [A] |
| 18 | Post-bump: one-time Q4_K weight re-pull (hash-mismatch with v1.0.2 manifest is expected) | High | M | Operations | [A] |
| 19 | Implement Hetzner StorageBox BX11 + BorgBackup offsite backup leg (DECIDED 2026-09-11, blueprint exists, not deployed) | Critical | L | Feature | [A] |
| 20 | clickhouse-backup: telemetry DB currently has ZERO backup coverage (btrbk excludes XFS mount) | High | L | Feature | [A] |
| 21 | PMA upstream CI guard: assert PMA lock's go-commit rev ≥ 22f0e4c AND `OPENAI_BASE_URL` still read (the 09-07 regression class) | High | M | Bug prevention | [A] |
| 22 | InboxClean upstream: health probe should validate refresh validity, not token presence (the 8h phantom-green of 2026-09-04) | High | M | Upstream | [A] |
| 23 | InboxClean upstream: `invalid_grant` should surface a re-auth action instead of "transient" classification | Medium | M | Upstream | [A] |
| 24 | Memory-emergency-guard: corpse-aware socket-restore skip (P1 candidate — restore re-arms doomed starts until daily cap) | Medium | M | Bug | [A] |
| 25 | Eval-time guard for `StartLimitBurst`/`StartLimitIntervalSec` inside `serviceConfig` (silently-ignored class; audit clean but unguarded) | Medium | M | Quality | [A] |
| 26 | Gate browser-history `importUsers` CSV path with `MaxUsers` (only ungated creation path left) | Medium | M | Upstream | [A] |
| 27 | USER (sudo): delete dead QLC `@nix` subvol at `/mnt/btrfs-root` (TODO Phase 1 leftover from Samsung flip) | Medium | S | USER-gated | [A] |
| 28 | Deploy-verify pool SMART metrics + SigNoz dashboard (§10 metric presence + §13 flags) on next deploy | Medium | S | Verification | [TL-adjacent commit 64b6d7bb] |
| 29 | docs-health ANNOTATE: bring the stale reports citing the attic "deterministic RED" current (falsified 2026-09-13) | Medium | S | Documentation | [TL] |
| 30 | Cosmetic: `atticd-storage-dir` ReadWritePaths derive from `dirOf cfg.storagePath` (VM-test-only wart, documented in attic closure) | Low | S | Cleanup | [TL] |
| 31 | AGENTS.md one-liner: pushed task-commit footers are immutable; verify pushed-state before any reword consideration; mapping is the closure | Medium | S | Documentation | [T] |
| 32 | Convention: every re-dispatch run files a `docs/status/<ts>_task-<id>.md` report | Low | S | Process | [T] |
| 33 | status-report skill: document `.md` as the task-queue-run default (HTML for interactive reviews) | Low | S | Meta | [T] |
| 34 | Annotate the original task report `docs/status/2026-09-13_task-000001a0935cb9377….md` (created by 005d1023) with a pointer to the remap | Low | S | Documentation | [T] |
| 35 | USER decision: mapping-only closure for stale pushed footers permanently, or is a one-time user-run history rewrite ever on the table? (record outcome) | Low | S | Decision | [T] |
| 36 | If a 4th ID ever appears for this item: extend the existing TODO_LIST remap line — do NOT spawn a new note (one source of truth) | Medium | S | Process | [T] |
| 37 | Weekly: confirm `work` account stays green post production-flip (7-day bomb must not recur) | Medium | S | Operations | [A] |
| 38 | Post-InboxClean-fix E2E: confirm paperless archiving/decryption resumes (PAPERLESS consumer path + ledger) | High | S | Verification | [A] |
| 39 | After next daemon push: re-run the three `git log --grep` checks to confirm nothing reordered the remap commits | Low | S | Verification | [T] |
| 40 | Consider adopting dots' nix#3768 TunnelLogger-abort overlay ONLY IF the SIGABRT signature appears during an attic outage (conditional, pre-studied) | Low | M | Conditional | [A] |
| 41 | Re-evaluate Helium `--disable-gpu-watchdog` after the hotplug observation window (experiment in progress per AGENTS) | Low | S | Cleanup | [A] |
| 42 | disko as declarative disk-layout documentation + autoinstaller ISO (document-only, triggered by stuck-boot sagas) | Medium | L | Documentation | [A] |
| 43 | Evaluate Forgejo-mirror fallback for flake inputs (GitHub-outage immunity; tradeoff: builds need Forgejo up) | Low | M | Decision | [A] |
| 44 | Queue/process: after ANY queue-ID remap, grep prior status reports for the old ID and annotate (keeps old reports reachable) | Low | S | Process | [T] |
| 45 | Verify the 6-commit unpushed batch reaches origin via the usual pusher (agent push ban respected) — local-only remaps are invisible to the public queue otherwise | Medium | S | Operations | [T] |
| 46 | Optional tooling: tiny `scripts/task-id-audit.sh` that verifies every ID in TODO_LIST remap notes still `git log --grep`s | Low | S | Tooling | [T] |
| 47 | After #2/#3 land: re-verify the TODO item's verify-gate wording (health `connected` is token presence — use sync journal as truth) | Medium | S | Documentation | [TL] |
| 48 | Sweep TODO_LIST for other BLOCKED items whose blockers are actually agent-executable now (the queue-routing failure found in the original run's self-review) | Medium | M | Process | [T] |
| 49 | Record the "View-before-edit even with grep output in hand" lesson where future agents trip less (AGENTS shell/tooling section) | Low | S | Documentation | [T] |
| 50 | Consider queue feature: `--supersedes <id>` linkage so remap chains are queryable instead of prose-grown | Medium | L | Process | [T] |

**HARVEST note:** #1 is the harness for the rest — items 2–10, 14–21 are already-tracked TODO_LIST/AGENTS work (route to existing entries, don't duplicate); process items 11–13, 31–36, 44–46, 48–50 are new and mostly ROADMAP fuel until the queue owner weighs in (§g).

## g) Questions I cannot figure out myself

1. **Which ID is canonical going forward?** The reviewer's finding mandates `000001a0936f08db2ea47b01b7299ca8930e`; this dispatch's contract mandates `000001a097a2c87f00e1adb5182169e264d1`. Both are now recorded in the remap, but: is the re-dispatch ID the item's *final* identity, or will every re-enqueue mint another? I tried to answer from the finding text, the dispatch prompt, and git history — all three only show the chain, not the rule. The answer decides whether the remap line keeps growing and whether #11/#50 get built.
2. **How does the queue actually consume closure?** TQ_RESULT only, or also `git log --grep` / TODO_LIST? I verified all three channels resolve, but if the queue keys solely on TQ_RESULT commit_shas from *its own* dispatch, the mapping is documentation, not consumption — and the stale footers remain formally unresolved forever. Only the queue operator can say.
3. **Is a one-time, user-run history rewrite ever acceptable for wrong task footers on pushed commits?** Agent force-push is banned unconditionally (AGENTS), and I chose the mapping path — but if the answer is "yes, rarely, via the purge runbook pattern", the two stale footers could be made literally correct instead of mapped-around. Pure owner decision; I will not touch history either way without it.

---

## Evidence appendix (commands run this session)

```
git log --oneline -15 / git log origin/master..master --oneline / git status -sb
git show d23e47ae --stat + %B            # prior-run remap commit, footer = 000001a097a2c8…
git log -1 --format=%B 8605c6bd 005d1023 # stale footers confirmed
git merge-base --is-ancestor 8605c6bd origin/master   # pushed → reword forbidden
git merge-base --is-ancestor 005d1023 origin/master   # pushed → reword forbidden
# 3x: git log --grep <queue-id> --oneline (before: canonical ID → nothing on 8605c6bd era;
#      after fix: all three IDs resolve)
edit TODO_LIST.md line 25 (after View)   # + re-dispatch ID sentence
git commit -- TODO_LIST.md               # b884c353, pre-commit: gitleaks OK, nix flake check OK
git log -1 --format=%B | tail -n 1       # == "Task-Queue-ID: 000001a097a2c87f00e1adb5182169e264d1"
git status -sb                           # clean, ahead 6 (unpushed, per doctrine)
date                                     # 2026-09-13 01:23 CEST
```

Commits authored across this item's lifetime: `8605c6bd` (BLOCKED note, stale footer), `005d1023` (status report, stale footer), `d23e47ae` (remap, first run of this dispatch), `b884c353` (remap completion, this run).
