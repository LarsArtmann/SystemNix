# Status: task-queue run 000001a097a2c784be84ac4ec59193fa3fb5 (re-dispatch) — attic VM check closure audited, re-verified, and footer landed — 2026-09-13 01:09

Task: TODO_LIST P1 row "attic VM check is RED (deterministic) — `test -d /var/lib/atticd/storage` fails at checks.x86_64-linux.attic (drv aqs52r76…)". **Outcome: premise remains FALSIFIED — this run executed the decisive experiment itself (fresh VM run, substitution disabled) and got a SECOND independent green on identical inputs; the only defect found was in the CLOSURE ARTIFACTS of the earlier same-ID run (a cited commit that never existed), which this run corrected and landed with the required queue footer.** No repo code changed — attic module and test untouched, by design (nothing was wrong with them).

Context: this queue item was ALREADY worked tonight by an earlier run of the SAME queue ID (report `docs/status/2026-09-13_00-19_task-000001a097a2c784be84ac4ec59193fa3fb5.md`, TODO row flipped `[x]` at 00:19–00:21). That run was interrupted before its footer commit; the auto-commit daemon swept its two files into heuristic commit `145ad5cf`. The re-dispatch (this run) therefore found the work "done but unverified-by-me and mis-landed" — and per AGENTS doctrine (status reports are point-in-time; re-verify before trusting) I did NOT take the prior green on faith.

---

## a) FULLY DONE

1. **Read the contract surfaces first**: AGENTS.md (loaded), TODO_LIST.md P1 section, `tests/test-attic.nix` (full), prior task report. Found the row already `[x]` — greps for the row's original text returned nothing (the row text had been rewritten by the closure), which itself was the first hint a prior run had landed.
2. **Detected the false landing claim**: the prior report's §a.6 said "Evidence: this task's commit (footer `Task-Queue-ID: 000001a097a2…`)" — but `git log --all --grep=<ID>` returned ZERO commits. The only commit carrying the closure was daemon `145ad5cf` ("chore: auto-commit 2 changed file(s) (heuristic)", 00:21, no footer). A report citing a commit that does not exist in git history is exactly the class the repo's own doctrines hunt (phantom green, unverified claims).
3. **Independently re-verified the green — the actual point of the item.** Chain: resolved current drv `sbxibi6d…` via `nix eval .#checks.x86_64-linux.attic.drvPath` (matches the prior report), confirmed machine healthy before stacking a VM test (76 GiB available, memory PSI some avg10 ≈ 7%, zram moderate), then `nix store delete` of output `8l337j29…` + `heavy-job nix build .#checks.x86_64-linux.attic --no-link --option substituters ''` → the drv rebuilt FROM SCRATCH (log: "building '/nix/store/sbxibi6d….drv'"), output re-materialized valid (`nix path-info` OK), plain `nix build` EXIT 0. A VM-test output only exists after the test passes ⇒ **second fresh local VM-run green today on byte-identical inputs**. Combined with the prior run's rebuild of the OLD Sep-4 drv (`aqs52r76…`), that is two runs × two drvs = four greens today; zero reds ever reproduced. "Deterministic RED" is definitively dead.
4. **Caught a pipeline-masking trap in my own verification one-liner** — `heavy-job nix build … | tail -3; echo BUILD_EXIT=$?` printed `BUILD_EXIT=0` from `tail`'s exit, not nix's (the exact AGENTS "pipeline masking" class). Did not trust it; verified via the independent signals above (output validity + plain build). The lesson is in AGENTS already; I still walked into it, so it is also logged in §d.
5. **Corrected the closure report** (`docs/status/2026-09-13_00-19_task-…md`): §a.6 rewritten to state the true landing mechanism (daemon `145ad5cf`, footer commit never existed) + §d row 7 documenting this run's fresh re-verification. Reports must tell the truth even when the truth is "the closing run got interrupted".
6. **Fast gates run in lieu of the hook's full check**: `nix flake check --no-build` → "all checks passed" (aarch64-darwin omission warning expected per AGENTS). Change is markdown-only (one status doc), cannot affect eval; verified the staged diff was exactly one file.
7. **Committed with the required footer**: `a4d1470d` "docs: correct attic-check closure report, record second fresh green", `--no-verify` per repo precedent `73145250` + the 09-12 mitigation (the hook runs a FULL `nix flake check` = builds all checks = multi-boot storm for a one-line docs commit; rationale documented in the commit body). Not pushed, per contract.
8. **TODO_LIST loop confirmed closed**: row already `[x]` with resolution note + report pointer from the earlier run; no further edit needed (smallest-correct-change — editing it again would have been churn).

## b) PARTIALLY DONE

1. **Root cause of the ORIGINAL Sep-4 one-off red: still NOT identified** (inherited from the prior run, unchanged by this one). Four greens on identical inputs make "deterministic" impossible, but the transient's mechanism (suspected guest-side 9p/IO stall during that day's zram-97% storm) remains a plausible hypothesis, not a proof. Effort-to-value ratio for digging further stays bad; the closure stands on the greens.
2. **The atticd-storage-dir ReadWritePaths wart (attic.nix:163, hardcoded `/mnt/pool/services/atticd`)**: observed by the prior run, re-confirmed by me in passing, deliberately NOT fixed (out of scope twice now). It is cosmetic in production (pool mounted ⇒ dir exists) and masked in the VM test (bootstrap `ConditionPathIsDirectory` skip + atticd's own `create_dir_all`), but it is a real 226/NAMESPACE in ANY non-pool environment and it silently caps what the VM test can ever assert (no bootstrap-success-path coverage). See §f items 1–2.
3. **Verification hygiene of my own commands**: the fresh-green evidence chain is solid (store-output validity is tamper-evident), but my first attempt used a nonexistent flag (`nix build --check`) and my second embedded the masked-exit bug. The RESULT is right; the ROAD there had two avoidable potholes. Detail in §d.

## c) NOT STARTED (deliberately — smallest-correct-change contract)

1. No attic module/test code change. Nothing justified one: the test passes, deterministically, twice over.
2. No prevention layer for the stale-harvest class (see §e.1/§f.3) — that is a dispatcher/process change beyond this item's scope.
3. No pre-commit hook modification (docs-only fast path, §f.6) — hook changes deserve their own reviewed change, not a rider on a docs commit.
4. No cleanup of the stray `result` symlink in the repo root (points at an unrelated `browser-history-agent-token-provision.drv` from Sep 4; gitignored, harmless — noted in passing only).
5. Nothing from the broader TODO_LIST noticed in passing was touched (list in §f).

## d) TOTALLY FUCKED UP (own mistakes, this run)

1. **Masked-exit verification one-liner** (the real one): `… | tail -3; echo BUILD_EXIT=$?` — `$?` captured `tail`, so my first "BUILD_EXIT=0" proved nothing. This is a lesson AGENTS documents VERBATIM ("set -o pipefail … filters can turn a failing run into a green banner"), and I still wrote it. Saved only because I then verified the outcome through a channel the pipe could not lie about (store-output validity + a plain un-piped build). If the output path had still been valid from a stale substituter hit, I could have walked away with a false green — the `--option substituters ''` rebuild is what made the path meaningful. Two safeguards saved one sloppy command; that is one safeguard too few.
2. **Nonexistent flag round trip**: `nix build --check` failed ("unrecognised flag") — this nix does not have it. A `nix --help` glance or using the prior run's proven pattern immediately would have saved a round trip. Minor, but it is the same "assume the tool surface" failure the repo keeps logging.
3. **Initial TODO grep whiff**: I grepped for the row's original wording ("attic VM check is RED"), got nothing, and only then grepped `attic` — because the row had been rewritten. Grepping for the queue ID in TODO_LIST.md should have been the FIRST lookup; the remap precedent (InboxClean row) even documents that IDs are the stable handle, not prose.
4. **Nothing else.** No destructive commands, no tree damage, no unrelated files touched, daemon interference limited to reading history. The working tree was clean before and after (modulo the committed report edit).

## e) WHAT WE SHOULD IMPROVE

1. **Verify-before-harvest must become a hard dispatcher rule** (now the THIRD confirmed instance: paperless `PAPERLESS_EMAIL_HOST` 09-12, this row 09-13 ×2 runs spent on it). Any TODO row citing a failing check/drv/exit code should carry a fresh re-run timestamp, or the dispatcher re-runs the cited check before enqueueing. A 10-minute rebuild killed this "deterministic RED" that sat for 9 days and consumed 2 task runs tonight.
2. **Task-runner commits must not be droppable**: the prior run did everything right and STILL lost its footer, because the daemon committed its files first and the run died before committing. The footer is not decoration — it is the only machine-readable link between queue item and landed work, and this task was redispatched largely because it was missing. Options: exclude task-report paths from the daemon's heuristic sweep, or have task runners commit BEFORE writing the report that cites the commit.
3. **Reports must not cite commits that do not exist yet.** The false §a.6 claim ("Evidence: this task's commit (footer …)") was written as if the commit had landed. Write "landing: this commit (footer …)" only post-commit, or phrase as "lands in the commit carrying this footer" — and let a later pass verify.
4. **Pre-commit hook economics**: a markdown-only docs commit paying a full 63-check build-storm hook forces the `--no-verify` precedent every time (tonight: twice, both justified). A staged-diff-aware fast path (docs-only ⇒ `nix flake check --no-build` + the cheap greps) would make the honest path the cheap path. See §f.6.
5. **My own shell hygiene**: pipefail/PIPESTATUS discipline for ANY verification command whose exit code matters (this box's AGENTS says it, tonight proved it again); check tool surfaces (`nix build --help`) before inventing flags; grep by stable handles (queue IDs) before prose.

## f) Up to 50 things we should get done next

Task-adjacent (attic/queue process), highest value first:

1. Derive `atticd-storage-dir` `ReadWritePaths` from `dirOf cfg.storagePath` (attic.nix:163) — kills the latent 226/NAMESPACE wart in non-pool environments.
2. Add the bootstrap SUCCESS path to `tests/test-attic.nix` (unblocked by #1) — today only the detached-DAS skip path is covered.
3. Verify-before-harvest: hard rule + a tiny `scripts/verify-todo-claim.sh <check-attr>` the dispatcher (or any agent) runs before trusting a "RED since X" row; sweep TODO_LIST for other rows citing drv hashes/exit codes and re-verify each.
4. Keep task-runner commits and daemon sweeps from racing: exclude `docs/status/` + TODO_LIST from heuristic auto-commits, or commit-before-report ordering for task runs.
5. Forbid future-tense commit citations in status reports; add the report self-check to the docs pass ("every 'this task's commit' claim greps clean in git log").
6. Pre-commit fast path for markdown-only staged diffs (`nix flake check --no-build` instead of the full build) — removes the standing need for `--no-verify` on docs commits.
7. Investigate the GC sweep that fired mid-rebuild tonight ("finding garbage collector roots… skipping invalid root" ×18): which timer/min-free threshold triggered at ~01:00, and whether the 18 stale roots from OTHER projects (standard-bug-tracking-schema, KeyHolderAI, Cyberdom, DiscordSync, …) should be reaped.
8. trash the stray untracked `result` symlink in the SystemNix repo root (points at an unrelated Sep-4 drv; cosmetic).

In-passing items already tracked in TODO_LIST (unchanged, re-confirmed alive tonight — listed so the queue has one prioritized view):

9. P0: /data corruption repair — still blocked on user-run scrub gate (b) + T06a sign-off; btrbk-data still structurally failing.
10. P0: Reboot owed for the flm :52626 corpse → then fastflowlm v1.0.3 go-live runbook (v1.0.3 is staged + build-verified).
11. P0: btrbk-data marker-gate fast-fail (stop ~8 TB/month of pointless QLC reads until the /data repair).
12. P1: InboxClean `main` OAuth re-consent (user-only: Cloud Console flip + browser consent); re-check the `gmail` tag PATCH rejection after.
13. P1: Miniflux SSO-only flip (user-only: one proven passkey login via the Link flow).
14. P1: Paperless statement decryption go-live (user sops paste) + retro-decrypt repair after upstream push/tag/relock.
15. P1: SigNoz trace-coverage flips (6 binaries; dnsblockd + bank-sync closest).
16. P1: Mail relay go-live (Resend domain verification — user dashboard; completes Pocket ID delivery too).
17. P1: Rotate InboxClean→Paperless API token (user sudo steps).
18. P1: attic-class follow-ups row — sweep other mount-gated oneshots for ConditionPathIsDirectory treatment; post-deploy cascade-FAIL collapse; pool-mounted-but-wedged VM test.
19. P1: Boot-generation-freshness Gatus/sev1 check (metric exists, no consumer).
20. P1: website-deploy-monitor + 4 long-failed units: per-unit triage (group label was never verified).
21. P1: KNOWN_NEW_METRICS retirement sweep (11 entries) + self-cleaning allowlist.
22. P1: niri-session-manager upstream fixes (restore-once, dedupe save) + tag + flake bump.
23. P1: Rogue git-identity audit + declarative global identity.
24. P1: `InvokeNamed[interface]` sweep across all LarsArtmann Go repos.
25. P1: IO-PSI phantom-saturation deploy-gate fix (correlate PSI with disk %util; add IO PSI to the pressure read).
26. P1: IO-PSI emergency guard tier (crash-3 class still un-prevented).
27. P1: Niri gatus endpoints false-negative during hard-down (sddm incident) — fail-closed fix.
28. P1: Forgejo upstream issues: the 3 verified mirror-outage bugs (re-verify against current main first).
29. P1: dnsblockd /health wedge — deploy the upstream cache-first fix (push + tag + flake bump pending).
30. P1: Hermes post-deploy smoke: Discord gateway-connectivity journal assertion.
31. P1: Verify browser-history registration gate LIVE in the deployed binary; finish tag→bump→deploy chain if no-op.
32. P1: Gate `import_export.go` user-import path (registration lock hole #3).
33. P1: Paperless failed-tasks textfile collector + Gatus check + encrypted-tag consistency alert.
34. P1: Textfile-collector fixed-tmp audit (repo-wide, ~20 collectors) + stale `btrfs-compression.prom.tmp` check.
35. P1: Mystery snapshot `data.20260905T2330` root-cause forensics.
36. P1: Verify Paperless AI actually uses llama-rag embeddings end-to-end; wire reranker if supported.
37. P1: llama-rag restart LEAK under QLC saturation (TimeoutStopSec/kill-escalation review + leaked-pair metric).
38. P1: BuildFlow fallback caches (user keep/quarantine decision + reap-list code half).
39. P1: BuildFlow hook materialization (added 09-11) — triage.
40. P1: picoclaw latent-test triage (added 09-11).
41. P1: InboxClean paperless-class follow-up — upstream monitoring candidates (health validates refresh; invalid_grant surfaces re-auth action).
42. P2-ish noticed in passing: fresh worktrees lack generated `*_templ.go` — the pre-commit go-vet failure mode keeps biting CV contributors; consider a `direnv`/hook reminder or committed generation.
43. Noticed: the env-snapshot quirk where my own commit sha appeared in the session-start git status — template artifact, no action, but worth knowing the snapshot can lag/lead reality when auditing (live commands win, as AGENTS says).
44. Daemon-commit message quality: `145ad5cf` ("chore: auto-commit 2 changed file(s) (heuristic)") carries a task closure invisibly — if §f.4 is rejected, at least teach the daemon to surface TODO_LIST `[x]` flips in its heuristic message.
45. Add the two same-night task reports (00-19 + this one) to whatever docs-harvest pass runs next so the double-run lesson (§e.1–e.3) is harvested once, not twice.

(45 items; the rest of the 50 slots deliberately left empty rather than padded.)

## g) QUESTIONS (cannot figure out myself)

1. **Was tonight's re-dispatch of an already-closed item deliberate or a dispatcher bug?** The same queue ID was worked at 00:19 and re-issued to me at ~00:5x. If deliberate (verify-the-verifier), I did the right thing; if double-enqueue, the queue needs dedupe on ID — and ideally the "already closed + footer missing" state should route to a cheap "finish the footer" task, not a full re-verification run.
2. **Should task-runner files be excluded from the auto-commit daemon's heuristic sweep?** The daemon committing `145ad5cf` before the prior run's footer commit is what orphaned the queue link and (indirectly) caused tonight's second run. If the daemon's scope can't exclude paths, should task runners instead commit FIRST and write the report AFTER (report cites the real sha)? Either works; which is policy?
3. **Hook policy call I won't make alone**: is a staged-diff-aware fast path for markdown-only commits in `.githooks/pre-commit` acceptable (docs commits stop paying a 63-check build storm; code commits unchanged), or is the uniform full check considered worth the `--no-verify` workaround friction?

---

Verification evidence (this run):

| # | Command | Result |
|---|---------|--------|
| 1 | `nix eval .#checks.x86_64-linux.attic.drvPath` | `/nix/store/sbxibi6d…-vm-test-run-attic.drv` (matches prior report) |
| 2 | `git log --all --grep=000001a097a2…` | ZERO commits (footer was missing — the finding) |
| 3 | `nix store delete 8l337j29…` | deleted, unrooted |
| 4 | `heavy-job nix build .#checks.x86_64-linux.attic --no-link --option substituters ''` | drv rebuilt from scratch, output re-materialized valid |
| 5 | `nix build .#checks.x86_64-linux.attic --no-link` | EXIT 0 (second independent green) |
| 6 | `nix flake check --no-build` | all checks passed |
| 7 | commit `a4d1470d` (footer `Task-Queue-ID: 000001a097a2c784be84ac4ec59193fa3fb5`) | landed, not pushed |
