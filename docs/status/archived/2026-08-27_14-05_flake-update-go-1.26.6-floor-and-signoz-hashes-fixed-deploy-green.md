# Flake update fallout fixed: go 1.26.6 floor + signoz hashes, deploy green

Session: 2026-08-27 ~11:20-14:00. Input: the failed `nix run .#deploy` log
(6 root build failures after a full `nix flake update` moved nixpkgs
0816→0823 and every LarsArtmann input forward).

## Root causes and fixes

### 1. go >= 1.26.6 floor vs nixpkgs go 1.26.5 (4 packages)

The LarsArtmann ecosystem bumped go.mod floors to 1.26.6; nixpkgs (pinned
56c02bc AND unstable HEAD, checked) still ships `go_1_26 = 1.26.5`, and
`buildGoModule` pins `GOTOOLCHAIN=local` → every `-go-modules` FOD died with
`go: go.mod requires go >= 1.26.6`.

- **go-nix-helpers 58f7257** (pushed): new `goTarballVersion`/`goTarballHash`
  — go-standard option + mkGoFlake params building Go from the go.dev SOURCE
  tarball (CV's proven pattern, now a shared option). flake-level option
  because `goPkgOverride` needs `pkgs` which flake-level config lacks.
- **browser-history a426256**: inline `go_1_26.overrideAttrs` tarball go in
  perSystem (hand-rolled flake, CV-style). Floor is dep-driven (own go.mod
  stays 1.26.5; go-sse v0.5.1 needs 1.26.6).
- **papdashboard ee67d8e**: inline tarball go + vendorHash refresh.
- **PMA 8b983bb3**: `go-standard.goTarballVersion = "1.26.6"` + vendorHash
  refresh. NOTE: SystemNix-side FOD initially diverged from repo-local
  (`j0C+…` vs `bSz8…`) — the subtree had followed go-commit/nixpkgs to root;
  the (other session's) flake.nix de-follow landed mid-flight and after
  `--update-input projects-management-automation` resynced the subtree to
  PMA's own lock both sides converged (bSz8… green).
- **crush-daily fca4a9a**: mkGoFlake `goTarballVersion` + three upstream
  follow-ons the nix tests then exposed:
  1. `GET /docs/d2` double-registration panic — go-cqrs-lite's
     `RegisterRoutes` now mounts `/docs/d2` (viewer) + `/docs/d2.txt` (raw);
     removed crush-daily's leftover manual `docs.D2Handler()` mount (would
     have panicked the deployed binary at startup too).
  2. Golden `catalog.json` drift — regenerated. Repo-local `DUMP_CATALOG=1`
     regen is WRONG for this repo: local go.mod resolution differs from the
     nix prepared-source `_local_deps` (extracted the authoritative catalog
     from the nix build log instead).
  3. `ssetest.StreamReader` undefined — tests need go-sse ≥ d29f4ad; bumped
     crush-daily's own go-sse lock.

### 2. signoz hash mismatches (SystemNix-local)

`signoz-src` f69bce9 / `signoz-collector-src` d3bd524 moved:

- `_signoz-packages.nix` frontend `pnpmDeps` → `sha256-qnVJAi…`
- `collectorVendorHash` → `sha256-iCkb7I…`
  All four (signoz, frontend, otel-collector, schema-migrator) build green.

## Verification

- per-repo `nix build` green; SystemNix `nix flake check --no-build` green;
  full `nixosConfigurations.evo-x2` toplevel built green (i3hr3cri…).
- `nix run .#deploy` succeeded (after waiting out a concurrent session's
  activation holding the stc lock ~45 min). Post-deploy: 62 PASS / 9 FAIL /
  5 SKIP. The live generation (13:47, switched by the concurrent session
  from the same tree) runs papdashboard ee67d8e + crush-daily fca4a9a; both
  healthy (papdashboard `/api/health` 200 every cycle).

## Failures NOT caused by this work (pre-existing)

- **8 of 9 post-deploy FAILs are the DAS-outage class** (Immich ×2, Attic,
  Paperless, Bank-Sync ×4) — pool on the detached DAS since Aug 22; awaiting
  physical recovery per `docs/status/2026-08-27_11-48_*`.
- **dnsblockd :9090 stats API wedged since 12:49** (other session's deploy
  restart; docs-only rev delta, not a code regression): DNS on :53 fully
  healthy, every :9090 handler hangs (CLOSE-WAIT pileup; not fds/accept-loop;
  last log = batch-writer flush WARN then silence). Recovery:
  `sudo systemctl restart dnsblockd`. Root cause needs a SIGQUIT goroutine
  dump — suspect `healthProbe.Evaluate()`'s DB check or a shared tracking-DB
  lock. Also unexplained: deploy.sh's dnsblockd-restart block did not fire on
  two deploys despite the OIDC bridge being active(exited).
- emeet-pixyd :8090 scrape failing (seen in collector logs) — pre-existing,
  uninvestigated.

## Drop-when list (when `nix eval nixpkgs#go_1_26.version` ≥ 1.26.6)

Remove: browser-history + papdashboard + crush-daily inline flake overrides,
PMA `goTarballVersion`/`goTarballHash`, CV's inline override.
