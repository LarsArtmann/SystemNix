# Status: task-queue run 000001a0981cd729157b22ac6724d6f6b34d — reviewer finding on the attic closure fixed (false "byte-identical inputs" claim retracted, evidence re-based) — 2026-09-13 02:29

Task: code-review rejection of commit `a4d1470d` (the attic "deterministic RED falsified" closure). One finding (medium): the closure evidence in `docs/status/2026-09-13_00-19_task-…3fb5.md` §a.4 and TODO_LIST row 28 claimed "semantically identical / byte-identical inputs" between the Sep-4 red drv and the current-tree green — unsupported and partly false. **Outcome: every reviewer claim verified against primary sources, §a.4 + TODO_LIST row rewritten to rest the falsification on valid evidence only, verdict unchanged (premise still falsified), committed as `9e972a5f`. No code touched.**

---

## a) FULLY DONE

1. **Reviewer claim (a) verified — machine-config identity is unprovable from the top-level diff.** `nix derivation show` on both top-level drvs (`aqs52r76…` old vs `sbxibi6d…` current): env diff is ONLY the `out=` path and the `nixos-test-driver-attic` store path embedded in `buildCommand` (`css3wv9r…` old vs `87cksqlv…` new; there is no separate `driver` env key). `inputSrcs` is EMPTY on both inner driver drvs — the machine systems hang behind `driverConfiguration.json.drv` and the driver drv itself, and the old driver copy's closure is GC'd, so nothing can prove the machine configs were identical. Reviewer right, pass-1 claim unsupported.
2. **Reviewer claim (a+) strengthened — the inner driver drvs PROVABLY differ.** Both inner `.drv` FILES survive (`pazpdhpam9sp…` old, `y865lzkn84zl…` current) even though their closures are GC'd. Diffing the ATerms: env KEYS identical; differing env VALUES = `buildInputs`, `nativeBuildInputs` (`ty` 0.0.75→0.0.77, `nixos-test-driver-1.1` path, make-shell-wrapper-hook chain), `out`, and the embedded generated test-script text; differing `inputDrvs` = ruff 0.16.4→0.16.5, ty, nixos-test-driver-1.1, driverConfiguration.json. "Semantically identical" is false at the drv level, full stop.
3. **Reviewer claim (b) verified — nixpkgs moved between red and green.** `git show 094d383d:flake.lock` pins `34ab99075…`; current tree pins `c043004d1…` (walked `.nodes[root.inputs.nixpkgs]` — the node-key-mapping gotcha). This also exposed a SECOND pass-1 error: the old §a.4 parenthetical claimed "the nixpkgs pin at the red observation (`e5bdc4a`)" — wrong, `e5bdc4a` was the mid-August pin; the Sep-4 lock was `34ab99075`. Retracted in the rewrite.
4. **Reviewer claim (c) verified — attic.nix's atticd-metrics unit changed post-red.** `c75c9261` (2026-09-06 03:09): mktemp unique-tmp + `CAP_FOWNER` in the atticd-metrics collector (+12/−1); `tests/test-attic.nix:72` starts `atticd-metrics.service`, so the changed unit is in the tested surface.
5. **Status doc corrected in place (3 spots).** §a.4 fully rewritten under a `CORRECTED after review (2026-09-13, second pass)` marker: falsification now rests ONLY on (i) the verbatim red-drv rebuild passing (alone kills "deterministic"), (ii) the provably-differing inner driver drvs, (iii) moved inputs (nixpkgs bump + atticd-metrics change); current-tree greens demoted to corroboration on moved inputs. §a.5's "One red + N greens on identical inputs" and §d row 5 fixed to match.
6. **TODO_LIST row 28 corrected (2 spots, stays `[x]`).** The byte-identical claim replaced with the valid evidence chain; the false "Nothing changed attic-side between red and green" + wrong `e5bdc4a` pin sentence replaced with what ACTUALLY did not change (`tests/test-attic.nix` since `84965dca`, attic.nix storage/bootstrap/atticd units since `fbc60ed5`). Verdict and row status unchanged.
7. **Gates green.** `nix flake check --no-build` → all checks passed (aarch64-darwin omission warning expected per AGENTS). The three `.nix` files mentioning TODO_LIST (snapshots.nix, configuration.nix, home.nix) re-checked: comments/strings only, no eval-time read. gitleaks clean.
8. **Committed with footer despite the auto-commit daemon winning the race.** My commit hit "nothing to commit" — the daemon had swept the two files into heuristic `bc20a4d9` seconds earlier. `git show --stat` confirmed that commit contained EXACTLY my two files (4 insertions/4 deletions, nothing from any other session), so a MESSAGE-ONLY amend was safe: `9e972a5f` "docs: correct attic closure evidence - inputs moved, not byte-identical" + `Task-Queue-ID: 000001a0981cd729157b22ac6724d6f6b34d`. Hooks re-ran green on the amend. Not pushed.

## b) PARTIALLY DONE

1. **Second-pass traceability lives only in the commit footer.** The corrected doc names "review (2026-09-13, second pass)" but does not cite the second queue ID (`…f6b34d`) inline — a one-line follow-up if the queue cross-ref matters inside the doc body (the filename + `git log` already carry it).
2. **The evidence's durability is unchanged (still luck).** The old-drv outputs (`wnid5dhy…`, `8l337j29…`) were built `--no-link` (no gcroot) and the inner driver ATerms survive GC only because nothing collected them yet. The corrected doc cites them; a future GC could make re-audit impossible. Not addressed (policy decision, see g.1).

## c) NOT STARTED (deliberately — smallest-correct-change contract)

1. **Annotating the three earlier reports that carried the stale attic red** (`2026-09-06_00-31` §18, `2026-09-06_02-33` §25, `2026-09-12_00-59` §b.2/f.6) — they still assert a red that this closure falsified. ANNOTATE-mode candidate, needs an owner decision on archived-report conventions (g.2).
2. **Any repo-wide sweep for the claim class** — other status docs may carry drv/pin "identity" claims with the same unprovenanced shape as pass-1 §a.4. Not started.
3. **The pre-existing attic rows** (atticd-storage-dir ReadWritePaths wart, verify-before-harvest dispatcher rule, docs-only pre-commit fast path) — untouched, already tracked in TODO_LIST rows 337/315/317.
4. **Re-running the decisive VM experiments this pass** — not needed: the finding disputed the doc's EVIDENCE CLAIMS, not the experiment outcomes, which the reviewer independently reproduced ("only driver path + out path differ" — matches).

## d) TOTALLY FUCKED UP

1. **The root sin happened in THIS task's first pass (`a4d1470d`): an identity claim made without reading the identity-bearing artifact.** "Machine config, test script, atticd package, and nixpkgs-atticd module inputs are byte-identical" was inferred from a TOP-LEVEL env diff, while the inner driver ATerms — which settle (and refute) the question — were sitting in `/nix/store`, diffable in seconds, no sudo, no GC recovery needed. The expensive experiments (VM rebuilds) were done; the cheap decisive one (ATerm diff) was skipped. That inversion is the fuckup, and it produced a status doc whose central sentence was false within one review pass.
2. **A second unverified factual claim rode in the same sentence:** the `e5bdc4a` "pin at the red observation" — actually the mid-August pin; the Sep-4 lock was `34ab99075`. One `git show <rev>:flake.lock` would have caught it. Two fabrication-class errors in one paragraph is how a reviewer stops trusting a report wholesale.
3. **This pass: ~5 wasted jq invocations** guessing the `nix derivation show` JSON shape (assumed an array; it is an object keyed by BARE drv filename; this box's jq also lacks `-S`). Cost minutes, no damage — but the shape should have been inspected (`jq 'keys'`) before querying.
4. **This pass: lost the commit race to the auto-commit daemon and only caught it from exit 1.** Had the daemon's sweep MIXED my files with another session's, a naive message-amend would have hijacked foreign work into my footer. The `git show --stat` exclusivity check saved it — but the check should have been part of the plan (AGENTS.md documents the daemon; AGENTS.md documents the pathspec-commit rule; the daemon beating a pathspec commit to the punch is the one variant neither rule covers).

## e) WHAT WE SHOULD IMPROVE

1. **Evidence-provenance convention for closure reports:** every identity/equivalence claim must name the artifact actually read (top-level env / inner ATerm / closure) and its scope. "Identical" requires closure-level evidence — or an explicit "unprovable, GC'd" label. The current docs have no such rule; AGENTS.md has secrets-doctrine but nothing for factual-claim doctrine in markdown.
2. **ATerm-first rule for drv-equivalence arguments:** `.drv` files survive closure GC and parse in milliseconds — `nix derivation show` diff should be step 1 of ANY "same inputs?" question, before any build. Encode in AGENTS.md (Nix gotchas) + docs/CONTRIBUTING.md.
3. **Pin-claim discipline:** any "the lock pinned X at time T" claim gets verified against `git show <rev>:flake.lock` before writing. The `e5bdc4a` error was a memory-sourced pin claim in a doc that cites drv hashes to 32 chars — precision theater without verification.
4. **Evidence durability policy:** when a closure verdict rests on drv evidence, gcroot the outputs and/or copy the ATerms into docs/evidence/. Today nothing pins them; the corrected report's citations have a shelf life.
5. **Queue-commit discipline vs the daemon:** write files → commit IMMEDIATELY (the daemon races in seconds); on a sweep, `git show --stat` exclusivity check BEFORE any message-amend. Both belong in the task-queue runbook, not in tribal memory.
6. **A cheap tripwire for the phrase class:** grep gate (pre-commit docs path or CI) flagging "byte-identical"/"semantically identical"/"identical inputs" in docs/status without an adjacent diff transcript or provenance note. Two instances of this class exist already (paperless fixture-trap, this).
7. **Record the tool facts learned (memory maintenance):** derivation-show JSON shape on this box (object keyed by bare filename, no `-S` in jq), test-driver drv anatomy (inputSrcs empty; machine systems behind driverConfiguration.json), inner ATerms survive GC.

## f) Things to get done next (brainstorm list — most are ROADMAP fuel, not commitments)

1. Annotate the three earlier reports carrying the stale attic red inline (2026-09-06_00-31 §18, 2026-09-06_02-33 §25, 2026-09-12_00-59 §b.2/f.6).
2. Repo-wide grep of docs/status for "byte-identical"/"semantically identical"/"identical inputs" — audit for other unprovenanced equivalence claims.
3. Same sweep for pin claims ("pin at the red", "the lock pinned") — spot-check against `git show <rev>:flake.lock`.
4. Add the evidence-provenance paragraph to docs/CONTRIBUTING.md status-report conventions.
5. Add the ATerm-first drv-equivalence rule to AGENTS.md (Nix & Nixpkgs gotchas).
6. Build the cheap grep tripwire for unprovenanced identity claims in docs/status (pre-commit or CI).
7. atticd-storage-dir: derive `ReadWritePaths` from `cfg.storagePath` (TODO row 337).
8. VM test exercising the attic bootstrap SUCCESS path (depends on 7).
9. Fold the ReadWritePaths wart into the "Attic-class follow-ups" row as planned (row 42).
10. Decide + implement evidence durability: gcroot `wnid5dhy…`/`8l337j29…` or copy driver ATerms (`pazpdhpa…`/`y865lzkn…`) into docs/evidence/ (g.1).
11. Re-run the verbatim old-drv rebuild once on a quiet machine to re-confirm the chain the corrected doc rests on.
12. Run the FULL `nix flake check` (all 63 checks, with builds) once off-peak — the known-red excuse is gone; confirm the pre-commit hook path is truly green end-to-end.
13. Post-reboot (the one owed on evo-x2): re-verify `checks.x86_64-linux.attic` green in the fresh generation.
14. Record the test-driver drv anatomy facts in AGENTS.md (inputSrcs empty, machine systems behind driverConfiguration.json, ATerms survive closure GC).
15. Record the jq/derivation-show query pattern in AGENTS.md Shell section.
16. Queue-runner rule (row 315): fresh re-run required before any "red since DATE" carry — keep pushing for the hard-gate version (g.3).
17. Docs-only pre-commit fast path (row 317) — ends the `--no-verify` habit.
18. Add second-pass traceability line (queue ID `…f6b34d`) inside the 00-19 doc's CORRECTED marker.
19. Watch the next daemon cycle: confirm the message-amend of `bc20a4d9`→`9e972a5f` doesn't confuse the daemon's bookkeeping (duplicate/missed commits).
20. Note in AGENTS.md that the attic check row is CLOSED (green re-verified 2026-09-13) so future harvests don't resurrect it from archived reports.
21. Route this report's (f) list through docs-health HARVEST with extra rigor — most items are ROADMAP, only 7/9/16/17 are TODO_LIST-grade.
22. Consider (and likely reject as YAGNI) a `scripts/drv-atterm-diff.sh` helper — decide deliberately, don't let it grow.
23. Verify no OTHER flake checks are currently red (the pass-1 session only proved attic; a one-off full check result table would catch the next stale row before dispatch).
24. When nixpkgs re-locks past `c043004d1`, the current-tree drv name in the corrected doc becomes historical — acceptable (point-in-time); no action, listed so nobody "fixes" it later.
25. Encode the expectation that nixpkgs bumps change the test-driver drv (ty/ruff churn) into the provenance convention — any future "identical test drv across a bump" claim is DOA.
26. Add the `git show <rev>:flake.lock` pin-verification step to the closure-report checklist (docs-health VERIFY input).
27. Decide whether archived status reports should be excluded from HARVEST carries without a fresh re-run — both phantom rows (paperless, attic) came from archived docs.
28. Capture `nix derivation show` transcripts for the current tree's attic check into docs/evidence/ as durable provenance for the corrected claims.
29. Follow up pass-1 §g.1 (dispatcher hard rule) with the queue owner — it is still the actual 80/20 for this bug class.
30. Post-reboot sweep: after the owed reboot, re-run the pre-deploy smoke to retire the flm corpse-era baseline FAILs from the smoke output.
31. Confirm the TODO_LIST row-28 edit reads coherently in the rendered markdown (nested backticks inside a long row) — cosmetic, on next touch of the file.
32. Keep the "land the replacement before removing the old source" doctrine in mind for the doc: the corrected §a.4 REPLACED the false claim in place — if the team prefers additive corrections (append-only) for status docs, say so in g.2 and I will convert.

## g) QUESTIONS (cannot figure out myself)

1. **Should drv-evidence be made durable when a closure verdict rests on it** (gcroot the VM-test outputs and/or commit the driver ATerms into docs/evidence/), or is impermanent evidence acceptable once a row is closed? This decides item f.10/f.28 and whether future sessions can re-audit the attic closure at all after the next GC cycles.
2. **Correct-in-place vs append-only for status docs:** I corrected §a.4 in place under a CORRECTED marker (mirroring the doc's own LANDING CORRECTION pattern). If the repo convention for PUBLISHED status reports is append-only annotation instead, I should convert. Which is canonical for (a) the live doc and (b) the three archived reports carrying the stale red?
3. **Is the verify-before-harvest rule (TODO row 315) meant to be a hard dispatcher gate or a convention?** Two consecutive phantom rows (paperless PAPERLESS_EMAIL_HOST, attic deterministic-RED) both died by a 10-minute re-verification that was never run; I recommended hard-gate in pass-1 §g.1 and still do — but it changes the queue tooling, so it needs an owner decision.

---

Verification transcript (this pass): `nix derivation show` diffs of aqs52r76/sbxibi6d and pazpdhpa/y865lzkn (see §a.1-2); `git show 094d383d:flake.lock` → `34ab99075…`, HEAD lock → `c043004d1…` (§a.3); `git show c75c9261` attic.nix mktemp+CAP_FOWNER (§a.4); `tests/test-attic.nix:72` (§a.4); `nix flake check --no-build` all passed; commit `9e972a5f` with queue footer, hooks green, not pushed.
