# Task 000001a0b7c158b007a49f73a85955717453 — Status Report (fourth dispatch)

> **[docs-health 2026-09-19] RESOLVED + ARCHIVED:** fourth dispatch — all agent-verifiable preconditions cleared; purge executes on the next deploy. Full five-dispatch chain tracked in docs/todo/services.md (probe-user item).


**Date:** 2026-09-19 05:52 CEST
**Work item:** TODO_LIST P1 — "Delete the browser-history probe user `probe-gate@example.com`" (fourth dispatch; 03-49 BLOCKED root-gated, 04:57 mechanism-verified, 05:19 BLOCKED deploy-gated, 05:28 third-dispatch BLOCKED same wall)
**Outcome:** **BLOCKED — unchanged wall: the purge executes only on a privileged deploy.** ~~agent-side preconditions open~~ ALL cleared — fires on the next `nix run .#deploy` (chain tracked in docs/todo/services.md). This run's delta: the LAST open agent-verifiable precondition (third-dispatch report §f.3) is now CLEARED — the probe-purge fixture check is build-verified at HEAD after the post-04:57 flake churn. The next privileged `nix run .#deploy` remains the sole trigger; the close-out is unchanged.

## Verified this run (05:42–05:52, all first-hand)

| Signal                                                | Result                                                                                                                                                                                                  |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `browser_history_user_count` (127.0.0.1:8087/metrics) | `0` — pre-probe value holding                                                                                                                                                                           |
| Server process                                        | PID 7177, started **Fri Sep 18 19:31:55 2026** (`ps -o lstart` + `process_start_time_seconds 1.78975271574e+09`) — NO restart since the 04:32 mechanism landed                                          |
| `journalctl -u browser-history` since 04:25           | ZERO unit restarts; `--grep "purged probe registration"` → no match (rc=1)                                                                                                                              |
| Deployed generation                                   | `/run/current-system` → system-785 (`…20260916.b1b8759`), pre-mechanism; deployed unit carries **0** `probeRegistrationPurge` references (a manual restart of the OLD generation would still not purge) |
| Deploy in flight                                      | None — `/tmp/.systemnix-deploy.lock` holds the same stale PID (3114005); no `nh os switch` process                                                                                                      |

## The delta: fixture build insurance CLOSED (§f.3 of the 05:28 report)

`nix build .#checks.x86_64-linux.browser-history-probe-purge-fixture` → **green at HEAD** (out-path `3dviwvybrga2…`; a check derivation printing an out-path proves its script ran to the final `echo PASS > $out`). Why it was owed: the 04:57 build-verification predates auto-commit `35e1a3d8`, which rewrote the fixture's marker assertions from `ls`-globs to a nullglob-safe `find` helper (the 2026-08-27 nullglob phantom-verdict class). That churn is now build-covered. No further agent-side preconditions exist — every remaining step of the close-out needs privilege.

## Not done / unchanged

- The purge execution, the `[x]` flip, and the `probeRegistrationCleanup` block removal — all gated on the next deploy (close-out: 04:57 NEXT VERIFIER in TODO line 42). Removing the block BEFORE the verified purge would orphan the mechanism unexecuted forever (third-dispatch §c.2).
- "Fresh registration probe still 403s" — deferred-by-safety until the count-gap fix (TODO line 43); at count 0 with `MAX_USERS=1` a probe would 201 and mint new debris.
- Sandbox capabilities re-confirmed unchanged: `sudo`/`systemctl` tool-blocked; `nix build`, `journalctl`, local HTTP work.

_Fourth dispatch, point-in-time 05:52 CEST. Mechanism attribution: parallel session `2c2a860c` (04:32); fixture hardening `35e1a3d8` (04:39). Not pushed._
