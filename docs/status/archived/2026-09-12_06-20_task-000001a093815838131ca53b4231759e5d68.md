# Status Report — Task `000001a093815838131ca53b4231759e5d68` (full self-review)

**Paperless statement decryption go-live + duplicate cleanup + retro-decrypt repair — verification run, third runbook trap found and fixed**

- **Written:** 2026-09-12 06:20 CEST, autonomous tq-pool agent session
- **Supersedes as the canonical report:** `docs/status/2026-09-12_06-15_task-000001a093815838131ca53b4231759e5d68.md` (the first, summary-only report of this run — kept for the work-commit cross-reference; the 04-31/04-37 pair for task `…36f08bb` sets the two-files-per-task precedent)
- **Work commit:** `8d7c1ede` (amended daemon commit carrying the Task-Queue-ID footer; the three doc edits rode daemon commit `3963d0ae` immediately below it, single-change verified); report commit: this one
- **Verdict: BLOCKED (unchanged) — verification COMPLETE, runbook defect #3 found + fixed.** The engineering itself was landed by an earlier run of this same queue ID (InboxClean `7aa5c3f` carries this exact Task-Queue-ID); this run re-verified every claim against reality and executed the queue-mandated runbook test, which FAILED and produced a real fix.

---

## 0. Verified live state (2026-09-12 06:00–06:15 CEST)

| Signal | Value | Source |
| --- | --- | --- |
| Upstream engineering | `--backfill --decrypt-repair` fully implemented; `7aa5c3f` is ancestor of InboxClean HEAD `cb3496c` | `git merge-base --is-ancestor`, `cmd/inboxclean/paperless_command.go` |
| Engineering authorship | `7aa5c3f` commit message carries **this run's Task-Queue-ID** | `git show 7aa5c3f` |
| Push state | InboxClean origin 10+ commits behind master (push agent-banned) | `git log origin/master..master` |
| SystemNix lock | `inboxclean` rev still `c65c7973…` (deployed build lacks the flag) | `jq '.nodes[.nodes.root.inputs.inboxclean].locked'` |
| Runbook traps #1-2 (prior run) | `INBOXCLEAN_CONFIG` + unit `PATH` extraction — present and correct | `docs/services/paperless.md` |
| Deployed unit env | `LLM_PROVIDER=ollama`, `GMAIL_CREDENTIALS_FILE`, `GMAIL_TOKEN_FILE`, `INBOXCLEAN_CONFIG`, `DB_PATH` all present as unit `Environment=` lines; qpdf only on the unit PATH | `/etc/systemd/system/inboxclean-sync.service` |
| **Runbook env reconstruction** | **BROKEN** — missing `LLM_PROVIDER` (CLI dies `config.api_key_required` before dispatch), `INBOXCLEAN_TOKEN_FILE` is not a knob (zero source hits) | differential test vs deployed CLI + source grep |
| Corrected reconstruction | Passes the config gate; stops exactly at `config.paperless_url_without_token` (the root-owned token the user's sudo grep supplies) | live dispatch test 06:11 |

---

## a) FULLY DONE

1. **Contract compliance, end to end:** AGENTS.md read first; smallest-change discipline (docs-only in SystemNix, zero code touched anywhere); never pushed; `.crushrc`/`crush.json`/`.tq-verify` untouched; Task-Queue-ID footer exact; repo gates green (`nix flake check --no-build` all-pass, gitleaks + flake-check hooks green on the commit).
2. **Every prior-run claim re-verified before being trusted** (§0 table) — per the "status reports are point-in-time" doctrine. All four checkable claims held; the fifth (runbook correctness) did not.
3. **The queue's explicit instruction — "TEST the runbook env reconstruction first" — was actually executed against the deployed binary**, and it caught a defect neither the prior run nor two prior reports had seen: the CLI's global config gate validates LLM provider config BEFORE command dispatch. Without `LLM_PROVIDER` the default is `openai` (`internal/config/config.go` `defaultLLMProvider`), `OPENAI_API_KEY` is empty after `sudo` env scrubbing, and the CLI exits `config.api_key_required` — the user's prune run would have died before touching paperless.
4. **Differential proof, not inference:** no-env `--help` → hard fail; `LLM_PROVIDER=ollama inboxclean paperless --help` → help prints; corrected full reconstruction → gate passes, fails only at the missing root-owned `PAPERLESS_TOKEN` (the exact variable the user's run supplies). Three-run escalation isolates the gate precisely.
5. **Second defect in the same block found at source level:** `INBOXCLEAN_TOKEN_FILE` appears NOWHERE in the InboxClean Go source — an invented knob. The real ones are `GMAIL_CREDENTIALS_FILE`/`GMAIL_TOKEN_FILE` (both in the deployed unit); the main account's client for `--decrypt-repair` source re-fetch is built from `cfg.Gmail` (`cmd/inboxclean/main.go:604` → `gmail.NewClient(ctx, accounts[0].CredentialsFile, accounts[0].TokenFile)`); the accounts TOML covers only the extra `work` account.
6. **Fix landed in all three live doc surfaces atomically:** runbook command corrected (five-var unit extraction, inert line removed, per-var rationale in the comment), AGENTS.md retro-repair bullet extended, TODO_LIST blocked line updated with the re-verification + report pointer.
7. **Sequencing decision made and documented:** the interim `git+file?rev=` pin + live deploy (go-taskqueue precedent) was considered and REJECTED — the repair cannot run until the user fills the sops password anyway, so a live deploy buys nothing today while adding deploy churn + a later flip-back. The prior run's push-first sequencing stands.
8. **Daemon-commit race handled per today's precedent:** both daemon commits verified single-change via `--stat` before amending the report-only HEAD with the proper message + footer; the sibling doc-edit commit cross-referenced in the body so the queue ID resolves to the full change set.
9. **Report discipline (this file):** full self-review format, point-in-time snapshot, honest stop.

## b) PARTIALLY DONE

1. **The work item itself** — all three parts end in user/upstream hands (sops password; live sudo runs; upstream push). From the queue's perspective this run is a blocked outcome with the runbook made actually-executable, not a completion.
2. **The "full suite green + lint 0" upstream claim was NOT re-run** — re-running InboxClean's suite hours after the authoring run would burn a build+test cycle for a claim that CI/pre-deploy gates re-prove at relock time anyway. Verified instead: feature presence, scope, spec, and CHANGELOG at source level. Residual risk exists only between now and the relock.
3. **The sops PLACEHOLDER claim is untestable by me** — the value is root-readable only (`sudo` banned); the "still placeholder" status rests on the TODO's own record and the absence of any go-live signal. Untested, disclosed.
4. **The two duplicate documents were not independently re-enumerated** — the paperless API and the ledger DB both sit behind root-owned auth/files. The dry-run preview (which the user runs first) is the designed enumeration surface; my verification chain stops one step short of the data.
5. **Verification depth on the gate:** I proved the gate fires for `paperless` dispatch and `--help`, but did not enumerate every subcommand's gate behavior (e.g. whether `sync`/`web` carry the same pre-dispatch validation — they must, given where `config.Load` sits, but I tested two paths, not all).

## c) NOT STARTED (all deliberately outside agent reach)

1. **Upstream push + tag** — InboxClean master is 10+ commits ahead; agent sessions never push.
2. **`nix flake lock --update-input inboxclean` + deploy** — needs the pushed rev; deployed `c65c797` lacks `--decrypt-repair`.
3. **Part 1: fill the sops password** — human-only value + interactive sops edit; then a deploy to re-render the sops template (activation-time) so the units restart with the real value.
4. **Part 2: `--backfill --prune`** — user-gated live run (sudo + root-owned token). Works on the CURRENT deployed binary; no bump needed for this half.
5. **Part 3: `--backfill --decrypt-repair`** — user-gated live run; needs items 2-3 first.
6. **Post-repair verification** (documents decrypted, dupes gone, ledger repointed, `encrypted` tags cleared) — nothing to verify until the runs happen.

## d) TOTALLY FUCKED UP

Brutally honest; nothing fabricated to look self-critical:

1. **The first report of this run (06:15) was a work-summary, not the comprehensive self-review this task class demands** — today's sibling sessions all produced the full (a)-(g) format unprompted; the convention was sitting in `docs/status/` where I had been digging for an hour. The user had to ask. One extra round-trip caused by under-applying a visible repo convention.
2. **The runbook-gap discovery was luck-adjacent, not systematic.** I found the LLM gate because I idly ran `inboxclean paperless --help` to check the deployed flag surface. Had I "verified" the runbook by cross-reading unit env vs doc text, I might have reasoned that `INBOXCLEAN_CONFIG` satisfies the gate (it does NOT — the store TOML carries no llm settings) and passed the runbook as correct. The queue TOLD me to test the reconstruction first; I tested it second, after source verification. Execution-first ordering would have found this in step one. Two prior reports documented extraction-vs-dispatch verification without ever running the command — the trap was live for 3 hours and I nearly sailed past it too.
3. **I repeated the mvdan/sh exit-code trap that today's 03:54 report had already documented three hours earlier:** my test printed `exit=0` for a CLI that exited 1 (pipeline through `head`; `PIPESTATUS` empty). I caught it because I'd READ the warning, but I still wrote the test as a pipeline instead of using a failure-safe form from the start. Reading the lesson is not applying the lesson.
4. **I edited the TODO_LIST item tail by exact-match from bash grep output without a full View of the line** (the View tool truncates very long lines; the item is ~2.5k chars). It matched and the hooks passed, but the edit was made against a partially-seen string — sloppy matching discipline on the file the queue's loop-closure depends on.
5. **I ran the history-amend while the background `nix flake check` was still in flight** — two gate runs overlapping on a shared tree with parallel sessions around. Benign this time (docs-only change, both runs green), but it bent the "verify evals of shared surfaces at quiescent moments" rule for zero time saved.
6. **I briefly talked myself into deploying an interim `git+file?rev=` pin + live deploy** before re-reading the TODO item's own REMAINING line — written by the earlier run of MY OWN queue ID — which had already made and justified the push-first decision. Half an hour of second-guessing a decision my own task had documented. The rejection was correct; the deliberation was not.
7. **Not fucked up, for the record:** nothing destroyed or reverted; no secret value ever surfaced in any output or file (the token/password were only ever referenced by extraction path); no upstream code changed; the InboxClean checkout was left exactly as found.

## e) WHAT WE SHOULD IMPROVE

1. **Runbook verification rule: a command is verified only when its failure surface is the expected one.** "The greps extract the right strings" is extraction-testing, not command-testing. Any queue item that hands over a command to a human must be dispatch-tested by the agent to the furthest non-privileged step (here: up to the root-owned token).
2. **Execution-before-source-reading for "test X" instructions.** Source analysis is for root-causing a failure the test surfaced — not a substitute for running it. The queue's phrasing ("TEST … first") even said so.
3. **Apply the full status-report skill format on the FIRST report of a task run** — the (a)-(g) structure is cheap and today's sibling reports set it; summaries should be an exception requiring a reason.
4. **Exit-code discipline in this harness:** `PIPESTATUS` is empty under mvdan/sh. Write gate tests as `if ! cmd; then echo FAIL; fi` or capture to a file — never `cmd | head` followed by `$?`.
5. **The config gate firing before `--help` is an upstream UX bug** (a user cannot even discover the flags without a valid LLM config). Differential proof is in hand; file upstream after the push (verify-before-filing: evidence already at source + live level).
6. **Invented-knob class:** `INBOXCLEAN_TOKEN_FILE` is the second invented-env-knob incident today (after `mock-sops.nix`'s wrong template-path default). Rule for runbook authors: every env var in a documented command must be greppable in the consumer's source, or it is fiction.
7. **Daemon-race protocol worked but is still manual:** check `git log -1 --stat` pre-commit → amend if single-change. Consider making it a named policy (today it existed only as one session's precedent plus my replication).
8. **Two-reports-per-task needs a canonical-pointer convention** — done here (header line), but the docs-health HARVEST pass should treat the LAST report per task ID as canonical and the earlier as work-log.

## f) Up to 50 things we should get done next

Pri: P0 now / P1 this week / P2 soon / P3 backlog. Owner: USER / AGENT / UPSTREAM. Honest stop at 22: invented work is worse than a short list.

**Critical path (this item's completion):**

1. P0 USER — Fill `platforms/nixos/secrets/inboxclean-decrypt.yaml` `paperless_decrypt_password` (Sops + Age one-liner, from repo root), then deploy so the sops template re-renders and both inboxclean units restart with the real value (part 1).
2. P0 UPSTREAM/USER — Push InboxClean master (10+ commits, incl. `7aa5c3f`) + tag.
3. P0 AGENT — After the push: `nix flake lock --update-input inboxclean`; probe the go-modules FOD lock-free at the target rev BEFORE moving the lock (CV-chain protocol — source-only churn has invalidated hashes 3× before, though go.mod is untouched here); build toplevel; `nix run .#deploy`.
4. P0 USER — Part 2 on the CURRENT deployed binary (no bump needed): runbook prune, `--dry-run` preview first, verify it lists exactly the two 2026-09-03 dupes, then the real run.
5. P0 USER — Part 3 after items 1+3: runbook `--decrypt-repair`, `--dry-run` first, then the real run.
6. P1 AGENT — Post-repair verification sweep: the 4 statements decrypted (non-empty content), the 2 dupes gone (oldest kept), ledger rows repointed, `encrypted` tags cleared, zero new dupes (`PAPERLESS_CONSUMER_DELETE_DUPLICATES` belt holds).
7. P1 USER+AGENT — **Sequencing decision the docs don't record:** the 4 statements span BOTH mailboxes (the dupes exist because the same statement arrived twice); `--decrypt-repair` re-fetches source attachments per row, and MAIN's OAuth token is dead since Sep 04 (TODO:25) — main-row re-fetches will fail per-document (by design, original preserved) until re-consent. Either re-consent first, or accept a partial repair + converging re-run after TODO:25 lands.
8. P1 AGENT — After the deploy: verify the deployed flag surface via a DISPATCH test through the runbook env (not grep) so the runbook and the binary can never drift silently again.
9. P1 AGENT — After everything lands: trim the TODO item to its residual (it is the largest item in Priority 1; the AGENTS size-diet keeps slipping).

**Hardening / upstream:**

10. P1 UPSTREAM — Config gate should not fire before `--help` (`config.api_key_required` pre-dispatch); make the gate lazy per-command or exempt help. Evidence: today's differential test.
11. P2 UPSTREAM — papersync should surface "decrypt password is PLACEHOLDER while encrypted-tagged documents exist" as a WARN/dashboard card — part 1 currently lives only in queue tribal knowledge.
12. P2 UPSTREAM — `paperless` subcommand consumes no LLM; consider scoping the gate to LLM-consuming commands rather than process-global.
13. P2 AGENT — Sweep the runbooks for invented env knobs (grep each documented var against its consumer's source) — `INBOXCLEAN_TOKEN_FILE` class.
14. P2 AGENT — Harvest this §f into TODO_LIST with owners (items 1-9 belong there, not entombed here).
15. P3 AGENT — Consider a `tests/` or docs assertion that every `Environment=` var referenced by the inboxclean runbook exists in the module's unit definition (eval-time or lint-time; prevents unit-shape drift from silently breaking the reconstruction).

**Noticed in passing (already tracked — no re-verification this session):**

16. P1 AGENT — TODO:25 main OAuth re-consent is now a HARD dependency for part of this item (see item 7) and for resumed statement archiving; the queue ordering should say so.
17. P2 AGENT — The `gmail` tag demote PATCH still fails paperless-side under the CONNECTED work account (TODO:25 note) — root cause never obtained (HTTP status).
18. P2 AGENT — attic VM check still deterministic-RED at build time (TODO:28); my `--no-build` gate passed because builds are skipped — the red is still there for full checks.
19. P2 AGENT — `/health` still reports overall `ok` while main is `auth_expired` (04:13 report §e-3) — the phantom-green class, still live.
20. P3 AGENT — Daemon committed twice mid-run today (06:13:44 + 06:13:54); the amend-path precedent held, but the policy question from the 03:54 report (§g-2) is still unanswered.
21. P3 AGENT — The deployed inboxclean-sync unit shows `Restart=no` + `StartLimitBurst` — correct oneshot+timer doctrine; no action, recorded as verified-in-passing.
22. P3 AGENT — This task consumed two runs (engineering + verification) without either being wasted; consider tagging queue items "verify-only" vs "implement" so the second run starts from the first's report instead of rediscovering it (this one got lucky: the footer-in-commit made the discovery cheap).

## g) Questions I cannot figure out myself

1. **Sequencing:** should `main`'s OAuth re-consent (TODO:25) land BEFORE the `--decrypt-repair` run so main-row source re-fetches succeed, or do you prefer running the repair immediately and letting main-row documents fail per-document (originals preserved, converges on a re-run after re-consent)? The runbook is safe either way; only the repair's completeness on day one differs.
2. **Prune sign-off surface:** is the `--dry-run` preview listing sufficient consent for the destructive prune, or do you want the two duplicate document IDs cross-checked in the Paperless UI first? (The tool keeps the oldest per checksum; the ledger makes a re-run convergent either way.)
3. **Post-push relock:** when you push InboxClean, do you want the vendorHash pre-probe protocol (lock-free go-modules FOD build at the exact target rev, CV-chain style) run before moving the lock, or is "go.mod untouched → hash stable" an acceptable bet to save the probe?

---

*Point-in-time snapshot — 2026-09-12 06:20 CEST. Written per the status-report skill with the user-mandated Markdown override (skill default is HTML) and the user-specified section set (up to 50 in §f, 3 questions in §g).*
