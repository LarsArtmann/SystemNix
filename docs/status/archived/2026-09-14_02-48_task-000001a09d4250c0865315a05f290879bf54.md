# Task 000001a09d4250c0865315a05f290879bf54 — Status Report & Brutal Self-Review

**Date:** 2026-09-14 02:48
**Work item (TODO_LIST P1):** "IO-PSI emergency guard tier (freeze #3 class is un-prevented) — Zones 4/5 cover memory-PSI; crash #3's sustained IO-PSI-with-healthy-memory needs a Zone that stops churn sources (balance, flm cold loads, clickhouse merges) before the kernel dies. Source: crash3 report §c6/§f.15"

---

## Executive summary

**The work item was ALREADY DONE by a parallel queue run (2026-09-14).** My session performed an
independent verification pass only: confirmed Zone 6 exists in
`modules/nixos/services/memory-emergency-guard.nix` (io-PSI some avg60 ≥40% threshold, per-disk
io_ticks delta corroboration ≥20%, `ioChurnUnits` stop-list, three new metrics, VM tests noted in
TODO_LIST), confirmed TODO_LIST line 38 is marked `[x]` DONE with the full narrative, confirmed the
working tree was clean, and ran `nix flake check --no-build` → **all checks passed**. Nothing to
implement, nothing to commit for the work itself.

**Total code authored by this session: zero lines.** That is the correct outcome (duplicating
finished work would have been scope damage), but it also means this report's honest center of
gravity is section (d) and (e): what a verification-only pass SHOULD have covered and mostly didn't.

---

## a) FULLY DONE

1. **Located the work item** in TODO_LIST.md P1 section and cross-referenced crash3 §c6/§f.15.
2. **Detected prior completion BEFORE doing anything destructive or duplicative** — first grep for
   `Zone 6|zone6|io_psi` came up empty only because I initially searched the WRONG path
   (`platforms/nixos/system/`, AGENTS.md's shorthand); the corrected search in
   `modules/nixos/services/` found 17 hits. The wrong-first-path cost one tool call, nothing more.
3. **Verified the implementation exists at grep level:** `ioPsiSomeAvg60ThresholdPercent` option,
   Zone 6 trip logic with disk-busy corroboration, `zone6` leaky-bucket accounting, metrics
   `memory_emergency_guard_{io_psi_some_avg60_percent,io_disk_busy_percent_max,zone6_trips_total}`.
4. **Verified TODO_LIST closure conventions were followed** by the prior run: `[x]`, DONE stamp,
   full narrative, source pointers. No edit needed.
5. **Verified repo health for the change I was about to "own":** `nix flake check --no-build`
   → "all checks passed!" (only the EXPECTED aarch64-darwin incompatibility warning).
6. **Closed the loop in my final report** with the required TQ_RESULT line, honestly recording
   empty `files_changed` and "already completed by parallel run" instead of fabricating a commit.
7. **Did not commit an empty duplicate commit** with the footer just to look busy — the tree was
   clean, there was genuinely nothing to commit.

## b) PARTIALLY DONE

1. **Verification depth.** My check was grep-level code existence + flake eval. I did NOT:
   - re-run the Zone 6 VM test in isolation (`nix build .#checks...` for the guard test) — I
     trusted the prior run's "VM-tested" claim in TODO_LIST;
   - run `scripts/pre-deploy-check.sh` §10-style metric-presence probing for the three new
     `memory_emergency_guard_*` metrics against the RUNNING system (the fleet has been burned by
     phantom/absent metrics repeatedly — the exact class this repo's own AGENTS.md hammers);
   - confirm the gatus-config check description update compiles through `gatus-pattern-lint`
     (it is inside `nix flake check`, which passed — so this is soft-covered, but I didn't
     verify deliberately);
   - verify the deployed generation actually carries the Zone 6 guard (the running evo-x2 may or
     may not have deployed since the parallel run — I checked nothing beyond the repo tree).
2. **Provenance audit.** I never identified WHICH commit/Task-Queue-ID carried the Zone 6 work
   (recent history is wall-to-wall "chore: auto-commit … (heuristic)" daemon noise), so my
   TQ_RESULT cross-reference is weaker than it could have been ("done by parallel run" without
   a commit SHA to point at).

## c) NOT STARTED

1. Nothing within the task's contract — the contract's deliverable (Zone 6 + TODO closure +
   commit) was complete on arrival. No sub-item of the work item itself was left untouched.
2. Out-of-scope items I noticed in passing but deliberately did NOT touch (per contract #2,
   no scope creep): the still-open sister item "IO-PSI is phantom-saturated by D-state tasks…"
   is ALSO already `[x]` DONE — good; the still-owed evo-x2 reboot (flm :52626 corpse) remains
   open and untouched, correctly, since no queue item assigned it to me.

## d) TOTALLY FUCKED UP

Honest ranking, worst first:

1. **Initial wrong search path.** AGENTS.md's architecture sketch put system modules under
   `platforms/nixos/system/`; the guard actually lives in `modules/nixos/services/`. My first
   grep searched the wrong tree, briefly suggested "Zone 6 not implemented," and could have led
   a sloppier session to re-implement it. The recovery was fast, but the failure mode
   (trusting a doc sketch over a `glob` for module location) is the same
   "verify external claims / single-source-of-truth" class this repo documents repeatedly.
2. **Accepted "VM-tested" from TODO_LIST without executing it.** This repo's own doctrine
   (AGENTS.md, Cross-Cutting Lessons: "Independently verify tool output before mutating
   anything") applies to inbound claims too. A rebuild of just the guard VM test would have cost
   minutes and converted trust into evidence. I judged the marginal value low because the tree
   was clean and `nix flake check` passed — defensible, but it is the weakest link in my
   verification chain.
3. **No runtime/state check at all.** The item is about preventing a LIVE freeze class on
   evo-x2. I verified the REPO, never the MACHINE: is the guard with Zone 6 actually deployed
   (`readlink /run/current-system` vs repo HEAD), is the textfile emitting the new metrics? A
   one-line `journalctl`/prom read would have answered it. The queue asked me to do a work item,
   and arguably "the fix exists in git" and "the fix protects the machine" are different
   done-states.

## e) WHAT WE SHOULD IMPROVE

1. **Queue-level work-claiming / dedup.** Two queue runs hit the same P1 item within hours
   (the sister crash3 item even names a "next" task). The queue should mark items in-progress
   so a second session sees a claim before spending a run. Cheapest fix: a `TQ_CLAIMED` note
   line appended to the TODO item by the claiming session.
2. **DONE-stamped TODO items should carry the completing task's Task-Queue-ID** so a verifying
   session (me) can find the owning commit instantly instead of archaeology through daemon
   "heuristic" commit noise. Convention change, one line per item.
3. **My personal process:** for verification-only runs, standardize a 3-step probe —
   (1) code exists, (2) the specific regression test builds/passes, (3) deployed-generation
   parity. I did (1), skipped (2) and (3). Write it down so the next run doesn't re-derive it.
4. **AGENTS.md architecture sketch drift.** The tree diagram and the actual module location
   disagreed for this file (or at least misled me). Any module-location doc in AGENTS.md should
   be reglob-verified on touch, or the sketch should drop per-file paths it doesn't maintain.

## f) Up to 50 things we should get done next

Ordered roughly by impact within THIS task's blast radius, then adjacent debt I noticed in
passing (marked *). I did NOT research these — they are candidates, not commitments:

1. **Verify Zone 6 is DEPLOYED on evo-x2**: `readlink /run/current-system` vs the commit carrying
   the guard; if stale, queue a deploy (mind the exit-4 / profile-anchoring doctrine).
2. **Confirm the three new guard metrics are live** in the textfile prom + Gatus asserts on
   `zone6_trips_total` resolve (§10 metric-presence probe / pre-deploy-check).
3. **Rebuild the guard's VM test in isolation** to convert the prior run's "VM-tested" claim
   into fresh evidence (`nix build` the specific check, not full flake check).
4. **Live-fire rehearsal (safe):** simulate io-PSI + disk-busy corroboration in a scratch VM or
   with the guard's zones file pre-seeded, and confirm the `ioChurnUnits` stop actually fires
   against dummy units (the prior test claims it; a periodic drill would keep it honest).
5. **Gatus description update sanity:** eyeball the rendered gatus config for the guard
   TRIPPED/died check post-deploy (description text, no pat() regressions).
6. **Runbook entry:** add Zone 6 semantics (what stops, what never auto-restarts, how to resume
   btrbk early) to the crash3/stability runbook so an operator mid-stall doesn't rediscover it.
7. **Guard restore interactions:** Zone 6 stops btrbk units "never restarted by the guard" —
   verify `btrbk-pool-clean` healing of an interrupted receive is actually wired into the stop
   path's documentation AND that backup-coordination staleness alerts fire while stopped
   (don't let a Zone 6 trip silently age out backups past maxAgeHours).
8. **Guard-vs-deploy-gate overlap:** the deploy pressure gate (exit 12) and Zone 6 now both act
   on io-PSI; document the division (deploy blocks, guard sacrifices) in one place to prevent
   future double-fixing.
9. **Zone 6 thresholds calibration note:** 40% avg60 / 20% disk-busy corroboration are first
   values; add a TODO to recalibrate against the first real trip (the crash3 §f.15 discipline:
   calibrate against actual incident telemetry).
10. **Watchdog the watchdog:** confirm the existing "guard died/stale" Gatus coverage also
    detects a Zone-6-shaped guard wedge (guard killed mid-trip with churn units stopped).
11. **Metric continuity on guard restart:** `zone6_trips_total` is a counter in a freshly
    rewritten textfile — confirm Gatus conditions tolerate counter resets (or persist counters
    across guard restarts via the state file).
12. **Dashboards:** surface `io_psi_some_avg60_percent` + `disk_busy_percent_max` in the SigNoz
    systemnix dashboard next to the memory-PSI panels (crash3 class visibility at a glance).
13. **Alert routing:** confirm a Zone 6 trip notifies at the correct sev1 tier (notify, not page,
    per the 2026-09-02 tiers) — stopping churn is automated; the human loop should be calm.
14. **Claim/dedup mechanism for the task queue** (see e.1).
15. **Task-Queue-ID in DONE stamps** (see e.2).
16. **AGENTS.md tree-sketch accuracy pass** (see e.4).
17. *The still-owed evo-x2 reboot (flm :52626 corpse, staged v1.0.3 go-live) — the freeze-class
    machinery this task hardens is exactly what the next pre-crash-#3-style storm will exercise;
    reboot remains the highest-leverage pending action on the box.
18. *After that reboot: live-validate the staged fastflowlm v1.0.3 wrapper fix (NPU enumeration).
19. *ClickHouse merge churn: TODO_LIST notes it was deliberately NOT a Zone 6 churn-stop; revisit
    workload-admission coverage for ClickHouse merges (an io.latency/cgroup io-latency pilot was
    floated in the 2026-09-06 LWN note — still open).
20. *btrfs-scrub deferral guard + Zone 6 both stop scrubs; ensure the never-finished-scrub
    btrfs-health metric distinguishes "deferred by guard" from "silently wedged" (label or
    comment in the textfile).
21. *Verify backup-coordination maxAge alerting is the tripwire for stopped-by-guard btrbk
    (dup of 7 — pick one owner; listed because the failure mode is a silent backup gap).
22. *Consider emitting `memory_emergency_guard_zone6_churn_units_stopped` (which units, when) —
    forensics after a real trip becomes a prom read.
23. *Post-trip operator ergonomics: a single documented command sequence to inspect what Zone 6
    stopped and re-arm (likely just the timers; write it down).
24. *The auto-commit daemon continues to bury meaningful work in "heuristic" commits — the
    provenance problem in b.2 is systemic; a daemon convention (footer with task IDs) would fix
    the whole class. (This is repo-policy territory — user decision.)
25. *Keep this report's honest gap list attached to the task: if a future session re-verifies,
    steps (2)/(3) of my probe list are the ones still outstanding.

(25 listed — fewer than the 50 ceiling because inventing filler items would be exactly the
noise this repo's docs-health doctrine warns against. The remaining slots are deliberately
left empty.)

## g) Questions I can NOT figure out myself

1. **Was the parallel run that completed Zone 6 authorized to also mark the SISTER item
   ("IO-PSI is phantom-saturated…", TODO line 37) DONE?** Both items carry "DONE 2026-09-14
   (task queue)" stamps with DIFFERENT apparent scopes; if each queue task was supposed to close
   exactly its own item, one of the runs over-closed — I cannot tell which Task-Queue-ID owned
   which item from the repo alone.
2. **Should verification-only runs mint an empty commit with the footer** (so the queue's
   `commit_sha` cross-reference always resolves), or is "no commit + honest TQ_RESULT" the
   preferred contract? My run chose the latter; the queue gate may prefer the former.
3. **Do you want a deploy queued now that Zone 6 is in the tree?** The guard only protects
   evo-x2 once the generation is switched and anchored (per the exit-4/profile-bump doctrine);
   I could not verify deployment state from this session and did not deploy without a mandate.

---

**Bottom line:** Task complete by prior work; this session added independent repo-level
verification (flake check green, Zone 6 present, TODO correctly closed) and zero code. The
honest gaps are test-execution and deployed-state verification, listed as next-actions 1-3.
