# Task Status Report — 000001a0d1a502e30b7e95d3ecda2ac1b29b

**Item:** Prove shellcheck covers `scripts/*.sh` in pre-commit (TODO_LIST storage section → docs/todo/storage.md)
**Date:** 2026-09-24 07:31 CEST
**Dispatch type:** task queue, autonomous run
**Verdict:** work item was ALREADY COMPLETE at dispatch time (duplicate dispatch); this run independently re-verified every close-out claim — all green. No code changes were needed, so no work commit exists for this run; this report is the deliverable.

---

## 0. What actually happened (the headline)

This dispatch raced a parallel session's completion:

| Time (2026-09-24) | Event |
| --- | --- |
| ~04:02 | Harvest snapshot lists the item `[queued]` (f35 in `docs/status/2026-09-24_04-02_task-000001a0d1120f56328c239ba336fc60e29c.md`) |
| 07:12:01 | Commit `e55c4d7d` lands — completes the item, carrying the **identical** Task-Queue-ID footer; TODO_LIST.md:39 and docs/todo/storage.md:52 marked `[x]` DONE |
| ~07:2x | This dispatch arrives for the same item; TODO_LIST row already `[x]` |
| 07:11–07:31 | This run: independent re-verification of every claim in the close-out (results below), then this report |

The queue harvested while the item was open and dispatched after it was closed — a harvest/completion race, not an agent error. Per the no-empty-commits rule, nothing was re-done or dummy-committed.

## a) FULLY DONE

### a.1 The work item itself (prior session, commit `e55c4d7d`, 10 files, +206/−6)

- **Leg proven sound:** the pre-commit shellcheck leg predates the borg drill that raised the question (hook leg `5a798cb6` < drill `f845d1e9`); wiring verified (core.hooksPath, nested pathspec, `-z`/`-0` NUL-safety, ACM deletion filter, fail-closed).
- **Real gap found + closed:** 18 latent warning-level SC2034 findings in 5 scripts (`check-todo-system.sh`, `internet-diagnostic.sh`, `test-pre-deploy-metrics.sh` ×13, `ucr-ffprobe-sweep.sh`, `verify-html-diagrams.sh`) — invisible to CI's error-level job, each would have hard-failed the next commit touching the file. Fixed via dead assignments/locals removal and justified `disable=SC2034`.
- **Persisted proof:** `scripts/test-precommit-shellcheck.sh` (151 lines: 5 static hook-text asserts S1–S5 + 3 dynamic end-to-end proofs D1–D3 in a scratch repo) + `checks.x86_64-linux.precommit-shellcheck-leg-selftest` flake check (positive run against the REAL hook with pinned `SHELLCHECK_BIN` + fail-closed mutation must be caught), wired into CI's trap-lint job (`nix-check.yml:85`).
- Evidence: `git show --stat e55c4d7d`; all four queue/CI surfaces verified present in the tree this run.

### a.2 This run's independent verification (fresh, 07:11–07:31)

| Check | Result |
| --- | --- |
| `scripts/test-precommit-shellcheck.sh` (hook's real `nix shell nixpkgs#shellcheck` invocation) | **8/8 green** (S1–S5 static, D1 nested-path SC2034 detection, D2 clean pass, D3 deletion skip) |
| Hook-exact sweep over ALL `scripts/*.sh` incl. `scripts/lib/` at `--severity=warning` | **rc=0** |
| `nix build .#checks.x86_64-linux.precommit-shellcheck-leg-selftest` | **builds green** (incl. the S5 fail-closed mutation negative test) |
| CI wiring (`nix-check.yml:85`, trap-lint job) | present |
| Todo gate (`scripts/check-todo-system.sh`) | OK |
| Working tree | clean |

## b) PARTIALLY DONE

### b.1 Shellcheck coverage is layered-partial (by design, but with one blind spot)

- **Works:** hook checks every STAGED `.sh` at the stricter warning bar; nested `scripts/` paths proven covered (D1); CI error-level job covers top-level `scripts/*.sh` + `.githooks/*`.
- **Missing:** `scripts/lib/*.sh` (`metrics-gate.sh`, `offsite-borg-smoke.sh`, `pressure-report.sh`) — CI's job (`nix-check.yml:249`) uses the top-level glob `scripts/*.sh`, so the lib is only ever checked when staged, at the warning bar, never by CI at the error bar. This is the same lib at the center of the 2026-09-24 §13/§16 packaging incident. Currently clean (this run's sweep proved it at the warning bar), but structurally blind.
- **Blocker:** none — S effort, one CI line (item N1 in §f).

### b.2 This dispatch's own close-out is partial by structure

- Nothing to change → no commit → the queue can derive nothing from this run (no footer-bearing commit, no changed files). The only durable record is this report (committed with the footer, below). There is no sanctioned no-op acknowledgment path — see Q1.

## c) NOT STARTED

Fresh items discovered by this run, not started (candidate harvest — none appended to TODO_LIST by this run; §f is the harvest input):

1. tq duplicate-dispatch dedup (§f N2)
2. tq sanctioned no-op/ack close-out (§f N3)
3. CI shellcheck coverage for `scripts/lib/*.sh` (§f N1)
4. Hook per-leg log races (fixed `/tmp/shellcheck.log`, `/tmp/ruff.log`) (§f N4)
5. Hook self-coverage (`.githooks/*` in its own leg) (§f N5)
6. Shellcheck source pinning decision (§f N6, pairs with Q3)
7. Pre-commit-leg proof doctrine in CONTRIBUTING (§f N7)
8. Fast static-only mode of the proof in pre-commit (§f N8)

Standing queue inventory (already harvested, not started): TODO_LIST.md rows 20–79 — ~40 open rows, dominated by the offsite-borg go-live stack, Phase-2 hot-DB waves, and gate-hardening batch. Highlights in §f (already-queued table); full inventory lives in TODO_LIST.md, not duplicated here.

## d) TOTALLY FUCKED UP

Radical honesty, scoped to what this run saw:

1. ~~**The pre-commit flake-check gate is reported DEAD for all agents (TODO_LIST row 43, unverified this run).**~~ RESOLVED 2026-09-25 (task queue `000001a0d6d444…`): environmental, not structural — the boot-ordering cycle deleted `/run/binfmt`; fixes in-tree (`af87ce31`/`d827f478`, deploy residual tracked), gate green 6× on 2026-09-25. Full chain: `docs/status/2026-09-25_09-52_task-000001a0d6d4440f02adfdb7dddfd259e173.md`. The rest of this item's caveat (deeper forcing level, reproduce-or-close needed) is superseded by that closure. This run's eval-level probe (`nix eval .#checks.x86_64-linux.disko-layout.drvPath`) is GREEN — consistent with the failure living at the deeper forcing level (realization of a nested drv during `nix flake check`, the "`is not valid`" class), but the failure itself was NOT reproduced this run. The irony is pointed: this task just proved the shellcheck leg of the same hook sound while the hook's heaviest leg is reported dead. Needs one reproduce-or-close pass (S).
2. **The queue double-dispatched a completed item and this run produces no derivable output.** Wasted one agent run; worse, the accounting is blind — the queue derives work from footers and diffs, and a verify-only run has neither. Systemic (will recur whenever harvest lags completion), not a one-off.
3. **Hook log-file race (micro, real in THIS repo's context).** The hook writes fixed paths `/tmp/shellcheck.log`, `/tmp/ruff.log` (`.githooks/pre-commit:320,333`). AGENTS.md's own multi-agent doctrine documents concurrent commits on this box; two racing commits clobber each other's logs. Exit status (the blocking decision) is per-process and unaffected — the lying surface is the printed log only. Low severity, S fix.

Nothing in the task's own scope is red: all four verification surfaces are green, the tree is clean, and the queue files are consistent.

## e) WHAT WE SHOULD IMPROVE (incl. self-critique: what I forgot / could do better)

**Process:**

1. **Harvest-vs-completion race** — dispatches need a state re-check at execution time (row still `[ ]`?) before doing work. Otherwise every fast-completed item risks a wasted dispatch.
2. **No-op dispatches are invisible** — a "found done, verified" run should be recordable (ack/footnote commit or queue CLI) so the queue's derivation does not read it as failed/silent.
3. **Proof-doctrine symmetry** — the shellcheck leg now has the repo's strongest closure (persisted proof + flake check + negative mutation + CI). The other pre-commit legs (ruff, formatter, templ, nullglob audit) have no equivalent end-to-end proof. Generalize: every pre-commit leg gets a persisted proof + flake check.

**This run's self-critique (what I forgot / could have done better):**

4. **Did not run a repo-wide `nix flake check --no-build`** — my targeted verifications prove the task's surfaces but not that no parallel session broke a shared eval surface mid-verify. Skipped deliberately (quiescence doctrine + time); noting as the honest gap.
5. **Did not append the queue-mechanics follow-ups to TODO_LIST.md** — the contract allows new items but forbids items describing THIS task's work; the tq-race items sit close enough to the line that I chose report-only. Defensible, but HARVEST must now do the routing (risk: they die in this timestamped file).
6. **Could have negative-probed harder myself** — the mutation test ran inside the flake check, but I trusted the harness for it instead of also hand-mutating. The harness IS the sanctioned mechanism; acceptable, not maximal.
7. **Report-format divergence (flag per skill contract):** the status-report skill's canonical format is a styled HTML dashboard; the user explicitly requested `.md` at this path, so the override is honored here and flagged — not propagated back into the skill.

## f) Top things to get done next (up to 50; 8 NEW from this run + top already-queued)

### NEW (this run — harvest candidates)

| # | Task | Impact | Effort | Category |
| --- | --- | --- | --- | --- |
| N1 | Extend CI shellcheck job to `scripts/lib/*.sh` (find -print0 \| xargs -0, keep `--severity=error`) — closes the error-bar blind spot on the deploy-gate lib | High | S | Quality |
| N2 | tq: re-check TODO_LIST row state at dispatch/completion time to stop double-dispatch of items completed while queued | High | M | Quality |
| N3 | tq: define a sanctioned no-op/ack close-out for already-done dispatches so verify-only runs are derivable by the queue | High | M | Process |
| N4 | mktemp (+trap rm) the hook's per-leg logs (`/tmp/shellcheck.log`, `/tmp/ruff.log`) — concurrent-commit log race in a multi-agent repo | Medium | S | Bug |
| N5 | Add `.githooks/*` to the hook's own shellcheck leg so hook edits are linted pre-commit, not CI-only | Low | S | Quality |
| N6 | Decide + implement shellcheck source for the hook: pin from this flake (hermetic, no per-commit `nix shell nixpkgs#` eval/network) vs status quo (pairs with Q3) | Low | S | Quality |
| N7 | Codify in docs/CONTRIBUTING.md: every pre-commit leg ships a persisted proof + flake check (shellcheck leg is the reference) | Medium | S | Documentation |
| N8 | Fast static-only mode (S1–S5) of `test-precommit-shellcheck.sh` runnable in pre-commit, so hook-text drift is caught locally before CI | Low | S | Quality |

### ALREADY QUEUED (top by impact; full inventory TODO_LIST rows 20–79)

| # | Task (row) | Impact | Effort | Category |
| --- | --- | --- | --- | --- |
| Q1 | Reproduce-or-close `checks.disko-layout` failure — reported to kill the pre-commit flake-check gate for ALL agents (row 43) | Critical | S | Bug |
| Q2 | Deploy authority: queue-fired vs user-manual deploy — every storage task since 2026-09-21 is runtime-zero behind this (row 60, BLOCKED on owner) | Critical | — | Decision |
| Q3 | Restic repo proof chain: first run + `restic check` + one-file restore smoke (row 56 — nothing has backed up yet) | High | M | Bug |
| Q4 | Offsite-borg go-live stack: recovery-copy policy gate (64), VM test (62), EIO-inode path before first real run (75), Zone-6 membership decision (79), real-repo drill (step 9) | High | M/L | Feature |
| Q5 | Borg gate hardening batch: §16 enable-blind SKIP fix (74), BORG_RSH shape asserts (76), lib-sourcing parity check (77), §13 classifier fixture (72), drill mountpoint fixture (78), positive-render lib extraction (71), VM rehearsal (73), drill fixture negative (42) | High | M | Quality |
| Q6 | Phase-2 hot-DB: migration waves (20), crush-DB baseline/follow-through (46), hot-db + crush-hot-db VM tests green in quiet-IO window (47), migrate stub fixture (50), T14 monitoring (49) | High | M/L | Feature |
| Q7 | buildcache: init must provision pnpm/cargo fallback targets (54), single-source the four reap inventories (55) | High | M | Bug |
| Q8 | Boot-mirror activation + reboot pending since 2026-09-19 (row 48) | High | M | Feature |
| Q9 | Paperless DR completion: pg_restore drill + dump-integrity gate (row 57) | High | M | Feature |
| Q10 | discordsync-attachments-migrate destructive oneshot fixture BEFORE the triggering deploy (row 58) | High | M | Quality |
| Q11 | /data repair: codify single-victim recipe (44) + damage-set inventory (45) | High | M/L | Bug |
| Q12 | PSI-gated rebuilds: hot-db/crush-hot-db VM tests + pool-quality remnants (rows 21, 47) — run in a quiet-IO window | Medium | M | Quality |
| Q13 | ClickHouse native-BACKUP restore drill (65); third-copy placement decision (68, BLOCKED owner) | Medium | M | Feature |
| Q14 | Repo-wide hardcoded help-slicer sweep in scripts/ (row 66) | Medium | S | Bug |
| Q15 | Docs batch: CONTRIBUTING provenance line (67), storage.md lineage annotations (63), Phase-2 plan T-table update (51), snapshot-pinning sweep (40), borg env-path-pin fixture (41), npm/go-lint reclaim (59) | Medium | S | Documentation |

(15 highlighted; ~25 further rows live in TODO_LIST.md. HARVEST: N1–N8 are new — route per docs-health; Q1–Q15 already exist and must NOT be re-harvested as duplicates.)

## g) Questions I cannot answer myself (up to 3)

1. **Queue mechanics:** when a dispatch arrives for an item already completed by another agent (same Task-Queue-ID, work committed minutes earlier), what is the sanctioned close-out? I grepped `docs/services/tq.md` (misbehavior/rescue only — no duplicate/no-op semantics) and found no ack mechanism; without one, verify-only runs like this are invisible to the queue's footer+diff derivation. Is there a queue CLI ack, or should agents commit a marker for the queue to see?
2. **TODO_LIST row 60 (deploy authority)** has been BLOCKED on your decision since 2026-09-21 and per its own note every storage task since is runtime-zero behind it, asked unanswered by 3+ closeouts: queue-fired deploys, user-manual only, or a gated hybrid?
3. **Hook toolchain preference:** keep `nix shell nixpkgs#shellcheck` (always-current, but per-commit nixpkgs eval + network dependency on the commit path) or pin shellcheck from this repo's flake (hermetic, faster commits, version-drifts only on flake bumps)? I can implement either; the latency-vs-freshness tradeoff is yours.

---

**Harvest note (per status-report skill):** section (f) NEW rows N1–N8 are the harvest input for docs-health → TODO_LIST routing. This report is a point-in-time snapshot and goes stale.
