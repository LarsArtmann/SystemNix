# Status Report — Task 000001a0d7c6e16844a893aafd7c9a19a748

**Date:** 2026-09-25 11:02
**Work item:** "Refresh the doctrine table's `/data/docker` sizing via `docker system df` (docker group, no sudo)" → docs/todo/storage.md (Source: same report §f4)
**Scope discipline:** docs-only, 3 files, +3/−3 lines. No code, no config, no deploy. Repo not broken (verified below).

---

## Summary

One queue item, fully executed: probed live Docker disk usage as the `lars` user (docker group, no sudo), refreshed the `/data/docker` row in AGENTS.md's snapshot-pinning doctrine table with dated provenance, and closed the loop in both the dispatch queue (TODO_LIST.md:47) and the domain library (docs/todo/storage.md row 67). Committed `6f7899a3` with the Task-Queue-ID footer (the auto-commit daemon had swept the files into a heuristic commit; amended per the daemon-race doctrine after verifying the daemon staged exactly my 3 files).

**Live probe result (2026-09-25):**

| Type          | Total   | Active | Reclaimable     |
| ------------- | ------- | ------ | --------------- |
| Images        | 9.09 GB | 8/26   | 4.18 GB (45%)   |
| Containers    | 4.4 MB  | 9/10   | 0 B             |
| Local Volumes | 4.34 GB | 6/212  | 4.09 GB (94%)   |
| Build Cache   | 2.08 GB | 0/119  | 1.67 GB         |
| **Sum**       | **~15.5 GB** |   | **~9.9 GB (64%)** |

The stale `~20G era 2026-08-31` figure is superseded; the row now reads `~15.5G per docker system df 2026-09-25: images 9.1G + volumes 4.3G + build cache 2.1G, ~9.9G / 64% reclaimable`.

---

## a) FULLY DONE

1. **Live sizing probe** — `docker system df` (+ `-v` for per-image shared/unique split) run from the docker group, zero sudo, as the item demanded.
2. **AGENTS.md doctrine table row refreshed** (line ~666) — dated provenance, per-component breakdown, reclaimable share, pin-window column untouched (still `/data ≈ 4-5w`, which is about the filesystem, not the figure).
3. **TODO_LIST.md queue item closed** (`[x]`, line 47) — follows the file's own convention.
4. **docs/todo/storage.md library row closed** (row 67) in the file's established DONE style (`~~strikethrough~~ + DONE 2026-09-25 (task queue):` + result summary + Source), keeping queue one-liner and library entry drift-free per the AGENTS.md TODO-system rule.
5. **Verification** — `scripts/check-todo-system.sh` → `OK: TODO queue/library structure clean`; the amend ran the full pre-commit gate (gitleaks clean, eval-only `nix flake check` green, templ/shell/py linters n/a).
6. **Daemon-race handled correctly** — daemon committed my 3 files as a heuristic auto-commit mid-run; verified `git show --stat` showed exactly my files (no co-mingled work), then amended into a properly-messaged commit with the footer. Working tree clean afterward.

## b) PARTIALLY DONE

Nothing in the item itself is partial. Two adjacent observations are deliberately *not* done (see c/e) — they are follow-ups, not fragments of this task.

## c) NOT STARTED (noticed in passing, deliberately out of scope)

1. **AGENTS.md Samsung section (line ~1225) still carries the stale docker figure** — "`/data/docker` IS the live Docker data-root (verified 2026-08-31 ...), ~20 G of which ~88% is pruneable garbage". My grep found it during the sweep; I judged it out of scope for the one-row refresh. It is now inconsistent with the refreshed table row (15.5G/64% vs 20G/88%). See e1 — this one arguably SHOULD have ridden the same commit.
2. **212 local volumes, only 6 active, 4.09 GB (94%) reclaimable** — `docker-prune` deliberately never auto-prunes volumes (real-data caution). Nobody has inventoried the 206 stale volumes; unknown whether any hold forgotten state (e.g. old DB-sidecar volumes from removed compose stacks).
3. **2 dangling images (<none>, 6h old at probe)** — within the weekly `image prune -af --filter until=168h` window; will be reclaimed at the next Sunday run. No action needed; noted only as passing state.
4. **Pre-existing backlog untouched** — the storage domain library carries ~20 open items (root `@` retention anomaly, offsite-borg go-live inputs, ClickHouse restore drill, crush-DB migration baseline, etc.). None touched; queue pacing owns them.

## d) TOTALLY FUCKED UP

Nothing. No broken state, no data at risk, no reverted work, no deploy, no secrets touched. The one process hiccup (daemon heuristic commit) was handled by the documented amend-forward path, not a fuckup.

## e) WHAT WE SHOULD IMPROVE

1. **I under-applied the repo's own "atomic doc sweep" doctrine.** The jan.md repair recipe (and the AGENTS.md land-the-replacement rule) says: grep every live surface of a stale fact and update them in ONE commit. I grep'd, found the second stale `~20G` mention (Samsung section), and left it. A one-line follow-up commit is cheaper than a future session citing the wrong number from the wrong section. Lesson: when the grep ALREADY returned the second hit, the marginal cost of fixing it is near zero and the scope argument is weak.
2. **Figures are docker-reported, not on-disk truth.** The row says "~15.5G per docker system df", which is correct provenance, but docker's SIZE accounting (shared layers, virtual sizes) is not identical to the btrfs on-disk usage of `/data/docker` (the tree is root-only; `du` needs sudo). The old ~20G figure was (presumably) a du; the new 15.5G is docker-internal. The drop from 20G→15.5G may partly be measurement-method, partly real reclamation. Worth a sudo `du`/`compsize` cross-check whenever a root window exists, so the doctrine table compares like with like.
3. **Recurring refresh is ad-hoc.** This is the second time a dated sizing row went stale (~5 weeks). A `[watch]` recurring row ("re-probe docker sizing quarterly or on any space-pressure investigation") would beat rediscovering the drift.
4. **Volume hygiene has no owner.** 94% of volume bytes reclaimable and growing (212 volumes vs 6 active) — but volumes are deliberately exempt from auto-prune. This needs a one-time inventory (`docker ps -a --filter volume` + `docker volume ls` cross-ref) before anyone prunes; right now it is an unowned blind spot.
5. **Commit-time efficiency.** The amend hit the full pre-commit hook suite (gitleaks + flake eval) on a 3-line docs change — correct behavior, just slow (~background-shell territory). No action; noted for expectations.

## f) Up to 50 things we should get done next

Sorted roughly by impact; items 1-3 derive directly from this task, the rest are the existing storage-adjacent backlog this report endorses (queue pacing decides when they run — most are ALREADY queued rows, listed here so this report is self-contained).

**From this task's findings:**

1. Sweep the second stale docker figure (AGENTS.md ~1225, `~20 G ... ~88% pruneable`) to match the refreshed table row — or replace the prose with a pointer to the doctrine table. (One-line commit.)
2. One-time Docker volume inventory: cross-ref the 212 volumes against compose/`mkDockerService` consumers, classify keep/forgotten, produce a prune candidate list for owner review. No auto-prune.
3. Add a `[watch]` recurring row: re-probe `docker system df` sizing quarterly (or at any /data space investigation) and refresh the doctrine row.
4. Sudo-window cross-check: `du`/`compsize /data/docker` vs the docker-reported 15.5G so the doctrine table's provenance is on-disk-truth, not just docker accounting.
5. After the next weekly `docker-prune`, log the reclaimed figure and the projected snapshot-pin free date (the row's own caveat: post-prune `df` lags up to a month).
6. Confirm the 2 dangling images (6h old) actually get reclaimed at the next Sunday `image prune -af` run (phantom-green class check on the prune unit's journal).

**Already queued — storage domain (NOT started, prioritized by the queue):**

7. Reconcile the root `@` retention anomaly (12-day-old weekly vs `snapshot_preserve 3d 1w`) — restates the doctrine's root window from measured behavior.
8. Offsite Borg go-live (owner-gated): StorageBox hostname/username + passphrase recovery-copy policy + exclusion exceptions.
9. VM test for the offsite-borg module (`platforms/nixos/system/backup.nix` has zero coverage).
10. Borg real-repo drill (go-live step 9) — after 8/9.
11. Borg verify semantics decision: live-`cmp` vs manifest-baked-in-archive (dead-host restore proof).
12. Borg passphrase recovery copy: physically stored off-host and proven to decrypt?
13. ClickHouse native-BACKUP restore drill: script + runbook section (dir-mode backup → scratch cluster).
14. ClickHouse dump third-copy placement decision (restic vs Borg vs pool-only) — owner owns the BX11 budget.
15. Restic repo post-deploy proof chain: first run + `restic check` + one-file restore smoke + dedup-ratio measurement.
16. Restic repo's offsite role decision (local dedup layer only vs THE offsite leg).
17. Paperless DR completion: pg_restore drill into a scratch cluster + dump-integrity gate.
18. Stub/fixture test for the destructive `discordsync-attachments-migrate` oneshot before any re-trigger.
19. buildcache-init must provision the new fallback targets (pnpm-cache, pnpm-state, cargo/registry) — recovery currently leaves HM symlinks dangling until next deploy.
20. Single-source the four env-less cache reap inventories (deploy.sh ×2, buildcache-usb-recovery, home.nix activation) — hand-kept name lists, no parity enforcement.
21. Crush-DB migration baseline + follow-through (io-psi forensics before/after; Zone 6 should stop cycling flm).
22. Rebuild `hot-db` + `crush-hot-db` VM tests green on the current tree (quiet-IO window).
23. Samsung boot-mirror activation + reboot pending (deploy landed 2026-09-19).
24. T14 hot-db tier monitoring: mount-presence textfile metric (fail-closed) + Gatus checks for `/mnt/hot`.
25. `migrate-hot-db.sh` stub-fixture test before its first user migration window.
26. Update the Phase-2 plan doc's T-task table with completion status.
27. storage.md cache-sweep lineage annotations (row 80 append, row 82 tick, TODO_LIST:21 parent note).
28. Deploy authority decision: queue-fired vs user-manual `nix run .#deploy` — BLOCKED on owner; every task since 2026-09-21 is runtime-zero behind it.
29. Converge `~/.npm` (103M stale fallback) onto the buildcache + give `~/tmp/go-lint` (1.8G) a reclamation path.
30. When the flm bump lands: verify the OLD weights' pin clock started + log the projected extent-free date (~45G pinned through the /data window).
31. flm re-pull headroom gate (owner threshold call) — moot today (316G free) but wanted before staged flm go-live.
32. Free-space triage order: encode the sanctioned order (nix-gc → ~/.cache → trash-empty → wait-out-window) in the doctrine — policy, not code.
33. Post-Borg-go-live: add the offsite column to the snapshot-pinning doctrine table.
34. Clean the `/mnt/pool` root-fs shadow directory (226-class hygiene) + boot-dropout recovery notes — blocked:user.
35. Prune `[x]` rows from docs/todo/storage.md to CHANGELOG.md at the next docs-health pass (my row 67 + the four other 2026-09-25 DONE rows are now prune candidates).

**Passing observations worth a row eventually (not urgent):**

36. SigNoz/dashboard-side: nothing noticed broken during this task (docs-only session, no runtime surface touched).
37. Consider documenting the docker-group no-sudo probe path (which figure is docker-reported vs on-disk) in docs/services or the doctrine table's footnote, so future refreshes compare like with like.
38. `docker system df -v` shows Build Cache 119 records fully inactive (0 active) — the 7-day `builder prune` window is doing its job; verify once more post-Sunday.
39. 26 images with 8 active: after the next prune, re-run the sizing refresh to see the steady-state image footprint (9.1G → expected ~5G).
40. If the volume inventory (item 2) finds forgotten DB-sidecar volumes, decide restore-vs-delete per volume BEFORE any prune — volumes are the only data docker-prune never touches by design.

*(Items 41-50 intentionally left empty — padding the list with speculative items would violate the HARVEST routing rigor; the 40 above cover this task's findings plus the full known storage backlog.)*

## g) Questions I can NOT figure out myself

1. **Docker volume policy:** are the ~206 inactive volumes (4.09 GB, 94% of volume bytes) known-forgotten state you are comfortable inventorying and pruning candidates for — or do any hold data you consider irreplaceable (old compose-stack DBs, manual `docker run` experiments)? This decides whether item 2 is a routine sweep or a careful archaeology.
2. **Provenance standard for the doctrine table:** should dated sizing rows be docker-reported (no sudo, refreshable anytime) or on-disk truth (needs a sudo window)? Mixed provenance makes week-over-week comparisons slightly apples-to-oranges (see e2); I picked docker-reported because the item explicitly said "docker group, no sudo".
3. **CHANGELOG pruning timing:** should a task-queue run prune its own freshly-`[x]`ed library row to CHANGELOG.md immediately, or is that exclusively the docs-health pass's job (my current assumption — I left all `[x]` rows in place)?

---

**Verification state at close:** working tree clean; `6f7899a3` carries the work + footer; todo-system check green; no deploy (docs-only). This report commits with the same Task-Queue-ID footer and is never pushed.
