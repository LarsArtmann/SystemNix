# Task 000001a0c10023cde007879240f2d8a3a5ae — Review-Fix Session Report

**Date:** 2026-09-21 11:46
**Task:** Fix exactly one reviewer finding on the hot-db Phase-2 task review: the status report cited the wrong SHA for the deploy-restart-audit config-layer fix.
**Work commit:** `36a7160c` (footer `000001a0c10023cde007879240f2d8a3a5ae`, this report's commit carries the same footer).
**Parent lineage:** 000001a0bbb06602422f206275102f26a74a (the hot-db module task; its review produced the finding).

---

## a) FULLY DONE

1. **Finding verified before acting.** Confirmed via `grep -rn 82d4fa4a` that the anchored verbatim text lives in `docs/status/2026-09-20_09-46_task-000001a0bbb06602422f206275102f26a74a.md` (section b.3, line 31). Confirmed via `git cat-file -e` that the rejected/dangling commit `067cbe97…` still exists, and via `git show --stat 50f73871` that the correct SHA really is the config-layer fix (it touched `modules/nixos/services/deploy-restart-audit.nix`, +27/−20, 2026-09-20 09:36:21 — matching the reviewer's claim). Evidence-first, no trust in the finding text alone.
2. **Minimal citation fix.** One-line edit in the status report: the config-layer fix is now credited to `50f73871` (09:36, minutes after the 09:35 green toplevel eval), with a parenthetical stating `82d4fa4a` only carried the commit MESSAGE claiming the fix and its diff is docs-only. The narrative (parallel fix landed before the 09:43 re-verification commit) is preserved intact, exactly as the finding requested.
3. **Committed with the correct footer discipline.** Exactly ONE Task-Queue-ID footer — the FIX ticket's id (`000001a0c10023cde007879240f2d8a3a5ae`), not the parent's, per the instruction. Not pushed.
4. **Gates green.** Pre-commit passed: gitleaks clean, trailing-whitespace fix, Nix flake check (eval-only) green. Docs-only change so no .nix/.sh/.py linters applied (expected path, not a skip of relevant gates).
5. **No scope creep.** No TODO_LIST edit (the parent task is already closed there; this fix is a review disposition, not a queue item), no code touched, no drive-by corrections.

## b) PARTIALLY DONE

1. **Same factual error exists elsewhere and was left standing (deliberate).** The misattribution repeats in two other docs: `docs/status/2026-09-20_12-26_deploy-chain-rescue-….md` line 17 ("already fixed at the config layer by a parallel session in `82d4fa4a` (09:40, twelve minutes AFTER…)") and a passing mention in the same 09-46 report's line 48/100/127 neighborhood (those refer to `82d4fa4a` as the gitleaks-reword commit, which IS correct per the 10-38 report — only the 12-26 doc carries the same wrong-fix attribution). I fixed ONLY the anchored site because the instruction said "fix EXACTLY this finding — no unrelated changes". The 12-26 doc's citation remains wrong. This is the correct call under the contract but leaves the evidence trail inconsistent across reports. Queued as follow-up item #1 below.
2. **Self-review depth on the parent session's work.** This session inherited a review finding that the parent session's own close-out should have caught (the parent's report even brags about checking the recent-commit log — yet it still cited the message-only commit as the fix). My "what could be better" contribution is the follow-up items below, not a re-audit of the parent task; that re-audit is out of scope for a one-finding fix run.

## c) NOT STARTED

1. Nothing assigned to this task was skipped. The finding was the entire scope and it is done.
2. No new feature work, no queue harvests, no TODO_LIST additions describing this task's own work (forbidden by contract).

## d) TOTALLY FUCKED UP

Nothing in this session. Honest near-misses worth recording:

1. **First edit attempt failed** ("you must read the file before editing") because I grepped/`sed -n`-read the target instead of using the View tool. Cost one tool call, zero damage. The lesson is already in AGENTS.md; I followed it late, not never.
2. **The `82d4fa4a` mention in line 48 of the same report** (gitleaks-reword context) is LEGITIMATE and must not be "fixed" — a less careful pass could have blindly replaced all `82d4fa4a` occurrences in the file and broken a correct citation. Verified against the 10-38 report before touching anything.

## e) WHAT WE SHOULD IMPROVE

1. **Status reports cite SHAs without diff-verifying them.** The 09-46 report's author ran `git log` (they say so), saw `82d4fa4a`'s message claim the fix, and cited it — the message LIED, the diff was docs-only. Rule candidate for AGENTS.md/CONTRIBUTING: every SHA cited as evidence for a CODE change must be verified with `git show --stat <sha>` at citation time. The AGENTS.md "commit-message evidence rule" covers claims in commit messages but not SHA citations in status reports.
2. **Heuristic daemon commits make message-vs-diff divergence structural here.** `82d4fa4a` exists precisely because the daemon batched a docs file under a message describing someone else's fix (the 10-38 report documents the split authorship `50f73871`/`82d4fa4a`). Citation-by-message is unusually dangerous in this repo specifically. The purge-grade fix would be a lint script: scan docs/status/*.md for 7/40-hex SHAs and verify each against its diff (fast, no network). Queued as #3.
3. **Cross-doc factual consistency has no sweep.** The same error now lives in two reports and will keep living in one. A one-line `grep`-based consistency check over status docs (do cited SHAs appear in `git show --stat`?) would catch both sites mechanically (same script as #3).

## f) Up to 50 things to get done next (agent-executable, in rough priority order; NOT appended to TODO_LIST.md by this run — see note below)

**Note on the queue contract:** the task says I MAY append NEW unchecked items to TODO_LIST.md. I am NOT appending them: the TODO system rules in AGENTS.md say queue rows live in the dispatch queue only as `[ready]` one-liners with `**Source:**` pointers, harvested into domain files, and — more importantly — several items below are `[decision]`/`[blocked:user]`-grade (docs-consistency policy, report archiving), which the queue explicitly does NOT harvest. Recording them here in the report keeps the trail without polluting the queue with non-`[ready]` rows. The genuinely ready ones (1–3) are listed precisely enough to be harvested by a future pass.

1. `[ready]` Correct the `82d4fa4a`→`50f73871` misattribution in `docs/status/2026-09-20_12-26_deploy-chain-rescue-nodejs-stop-phase-traps-cv-upstream-rm-rf.md` line 17 (same finding, second site; the option-default claim there is also worth re-verifying against the actual 50f73871 diff before editing).
2. `[ready]` Re-verify the 09-46 report's OTHER substantive claims that cite parallel-session commits (line 48's `82d4fa4a` reword claim — confirmed correct via 10-38 report; nothing else found, but a one-pass `git show --stat` sweep of every SHA cited in that report is cheap insurance).
3. `[ready]` Write `scripts/verify-status-citations.sh`: grep docs/status/*.md for bare 7/40-hex SHAs, run `git cat-file -e` + `git show --stat` on each, fail on missing SHAs and WARN on docs-only diffs cited as code fixes. Wire into CI as a non-blocking WARN first (status docs are append-heavy; blocking would be noisy).
4. AGENTS.md rule candidate: "cite the SHA, never the message" — extend the existing commit-message evidence rule to status-report SHA citations.
5. Consider making the heuristic auto-commit daemon include a `Implements-claim:` marker or suppress message body claims entirely (split authorship made `82d4fa4a` misleading; root-cause is daemon messages describing content they don't carry). `[decision]` — daemon behavior is user-owned.
6. Reviewer-finding dispositions would benefit from a standardized `docs/status/…-review-fix.md` template (this report is the first instance; formalize it once a second exists).
7. The 10-38 report's "split authorship noted" thread deserves a one-line closure in the 12-26 doc once item 1 lands, so no future reader re-derives it.

That is 7 — deliberately under 50: inventing filler items for a one-line docs fix would violate the smallest-correct-change contract and the queue's pacing gates. The honest backlog for THIS task is items 1–3; 4–7 are governance ideas for the user to adopt or drop.

## g) Questions I cannot figure out myself

1. **Should the `82d4fa4a` citation in the 12-26 report be corrected as part of THIS finding's lineage (same factual error, second site), or does "fix EXACTLY this finding" permanently scope the correction to the anchored site only?** I chose the conservative reading; if the first, item 1 above is ready to execute.
2. **Is the daemon's heuristic commit message (which claimed a code fix while carrying only a docs file) worth a systemic fix (message discipline for batched commits), or is the accepted cost of the auto-commit daemon?** If the latter, item 3's citation-verifier is the only real defense and should graduate to blocking.
3. **Do you want status reports' SHA citations enforced as a hard gate (CI-blocking) once the verifier script exists, or kept as WARN forever given how many status docs accumulate per day?**
