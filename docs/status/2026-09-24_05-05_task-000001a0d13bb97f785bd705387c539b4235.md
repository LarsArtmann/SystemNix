# Status Report — Task Queue Item `000001a0d13bb97f785bd705387c539b4235`

**Date:** 2026-09-24 05:05 CEST
**Work item:** "Persist the offsite-borg positive-path render probe (eval-time flake check: BE/6 + tripwire/marker/borg-env on enable=true)" (TODO_LIST → docs/todo/storage.md:51, Source §f3)
**Repo:** `/home/lars/projects/SystemNix` @ `14fc6816`, tree clean at handoff and at report time.
**Prior reports in this chain:** `2026-09-24_03-14_task-…0d1f8b.md` (§d3), `2026-09-24_04-02_task-…20f56.md` (§d4/§f1 — its b3 folds into THIS item, see c2).

---

## Executive summary

This dispatch arrived to find the item **already implemented and committed by a prior dispatch of the same queue item** — HEAD `14fc6816` (04:45:49, ~20 min before this session started) carries the exact Task-Queue-ID footer, the check, and both queue closures. This run was therefore **verification-only**: it proved the committed artifact is real and green (`nix build .#checks.x86_64-linux.offsite-borg-positive-render` → green), proved the guard has teeth (a priority-49 `idle` drift override renders through and trips the assert with exactly `string '"idle"' is not equal to string '"best-effort"'`), and verified the loop is closed on both surfaces (TODO_LIST.md:37 `[x]`, docs/todo/storage.md:51 `[x]`, AGENTS.md offsite-borg section documents the check). No tree changes were produced; nothing needed re-committing. This report is this session's only artifact.

## Self-review: what I forgot / could have done better / can still improve

- **Forgot — nothing material.** The item was pre-complete; closure rows were accurate (I re-read both rather than trusting the commit message). No secret material, no queue-file drift.
- **Could have done better — the negative probe is a replica, not the real guard.** The check's guard expression is an inline `let`-binding inside flake.nix and cannot be externally driven on a drifted config without editing the tree. My probe hand-replicated the assert shape (`assert svc.IOSchedulingClass == "best-effort"`) against a genuinely drifted render — that proves the assert READS the rendered value (not a constant), the substantive property, but the exact committed guard code did not execute on the drift path. The prior dispatch's closure notes show it did the same. Residual: a future edit that weakens the check's own guard LOGIC (e.g. assert → `true`) would pass both my probe and the positive build; only diff review catches that. Inherent to eval-time checks (the check IS the code), acceptable, but worth naming. Fix path: extract the guard into a small pure lib function taking `(svc, envTemplate)` so probes and the check share one implementation (new follow-up, f4).
- **Could have done better — verification was strictly item-scoped.** I did not re-run the sibling `offsite-borg-smoke-selftest` (belongs to the §d3/§d4 sibling items, already green at their close) nor a full `nix flake check --no-build` (fleet-state check; the single-check build already forces the full evo-x2 `extendModules` eval, and parallel sessions churn the tree — a fleet-wide green here would prove little about this item). Defensible scoping, noted for transparency.
- **Eval-cache honesty.** The positive build's store path (`c9nn18w5…`) is indistinguishable from an eval-cache replay (same flake.nix content = same eval result, per the negative-test-convention doctrine). The positive build alone therefore has no teeth; the drift probe is what makes this verification mean something. Ran it; documented.
- **Sequential when parallelizable.** The positive build and the negative probe were independent; I ran them sequentially. Cost: ~1 extra eval-eval of latency. Trivial.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| a1 | The work item itself — `checks.x86_64-linux.offsite-borg-positive-render` in flake.nix (lines 1920-1964, next to `offsite-borg-smoke-selftest`): `extendModules` render of evo-x2 with `services.offsite-borg.enable = lib.mkOverride 50 true` (the plain `enable = false` in configuration.nix forces every probe override to ≤50) + `builtins.deepSeq` eval asserts on the rendered `borgbackup-job-hetzner.serviceConfig` | Commit `14fc6816` (HEAD); flake.nix:1936-1964 |
| a2 | The four go-live deliverables asserted at eval time: `IOSchedulingClass == "best-effort"` + `IOSchedulingPriority == 6` (ioTier.background mkForce intact), ExecStartPre carries `borg-offsite-golive-check` (PLACEHOLDER tripwire), ExecStartPost carries `/var/lib/borg-offsite/.last_success` (freshness marker), `EnvironmentFile == sops.templates."borg-env".path` (compared against the template's own `.path` — drift on either side fails) | flake.nix:1951-1957 |
| a3 | Exec-line asserts route through a list-normalizing helper (`execLines`), so a future nixpkgs-side list merge of Exec* entries still matches — future-proofing present in the committed shape | flake.nix:1947-1950 |
| a4 | Fires on every `nix flake check` incl. `--no-build` (deepSeq forces the asserts when the check attr is forced — disko-samsung-tlc pattern); the check comment block documents the priority-50 rationale for the next probe author | flake.nix:1920-1935 |
| a5 | Prior dispatch's negative hand-probes (per closure rows): priority-49 idle override renders `idle` and the guard expression throws; priority ≥50 (plain/mkDefault/mkForce-tie) correctly leaves BE/6 untouched — `lib.mkForce` IS priority 50, so 98/100-class probes lose | TODO_LIST.md:37 + docs/todo/storage.md:51 closure text |
| a6 | THIS dispatch's independent verification — positive: `nix build .#checks.x86_64-linux.offsite-borg-positive-render` green (`/nix/store/c9nn18w5…-offsite-borg-positive-render-check`) | This run |
| a7 | THIS dispatch's independent verification — negative: throwaway `extendModules` with `enable = mkOverride 50 true` + `IOSchedulingClass = mkOverride 49 "idle"` renders `idle` and the assert throws exactly `string '"idle"' is not equal to string '"best-effort"'` — proves the guard reads the rendered config, is priority-sensitive, and is not a constant | This run, pure `nix eval --impure` (no tree contact) |
| a8 | Queue loop closed on BOTH surfaces, no drift: TODO_LIST.md:37 `[x]` + DONE summary naming the check, the override rationale, and both probe classes; docs/todo/storage.md:51 `[x]` + full DONE row with verification narrative | Read this run; commits in `14fc6816` |
| a9 | AGENTS.md Offsite Borg section updated: the `offsite-borg-positive-render` check is documented alongside the smoke-lib hardening, incl. the priority-50 doctrine (`mkForce` is 50, drift must be <50; configuration.nix's plain `false` forces ≤50) and "permanent, fires on every flake check incl. `--no-build`" | AGENTS.md, read this run |
| a10 | Commit hygiene: `14fc6816` carries the exact Task-Queue-ID footer, names its motivation ("ephemeral hand probe re-run at every dispatch"), and documents both negative probes in the body | `git log -1 14fc6816`, read this run |

## b) PARTIALLY DONE

| # | Item | Works now | Remains open | Blocker | Effort |
|---|------|-----------|--------------|---------|--------|
| b1 | First REAL runtime exercise of the gated surface | The eval-time render + asserts are permanent and green | The runtime PASS branches (§13/§16 asserts, tripwire actually gating a real go-live) fire only on the go-live deploy — enable-gated by design | Owner go-live inputs (StorageBox hostname/username, `ssh-keyscan -p 23` host-key pin, borg-password recovery-copy policy — storage.md [decision]/todo rows) | S (rides go-live) |
| b2 | "Committed artifact pinning the REAL nix shapes" (04:02 report's b3, which folded into this item) | HALF-RESOLVED: the check now permanently pins the rendered unit surface (IO values, tripwire, marker, env path) at eval — a nixpkgs reword of the UNIT render or the ioTier shape now fails loud | The §13-side shapes remain unpinned: the eval-error TEXT the disabled-shape classifier (`ob_classify_eval_error`) matches, and the eval JSON key casing §13 jq-reads — a nix reword there still needs re-discovery (the prior chain's real-stderr fixture idea, still open) | none | S |
| b3 | Guard testability | Guard is committed, commented, and drift-sensitive | Guard expression is inline in the check attr — cannot be driven by external probes on mutated inputs (this session's probe had to replicate the assert, see Self-review); extraction to a shared pure lib function would let probes and the check share one implementation | none | S |

## c) NOT STARTED

| # | Item | Note |
|---|------|------|
| c1 | §f2: "Document the nixpkgs override-shape change for probe authors" — inline override attrsets now need `{ _type = "override"; priority = 50; content = …; }` (inner field `content`, NOT `value`; error `attribute 'content' missing` at `lib/modules.nix:1458`), plus the plain-`enable = false` forces-≤50 conflict class | Already routed: unchecked `[ready]` item in TODO_LIST → docs/todo/pipeline.md:154 (which already carries the technical content — the TODO row tracks the queue work, not the knowledge). Separate queue item; deliberately not touched this run |
| c2 | "Prove shellcheck actually covers `scripts/*.sh` in pre-commit" (§b.2/§f.7) | Unchecked `[ready]` in TODO_LIST + storage.md. Separate queue item |
| c3 | Post-go-live restore-drill cadence ([decision]): scheduled timer + OnFailure vs manual-only | Owner decision queued in storage.md. Separate item |
| c4 | VM rehearsal of the runtime PASS/FAIL shapes (see f6) | New idea from this run, not yet queued before this report (f6 adds it) |

## d) TOTALLY FUCKED UP

Nothing. This session produced zero tree changes, zero broken states, and one report. The only drama was discovering the item pre-completed — which is the shared-queue working as designed (parallel dispatches), not a failure. The one genuine limitation (replica-vs-real-guard probe, b3) is documented above with a fix path; it does not affect the committed artifact's correctness.

## e) WHAT WE SHOULD IMPROVE

1. **Queue-side "already done" fast-path:** when a dispatch finds its item pre-closed by a prior dispatch of the SAME queue ID, the verify-and-report shape (this run) is right — but the queue could surface "item X has a footer commit already" at dispatch time to skip redundant re-verification evals.
2. **Extract the render guard into a shared pure lib function** (`(svc, envTemplate) → asserts`), consumed by the flake check and exercisable by throwaway probes — closes the replica-vs-real-guard gap permanently (b3/f4).
3. **Pin the remaining eval shapes** the §13 classifier depends on (disabled-shape eval-error text, JSON key casing) in a fixture or check, so nix rewords fail at eval instead of at the next deploy gate (b2/f5).
4. **Status-report convention nit (in passing):** the 04:02 sibling report's cross-reference to this item ("folds with the still-open … storage.md:51") was accurate at write time and resolved 43 min later — chain reports that fold into each other could carry a "resolved by" follow-up line when the folding item closes. Low value, cosmetic.
5. **Nothing else.** The committed check is well-commented, future-proofed (`execLines`), correctly layered (eval-time vs deploy-gate), and its closure narrative documents the exact probe methodology — above the bar.

## f) Up to 50 things we should get done next (honest list — quality over count; drawn ONLY from this session's observations)

| # | Item | Source |
|---|------|--------|
| f1 | Execute the queued §f2 item: finalize probe-author docs for the nixpkgs override-shape change (`content` not `value`) + plain-`enable=false` ≤50 conflict — content already drafted in docs/todo/pipeline.md:154, needs the TODO row worked | TODO_LIST unchecked row (this run's first grep) |
| f2 | Execute the queued shellcheck-coverage proof (§b.2/§f.7): verify the pre-commit hook's `.sh` leg actually runs over `scripts/*.sh` | TODO_LIST + storage.md unchecked rows |
| f3 | Owner-decision item: post-go-live restore-drill cadence (timer+OnFailure vs manual-only) | storage.md `[decision]` row |
| f4 | Extract the positive-render guard into a shared pure lib function so probes can drive the REAL guard on mutated inputs (closes b3) | This run, Self-review |
| f5 | Pin the §13 classifier's eval shapes (disabled-shape error text, JSON key casing) in a committed fixture so nix rewords fail at eval, not at the next deploy gate (completes 04:02-report b3) | 04:02 report b2/b3, re-read this run |
| f6 | VM rehearsal: enable offsite-borg with mock sops in a NixOS VM test to exercise the runtime FAIL shapes (tripwire fires with the checklist pointer on PLACEHOLDER env; §16 grep patterns against a REAL rendered unit in a booted system — the eval asserts only prove the render, not a booted unit file) | This run's b1 reasoning |
| f7 | Go-live execution (owner-gated): StorageBox hostname/username, `ssh-keyscan -p 23` pin into `borg_known_hosts`, borg-password recovery-copy policy decision, then the deploy that turns all the PASS branches live | storage.md go-live rows (in passing) |
| f8 | After go-live: the real-repo restore drill (`scripts/borg-restore-drill.sh` real mode = go-live checklist step 9) — the drill hardening (mountpoint gate, record ordering, `--verify-data`) shipped 2026-09-24 but the real-mode path has only stand-in proof | AGENTS.md offsite-borg section (in passing) |
| f9 | Sweeping rule candidate from this chain: eval-time checks that inline their guard (this check, disko-samsung-tlc) can't be negative-probed without tree edits — a one-paragraph doctrine note in docs/CONTRIBUTING.md "Eval-Time Guards" ("inline guards: probe via replicated assert; shared-lib guards: probe directly") would save the next author the discovery | This run, Self-review |
| f10 | When ioTier doctrine next changes, `grep -rn "best-effort"` across the check + smoke lib + ioTier table — three sites must move in lockstep (eval check, §13/§16 asserts, lib/systemd ioTier); the duplication is deliberate defense-in-depth but untracked | This run's read of both assert sites |

## g) Questions I cannot figure out myself

1. **Re-dispatch verification policy:** when a queue item arrives already complete (prior dispatch, footer commit present), should the verifying session ALSO produce a small "verified" no-op commit (timestamping verification in git, at the cost of commit noise), or is report-commit-only — this run's shape — the desired convention?
2. **Restore-drill cadence (f3, the standing `[decision]`):** given the BX11 is a 1 TB-class WAN remote and a failed scheduled drill pages via OnFailure, is the owner's risk tolerance closer to "scheduled timer catches repo/credential rot" or "manual-only, no WAN-flap noise"? A one-word leaning unblocks the queue item.
3. **Go-live timing (f7):** are the StorageBox inputs (hostname/username, host-key pin, borg-password recovery-copy policy) still on the near-term agenda? Every runtime PASS branch (§13/§16, tripwire, marker, drill real-mode) is sequenced behind them — if go-live slipped indefinitely, f6's VM rehearsal becomes the only way those shapes ever get runtime-exercised, which would reorder the backlog.

---

**Verification commands (reproducible):**

```bash
nix build .#checks.x86_64-linux.offsite-borg-positive-render --no-link --print-out-paths
# negative probe (no tree contact): mkOverride 49 "idle" must throw
nix eval --impure --expr 'let
  lib = (builtins.getFlake "nixpkgs").lib;
  f = builtins.getFlake (toString /home/lars/projects/SystemNix);
  sys = f.nixosConfigurations.evo-x2.extendModules { modules = [
    { services.offsite-borg.enable = lib.mkOverride 50 true; }
    { systemd.services.borgbackup-job-hetzner.serviceConfig.IOSchedulingClass = lib.mkOverride 49 "idle"; }
  ]; };
  svc = sys.config.systemd.services.borgbackup-job-hetzner.serviceConfig;
in builtins.deepSeq (assert svc.IOSchedulingClass == "best-effort"; true) "UNEXPECTED"'
# → error: string '"idle"' is not equal to string '"best-effort"'
```
