# Task 000001a0d5af4c01977b1f874e6689edcaae — Status + brutal self-review (verification dispatch 2)

Date: 2026-09-25 02:20 CEST · HEAD: `0eddc2e8` · Scope: THIS task only — re-verification of the
already-landed borg env-path-pin fixture fold-in, plus what this session noticed in passing. No
unrelated research.

Format note: the status-report skill's canonical output is styled HTML; this dispatch explicitly
demands this `.md` path, so markdown wins (same one-off as this task's two prior reports, not
propagated into the skill).

Series note: this is the THIRD report for this task ID (after `02-00` and `02-07`). The `02-07`
report's §h addendum (appended by this session's earlier work commit) is superseded by §a below —
this report is the fuller record of the same verification pass plus the two probes completed
during report preparation.

**TL;DR:** The dispatched fold-in was already landed and queue-closed by prior dispatches of the
same ID; this session independently re-verified ALL FOUR guard directions of
`checks.x86_64-linux.borg-restore-drill-fixture` with persisted live probes (control green,
script-drift red, template-override red, anchor-rot red) and independently re-derived the
harness-equivalence claim by diffing the fold-in commit. Build-level verification remains
host-blocked (the known `/run/binfmt` boot casualty, live 4h+, still zero detection). The
session's own worst moment: my work commit shipped with a 78-char subject against the 72-char
house rule — caught during THIS report's preparation and repaired by amend-forward
(`dcefaaa0` → `0eddc2e8`), but it shipped broken for ~3 minutes and only my own review caught it.

---

## §a FULLY DONE

| # | Item | Evidence |
| --- | --- | --- |
| a1 | Task item confirmed complete BEFORE this dispatch's changes: fold-in landed (`0400a345`, flake.nix +91/−3), docs closed on all four surfaces | Read, not trusted: `TODO_LIST.md:41` `[x]`, `docs/todo/storage.md:47` `[x]`, CHANGELOG entry, AGENTS.md drill paragraph — all verified in-tree this session |
| a2 | Green direction re-verified independently on the committed tree | `nix eval .#checks.x86_64-linux.borg-restore-drill-fixture.drvPath` → `/nix/store/fchgkl7x…-borg-restore-drill-fixture.drv`; the deepSeq'd guards run before `derivationStrict`, so the drvPath result proves all four asserts evaluated clean |
| a3 | Red direction (script-drift) re-verified — the queue item's core acceptance property: drifted `BORG_ENV_FILE` literal → the `backup.nix` assertion fires | Throwaway `git worktree`, sed-drift `…/borg-env` → `…/borg-env-DRIFT`, eval → RED: the fixture's `assert controlFailing == [ ]` threw and the error body IS the real `backup.nix` pin assertion (`assertion = false`) naming the expected literal `BORG_ENV_FILE="${BORG_ENV_FILE:-/run/secrets/rendered/borg-env}"`. (The control guard fires first — real module vs drifted REAL script — before the drifted-copy machinery is reached; same observable class.) |
| a4 | `02-07` §b4 CLOSED: template-override direction live-probed end-to-end and PERSISTED (the d6 "unpersisted hand probe" class fixed) | Minimal `nixosSystem` (integration + sops-nix + backup.nix, intact drill) with `sops.templates."borg-env".path = lib.mkOverride 50 "/run/secrets/rendered/borg-env-PROBE"` → the pin fires and the message names the FORCED path (`borg-env-PROBE`): the pin consults the template's real `.path`, not just the dormant default shape. Recorded in the `02-07` report §h |
| a5 | NEW: anchor-rot guard live-probed — the fourth guard direction, never probed by any prior dispatch | Worktree with the anchor line rotated (`scripts/borg-restore-drill.sh;` → `scripts/./borg-restore-drill.sh;`, still-valid path, evals fine) → the fixture throws its OWN message: `env-pin negative-case anchor no longer matches platforms/nixos/system/backup.nix — update scriptAnchor in the fixture`. The rot-detection escape hatch works |
| a6 | Harness-equivalence independently confirmed by diffing `0400a345` (previously inherited from the prior session's a4 claim — now first-hand) | The commit's only removed lines are the bare `pkgs.runCommand` line (rewrapped into `builtins.deepSeq pinGuards (…)`), the old PASS marker, and the closing `'';` — ZERO scenario lines (`need_rc`/`need_has`/run/stub) touched. The behavioral harness is byte-identical to the previously build-green `bdz4gkd…` era |
| a7 | Probe hygiene | Both throwaway worktrees created, mutated, evaluated, REMOVED, pruned; main tree clean throughout (verified before and after each commit) |
| a8 | Work commit repaired and finalized: `dcefaaa0` (78-char subject, rule violation) amended forward → `0eddc2e8` (56 chars) | Footer intact as last line; history-rewrite checklist run: no refs pin the old SHA (`git for-each-ref` = 0 hits), commit was unpushed HEAD, tree clean |
| a9 | Verification persisted in the queue's cross-reference surfaces | `02-07` report §h addendum + this report, both committed with the Task-Queue-ID footer; one NEW process item filed queue-side (commit-msg 72-char lint, §d1) |

## §b PARTIALLY DONE

| # | Item | What works | What's missing | Blocker | Effort |
| --- | --- | --- | --- | --- | --- |
| b1 | Build-level verification of the guarded fixture drv | Eval-green (a2) + harness first-hand-verified unchanged (a6) | `nix build .#checks.x86_64-linux.borg-restore-drill-fixture` never succeeded | HOST: `getting attributes of path "/run/binfmt": No such file or directory` — re-attempted this dispatch (02:11), casualty unchanged. Needs `sudo systemd-tmpfiles --create` (§g q1) or the cycle fix + reboot | S post-fix |
| b2 | Whole-flake `nix flake check --no-build` green | The failure SHAPE was re-documented this dispatch for free: my (aborted) commit attempt's flake-check leg failed on the identical formatter/`derivationStrict`/`/run/binfmt` error | The green re-run itself | Same host blocker as b1 | S post-fix |
| b3 | Formal post-fix probe re-run (`02-07` §f33) | All four probe directions now done PRE-fix with persisted evidence | The "re-run from a clean shell after the host fix" formal close | Same host blocker | S post-fix |

## §c NOT STARTED (deliberately — queued or owner-gated, with pointers)

| # | Item | Where it lives | Why untouched |
| --- | --- | --- | --- |
| c1 | Root-fix the `hot-user-caches-nix-bootstrap` ↔ `systemd-tmpfiles-setup` ordering cycle + the tmpfiles-applied tripwire | Filed `[ready]`, TODO_LIST → stability + docs/todo/stability.md (prior dispatch) | Distinct queued work; this is a verification dispatch |
| c2 | Fixture-negative for the fixture itself (mutate a drill copy INSIDE the fixture → fixture must FAIL) | TODO_LIST:42 / storage.md:117 | Pre-existing queue row; no scope creep |
| c3 | Mountpoint-gate fixture reachability (env override for `/mnt/hot`) | storage.md:125 | Pre-existing queue row |
| c4 | Offsite-borg go-live owner inputs (borg.yaml host/user + known_hosts pin) | docs/todo/storage.md `[blocked:user]` | Owner hands only |
| c5 | The `02-07` §f host/process clusters (items 1-13, 21-33 there) | That report §f | Carried; §f below restates the still-open set |

## §d TOTALLY FUCKED UP (this dispatch, ranked)

**Nothing landed by this task is broken — the fold-in and the pin guard are sound (§a). Every
item below is process/verification hygiene MINE. The biggest genuinely fucked-up thing in view is
still the HOST: the boot casualty (`systemd-tmpfiles-setup` deleted from the transaction → no
`/run/binfmt` → every sandbox build fails) is now 4h+ old, has zero detection, and has now cost
FOUR agent sessions their build capability — and it still pages nobody.**

| # | Fucked up | Why it matters |
| --- | --- | --- |
| d1 | **My work commit shipped with a 78-char subject against the explicit 72-char house rule** — and I did not notice until writing THIS report | The rule is in my face at every commit; nothing enforces it, so it relied on discipline, and my discipline failed for the exact commit this task will be judged by. Repaired by amend-forward (`0eddc2e8`) only because it was still unpushed HEAD — one later commit and the fix would have been a rebase. Filed the enforcement gap queue-side (commit-msg lint leg) so the next session doesn't need discipline, it needs a hook |
| d2 | **The `02-07` report's own d5 class RECURRED in me: I constructed the §h addendum's edit `old_string` from memory instead of copying it from the earlier view** — footer blank-line mismatch, one failed edit round | The prior report flagged exactly this sloppiness in its author, hours before I repeated it. Reading-then-not-copying is functionally the same as not reading. The fix cost 30 seconds; the pattern costs trust in every diff I claim to have verified |
| d3 | **I ran a commit attempt I could PREDICT would abort**: the pre-commit flake-check leg was already proven deterministically red this boot (host casualty), yet I fired the plain commit first | The only defense is that gitleaks + whitespace legs ran and passed — but those run under `--no-verify`'s alternative too (they'd have run in the successful attempt). The honest sequencing was `--no-verify` immediately; the documented precedent came from THIS task's own prior dispatch the same night |
| d4 | **Misleading tool output left in the session transcript**: the script-drift probe command printed `EXIT: 0` directly beside a failed eval (PIPESTATUS not valid in that shell context) | I interpreted the evidence correctly, but a future session auditing the raw transcript sees a self-contradicting record — the exact "assert WHICH entity served it" class. Fix: explicit `rc=$?` capture, always |
| d5 | **I initially carried the prior session's harness-equivalence claim (their a4) into my §h addendum without first-hand verification** — closed the gap only during THIS report's preparation (now a6) | A verification dispatch that inherits ANY load-bearing claim partially defeats its purpose; the whole value of a re-dispatch is independence. The claim happened to be true; that was luck, not process |
| d6 | **Scope judgment exercised but not documented in the artifact**: I added two probes beyond the queue item's literal ask (anchor-rot, harness diff) — both verify THIS task's fixture, so in scope, but the §h addendum didn't say why | The next auditor shouldn't have to re-derive my scope reasoning; one sentence in the addendum would have made the additions self-justifying |

## §e WHAT WE SHOULD IMPROVE

1. **Copy-paste exact text for edits — never reconstruct.** Concrete rule: every `old_string` is transcribed from the immediately preceding view of the target lines, no exceptions (d2; same lesson as `02-07` d5 — it recurred within hours, so the fix is a mechanical rule, not a reminder).
2. **Commit-subject length: stop relying on discipline.** Filed: a one-line commit-msg hook leg rejecting >72-char subjects (queue, pipeline.md). Impact: prevents the d1 class fleet-wide at zero cost.
3. **Known-red gate legs: decide the escape upfront.** When a gate leg is proven host-caused THIS boot, go straight to the documented `--no-verify` path (d3); re-deriving the decision per commit wastes a cycle and pollutes history with known-dead attempts.
4. **Persist probe commands AND outputs together.** The §h addendum carries results; future addenda should carry the exact commands beside them so a re-run is copy-paste (supports the 02-07 §e.3 "persist probe artifacts" convention).
5. **rc-capture discipline in probe commands** (d4): `cmd; rc=$?` — never PIPESTATUS after a pipeline in shells where it isn't defined.
6. **The host casualty tax keeps compounding.** Four sessions now re-derived "builds are broken" independently; the §g q1 one-liner remains the single highest-leverage action available to anyone with root, and until it runs, every dispatch pays a diagnosis toll. This is the standing argument for answering q1 with "now".

## §f NEXT (33 honest items — ✅ closed by this dispatch, 📋 already queued, ★ carried/open; the queue's pacing gates decide the rest)

**Host / boot-transaction cluster (the urgent one):**
1. ★ USER (root, one command, unblocks ALL builds today): `sudo systemd-tmpfiles --create` — then `ls /run/binfmt/` and `systemctl restart systemd-binfmt`
2. 📋 Root-fix the `hot-user-caches` ↔ `systemd-tmpfiles-setup` cycle (VM full-boot-transaction diagnosis first) — filed
3. 📋 Persist the tmpfiles-applied boot tripwire (binfmt canary / cycle-deletion journal scan) — filed
4. ★ Post-fix: `nix build .#checks.x86_64-linux.borg-restore-drill-fixture` (closes §b1)
5. ★ Post-fix: full `nix flake check --no-build` green re-run (closes §b2)
6. ★ Post-fix: clean-shell re-run of all four probe directions (closes §b3)
7. ★ Post-fix: one emulated (aarch64) build to prove the `L+` interpreter symlink restores emulation
8. ★ Diagnose why `derivationStrict` stats `/run/binfmt` for the formatter derivation; make the eval gate hermetic (§02-07 e.6)
9. ★ Audit this boot's OTHER missing tmpfiles state (credstore, lastlog2, journal-nocow, graphics rules vs live `/run`)
10. ★ Boot-casualty detector: journal-scan collector for "deleted to break ordering cycle" lines (class-wide)
11. ★ VM regression test: full boot with hot-user-caches enabled asserts tmpfiles-setup's job SURVIVES the transaction
12. ★ Annotate the pipeline row's 2026-09-25 gate-failure instances + the 00-20 report's "3 distinct shapes" with the `/run/binfmt` root cause (docs-health ANNOTATE)

**Offsite-borg / fixture cluster (this task's neighborhood):**
13. 📋 Fixture-negative for `borg-restore-drill-fixture` (mutate a drill copy inside the fixture → fixture FAILS) — TODO_LIST:42
14. 📋 Mountpoint-gate fixture reachability via env override — storage.md:125
15. ✅ Template-override probe — CLOSED (§a4)
16. ✅ Anchor-rot probe — CLOSED (§a5)
17. ✅ Harness-equivalence diff — CLOSED (§a6)
18. ★ After the next deploy: confirm the offsite-borg §13/§16 smoke entries still pass
19. 📋 Offsite-borg go-live inputs (borg.yaml repo host/user + known_hosts pin) — owner-gated
20. ★ When builds heal: spot-verify `backup_ever_succeeded`/freshness rows green after the outage window

**Queue / process hygiene cluster:**
21. ★ NEW (filed this dispatch): commit-msg hook leg rejecting >72-char subjects — pipeline.md + TODO_LIST
22. ★ Codify "build-verified vs eval-verified" as a required close-out field (CONTRIBUTING one-liner)
23. ★ Codify the eval-order argument (guards-before-derivationStrict makes `drvPath` sound evidence) as a testing-pattern note
24. ★ Generalize `negative-test-lints.sh`-style negative testing to behavioral fixture checks (subsumes "fold the probes into a scripted check" — don't build both, per 02-07 item 34)
25. ★ Host-health banner convention in TODO_LIST for agents (§02-07 e.5)
26. ★ Sweep recent close-out docs for unpersisted hand-probe claims; annotate or re-probe (d6-of-02-07 class)
27. 📋 "Status reports self-harvest §f at authoring time" codification — TODO_LIST:49 (this report self-harvests: items 21 filed, ✅ items marked)
28. 📋 Re-dispatch verification protocol codification — TODO_LIST:50 (this dispatch is a worked example of its steps 1-4)

**Noticed in passing (real, small, carried — from this window's tool output):**
29. ★ `tq-serve`/`tq-agent-pool` carry `startLimitBurst`/`startLimitIntervalSec` in `[Service]` — the silently-ignored placement class (02-07 item 26)
30. ★ `projects-management-automation.service` malformed Environment lines — check whether `GIT_AUTHOR_*` identity lines are casualties (Unknown-Author incident class)
31. ★ `cups.socket` legacy `/var/run/cups` ListenStream — one-line fix
32. ★ `pocket-id.service` Type=simple + ExecStartPost credentials race — evaluate Type=notify-vs-exec
33. ★ Gate enumeration mode (`--keep-going`-style) so one broken check doesn't hide the rest; stale worktree `~/.cache/immich-audit/nixtest/sn-base` triage; `disko-layout` re-test in isolation post-fix

*(33 items; padding to 50 would be queue spam. New-fuel items this dispatch: #21 only — everything else was already queued or carried; §a4-§a6 CLOSED three.)*

## §g QUESTIONS (3 — owner calls I cannot make myself; all three re-confirmed by this dispatch)

1. **Host recovery timing (carried, now 4h+ old):** run `sudo systemd-tmpfiles --create` NOW — the safe, idempotent one-liner that unblocks every agent's builds immediately, no reboot needed — or leave the box as-is until the cycle fix lands and reboots in? Every session until then re-pays the diagnosis toll and falls back to `--no-verify`.
2. **Tripwire severity (carried):** if a boot job is ever deleted from the transaction again ("deleted to break an ordering cycle"), should that PAGE (sev1 warn banner, house pattern for infra criticals) or also HARD-BLOCK deploys via a pre-deploy gate leg (stronger, but a false positive bricks all agent work until root intervenes)?
3. **Degraded-gate policy (carried; my own d3 is the freshest data point — the gate burned a guaranteed-abort commit cycle tonight):** while the box cannot build, keep the pre-commit flake-check leg fully red (honest, noisy, normalizing `--no-verify`) or add a sanctioned host-caused-failure skip (cleaner, but creates a habit-shaped hole in the gate)?

---

Task-Queue-ID: 000001a0d5af4c01977b1f874e6689edcaae
