# Status Report — task-000001a0c6bd943ab3a1f2f046476b881acf (review-fix: stale cargo comment)

- **Date:** 2026-09-22 07:15 CEST
- **Ticket:** review-fix for finding (low) "Stale home.nix cargo comment and AGENTS.md buildcache section now contradict the new symlinks" — descends from original task `000001a0c5fdb2dd3166a37c82d89dc5be6c` (storage: cache-fallback sweep, rejected commit `f39e144b`)
- **Work commit:** `db2b44c0` (footer: Task-Queue-ID 000001a0c6bd943ab3a1f2f046476b881acf)
- **Format note:** the status-report skill's canonical output is styled HTML; the user explicitly requested `.md` at this exact path, so Markdown wins for this report (one-off override, not propagated anywhere).
- **SUPERSEDED on disposition (2026-09-22 08:03):** the queue re-dispatched this ticket; the verification-only re-run confirmed this report's disposition stands (fix `db2b44c0` footered + live, AGENTS.md half satisfied, gates green) and made no further change — see `2026-09-22_08-03_task-000001a0c6bd943ab3a1f2f046476b881acf.md` for the re-dispatch evidence and the queue-idempotency finding. This report's sections b-f remain the authoritative open-item inventory.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | Finding disposition executed per protocol: rejected commit `f39e144b` verified to still exist (`git cat-file -e` OK) AND verbatim anchor text verified live in the current tree (`platforms/nixos/users/home.nix`, `~/.cargo remains as a plain dir only for tools…cargo itself no longer touches it.`) — finding applies, no re-anchoring needed | command output, this session |
| 2 | Stale home.nix comment corrected: reworded so both cargo paths are documented as landing on the buildcache mount — env'd cargo writes to `CARGO_HOME` here; env-less cargo (CARGO_HOME absent) falls back to `~/.cargo/registry`, which the 2026-09-22 HM symlink sweep redirects onto the same registry | `db2b44c0`, diff +4/−2, `platforms/nixos/users/home.nix:596-599` |
| 3 | AGENTS.md half of the finding verified ALREADY satisfied in the current tree — all four expected elements present in the Build Cache SSD section: (1) three new symlinks (AGENTS.md:739 Consumers bullet + :748), (2) the `migrate-buildcache-fallback-caches` activation migration (:748), (3) the reap-stack/reap-list change ("FOUR hand-kept surfaces", :748), (4) the deploy.sh pre-switch gap from finding 1 (`d60d598c`, :748) — landed by the intervening docs pass `59fc4294`, so NO duplicate doc edit was made (re-adding it would have violated the no-drift rules) | AGENTS.md:739,748 + `git log -S` attribution to `59fc4294` |
| 4 | Verification gates green: `nix eval .#nixosConfigurations.evo-x2.config.system.build.toplevel.drvPath` succeeded; pre-commit suite passed twice (gitleaks, trailing whitespace, deadnix, statix, treefmt, eval-only `nix flake check`) — once on the daemon's commit, once on the amend | hook output, this session |
| 5 | Commit hygiene: the auto-commit daemon raced the fix into heuristic commit `bfc692bd`; contents verified (`git show --stat`: exactly 1 file, home.nix, only my change) before amending per the multi-agent write discipline → `db2b44c0` with exactly ONE Task-Queue-ID footer. Working tree clean. Nothing pushed. | `git show --stat bfc692bd`, `git log -1 --format=%B` |
| 6 | Bonus in-scope verification: swept the LIVE tree (docs/ minus status/todo/planning archives, README, modules) for sibling claims falsified by the sweep — ZERO live falsified claims remain. The only `~/.cargo` mentions outside status/todo are historical research/brainstorm proposals (2026-07-11 btrfs-wiki doc, secondary-ssd proposal), which describe superseded designs, not current-state claims | grep output, this session |
| 7 | Correctly scoped: comment-only change, no deploy run (zero runtime effect; the next natural deploy picks it up), no unrelated files touched, TODO_LIST.md untouched (fix tickets descend via footer; the queue derives lineage) | `git show db2b44c0 --stat` |

## b) PARTIALLY DONE

| # | Item | What works | What remains open | Effort |
|---|------|-----------|-------------------|--------|
| 1 | 2026-09-22 cache-fallback sweep cluster (this task's lineage) | HM symlinks for `~/.cache/pnpm`, `~/.local/state/pnpm`, `~/.cargo/registry` (home.nix:418-420); mount-gated `migrate-buildcache-fallback-caches` activation block (home.nix:472-482); reap stack covers all names in deploy.sh (both loops, `d60d598c`) + `buildcache-usb-recovery` step 2.5; AGENTS.md fully documents it | `buildcache-init` does NOT provision the three new targets (post-USB-recovery the symlinks dangle until the next deploy — documented "queued" in AGENTS.md:748); reap surfaces are FOUR hand-kept lists ("single-sourcing is queued"); no VM/regression test for the activation block | S–M each |
| 2 | Original work item (TODO_LIST.md:21, storage) | Cache-fallback sweep executed + converged; review cycle now fully closed (finding 1 fix `d60d598c` by another run, AGENTS.md docs `59fc4294`, stale comment `db2b44c0` this run) | Item deliberately still `- [ ]` with a BLOCKED note: (a) VM-test rebuild was PSI-gated (io avg10 ~68% at session time, gate <20%), (b) the mountPoint-vs-HM-symlink eval guard "left for queue pacing". Neither has been picked up since | M |
| 3 | This fix ticket | 100% of the finding addressed | None outstanding. Lineage nuance for the record: the ticket's literal expected fix had two halves; the home.nix half landed here, the AGENTS.md half was satisfied by the intervening `59fc4294` — the finding was partially stale relative to the tree at fix time | — |

## c) NOT STARTED

Everything below is real, named, and untouched (no code written). Priority per the queue's gates, not this report.

1. **VM-test rebuild for the buildcache sweep** — BLOCKED at the original run by the deploy PSI gate (io avg10 ~68%, gate <20%); TODO_LIST.md:21 BLOCKED note. Still wanted.
2. **mountPoint-vs-HM-symlink eval guard** — TODO_LIST.md:21 "later run"; the 2026-09-20 class (systemd canonicalizes mount units through HM symlinks → nested-autofs exit-4).
3. **`buildcache-init` provisioning of the three new targets** (pnpm-cache, pnpm-state, cargo/registry) — documented queued gap, AGENTS.md:748.
4. **Reap-surface single-sourcing** — four hand-kept lists (deploy.sh `.cache` loop, deploy.sh non-`.cache` loop, `buildcache-usb-recovery` step 2.5 both loops, HM activation block); already drifted once (finding 1 was exactly the missing deploy.sh leg).
5. **Consistency gate** asserting HM symlink names == reap lists == init provisioning targets (would have caught the finding-1 class mechanically).
6. **Closure of the reviewer-referenced "status-report backlog items 43/44"** — location never verified; they now claim AGENTS.md work that `59fc4294` already landed.
7. **`$DRY_RUN_CMD` alignment for the migration block's `rm -rf`** (home.nix:477) — the sibling `jan-data-link` block in the same file wraps destructive ops in `$DRY_RUN_CMD`; the cache-reap block does not (noticed in passing; data is rebuildable cache, so severity is low, but the discipline exists).
8. **VM/regression test for `migrate-buildcache-fallback-caches`** (real-dir occupant removed; mount-absent clean skip; idempotent second run).
9. **Triage of an eval-warning fragment** ("the module header.") seen in the tail of the `nix eval` toplevel output this session — eval succeeded, warning un-investigated (out of fix scope), likely pre-existing.
10. **`/mnt/hot` scrub family + metrics** (storage.md "Pool + disk-domain quality" row item 1; noticed in passing: 45 GiB of live crush session DBs on the hot tier with ZERO scrub coverage since 2026-09-18) — pre-existing queue row with 13 open sub-items, referenced here because this task's lineage points at that row.
11. **docs/services runbook for the buildcache domain** — verified absent (no buildcache/cache file in docs/services/); the domain has now produced two review rounds, which is the house signal for a runbook.
12. **Annotate the two historical docs that still describe retired `.cargo`/`@cargo` subvolume designs** (docs/research/2026-07-11 btrfs-wiki recommendations; docs/brainstorming/secondary-ssd-990-proposal.md) as superseded per docs-health ANNOTATE mode, so no future reader mistakes them for current state.
13. **Interim runbook note in AGENTS.md** until item 3 lands: after a USB-outage recovery, pre-create the three targets on the mount (or re-run a deploy) so the symlinks don't dangle.
14. **Cross-project lesson entry** (crush-config repo, references/lessons.md): "a change that falsifies an existing claim must grep the entity repo-wide in the same change" — the stale-comment class the reviewer caught twice now (this + the 2026-09-22 home comment precedent cited in AGENTS.md).

## d) TOTALLY FUCKED UP

**Nothing from this task.** Verified, not vibes: eval green, `nix flake check` green (twice), working tree clean, HEAD properly footered, repo not broken, nothing pushed.

Near-misses that did NOT become damage, recorded for honesty:

1. **Daemon race fired live.** The auto-commit daemon committed my in-flight fix as heuristic `bfc692bd` between my `git add` and `git commit`; my commit then exited 1 ("nothing to commit") with confusing interleaved hook output in a background shell. Handled per the multi-agent write discipline: verified `git show --stat HEAD` contained exactly one file (mine) BEFORE amending — an unverified amend would have absorbed any foreign files the daemon had batched. No mess left.
2. **The original run's real miss is already on the record:** the rejected work created an in-file split brain (the new `~/.cargo/registry` symlink comment at home.nix:409-417 vs the falsified `CARGO_HOME` comment 90 lines below) and deferred AGENTS.md memory-maintenance to a backlog — the reviewer caught both as this finding. The fix run healed the tree; the process lessons are in section (e).
3. Pre-existing repo-wide risks (unpushed 35-commit backlog, storage row's open sub-items, etc.) are intentionally NOT listed here as fucked-up — out of this task's scope per instruction, and they live in the queue where they belong.

## e) WHAT WE SHOULD IMPROVE

1. **Split-brain creation is the repeat offender.** The original run wrote a new mechanism comment (symlink) while a sibling comment about the same entity (`CARGO_HOME`, 90 lines away) kept asserting the old world. Concrete rule going forward: any change that falsifies an existing claim triggers an immediate repo-wide grep of the entity name (`~/.cargo`, the path, the var) IN THE SAME CHANGE — not deferred to a backlog. Cost when skipped: a full review round-trip.
2. **Memory-maintenance deferral violated the protocol.** AGENTS.md's update rules say "immediate — at the moment of discovery"; the original run queued the AGENTS.md update into status-report backlog items instead. The docs pass `59fc4294` healed it, but the miss was real and is what half this finding was about.
3. **Hand-kept parallel lists keep drifting.** The reap surface is now FOUR copies of the same knowledge (deploy.sh ×2, buildcache-usb-recovery, HM activation) plus a fifth implicit list (HM symlinks) and a sixth (buildcache-init targets). Finding 1 was a drift between copies; this finding was a drift between comment and code. Every additional copy is a future review finding. Single-source it and gate it.
4. **Commit-before-check raced the daemon.** With the auto-commit daemon active, `git add && git commit` without a fresh `git log -1` immediately prior is a race this session actually lost (harmlessly). Cheap fix: check `git log -1 --oneline` right before committing, or commit with a pathspec immediately after staging.
5. **Verification was hook-mediated.** The flake-check gate ran inside pre-commit rather than as an explicit pre-stage step — adequate, but an explicit gate before staging separates "my change is green" from "the shared tree happened to be green at commit time" (concurrent-session discipline).
6. **Dry-run discipline inconsistency within one file.** `jan-data-link` wraps destructive ops in `$DRY_RUN_CMD`; the new cache-reap block does not. Pick one policy for HM activation blocks and apply it uniformly (see question 2).

## f) NEXT TASKS (20 — deliberately unpadded; ranked by impact)

Impact / Effort / Category per the section guide. This list is HARVEST input for docs-health → TODO_LIST.md / docs/todo/storage.md.

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | Rebuild the buildcache VM test (PSI-gated since the original run) once io PSI avg10 < 20% for a sustained window | High | M | Quality |
| 2 | Add the mountPoint-vs-HM-symlink eval guard (reject `fileSystems` mountPoints that are HM `mkOutOfStoreSymlink` targets — the 2026-09-20 nested-automount exit-4 class) | High | M | Quality |
| 3 | Run docs-health HARVEST on this report: route items 1-9 into TODO_LIST.md / docs/todo/storage.md, items 11-14 as tagged | High | S | Process |
| 4 | Provision the three new cache targets in `buildcache-init` so post-USB-recovery symlinks never dangle until the next deploy | High | S-M | Bug |
| 5 | Single-source the four hand-kept reap surfaces into one shared list consumed by deploy.sh, buildcache-usb-recovery, and the HM activation block | High | M | Quality |
| 6 | Add a consistency gate (flake check or CI script): HM symlink names == reap lists == buildcache-init provisioning targets | High | M | Quality |
| 7 | Locate and close/annotate the reviewer-referenced "status-report backlog items 43/44" — they now claim AGENTS.md work that `59fc4294` already landed | Medium | S | Cleanup |
| 8 | Wrap the migration block's `rm -rf` in `$DRY_RUN_CMD` (or ratify the current unconditional form as policy — see question 2) | Medium | S | Quality |
| 9 | Add a VM regression test for `migrate-buildcache-fallback-caches` (real-dir occupant removed; mount-absent clean skip; idempotent re-run) | Medium | M | Quality |
| 10 | Work the storage.md "Pool + disk-domain quality" row's 13 open sub-items via queue pacing — highest-priority sub-item noticed in passing: `/mnt/hot` scrub family + metrics (45 GiB live crush DBs, zero scrub coverage) | High | M+ | Feature |
| 11 | Triage the "the module header." eval-warning fragment from `nix eval` toplevel output (capture the full warning once, classify benign vs fix) | Low | S | Quality |
| 12 | Write `docs/services/buildcache.md` runbook if the domain produces a third review round (verified absent; two rounds so far) | Low | M | Documentation |
| 13 | Annotate docs/research/2026-07-11 btrfs-wiki doc + secondary-ssd-990 proposal as SUPERSEDED re: `.cargo`/`@cargo` subvolume automount designs (docs-health ANNOTATE, non-destructive) | Low | S | Documentation |
| 14 | Add the interim AGENTS.md note: after USB-outage recovery, pre-create the three cache targets before relying on the symlinks (superseded when item 4 lands) | Low | S | Documentation |
| 15 | Record the cross-project lesson in crush-config references/lessons.md: falsified-claim changes must grep the entity repo-wide in the same change | Medium | S | Process |
| 16 | Record the second lesson: with the auto-commit daemon live, check `git log -1` immediately before commit (or pathspec-commit instantly after staging) | Medium | S | Process |
| 17 | Verify the TODO_LIST.md:21 claim "storage.md row marked done" resolves to an actual `[x]` row in docs/todo/storage.md; fix the pointer if drifted (queue↔library no-drift rule) | Low | S | Cleanup |
| 18 | Extend the audit-script family with a source-grep selftest: HM activation blocks using `rm` without `$DRY_RUN_CMD` (audit-serviceconfig-merge precedent), with an allowlist for the deliberate cases | Low | S | Quality |
| 19 | When the btrfs+zstd buildcache conversion (scripts/buildcache-btrfs-convert.sh, deferred) is next scheduled, fold it into a single maintenance window with items 4-6 (same quiesce requirements) | Low | L | Maintenance |
| 20 | Verify the old `/rust-cache` p9 user-run cleanup is complete (`rmdir /rust-cache`, `@go`/`@npm`/`@cargo` subvol deletes) and file whatever remains | Low | S | Cleanup |

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Harvest priority:** of the buildcache follow-up cluster (items 4-6, 8, 9 — provisioning gap, single-sourcing, consistency gate, dry-run policy, VM test), which should enter the queue first, or does the whole cluster stay parked until the hot-db Phase-2 fold (`services.hot-db` absorbing `crush-hot-db`) lands? The queue's pacing gates own this, but your ordering preference decides what I queue next.
2. **Policy call on dry-run reaping:** should destructive `rm -rf` in HM activation blocks honor `$DRY_RUN_CMD` (like `jan-data-link` does), or is unconditional reaping acceptable for rebuildable cache data? Both are defensible; the answer decides item 8 and whether item 18's audit has any teeth.
3. **Process contract:** when a review-fix ticket invalidates sibling backlog entries (the items 43/44 class — falsified by an intervening commit like `59fc4294`), should the fix run close them as part of the ticket, or are they always separate queue items? This run treated them as out of scope per "fix EXACTLY this finding"; if the queue wants them folded into fix tickets, the tq-pool instructions should say so.

---

*Point-in-time snapshot. Section (f) is HARVEST input — do not let it die in this file. Historical docs are annotated, never rewritten (docs-health ANNOTATE).*
