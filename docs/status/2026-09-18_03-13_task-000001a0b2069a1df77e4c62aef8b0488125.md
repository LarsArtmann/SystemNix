# Task 000001a0b2069a1df77e4c62aef8b0488125 — Hermes Discord-Connectivity Post-Deploy Smoke

**Date:** 2026-09-18 03:13 CEST
**Scope guard:** this report covers ONLY this task's work and what was noticed in passing. It is not a fleet-wide status snapshot.
**Work commit:** `7dba37d0` ("Hermes Discord-connectivity smoke: raise gateway log verbosity to INFO") — carries the Task-Queue-ID footer. The smoke-script edit itself was swept into auto-commit `9d9cfc5a` by the daemon before I could pathspec-commit it (noted below).

---

## a) FULLY DONE

1. **Task item closed in TODO_LIST.md** — "Hermes post-deploy smoke: Discord gateway connectivity line" (hermes 08-21 §f.7) marked `[x]` with full rationale, root cause, and deploy caveat. 2026-09-18.
2. **Root cause found by source-verification, not guessing:** hermes's stderr logging defaults to **WARNING+** (`gateway/run.py:34038`, verbosity=0 default), so the INFO-level `[Discord] Connected as <bot>` `on_ready` line (`plugins/platforms/discord/adapter.py:1450`) **never reached journald at all**. The 08-21 report's "journal-quiet (buffering)" theory was wrong — nothing was buffered; the line was never emitted to stderr in the first place. Live journal probe confirmed: `Connected as` = zero entries this boot while WARNING lines flow fine.
3. **Module fix:** `modules/nixos/services/hermes.nix` ExecStart now runs `hermes gateway run --replace -v` (verbosity=1 = INFO; flag verified against `hermes_cli/subcommands/gateway.py` argparse — `-v` is a `count` flag, `-v`=INFO, `-vv`=DEBUG). Comment in-tree explains why.
4. **Smoke assertion added:** `scripts/post-deploy-check.sh` hermes block gains a boot-scoped `journalctl -u hermes -b --grep "\[Discord\] Connected as"` check with pass/fail messages naming the recovery command. Design choices:
   - **Boot-scoped, not "within N minutes"** — a time-window bound false-fails long-lived boots (the item's literal "within N minutes" wording was deliberately generalized; noted in TODO_LIST).
   - journalctl-native `--grep` (no pipe) — avoids the known SIGPIPE/pipefail false-fail class; same pattern as the adjacent `hermes-workspace:` check.
5. **Verification:** `bash -n` clean; `nix flake check --no-build` all-green (ran twice — standalone and via the pre-commit hook); journal grep syntax proven live against the real journal (no permission error, deterministic no-match until the `-v` deploy).
6. **Committed with the exact Task-Queue-ID footer** (`7dba37d0`); all pre-commit hooks green (gitleaks, deadnix, statix, treefmt, flake check). Never pushed.

## b) PARTIALLY DONE

1. **The fix is deployed-proof but NOT deployed.** The ExecStart change (`-v`) requires `nix run .#deploy`; until then the new smoke check **FAILS red on hermes-enabled hosts** — a deliberate true-positive (the unit genuinely emits no connectivity signal yet), but it means post-deploy-check is red from the moment the script lands on the deployed tree until the next deploy. deploy.sh post-switch smoke runs will report "Hermes — no Discord 'Connected as' line" until the unit restart carries `-v`. Since both changes ride the SAME deploy, the red window is effectively zero in practice — but only if nobody cherry-picks the script alone.
2. **"Within N minutes" semantics:** I generalized to boot-scoped instead of implementing an actual N-minute window (e.g. `journalctl --since "-10min"`). Defensible (window bounds false-fail long boots) but it is a deviation from the item's literal wording — an owner could still want the stricter freshness guarantee.
3. **Positive live proof of the INFO line is still pending** — I verified the flag surface and log-level code paths from source, but the actual `Connected as` line appearing in journald can only be confirmed after the deploy restarts the gateway. The task queue's next hermes deploy should journal-grep for it.

## c) NOT STARTED

1. **TODO_LIST follow-up items I noticed in passing but did NOT touch** (per contract: follow-up work belongs in the backlog, and these were already tracked or out of scope):
   - The WARNING in this boot's journal `[Discord] Slash command sync timed out — Discord rate-limit bucket may be saturated` (22:00:57) — pre-existing upstream noise pattern, already described in AGENTS Hermes section as historically benign 429 noise; worth a look only if it becomes chronic at INFO visibility.
   - Nothing else. Scope discipline held: no drive-by refactors, no edits to the 08-21 archived reports.

## d) TOTALLY FUCKED UP

Nothing rises to "fucked up." Two honest mistakes worth recording:

1. **Lost my own smoke-script commit to the auto-commit daemon race** — the `post-deploy-check.sh` edit was swept into daemon commit `9d9cfc5a` ("chore: auto-commit 1 changed file(s) (heuristic)") before my pathspec commit ran. This is the exact daemon-race discipline AGENTS.md warns about ("re-check `git status --short` immediately before `git add`"). Harmless here (content landed, history tells the story via the queued commit message), but the split commit means the daemon message carries half the change with no explanation.
2. **~6 tool calls spent hunting the Discord adapter's location** in the hermes source tree (`gateway/platforms/discord.py` guesses, glob chains) before finding it at `plugins/platforms/discord/adapter.py` via a repo-wide filename search. Should have gone filename-first immediately — hermes is a plugin-architecture repo and the platform registry comment said as much.

## e) WHAT WE SHOULD IMPROVE

1. **Daemon-race procedure for multi-file changes:** when a task touches several files, check `git status --short` BEFORE every commit (I did — but only after the daemon had already committed file #1; the check must precede the FIRST commit, not just each one).
2. **Smoke checks that depend on a module change should note the coupling in-tree:** the smoke now asserts a line the current DEPLOYED unit cannot produce. A one-line pointer in the smoke's fail message ("requires hermes ≥ the -v deploy") would save the next operator a diagnosis round. (The fail message names the journal command but not the version dependency — TODO_LIST carries it, the script does not.)
3. **Boot-scoped vs window-scoped assertions deserve a stated convention.** I chose boot-scoped by reasoning; the repo has at least three precedent styles (boot `--grep`, `--since` windows, freshness mtime checks). One paragraph in `docs/CONTRIBUTING.md` would stop each session re-deriving the tradeoff.
4. **hermes log verbosity is now INFO for the WHOLE gateway** — that raises journal volume (every adapter INFO line, not just Discord connect). If journal growth becomes measurable, the targeted alternative is a `logger.setLevel` for the discord adapter only (upstream change) rather than global `-v`. Revisit only on evidence.

## f) Up to 50 things we should get done next

Scoped honestly: most of these came from THIS task's blast radius and passing observations; the backlog items already in TODO_LIST are not re-listed. Ordered by impact.

1. **DEPLOY** — both changes (hermes `-v` + smoke assertion) go live together; the smoke is red on hermes hosts until then.
2. Post-deploy: `journalctl -u hermes -b --grep "Connected as"` must show the line once — the positive live proof for this whole task.
3. Post-deploy: measure hermes journal volume for a day at INFO verbosity (was WARNING-only); revert to `-v`-less + adapter-scoped level if it balloons.
4. Add the hermes-version dependency note to the smoke's fail message (script-side, one line).
5. Check whether the INFO level surfaces a NEW `[Discord] Connected as` line on EVERY reconnect (flapping gateway would re-log repeatedly) — if noisy, the assertion may need a count cap or the connect line is idempotent enough.
6. Consider asserting the NEGATIVE side too: a WARNING `Failed to connect to Discord` (adapter.py:1558) in this boot's journal should also fail the smoke — presence-of-success AND absence-of-failure is the stronger gate (repo doctrine: liveness + health).
7. The `Slash command sync timed out` WARNING (this boot 22:00) — confirm it's the known benign 429 pattern, not a new saturation regime; if chronic, file upstream for exponential backoff.
8. Convention doc: boot-scoped vs window-scoped journal assertions in `docs/CONTRIBUTING.md`.
9. hermes upstream: consider logging the Discord connect line at WARNING (or a dedicated metric) so default-verbosity installs keep a positive connectivity signal — the root cause here is upstream's silent-at-default design; `-v` is a workaround.
10. hermes upstream: `gateway/run.py` default verbosity — an env var (e.g. `HERMES_LOG_STDERR_LEVEL`) instead of a CLI flag would let supervisors like systemd units raise verbosity without ExecStart edits.
11. VM test extension: `tests/test-hermes.nix` could assert the unit's ExecStart carries `-v` (cheap render-level guard against future ExecStart rewrites silently dropping it).
12. Same VM test could mock a Discord-ready journal line and run the new smoke's grep predicate — the predicate is currently only hand-probed.
13. Renamed-input hygiene: nothing found this run, but the hermes flake input was not touched — a scheduled input-bump TODO already exists; do NOT conflate this task's deploy with a bump.
14. When the hermes bump eventually lands, re-verify `-v` survives upstream CLI changes (flag is `count` type; stable, but check `--verbose` long-form parity).
15. Gatus note: hermes deliberately has NO HTTP endpoint — the new smoke is deploy-time only. A periodic (not deploy-time) journal-freshness check for the `Connected as` line could close the gap between deploys; owner decision (a flapping gateway would need a grace window).

16–50 are backlog-material observations, listed without priority ranking:
16. The daemon's heuristic commit messages ("auto-commit N changed file(s)") carry semantic changes (my smoke script) with zero discoverability — a daemon rule that never auto-commits `scripts/post-deploy-check.sh` would keep deploy-gate history attributable.
17. `docs/services/hermes.md` monitoring section should gain one line about the new journal assertion (runbook parity — the doc's monitoring table doesn't know about it yet).
18. Consider a shared `journal_grep_boot()` helper in post-deploy-check.sh — the hermes block now has three identical `journalctl -u hermes -b --no-pager --grep …` calls; a function would stop the nullglob/pipefail trap re-derivation per check.
19. The hermes block's fail messages are excellent (they name recovery commands) — worth promoting as the pattern for other blocks (some older blocks don't).
20. If the `-v` deploy lands during a hermes agent activity window, deploy.sh's existing 10-min activity WARN will fire — expected, no action.
21. Potential upstream-adjacent: the `on_ready` handler runs `_resolve_allowed_usernames()` before setting `_ready_event` — if that await hangs, `Connected as` is logged but "ready" is not; the smoke's line therefore proves connection, not full readiness. Acceptable; noted for honesty.
22–50. **Explicitly not enumerated as filler** — the remaining backlog lives in TODO_LIST.md (P0 stability items, browser-history registration gates, llama-rag post-deploy verification, Resend domain verification, etc.); duplicating them here would violate the no-scope-creep contract and docs-health HARVEST dedup rules. The 21 above are this task's honest yield.

## g) Up to 3 questions I can NOT figure out myself

1. **Owner: is boot-scoped acceptable, or do you want the literal "within N minutes" freshness window?** Boot-scoped cannot distinguish "gateway connected 5 minutes after this deploy" from "connected once 9 days ago and Discord has been down since." My judgment says boot-scoped (window bounds false-fail long boots), but a freshness-capped variant (`--since` = unit's last restart time) would give both properties — it needs a deploy.sh/QA judgment call on how strict post-deploy smoke should be.
2. **Owner: is global `-v` (INFO for the whole gateway) acceptable as the permanent posture, or should this be an upstream request for an adapter-scoped or env-var-scoped log level?** I could not measure the journal-volume delta without deploying; if you'd rather not carry INFO-level journal noise permanently, the upstream env-var route (question embedded in e.4) is the follow-up.
3. **Owner: should the smoke also FAIL on a `Failed to connect to Discord` WARNING in this boot's journal (negative-side assertion, item f.6)?** A boot can legitimately contain both (connected, later dropped, reconnected) — I judged presence-of-success alone sufficient and avoided a false-fail on transient drops, but Discord being down _right now_ with an old success line is a real blind spot you may care about.
