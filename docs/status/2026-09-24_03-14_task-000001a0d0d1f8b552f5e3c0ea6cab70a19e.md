# Status Report — Task Queue Item `000001a0d0d1f8b552f5e3c0ea6cab70a19e`

**Date:** 2026-09-24 03:14 CEST
**Work item:** "Fix pre-deploy §13 silent-skip: distinguish offsite-borg disabled from a transient eval error — fail loud on the latter" (TODO_LIST → docs/todo/storage.md, Source §d3)
**Repo:** `/home/lars/projects/SystemNix` @ `eb2c6b3e`, tree clean at handoff and at report time.

---

## Executive summary

This dispatch turned out to be a **re-dispatch of already-completed work**. Commit `eb2c6b3e` ("Fix pre-deploy gate blind skip of offsite-borg eval errors") carries this exact `Task-Queue-ID` footer and closes the item: the §13 classifier (`ob_classify_eval_error` in `scripts/lib/offsite-borg-smoke.sh:22`), the fail-loud wiring (`scripts/pre-deploy-check.sh:756-774`), the 18-branch fixture, and the adjacent fix staging the lib into both packaged gates.

My run was therefore a **verification pass**, not a redo: fixture test (18 branches, RC 0), `offsite-borg-smoke-selftest` flake check, source inspection of §13 + both wrapper source lines, and — closing the one gap the first pass left — a **full packaged `nix run .#pre-deploy-check` end-to-end run: 64 passed / 36 warnings / 0 failed**, with §13 classifying the real disabled eval to SKIP via the actual classifier. Both TODO surfaces were confirmed closed (TODO_LIST.md:35 `[x]`, docs/todo/storage.md:49 `[x]`).

Verified-in-passing new findings (grounded in source read during this run, not researched elsewhere):

- **§16 still has the blind-skip class §13 just fixed** (latent): `ob_post_deploy` SKIPs on a missing unit file while claiming `services.offsite-borg.enable = false` — it never reads the enable flag. On the go-live deploy itself, a botched activation → absent unit → phantom-green SKIP of the exact block that exists to gate go-live.
- **The EIO-corrupt /data inode (root 256 ino 1331118) is a likely first-real-run blocker**: `/data` is a Borg `paths` entry and the known-bad inode's path is not in any exclude; borg read errors exit non-zero → `ExecStartPost` marker never lands → backup-coordination pages. Repair/exclude decision needed BEFORE go-live.
- **`borgbackup-job-hetzner` is not in Zone-6 `ioChurnUnits`** — the nightly read burst is invisible to the IO-storm guard's stop/re-arm list.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| a1 | §13 silent-skip fix: eval stderr captured; `ob_classify_eval_error` SKIPs ONLY the `…does not provide attribute…borgbackup-job-hetzner.serviceConfig` disabled shape; every other eval failure (daemon restart, store hiccup, real config error, attribute-missing for a DIFFERENT unit) fails loud with raw stderr | Commit `eb2c6b3e`; `scripts/lib/offsite-borg-smoke.sh:22-31`; `scripts/pre-deploy-check.sh:756-774` |
| a2 | Adjacent fix: §13/§16 smoke lib staged into BOTH packaged gates (the lib-extraction commit had shipped only metrics-gate.sh/pressure-report.sh — every deploy died at the §13 source line) | Commit `eb2c6b3e`; source lines at `pre-deploy-check.sh:51`, `post-deploy-check.sh:165` |
| a3 | Fixture coverage: 18 pass/fail/warn/skip/classifier branches through the sourced lib, incl. path-specificity discriminator | `bash scripts/test-offsite-borg-smoke.sh` → 18 ok, RC 0 (this session) |
| a4 | `offsite-borg-smoke-selftest` flake check | `nix build .#checks.x86_64-linux.offsite-borg-smoke-selftest` → builds clean (this session) |
| a5 | Full packaged pre-deploy gate end-to-end green on MY OWN run (not inherited from the prior run's claim) | `nix run .#pre-deploy-check` → "13. Offsite Borg go-live wiring — disabled … skipped" via the real classifier; Summary: 64 passed, 36 warnings, 0 failed (this session) |
| a6 | Queue loop closed on BOTH surfaces, no drift | TODO_LIST.md:35 `[x]` + DONE annotation; docs/todo/storage.md:49 `[x]` + DONE row |
| a7 | Commit cross-references the queue | `git log -1 --format=%B eb2c6b3e` ends with `Task-Queue-ID: 000001a0d0d1f8b552f5e3c0ea6cab70a19e` |
| a8 | Sibling offsite-borg queue items landed by earlier runs same domain (context, all `[x]` in storage.md): drill hardening (mountpoint gate, record-first, borg-from-lock), restore-runbook accuracy pass incl. Discord leg wiring, §13/§16 fixture extraction, `backup_ever_succeeded` integration, env-path-pin eval assertion | storage.md:41-46, 109 |

## b) PARTIALLY DONE

| # | Item | Works now | Remains open | Blocker | Effort |
|---|------|-----------|--------------|---------|--------|
| b1 | Offsite-Borg leg overall (the feature this gate protects) | Implemented + deployed DORMANT system-796: module, sops scaffolds, tripwire, marker, backup-coordination row, Gatus check, runbooks, drills — all committed and fixture/eval-verified | Go-live (owner inputs → paste → enable → first seed → real-repo drill) | User-held StorageBox hostname/username + password-recovery-policy decision (storage.md:63) | S once inputs arrive; seed itself is WAN-bound |
| b2 | My re-verification pass | At handoff: fixture + selftest + source inspection done | Full packaged gate leg was pending — **closed during this report** (a5) | none | — |
| b3 | Disabled-shape classification durability | Classifier + hand-written fixture branches | No committed artifact proving REAL nix stderr matches the pattern (the 09-24 live probe was ephemeral; a nix version bump that rewords the eval error would flip every disabled config to fail-loud — safe direction, but noisy) | none — S to add a real-stderr fixture branch | S |
| b4 | Go-live gate coverage symmetry | §13 (to-be-deployed side) classifies eval failures | §16 (deployed side) has the enable-blind SKIP (see d1) | none | S |

## c) NOT STARTED

| # | Item | Why not started | Still wanted? |
|---|------|-----------------|---------------|
| c1 | Go-live operational chain: sops paste (`borg_repo`, `borg_known_hosts`), host-key pin via `ssh-keyscan -p 23`, `enable = true` flip, first WAN seed, real-repo restore drill (= go-live checklist step 9) | Owner-gated inputs (storage.md:63) | Yes — this is the entire point of the leg |
| c2 | §16 enable-awareness hardening (d1) | Discovered during THIS report's pass | Yes — High |
| c3 | EIO /data inode vs first Borg run (d2) | Discovered during THIS report's pass | Yes — Critical pre-go-live |
| c4 | Zone-6 `ioChurnUnits` decision for the borg job (d3) | Discovered during THIS report's pass | Yes — needs a decision, then S edit |
| c5 | Queued adjacents, untouched: §13/§16 IO-tier asserts (storage.md:50), positive-path render probe (:51), env-path-pin negative case (:47), drill-fixture negative (:110), help-slicer sweep (:56) | Not picked by the queue yet | Yes — all `[ready]` |
| c6 | Post-first-real-run watch items (archive-vs-exclude audit, irreplaceable-set size vs ~800 G BX21 trigger, AGENTS.md DORMANT→live flip — storage.md:54) | Correctly parked until go-live | Yes |

## d) TOTALLY FUCKED UP

Nothing shipped by this task is broken. Three honest entries, two of them latent and discovered during this run:

- **d1 — §16 phantom-SKIP on a failed go-live deploy (latent, pre-existing).** `ob_post_deploy` (`scripts/lib/offsite-borg-smoke.sh`) returns SKIP with the message "not deployed (services.offsite-borg.enable = false)" whenever the unit file is absent — **without ever consulting the enable flag**. On the go-live deploy, if activation fails to render/drop the unit (exit-4 class, stale tree, unit-path drift), §16 green-SKIPs the exact block that exists to gate go-live — the same failure class this task just fixed for §13, on the other side of the deploy. Severity: latent (go-live hasn't happened; today enable=false so SKIP is truthful). Mitigation: none today. Fix is small: pass the enable state into `ob_post_deploy`; enabled+absent = FAIL, disabled+absent = SKIP.
- **d2 — EIO /data inode will likely fail the first REAL Borg run (latent, pre-existing).** `/data` is a Borg source path; the known corruption (root 256 ino 1331118, 2026-08-18) kills nightly `btrbk-data` and its path is not covered by any offsite exclude. borg create exits non-zero on read errors → `ExecStartPost` never touches `.last_success` → backup-coordination + aggregate check page, and the seed may partially land. The path must be identified and either repaired (owner decision) or excluded BEFORE the enable flip. Severity: blocks go-live, not current operations (job is dormant).
- **d3 — Queue re-dispatched its own completed item (process, live).** `eb2c6b3e` closed this item with the exact footer, yet the queue issued it again. A verify-only re-run produces NO commit, so if the queue's done-derivation keys on per-run commits it may loop indefinitely. Severity: wasted agent runs, no repo damage. My run leaves this report — footer-stamped — as the completion marker.

## e) WHAT WE SHOULD IMPROVE

1. **Verify-first re-dispatch protocol** — workers should grep `git log` for the Task-Queue-ID footer BEFORE starting work (this run did; it should be the documented default, not luck). Impact: one full agent run saved per occurrence.
2. **Completion signal for no-op runs** — a verify-only re-dispatch leaves no commit for the queue to derive. Allow footer-stamped report/marker commits as completion evidence. This report is the existence proof.
3. **Real-stderr fixture branch** for `ob_classify_eval_error` — guard the nix-version drift of the "does not provide attribute" message shape with REAL nix output in the fixture sandbox, not hand-written strings.
4. **Gate WARN baseline** — the pre-deploy gate carries 36 standing warnings (26× "ExecStart binary not built yet", 6× "may not be a buildGoModule", …). Publish a known-good expected set so NEW warnings stand out; classify substitutability-aware ones down to INFO.
5. **§13/§16 parity automation** — the lib-extraction miss (wrappers shipped without the lib) was found on a live run. A flake check asserting both wrappers source `scripts/lib/offsite-borg-smoke.sh` (plus hash parity) makes the class structurally impossible.
6. **Assert what the smoke claims** — §13/§16 verify tripwire/marker/env-file wiring but not the `-p 23` + `StrictHostKeyChecking=yes` shape of `BORG_RSH` (backup.nix:221); one grep-assert each closes it.
7. **Report-then-verify sequencing** — I ran fixture+selftest first and the full gate only during report writing. For gate-type tasks the full gate IS the verification; run it first.

## f) Up to 50 things we should get done next

44 grounded items (the cap is a brainstorm ceiling, not a quota — padding with ungrounded entries would poison HARVEST). `[queued]` = already tracked in docs/todo/storage.md or TODO_LIST (harvest should dedupe, not duplicate); `[new]` = candidate from this run's pass. Impact / Effort per section-quality guide (S <30min, M 30min-2h, L >2h).

**Go-live preparation (Critical path, ordered):**

| # | Task | Impact | Effort | Cat |
|---|------|--------|--------|-----|
| f1 | `[queued·user]` Offsite Borg go-live inputs: StorageBox hostname + username (storage.md:63) | Critical | S | Decision |
| f2 | `[queued·user]` Borg password recovery-copy policy — wrong answer here is unrecoverable loss on first dead disk (storage.md:63) | Critical | S | Decision |
| f3 | `[queued·user]` Irreplaceable exceptions inside excluded trees (Steam saves, Proton prefixes, curated GGUFs) vs 1 TB budget (storage.md:63) | High | M | Decision |
| f4 | `[new]` Identify the EIO /data inode's PATH (d2) and decide repair (owner) vs temporary exclude — REQUIRED before first real borg run | Critical | S invest + M repair | Bug |
| f5 | `[new]` sops-paste `borg_repo` + `borg_known_hosts` into `platforms/nixos/secrets/borg.yaml` once f1 lands | Critical | S | Feature |
| f6 | `[new]` Pin StorageBox host key: `ssh-keyscan -p 23 <host>` and verify the fingerprint against the Hetzner console BEFORE pasting (TOFU guard) | Critical | S | Feature |
| f7 | `[new]` Flip `services.offsite-borg.enable = true` + deploy; watch §13/§16 fire their first real PASS run (first non-SKIP execution of both blocks) | Critical | S | Feature |
| f8 | `[new]` Supervise first WAN seed: journal follow, confirm `.last_success` lands, `backup_ever_succeeded{backup="offsite-borg"}` flips to 1 | High | S | Feature |
| f9 | `[queued]` Real-repo restore drill = go-live checklist step 9 (`scripts/borg-restore-drill.sh` real mode incl. `--verify-data` phase) | Critical | M | Quality |
| f10 | `[new]` OnFailure delivery proof post-go-live: synthetic unit failure → Discord + desktop legs (staged 2026-09-24, delivery unproven live) | High | S | Quality |

**Go-live follow-ups:**

| # | Task | Impact | Effort | Cat |
|---|------|--------|--------|-----|
| f11 | `[queued·watch]` Archive-vs-exclude audit after first real run: `borg list ::archive` vs the 27 excludes (storage.md:54) | High | M | Quality |
| f12 | `[queued·watch]` Record `borg create --stats` irreplaceable-set size vs ~800 G BX21 trigger into the runbook (storage.md:54) | Medium | S | Documentation |
| f13 | `[queued·watch]` Flip AGENTS.md Offsite Borg header DORMANT → live (storage.md:54) | Low | S | Documentation |
| f14 | `[new]` Day-2 freshness verification: 06:30 job vs 25h backup-coordination maxAge stays green; `backup_ever_succeeded` MTIME gate distinguishes never-worked vs stale | High | S | Quality |
| f15 | `[new]` StorageBox quota monitoring — no remote-side usage check exists; add one (SFTP `df`/rclone/borg info) before the repo can silently hit the BX11 ceiling | Medium | M | Feature |
| f16 | `[new]` /data offsite-coverage decision: with `backups/{root,data}` excluded, /data's third copy is same-chassis-only until f4/f31; decide interim posture | High | M | Decision |
| f17 | `[new]` Post-go-live prune verification: archive counts match 7d/4w/6m after retention windows mature; finalize retention (backup.nix flags it as owner decision) | Medium | S | Quality |
| f18 | `[new]` `borg compact` cadence decision (dedup frees need compaction; quota-bound target) after the first month of real data | Medium | S | Decision |
| f19 | `[new]` Restore runbook addition: loss of `/mnt/hot/borg/cache` (Samsung-tier) = full remote re-index over WAN — document rebuild cost + whether to cache-backup the security dir | Low | S | Documentation |
| f20 | `[new]` Runbook the DORMANT→go-live rollback: flip enable=false + redeploy mid-incident | Low | S | Documentation |
| f21 | `[new]` Verify the drill's `--verify-data` capability probe against the REAL borg 1.4.5 binary (flag is borg2-era; code guards it, unproven on the real binary) | Low | S | Quality |
| f22 | `[new]` Stale-lock handling on the JOB unit (not just the drill): interrupted seed → `lock.exclusive`; runbook cross-ref `borg break-lock` + `pgrep borg` gate for the job path | Low | S | Documentation |
| f23 | `[new]` Consider `borgbackup-job-hetzner` in system-health `monitoredServices` (restart-churn visibility; OnFailure already covers hard fail) | Low | S | Feature |
| f24 | `[new]` Verify sops `borg.yaml` inventory at go-live prep: all four keys (`borg_repo`, `borg_known_hosts`, `borg_ssh_key`, `borg_password`) present before the flip (sops-key-audit catches declared-but-missing at eval; confirm declarations match the checklist) | Medium | S | Quality |

**§13/§16 + gate hardening:**

| # | Task | Impact | Effort | Cat |
|---|------|--------|--------|-----|
| f25 | `[new]` §16 enable-awareness (d1): pass enable state into `ob_post_deploy`; enabled+absent = FAIL, disabled+absent = SKIP; extend fixture | High | S | Bug |
| f26 | `[new]` Zone-6 guard interplay (d3): `borgbackup-job-hetzner` is not in `ioChurnUnits` — nightly /home+/data+pool read burst is unstopped during an IO storm; decide include-with-re-arm vs document-why-not | High | S | Decision |
| f27 | `[new]` §13/§16: assert `BORG_RSH` shape (`-p 23`, `StrictHostKeyChecking=yes`, `UserKnownHostsFile`) on eval'd + deployed unit — today only "EnvironmentFile contains borg-env" is checked | Medium | S | Quality |
| f28 | `[queued]` Assert IO tier (IOSchedulingClass/Priority) in §13 eval + §16 deployed-unit grep (storage.md:50) | Medium | S | Quality |
| f29 | `[queued]` Persist the positive-path enable=true render probe as a committed artifact (extendModules eval asserts; storage.md:51) | Medium | S | Quality |
| f30 | `[queued]` Fold the borg env-path-pin negative case into the drill fixture (extendModules drift fixture; storage.md:47) | Medium | S | Quality |
| f31 | `[queued]` Fixture-negative for `borg-restore-drill-fixture`: mutate a drill copy inside the fixture and assert the derivation FAILS (storage.md:110) | Medium | M | Quality |
| f32 | `[new]` Real-stderr fixture branch for `ob_classify_eval_error` (e3): run actual nix eval against a mkIf-hidden attr in the fixture; pin the true message shape against nix-version drift | Medium | S | Quality |
| f33 | `[new]` Automated wrapper↔lib parity check (e5): flake check asserting both gates source `scripts/lib/offsite-borg-smoke.sh` + hash parity — makes the extraction-miss class impossible | Medium | S | Quality |
| f34 | `[new]` Cover §16's call-site argument wiring in the fixture (real paths `/etc/systemd/system/borgbackup-job-hetzner.service` + backups.prom passed at `post-deploy-check.sh:1698`; fixture calls the lib directly) | Low | S | Quality |
| f35 | `[new]` Document §13/§16 gate behavior in `docs/services/offsite-borg.md` (what SKIP/FAIL means on the go-live deploy; fail-loud on a daemon hiccup is BY DESIGN) | Low | S | Documentation |

**Gate hygiene (noticed during this run's packaged-gate execution):**

| # | Task | Impact | Effort | Cat |
|---|------|--------|--------|-----|
| f36 | `[new]` Classify §12 "ExecStart binary not built yet" WARNs by substitutability (26 of 36 warnings; cached/substitutable → INFO) | Low | M | Quality |
| f37 | `[new]` Classify input-hygiene "unable to determine status (may not be a buildGoModule)" WARNs for known non-Go inputs (bun: crush-daily, file-and-image-renamer, qmd) | Low | S | Quality |
| f38 | `[new]` monitor365 ghost probe: removed from the packages surface 2026-09-15 yet the hygiene check still probes `monitor365.goModules` — verify the probe list derives from the live lock, not a stale list | Low | S | Bug |
| f39 | `[new]` Known-good WARN baseline for the pre-deploy gate (committed expected set; diff each run) (e4) | Low | S | Quality |

**Queue/process (this run's meta-findings):**

| # | Task | Impact | Effort | Cat |
|---|------|--------|--------|-----|
| f40 | `[new]` Document the verify-first re-dispatch protocol (grep commits for the queue-ID footer before redoing; verify fixture+selftest+FULL gate) in project docs | Medium | S | Documentation |
| f41 | `[decision·queue-side]` Allow footer-stamped report/marker commits as completion evidence for no-op re-runs (this report is one) | Medium | S | Decision |
| f42 | `[new·queue-side]` Investigate why the queue re-dispatched `eb2c6b3e` — done-derivation missed its own footer commit; loop risk if it keys on per-run commits | Medium | M | Bug |
| f43 | `[queued]` Repo-wide sweep for hardcoded help-slicer ranges + canonize the derive-until-first-non-comment awk idiom (storage.md:56) | Low | M | Cleanup |
| f44 | `[queued]` /data EIO inode repair proper (P0; blocks `btrbk-data` leg AND f4/f16) | Critical | L | Bug |

## g) Questions I can NOT figure out myself

1. **Why did the queue re-dispatch this item?** Commit `eb2c6b3e` closed it with the exact `Task-Queue-ID` footer, and both TODO surfaces are `[x]`. I cannot see the queue's done-derivation logic (and must not edit `.tq-verify`): does it key on the footer commit, on TODO_LIST state, or on per-run commits? If it's the latter, verify-only re-runs (no commit) can loop forever — f41/f42. What makes THIS dispatch count as done?
2. **Go-live inputs (the standing owner-gated blocker):** do you have the StorageBox hostname + username ready, and what is the borg-password recovery-copy policy (password manager / printed / split)? Also: for the EIO /data inode (f4) — repair-by-delete (it's been failing nightly btrbk since 2026-08-18) or temporary exclude from the Borg path set?
3. **Guard membership decision:** should `borgbackup-job-hetzner` join Zone-6 `ioChurnUnits` (guard-stoppable with re-arm, f26), or is the 06:30 slot + `ioTier.background` judged sufficient even though a multi-hour read burst can outlast an IO storm window?

---

*Format note: this report is Markdown per the tasking instruction; the status-report skill's default is a styled HTML dashboard — the explicit `.md` path in the task won, override not propagated back into the skill. Section (f) is the HARVEST ground: `[queued]` rows already live in docs/todo/storage.md; `[new]` rows need HARVEST routing (storage/pipeline sections) before they decay in this timestamped file.*
