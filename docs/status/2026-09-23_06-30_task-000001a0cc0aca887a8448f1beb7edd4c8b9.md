# Status Report — Task 000001a0cc0aca887a8448f1beb7edd4c8b9 (run 2, verification pass)

**Item:** "disko config for the already-deferred reinstall" (TODO_LIST storage section → docs/todo/storage.md)
**Date:** 2026-09-23 06:30 CEST · **Repo:** SystemNix @ master, clean tree · **Not pushed**
**Companion report:** [docs/status/2026-09-23_05-35_task-000001a0cc0aca887a8448f1beb7edd4c8b9.md](./2026-09-23_05-35_task-000001a0cc0aca887a8448f1beb7edd4c8b9.md) — the prior run's report (same Task-Queue-ID footer, commit `27e8e044`, 05:22:55). This run was a **re-dispatch of an already-closed item**; it is a verification pass plus the on-sight fixes the prior run deferred. This report covers run 2 only and does not repeat the prior report's inventory.

> Format note (2nd occurrence): the status-report skill prescribes a styled HTML dashboard; the tasking explicitly requested `.md` at this exact path. Override honored again — if task-scoped reports are now `.md`-at-path by convention, that should be baked into the skill/tasking template rather than overridden silently every run.

---

## a) FULLY DONE

1. **Re-dispatch detected before any work was duplicated.** `git log --grep` for the Task-Queue-ID found commit `27e8e044` (05:22:55, "Close disko reinstall item: add rescue-path decision one-pager") carrying this exact footer — the item was closed ~40 min before this dispatch. Run scope correctly became verification, not re-implementation.
2. **All five claimed artifacts verified present and coherent:** `disko/samsung-tlc.nix` (by-id-pinned GPT spec: p1 EF00 4G vfat `SAMSUNG-EFI` unmounted, p2 btrfs `-L tlc`, toplevel at `/mnt/hot` + `/nix` + `/users/lars/cache/nix` subvols); `flake.diskoConfigurations.samsung-tlc` (flake.nix:915); eval guard `checks.disko-samsung-tlc` (flake.nix:1136, geometry asserts + discovery-trap assert `!(evoConfigOptions ? disko)`); VM rehearsal `tests/test-disko-layout.nix` wired at tests/default.nix:46; runbook `docs/services/disko.md` (T24 decision one-pager).
3. **Eval guard BUILT green this run:** `nix build .#checks.x86_64-linux.disko-samsung-tlc` → `aj3r0vr7509cv7lbrjbqrm62my5n3v0m`. The discovery-trap positive side is thereby proven live on today's tree (the guard forces `nixosConfigurations.evo-x2.options` and asserts no `disko` option).
4. **VM-rehearsal check evals green:** `nix eval .#checks.x86_64-linux.disko-layout.drvPath` → `a4w45pj…vm-test-run-disko-layout.drv`. VM boot itself NOT re-run (see b.1).
5. **NEW this run — live geometry parity probes (read-only):** `lsblk` on the pinned by-id confirms p1 `SAMSUNG-EFI` vfat 4G + p2 `tlc` btrfs 927.5G (disk currently enumerates as nvme1n1 — the by-id pin doctrine proven yet again); `/etc/fstab` + `findmnt` confirm `/mnt/hot` on subvolid=5 toplevel, `/nix` on subvol=nix, `/boot-mirror` mounted. Spec↔live parity holds TODAY. `compress=zstd` is the correct spec text; the kernel normalizes it to `zstd:3` + `ssd` at mount — normalization, not drift.
6. **Repo-wide eval green on the settled tree:** `nix flake check --no-build` → "all checks passed" (aarch64-darwin omission expected). First check attempt had failed — see d.2/d.3.
7. **Queue loop confirmed closed in both files:** TODO_LIST.md:27 and docs/todo/storage.md:37 marked `[x]` with full provenance (prior run; re-verified this run).
8. **Closed the two stale doc mentions the prior run saw but deferred (2-line on-sight class, owner-permitted 2026-09-06):** AGENTS.md:703 ("disko … not implemented" → disko AUTOINSTALLER remains unimplemented, geometry part points at the landed surfaces) and ROADMAP.md:31 ("draft the config when the reinstall is scheduled" → spec landed, what remains deferred named). Both were contradicted by the landed `disko/` surfaces until this fix.

## b) PARTIALLY DONE

1. **VM-level rehearsal not re-executed this run.** `/proc/pressure/io` showed some avg60 = 64-65% (full avg10 = 33%) — freeze-doctrine conditions; booting a VM into an active IO storm is exactly the freeze #3-#6 amplification class. Last executed evidence is 2026-09-14 (green ×2 then). This run re-proved: eval guard build, check drv eval, live geometry parity — the VM layer is the one un-re-executed leg. Effort to close: S (one quiet-window run), blocked only on IO calm.
2. **Discovery-trap NEGATIVE case not hand-probed this run.** The guard is a plain `assert` inside `builtins.deepSeq`; the positive side is proven by the green build (a.3), and the case is not newly-added (landed 2026-09-14), so the eval-cache hand-probe convention arguably doesn't re-arm — but this run did not demonstrate the guard FAILS when the trap is violated. Effort: S (`extendModules` + import disko module → expect the assertion/TypeError named).
3. **`?ref=master` pin-policy compliance not applied to the disko input.** flake.nix:129 carries bare `github:nix-community/disko`; the 2026-09-16 pin policy wants `?ref=master` on every input unless upstream is broken. Deliberately not changed this run: a URL edit requires the matching flake.lock `original` edit with `locked` byte-identical (python json round-trip) plus re-verification — backlog material, not an on-sight fix.
4. **Section-f harvest not run.** Per docs-health HARVEST the f-lists of BOTH this report and the 05:35 report feed TODO_LIST/domain files; the tasking for this run asked for the report + commit only. The two reports' f-sections now need a combined harvest pass.

## c) NOT STARTED (task-surface only; by-design deferrals, not omissions)

1. **nixos-anywhere / autoinstaller ISO** — deferred by recorded decision (docs/services/disko.md): needs a scheduled reinstall + console presence + a design for private git+ssh inputs + per-host sops identity at bootstrap.
2. **QLC root + `/data` disko specs** — deliberately absent (live data, hand-managed); only becomes a question at reinstall planning.
3. **Installer-ISO step-1 rehearsal** (clone/`nix copy` the repo onto a stock ISO outside a crisis) — never done; the recipe's network/auth assumptions are untested.
4. **Replacement-drive procedure** (different serial ⇒ different by-id ⇒ spec + guard assertion change) — undecided; prior report's question g.2.
5. **Reinstall trigger** — undocumented (prior report's question g.1, still open).
6. **CI cadence decision for `checks.disko-layout`** (it is the only guard that executes the generated disko script; scheduled vs on-touch undecided).

## d) TOTALLY FUCKED UP

Nothing in the disko surface — the item was verifiably complete before this run started and remains so; no broken state was created or left (tree clean, checks green, no destructive ops, no secrets).

The honest fucked-up-adjacent findings, all outside the disko work itself:

1. **The queue re-dispatched a completed item.** The same Task-Queue-ID was already closed at 05:22 with its footer in history and both TODO files marked done; this dispatch arrived ~06:00 anyway. Wasted a run slot and produced a second report file for one item. Root cause is in queue done-detection (racing the closing commit or the TODO flip), not in the repo — needs a queue-side fix (f.11).
2. **Repo-wide eval was transiently broken 06:03-06:20 by a parallel session's mid-edit module** (`modules/nixos/services/architecture-catalog.nix`: `serviceOneshotDefaults` passed uncalled → `serviceConfig` TypeError at `nix flake check`). Self-healed by the owning session within ~15 min (commits `c3e98f6b`/`af688eca`/`a3f636a0`, the 06:10 paren fix + 131-line addition); all-green after. Not mine to fix — actively-owned file — but for ~15 min every flake command on the box failed.
3. **My first `nix flake check` (started 06:03) raced that exact mid-edit window** and burned a ~5-min eval cycle on a transient failure. I applied the quiescent-moment rule (check mtimes/recent daemon commits before repo-wide evals) only REACTIVELY, after the confusing failure — it should have been the pre-flight step.

## e) WHAT WE SHOULD IMPROVE

1. **Queue-run pre-flight:** step 1 of any queue dispatch should be `git log --all --grep=<Task-Queue-ID>` + a TODO grep for the item — re-dispatch detection in 10 seconds instead of artifact-by-artifact verification. (This run got there, but via the slow path.)
2. **Quiescent-moment check BEFORE repo-wide evals, not after:** on this box, `stat` the newest files under `modules/` + `git log --format='%h %ad %s' -3` — if the daemon committed within the last ~10 min, wait or scope evals narrowly (lazy `nix build .#checks.<x>` first, full flake check last).
3. **Two-line doc staleness: fix on sight at LANDING time.** The prior run deferred the AGENTS/ROADMAP fixes to the backlog; they then sat stale for an hour across two runs of the same item. The on-sight rule exists precisely so this class doesn't survive the commit that makes it stale. (Closed by this run.)
4. **New flake inputs should land with `?ref=master`/`?ref=main` from day one** — the disko input missed the 2026-09-16 policy and now needs lock surgery to comply (f.3).
5. **`.md`-report override is now 2-for-2** on task-scoped dispatches: either bake the task-path convention into the status-report skill, or the override note should stop being repeated in every report.
6. **Report-per-run on re-dispatched items doubles files** — acceptable for point-in-time doctrine, but the HARVEST pass must cross-link sibling reports for the same Task-Queue-ID or the backlog grows two divergent f-lists (this report's f-list is deliberately delta-only for that reason).

## f) Things to get done next (delta list — the prior report's 50 items remain the standing backlog; these are NEW or re-verified-still-open from run 2)

| #  | Task                                                                                                                                                                                                  | Impact | Effort | Category      |
| -- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------ | ------------- |
| 1  | Re-run `checks.disko-layout` (VM rehearsal) in a quiet window (IO PSI some avg10 <20%); paste fresh evidence + date into `docs/services/disko.md`                                                                       | High   | S      | Quality       |
| 2  | Hand-probe the discovery-trap negative case once: `extendModules` on evo-x2 + import disko module → assert the guard fails loudly (eval-cache convention)                                                                | Medium | S      | Quality       |
| 3  | Add `?ref=master` to the disko flake input per the 2026-09-16 pin policy (URL edit + matching lock `original` edit, `locked` byte-identical via python json round-trip; verify `nix flake metadata`)                      | Medium | S      | Quality       |
| 4  | Decide + document the replacement-drive procedure (different serial ⇒ by-id pin + guard assertion update; or move to a stable alias) — owner decision, carries prior-report Q2                                           | High   | M      | Decision      |
| 5  | Record the reinstall TRIGGER (what condition forces the rebuild) in docs/todo/storage.md — owner decision, carries prior-report Q1                                                                                       | High   | S      | Documentation |
| 6  | Installer-ISO step-1 rehearsal: clone/`nix copy` the repo onto a stock NixOS ISO once, so the rescue recipe's network/git+ssh/token assumptions are tested outside a crisis                                              | High   | M      | Quality       |
| 7  | Render `disko --dry-run` host-side once against the current lock and stamp "last verified <date>" into docs/services/disko.md                                                                                            | Medium | S      | Documentation |
| 8  | Add one rescue-recipe line for the boot-mirror interplay: after a Samsung rebuild, `boot-mirror-sync` repopulates p1 on next boot and pre-reboot-check §11 grades it                                                      | Low    | S      | Documentation |
| 9  | Document kernel-normalized mount options in the runbook (spec says `compress=zstd`; kernel serves `zstd:3,ssd` — expected, not drift)                                                                                    | Low    | S      | Documentation |
| 10 | Cross-link `platforms/nixos/system/boot-mirror.nix` comments ↔ `docs/services/disko.md` (shared Samsung ESP story)                                                                                                       | Low    | S      | Documentation |
| 11 | File the queue re-dispatch gap upstream (go-taskqueue): done-detection should consult Task-Queue-ID footers in git history before re-harvesting a closed item                                                            | Medium | S      | Upstream      |
| 12 | Decide CI cadence for `checks.disko-layout` (scheduled vs on-touch) — it is the only guard that executes the generated disko script                                                                                      | Low    | S      | Decision      |
| 13 | Annotate the Phase-2 plan doc T-task table: T16/T17/T24 DONE pointers (docs-health ANNOTATE mode) — carried from prior report f.2, still open                                                                            | Low    | S      | Documentation |
| 14 | QLC root disko spec: decide at reinstall planning whether the primary disk also gets a document-only spec (the one-pager names the gap explicitly)                                                                       | Low    | S      | Decision      |
| 15 | `/data` disko spec: same decision, same trigger                                                                                                                                                                          | Low    | S      | Decision      |
| 16 | Confirm the first restic-app-dumps nightly run (scheduled 05:45 TODAY) landed + `.last_success` + backup-coordination green — noticed in passing, still unverified                                                       | High   | S      | Bug           |
| 17 | Confirm the first ClickHouse DB backup run (03:00 timer, deployed 2026-09-23) landed in `/mnt/pool/backups/clickhouse` — noticed in passing                                                                             | High   | S      | Bug           |
| 18 | Watch the parallel session's `architecture-catalog` module land cleanly: next full `nix flake check` + confirm `architecture-catalog-sync` satisfies deploy-restart-audit (it converged to green at ~06:20 mid-run)       | Medium | S      | Quality       |
| 19 | Combined HARVEST pass over BOTH this report's and the 05:35 report's f-sections into TODO_LIST/domain files, cross-linking the two reports per Task-Queue-ID                                                             | Medium | M      | Documentation |
| 20 | Verify `~/.cache/nix` automount still resolves post-crash-hot-db eras: fstab shows `users/lars/cache/nix` automounted from `tlc` — spec parity confirmed this run; re-check after the next hot-db migration wave           | Low    | S      | Quality       |

## g) Questions I cannot figure out myself

1. **Why did the queue re-dispatch this item ~40 minutes after a run closed it with the same Task-Queue-ID?** I checked git history (footer present in `27e8e044` at 05:22), both TODO files (marked done), and the queue's own surfaces as far as the repo shows them — no local queue-state file explains the dispatch. Is done-detection racing the closing commit (or the TODO flip), and should the queue consult git-footers before harvesting? (Feeds f.11.)
2. **(Persistent — carried unanswered from the 05:35 report, re-asked because it gates real work): what condition actually triggers the deferred reinstall?** QLC end-of-life, `/data` repair-reformat, Samsung role expansion — the answer decides whether the nixos-anywhere/autoinstaller path is ever worth building or stays rescue-manual forever.
3. **Risk acceptance for the rescue path: is VM-level rehearsal (2026-09-14, green ×2) + this run's eval-guard build + live geometry parity sufficient to treat the rescue recipe as proven, or should a real-hardware rehearsal on a blank spare disk be scheduled before the recipe is ever needed in anger?** This is an owner risk call — I cannot derive the acceptable evidence bar from the repo.
