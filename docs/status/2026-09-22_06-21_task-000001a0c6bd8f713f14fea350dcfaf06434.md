# Status Report — task `000001a0c6bd8f713f14fea350dcfaf06434`

**2026-09-22 06:21 CEST** · RE-DISPATCH run of the review-fix ticket "Dead-mount deploy abort re-introduced: new HM-managed cache paths missing from deploy.sh pre-switch reap" (reviewer finding, medium) against rejected commit `f39e144b`.

**Verdict: verification-only re-dispatch. The finding's fix was ALREADY fully landed by the earlier run of this same ticket — `d60d598c` (05:28, exact footer, in HEAD ancestry). This run verified the current tree satisfies every clause of the finding clause-by-clause, ran the repo gates (all green), and correctly made NO new change: an empty or duplicate-footer commit would corrupt the queue↔git cross-reference and violate smallest-correct-change. One process miss to own: the dedup discovery was accidental, not the first command.**

Worked files: none (verification-only). Tree clean at exit. Nothing pushed.

---

## a) FULLY DONE

1. **Finding re-anchored per the disposition protocol.** `git cat-file -e f39e144b` → exists; anchor text located via the verbatim string at `platforms/nixos/users/home.nix:470` (no line-number dependence). The finding APPLIES to the current tree shape — its remedy is already in it.
2. **Dedup discovery.** `git log --all --grep=<ticket-id>` (run during the report, should have been step 1) shows exactly TWO commits carry this ticket's footer: `d60d598c` (the work commit) and `a5377c94` (the prior run's report). Both verified reachable from HEAD (`git merge-base --is-ancestor`). The queue therefore already holds a footer-suffixed cross-reference for this ticket.
3. **Clause-by-clause verification of the expected fix in the CURRENT tree:**
   - `~/.cache/pnpm` in the deploy.sh pre-switch reap loop → `scripts/deploy.sh:175` (`pnpm` in the `.cache` loop).
   - `$HOME/.local/state/pnpm` + `$HOME/.cargo/registry` reaped the same way → `scripts/deploy.sh:186-191`: new loop, exact names, `[ -e ] && [ ! -L ]` guard, and UNCONDITIONAL of mount state — verified by reading the whole pre-switch section (lines 149-201) that no `mountpoint` gate surrounds the reap.
   - buildcache-usb-recovery step-2.5 mirror → `modules/nixos/services/buildcache.nix:341` (`pnpm` in the `.cache` loop) and `:350-355` (the two non-`.cache` names).
4. **Fix-file stability since the fix.** `git diff d60d598c..HEAD` over the three fix files is EMPTY; the post-fix commits (`a5377c94`, `0535a0b6`, `59fc4294`) touched only docs/queue/AGENTS/CHANGELOG plus a parallel session's unrelated files (discordsync.nix, two test files — none reap-related, confirmed via full diff-stat).
5. **Repo gates green on the current, clean tree:** `bash -n scripts/deploy.sh` OK; `nix fmt --no-update-lock-file -- --ci` → 2426 traversed, 0 changed; `nix flake check --no-build` → all checks passed (aarch64-darwin omission expected per AGENTS.md). Because the tree was clean, every gate ran against the COMMITTED state.
6. **Correct disposition discipline.** No new commit invented for the fix itself; no history ops; nothing pushed; parallel-session content untouched.
7. **In-passing verification:** AGENTS.md's Build Cache section NOW documents the three new HM-managed fallback paths, the activation block, and the four reap surfaces including `d60d598c` (the prior report's §b.2 gap — closed by the 05:55 window-closeout pass, `59fc4294`).

## b) PARTIALLY DONE

1. **Build-level verification was INHERITED, not re-executed.** The recovery unit-script derivation (writeShellApplication → shellcheck at build time) was realized and green in the prior run; this run relied on byte-identity of `buildcache.nix` instead of re-building it. Right-sized for identical bytes, but the inheritance was not stated in the run's close-out message — only in this report.
2. **"No regression test added since" was under-evidenced until the report.** My first pass diffed only the three fix files post-`d60d598c`; only while writing this report did the full diff-stat confirm no reap test was added (the two `tests/` changes since are the parallel session's paperless/restic edits).
3. **TODO_LIST/queue lineage closure.** Correctly untouched per this ticket's contract (the queue derives via the footer), but the parent note (TODO_LIST:21) and storage.md rows 80/82 still do not reference `d60d598c` as the review-fix completion — harvest item, flagged in §f.6.
4. **CHANGELOG: the fix has no standalone entry** — it is only mentioned inside the 05:55 AGENTS-update entry (CHANGELOG line 21). Minor doc gap.

## c) NOT STARTED (all adjacent, none invented)

1. **Regression test for the reap discipline — still nonexistent.** No fixture covers the real-dir/symlink-occupant/absent cases for either reap site. The parent run's VM test was PSI-gated and never built; nothing since.
2. **Reap-list single-sourcing.** The name set still lives in FOUR hand-kept places (deploy.sh `.cache` loop, deploy.sh non-`.cache` loop, usb-recovery step 2.5's two loops, home.nix activation block). The drift class that minted this very finding remains structurally open — the reviewer finding is the proof it recurs.
3. **buildcache-init provisioning gap (confirmed still open).** `buildcacheDirs` has `pnpm-store` but not `pnpm-cache`, `pnpm-state`, or `cargo/registry`; the only creator of those targets is the mount-gated home.nix activation block. After a mount recovers via usb-recovery, the HM symlinks dangle with ENOENT until the NEXT deploy.
4. **Deploy of the fix generation + post-deploy symlink verification** (storage.md row 82 `[watch]`): not run — outside a fix-ticket verification scope and queue/owner-paced. Live stakes unchanged: `~/.cache/pnpm` (~68M) and `~/.cargo/registry` sit on evo-x2 as REAL dirs (prior-run live check), so the next deploy from a pre-`d60d598c` tree would abort at checkLinkTargets; from this tree it reaps both.
5. **mountPoint-vs-HM-symlink eval guard** (parent deferral): still open.
6. **Queue-side dedup**: this ticket was re-dispatched after completion — a queue bookkeeping gap I cannot self-diagnose.

## d) TOTALLY FUCKED UP!

No data loss, no history operations, tree clean at exit — but two real process misses:

1. **The dedup probe was discovery-by-accident, not step 1.** I learned `d60d598c` carried this ticket's footer only because my second tool call ran `git show --stat` on BOTH commits and the stat output happened to include the full commit message. The disciplined first action on any review-fix re-dispatch is `git log --all --grep=<Task-Queue-ID>` — one command that would have surfaced the completed fix immediately. Had I not stumbled into it, the likely failure mode was re-implementing an already-present fix and minting a redundant footer-suffixed commit — exactly the queue-corruption class the contract warns about ("exactly ONE Task-Queue-ID footer per commit … duplicate footers corrupt the queue↔git cross-reference").
2. **Read-the-lineage miss, repeated.** The prior run's own §d.3 flagged "read the queue lineage before touching shared surfaces" — and I read the prior status report only AFTER finishing verification, while hunting report filenames for comparison. Reading it first would have handed me the adjacent-gaps inventory and the daemon-race history up front.
3. **Minor, self-caused:** the under-evidenced "no regression test exists" claim in my working notes (fixed by the full diff-stat during this report) — assert from full scope, not from the slice you happened to diff.

Nothing on the scale of the prior run's daemon-race amend occurred here: zero amend/rebase/reset, no staged-set ambiguity (nothing was staged), nothing pushed.

## e) WHAT WE SHOULD IMPROVE!

1. **Fix-task runbooks should mandate the footer dedup probe as step 1** (`git log --all --grep=<Task-Queue-ID>`): re-dispatch of a completed ticket is evidently a real queue mode, and the probe is one command that converts the whole run into verification.
2. **Read the same-ticket prior report BEFORE verification** (`ls docs/status | grep <ticket-id>`): it enumerates known residuals, prior incidents, and adjacent gaps — this run re-derived §c from it late.
3. **State verification inheritance explicitly in the close-out** when relying on a prior run's stronger gate over byte-identical content (build realization vs eval-only): "verified byte-identical to the build-verified state" is a different (weaker, honest) claim than "build-verified this run".
4. **Full-scope diff-stat belongs in every re-dispatch verification** (`git diff <fix-commit>..HEAD --stat`, unscoped): answers "did anything else touch this area / were tests added" in one shot; the scoped variant I ran first answered only half.
5. **The queue should mark or detect completed tickets** (footer already present in history) so verification-only runs are a first-class, cheap disposition rather than a full re-anchoring exercise.
6. **Keep the gates-run-on-committed-state habit**: this run's tree was clean so gates covered the committed state for free; when a tree is dirty at verification time, gates should be re-run after any stashing/commit to keep the evidence honest.

## f) Up to 50 things we should get done next

Harvest-ready, sorted by impact, all inside this ticket's blast radius (the reap/cache-fallback domain):

1. Deploy the current generation — the load-bearing test: deploy.sh's pre-switch reap must reap the live `~/.cache/pnpm` (~68M) + `~/.cargo/registry` real dirs; from the pre-`d60d598c` tree this deploy would abort at HM checkLinkTargets.
2. Post-deploy: verify the three HM symlinks resolve (`readlink` → mount paths) and `migrate-buildcache-fallback-caches` journaled its reap lines; close storage.md row 82 `[watch]`.
3. Regression test for BOTH reap sites: temp-home fixtures with PATH-stubbed loops covering real-dir (reaped), symlink-occupant (kept), and absent (no-op) cases — extract the loops into shell functions first for testability.
4. Single-source the reap name list into one Nix attrset + eval-time (or pre-commit) parity assertion across the four sites — kills the drift class that produced this finding.
5. buildcache-init: add `pnpm-cache`, `pnpm-state`, `cargo/registry` to the provisioning list (close the dangling-ENOENT window between mount recovery and the next HM activation).
6. Lineage closure: reference `d60d598c` as the review-fix completion in storage.md rows 80/82 and the TODO_LIST:21 parent note.
7. CHANGELOG: standalone entry for the deploy.sh pre-switch reap fix (currently only inside the 05:55 AGENTS-update entry).
8. Queue-side: investigate why a completed fix ticket was re-dispatched; add dispatch-time footer dedup so re-dispatches are marked verification-only.
9. Cheap interim parity tripwire: static test asserting deploy.sh and buildcache.nix reap loops contain the same name set (until item 4's attrset lands).
10. post-deploy-check: assert all three paths are symlinks (`test -L`) and WARN when the reap section actually reaped something (operational visibility of the fix firing).
11. usb-recovery step 2.6: pre-create the three targets when the mount is healthy post-recovery (partial bridge for item 5 at recovery time).
12. Eval guard (parent deferral): every `home.file` mkOutOfStoreSymlink whose target is under `/mnt` must have a matching reap entry + init provisioning — make the class structurally impossible.
13. Sweep for OTHER HM-managed out-of-store symlinks whose targets live on mountable filesystems (Jan `/data` link is activation-handled; anything else?) — same dead-mount abort class.
14. Env-less cache churn audit: which cache paths still fall back to NVMe during dead-mount windows without HM management (`~/.cache/pip`, `~/.cache/uv`, …) — quantify, then symlink or accept per path.
15. Verify the pre-commit hook's shellcheck leg actually covers `scripts/deploy.sh` (the prior run hit a staged-file-detection silent-skip under daemon races — confirm the hook sees `.sh`).
16. Investigate the pre-commit "No staged .nix files" false report under concurrent staging (prior report §e.5) — potential silent-skip class for ALL language gates.
17. Fold the non-`.cache` reap loop into the main loop via a name→relpath map in deploy.sh (readability; one loop per file instead of two).
18. VM test for the buildcache module (parent deferral; PSI-gated then — queue paces).
19. AGENTS.md one-liner: which sessions hit which cargo registry path (env'd `CARGO_HOME` → `/mnt/buildcache/cargo/registry`; env-less → HM symlink `~/.cargo/registry`) — prevents future confusion about double registries.
20. post-deploy smoke: extend buildcache checks to assert the new targets EXIST on the mount (pairs with item 5).
21. Confirm CI (`nix-check.yml`) runs shellcheck/`bash -n` on `scripts/deploy.sh` (unverified in passing).
22. Re-verify buildcache-gc's ReadWritePaths holes match the new symlink targets (`~/.cache/pnpm`, `~/.local/state/pnpm`) post-deploy.
23. During the next dead-mount episode: capture deploy output showing the reap lines — operational proof the fix fires under the real conditions it was built for.
24. Consider a `buildcache_fallback_symlink_ok` textfile metric — only if the post-deploy smoke (item 10) proves insufficient; probably skip.
25. Runbook: short buildcache ops page under `docs/services/` if ops friction shows (AGENTS.md currently carries the knowledge).
26. Standing rule (from prior report §e.6, re-earned here): when a commit path deviates from normal pre-commit flow (daemon commits, amends, plumbing), re-run the gate sweep against the COMMITTED state.
27. If item 4 lands, add the reap-parity assertion to docs/CONTRIBUTING's guard inventory.
28. Audit deploy.sh's incident-debris loop names (`go-build-fallback`, `go-build-override`, `go-mod-fallback`, `golangci-lint-override`) for staleness post-symlink-coverage — prune or keep with dates.
29. AGENTS.md one-liner for the `[ ! -L ]` keep-symlinks semantics (why symlink occupants survive the reap — tool state preserved).
30. For `.nix`-only verification runs: consider a targeted `nix eval` of touched units before the full `nix flake check --no-build` (speed; the full check remains the final gate).

## g) Questions I cannot figure out myself

1. **Queue re-dispatch semantics:** this ticket was re-dispatched ~50 min after its fix and report were committed. Is that a queue defect (footer association lost) or intentional pacing? Should verification-only re-dispatches produce a queue-visible artifact beyond the report file — e.g., is a NO-OP commit with the footer wanted for bookkeeping, or is report + the existing footer-suffixed work commit the sanctioned close?
2. **Deploy scheduling:** the reap fix makes the NEXT deploy load-bearing (it reaps the two live real-dir occupants), and the parent row's PSI-gated VM-test item is also waiting on a quiet window. Do you want the reap-bearing deploy scheduled deliberately in a quiet window, or ride the next routine deploy?
3. **Parent-row fold:** the TODO_LIST:21 storage item is BLOCKED-flagged with remaining library sub-items (VM test, eval guard) adjacent to this fix. Should those fold into the deploy window as one task, or stay separate queue items paced independently?
