# Status Report — Task `000001a0d7ddc4c5db6b3a8da3acdfb9a1d8`

**Date:** 2026-09-25 11:51 CEST
**Task:** Reconcile the root `@` retention anomaly (12-day-old weekly vs `snapshot_preserve 3d 1w`) and restate the doctrine's root window
**Scope:** single queue work item (storage domain). No unrelated research; "noticed in passing" items are explicitly labeled.
**Work commit:** `22a4e88f` — "docs: reconcile root @ snapshot retention — weekly buckets are calendar-anchored, pin window is 2w sharp" (Task-Queue-ID footer present, not pushed)

> **Format note:** the status-report skill's canonical output is a styled HTML dashboard; the tasking explicitly requested `.md` at this path. User instruction wins — Markdown it is (flagged per skill contract).

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | **Root cause of the "12-day-old weekly" established from btrbk source, not folklore.** Read the deployed btrbk 0.32.7 `schedule()` implementation (store path `/nix/store/h6dbk7…/bin/.btrbk-wrapped`): retention buckets are **calendar-anchored** — days start 00:00, weeks start Sunday (`preserve_day_of_week` default `sunday`, matching the observed Sunday-dated weeklies). `snapshot_preserve 3d 1w` keeps ALL snapshots from the last 3 day-buckets **plus the FIRST snapshot of each of the TWO calendar weeks covered** (current + previous). Verified 0.32.7 is the deployed version via the store path before reading its logic. | btrbk source lines 4630–4840 (`sub schedule`), `config_preserve_hash` (line 3879), `FORCE_PRESERVE latest-common-snapshot` branch (line 7080s, ruled out as the cause) |
| 2 | **Live inventory cross-check matches the model exactly.** `/mnt/btrfs-root/.snapshots/` holds `@.20260913`, `@.20260920` (the two calendar-week weeklies) + `@.20260921`–`0924` (daily buckets 3..0 at the 09-24 23:00 run). Every surviving snapshot is explained by a named preserve rule; nothing unexplained lives in the set. | `ls /mnt/btrfs-root/.snapshots` at 11:41 |
| 3 | **Deletion-timing math derived:** a weekly landing at a week start (Sunday 23:00) survives until the first run after its second Sunday boundary = **exactly 14 days** (delta_weeks flips 1→2 at the Sunday-boundary run). The 12-day-old weekly was IN-SPEC — no prune gap, no preserve_min involvement, no send-parent role. | derived from `schedule()` delta formulas + live dates |
| 4 | **Doctrine restated in AGENTS.md** (BTRFS section, "Snapshot-pinning doctrine" paragraph): root `@` window changed from "≈ 1-2w approximate / may exceed nominal (prune gaps ride guard/outage-killed nights)" to **2w sharp**, with the calendar-anchor mechanics spelled out and the old wrong "prune gaps" framing removed. /data's 25d weekly noted as the same calendar math within its 28d 4w budget. | AGENTS.md line ~657, commit `22a4e88f` |
| 5 | **Both todo surfaces closed without drift** (queue rule: one-liner and library entry must not disagree): `docs/todo/storage.md` item marked `[x]` with the full finding; `TODO_LIST.md` row marked `[x]` with a compact version + pointer. Neither renumbered nor reworded siblings. | storage.md line 68, TODO_LIST.md line 48 |
| 6 | **Commit discipline held under the daemon race.** The auto-commit daemon committed my 3 files with a heuristic message mid-flight (first `git commit` exited 1 "nothing to commit"). Verified the daemon commit contained EXACTLY my 3 files / 1 line each via `git log --stat` (no parallel-session contamination), then amended it into the properly-messaged commit with the task footer — amend-forward, no reset, per the multi-agent write-discipline rules. | git log: `7c2581962` → amended to `22a4e88f` |
| 7 | **Verification gate ran and passed:** pre-commit hooks (gitleaks clean, no trailing whitespace, eval-only `nix flake check` green) executed on both commit attempts. Docs-only change; nothing left the repo broken. | pre-commit output in both runs |

## b) PARTIALLY DONE

| # | Item | What's missing |
|---|------|----------------|
| 1 | ~~**One stale cross-reference survived my sweep.** The BTRFS "Emergency reserve (T14)" paragraph in AGENTS.md (~line 696) still says the reserve's extents are pinned by "3d+1w retention" — I updated the doctrine paragraph but did not grep-sweep the file for OTHER restatements of the root window. The correct figure there is now "2w sharp". One-line fix, should have caught it.~~ done at `eb844911` (reserve T14 caveat + both doctrine-table rows + the ClickHouse note aligned 2026-09-25; FEATURES.md's late echo fixed `3e3d718a` 2026-09-26) | queued below (f.1) |
| 2 | **Reconciliation depth: source-read + inventory, no scheduler confirmation run.** I derived the bucket assignment by hand from the perl math. A `btrbk … --dryrun`/`stats`-style invocation (even read-only `btrbk info` against the live config) would have printed the scheduler's own "preserve weekly: 1 weeks ago, 0d after sunday" labels and turned derivation into observation. Not run — did not want to touch btrbk's config plumbing without need; hand-derivation was verified against 6/6 live snapshots. | optional confirm below (f.4) |
| 3 | **Scope of window restatement: root `@` only, per the work item.** The same calendar-anchor math governs `/data` (14d 4w) and pool `services/*` (7d 4w) — their worst-case windows in the doctrine table are still the sweep's approximations (≈4-5w / ≈4w). They are consistent with the model (calendar buckets add up to ≤ +1 bucket over the naive reading) but were not individually reconciled. | see (g.3) |
| 4 | ~~**(Noticed in passing, unfinished by design)** The doctrine table row for the emergency reserve still carries the old "root ≈ 1-2w" pin figure in its FS/worst-case column ("root ≈ 1-2w" — the table row says `root ≈ 1-2w` / emergency-reserve row). Same class as b.1: the reconciled "2w sharp" wording did not propagate into every cell that repeats it.~~ done at `eb844911` (both table rows now read `root = 2w sharp`) | part of f.1's grep sweep |

## c) NOT STARTED

Nothing within the work item's contract was left unstarted — the item is fully closed. Items deliberately NOT started because they are follow-ups, not this task:

| # | Item | Why not started |
|---|------|-----------------|
| 1 | Matching calendar-anchor reconciliation for `/data` and pool `services/*` windows in the doctrine table | Outside the item's ask (root `@` only); tracked as a candidate (f.5, question g.3) |
| 2 | Any eval-time guard tying docs numbers to `snapshots.nix` values | Decision-grade, not agent-executable unilaterally (f.6, question g.1) |
| 3 | A scheduler-output-based retention self-check metric (dryrun label scrape → textfile → Gatus) | New tooling; would need design + VM surface; ROADMAP-tier (f.7) |

## d) TOTALLY FUCKED UP

**Nothing.** No broken state left behind, no data at risk, no revert needed. Honest near-misses, already contained:

| # | Near-miss | Containment |
|---|-----------|-------------|
| 1 | **Daemon race produced an exit-1 commit attempt.** Between `git add` and `git commit`, the auto-commit daemon swept my 3 files into a heuristic commit, so my commit found "nothing to commit" and exited 1. | Diagnosed immediately from `git log --stat`; verified daemon commit contents were EXACTLY my files (no foreign files absorbed); amended into the proper message. Net state correct. Lesson already in AGENTS.md; I followed it rather than repeating the "bare re-commit" mistake. |
| 2 | **First edit attempt failed the read-before-edit gate** (edit tool refused AGENTS.md — a 700+ line file I had only grepped). | Re-viewed the target paragraph with line numbers, then edited with an exact match. No blind writes occurred. |
| 3 | **The task queue's own description embeds a now-corrected false premise** ("determine whether the preserve_min floor / send-parent role keeps it") — neither mechanism was involved. If a future session had read the queue row as evidence rather than as a question, it would have chased a ghost. | The closed-out row now states the answer explicitly in both todo surfaces. |

## e) WHAT WE SHOULD IMPROVE

1. **Cross-reference discipline on "restate the number" edits.** A retention window figure appears in at least 3 places (doctrine paragraph, doctrine table rows, reserve T14 caveat). Any future figure change needs a one-grep sweep (`3d 1w`, `1-2w`, `4-5w`) across AGENTS.md + docs/todo + docs/services, not a paragraph edit. I fixed the paragraph and missed two echoes.
2. **Prefer observation over derivation when the observation is one command away.** The hand-derived bucket math was right, but the scheduler's own dryrun labels would have been both faster to trust and self-documenting evidence for the report. (Bounded by not wanting to run btrbk config plumbing casually — a documented read-only invocation would close this.)
3. **The doctrine's root-window entry had been written as "approximate, may exceed nominal" one day before this reconciliation.** The 2026-09-25 sweep session had the same live inventory in hand and still shipped the wrong (too-loose, wrong-mechanism) framing. Writing "we don't know why it's older" into doctrine invites ghost-hunting; "here is the mechanism that keeps it" should be the bar for doctrine numbers.
4. **Queue-row phrasing should not assert candidate mechanisms as likely.** "or the preserve_min floor / send-parent role keeps it" cost nothing here but encodes a wrong prior into future sessions' context.
5. **Daemon-race handling worked but was manual.** Amend-after-daemon-commit is now a repeated pattern (this is at least the second task window where the daemon committed the queue's work first). A wrapper habit — `git add` → immediate `git commit` in one compound command — would shrink the race window; the queue harness could also accept "amended the daemon's commit" as the normal path without an intervening exit-1.

## f) WHAT WE SHOULD GET DONE NEXT (≤50, most impactful first)

Tier 1 — direct fallout of this task (small, agent-executable):

| # | Task |
|---|------|
| 1 | Grep-sweep the root-window echoes: fix the reserve T14 caveat ("3d+1w" → the reconciled 2w-sharp figure) and the emergency-reserve doctrine-table row ("root ≈ 1-2w"), and any other stale restatements (`3d 1w`, `1-2w` across AGENTS.md, docs/todo, docs/services). |
| 2 | Re-verify whether the removed "prune gaps ride guard/outage-killed nights" framing in yesterday's doctrine ever tracked a REAL historical prune gap (check the 05-32 status report's §f.41 evidence) — if yes, that incident needs its own entry separate from retention math; if no, note the misdiagnosis. |
| 3 | Add the calendar-anchor semantics (weeks start Sunday; weeklies live 14d) as a short comment in `snapshots.nix` next to `snapshot_preserve = "3d 1w"` so the config carries the "why" where operators look first. |
| 4 | One-shot observation proof: capture a btrbk dryrun/info scheduler output showing "preserve weekly" labels against the live inventory and archive it (closes the b.2 derivation-only gap). |

Tier 2 — same-class extensions (owner-gated or decision-gated):

| # | Task |
|---|------|
| 5 | Reconcile `/data` (14d 4w) and pool `services/*` (7d 4w) worst-case windows in the doctrine table the same way (calendar buckets, live inventory probe). |
| 6 | Decide: docs-only doctrine numbers vs a lightweight eval-time guard that fails `nix flake check` if AGENTS.md's window figures drift from `snapshots.nix` values (grep-style parity check; cheap but adds a coupling surface). |
| 7 | Consider a retention self-check textfile metric (btrbk dryrun labels scraped 1×/day → `btrfs_retention_*` gauges + a Gatus presence check) so a future "snapshot older than expected" question is answered by reading a metric, not source-diving. |
| 8 | Same treatment for `@home-hermes` (3d) and `hot/forgejo` (3d) windows if their weeklies/anchor days ever change. |

Tier 3 — noticed in passing during this task (not researched, flagging only):

| # | Task |
|---|------|
| 9 | `@home-hermes` local set shows `09-16` + `09-20..24` — the 09-16 snapshot is 9 days old against `snapshot_preserve 3d` (no weekly in its config, per snapshots.nix line 329). Either its retention differs from my reading, a send-parent/correlation is holding it, or a prune gap exists there. Worth one look — NOT touched this run. |
| 10 | The doctrine table still frames windows as the sweep left them ("/data ≈ 4-5w" etc.) with "probed 2026-09-25" dating — after f.5 those dates should be refreshed in one pass to avoid a mixed-provenance table. |
| 11 | AGENTS.md still contains, elsewhere, the pre-reconciliation sentence "root `@` … ≈ 1-2w" in the sweep-summary context if any — covered by f.1's grep; do not fix piecemeal. |

(Tiers beyond this are backlog/ROADMAP material; per the skill contract, larger-N brainstorm items belong in docs-health HARVEST routing, and items 1–4 are the only ones I appended/directly queue below.)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Do you want doctrine window figures guarded by eval (docs↔config parity check) or kept docs-only?** A grep-based flake check could fail `nix flake check` if AGENTS.md's stated windows drift from `snapshots.nix` retention — but it adds a coupling surface for one number that has changed exactly once. Your call on the trade.
2. **Was the removed "prune gaps ride guard/outage-killed nights" framing tracking a real historical gap?** Yesterday's sweep session wrote it from the same 12-day observation; if there was ALSO a genuine storm-skipped prune night in the evidence, that incident deserves its own record rather than being silently folded into (now-disproven) retention math. I can re-read §f.41 if you want it chased.
3. **Should the same calendar-anchor reconciliation be extended to `/data` and pool `services/*` windows now, or is root-only the accepted scope?** The model says their approximations are consistent, but "consistent with the model" is what the root window claimed yesterday too.
