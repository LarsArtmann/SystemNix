# Task 000001a0a715dc510818faf611ea0ca534cc — Status Report

**Date:** 2026-09-16 02:34 CEST
**Scope:** Reviewer-finding fix on the crush-hot-db work (parent task 000001a0a1f149e85a362b51f59342b2d73d, TODO_LIST P1 "Move crush session DBs off the QLC root"). Per instruction: THIS task's work only, plus what was noticed in passing. No unrelated research.

---

## The finding (verbatim summary)

> `crush-hot-db-migrate.service` declares no `wantedBy` (eval → `[]`), so it is a static unit; `systemctl is-enabled --quiet` returns rc!=0 and the loop's gate in `scripts/deploy.sh:425` silently skips it — the claimed deploy-time convergence never happens. This is the documented dnsblockd trap. Every other loop member sets `wantedBy = [ "multi-user.target" ]`. Fix: add `wantedBy` (optionally + `RemainAfterExit`, tq-storage-dir shape) or an is-active-gated dedicated block; update the "deploy.sh post-switch restart converges it" claims in AGENTS.md, docs/services/crush.md, and TODO_LIST.md.

**Verdict: the finding was CORRECT and is FIXED.** I verified it first (`rg` over the module showed no `wantedBy`; the module's own header even claimed deploy-restart-audit "enforces" a loop that could never reach the unit — a claim-vs-mechanism split).

---

## a) FULLY DONE

1. **Finding reproduced before fixing** — confirmed `crush-hot-db-migrate` had no `wantedBy` (static unit) and that `scripts/deploy.sh:420-429` gates the provisioner loop on `systemctl is-enabled --quiet` (rc=1 for static units → silent skip). Matched the AGENTS.md dnsblockd-bridge trap class exactly.
2. **Module fix** (`modules/nixos/services/crush-hot-db.nix`): added `wantedBy = [ "multi-user.target" ]` + `RemainAfterExit = true` (the tq-storage-dir shape the finding recommended; `Restart` stays `mkForce "no"` via `serviceOneshotDefaults` — correct for oneshots). Comments record WHY: the is-enabled gate and the dnsblockd-bridge trap class.
3. **Eval-verified the rendered unit** on evo-x2: `wantedBy = ["multi-user.target"]`, `serviceConfig.RemainAfterExit = true`, `serviceConfig.Restart = "no"`, timer `wantedBy` unchanged (`["timers.target"]`).
4. **Deploy-restart-audit re-checked by construction**: the unit matches the `-migrate` converger pattern AND now appears in deploy.sh → audit stays satisfied; no allowlist entry needed.
5. **All three doc claims updated** so they now describe a real mechanism: AGENTS.md (crush-hot-db section — "ENABLED via wantedBy multi-user.target … runs at boot + daily 04:10 + every deploy"), docs/services/crush.md (same), TODO_LIST.md item 19 (convergence sentence + REVIEW FIX mapping line).
6. **Full eval gate green twice**: `nix flake check --no-build` → exit 0 (all module checks + NixOS assertions, including the unit-shape audits: systemd-shape, mount-gating, deploy-restart, start-limit). `nix fmt --no-update-lock-file -- --ci` → 0 changed (no formatter drift, no lock churn).
7. **Footer-carrying commit landed**: `d1e84088` carries the exact `Task-Queue-ID: 000001a0a715dc510818faf611ea0ca534cc` footer (verified with `git log -1 --format=%B | rg`). Never pushed. Working tree clean at finish.
8. **gitleaks red root-caused and DOCUMENTED, not silenced blindly**: replicated the pre-commit hook's exact staged-tree scan (`git checkout-index -a` + gitleaks 8.30.1 with the repo config, JSON report). All 17 findings are `sourcegraph-access-token` on bare 40-hex content in `flake.nix` (15 — `github:` URL rev pins, oldest flagged pin landed 2026-09-13 in `72653bc4`) and `scripts/audit-push-protection-literals.sh` (2 — fixture hex literals). **Zero findings in the files this task commits.** `--no-verify` used with this evidence in the commit message (precedents: `cf56690e`, `73145250`).

## b) PARTIALLY DONE

1. **The parent work item is NOT deployable yet** (not this finding's scope): TODO_LIST P1 item 19 stays `[ ]` — deploy is gated on the ratified /nix soak (~2026-09-17) and is sudo-gated; first migration run needs a no-crush-session window; PSI before/after measurement is post-deploy by definition. The finding fix rides the same future deploy.
2. **Comment reflow cleanup**: my header-comment edit initially joined two comment lines awkwardly (noticed only when reviewing the daemon's swept diff via `git show e9beae89`). Fixed in `6c85ee1b`, but the fix rode a second daemon sweep instead of my own commit — see d)1.
3. **Live verification of the fix is impossible pre-deploy**: eval-time shape is proven, but `systemctl is-enabled crush-hot-db-migrate` on the RUNNING host still fails until the next deploy lands the new generation. Docs claims are now true of the config, not yet of the machine.
4. **Commit-mapping hygiene**: the first REVIEW FIX mapping line I wrote said "mapping = this commit" before I knew which commit would survive the daemon race — self-referential claim, swept into `ce4cf783`/`6c85ee1b` within minutes. Corrected in `d1e84088` to the durable form ("the commit carrying the Task-Queue-ID cross-reference is the queue-ID-remap precedent"), but the repo now has one stale intermediate phrasing baked into `6c85ee1b`'s TODO_LIST content (superseded by `d1e84088`; harmless, historical).

## c) NOT STARTED (all post-deploy or follow-up guards, deliberately not pulled into this minimal fix)

1. **Deploy + post-deploy verification chain**: `nix run .#deploy` after the soak → assert `systemctl is-enabled crush-hot-db-migrate` = enabled → deploy.sh log line `Restarting provisioner: crush-hot-db-migrate.service` → symlink sweep (`ls -la ~/projects/*/.crush | grep '^l'`) → `node_psi_io_some_avg60` vs the 40-60% baseline in SigNoz.
2. **Structural guard against the finding class recurring** (see e)3/f)7): no eval-time check currently forces "named in deploy.sh's provisioner loop ⇒ unit is enabled or has an is-active-gated block". The reviewer found this by hand; a guard would have caught it at `nix flake check`.
3. **Fold-in to `services.hot-db` Phase-2** (`crush.projects` attrset + tmpfiles) when that module lands — documented as INTERIM; no code moved.
4. **onFailure/alerting decision for the unit** (see e)4): with `wantedBy = multi-user.target`, a boot with the Samsung absent (`nofail` mount down) now leaves a FAILED unit at every boot — tq-storage-dir precedent says fail-loudly is accepted, but this unit has NO `onFailure` routing and is NOT in `system-health.monitoredServices`, so "loudly" currently means "visible in systemctl only".

## d) TOTALLY FUCKED UP

Nothing destructive, no data loss, repo gates green — but two process failures worth naming honestly:

1. **Lost the daemon race twice; the code commit carries no footer.** Sequence: (1) first `git commit` attempt ran the pre-commit gitleaks scan (~10s, whole-index scan = the daemon's window); (2) while it ran/was retried, the auto-commit daemon swept my staged files into heuristic commits (`e9beae89`, `ce4cf783`, `6c85ee1b`) — each also carrying parallel sessions' files, so amending was forbidden by the daemon-race doctrine; (3) the footer had to land in a separate docs-only cross-reference commit (`d1e84088`, queue-ID-remap precedent `cf56690e`). Net: `git log` again tells the story through a mapping commit instead of the code commit — the exact "meaningless heuristic history" pattern the per-task-commit lesson warns about. Root causes on my side: I ran the full pre-commit hook despite ALREADY having evidence the gitleaks leg was red at HEAD (I had the 17-finding report from my own replicated scan minutes earlier) — the correct move per precedent was `--no-verify` + justification on the FIRST attempt, which minimizes the hook's 10s race window to zero.
2. **Repo-wide: every verified commit is currently blocked by a pre-existing gitleaks red.** The `sourcegraph-access-token` rule matches bare 40-hex, and `flake.nix` pins ~15 flake inputs by 40-hex rev in `github:` URLs (plus 2 fixture literals in `scripts/audit-push-protection-literals.sh`). This has been true since `72653bc4` (2026-09-13) — three days of daemon-only commits (presumably `--no-verify`). This is not my finding to fix, but it means the repo's primary secret gate is currently a silent no-op for anyone following the verified-commit path, and the only scanner that actually runs is the daemon, which skips it. Flagged here so it gets an owner (see f)10/f)11).

## e) WHAT WE SHOULD IMPROVE

1. **Pre-verify gates before commit attempts.** Run the hook's expensive/known-flaky legs manually first (the replicated gitleaks staged-tree scan takes ~35s but settles "is the red mine?"); then commit with the correct flag ONCE. The wasted attempt cost a daemon race, not just seconds.
2. **Treat the 10s in-hook gitleaks scan as a daemon-race window.** Either pre-settle the gate, or accept that pathspec commits during heavy parallel-session activity may lose the race and plan the mapping-commit fallback up front (it worked, but by reaction, not by plan).
3. **Make the finding class a guard, not a review catch.** Extend `deploy-restart-audit.nix` (it already cross-references deploy.sh text): any unit NAME appearing in the deploy.sh provisioner loop must have a non-empty `wantedBy` OR be exempted via an explicit `allowUnits`-style list with reason. Negative-test via the `extendModules` + hand-probe convention (eval-cache trap).
4. **Decide the Samsung-absent boot posture for the now-ENABLED unit.** Options: (a) accept failed-unit-at-boot (tq-storage-dir precedent) + add `onFailure` routing and/or `system-health.monitoredServices` registration so it actually alerts; (b) `ConditionPathIsMountPoint = cfg.mountPoint` skip-cleanly (atticd-bootstrap precedent — but that one had a readiness probe so it could still fail loudly when wedged). My recommendation: (a) — a dead hot-DB disk should be visible, and the unit's own job failure is the signal.
5. **AGENTS.md "Adding a Service" checklist compliance gap**: step 5 says every service must set `startLimitBurst = 5; startLimitIntervalSec = 300`. `crush-hot-db-migrate` (and, noticed in passing, `tq-storage-dir`) set neither. Harmless for Restart=no oneshots, but the checklist is written as unconditional — either add the keys for consistency or scope the rule to daemon units explicitly.
6. **Write self-referential mapping claims only about commits that already exist.** "mapping = this commit" was unwritable truth at authoring time. Durable form: name the swept SHAs, then say the footer "rides the queue-ID-remap precedent".
7. **Review the file after multiedit, not after the daemon swept it.** The comment reflow artifact survived one commit cycle because I verified via `nix fmt`/flake-check (both green — they don't catch prose wrapping) instead of a quick `git diff` of my own edit.

## f) Up to 50 things we should get done next

Ordered roughly by impact. Items 1-6 close THIS task's loop; 7-15 harden against the finding class; 16-30 are the noticed-in-passing repairs in this task's orbit; 31-50 are lower-priority/ROADMAP-grade.

**Close the loop (post-soak deploy window):**
1. After the /nix soak (~2026-09-17): `nix run .#deploy` carrying this fix.
2. Post-deploy: assert `systemctl is-enabled crush-hot-db-migrate` → `enabled`, and the deploy.sh journal line `Restarting provisioner: crush-hot-db-migrate.service`.
3. Post-deploy: symlink sweep `ls -la ~/projects/*/.crush` (incl. `~/projects/.crush` → `projects-root`) + spot-check one moved DB opens (crush session works).
4. Post-deploy: SigNoz `node_psi_io_some_avg60` vs the 40-60% storm baseline; annotate the deploy timestamp on the dashboard.
5. Post-deploy: update TODO_LIST item 19 → `[x]` (drop the BLOCKED suffix per file conventions) and AGENTS.md's "Deploy gated on the /nix soak" sentence.
6. Watch the first 04:10 timer run + first post-deploy boot run for correct skip-guards (crush-active skip, <10min-fresh skip, flock) — capture expected journal lines in docs/services/crush.md.

**Harden the finding class (guards + sweeps):**
7. Extend `deploy-restart-audit.nix`: provisioner-loop membership ⇒ unit must be enabled (`wantedBy` non-empty) or explicitly exempted with reason.
8. Negative-test that guard the repo way: `extendModules` on evo-x2, force `wantedBy = []` on a loop member, assert the new assertion message in `config.assertions`; hand-probe once (eval-cache trap convention).
9. Sweep ALL ~20 remaining provisioner-loop members for the same static-unit trap (the reviewer found one; nobody has proven the rest clean — `tq-bootstrap` is the one I'd check first, it was added alongside `tq-storage-dir`).
10. Repair the repo-wide gitleaks red: add a narrow `[[allowlists]]` for bare 40-hex inside `github:`/`git+ssh:` URL pins in `flake.nix` (or stop hard-pinning revs in input URLs where the lock can carry the pin instead — several pins are documented as INTERIM and could move to lock-only).
11. Fix the 2 fixture literals in `scripts/audit-push-protection-literals.sh` (lines ~50/55) to the `@HEX40@` template pattern (GitHub push-protection lesson: template, never literal).
12. Decide whether the auto-commit daemon may keep bypassing pre-commit (`--no-verify`): right now the ONLY staged-tree secret gate is skipped by the one writer that commits most often.
13. Add a deploy.sh guard that WARNs (or fails) when a KNOWN provisioner-loop member fails `is-enabled` — cheap bash defense-in-depth under #7.
14. Consider `post-deploy-check.sh` smoke: if the module is enabled on the host, assert the unit is `enabled` + has a recent successful run.
15. Decide the Samsung-absent boot posture (e)4) and implement: `onFailure` + `system-health.monitoredServices` registration, or skip-cleanly condition — owner call.

**Noticed-in-passing repairs in this task's orbit:**
16. Verify btrbk does NOT snapshot `/mnt/hot` (the interim toplevel has no subvolume; confirm no btrbk config globs catch it — hot-DB tier is designed unsnapshotted until Phase-2's bounded-retention design lands).
17. Decide interim `nodatacow` for `/mnt/hot/crush` (`chattr +C` on the dir → new files) per the ratified hot-tier intent (SQLite WAL on CoW btrfs = checksum/compression churn the Phase-2 design explicitly rejects); or document why interim CoW is acceptable.
18. Add `startLimitBurst = 5; startLimitIntervalSec = 300` to the unit (and audit `tq-storage-dir`) or scope the AGENTS checklist rule to daemons.
19. Make migration failures loud: `mv` failure currently logs "FAILED to migrate" but the script still exits 0 — track a failure count and exit non-zero (timer next-run visibility; OnFailure then has something to route).
20. Research whether crush offers a supported state-dir redirect (XDG_STATE_HOME or CLI flag) — the original item's alternative; a native redirect would retire the symlink mechanism cleanly.
21. Measure and document ACTUAL post-move DB+WAL sizes (the 2-5 GB/proj claim is from the storm report; record real numbers in docs/services/crush.md).
22. Confirm `deploy.sh`'s restart of the unit during a live crush session self-skips (pgrep guard) and that a later run converges — write the expected journal line into the runbook.
23. Check `git log -- flake.lock` after this session: my fmt runs used `--no-update-lock-file`; assert no daemon fmt pass re-locked moving-ref inputs as a side effect.
24. Confirm boot-time behavior: with `wantedBy = multi-user.target` the unit now runs at EVERY boot (previously timer+deploy only) — verify the boot run doesn't contend (ioTier.background + flock should hold) and note it in the runbook.
25. Consider a Gatus-independent freshness signal for the migration (e.g., textfile metric `crush_hot_db_migrate_last_success_timestamp`) — fail-closed absence, per the collectors doctrine — so "converged" is observable without journal digs.
26. Sweep for other `-migrate`-pattern oneshots anywhere in `systemd.services` that are static AND not in deploy.sh (deploy-restart-audit only fires when the name appears in deploy.sh; a static `-migrate` unit NOT listed would be silently unconverged AND unaudited).
27. docs/CONTRIBUTING.md "Eval-Time Guards" inventory: add the (future) #7 guard once it exists.
28. Update the AGENTS.md crush-hot-db paragraph after live deploy with verification RESULTS (it currently promises post-deploy verification; record that it happened and what it showed).
29. Compact TODO_LIST item 19 (500+ words) into the item + a pointer to docs/services/crush.md — docs-health HARVEST/ANNOTATE territory.
30. Verify `~/projects/.crush` (projects-root) migration is actually desired (it is the crush DEFAULT project dir — moving it means the default session DB also lands on /mnt/hot; confirm that's the intent, it looks right, but nobody wrote it down).

**ROADMAP-grade / adjacent:**
31. `services.hot-db` Phase-2 module (TODO item 20): the real mechanism; fold crush in, retire this interim module (avoid two mechanisms — documented INTERIM trigger).
32. Offsite backup leg (Hetzner StorageBox + BorgBackup) — decided 2026-09-11, not deployed (untouched by this task; noticed in TODO_LIST).
33. The owed reboot (flm corpse pinning :52626, llama.cpp regression containment) — crush-hot-db's IO win is measurable only on a clean post-reboot baseline.
34. Pin/fix the llama-cpp gfx1150 mid-load spin (RAG dark) — separate P1, untouched here.
35. `/data` corruption repair (btrbk-data full re-send blocker, TODO P0) — untouched here.
36. Tracker: deploy-restart-audit's `allowUnits` currently documents upstream-nixpkgs plumbing — keep the new #7 exemption list separate and justified.
37. Consider moving INTERIM URL rev pins (signoz etc.) to lock-only as their upstreams fix vendorHashes — shrinks the gitleaks surface (#10) at the root.
38. Add the `sourcegraph-access-token`/bare-hex lesson to the gitleaks-coverage-selftest fixture set if the allowlist approach is chosen (prove the allowlist regex matches pins but not real `sgp_` keys).
39. Consider wiring the queue-ID footer check into a flake check (fail commits/PRs whose task-queue work lacks the cross-reference) — mechanical, low priority.
40. Evaluate `journalctl` noise from `Using saved setting` nix warnings in deploy logs — cosmetic.
41. Review whether OTHER recently-added enabled-at-boot oneshots (from the 2026-09-15 wave) have the same no-onFailure profile as this one.
42. Keep the `services.crush-hot-db` options minimal — if Phase-2 lands with `crush.projects`, delete `projectsDir`/`mountPoint` duplication rather than aliasing.
43. Consider documenting in AGENTS.md that the daemon's heuristic commits sweep race-y staged files within seconds under parallel sessions — the mapping-commit fallback is now used twice; write the pattern down as doctrine with both SHAs.
44. Add `crush-hot-db` to the architecture tree diagram in AGENTS.md's module map if it survives Phase-2.
45. Verify `/mnt/hot` free-space monitoring exists (migrations COPY-ish: `mv` across filesystems = copy+delete; a full Samsung mid-migration would fail the `mv` — currently only a logged failure, see #19).
46. Confirm `mv` across filesystems preserves the project dir's ownership/permissions (it does for the user-owned dirs, but the root-run unit's `chown` of `$dest` covers only the parent — spot-check one migrated project post-deploy).
47. Re-check the 10-min freshness skip reads through the SYMLINK after migration (it does — `-d` follows symlinks — but one live confirmation post-deploy is cheap).
48. Prune the stale intermediate REVIEW FIX phrasing history concern: no action needed (immutable history), listed only to close b)4 explicitly.
49. If #20 (upstream redirect) pans out, upstream the finding to crush and use the verify-before-filing gate first.
50. Close the whole task queue item: after 1-5, append the completion evidence (deploy generation, PSI numbers, symlink count) to docs/services/crush.md and mark the queue item done.

## g) Questions I cannot figure out myself

1. **Samsung-absent boot posture:** with the unit now `wantedBy = multi-user.target`, a boot without the Samsung leaves it FAILED every boot (no `onFailure`, not in system-health). Do you want it LOUD (onFailure + monitoredServices registration — my recommendation, tq-storage-dir precedent) or SKIP-CLEANLY (`ConditionPathIsMountPoint`, atticd-bootstrap precedent)?
2. **The repo-wide gitleaks red (d)2):** shall I add the narrow `[[allowlists]]` for bare 40-hex `github:` URL pins in `flake.nix` (unblocks all verified commits), or do you prefer retiring the URL pins themselves (move to lock-only) — and if the allowlist: does it need your sign-off since it loosens the primary secret gate?
3. **Deploy authorization:** the fix + the whole crush-hot-db mechanism deploy is gated on the /nix soak (~2026-09-17) and sudo. Can I get explicit go/no-go for running `nix run .#deploy` in that window (with the pre-deploy pressure gate as usual), or is that still owner-run only?

---

**Artifacts:** module fix `e9beae89` + `6c85ee1b` (daemon-swept), docs `e9beae89`/`ce4cf783`, footer cross-reference `d1e84088` (this task's queue ID). Gates at finish: `nix flake check --no-build` exit 0, `nix fmt --ci` 0 changed, tree clean, nothing pushed.
