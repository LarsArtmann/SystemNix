# Status Report — Review-Finding Closure: Root-Window Doctrine Contradictions

- **Date:** 2026-09-26 01:09 CEST
- **Task (this report):** fix ticket `000001a0da959282715434d3deed08352a8c` — reviewer finding (medium) against the rejected commit `58cb44ae`
- **Finding:** the original task (`000001a0d7ddc4c5db6b3a8da3acdfb9a1d8`, commit `22a4e88f`) restated the doctrine paragraph to 2w sharp but left the doctrine-sweep TABLE rows (`/btrfs-emergency-reserve`, `~/projects`) and the Emergency-reserve T14 caveat on the old figures — an internal contradiction inside one AGENTS.md section
- **Repo state at write:** clean tree at `f18f4ce2`, zero changes from this fix
- **Format note:** user explicitly requested `.md`; the status-report skill's canonical format is HTML — this is a per-user override, not a new default

---

## Direct answers first

### What did I forget?

Nothing inside the finding's named scope — but my first verification pass stopped exactly at that scope (AGENTS.md, docs/todo, docs/services). I initially did NOT check `scripts/` (where the same figure had lived in `migrate-clickhouse-xfs.sh` runtime messages until the sibling sweep fixed it) and did not cross-check the doctrine's retention figures against the LIVE `snapshots.nix`. Both were closed during this report's preparation, and both came back clean — but they should have been part of the disposition pass, not an afterthought during report writing.

### What could I have done better?

1. Run the extended sweep (scripts/ + docs/status) and the live-config cross-check (`grep snapshot_preserve snapshots.nix`) IN the disposition pass. The finding said "verify every root-window echo in AGENTS.md, docs/todo, and docs/services" — compliant, but the spirit is "no contradiction remains," and the spirit needed three more greps I ran only later.
2. State the scope boundary explicitly in the disposition output instead of letting "clean" imply "checked everywhere."
3. The 12-56 sibling report had left an explicit residual ("snapshots.nix numbers cited in the doctrine were cross-checked against AGENTS.md quotes only, not the live file") — I should have grepped the sibling reports for open residuals before declaring the verification complete. I found it by reading them for this report.

### What could I still improve?

Mechanize. My entire manual verification (4 patterns × 3 scopes, then 2 more scopes, then the config cross-check) is exactly what the queued grep-gate (`docs/todo/storage.md:136`) would do mechanically on every commit. Until that gate lands, every figure change in this repo's prose depends on an agent remembering a grep — and this lineage has now proven, three times, that agents don't reliably remember (`22a4e88f` missed 3 echoes; the first sweep found 3 more in scripts/ beyond its named scope; my own first pass under-covered).

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | **Review finding dispositioned as no-edit (this task).** Dangling commit `58cb44ae` verified to still exist (`git cat-file -e` → OK). Anchor text (`\| /btrfs-emergency-reserve (10G) … root ≈ 1-2w …`) confirmed GONE from master. All four patterns (`3d 1w`, `3d+1w`, `1-2w`, `2w sharp`) grepped across the finding's three scopes; every root-window echo reads the reconciled figure. Per the finding's own instruction ("if all echoes are already aligned on master, close this finding with no further edits") — closed with zero edits, zero commit. | Session greps; AGENTS.md:660 (`root @ (3d 1w, min 2d) = 2w sharp`), :670 + :671 (both table rows `root = 2w sharp`), :699 (T14 caveat `root pin window = 2w sharp — calendar-anchored`), :972 (ClickHouse finalize note `pin window 2w sharp`) |
| 2 | **Extended verification beyond the finding's scope** (run during this report): `scripts/` carries NO live `1-2w`/`3d+1w` hits (the three `migrate-clickhouse-xfs.sh` sites were already aligned by the sibling sweep commit `eb844911`, with `bash -n` + shellcheck green); non-archived `docs/status/` hits are only historical dated reports and task-definition quotes (legal). | Session grep, non-archived filter; `docs/status/2026-09-25_12-27_…md` §2 |
| 3 | **Live-config cross-check (closes the sibling report's §98 residual):** all four doctrine figures verified against `platforms/nixos/system/snapshots.nix` — root `@` min 2d + `3d 1w` (lines 218-219), /data min 7d + `14d 4w` (266-267), pool `services/*` min 2d + `7d 4w` (295-296), `hot/forgejo` min 2d + `3d` (328-329). The doctrine is not just self-consistent — it matches the config it claims to describe. | `rg snapshot_preserve snapshots.nix`, this session |
| 4 | **Original task lineage confirmed fully done (verified in passing):** `22a4e88f` restated the doctrine paragraph (2w sharp + calendar-anchor mechanics from btrbk 0.32.7 `schedule()` source); sibling sweep task `000001a0d80b8ba29cede448b76050225c24` (`eb844911`) aligned the 3 AGENTS.md echo sites + 3 script sites. TODO_LIST/storage.md rows 13, 66, 72 carry DONE notes. | Prior task reports `…_11-51`, `…_12-27`, `…_12-56` (same ticket family); storage.md:13/72 |
| 5 | **Repo integrity:** clean tree at `f18f4ce2`; this fix touched nothing; no build/test surface affected (docs-only disposition — nothing to run). This report is committed with the fix ticket's footer, closing the queue↔git cross-reference that a no-edit disposition otherwise leaves dangling. | `git status` (0 entries), `git log -1` |

## b) PARTIALLY DONE

| # | Item | What works | What remains | Effort |
|---|------|-----------|--------------|--------|
| 1 | **The retention-reconciliation epic** (anomaly → doctrine → echoes → prevention) | Root cause source-verified (btrbk calendar-anchored buckets, 2w sharp); doctrine + all echoes aligned across AGENTS.md, docs, scripts; figures cross-checked live | The two structural preventions are still queued: the mechanical grep-gate (storage.md:136) and the authoring-time calendar-anchor comment (storage.md:15) | S each |
| 2 | **DONE-note figure hygiene** | Convention proposed (`docs/todo/pipeline.md:166`) | Applied nowhere; `storage.md:66` and `docs/status/2026-09-25_00-20 §a10` still state "≈1-2w" as landed-history with no supersession marker — accurate history a skimming reader can re-cite (this exact re-citation risk is what pipeline.md:166 exists for) | S |
| 3 | **Archived-report era markers** | Policy question already asked (sibling 12-56 report §79, unanswered) | 5+ archived reports state "3d+1w" as the root window with no era banner; untouched per historical-record doctrine — whether that is absolute or warrants a one-line ANNOTATE banner is an open owner decision | S |
| 4 | **Prune-gaps verdict stability** | The misdiagnosis verdict is recorded (storage.md:14, DONE) and re-verified | A sibling ticket re-verified it three times today (`docs/status/2026-09-26_00-16`, `_00-23`, `_00-50` — "re-verify run 3"), each writing another report; the loop has no terminal state, so later sessions cannot cheaply cite the settled verdict and skip re-derivation | S-M |

## c) NOT STARTED

All deliberate non-work — separate queue items, excluded by the smallest-correct-change contract:

- **Grep-gate for stale root-window restatements** (storage.md:136, `[ready]`) — patterns `root ≈ 1-2w` anywhere live, `3d+1w` in scripts/ + docs/services/ + docs/todo, with negation/DONE-note context rules. The 12-27 report's canonical hit-list is the ready-made fixture set. Priority: highest of this lineage (it is the durable fix for the failure class this whole ticket family exists because of).
- **Calendar-anchor comment in snapshots.nix** (storage.md:15, `[ready]`) — weeks start Sunday (btrbk `preserve_day_of_week` default), weeklies live 14d. The authoring-time fix: the sweep session shipped a wrong parenthetical because the config carried no "why" where it was read.
- **DONE-note era-annotation convention** (pipeline.md:166, `[ready]`) — codify in CONTRIBUTING.md TODO-system section; ratify + apply retroactively to the 2 known sites.
- (Everything else observed and not started belongs in section f.)

## d) TOTALLY FUCKED UP

Nothing in the current tree is broken by this task. Radical honesty about what WAS and what the pattern reveals:

1. **Master carried a self-contradicting doctrine for hours.** `22a4e88f` shipped "2w sharp" in the doctrine paragraph while two table rows and the T14 caveat in the SAME section said "1-2w"/"3d+1w". Severity at the time: medium — this is the doctrine operators consult before deleting multi-hundred-GB trees on a historically-full box; a wrong window means either premature space panic or wrong budgeting. Healed by `eb844911`. Root cause: a numeric figure lives in ≥6 prose surfaces and no mechanical gate checked propagation.
2. **Verification is currently reviewer-powered, not system-powered.** The disposition protocol that closed this finding (verify the echoes, then close) was written INTO the review finding — the original agent's close-out did not run it. Three consecutive sessions in this lineage failed the same "one-grep sweep" discipline. Until the grep-gate lands, prose-figure changes are gated by luck and reviewer attention, both of which demonstrably run out.
3. **Re-verification churn is burning queue budget.** One sibling ticket produced three full status reports today (00-16, 00-23, 00-50, "prune-gaps re-verify run 3") re-confirming an already-recorded verdict (storage.md:14). Each run re-reads the same evidence and writes another timestamped report nobody will diff. The queue has no "settled verdict — cite and skip" mechanism, so conclusions without a terminal registry get re-derived forever. Not data-loss broken, but structurally wasteful and it obscures genuinely new findings under repeated confirmations.

## e) WHAT WE SHOULD IMPROVE

1. **Mechanize figure propagation** — the grep-gate (storage.md:136) with fixtures from the 12-27 canonical hit-list. Impact: eliminates the entire failure class this finding represents. Effort: S-M. This is the single highest-leverage item in the report.
2. **Make the figure-change runbook explicit in CONTRIBUTING.md** — any numeric doctrine change ships with: (a) grep of the figure across all live surfaces, (b) live-config cross-check, (c) a close-out checklist line naming the scopes swept. Today this lives only as lessons in three status reports.
3. **Era-annotate figures in DONE notes and archives** — ratify pipeline.md:166; decide the archive-banner policy (12-56 §79). Cheap, prevents future skimming readers from re-citing dead figures.
4. **Sweep outputs must state their scope boundary** — "clean" without "checked X, Y, Z, explicitly not A" invites false confidence (my own first-pass miss).
5. **Add a settled-verdict registry** — a short file (or TODO_LIST section) listing conclusions verified N times with their canonical report pointer, so later sessions cite instead of re-derive. Would have collapsed today's 3-run re-verify into zero runs.
6. **Keep the review loop** — the finding was precise, verbatim-anchored, carried its own disposition rules and a dangling-commit fallback. It caught exactly what the original close-out missed. This pipeline works; the gap is only that the FIRST pass didn't do what the reviewer had to prescribe.

## f) Top 50 things to get done next

> Brainstorm per the status-report skill: a larger N is ROADMAP fuel; docs-health HARVEST applies routing rigor. Items 1-8 descend directly from this ticket family; 9+ are owner-known open items noticed in passing (AGENTS.md TODO system / docs/todo), not new research.

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | Add the grep-gate (flake check or pre-commit) failing on `root ≈ 1-2w` live + `3d+1w` in scripts/docs/services/docs/todo, fixtures from the 12-27 hit-list (storage.md:136) | High | S-M | Quality |
| 2 | Add the calendar-anchor comment at `snapshots.nix:219` (weeks start Sunday, weeklies live 14d) (storage.md:15) | Medium | S | Docs |
| 3 | Ratify + apply the DONE-note era-annotation convention; annotate storage.md:66 + 00-20 §a10 (pipeline.md:166) | Medium | S | Docs |
| 4 | Decide archived-report era-banner policy and apply if ratified (12-56 §79 open question) | Medium | S | Docs |
| 5 | HARVEST this report's (f) section into TODO_LIST/ROADMAP via docs-health | High | S | Process |
| 6 | Create the settled-verdict registry; seed it with the prune-gaps verdict (storage.md:14) and the 2w-sharp reconciliation | Medium | S-M | Process |
| 7 | Give the prune-gaps re-verify ticket (`…da9592025…`) a terminal state pointing at the registry — stop run 4 | Medium | S | Process |
| 8 | Add "scope boundary + live-config cross-check" to the figure-change runbook in CONTRIBUTING.md | Medium | S | Docs |
| 9 | **OWED REBOOT** — clears the flm zombie pinning :52626 (since 2026-09-07), D-state corpse pile, and accumulated driver state; run `nix run .#pre-reboot-check` first | High | S | Ops |
| 10 | Offsite Borg go-live: owner inputs (StorageBox host/user + `ssh-keyscan -p 23` host-key pin) → flip `services.offsite-borg.enable` (system-796 dormant) | High | S | Ops |
| 11 | Rotate the still-LIVE Context7 key from the 2026-08-18 incident (rotation, not relocation, is the fix) | High | S | Security |
| 12 | Verify `larsartmann.cloud` in Resend (Domains → SPF/DKIM) — completes mail-relay + Pocket-ID delivery go-live | High | S | Ops |
| 13 | hot-db Phase-2 migration waves via `scripts/migrate-hot-db.sh` (module landed, dormant) — structural fix for the crush-DB IO-storm class | High | L | Infra |
| 14 | Fold crush-hot-db into the ratified `services.hot-db` Phase-2 module (interim mechanism, both exist) | Medium | M | Refactor |
| 15 | Decide DiscordSync Turso: upgrade the plan (sync auto-resumes) or permanent local-only (drop check + env) | Medium | S | Decision |
| 16 | Root-cause the llama.cpp mid-load spin upstream of llama.cpp (ROCm runtime / kernel / GPU-state bisect; `nixpkgs-llama-rag` pin retained) | Medium | L | Bug |
| 17 | Re-enable llama-rag only after a ≥10-min soak under the REAL systemd units (unit-context differs from shell direct-run — freeze-5 lesson) | Medium | M | Bug |
| 18 | FastFlowLM v1.0.3 staged go-live (eligible per staged-bump gate; needs live-serve validation + one-time weights re-pull discipline) | Medium | M | Upgrade |
| 19 | buildcache 2-device btrfs merge (decided 2026-09-22, awaiting maintenance window; module flip + reformat same window) | Medium | L | Infra |
| 20 | `boot.binfmt.preferStaticEmulators = true` — drops `/run/binfmt` from sandbox paths, immunizes builds against the tmpfiles-cycle class | Medium | S | Infra |
| 21 | CV: probe `nix build github:LarsArtmann/CV/master#default.goModules` periodically; lift the branch-ref go1.27 hold when upstream adopts `go_1_27` or drops the floor | Medium | M | Upstream |
| 22 | Wire a groq API key into CV chat config or disable the provider (overall /health sits at `warn` — owner call) | Medium | S | Decision |
| 23 | Architecture-catalog go-live: `setup-forgejo.sh` → first CI run → sops-paste sync token → deploy → first sync | Medium | M | Ops |
| 24 | Google Sync go-live sequence (OAuth production client, `rclone authorize` ×3, sops fill, enable) — user-gated | Medium | M | Feature |
| 25 | Paperless retro-decrypt backfill: deploy once upstream pushes `--backfill --decrypt-repair` + flake bump | Medium | M | Feature |
| 26 | Miniflux: prove one live SSO login, then flip `disableLocalAuth` (the designed go-live gate) | Medium | S | Ops |
| 27 | PMA ecosystem check: verify all `project-discovery-sdk` requirers moved pins forward to the v1.0.0-glob fix (dep-sweep daemon re-breaks this) | Medium | M | Upstream |
| 28 | SigNoz: human `DROP TABLE` decision for the 13 permanently read-only zombie log tables (~10G reclaim) | Low | S | Cleanup |
| 29 | Delete or re-mirror the 2 transferred Forgejo mirrors (DarkBlocks, DialogesWebInterface — frozen + failing against redirects) | Low | S | Decision |
| 30 | Remove stale `commit-graph.lock` on 2 forgejo mirrors (golangci-lint, DynamicMinecraftNetwork; needs the forgejo user) | Low | S | Cleanup |
| 31 | GeoMetrikks: paste MaxMind/CARTO keys to exit geo-degraded mode (user-gated) | Low | S | Ops |
| 32 | Monitor365 re-enable decision (private wireguard-collector crate: publish / public / vendor) | Low | S | Decision |
| 33 | Browser-history: consume the importUsers registration-gate once the cqrs-htmx tag + bump chain lands | Low | M | Upgrade |
| 34 | Hermes: decide whether hermes-agent's forgejo write grant needs branch-protection rules | Low | S | Decision |
| 35 | Health-dashboard: expose federation cadence/timeout as module options (parked in TODO_LIST) | Low | S | Feature |
| 36 | Old paperless SQLite export (`/mnt/pool/services/paperless/export`): recover via document_importer or delete — user decision pending | Low | S | Decision |
| 37 | Measure `/var/lib/forgejo` size post mass-migration (needs forgejo user) to project btrbk root-snapshot growth | Low | S | Verification |
| 38 | Immich inbound-mail decision (UI-configured mail account/rules — needs a mailbox decision, nothing to deploy) | Low | S | Decision |
| 39 | dnsblockd `/health` cache behavior live-probe items (docs/todo/services.md — post-deploy confirmation pending) | Low | S | Verification |
| 40 | History-purge push decision (held; rotation is the real fix — reconsider only after items 11/12) | Medium | S | Decision |
| 41 | Root `@` retention retune decision — now that the 2w-sharp window is understood, is `3d 1w` the intended final config? (see question g1) | Medium | S | Decision |
| 42 | Grep-gate sibling: extend the same mechanical-gate pattern to OTHER recurring-figure classes (port numbers already gated; pin windows next; consider VRAM/zram figures) | Low | M | Quality |
| 43 | Annotate the status-report skill's `.md`-override as a recurring user preference (third consecutive .md report in this family) — or revert to HTML dashboards | Low | S | Process |
| 44 | Deadlink sweep: this lineage's reports cross-reference each other by path; a link check over docs/status/ would catch rename rot | Low | S | Quality |
| 45 | Compact the retention-lineage reports (11-51, 12-27, 12-56, plus today's three) into one summary doc with pointers — the epic now has 6+ reports | Low | S | Docs |
| 46 | Add `snapshot_preserve` values to the grep-gate allowlist as LIVE-config context (they are legal) so the gate needs no per-hit judgment | Low | S | Quality |
| 47 | Verify the `2w sharp` figure survived into the macOS/darwin docs if any retention prose exists there (quick check; likely N/A) | Low | S | Verification |
| 48 | Consider `btrbk` upstream note: calendar-anchored `schedule()` semantics surprised three operators here — a doc PR would fit the verify-before-filing skill | Low | M | Upstream |
| 49 | Queue-review: check whether sibling tickets in the `…da9592…` wave have unexecuted dispositions (this one closed no-edit; siblings may too) | Low | S | Process |
| 50 | After item 1 lands, delete the manual loan entries / one-grep-sweep lessons from the three lineage reports via docs-health ANNOTATE (superseded-by-gate) | Low | S | Docs |

## g) Questions I cannot figure out myself

1. **Is the root `@` retention config itself final?** The reconciliation restated the UNDERSTANDING (2w sharp, calendar-anchored) but not the CONFIG (`snapshot_preserve 3d 1w` + min 2d). Every deleted NVMe byte is now known to pin for 14 days sharp. Is that the intended tradeoff (maximum rollback depth, space recovery is slow) — or should retention tighten now that the semantics are understood? I cannot make the space-vs-recovery-depth call for a historically-full box.
2. **Where should the grep-gate live, and how should historical text be treated?** pre-commit (fast, staged-only) vs flake check (whole-tree, catches parallel-session drift) changes the false-positive policy; and should DONE-notes/archived reports be pattern-allowlisted or context-ruled? The queue item says "flake check or pre-commit" — the choice is a repo-gate policy call that shapes every future figure change.
3. **Does the task queue accept a no-edit finding closure signaled only by a report commit footer?** My disposition required zero changes (the finding's own instruction), so no work commit exists — the fix ticket's only git trace is this report commit. If the queue's derivation expects a work commit per fix ticket, tell me what a compliant no-op closure looks like and I'll encode it for future no-edit findings.

---

*Task-Queue-ID footer carried by the commit of this file: `000001a0da959282715434d3deed08352a8c`. Never pushed.*
