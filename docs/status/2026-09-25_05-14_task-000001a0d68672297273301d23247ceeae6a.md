# Task Report: task-000001a0d68672297273301d23247ceeae6a

| Field | Value |
| ----- | ----- |
| Task  | Codify the single-victim /data repair recipe (TODO storage row; source: archived 2026-09-12_03-54 §3/§6) |
| Date  | 2026-09-25 05:14 |
| Status | DONE (docs-only) |

## TL;DR

The 3-step /data single-victim repair procedure (distilled from the 2026-09-12 corrupt-GGUF fix and its self-review) is now codified: a new "Single-victim /data repair recipe" section at the end of `docs/services/jan.md` (same-filesystem trash → `dd bs=4k count=1` header re-verify on the TRASHED copy, run unmasked → atomic doc sweep across every live surface in ONE commit), with jan.md's RESOLVED bullet and AGENTS.md's corrupt-GGUF bullet both cross-linking it. TODO_LIST queue row + the docs/todo/storage.md library entry closed per convention.

## Done

1. **jan.md** — new `## Single-victim /data repair recipe` section (3 numbered steps): (1) same-fs `trash` is a rename (zero copy, no NVMe write), `stat`-verified both paths, snapshot-pinning doctrine applies to the trashed copy; (2) `dd if=<trashed> bs=4k count=1` re-verifies the corruption on the TRASHED copy (damaged ⇒ `Input/output error` os error 5 + `0+0 records in`; healthy ⇒ exactly 4096 bytes), run UNMASKED (the `| tail` pipeline-masking trap); (3) atomic doc sweep — `grep -rn "<filename>" --include='*.md'`, update every live surface (TODO row, service runbook, AGENTS.md) in ONE commit; `docs/status/**` archives never edited; re-download preconditions: recorded source URL + expected size + pre-import GGUF header validation.
2. **AGENTS.md** — the Jan section's "Corrupt GGUF RESOLVED" bullet extended with a pointer to the recipe plus the one-line why (the 2026-09-12 fix skipped AGENTS.md and it went stale within hours).
3. **docs/todo/storage.md** — row marked `[x]` DONE with the full note (queue/library no-drift rule honored: both sides edited).
4. **TODO_LIST.md** — queue row marked `[x]` with the short DONE note.

## Verification

- **Pre-change baseline at clean HEAD** (`4f3f552f`): `nix flake check --no-build` FAILS with the documented pre-existing `/run/binfmt` shape (`error: getting attributes of path "/run/binfmt": No such file or directory` — the TODO pipeline row's "SECOND instance observed 2026-09-25" class, present at clean HEAD BEFORE any of this change's edits). The pre-commit hook's flake-check leg is dead for ALL agents until that queue item lands; unrelated to this docs-only change.
- **Compensating manual guard runs** (the legs a docs commit actually exercises, run exactly as the hook runs them): gitleaks on the staged tree → no leaks (rc 0); `scripts/check-todo-system.sh` → OK; `scripts/audit-push-protection-literals.sh` → OK; commit identity resolves (LarsArtmann, not "Unknown Author"). The commit therefore carries `--no-verify` ONLY to bypass the proven-pre-existing flake-check leg, per the docs-only precedent (`73145250`, reaffirmed by the 2026-09-25 third-dispatch close-out commit).

## Self-review

- Recipe commands cross-checked against the archived source report's live-verified evidence (the `0+0 records in` tell, the 4096-byte healthy-copy shape, the stat-verified rename semantics) — nothing invented.
- The recipe's own step 3 was exercised while landing it: the grep over `*.md` confirmed jan.md and AGENTS.md are the only LIVE surfaces mentioning the corrupt GGUF (TODO_LIST was already pruned; historical `docs/status/**` reports are frozen archives, deliberately untouched), and both now link the recipe.
- No new follow-up items discovered: the damage-set inventory, the /data scrub, and the trash-purge policy are all already queued in docs/todo/storage.md; nothing this run uncovered belongs in the backlog that isn't there.
