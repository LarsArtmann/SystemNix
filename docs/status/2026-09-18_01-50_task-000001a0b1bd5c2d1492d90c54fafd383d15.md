# Task 000001a0b1bd5c2d1492d90c54fafd383d15 — Status Report

**Date:** 2026-09-18 01:50 CEST
**Work item:** TODO_LIST P1 — "Verify Paperless AI actually USES the llama-rag embeddings end-to-end — UI-saved values override env; check the effective embedding endpoint; wire `PAPERLESS_AI_LLM_RERANKER_*` if supported."
**Work commit:** c029cb07 (footer `Task-Queue-ID: 000001a0b1bd5c2d1492d90c54fafd383d15`)
**Verdict:** BLOCKED (item left unchecked with BLOCKED reason) — but three of the four sub-questions were resolved definitively; only the live end-to-end probe is impossible, for structural reasons.

---

## a) FULLY DONE

1. **Reranker question closed as MOOT (source-verified).** Paperless-ngx **3.1.3** (deployed store path `syxlbi6x`) contains **zero reranker code**: no `PAPERLESS_AI_LLM_RERANKER_*` env var, no `rerank` hits anywhere in `src/`. There is nothing to wire — the llama-rag `:8849` reranker instance can never be consumed by paperless-ai as-is. Verified against the actual deployed package, not documentation.
2. **Embedding env wiring verified correct in the deployed unit.** `/etc/systemd/system/paperless-web.service` carries `PAPERLESS_AI_LLM_EMBEDDING_BACKEND=openai-like`, `EMBEDDING_ENDPOINT=http://127.0.0.1:8848/v1`, `EMBEDDING_MODEL=bge-m3`, `EMBEDDING_API_KEY=llama-server-no-auth` — exactly what the module declares (`modules/nixos/services/paperless.nix`).
3. **DB-over-env precedence confirmed from source.** `paperless/config.py:253-259` reads `app_config.llm_embedding_backend or settings.LLM_EMBEDDING_BACKEND` (same for endpoint) — a UI save in Application Configuration silently overrides every env var, confirming the AGENTS.md doctrine.
4. **Durable findings encoded in the tree** (commit c029cb07): a comment block in `paperless.nix` at the embedding env vars documents precedence + the reranker dead-end, so no future session re-investigates; TODO_LIST item annotated with the full BLOCKED reason and re-verification trigger.
5. **Verification gates run:** `nix flake check --no-build` all-green; pre-commit hooks green (gitleaks, flake check); `nix fmt -- --ci` produced no drift.

## b) PARTIALLY DONE

1. **The end-to-end verification itself** — the core of the item — is **not executable**, for two independent structural reasons:
   - **llama-rag is config-disabled** (`llama-rag.enable = false`, configuration.nix, 2026-09-16 llama.cpp mid-load CPU-spin regression, owner decision). The `:8848` endpoint is intentionally dead; paperless-ai degrades gracefully (RAG off). "Verify paperless actually USES the embeddings" has nothing to verify against.
   - **The agent sandbox forbids `sudo`/`systemctl`**, so the `paperless_appconfig` DB row (the effective, UI-overridable values) cannot be read: `sudo -u postgres psql paperless` → "command is not allowed for security reasons". The one check that would answer "are UI-saved values overriding the env" is precisely the one requiring root.
   Both blockers are documented in the TODO_LIST entry so the item resumes with zero re-diagnosis when llama-rag re-enables (which itself requires the llama-cpp gfx1150 bisect/pin-back — see the separate llama-rag TODO row).

## c) NOT STARTED

1. **No live re-test of the embedding path post llama-rag re-enable** — deliberately deferred; blocked on (b).
2. **No new follow-up items appended to the TODO_LIST** — nothing discovered this run warrants a new row: the reranker finding *removes* work (no wiring task), and the blockers already have their own tracked rows (llama.cpp regression row; sudo needs interactive runs like the 2026-09-18 token-rotation row).

## d) TOTALLY FUCKED UP

Nothing. No broken states left behind. Two process notes (not failures, but worth owning):

1. **The work commit initially landed as a daemon heuristic commit** ("chore: auto-commit 2 changed file(s) (heuristic)") — the auto-commit daemon raced me and batched my two files. I amended it into a properly-worded commit with the queue footer (c029cb07). Per the daemon-race discipline this was safe (verified `git show --stat HEAD` first: exactly my two files, no foreign files absorbed into the amend). But the clean sequence would have been to commit immediately after editing, before the daemon's next sweep.
2. **Edit-tool read-before-edit friction on TODO_LIST.md** — two failed edit attempts before reading the file. Cost: two round trips, zero damage. Trivial, but avoidable.

## e) WHAT WE SHOULD IMPROVE

1. **The task was pre-doomed by its source report (2026-08-20)** — written when llama-rag was live; the 2026-09-16 disable made it untestable, and nobody annotated the TODO row. TODO items depending on a specific service being enabled should be cross-checked against current enable state at pickup (I did this, but the queue could encode it).
2. **Sudo-gated verification steps should be tagged as USER-run at queue-entry time.** The token-rotation row (same session day) carries a proper "BLOCKED: agent sandbox forbids sudo" annotation; this row did not — it cost a discovery cycle. The queue's item-authoring convention should include "requires sudo?" as a first-class field.
3. **Paperless source greps were done against the deployed store path** — correct method, but the store path will GC; findings are now durable in `paperless.nix` comments, which is where they belong. Good outcome; the near-miss was writing findings only into the TODO row.
4. **`harden{}`-style verification env:** the sandbox blocks `sudo` and `systemctl` but allowed reading `/etc/systemd/system/*.service` and the nix store — the exploration path was fine. No improvement needed beyond (2).

## f) NEXT (up to 50, brainstorm-grade — queue/priority gates decide; items 1-3 are the real actionable ones)

1. **Re-run this verification after llama-rag is re-enabled** (llama.cpp gfx1150 spin regression fixed via pin-back to the 20260905 build or upstream bisect): flip `llama-rag.enable = true`, prove `:8848/v1/embeddings` returns a 1024-dim vector, trigger one paperless-ai embedding call, and read `paperless_appconfig` (sudo) for UI overrides. *(This IS the blocked remainder — already tracked in this row.)*
2. **Fix the llama-cpp-rocwmma regression itself** (the separate llama-rag TODO row: pin back or bisect) — this is the gate for #1.
3. **Surfacing reranker demand upstream:** if semantic rerank is actually wanted in paperless, it needs an upstream feature request (paperless-ngx) or a proxy sidecar (a tiny service in front of `:8848` that paperless calls for "embeddings" but which also applies the `:8849` reranker is NOT semantically valid — reranking is a query-time operation). Proper path: file/track upstream interest first. Only if the user actually wants it.
4. When llama-rag returns, re-verify the paperless Gatus checks and post-deploy smoke §"llama" sections go green (they are enable-gated and currently vanish).
5. Consider a docs/services/paperless.md note pointing at the new reranker-moot finding (one line), so the runbook doesn't send anyone wiring reranker env vars.
6. Queue hygiene: sweep TODO_LIST P1 rows for other "verify X live" items that became untestable by config-disables (monitor365 disabled since 2026-08-12 is the obvious sibling class).

*(Items 7-50 would be filler; the honest list is six. The task was narrow by design.)*

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Do you actually want reranking in paperless semantic search?** The `:8849` reranker instance exists solely because llama-rag was specced for it; with paperless 3.1.3 having zero reranker support, the instance serves no consumer. If the answer is "no", the reranker side of llama-rag (and its Gatus/smoke checks) is dead weight to drop at re-enable time — a config simplification. If "yes", the work is upstream (paperless-ngx feature request), not SystemNix.
2. **When llama-rag re-enables, should I read `paperless_appconfig` for UI overrides — and if UI values ARE saved, is the desired end-state env-owned (clear the UI rows) or is a deliberate UI override acceptable?** I cannot decide whether a user-entered UI config is intentional truth or leftover experiment state without you.
3. **Is the sudo-forbidden sandbox a permanent property of unattended queue runs?** If yes, the queue should stop admitting items whose completion requires root (there are now two in the 2026-09-18 window: token rotation and this DB check); if it is per-run policy, a `sudo`-capable run mode would close both blockers mechanically.

---

*Report is point-in-time. Section (f) items 1-2 are already tracked in TODO_LIST; nothing new to HARVEST.*
