# Status Report — Review-Fix Task `000001a0dad1143cdb10aef5d1afc4accbb1` (run 2, post-close-out)

**Date:** 2026-09-26 01:52
**Scope:** this dispatch's no-edit verification close-out plus everything noticed in passing while doing it. No unrelated research was performed; pre-existing items appear only where this run's orbit touched them.

**Lineage (disambiguated — two fix tickets have been interleaving tonight):**

| Ticket | Work | Commits |
| --- | --- | --- |
| `000001a0d80b8ba29…` (original sweep) | verification-only close-out | `323de6cf` (REJECTED by reviewer — closure claims false, FEATURES.md echo missed) |
| `000001a0dad1143…` (THIS ticket) | run 1: the actual FEATURES.md fix + report | `3e3d718a`, `7f724d04` |
| `000001a0dad1143…` (THIS ticket) | run 2 (this dispatch): no-edit re-verification + close-out | `35bd7121` |
| `000001a0da9592827…` (sibling ticket, NOT this one) | AGENTS.md doctrine-contradiction no-edit verdicts | `9c175c70`, `9c338331` |

---

## a) FULLY DONE

| # | Item | Evidence |
| --- | --- | --- |
| 1 | **Finding disposition executed per its own protocol** — rejected commit `323de6cf` verified present (`git cat-file -e` OK); anchor text `space frees only as pinning snapshots expire (3d+1w), not instantly …` determined GONE from the live site before deciding anything | session, `git cat-file -e`; repo grep (0 hits outside `docs/status/` historical reports quoting the finding) |
| 2 | **All three reviewer directions independently re-verified against the current tree** — (1) FEATURES.md reserve row reads `(root pin window = 2w sharp, calendar-anchored)` with the `:289` config restatement untouched as directed; (2) grep-gate row `docs/todo/storage.md:136` scope now includes top-level living docs; (3) six-surface grep (`AGENTS.md`, `TODO_LIST.md`, `FEATURES.md`, `docs/todo/`, `docs/services/`, `scripts/`) shows only legal hits — the `NOT "3d+1w" flat` negation (AGENTS.md:658), live-config restatements, DONE-note history, and the gate row naming its own patterns | fresh greps this dispatch, output reviewed line by line (not cited from run 1) |
| 3 | **Closure claims confirmed true as written** — `TODO_LIST.md:49` and `docs/todo/storage.md:13` both record the review-caught echo with a date ("fixed 2026-09-26"); queue row remains checked | session reads |
| 4 | **No scope creep, no invented changes** — zero source edits this dispatch; the finding's disposition rule ("if the anchor text is also gone … state that instead of inventing a change") was followed exactly; run 1's commits untouched and unamended | `git show --stat 35bd7121` (1 markdown file, 28 insertions) |
| 5 | **Gates run and verdicts recorded** — `scripts/check-todo-system.sh` green; `nix flake check --no-build` red on exactly ONE error headline, live-reproduced as the documented pre-existing hermes VM-test eval failure (`path 'ix80s6aj…-source' is not valid` via `checks.x86_64-linux.hermes`) | session runs; error count `grep -c "^error:"` = 1 |
| 6 | **Close-out committed correctly** — `35bd7121` with exactly one `Task-Queue-ID` footer (this ticket's), `--no-verify` rationale documented in the commit body per the established protocol, working tree clean, never pushed | `git log -1 --format=%B \| grep -c Task-Queue-ID` = 1; `git status --short` empty |

## b) PARTIALLY DONE

| # | Item | Works | Remaining | Blocker | Effort |
| --- | --- | --- | --- | --- | --- |
| 1 | **The finding's fix itself** (run 1's `3e3d718a`) | All three directed changes landed and independently re-verified this dispatch | Reviewer acceptance of the FIX (as opposed to the original task) is the queue's call; my run can only record the verification verdict | external (review cycle) | — |
| 2 | **The durable grep-gate** (`storage.md:136`, `[ready]`) | Row is now well-specified: extended scope + context rules + 7-site fixture list | The gate still does not exist; the failure class it prevents just consumed a SIXTH session round (11-51 → 12-27 → 12-56 → 01-09 → 01-20 fix → this re-dispatch) — a no-op verification alone cost a full agent dispatch and 4 full flake evals | owner decision on placement (flake check vs pre-commit) has been asked twice and ages | S-M |
| 3 | **`3e3d718a`'s commit-message accuracy** (inherited from run 1's own §d-1 admission) | The technical conclusion is independently validated (run 1's claim was cross-checked against `c9c287b9`'s prior documentation) | The message still asserts a "clean HEAD worktree" baseline that run 1 admitted was created AFTER its own edits were committed — a misleading evidence claim inside a shipped commit message | correcting it requires knowing whether `3e3d718a` has reached origin (amend-safe only if unpushed); I skipped `git fetch` (network) this run — push state unresolved | S |
| 4 | **The pre-commit flake leg** | Every non-flake hook leg passed on first attempt this run (gitleaks, whitespace, lint skips) | Leg stays red on the hermes eval failure; `--no-verify` count is now 6 documented instances (7467e2e2, 37869731, e62e64db, c9c287b9, 3e3d718a, 35bd7121) — and the commit carrying THIS report is the 7th | root-cause triage of the `-source is not valid` failure not started (queued, run-1 f-1) | M-L |

## c) NOT STARTED

All pre-existing queue rows in this orbit; none were in scope for a no-edit verification dispatch and none were started. Listed with why they wait:

| # | Item | Where | Why not started |
| --- | --- | --- | --- |
| 1 | Implement the grep-gate with the 7-site fixture set | storage.md:136 `[ready]` | owner decision on placement pending (asked 12-27, re-asked 01-09) |
| 2 | Calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix | TODO_LIST.md:51 / storage.md:15 `[ready]` | queue pacing — authoring-time fix, separate item |
| 3 | Root-cause the hermes VM-test `-source is not valid` eval failure | run-1 report f-1 (NEW, unqueued at harness's discretion) | out of scope here; touches the hermes input's locked source — deploy-risk ordering is an owner call |
| 4 | Docs-only fast-path for the pre-commit flake leg | queued by `02691521` | not implemented; would have made this run's `--no-verify` unnecessary |
| 5 | DONE-note era-annotation convention | docs/todo/pipeline.md:166 `[ready]` | convention work, separate item |
| 6 | Sweep + scope decision for non-archived `docs/planning/` figures | run-1 e-6 | owner decision (gate scope) pending |
| 7 | Remaining storage items (clickhouse shadowed originals, `@home-hermes` snapshot probe, /data corruption repair, mystery snapshot forensics, trash-purge policy) | storage.md:16-18,135-138 | queue pacing / `[blocked:user]` / `[decision]` — untouched, listed only because the doctrine table this ticket family centers on sits beside them |

## d) TOTALLY FUCKED UP

Nothing in the shipped tree is broken — tree clean, both runnable gates green except the documented pre-existing red. The fucked-up entries are about method and systemic friction:

| # | Item | Severity | Detail | Mitigation |
| --- | --- | --- | --- | --- |
| 1 | **I ran the full `nix flake check --no-build` eval 4 times in ~10 minutes; 2 were pure duplicates** | Low (machine idle; no freeze) | The error-counting command invoked the eval TWICE in one bash call (`grep -c` pipeline, then `sort -u` pipeline), plus a standalone run, plus the hook's required run. On a box with a documented IO-PSI-livelock history and a Zone-6 guard, redundant full-flake evals from parallel sessions are exactly the load class the doctrine restricts. Capture the output to a file ONCE and grep the file | discipline item (e-1); no harm materialized |
| 2 | **The `--no-verify` lineage grew again on my watch** | Medium (process erosion) | 6 → 7 documented instances across tonight's doc commits. Each instance is individually justified and documented, but the growing count normalizes the next one, and the class most dependent on the hook (docs — where grep-guards and closure claims live) is exactly the class committing with verification disabled | hermes triage + docs-only fast path (c-3/c-4) are the two unblocks; root-causing the hermes leg is the durable one |
| 3 | **A misleading evidence claim ships inside `3e3d718a`'s commit message** | Low-Medium (trust in the audit trail) | The message claims a "clean HEAD worktree" reproduction that run 1 itself admitted was post-edit. Per the repo's commit-message evidence rule, a verification claim that does not prove what it says is a defect even when the conclusion is independently correct. I inherited this, verified the conclusion independently, but did not correct the message | correction path depends on push state (b-3); the honest record now exists in run-1 §d-1 and this report |
| 4 | **The queue budget is going to verification round-trips, not the durable fix** | Medium (systemic) | Tonight, three separate dispatches across two sibling tickets produced four no-edit/close-out commits (`c9c287b9`, `9c175c70`, `9c338331`, `35bd7121`) while the ONE queued item that would end the echo class permanently (the grep-gate) sits `[ready]` behind an aging owner question. This re-dispatch arrived AFTER run 1 had already committed footer-bearing fix + report commits — the harness either re-verifies deliberately or cannot see its own footers (question g-1) | land the gate (c-1); clarify dispatch semantics (g-1) |

## e) WHAT WE SHOULD IMPROVE

1. **Capture-once-then-grep for expensive eval gates** — my own miss this run (d-1). Any command that will be inspected more than once should redirect to a file first (`nix flake check --no-build 2>&1 | tee /tmp/fc.log`), then grep the file. One eval, N inspections. Worth one sentence in AGENTS.md's heavy-command doctrine.
2. **Report series cross-linking** — this run's close-out cites run 1's commit but not its report path; the sibling tickets' reports likewise form an unlinked chain. A self-contained `Prior reports:` line (paths, one-line verdicts each) would make the lineage navigable without git archaeology. The brutal-self-review skill already prescribes this for review series; status reports should inherit it.
3. **Stop re-growing manual grep lists** — every session in this lineage re-derives the surface list by hand (six surfaces, nine, twelve…). That list is exactly what the queued gate would own. Until it lands, the canonical surface list should live ONCE in `storage.md:136`'s row (it now does) and sessions should cite it, not re-derive it.
4. **`--no-verify` instance counting** (run-1 f-20, carried) — six-plus instances and the count is only visible because sessions hand-document it. A machine-countable marker (grep-able trailer or metric) would make the drift visible without relying on each session's honesty.
5. **Closure-claim lint** (run-1 f-19, carried) — three rounds of false closure claims in this lineage came from unnamed grep scopes. A docs lint requiring "no X remains" sentences to name pattern + file list would have caught the original rejection mechanically.
6. **Status-report format divergence should be flagged, not silent** — this report is Markdown at a task-named path per explicit user instruction, while the status-report skill's canonical output is HTML at a well-named path. The override is honored and flagged here (skill contract); future sessions reading `docs/status/` will see both formats and should not "fix" either.

## f) Top things to get done next (ranked; ≤50 requested — 25 honest items beat 50 padded ones; NEW items flagged, queue pacing owns execution; this section is HARVEST input)

| # | Item | Why now | Impact | Effort | Category | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | Implement the grep-gate (scope/context rules/7-site fixtures per `storage.md:136`) | Six session rounds prove manual sweeps fail; the row is fully specified | Critical | S-M | Quality | queued `[ready]`, owner placement question pending |
| 2 | Root-cause the hermes `-source is not valid` eval failure (`checks.x86_64-linux.hermes`) | Unblocks the pre-commit flake leg for ALL docs work; ends the 7-instance `--no-verify` pattern | High | M-L | Bug | NEW in run-1 f-1, still unqueued |
| 3 | Docs-only fast-path for the pre-commit flake-check leg | Docs commits shouldn't pay for VM-test evals at all; complements #2 as containment | High | S | Quality | queued (`02691521`) |
| 4 | Decide grep-gate placement + whether `docs/planning/` joins scope | Blocks #1's landing shape; asked 12-27 and 01-09 | High | S | Decision (owner) | aging |
| 5 | Calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix | Authoring-time fix for the whole framing episode | High | S | Documentation | queued `[ready]` |
| 6 | Correct or annotate `3e3d718a`'s commit-message baseline claim (after checking push state) | Commit-message evidence rule; the honest record currently lives only in reports | Medium | S | Cleanup | NEW this run (b-3/d-3) |
| 7 | Capture-once-then-grep doctrine sentence in AGENTS.md heavy-command rules | One-sentence prevention of the redundant-eval pattern (d-1) | Medium | S | Documentation | NEW this run |
| 8 | Machine-countable `--no-verify` marker (trailer grep or metric) | Make the 7-instance drift visible without per-session honesty | Medium | S | Quality | carried (run-1 f-20) |
| 9 | Closure-claim docs lint ("no X remains" must name pattern + file list) | Three false closures in this lineage were scope-omission class | Medium | S | Quality | carried (run-1 f-19) |
| 10 | DONE-note era-annotation convention, codified in CONTRIBUTING | Closure notes quoting superseded figures keep re-citing stale numbers | Medium | S | Documentation | queued (pipeline.md:166) |
| 11 | Report-series cross-link convention for `docs/status/` | Lineage navigability; four reports tonight, zero links between them | Low | S | Documentation | NEW this run (e-2) |
| 12 | After the gate lands: re-run the full-lineage grep as its first live validation | Proves the allowlist against reality before trusting it | Medium | S | Quality | follow-on to #1 |
| 13 | Inventory `docs/services/` runbooks for drift-prone figures | jan.md/paperless.md/offsite recipes quote retention numbers; gate won't cover prose runbooks | Medium | M | Documentation | queued (storage.md:137) |
| 14 | Verify no clickhouse-XFS-finalize shadowed originals remain | Migration residual; one-command composable check | Medium | S | Cleanup | queued (storage.md:138) |
| 15 | Investigate `@home-hermes` 09-16 snapshot (9d vs `3d` config) | Only candidate real over-retention in the live inventory | Medium | S | Bug | queued (storage.md:16) |
| 16 | Sweep non-archived `docs/planning/*.md` for stale doctrine figures once | The 09-15 subvolume analysis carries `3d+1w local` twice, outside every proposed gate scope | Low | S | Documentation | carried (run-1 f-13) |
| 17 | Decide era-banner policy for archived reports with misleading figures | 5+ archived reports state `3d+1w` as currently true; asked twice | Medium | S | Decision (owner) | aging (12-27 §79, 01-09 §45) |
| 18 | Decide root `@` retention retune (is 14-day pin window intended?) | The reconciliation restated understanding, not intent; re-runs this whole ticket family if it changes | Medium | S | Decision (owner) | carried (run-1 g-3) |
| 19 | Suppress the `system.stateVersion is not set` eval-warning wall from VM-test fixtures | Dozens of identical warnings per eval drown real signals (noticed in passing on every flake check this run) | Low | S | Quality | NEW this run |
| 20 | Clean the `go-cqrs-lite` flake warning: override for non-existent input `systems` | Pre-existing warning on every flake eval (noticed in passing); fix belongs upstream in go-cqrs-lite's flake | Low | S | Cleanup | NEW this run — route to docs/todo/upstream.md |
| 21 | /data corruption repair | The only backup tier that has NEVER completed | High | L | Bug | `[blocked:user]` (storage.md:17) |
| 22 | Mystery snapshot `data.20260905T2330` forensics | Unexplained snapshot, zero audit trail | Medium | S | Bug | `[blocked:user]` (storage.md:18) |
| 23 | Trash-purge policy for `/data/.Trash-1000` | One owner sentence closes it permanently | Low | S | Decision (owner) | `[decision]` (storage.md:135) |
| 24 | After #1+#2 land: delete the now-redundant manual-sweep prose from the four close-out reports in this lineage (annotate, don't rewrite — docs-health ANNOTATE mode) | Historical reports should point at the gate instead of preserving hand-run grep recipes as method | Low | S | Documentation | NEW this run |
| 25 | Re-examine queue dispatch semantics once g-1 is answered (no-op dispatches should be cheap or absent) | If re-verification is deliberate, codify the expected verdict shape; if not, fix the harness | Medium | M | Quality | pending g-1 |

Items 6, 7, 11, 19, 20, 24, 25 are new observations from THIS run; they are NOT yet appended to the TODO queue (same choice as run 1: flagged for HARVEST rather than self-servicing the backlog mid-report — the harness's pacing gates own appends).

## g) Questions I can NOT figure out myself

1. **Why was this ticket re-dispatched after run 1 had already committed footer-bearing fix + report commits (`3e3d718a`, `7f724d04`)?** I looked for a queue-side detection mechanism (how the harness "derives what you did — commits via the footer") and cannot tell from inside the repo whether re-dispatch is deliberate second-opinion verification (in which case no-edit verdict runs like mine are the intended unit of work) or a harness gap (the queue cannot see its own footers, and every completed ticket will spin extra verification dispatches). The answer decides whether item f-25 is harness work or nothing.
2. **Grep-gate placement and scope (owner, aging since 12-27):** flake check (eval-time, deploy-coupled) or pre-commit (per-commit, currently bypassed via `--no-verify` whenever the hermes leg is red)? And does non-archived `docs/planning/` join the guarded scope? This is the only blocker on the one durable fix for the failure class this entire ticket family exists for.
3. **Ordering/risk call on the hermes eval triage:** root-causing `path 'ix80s6aj…-source' is not valid` likely touches the hermes input's locked source (possibly a lock bump with deploy risk on a freeze-prone box). Should it be sanctioned as the priority unblock now (7 documented `--no-verify` instances and counting), or does the docs-only pre-commit fast-path land first as containment? I cannot weigh the deploy-risk tradeoff from inside this repo.

---

*Point-in-time snapshot. Section (f) is HARVEST input for docs-health; the queue's pacing gates own execution. Format note: Markdown at a task-named path per explicit user instruction (status-report skill's HTML default overridden, flagged in e-6). Never pushed; work commit `35bd7121`, this report commits next with the same `Task-Queue-ID` footer.*
