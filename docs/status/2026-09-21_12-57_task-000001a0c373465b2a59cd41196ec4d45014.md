# Task 000001a0c373465b2a59cd41196ec4d45014 — Paperless PG-level dump: verification re-dispatch report

**Date:** 2026-09-21 12:57 CEST
**Work item:** TODO_LIST.md → storage: "Paperless PG-level dump (or ratify exporter-only RPO) before the postgres hot-db wave"
**This run:** verification re-dispatch. The queue handed me the item, but it was already fully landed by the earlier run of the same queue ID (implementation `9546e7a1` + docs/loop-closure `e9407432` + first status report `77915281`, all footer-stamped). This run therefore verified the delivered work end-to-end, found and queued the one genuine gap, and added new verification evidence the first run did not have. Companion commit this run: `eae85c7b` (footer-stamped, tree clean, pre-commit hooks green).
**Prior report:** `docs/status/2026-09-21_12-29_task-000001a0c373465b2a59cd41196ec4d45014.md` covers the implementation session; this report covers the re-dispatch and does not repeat its detail.

---

## a) FULLY DONE

1. **The item's core ask is landed and verified.** Nightly `pg_dump --format=custom` of the paperless PostgreSQL DB (miniflux-backup pattern): `paperless-db-backup.timer` (02:00 + 10min jitter, Persistent) → `paperless-db-backup.service` (User=postgres, peer auth, 14d retention) + mount-gated `paperless-db-backup-dir` oneshot — all inside `lib.mkIf cfg.enable` (modules/nixos/services/paperless.nix:1009-1082).
2. **Eval-level verification this run:** unit renders with `User = postgres` and `OnCalendar = "*-*-* 02:00:00"` on evo-x2; backup-coordination carries BOTH surfaces (`paperless` exporter manifest + `paperless-db` dump) — the independent-alerting fan-out works; deploy.sh provisioner loop contains `paperless-db-backup-dir` (deploy-restart-audit contract satisfied).
3. **NEW evidence this run — disabled-host shape is clean:** on `rpi3-dns` (paperless disabled) all four artifacts are correctly ABSENT (`systemd.timers ? paperless-db-backup` = false, service = false, dir-unit = false, `services.integration ? paperless-db` = false). No timer/service/registry leak onto hosts that don't run paperless. The first run verified the enabled host only; this closes that hole.
4. **`nix flake check --no-build` green on the current tree** — including the parallel session's `817dc904` (superfile.nix + home-base.nix) that landed between the first run and mine, i.e. my green check covers a tree one commit NEWER than the first run's.
5. **Queue loop closure confirmed correct per house conventions:** queue row pruned (never persists as `[x]`), `docs/todo/storage.md:27` marked `[x] DONE 2026-09-21 (task queue)` with the decision + wiring summary + Source pointer, CHANGELOG entry present, AGENTS.md paperless section carries the one-line bullet.
6. **The one gap this run found is queued, not just reported:** VM-test regression coverage for the new units did not exist (`tests/test-paperless.nix` asserts only the exporter timer). Queued as `[ready]` in TODO_LIST + docs/todo/services.md with a Source pointer, queue/library pair kept in sync per the no-drift rule — committed as `eae85c7b`.

## b) PARTIALLY DONE

7. **Runtime is still undeployed (DEPLOY REQUIRED stands).** Everything is eval/fixture-green; the dir-oneshot convergence, the first actual 02:00 dump, and the backup-coordination freshness flip only become true at the next `nix run .#deploy` plus one timer tick. Neither the first run nor this run deployed — no deploy permission in the contract, and the box is post-freeze-6 (recovery readers were mid-flight earlier today).
8. **My verification was eval-level, not VM-level.** I did not run the existing `tests/test-paperless.nix` (heavy, and the box's IO state argues against unprompted VM runs — freeze-6 doctrine). The flake-check eval gate plus targeted evals plus the disabled-host probe is what this run claims; nothing more.
9. **The dump's DR value is unproven until a restore happens.** A dump file existing is artifact-level, not recovery-level — the `pg_restore` drill (below) is the missing half of the RPO story.

## c) NOT STARTED

10. **Deploy + first-tick proof** (owner/queue-gated): activate the generation, provisioner-restart the dir oneshot, watch the first dump land, confirm backup-coordination `paperless-db` freshness goes green.
11. **Restore drill:** `pg_restore` of the first nightly dump into a scratch cluster, asserting document counts (mirrors the queued twenty/manifest drill; nothing analogous exists for paperless yet).
12. **VM-test step for the db-backup units** — now QUEUED (`[ready]`, docs/todo/services.md), not built.
13. **Post-deploy smoke entry for the dump** (file exists, <26h old) — needs a decision on coverage ownership (post-deploy-check vs backup-coordination) to avoid double-alerting.
14. **Dump-size / pool-space observation** after the first run — custom-format size unknown; projects the RAID1 pool growth and validates the 14d-retention footprint.

## d) TOTALLY FUCKED UP (honest list — this run)

15. **My verification plan had a hole I only closed by accident.** During the run I verified the units on the ENABLED host and called it verified; the disabled-host leak check happened only while drafting this report (when I asked myself "what did the first run not prove?"). It came back clean — but a unit leaking onto every host via module auto-discovery is exactly the auto-discovery trap class in AGENTS.md, and I nearly shipped a verification claim without it. The gap is now evidence (§3), but the process failure is real: enable-gated units need BOTH host shapes checked, by plan, not by report-writing luck.
16. **Two wasted flake evals before the right probe.** My first disabled-host attempt used `.#nixosConfigurations.rpi3-dns.config...OnCalendar or "ABSENT"` as a flake attrpath — invalid (`or` doesn't work there), then `rpi3-dns` attr-shape confusion. AGENTS.md documents the `builtins.getFlake (toString …)` + `--impure --expr` probe pattern as the sanctioned verification shape (used it minutes later, worked first try). Reached for the wrong tool first; cost ~2 minutes, worth recording because the correct pattern is already written down.
17. **Nothing else broke this run.** No edit errors, no formatter footguns, no hook failures, no collateral to foreign files. Given the first run's honest list (eaten edit anchor, tree-wide `--ci` reformat near-miss, out-of-order flake check), this run was deliberately small and stayed clean.

## e) WHAT WE SHOULD IMPROVE

18. **Verification checklists for re-dispatched queue items should be derived from the DONE claim's own evidence list.** The first run's report listed eval-level checks; my run re-ran those and added the missing host-shape probe. A standing two-line checklist ("enabled-host render + disabled-host absence") for any enable-gated unit work would make §15's class impossible to forget.
19. **The redundant-dispatch cost is small but real.** The queue derived this item as open although the prior run had already closed it (dispatch snapshot predates the closure). My run cost ~15 min, most of it verification. Cheap insurance for high-stakes storage work — keep it; but a fast pre-flight ("item already closed → confirm + report, don't re-implement") should stay the explicit contract of a re-dispatch, which this run followed.
20. **The split commit topology from the first run persists in history:** code sits in heuristic-message `9546e7a1`, docs in footer commit `e9407432`. Nothing to do (no history rewrite), but queue tooling scanning only footer commits sees docs-without-code. The `git reset --soft`-fold carve-out question from the first report's §g.3 is still open.
21. **Backup-coordination's two-surfaces-per-service shape** still requires a synthetic second registry entry (`paperless-db` with `vHost.layer = "none"`); a native `backups = [ … ]` list option would remove the synthetic entry. Design change, low urgency, noted by the first run too.

## f) Next things (curated from THIS task's horizon; queued items marked)

Paperless-adjacent, highest value first:
1. Deploy the generation carrying the dump; verify dir-oneshot convergence + first dump + backup-coordination `paperless-db` green (DEPLOY REQUIRED follow-through).
2. `pg_restore` drill: restore the first dump into a scratch cluster, assert document counts; add the result to docs/services/paperless.md as the DR proof.
3. VM-test regression step for `paperless-db-backup.timer`/`.service` + the `paperless-db` registry entry in `tests/test-paperless.nix` — **QUEUED this run** (`eae85c7b`).
4. Decide smoke-coverage ownership for dump freshness (post-deploy-check entry vs backup-coordination) — one owner only.
5. Record dump size + pool-space projection after the first nightly run; validate the 14d retention footprint.
6. Dump-integrity hardening: a `pg_restore --list` sanity gate inside the dump unit (or a checksum sidecar) — exit-0 alone is trust-based.
7. Extend the postgres hot-db wave runbook to name the paperless dump as the DR path once the cluster leaves `@` snapshot coverage (cross-link the 2026-09-21 §e finding).
8. Audit ALL `createLocally` PG consumers vs pg_dump jobs (twenty/manifest drill item already queued; make it a one-shot enumeration so no other DB silently lacks a dump — browser-history is the next identical gap shape, already queued in the storage domain).
9. `paperless-exporter` `RandomizedDelaySec` (open inside "Pool + disk-domain quality" (8)) — now interacts with the 02:00 slot; close together.

House/tooling, noticed in passing on THIS task's path:
10. Fold-allowance (`git reset --soft`) for daemon heuristic commits into footer commits — first report's §g.3, still open.
11. Generalize the enabled/disabled host-shape probe (§3) into a small flake check or module assertion for enable-gated unit work.
12. Consider a treefmt ignore entry for the two never-format multi-MB HTML bundles so agents stop nearly-destroying them on formatter passes (first report §14/§f.11 — still unresolved on the tree I verified).
13. The dispatch/verification no-op contract for re-dispatched items (§19) — worth one line in the queue's dispatch docs.

(Stopped at 13: items beyond this are other domains' queue entries I merely saw in passing while reading TODO_LIST — the queue owns them; padding to 50 would be research, not reporting.)

## g) Questions I cannot answer myself

1. **Deploy window:** should the next queue dispatch (or you, manually) run `nix run .#deploy` for this generation now, or does it ride the next multi-feature deploy train? The dump is inert until activation, and the box is post-freeze-6 — if the wait is long, the "DEPLOY REQUIRED" half of this task stays open past its own due date.
2. **Restore-drill form:** one-time manual `pg_restore` drill (runbook entry in docs/services/paperless.md, needs sudo for a scratch PG) vs an automated periodic check (VM-test-based restore assertion)? The first proves RPO once; the second keeps proving it.
3. **Retention/RPO target:** is 14d the intended window for the paperless DB dump, or should it match the longer btrbk horizons (pool RAID1 space is cheap; the DB is small next to the document tree)? And to confirm the intended split: originals/media are covered by pool snapshots + btrbk, the dump covers ONLY the catalog — correct?
