# FastFlowLM v1.0.2 Upgrade + OOM-Resilience Overhaul — Session Status

**Date:** 2026-08-22 01:06 CEST
**Scope:** flm metrics/logs inquiry → usage forensics → restart-backoff fix → v1.0.2 upgrade (package + weights + ceilings + timeouts + docs)
**Commits authored this session (via auto-commit daemon):** `e001d418` (ceilings/timeouts), `7b04c34f` (v1.0.2 package + GPU budgets, batched with concurrent session), `4ad52e85` (paperless 480s). Two files still pending daemon commit at report time (`fastflowlm.nix` comment fix, `post-deploy-check.sh` comment fix) — both trivial, `nix flake check --no-build` exit=0 over the current tree.
**Concurrent sessions active** (dns-blocker OIDC + ai-stack OLLAMA_GPU_OVERHEAD work) — batched into shared commits per repo rules; their diffs flagged, untouched.

---

## a) FULLY DONE (verified)

1. **flm-real metrics question — answered with binary evidence.** No Prometheus, no OTel, no `/metrics`. Static analysis of the deployed v1.0.1 ELF: served routes are OpenAI-compat (`/v1/*`) + Ollama-compat (`/api/tags|ps|npu/status|...`); perf strings (`ttft_avg_s`, `decoding_speed_tps`, bench CSV writer) are REPLPL `/verbose`+`/status` and offline `flm run` benchmarks — nothing scrapeable. Confirmed against upstream README + release notes. SystemNix's journal-metric monitoring (system-health → Gatus) is the correct and sufficient design.
2. **Usage forensics (journal, Aug 18–21).** 349 conns (~87/day, steady), ~40 chat completions/day, morning peak 06–08h. 61 lifecycles, **742 GB disk reads** (~22 GB/cold load). Only 21/61 (34%) ended via the designed idle-TTL path; 33 SIGKILL (systemd-oomd slice-PSI kills — `/system.slice` >60%/30s, flm top candidate at 26G peak; 4 kernel global-OOM kills 22:25–22:28 with `oom_score_adj:300` working as designed), 4× SIGABRT `double free or corruption (fasttop)` + 1 SEGV (upstream heap bug at 26G peak), 5× 203/EXEC (old fixed bug). Consumers: PMA go-commit (heaviest), papdashboard enricher; paperless-ai wired, zero observed traffic.
3. **Exponential restart backoff** (`fastflowlm.nix`): `RestartSec=60` + `RestartSteps=5` + `RestartMaxDelaySec=15min`. Root cause measured: flat 60s re-paid the 22.5 GB cold load every 2–3 min under sustained oomd pressure (18:09–18:25: 8 kills, ~180 GB QLC reads/20 min) until start-limit wedged activation. Verified rendered (`nix eval` shows `RestartSteps=5`).
4. **flm v1.0.2 package** (`pkgs/fastflowlm.nix`): version bump + prefetched hash (`sha256-em+KMNs…`), built clean, `flm version` → `FLM v1.0.2`, layout self-check passed, `nix eval` pins `"1.0.2"`.
5. **Weights re-pull executed** (v1.0.2 requires it — new quantizer, hash-incompatible): `flm pull qwen3.6-moe:35b-a3b` with `FLM_MODEL_PATH=/data/ai/models/fastflowlm`. **Discovered: new `model.q4nx` is 21.6 GB, not 13.6** (58% larger — nowhere in release notes, only the download size betrayed it). 21,573 MB downloaded, hash-verified by flm; tokenizer/vision files kept (verified compatible). In-place overwrite, no disk doubling (/data 86%, 146 GB free).
6. **Memory ceilings re-sized** for the 21.6 GB model: `MemoryMax 32G→40G`, `MemoryHigh 26G→32G`. Justification: the OLD 13.6 GB model already peaked 26G; old ceilings would have cgroup-OOM-killed the new load on arrival. Verified rendered.
7. **Timeouts retuned** for ~5-min worst-case cold load: proxy `/dev/tcp` deadline 300s→480s (`fastflowlm-proxy-conn`), deploy smoke `--max-time 240`→`480` (`post-deploy-check.sh`), Paperless AI `PAPERLESS_AI_LLM_REQUEST_TIMEOUT 300`→`480` (300s sat exactly at the new boundary). All syntax-checked/eval-gated.
8. **Docs converged to 21.6 GB reality**: AGENTS.md (package/upgrade narrative, smoke timeout, socket-activation rationale, paperless timeout, crash section: "UPGRADED to v1.0.2, watch for recurrence"), module header + assertion messages, papdashboard module comment. Stale `13.6 GB`/`1-3 min` refs swept from all live configs and scripts; historical `22.5 GB` refs in incident comments intentionally preserved.
9. **Gatus alert text** updated (memory-pressure message now says 40G ceiling).
10. **Every change gated**: `nix flake check --no-build` exit=0 (run 3× across the session), `bash -n` on touched scripts, `nix fmt` clean.

## b) PARTIALLY DONE

1. **PapDashboard enricher LLM timeout** — comment updated to warn that the upstream 300s default is now *marginal* at the 5-min boundary, but the actual value lives in the papdashboard flake's Go code; I did NOT investigate whether the module's env-override surface (`extraEnvironment`-style) can raise it from SystemNix. Enricher failures are best-effort by design, so the blast radius is "first insight after idle unload is dropped", not a crash — but it's a known-open edge.
2. **AGENTS.md "v1.0.2 may fix the double-free"** — honest phrasing landed ("watch for recurrence"), but the release notes say NOTHING about the heap bug. My upgrade partly rests on an UNVERIFIED hope. Recurrence watch = journal grep for SIGABRT, which nobody currently automates.

## c) NOT STARTED

1. **THE DEPLOY.** Everything is committed, nothing is live. `sudo` is blocked in this session.
2. **PMA/go-commit OpenAI client timeout audit** — PMA is the HEAVIEST consumer; I checked paperless + papdashboard timeouts but never verified go-commit's HTTP timeout against a 5-min cold load. If it's ≤120s, every first-request-after-idle silently falls back to heuristic commit messages.
3. **Cold-load economics monitoring** — no metric/alert exists for "cold loads per day" or "GB re-read by flm"; the 742 GB/4-day figure came from one-off forensics.
4. **MemorySwapMax retune** (still 20G) — never re-examined against the 58% larger model.

## d) TOTALLY FUCKED UP (honest ledger)

1. **Sequencing failure on the weights pull — created a live race I then left open.** I pulled the new weights at 23:44 while the v1.0.1 backend was still the deployed binary. The moment that backend stops (idle TTL/crash/oomd) and ANY client reconnects, v1.0.1's manifest hash check sees the new weights as corrupt/missing and **re-downloads the OLD 13.6 GB weights over my pull**, silently undoing the upgrade pre-deploy. Correct order: deploy-ready package FIRST, weights pull LAST (or immediately pre-deploy). Mitigating luck: the backend has kept receiving traffic (01:06 conn logged), so it holds the old mmap and stays up — a running backend can't re-pull. The race fires on the first restart-before-deploy. **This is why the deploy is urgent.**
2. **I noticed the 21.6 GB size only AFTER the pull finished** — the download progress line (`21572.8MB`) revealed it. Had I read the asset manifest (`model_list.json` in the tarball, or the pull's "Missing files" listing) before pulling, the ceiling/timeout retune would have been planned, not discovered mid-flight. Cost: one re-edit round + a concurrent-session edit collision (my `multiedit` failed on a file the daemon had just committed — handled by re-reading, but self-inflicted).
3. **Urgency communicated too late.** The deploy-or-race warning only appeared in my final message, though the race window opened at 23:44.

## e) WHAT WE SHOULD IMPROVE (process/systemic)

1. **Model-pull before deploy-binary exists = footgun.** Rule: never place new-version model files under a path served by an old-version binary that auto-verifies+re-pulls on start. Longer term: the module could pin a `modelVersion` and gate `flm pull` on the deployed package version (a `fastflowlm-weights-pull` oneshot with `ConditionPathExists` on the new store path).
2. **flm cold-load economics are invisible.** A tiny journal-based collector (`system_service_restart_churn`-style) emitting `flm_cold_loads_total` + `flm_disk_read_bytes_total` from the `Consumed … read from disk` lines would have surfaced the 742 GB bomb within a day, not after 4 days of forensics. Same class as the existing restart-churn metric.
3. **Package version ↔ weights manifest coupling is undocumented upstream.** We now know (empirically): v1.0.2 `flm` treats hash-mismatched weights as missing and overwrites. Record: pull logs show exactly which files are re-verified vs re-fetched — read them BEFORE pulling next time.
4. **Deploy smoke pins a 21.6 GB model on EVERY deploy** (documented before, worse now: ~5 min + 22 GB reads per deploy). Consider a `--no-block`/skip flag for routine deploys, or accept and document the cost.
5. **Release-notes ≠ full changelog.** The 13.6→21.6 GB growth appeared nowhere in v1.0.2 notes. For flm upgrades, always check `model_list.json` diff + asset sizes first (I did check the tarball size delta — 34 MB binary — and wrongly concluded "weights unchanged, only re-quantized upstream"; the size lives in the MANIFEST, not the release).

## f) NEXT UP TO 50 (prioritized)

**P0 — today:**
1. `nix run .#deploy` (sudo required — outside this session)
2. Post-deploy: verify journal shows `FLM v1.0.2` banner on backend start
3. Post-deploy: verify smoke pass (`/v1/models` through :52625 ≤480s) — first real 21.6 GB cold load
4. Post-deploy: watch `systemd-cgtop`/`Consumed` line — confirm peak memory vs 40G ceiling; resize if peak >36G or <30G
5. Verify the daemon committed the 2 pending comment-fix files; if not, they ride the next batch
6. If the race fired first (old weights re-downloaded — check `model.q4nx` mtime/size = 13.6 GB): re-run `flm pull` with the NEW binary post-deploy
**P1 — this week:**
7. Audit PMA/go-commit OpenAI client timeout vs 5-min cold load (heaviest consumer, silent fallback risk)
8. Investigate papdashboard enricher timeout override surface (env var? upstream option? flake PR if needed)
9. Add `flm_cold_loads_total` / disk-read journal collector → system-health textfile + Gatus condition
10. SIGABRT/core-dump watcher for flm (v1.0.2 double-free recurrence detection — `system_service_state_failed` already catches crashes, but not "crashed with SIGABRT specifically")
11. MemorySwapMax=20G review vs 21.6 GB model
12. Delete (`trash`) `/data/ai/models/fastflowlm/models/Embedding-Gemma-300M-NPU2` (~300 MB, orphaned by llama-rag migration)
13. Morning warm-keep window evaluation (peak 06–08h; a timed `systemctl start` pre-warm vs ~87 conn/day cold-load cost) — product decision, see questions
14. Re-baseline usage stats after one week on v1.0.2 (conns/day, cold loads, crash count) — compare against Aug 18–21 baseline
15. Check `model_list.json` v1.0.2 diff for newly available models (qwen3.5 family) — informational
**P2 — backlog candidates:**
16. flm request/response body echo → journald/SigNoz privacy review (prompts are in the logs; LogFilterPatterns while preserving `TCP connection established` for the idle check)
17. Consider `fastflowlm-weights-pull` declarative oneshot (package-version-gated pull, kills the manual runbook step)
18. Proxy `fastflowlm@.service` `MemoryMax=64M` sanity re-check (socat + backlog-queued bytes)
19. idle-check 10-min min-age guard vs 5-min cold load — confirm still safe margin
20. Document the "manifest hash mismatch = silent overwrite" flm behavior in the module header (one line)
21. Consider warmCalendar option for real (module header references it; no such option exists — stale doc to fix or feature to add)
22. Deploy-smoke skip flag for routine deploys (see e.4)
23. Track upstream v1.0.3+ for the heap-bug fix confirmation (then remove the recurrence watch)
24. /data at 86% — routine space review (old weights' CoW extents pinned by 14d+4w snapshots until ~mid-Sept)

## g) QUESTIONS (cannot resolve myself)

1. **Deploy now?** sudo is blocked for me. Given the re-download race is live until the new toplevel is activated (running backend currently shields it), do you want to run `nix run .#deploy` immediately, or should I prepare anything else first (e.g., pre-check that no deploy-hostile units are in restart churn)?
2. **Morning warm-keep:** the 06–08h usage peak justifies ~2h of pinned ~30 GB RAM to save 1–2 cold loads/day. Pin (timer-based pre-warm + longer keepAlive in that window), or keep the strict idle-unload economics? This is a resource-philosophy call, not discoverable from data alone.
3. **Prompt privacy:** flm echoes full LLM request/response bodies into journald → SigNoz. Keep as-is (debuggability wins, it's a homelab), or filter (I'd preserve the connection lines the idle check greps)? Your call on the tradeoff.

---

**Bottom line:** upgrade fully prepared and committed; running system still on v1.0.1 with new weights on disk — deploy is the single step that closes the loop (and the race). All eval gates green.
