# Task 000001a0b74a4b7a56170d0609ddd0184ce3 — Status Report (re-run)

**Date:** 2026-09-19 04:57 CEST
**Work item:** TODO_LIST P1 — "Delete the browser-history probe user `probe-gate@example.com`" (second dispatch; first run 03-49 same day = BLOCKED root-gated)
**Outcome:** EXECUTION MECHANISM NOW EXISTS AND IS VERIFIED — but the live deletion has NOT run yet. A parallel session (NOT this queue ID) landed the declarative purge at 04:32 (`2c2a860c`); this run independently verified it end-to-end and re-confirmed the live state. The item stays unchecked with handoff instructions: the purge fires automatically at the next `browser-history.service` start (deploy.sh restarts the unit on every deploy; reboots and manual restarts also qualify), so no dedicated action is needed.

## a) What the parallel session shipped (attributed honestly — not this run's work)

- `services.browser-history.probeRegistrationCleanup` option + wiring (`modules/nixos/services/browser-history.nix`): a marker-guarded, email-scoped `ExecStartPre` ON the server unit — the DynamicUser service purges its OWN StateDirectory, so the root path that blocked the 03-49 run is eliminated entirely (no sudo, no concurrent-writer window: the server has not opened the DB yet when ExecStartPre runs). `-`-prefixed (non-fatal) and an in-module eval assertion pins the wiring against the split-brain class (option on but ExecStartPre entry lost = phantom no-op).
- `modules/nixos/services/_browser-history-scripts.nix` `probeRegistrationPurge`: `BEGIN IMMEDIATE` transaction, `busy_timeout=5000`, email-scoped DELETE on BOTH surfaces (`events` via `json_extract(CAST(payload AS TEXT),'$.email')` + `users_view` via the `email` column), `changes()` reported to the journal, sha256-suffixed marker stamped on success only, single-quote refusal, missing-DB clean skip.
- Enabled in `configuration.nix` (`email = "probe-gate@example.com"`) with a self-removal note (remove the block once the marker is observed) and the no-probe warning encoded.
- `checks.x86_64-linux.browser-history-probe-purge-fixture` flake check (schema-faithful behavioral fixture).
- This supersedes the 03-49 §d root runbook as the execution path (the runbook remains valid for an immediate manual purge before the next restart).

## b) This run's independent verification (all first-hand, 04:40–04:57)

1. **Schema claims re-derived from source, not trusted:** go-cqrs-lite `storage/sql/dialect_sqlite.go` `events` DDL — `payload BLOB`, so the script's `CAST(payload AS TEXT)` path is LOAD-BEARING (a bare `json_extract(payload,…)` on a BLOB column would silently never match). cqrs-htmx `usermgmt/sql_readmodel.go` — `users_view` via `AutoMapperWithTombstone` with the indexed `email` column. Lock moved to browser-history `10fe5d8a` (local checkout = lock) since the 03-49 report's `0971fe9` — verification done against the tree the next deploy will build.
2. **Functional:** `nix build .#checks.x86_64-linux.browser-history-probe-purge-fixture` → PASS. Covers: both-surface email-scoped delete including a BLOB payload row (`events_deleted=2`, `users_view_deleted=1`), decoy `UserRegistered` + non-registration events survive, marker created on success only, idempotent re-run is a no-op, missing-DB clean skip, corrupt-DB fails WITHOUT stamping, empty argv refused.
3. **Eval:** `nix flake check --no-build` → "all checks passed" (forces the NixOS assertions, including the purge block's own ExecStartPre-wiring assertion reading the FINAL merged unit config).
4. **Live state:** `/health` ok (journal_mode wal, events 135282 — unchanged since 03-49), `/metrics` → `browser_history_user_count 0` (= the pre-probe value; the read model still contains no users). Server boot 2026-09-18 19:31:55 CEST PREDATES the 04:32 mechanism → the purge has NOT executed yet; the `UserRegistered` journal event is still in the DB (invisible to gate + metric).

## c) Explicitly not done (scope discipline)

- **No deploy:** deploys are not an unsupervised queue run's authority on this box (freeze-era deploy discipline; sudo/systemctl are tool-policy-blocked here). The mechanism needs no dedicated action — deploy.sh's unconditional `browser-history.service` restart (deploy.sh ~line 500) fires the ExecStartPre on the next deploy.
- **No fresh registration probe:** count 0 + `MAX_USERS=1` → the pinned gate ACCEPTS (`Count() < maxUsers`); the probe would 201 and manufacture NEW debris. The "fresh probe still 403s" criterion stays deferred-by-safety until the count-gap fix (TODO P1, gate-count gap) lands — unchanged from the 03-49 finding, and encoded as a warning in the configuration.nix block itself.
- **TODO item left unchecked:** the live DB still carries the probe's journal event until the next server start.

## d) Handoff for the next verifier (after ANY browser-history.service restart on evo-x2)

1. `journalctl -u browser-history | grep "purged probe registration"` → expect `purged probe registration for probe-gate@example.com` with `events_deleted=1` (the live DB carries one TEXT-payload event; the fixture's BLOB row is synthetic coverage).
2. `curl -s http://127.0.0.1:8087/metrics | grep browser_history_user_count` → still 0.
3. Flip the TODO item to `[x]` and remove the `probeRegistrationCleanup` block from configuration.nix (self-neutralizing debt, per its own comment). The marker check itself is root-only (`ls /var/lib/browser-history/.probe-registration-purged-*`); the journal line + metric are the agent-verifiable signals.

---

*Point-in-time snapshot, 2026-09-19 04:57 CEST. Parallel-session attribution: auto-commit `2c2a860c` (04:32) + its predecessor commits landed the mechanism; this run's contribution is the independent verification (§b), the live-state re-check, the TODO close-out annotation, and this report.*
