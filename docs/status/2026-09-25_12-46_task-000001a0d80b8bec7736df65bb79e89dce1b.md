# Status Report — Task 000001a0d80b8bec7736df65bb79e89dce1b

**Date:** 2026-09-25 12:46 CEST
**Scope:** Reviewer-finding fix on top of task 000001a0d7c6e16844a893aafd7c9a19a748 (refresh `/data/docker` doctrine-table sizing).
**Verdict:** ✅ COMPLETE — one-line doc fix, committed, all gates green.

> **Format note:** the user's prompt explicitly requested a Markdown file at
> `docs/status/<ts>_task-….md`; that overrides the status-report skill's HTML
> default. One-off override, not propagated into the skill.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | Dangling commit re-anchored: verified `050ec1ee` still exists (`git cat-file -e`), then judged the CURRENT tree per the finding's disposition protocol | shell check, first command of the run |
| 2 | Stale `/data/docker` figure in AGENTS.md Samsung section located via the verbatim anchor (`~20 G of which ~88% is pruneable garbage`), single grep hit | AGENTS.md:1225 (pre-fix) |
| 3 | Prose updated to the 2026-09-25 figures: ~15.5G total, images 9.1G + volumes 4.3G + build cache 2.1G, ~64% reclaimable, `docker system df` provenance, plus a pointer to the Snapshot-pinning doctrine table | AGENTS.md Samsung section |
| 4 | Commit `fad88cbc` with exactly ONE Task-Queue-ID footer (the FIX ticket id, not the parent's) | `git log -1` |
| 5 | Repo gates green: gitleaks, trailing-whitespace, `nix flake check --no-build` (via pre-commit hook; no `.nix` files touched so nix linters correctly skipped) | pre-commit output |
| 6 | Pathspec commit (`-- AGENTS.md`) — no unrelated daemon-staged files swept in | commit stat: 1 file, 1+/1− |

## b) PARTIALLY DONE

| # | Item | Gap |
|---|------|-----|
| 1 | Reviewer's alternative ("or replace the sizing prose with a pointer to the doctrine table row") — I chose the inline-figures variant; a fuller de-duplication (making the doctrine table the ONLY home for the numbers, prose pointing at it) was not done | Deliberate minimalism per "smallest correct change wins", but the same figure now lives in 2 places and can drift again (see (e)) |
| 2 | Cross-sweep for other stale docker-derived figures — I grep'd for the anchor text and `data/docker` and reviewed the hits I saw; a systematic sweep of every dated sizing figure in AGENTS.md was NOT performed (out of scope: "no unrelated changes") | The adjacent `/data` composition figures in the same paragraph still say `(2026-09-06)` |

## c) NOT STARTED

| # | Item | Why |
|---|------|-----|
| 1 | TODO_LIST queue close-out for the PARENT task (`[x]` the storage row) — done in the parent run, not re-touched here; this fix ticket had no queue row of its own | Out of scope for the fix |
| 2 | No new backlog items appended — nothing agent-executable surfaced that isn't already covered below in (f) as suggestions for the owner/harvest | Contract §5: only append discovered follow-ups; I chose to propose rather than enqueue (see question 2) |
| 3 | No automation for docker sizing freshness (a metric/collector that would make manual figure refreshes unnecessary) | Feature work, not this task |

## d) TOTALLY FUCKED UP

**Nothing.** No damage, no broken gates, no secret exposure, no scope creep.
Honest process blemishes (not damage), listed for the self-review record:

- First `edit` call was rejected (`you must read the file before editing`) — I
  had only `grep`'d the file, not `view`'d it. Cost: one wasted round-trip.
  The AGENTS.md read-before-write rule is explicit; grep is not read.
- The `git commit` was slow enough to be backgrounded (`mvdans/sh` harness);
  harmless, but I should have expected the pre-commit flake-check leg to take
  ~30s+ and set expectations instead of letting the harness guess.

## e) WHAT WE SHOULD IMPROVE

1. **The root pattern, not the instance:** this is the SECOND stale docker
   figure in AGENTS.md (the reviewer caught the first survivor). Two hand-synced
   copies of the same `docker system df` snapshot in one file = a standing
   drift generator. Options: (a) keep figures ONLY in the doctrine table and
   make all prose pointer-only; (b) emit a textfile metric (`docker_df_*`)
   so figures are read live, never transcribed. (a) is a 5-min edit, (b) is a
   small collector task — both belong in the backlog, not this run.
2. **Own-status-report honesty gap the reviewer cited:** the parent run's
   report ADMITTED grep found this second hit and queued it anyway. The
   Proactive-Maintenance rule (owner permission 2026-09-06) already forbids
   exactly that. Lesson: the "queue it" reflex must yield to the two-line-fix
   rule for doc staleness inside files already being edited — fix on sight.
3. **Read-before-edit discipline:** use `view` (not grep) on any file the edit
   tool will touch — even for one-line changes.
4. **Figure provenance dating:** the fixed prose now carries `2026-09-25`
   provenance like the table row does; the sibling `/data` composition figures
   (2026-09-06) in the same paragraph have no such guard and will silently age.
   A one-line "(figures as of <date>)" convention for ALL sizing prose in
   AGENTS.md would make staleness greppable.

## f) Up to 50 things we should get done next

*(Brainstorm, not commitments — HARVEST must route rigorously; most are ROADMAP fuel or one-liners for docs-health.)*

**Doc-consistency (directly downstream of this finding):**
1. Decide: figures-only-in-table vs pointer-only prose for `/data/docker` in AGENTS.md (see question 1).
2. Sweep AGENTS.md for every sizing figure with a stale/absent provenance date; add "(figures as of <date>)" convention.
3. Refresh the `/data` composition paragraph (ai/models/Steam/llamacpp, dated 2026-09-06) with a current `du` pass.
4. Grep all of `docs/` for the retired "~20 G / 88% pruneable" docker figure — confirm zero survivors.
5. Add a docs-health VERIFY case: "any `docker system df` figure in AGENTS.md/docs must cite a date ≤ 30 days old."
6. Check whether docs/todo/storage.md carries its own copy of the docker figures that can now go stale vs the table.

**Automation alternatives (so this class of fix stops recurring):**
7. Spike: `docker-df-metrics` textfile collector (images/volumes/buildcache/reclaimable) → Gatus-free, dashboard-only; then prose can say "see metric".
8. If (7) lands, add a monthly reminder (or docs-health VERIFY rule) that AGENTS.md docker figures match the live metric.

**Process:**
9. Encode the "two-line doc fixes never get queued" rule as a checklist item in the tq task-queue template (the parent run violated it knowingly).
10. Consider a lightweight grep-guard (pre-commit) rejecting the literal string `pruneable garbage` with an old date in the same line — cheap tripwire for this exact incident shape.

**Adjacent hygiene noticed in passing (NOT researched, flag-only):**
11. AGENTS.md contains a duplicated paragraph block (the Gatus-pat doctrine text repeats mid-bullet in the "Adding a Service" section) — likely an edit accident; worth a dedup pass.
12. Same section: the CV bullet block shows signs of copy-paste doubling ("Gatus `pat()` uses GLOB" paragraph appears twice).
13. The fleet of manually-dated figures across AGENTS.md (RAM sizes, snapshot windows, counts) has no owner process — ROADMAP: a quarterly "figures day".

*(Items 11–13 were noticed in passing while anchored in the file; not verified beyond the visual duplicate — treat as leads, not findings.)*

## g) Questions I cannot figure out myself

1. **Owner preference:** for sizing figures that live in the doctrine table, should AGENTS.md *prose* keep inline numbers (with dates) or become pointer-only ("see the doctrine table")? The reviewer offered both; pointer-only kills the drift class permanently but loses at-a-glance context in the Samsung section.
2. **Queue routing:** should items 7–8 (docker-df metric collector) be enqueued as agent-executable `[ready]` rows in `docs/todo/monitoring.md` now, or is live-figures automation unwanted and inline-dated prose the permanent stance? (This decides whether 1/5/10 make sense at all.)
3. **Scope of the same-paragraph refresh:** the `/data` composition figures (2026-09-06: ai 291G, models 210G, Steam 106G, llamacpp 92G) sit in the sentence I just edited — is refreshing them in-scope for a follow-up commit under this ticket's lineage, or does it need its own task-queue item (it's more than two lines and needs a fresh `du` pass)?

---
**Commits this run:** `fad88cbc` (fix) + this report commit — both footer `Task-Queue-ID: 000001a0d80b8bec7736df65bb79e89dce1b`. Nothing pushed.
