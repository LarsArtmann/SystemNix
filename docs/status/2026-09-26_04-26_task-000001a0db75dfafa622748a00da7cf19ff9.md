# Task `000001a0db75dfafa622748a00da7cf19ff9` — FULL STATUS + BRUTAL SELF-REVIEW

**Date:** 2026-09-26 04:26 CEST · **Scope:** THIS task only (the `@home-hermes` 09-16 snapshot investigation) + what was noticed in passing. No unrelated research.
**Format note:** the status-report skill's canonical output is a styled HTML dashboard; the dispatch explicitly requested `.md` at a fixed path — honored as the user override, not propagated as a new default.

**Headline:** the investigation is CLOSED with verdict IN-SPEC (different effective retention; the queue item's premise misread the config), and this review pass UPGRADED the verdict from "corroborated by the 2026-09-25 reconciliation" to "independently source-verified" — which surfaced two comment-imprecision bugs in the repo (one fixed on sight, one flagged).

---

## a) FULLY DONE

| # | Item | Evidence |
| - | ---- | -------- |
| a1 | Root-cause verdict delivered: **IN-SPEC — different effective retention.** All three candidate explanations resolved with evidence: parent hold RULED OUT (Sep 25 23:02 send parent = `@home-hermes.20260924T2300`; pool current through 09-25), prune gap RULED OUT (same journal second: `subvolume delete` of BOTH `@.20260921` and `@home-hermes.20260921` — lockstep), different retention CONFIRMED (block sets only pool-side retention; local inherits root instance's `snapshot_preserve = "3d 1w"`). | commit `3ada6e25`; `journalctl -u btrbk-root --since "2026-09-25 22:55"`; snapshots.nix:227 + :251-252 |
| a2 | Misread source-pinned: the item's "`snapshot_preserve 3d`, no weekly in its config" matches the FORGEJO btrbk instance (`snapshot_preserve = "3d"`, snapshots.nix:337), not `@home-hermes` — whose block comment states the inheritance verbatim. | snapshots.nix:246-259 vs :336-337 |
| a3 | 09-16 explained mechanically: previous calendar week's (Sun Sep 13–Sat 19) first-of-week snapshot; the subvol was created 09-15, so its first-ever snapshot became that week's weekly (why it is mid-week, not Sunday). Root `@` carries the symmetric pair (`09-13` + `09-20`). Live sets identical in shape (2 weeklies + dailies 09-22..25, both prefixes). | `ls` of `/mnt/btrfs-root/.snapshots` + `/mnt/pool/backups/root`, 2026-09-26 04:0x |
| a4 | Queue closed on BOTH surfaces, no drift: `TODO_LIST.md:52` marked `[x]` (compact + pointer), `docs/todo/storage.md:16` replaced with the full DONE entry + a new `[watch]` row appended directly below. `check-todo-system.sh` green. | commit `3ada6e25`; script output "OK: TODO queue/library structure clean" |
| a5 | Falsifiable `[watch]` row with dates, predictions, and check commands (re-sharpened this pass — see a6): both `09-22` dailies should die at the 2026-09-26 23:00 run; `@home-hermes.20260916` + `@.20260913` weeklies at the 2026-09-27 23:00 run. | storage.md [watch] row |
| a6 | **Independent source verification (this pass, ~5 min):** read btrbk 0.32.7 `schedule()` in the installed `.btrbk-wrapped` (lines ~4656-4760). Daily rule: keep the FIRST snapshot per `delta_days` group while `delta_days <= N`, day boundaries hour-of-day-corrected ⇒ nightly cadence retains **4 dailies** (run day + 3 back). Weekly rule: first per `delta_weeks` group while `<= N` ⇒ **2 weeklies**. Retention NEVER consults send-parents ⇒ the parent-hold class is dead at source level, not just by evidence. | `.btrbk-wrapped:4656-4760`; predictions re-derived and they match live behavior exactly (kept 09-22, deleted 09-21 at the Sep 25 run) |
| a7 | Two comment-imprecision bugs found and dispositioned: (1) snapshots.nix calendar-anchor comment said `3d` "keeps all snapshots from the last 3 day-buckets" — wrong (first-of-group rule ⇒ 4 dailies); **fixed on sight** under the owner-granted two-line-fix permission (snapshots.nix:219-229). (2) The forgejo instance's "(~9 snaps)" estimate contradicts the same source semantics (daily keeps first-of-day-group only ⇒ ~4 at the 3-slot/day cadence) — flagged, not asserted (live `/mnt/hot/.snapshots` unreadable without sudo). | diff in this commit; report §f5 |
| a8 | Commit discipline: footer commit landed as `3ada6e25` after the auto-commit daemon raced my two-step add→commit (see d1) — daemon commit contents verified (`git show --stat` = exactly my 3 files), then amended forward per house doctrine. All hooks green (gitleaks, flake check eval), tree clean, not pushed. | `git log --grep 000001a0db75…` → `3ada6e25` |

## b) PARTIALLY DONE

| # | Item | What works | What remains | Blocker / effort |
| - | ---- | ---------- | ------------ | ---------------- |
| b1 | Empirical confirmation of the predicted prunes | Predictions are source-derived AND match all live behavior so far (the Sep 25 run kept 09-22 / deleted 09-21 in both prefixes, exactly as the `delta_days <= 3` rule predicts) | Time-gated: tonight's (09-26 23:00) and tomorrow's (09-27 23:00) runs must delete `09-22`×2 then the two weeklies. One `ls` + journal grep after each run | None — pure time gate. S |
| b2 | The verdict's independence | This pass closed the gap: `schedule()` is now verified in-source by THIS session (a6), not inherited from the 2026-09-25 sweep | None for the mechanism itself; the forgejo `(9 snaps)` suspicion (a7.2) remains unverified live (sudo-gated read of `/mnt/hot/.snapshots`) | sudo needed for the live forgejo count. S |
| b3 | Report lifecycle | 04-11 closure report + this 04-26 full report exist; the [watch] row's Source pointer now targets this (fuller) report | After b1 confirms, the [watch] row and both reports are CHANGELOG-prune candidates at the next docs pass | Harvest owns routing. S |

## c) NOT STARTED (deliberately — contract says follow-ups belong to the backlog, and §f feeds HARVEST)

| # | Item | Why not started | Still wanted? |
| - | ---- | --------------- | ------------- |
| c1 | Hermes restore drill — receive one `@home-hermes.*` snapshot into a scratch subvol + diff against live ("untested restore path is not a backup"; suggested in the 2026-09-18 hermes-backup-leg session, never tracked as a queue row as far as this pass saw) | Out of this task's ask; requires root for `btrfs receive` | Yes — queued as §f7 |
| c2 | Explicit `snapshot_preserve` on the `@home-hermes` block (inheritance is correct today but implicit; an explicit line would make the effective policy grep-able) | Owner preference tradeoff (QLC pin window vs policy visibility) — see §g1 | Owner call — §f6 |
| c3 | btrbk `transaction_log` persistence (retention-bounded) so prune/send forensics don't ride journald | Journald sufficed for this investigation; new feature = owner call | Parked — §f8 |
| c4 | Forgejo-instance cross-ref comment ("no weekly here — do not confuse with root's `3d 1w`") | The real fix (correcting the premise + the root-comment imprecision) is landed; a guard comment is marginal | Optional — §f5 covers the load-bearing part (the `~9 snaps` figure) |
| c5 | A snapshot-age lint that auto-detects over-retention vs doctrine | Over-engineering: the [watch]-prediction pattern + source-verified doctrine already catch the class cheaper | Rejected for now |

## d) TOTALLY FUCKED UP (radical honesty — nothing repo-damaging, but real misses)

| # | What | Severity | Root cause | Mitigation |
| - | ---- | -------- | ---------- | ---------- |
| d1 | My first `git commit` exited 1 ("nothing to commit"): I staged in one shell call and committed in a separate one, and the auto-commit daemon swept the staged files in between | None (self-healed) — but the race is DOCUMENTED repo behavior, so it was foreseeable | Two-step add→commit sequence | Recovered per doctrine (verified daemon commit = exactly my files, amend-forward → `3ada6e25`). Fix: stage+commit in ONE command — applied for this report's commit |
| d2 | The 04-11 verdict leaned on the PRIOR session's 2026-09-25 source-verification for its load-bearing mechanism — inherited trust, not independent verification | Medium (process): the verdict was right, but a wrong inherited claim would have propagated | I stopped at "ratified comment + live data corroborates" instead of reading ~100 lines of Perl | CLOSED this pass: `schedule()` re-verified in-source (a6) — which immediately surfaced two comment bugs (a7) the inherited trust had hidden |
| d3 | Off-by-one citation in the landed closure (`snapshots.nix:250-252` for the inheritance sentence; exact lines 251-252) | Trivial | Casual line-number citation without re-grepping after writing | Corrected in this commit (storage.md DONE row) |
| d4 | The on-sight two-line-fix permission was applied at report time, not during the work run — the root-comment imprecision was discoverable then (I had the journal evidence + the live set in hand) | Low (the fix landed 20 min later instead of immediately) | I categorized the comment mismatch as "prior session's ratified work, don't touch" instead of re-deriving the semantics first | Lesson in §e2/e5 |
| d5 | NOT fucked up, stated explicitly so the absence is informative: no config semantics changed (comment-only .nix diff), no tests broken, no daemon pollution beyond d1 (which was absorbed cleanly), queue surfaces drift-free, nothing pushed |

## e) WHAT WE SHOULD IMPROVE

| # | Pattern | Impact | Concrete fix |
| - | ------- | ------ | ------------ |
| e1 | Two-step stage→commit invites the daemon race | One wasted commit cycle per occurrence | Always `git add … && git commit …` in a single bash call |
| e2 | Inherited verification is not verification | A wrong "source-verified" claim from a prior session propagates silently | Re-read the load-bearing source (~5 min here) before citing it as your verdict's foundation |
| e3 | Queue-item premises are claims, not facts | The §f9 premise ("3d, no weekly") was itself the bug and cost a dispatch cycle | Spot-verify a queued item's config claims against the file BEFORE executing; treat premise errors as first-class findings (they were here) |
| e4 | Falsifiable [watch] rows (prediction + date + check command) | Turns "monitor it" into a pass/fail check a later session can execute blindly | Keep the pattern: exact expected state, exact run timestamp, reopen condition |
| e5 | Apply on-sight fixes when the evidence is in hand, not at report time | 20-minute latency + an extra commit | If the owner granted the two-line-fix permission, spend it during the run |
| e6 | When a misread is found, check sibling configs for the SAME misreading | The forgejo `~9 snaps` figure only surfaced because the semantics were re-derived from source | After correcting one comment's model, re-derive (not re-read) adjacent comments from the same source |

## f) TOP NEXT TASKS (HARVEST input — routing column marks what is already tracked; do NOT duplicate)

| # | Task | Impact | Effort | Category | Routing |
| - | ---- | ------ | ------ | -------- | ------- |
| 1 | After the 2026-09-26 23:00 btrbk-root run: confirm BOTH `09-22` dailies pruned locally (`ls /mnt/btrfs-root/.snapshots/`) | High | S | Verification | [watch] storage.md (this commit) |
| 2 | After the 2026-09-27 23:00 run: confirm `@home-hermes.20260916` + `@.20260913` pruned; if yes, prune the [watch] row to CHANGELOG; if no, reopen the doctrine | High | S | Verification | [watch] storage.md |
| 3 | Re-run `btrfs-verify-pool-backups` after 09-27 and confirm the per-prefix hermes freshness gate rides through the weekly's local expiry untouched | Medium | S | Verification | new |
| 4 | Watch the first POOL-side prune of hermes receives (`target_preserve 14d 4w` ⇒ weekly 09-16 should survive ~4w, prune around mid-Oct) — the pool retention has never pruned a hermes weekly yet | Low | S | Watch | new |
| 5 | Fix the forgejo btrbk instance comment "(~9 snaps)": source semantics (first-of-day-group daily rule) imply ~4 snapshots at the 3-slot/day cadence; verify live (`/mnt/hot/.snapshots` needs sudo) then correct the comment | Medium | S | Quality | new |
| 6 | Owner decision then one-liner: explicit `snapshot_preserve` on the `@home-hermes` block vs documented inheritance (see §g1) | Low | S | Decision | new |
| 7 | Hermes restore drill: receive one `@home-hermes.*` into a scratch subvol + diff against live (root needed; untested restore path is not a backup — suggested 2026-09-18, apparently never tracked) | Medium | M | Verification | new (dedup-check 2026-09-23 report §82 at harvest) |
| 8 | btrbk `transaction_log` persistence (retention-bounded) so prune/send forensics survive journal vacuum — owner call, journald sufficed this time | Low | S | Feature | new |
| 9 | Codify "spot-verify config claims in queued items at queueing time" in the AGENTS.md TODO-system section (§e3; this task's premise was the bug) | Low | S | Quality | pipeline.md candidate |
| 10 | Surface re-fire counts in tq verify output — this investigation was referenced as "queued" in ≥9 status reports (2026-09-26 00-16…03-48) before its dispatch | Low | M | Quality | extends tracked dedup-preflight row (pipeline.md:157) |
| 11 | Document in `btrfs-verify-snapshots` (snapshots.nix ~:827-850) that the hermes gate checks the NEWEST snapshot's age only — 11d mid-week weeklies are EXPECTED and must not trip anyone reading the script | Low | S | Docs | new |
| 12 | Add one sentence to the AGENTS.md BTRFS snapshot-pinning doctrine: empirical nightly-stable set = 4 dailies + 2 weeklies = 6 per prefix (first-of-group rules, source-verified) | Low | S | Docs | new |

**Already tracked — noticed while working, listed ONLY for HARVEST dedup (do not re-add):**

| # | Item | Tracked at |
| - | ---- | ---------- |
| 13 | /data corruption repair (P0, blocked:user) | storage.md:17 |
| 14 | Mystery snapshot `data.20260905T2330` forensics (blocked:user) | storage.md:18 |
| 15 | `snapshot_preserve` widening 3d→7d (decision) | storage.md:24 |
| 16 | Crush-DB migration baseline + hot-db/crush-hot-db VM test rebuild | TODO_LIST:62-63 |
| 17 | boot-mirror activation + reboot pending | TODO_LIST:64 |
| 18 | T14 hot-db monitoring metric + Gatus | TODO_LIST:65 |
| 19 | `migrate-hot-db.sh` stub-fixture test | TODO_LIST:66 |
| 20 | Phase-2 plan T-task table status update | TODO_LIST:67 |
| 21 | Borg drill unknown-flags junk-file fix | TODO_LIST:53 |
| 22 | Docs-only pre-commit flake-check skip (would have made d1-class commits cheaper and this run's hooks faster) | TODO_LIST:59 |

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Retention preference for the dedicated subvolumes:** should `@home-hermes` (and `hot/forgejo`) keep INHERITING the root local-retention policy (`3d 1w` ⇒ 4 dailies + 2 weeklies each), or do you want explicit per-subvolume windows (e.g. a weekly for forgejo rollback depth, or tighter hermes)? I verified both options are one-liners and source-verified the exact mechanics — but the QLC pin-window vs rollback-depth tradeoff is an owner preference nothing in the repo states. (§f6 unblocks on your answer.)
2. **Process policy:** should premise-verification of queued items (§e3/§f9) become a codified TODO-system rule ("authoring session greps the claimed config line before queueing"), or is the current ask+source convention acceptable with occasional burned dispatches like this one? This one cost one dispatch cycle + ~25 min; I can codify either stance but cannot decide the repo's process policy.

*(Only 2 of the allowed 3 — the third candidate questions were answerable from source/config and got filed as §f items instead.)*
