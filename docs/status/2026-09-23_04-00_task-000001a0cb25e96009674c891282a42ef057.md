# Task-Queue Status Report — 000001a0cb25e96009674c891282a42ef057

**Date:** 2026-09-23 04:00 CEST
**Work item:** "ClickHouse backup before the next SigNoz upgrade" (TODO_LIST.md, storage section)
**Run type:** Verification-only close-out — the item was ALREADY complete when the run started.

---

## a) FULLY DONE

| Item | Evidence |
| --- | --- |
| Native ClickHouse server-side backup implemented | `modules/nixos/services/signoz.nix` — `clickhouse-db-backup.service` runs `BACKUP ALL EXCEPT DATABASES system, INFORMATION_SCHEMA, information_schema TO File(...)` into timestamped dir-mode runs under `/mnt/pool/backups/clickhouse` |
| 3-run retention | Timestamped run dirs under the backup root; failed runs self-clean server-side (`remove_backup_files_after_failure`) |
| Backup destination legality wired | `<backups><allowed_path>${chBackupDir}</allowed_path>` in clickhouse extraServerConfig (default = NOTHING allowed, BAD_ARGUMENTS); rides clickhouse's existing restartTriggers |
| Pool leaf via mount-gated oneshot | `clickhouse-db-backup-dir` (cv-backup-dir pattern), chown clickhouse |
| Deploy wiring | `clickhouse-db-backup-dir` present in deploy.sh's provisioner restart loop (verified in `scripts/deploy.sh`) |
| Dependency-safety gating | Backup unit: `User=clickhouse`, `RequiresMountsFor` the pool leaf (detached DAS = clean dependency failure, never 226), 30min TimeoutStartSec, `SELECT 1` readiness wait, ioTier.background |
| Freshness monitoring | `.last_success` sentinel → integration registry `backup` row (maxAge 25h) → backup-coordination + aggregate "All Backups Healthy" Gatus check |
| Server can still start pool-less | Server ReadWritePaths carries `-${chBackupDir}` (optional entry) — only the backup unit fails on a detached DAS |
| Queue loop closed | TODO_LIST.md:26 marked `[x]` DONE 2026-09-23; `docs/todo/storage.md:36` carries the full resolution record |
| Verification (this run) | Confirmed all of the above by direct inspection of signoz.nix + deploy.sh at HEAD; working tree clean; eval already verified green at HEAD by the prior session (`nix flake check --no-build` + full evo-x2 toplevel eval) |
| Schedule placement | Timer 03:00 (Persistent, 10min jitter) — inside the 01:00–04:00 dump window, staggered clear of paperless/browser-history/miniflux/cv/forgejo |

## b) PARTIALLY DONE

| Item | Gap |
| --- | --- |
| Backup OPERATIONAL readiness | Code + eval are green, but ZERO backup runs have executed. First run is deploy-gated: until `nix run .#deploy` happens and the 03:00 timer fires (or the unit is started manually), `/mnt/pool/backups/clickhouse` does not exist and the pre-upgrade rollback case is UNCOVERED. Do NOT upgrade SigNoz before a backup dir exists post-deploy. |
| Backup restore drill | A backup that has never been RESTORE-tested is a claim, not a capability. Not done (no restore runbook / drill for the native BACKUP dir-mode format). |
| Restic app-dumps coverage of the new dir | `/mnt/pool/backups/clickhouse` is NOT in `services.restic-app-dumps.paths` (browser-history is the precedent). Deliberate? The pool is same-chassis; the offsite Borg leg would cover it, but Borg excludes were sized before this dir existed. |

## c) NOT STARTED (noticed in passing, NOT this task's scope)

- SigNoz upgrade itself (blocked on the backup existing — see b).
- Post-deploy first-run verification: confirm the first 03:00 run lands a dir + `.last_success` + backup-coordination goes green.
- Nothing else queued by this run (no follow-up items appended — nothing this task surfaced warranted a new queue line beyond b/c above, which are recorded here).

## d) TOTALLY FUCKED UP!

**Nothing.** This run found no broken state, no ghost wiring, no split brains. The task was completed by a prior session (commit `f264a805`, "docs: close ClickHouse backup task — verify wiring, mark queue item done") and the closure was honest: every claim in the storage.md resolution row checked out against the tree at HEAD.

The only "fucked up"-adjacent finding is process-level: **the queue dispatched a work item that was already done.** The queue's harvest passed over the completed item because it had not been marked `[x]` at harvest time (or harvested it before the closure commit). Cost: one wasted agent run; benefit: an independent re-verification pass, which found no drift.

## e) WHAT WE SHOULD IMPROVE!

1. **Queue freshness gate**: harvest should skip `[x]`-marked items atomically with the closure commit (it now does post-hoc, but the race class remains for parallel sessions).
2. **"DONE (code) ≠ DONE (operational)"**: the closure row correctly says "first run deploy-gates", but the item reads as fully closed while the actual protection does not exist until post-deploy + first timer fire. Consider a separate `[ready]` item "verify first ClickHouse backup run post-deploy" so the gap is queue-visible rather than prose-visible.
3. **Restore drills for ALL dump-style backups**: ClickHouse joins pg_dump (twenty/manifest/miniflux/paperless/cv) as dump-format backups never restore-tested (item 13 of the pool-quality task in storage.md covers twenty/manifest; ClickHouse native BACKUP should join that drill family).
4. **This report is Markdown, not HTML** — the status-report skill's canonical format is a styled HTML dashboard; the tasking harness explicitly requested `.md`, so the override is honored here and flagged (not propagated back into the skill).

## f) Up to 50 things we should get done next

Ordered by impact. Most items below Top ~10 are ROADMAP fuel, not commitments.

1. **Deploy** (`nix run .#deploy`) so `clickhouse-db-backup` can take its first run — the entire point of the task.
2. **Verify first backup run post-deploy**: `/mnt/pool/backups/clickhouse` dir exists, `.last_success` fresh, backup-coordination + "All Backups Healthy" green.
3. **Only then upgrade SigNoz** (the pre-upgrade rollback case this task exists for).
4. **ClickHouse native-BACKUP restore drill** (dir-mode → fresh cluster; prove signoz dbs recover).
5. Decide: add `/mnt/pool/backups/clickhouse` to `restic-app-dumps.paths` (or document the exclusion rationale).
6. Re-size the offsite Borg excludes now that a new backup dir class exists (BX11 = 1 TB budget).
7. Verify the "All Backups Healthy" aggregate actually flips red if `.last_success` goes stale (mutation-test the freshness row once).
8. Confirm retention actually prunes to 3 runs (let 4 runs accrue — or run BACKUP 4× manually — and check cleanup).
9. Confirm failed-run self-cleanup works (induce one failure; verify `remove_backup_files_after_failure` leaves no garbled run dir).
10. Post-deploy: check the 03:00 timer's Persistent boot catch-up does not stack with btrbk sends (Zone-6 guard interplay).
11. (storage.md P0) /data EIO inode repair — the `btrbk-data` leg has aborted since 2026-08-20.
12. (storage.md) Pool + disk-domain quality mega-item: `/mnt/hot` scrub family, `commit=300`+`nodiscard` eval assertion, device-constants consolidation, `pool-subvols-ensure`, shadow-dir cleanup, btrbk-root boot catch-up trigger (USER Q), paperless-exporter jitter, DAS runbook per-section exit codes, Discord-delivery proof for "Pool Mounted", docker.md runbook, snapshot→pool delay SLO, pg_dump restore drills for twenty/manifest.
13. (storage.md) Offsite Borg go-live inputs (owner-held: StorageBox host/user + host-key pin).
14. (storage.md) Fold `crush-hot-db` interim mechanism into the ratified `services.hot-db` Phase-2 module (two mechanisms in-tree is the split brain).
15. (storage.md) `buildcache-init` should provision the three 2026-09-22 fallback cache targets (`~/.cache/pnpm`, `~/.local/state/pnpm`, `~/.cargo/registry`) so post-recovery symlinks don't dangle until next deploy.
16. (storage.md) Single-source the FOUR hand-kept displaced-cache reap surfaces (deploy.sh ×2 loops, buildcache-usb-recovery, HM activation block).
17. (storage.md) 2-device btrfs buildcache merge (both SanDisks, `-d single -m raid1`) — needs a maintenance window.
18. (storage.md) Samsung hot-DB wave: postgres hot-db relocation (paperless/immich/twenty/manifest share the cluster).
19. Root-cause the llama-rag mid-load CPU spin (pinned 0.3.0 build spun identically — NOT llama.cpp-version-specific; suspect ROCm runtime/kernel/GPU state). Bisect upstream of llama.cpp before any re-enable.
20. Re-enable llama-rag ONLY after a unit-context soak (`systemd-run` with the exact sandbox, ≥10 min) — shell direct-runs are proven non-evidence.
21. Reboot is OWED: clears the flm :52626 zombie corpse (EADDRINUSE start-limit), the amdxdna-wedged D-state class, and cold page cache. Run `nix run .#pre-reboot-check` first.
22. Stage the FastFlowLM v1.0.3→v1.0.4 go-live per the staged-bump gate (v1.0.3 failed; v1.0.2 held; release notes claim no crash fix — treat as accuracy bump).
23. Wire a groq key for CV's ChatService OR disable the provider (overall `/health` sits at `warn` — owner decision pending since 2026-09-17).
24. CV: unblock the go-modules FOD — upstream must drop the go 1.27.1 floor or adopt go_1_27 (branch-ref governed hold; probe before any lock move).
25. Forgejo: delete-or-re-mirror decision for the 2 transferred org repos (DarkBlocks, DialogesWebInterface) + clean the 2 `commit-graph.lock` files + measure `/var/lib/forgejo` post-mass-migration for btrbk growth projection.
26. Forgejo staged-primary Phase 2/3: first `forgejo-flip` of a disposable repo through the full flip ladder (plan exists, phase 1 shipped inert).
27. browser-history: consume the cqrs-htmx `68a2cd68` importUsers MaxUsers gate chain (tag + browser-history bump) — queued in TODO_LIST backlog.
28. browser-history SQLITE_READONLY doctrine post-mortem is DONE, but confirm the ownership-heal ExecStartPre fires on the next deploy (first run deploy-gates).
29. Paperless: retro-decrypt repair (`--backfill --decrypt-repair`) — upstream code exists, NOT yet deployed (needs push + flake bump).
30. InboxClean: resolve the unresolved `work` account testing-mode-expiry anomaly (alive 14d past grant — either silently re-consented or expiry isn't clean-7d; flip OAuth client to production either way).
31. Mail relay: final go-live — confirm "Verified" in Resend dashboard + non-owner delivery probe (paperless share link / forgejo notification).
32. DNS: dnsblockd post-deploy live-probe items (cache-served `/health` behavior, fast-503 on staleness) still pending from the 2026-09-06 fix.
33. Monitor365 re-enable decision (disabled since 2026-08-12; needs the private wireguard-collector crate published/vendored — owner decision).
34. DiscordSync Turso: upgrade-or-permanent-local-only decision (local-first stance encoded; the red Gatus check is the standing signal).
35. DiscordSync immich key paste (sops `discordsync-immich.yaml` PLACEHOLDER → real key; runbook exists).
36. GeoMetrikks: MaxMind/CARTO key paste (geo-degraded banner mode until then).
37. Health Hub: decide fetch-load posture before adding remote #2 (merge-on-read ≈43k req/remote/day); expose cadence/timeout as module options.
38. Health Hub: re-pin the `federation` dep to go-health v0.3.0 when tagged (parked upstream).
39. Hermes: workspace layout decision DEFER — revisit trigger check.
40. Hermes: P1 corpse-aware restore skip in memory-emergency-guard (flm EADDRINUSE class re-arms via guard restore branch).
41. Extend `ioChurnUnits` with `crush-hot-db-migrate` + `discordsync-db-heal` (freeze-#6 lesson, still pending).
42. PMA: add the upstream CI guard for go-commit `DefaultChainFromEnv` regression class (docs/todo/upstream.md carries the item).
43. SigNoz: DROP the ~10 permanently READ-ONLY zombie log tables (`TABLE_IS_PERMANENTLY_READ_ONLY`, ~10 GiB reclaim, needs human `DROP TABLE`).
44. SigNoz: dnsblockd release tag so tag-pinning consumers get the cached-health + OTLP-scheme fixes (tracked in docs/todo/upstream.md).
45. Root-context repro of the FSETID/sandbox setattr EPERM mechanism (P0, docs/status/2026-09-22_14-35) — unresolved mechanism, affects future bootstrap units.
46. SystemNix: drop the sops-nix buildGo125Module overlay shim once sops-nix > 13616fff lands.
47. SystemNix: drop the playwright overlay shims when nixpkgs repairs playwright (django-polymorphic checkInputs + libmanette).
48. History purge: key rotation remains the real fix (Context7 key possibly still live; push held by owner decision — decision can flip at any time).
49. YubiKey PAM (u2f) for sudo/login — hardware attached, implementable whenever wanted.
50. diskko as declarative disk-layout documentation + autoinstaller ISO (document-only; triggered by the Samsung flip/stuck-boot sagas).

## g) Questions I can NOT figure out myself

1. **Should the pre-SigNoz-upgrade gate be queue-enforced?** The "do NOT upgrade SigNoz before a backup dir exists post-deploy" rule lives only in prose (TODO_LIST + storage.md). Should I append an explicit `[ready]` queue item "verify first ClickHouse backup run + restore drill BEFORE any SigNoz upgrade" so the tq pool owns the gate, or is prose sufficient given upgrades are owner-initiated anyway?
2. **Restic + Borg sizing decision**: should the new `/mnt/pool/backups/clickhouse` dir (≈3.7 GiB/run × 3 runs) ride restic-app-dumps, the offsite Borg job, or neither (pool RAID1 only)? I cannot judge the BX11 1 TB budget tradeoff or whether you consider ClickHouse dumps important enough for a third copy.
3. **Was the duplicate dispatch intentional?** The item was fully closed (commit `f264a805`) before this run started — was this a queue-harvest race to be tolerated, or should I look for a systematic fix in the harvest/closure ordering (and if so, is there a known reason the closure commit didn't update the harvested copy in time)?
