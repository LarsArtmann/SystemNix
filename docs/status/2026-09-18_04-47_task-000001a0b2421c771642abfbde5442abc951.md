# Task Status Report — `000001a0b2421c771642abfbde5442abc951`

**Date:** 2026-09-18 04:47 CEST
**Work item:** "Gate `import_export.go` user-import path (registration lock hole #3)" — TODO_LIST Priority 1 (Service Outages / Security)
**Scope discipline:** upstream fix in `~/projects/cqrs-htmx`, doc loop in SystemNix. No drive-by refactors.

---

## a) FULLY DONE

1. **Verified the fix exists and is correct (upstream).** cqrs-htmx master commit `68a2cd68` ("fix(usermgmt): gate batch user import behind MaxUsers + registration lock") implements exactly what the item asked:
   - `Service.importUsers()` holds `s.registrationMu` for the WHOLE batch (the same shared mutex that serializes `Register()` and OAuth2 auto-provisioning — TOCTOU-safe), gated on `s.maxUsers > 0`.
   - Any user past `MaxUsers` is skipped with a `"<email>: registration is closed"` error entry in `ImportResult.Errors` — mirrors `ErrRegistrationClosed` semantics of the other two paths.
   - Existing-email dedup preserved; the gate comment cites hole #3 by name.
2. **Verified with the real gates, not output text:** `go build` clean, full `usermgmt` test suite green (`ok ... 16.891s`).
3. **TODO_LIST closed the loop** (SystemNix `9ce11b41`): item marked `[x]` with the queue ID, a new unchecked propagation item appended (see b3).
4. **AGENTS.md staleness fixed on sight** (SystemNix `ae32c7dc`): the Browser History bullet no longer claims "`importUsers()` CSV path is NOT yet gated"; it now cites upstream `68a2cd68` and points at the pending tag/bump chain.
5. **Both SystemNix commits carry the `Task-Queue-ID` footer** — pre-commit hooks (gitleaks, flake check) passed on the doc commit.

## b) PARTIALLY DONE

1. **Push state of the upstream fix: NOT pushed.** `git merge-base --is-ancestor 68a2cd68 origin/master` → NOT an ancestor; origin/master tip is `c0ae6224` (daemon commit). The contract forbids pushing, so the fix is unreachable for consumers until a later authorized push. This is the correct boundary but leaves the security hole LIVE in deployed browser-history until the full chain lands.
2. **Verify depth:** I ran the `usermgmt` package suite, not `go test ./...` for the whole cqrs-htmx repo, and did not add a NEW regression test specifically pinning import-under-cap behavior (the commit's existing tests cover the gate; I verified the ones present rather than expanding them — defensible scope call, but noted).
3. **Propagation chain not started (belongs to the backlog item, correctly):** tag cqrs-htmx (v4 sub-modules — `usermgmt/go.mod` must carry the fix), require the tag in browser-history's go.mod, probe browser-history's goModules FOD, bump the SystemNix `browser-history` input, deploy, and verify a JSON/CSV user import over the cap is rejected on the live binary.

## c) NOT STARTED

1. The entire tag → browser-history → flake-bump → deploy → live-verification chain (see b3) — intentionally deferred to the queue per the contract.
2. A live end-to-end probe that the deployed `MAX_USERS=1` binary now refuses a second user via the import path (only meaningful after the chain).
3. Nothing else in scope.

## d) TOTALLY FUCKED UP

**Nothing.** No broken builds, no broken trees (SystemNix working tree clean after commits; cqrs-htmx tree clean). The one near-miss: the first `edit` of AGENTS.md was rejected (file not read first) — caught by the tool guard, corrected immediately. Small process error, zero damage.

Honest caveat rather than "fucked up": a prior session (same queue ID, commit `9ce11b41`) had already done the fix + TODO_LIST loop before this run started. This run re-verified everything independently (push state, tests, docs) instead of trusting the trail — consistent with the "re-verify point-in-time claims" doctrine.

## e) WHAT WE SHOULD IMPROVE

1. **Push-gated security fixes need an explicit, tracked push step.** "Fix exists locally, never pushed" is a silent half-state; the TODO_LIST propagation item carries it, but the push itself should be its own named backlog line so the queue doesn't skip it.
2. **AGENTS.md staleness was detected by grep, not by process.** The docs-health VERIFY pass should cross-check every "NOT yet gated / not yet consumed" claim against upstream git history (cheap: one `git log --grep` per claim).
3. **TODO_LIST items phrased as "~15-min upstream fix" understate the real cost.** The actual remaining chain (tag + consumer go.mod + FOD probe + flake bump + deploy + live verify) is the 80% of the work. Future item text should scope the whole chain.
4. **Commit `9ce11b41` was followed by a daemon commit (`91112791`) with no message body** — the queue cross-references via footers; daemon batches between explicit commits blur attribution. Not fixable from a single session; noted as systemic.

## f) Up to 50 things we should get done next

Ranked by impact; items 1–6 are directly adjacent to this task, the rest are observations-in-passing + standing fleet work (ROADMAP fuel for the extras — do NOT harvest 20+ verbatim into TODO_LIST):

1. **Push cqrs-htmx `68a2cd68` to origin** (owner-authorized; it is the only thing between "hole closed" and "hole closed for real").
2. **Tag cqrs-htmx usermgmt v4 with the fix** (sub-module go.mod carries it).
3. **Require the new tag in browser-history's go.mod**; regenerate/commit `*_templ.go` if needed; probe browser-history's goModules FOD.
4. **Bump SystemNix `browser-history` flake input**; build + `nix flake check`.
5. **Deploy and live-verify** a JSON/CSV user import past `MAX_USERS=1` is rejected ("registration is closed" in `ImportResult.Errors`).
6. **Update docs/services browser-history runbook** if it mentions the import gap.
7. Re-run full `go test ./...` on cqrs-htmx before tagging (only `usermgmt` ran this session).
8. Check whether any OTHER cqrs-htmx consumer besides browser-history exposes import endpoints (grep LarsArtmann repos).
9. Add a dedicated regression test pinning import-under-cap counts (import 3 users at MaxUsers=2 → exactly 2 imported).
10. Extend docs-health VERIFY: script that greps AGENTS.md/TODO_LIST for "NOT yet"/"not yet consumed" claims and checks them against upstream `git log`.
11. Follow up on the AGENTS.md-flagged browser-history hold (2026-09-17 master `f3561fd8` SQLITE_READONLY crash-loop) — it will interact with the bump in item 4: pick a lock rev with BOTH the fix and a working server.
12. Consider whether the import endpoint itself should be admin-gated at the HTTP layer, not only the MaxUsers counter (defense in depth).
13. Audit for sibling registration holes: any other code path creating users that bypasses `registrationMu` (e.g., CLI/admin tools) — a systematic sweep of `NewRegisterUserCmd` call sites.
14. Verify the SystemNix VM test (`tests/test-browser-history.nix`) covers the import gate once the bump lands; add a step if not.
15. After the chain: resolve the 2026-09-18 TODO_LIST "not yet consumed" note and the AGENTS.md pointer (close the loop fully).
    16–50. (Standing fleet backlog, not new — pointers only) the owed evo-x2 reboot (flm corpse holding :52626), the held git-history purge (key-rotation-gated), Hetzner StorageBox + BorgBackup offsite leg, `services.hot-db` Phase-2 ratification (fold crush-hot-db in), the `GOEXPERIMENT=jsonv2` satellite-repo repair list (21 repos), hot-db first-run verification sweep, llama-rag 0.4.0 upstream-fix watch (drop the pinned input when fixed), cv go_1_27 upstream floor watch, sops-nix `buildGo125Module` shim drop, playwright/d2 overlay shim drop, CI `NIX_GITHUB_RO_TOKEN` secret (120+ dark runs), monitor365 re-enable owner decision, Turso plan decision (DiscordSync), groq key wiring or provider disable (CV ChatService warn), `/data` EIO inode repair (btrbk-data red), ClickHouse backup follow-up, DAS cable swap test, per-service btrfs subvolume doctrine rollout, Resend domain SPF verification, Per-service `MAX_USERS` audit for other browser-history consumers, importUsers OpenAPI doc update upstream, changelog entry for `68a2cd68` in cqrs-htmx CHANGELOG, backfill PR for usermgmt property-test coverage of `ImportResult.Errors` ordering, retention-policy check on old status reports, and further ROADMAP ideas. (Each needs its own scoping pass before becoming queue items.)

## g) Questions I can NOT figure out myself

1. **May I push `68a2cd68` to cqrs-htmx origin** (and cut the tag) — or is the push backlog being batched for a specific window? Everything downstream is blocked on this one authorized push.
2. **Should the import endpoint also require admin auth** (HTTP-layer gate) rather than relying solely on `MaxUsers` — and if yes, does browser-history even expose the import route publicly today, or only via the admin dashboard?
3. **Which browser-history lock rev should the eventual bump target** — the currently held proven `0971fe9c` (pre-regression) + cherry-pick chain, or the newest upstream once the SQLITE_READONLY checkpoint bug is fixed? This decides whether items 4/11 are one deploy or two.

---

_Point-in-time snapshot. Section (f) items 1–6 are queue-ready; items 7–15 need scoping; 16+ are ROADMAP fuel — route through docs-health HARVEST with rigor._
