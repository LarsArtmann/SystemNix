# Status: task-queue run 000001a097a2c784be84ac4ec59193fa3fb5 — attic VM check "deterministic RED" traced and closed as FALSIFIED — 2026-09-13 00:19

Task: TODO_LIST P1 row "attic VM check is RED (deterministic) — `test -d /var/lib/atticd/storage` fails at checks.x86_64-linux.attic (drv aqs52r76…)". **Outcome: the premise is FALSIFIED — the check is GREEN on the current tree AND on the exact cited drv when rebuilt verbatim today (fresh local VM executions, substitution disabled). No code change; row closed in TODO_LIST with evidence.**

---

## a) FULLY DONE

1. **Reproduced the row's exact drv from the Sep-4 tree state.** Evaluated the attic check at the tree cited by the row (`git+file:///home/lars/projects/SystemNix?rev=094d383d`, the HEAD when the red was observed 2026-09-04) → `/nix/store/aqs52r76xv8b2r1x3m9j07z2asz1654j-vm-test-run-attic.drv` — byte-identical to the drv named in the TODO row. Evidence: `nix eval …#checks.x86_64-linux.attic.drvPath`.
2. **Decisive experiment: rebuilt the OLD drv locally today — PASSED.** `nix build '/nix/store/aqs52r76….drv^*' --no-link` ran the full attic VM test fresh on this machine (build log shows the drv building, output `/nix/store/wnid5dhy…-vm-test-run-attic` now valid). A VM-test drv output can only exist after the test passes, so this is a genuine green of the very derivation the row calls "deterministic RED".
3. **Current-tree green, verified twice without cache/substitution shortcuts.** (a) `nix build .#checks.x86_64-linux.attic` → EXIT 0 (drv `sbxibi6d…`, output `8l337j29…`). (b) Deleted that output (`nix store delete`), rebuilt with `--option substituters ''` → fresh local VM run, EXIT 0.
4. **CORRECTED after review (2026-09-13, second pass): the earlier "the two drvs are semantically identical / inputs byte-identical" claim was FALSE and is retracted — the falsification rests on the evidence below, NOT on identical inputs.** The top-level env diff (aqs52r76 vs sbxibi6d: only the `out=` path and the `nixos-test-driver-attic` store path inside `buildCommand` differ) does NOT prove machine-config identity, because the machine systems hang behind the INNER test-driver drv (`pazpdhpa…` old vs `y865lzkn…` current), whose old copy's closure is GC'd and therefore uninspectable. The inner drv is PROVABLY different anyway — diffing the surviving `.drv` ATerms shows differing `buildInputs`/`nativeBuildInputs` (`ty` 0.0.75→0.0.77, `nixos-test-driver-1.1`, ruff 0.16.4→0.16.5, `driverConfiguration.json`), a differing `out`, and a differing embedded generated test script. And the inputs moved between red and green regardless: the nixpkgs lock went `34ab99075` (Sep-4 tree, `git show 094d383d:flake.lock`) → `c043004d1` (current; moved 2026-09-05), and `modules/nixos/services/attic.nix` changed 2026-09-06 (`c75c9261`: atticd-metrics collector to the mktemp+CAP_FOWNER pattern — the unit the VM test starts at `tests/test-attic.nix:72`). What did NOT change: `tests/test-attic.nix` itself since `84965dca` (2026-08-24) and attic.nix's storage/bootstrap/atticd units since `fbc60ed5`. Consequence: the current-tree greens (a.3) are CORROBORATION on moved inputs, not identical-inputs reproductions; the ONLY identical-inputs green is the verbatim rebuild of aqs52r76 itself (a.2) — and a red drv that builds green today kills "deterministic" on its own. **PIN-CITATION annotation (review finding, third-pass dispatch `000001a0981cd72c9e4b0fe9df339f7d003a`, 2026-09-13):** this item's superseded first-pass text had quoted the red-era pin as `e5bdc4a` — that is the Aug-16-era pin (AGENTS DAS section), not the red-era one; the reviewer's evidence is independently re-verified here: `git show 094d383d:flake.lock` contains ZERO occurrences of `e5bdc4a` and pins nixpkgs at `34ab99075ac4f7e40cf037eef32cb1c360bb85e9` (walked the string node-key `root` → `nodes[root].inputs.nixpkgs.locked.rev`), matching the corrected §a.4 text above and the TODO_LIST row (correction landed in review-fix commit `9e972a5f`).
5. **Verdict: at-most-a-flake, mislabeled deterministic.** The single observation (2026-09-04, during the cv lock-bump session's full 63-check pre-commit run) came from a day the box was under the documented zram-97%/IO-storm conditions (AGENTS 2026-09-02 era). One red, then a PASS on the verbatim same drv (a.2) plus corroborating current-tree greens on moved inputs (a.4) = a nondeterministic environment, not a deterministic test failure. The red belief then propagated through three later reports (2026-09-06_00-31 §18, 2026-09-06_02-33 §25, 2026-09-12_00-59 §b.2/f.6) WITHOUT any re-run — the exact stale-harvest class the 09-12 paperless closure (commit `8f3a21c7`) documented. This row is the second confirmed instance.
6. **TODO_LIST loop closed per the file's own conventions:** row flipped to `[x]` with an inline resolution note (date, task attribution, evidence chain, the latent-wart observation below). LANDING CORRECTION (re-dispatch run, same queue ID, later 2026-09-13): this run was interrupted before its footer commit — the closure files landed via the auto-commit daemon's heuristic commit `145ad5cf` (00:21, no queue footer), so the original "Evidence: this task's commit" claim was never true in git history. The queue cross-reference footer rides the re-dispatch commit carrying this correction, which ALSO re-executed the decisive experiment fresh: `nix store delete` of the current-tree output + rebuild with `--option substituters ''` → second independent local VM-run green on identical inputs the same day (§d row 7).
7. **Commit-safety gates run in lieu of the hook's full flake check** (see b.2): `nix flake check --no-build` → all checks passed (aarch64-darwin omission warning expected per AGENTS); staged diff is TODO_LIST.md + this status doc only (markdown); verified no `.nix` file reads TODO_LIST.md at eval time (comments only), so the edit cannot affect eval.

## b) PARTIALLY DONE

1. **Root cause of the one-off Sep-4 red: NOT identified.** Reproduction is gone; the guest-side condition (storage dir absent at step 5 while atticd served port 8200) has no plausible in-VM mechanism — nothing in the module, the test, or atticd (`LocalStorage::new` does `create_dir_all`, sourcegraph-verified against zhaofengli/attic) ever deletes or skips creating the storage dir. Most plausible: guest-level transient (9p/IO stall during that day's storm) surfacing at whatever assertion ran next. Effort to chase further: L, value: low (non-reproducible on identical inputs).
2. **Committed with `--no-verify`.** The pre-commit hook runs a FULL `nix flake check` (`.githooks/pre-commit`, no `--no-build`) = builds all VM tests (a multi-boot storm). Followed repo precedent `73145250` + the documented 09-12 mitigation (§b.2): markdown-only commit, fast gates run manually (a.7), reason documented here. The hook's full check is now UNBLOCKED for future code commits — this row was its known-red excuse.

## c) NOT STARTED (follow-up candidates, deliberately not done here — smallest-correct-change contract)

1. **Latent wart observed while tracing (NOT fixed, cosmetic):** `atticd-storage-dir` (`modules/nixos/services/attic.nix:163`) hardcodes `ReadWritePaths = [ "/mnt/pool/services/atticd" ]` instead of deriving it from `cfg.storagePath`. In ANY environment without the pool (i.e., the attic VM test) the unit dies `status=226/NAMESPACE` before its `mkdir` — harmless today because the bootstrap's `ConditionPathIsDirectory` skip masks it and atticd's own `create_dir_all` provides `/var/lib/atticd/storage` (that is WHY the VM test passes despite the 226). Production unaffected (pool mounted ⇒ dir exists; detached DAS ⇒ loud `RequiresMountsFor` failure by design). If the VM test ever needs to exercise the bootstrap's SUCCESS path, derive `ReadWritePaths` from `dirOf cfg.storagePath` first. Candidate rider on the existing "Attic-class follow-ups" row.
2. **No prevention layer for the double-instance stale-harvest class** — the 09-12 report's f.1/f.2 (verify-before-harvest: re-run the cited check before carrying any "red since X" claim) remains unbuilt and is the actual 80/20; this task exists because a TODO row was carried for 9 days citing a drv nobody rebuilt.

Nothing else was inside this item's scope.

## d) Verification evidence (commands + results, this session)

| # | Command | Result |
|---|---------|--------|
| 1 | `nix build .#checks.x86_64-linux.attic --no-link` | EXIT 0, output `8l337j29…` |
| 2 | `nix store delete 8l337j29…` + rebuild `--option substituters ''` | fresh local VM run, EXIT 0 |
| 3 | `nix eval 'git+file:///home/lars/projects/SystemNix?rev=094d383d…'#checks.x86_64-linux.attic.drvPath` | `aqs52r76xv8b2r1x3m9j07z2asz1654j-vm-test-run-attic.drv` (matches the TODO row) |
| 4 | `nix build '/nix/store/aqs52r76….drv^*' --no-link` | PASSED (output `wnid5dhy…` valid) |
| 5 | `nix derivation show` diff aqs52r76 vs sbxibi6d | top-level env: only test-driver path + out path differ; INNER driver drvs provably differ (`pazpdhpa…` vs `y865lzkn…`, old closure GC'd — inputs moved, see §a.4) |
| 6 | `nix flake check --no-build` | all checks passed |
| 7 | `nix store delete 8l337j29…` + `nix build .#checks.x86_64-linux.attic --no-link --option substituters ''` (re-dispatch re-verification, same day) | drv rebuilt from scratch, fresh local VM run, output re-materialized valid; plain `nix build` EXIT 0 — second independent green |
| 8 | `git show 094d383d:flake.lock` grep + `jq '.root as $r \| .nodes[$r].inputs.nixpkgs as $n \| .nodes[$n].locked.rev'` (review-finding re-verification, third-pass dispatch) | 0 occurrences of `e5bdc4a`; nixpkgs rev = `34ab99075ac4f7e40cf037eef32cb1c360bb85e9` — red-era pin confirmed, as now cited in §a.4 and TODO_LIST row 28 |

## g) QUESTIONS (cannot figure out myself)

1. **Should TODO rows citing a specific failing drv/pattern require the harvest pass to attach a fresh re-run (per 09-12 f.1) before the row is allowed into the queue?** Two consecutive phantom rows (paperless PAPERLESS_EMAIL_HOST, this one) both cite stale evidence that a 10-minute re-verification would have killed. I recommend making it a hard dispatcher rule, not a convention.
