# Status Report — Review-Fix Task `000001a0dad1143cdb10aef5d1afc4accbb1`

**When:** 2026-09-26 02:28 (Saturday)
**Scope:** Reviewer finding against the stale root-window echo sweep (fix ticket descended from task `000001a0d80b8ba29cede448b76050225c24`). **This report covers THIS task's work + what was noticed in passing — no unrelated research.**
**Run:** disposition run (4th dispatch of this fix ticket; runs 1–3 preceded it)

---

## Executive Summary

The reviewer finding (medium) is **fully resolved and no longer applies**. The anchor text — `space frees only as pinning snapshots expire (3d+1w), not instantly (T14 caveat 2026-09-11, AGENTS)` — is **gone from every live surface**. It was eliminated by fix commit `3e3d718a` (run 1 of this ticket), which implements all three of the finding's expected directions and carries the exact fix-ticket footer `Task-Queue-ID: 000001a0dad1143cdb10aef5d1afc4accbb1`. Runs 2–3 re-verified with no-edit verdicts; this run independently re-verified the CURRENT tree (re-greps clean, todo-system gate green, tree clean) and issued a **no-change disposition** per the finding's own protocol ("If the anchor text is also gone, the finding no longer applies — state that instead of inventing a change").

The one honest systemic gap that remains: the **grep-gate that would mechanically prevent this entire finding class is still queued, not built** (docs/todo/storage.md:136). Until it lands, the class is protected only by prose discipline — which is exactly what failed in the original sweep.

| Verdict | Count |
| --- | --- |
| Finding points implemented | 3 / 3 |
| Live surfaces still carrying the stale echo | 0 |
| Footer-bearing commits on this ticket | 4 of 9 ticket commits (runs 3–4 commits lack the footer) |
| Status reports written for a one-line docs fix | 4 (incl. this one) |

---

## a) FULLY DONE

| # | Item | Evidence |
| --- | --- | --- |
| 1 | **Finding (1): FEATURES.md:291 parenthetical corrected** — emergency-reserve row now reads `space frees only as pinning snapshots expire (root pin window = 2w sharp, calendar-anchored), not instantly`; FEATURES.md:289 (snapshots row, live-config restatement of `snapshot_preserve 3d 1w`) deliberately untouched exactly as the finding directed | commit `3e3d718a`; live grep this run |
| 2 | **Finding (2): grep-gate row extended** — docs/todo/storage.md:136 now scopes `3d+1w` to `scripts/ + docs/services/ + docs/todo + top-level living docs (FEATURES.md/README/CHANGELOG/ROADMAP)` and records WHY (the 7th echo was missed because both sweeps' file lists omitted top-level living docs) | commit `3e3d718a`; live grep this run |
| 3 | **Finding (3): closure claims made true** — TODO_LIST.md:49 now says "review caught one late echo — FEATURES.md's emergency-reserve parenthetical — fixed 2026-09-26"; docs/todo/storage.md:13 carries the "Review fix 2026-09-26" note. Both amended inside the same footer-bearing fix commit, so the claims became true at the moment they were written | commit `3e3d718a` body + diff |
| 4 | **Re-grep verification over the full finding scope** — AGENTS.md + TODO_LIST.md + FEATURES.md + README/ROADMAP/CHANGELOG + docs/todo + docs/services + scripts + platforms + modules: only legal hits remain (AGENTS.md:658 live-config restatement with the explicit NOT-"3d+1w" negation; FEATURES.md:289 live-config restatement; docs/todo/storage.md:66/72 DONE notes, where :72 documents the correction itself; historical `docs/status/**` records) | this run, independent re-grep |
| 5 | **Anchor-text scan** — 0 hits outside `docs/status/**` historical reports quoting the finding itself (archived-record policy leaves those untouched) | this run |
| 6 | **Dangling-commit disposition executed per protocol** — rejected commit `323de6cf` verified present (`git cat-file -e` OK); anchor judged GONE from live site; no invented change | this run |
| 7 | **Gates green, tree clean** — `scripts/check-todo-system.sh` rc=0; `git status` clean; no Nix surface touched (markdown-only fix, so no build needed) | this run |
| 8 | **Queue cross-reference** — the actual fix landed under the exact fix-ticket footer in `3e3d718a`; this report's commit carries the same footer, giving the no-edit disposition run a footer-bearing terminal commit | git log |

## b) PARTIALLY DONE

| # | Item | State |
| --- | --- | --- |
| 1 | **Systemic protection against the echo class** — the grep-gate is fully SPECIFIED (patterns, scope, allowlist/context rules, and the lesson narrative all live in docs/todo/storage.md:136) but NOT BUILT. The finding's part (2) asked only to extend the row — done — so the finding is closed; the gate itself was never in this ticket's contract. Until it lands, closure claims rest on hand-run greps | queued `[ready]`, unbuilt |
| 2 | **Queue↔git footer lineage for this ticket** — 4 of the ticket's 9 commits carry the fix-ticket footer (`3e3d718a`, `7f724d04`, `35bd7121`, `3ac34774`). Runs 3 and 4 (`9c175c70`, `9c338331`, `563266db`, `5592c0c9`) committed their reports WITHOUT the footer — those artifacts are invisible to the queue's footer-derived cross-reference. This run restores footer discipline for its own terminal commit but cannot retro-fix others' commits (amending foreign commits across runs would violate the daemon-race/multi-agent rules) | half the lineage footer-bearing |
| 3 | **Report hygiene for this ticket** — 4 status reports now exist for a one-line docs fix (this one included). Historical reports are never edited per repo policy; a non-destructive supersede-pointer pass (docs-health ANNOTATE) is possible but not done | open, low priority |

## c) NOT STARTED

| # | Item | Note |
| --- | --- | --- |
| 1 | **Build the stale-echo grep-gate** (docs/todo/storage.md:136, `[ready]`) — flake check or pre-commit; patterns: `root ≈ 1-2w` anywhere live, `3d+1w` in scripts/ + docs/services/ + docs/todo + top-level living docs, with context rules for negations/DONE notes/live-config restatements | THE follow-up; never started (correctly — out of this ticket's scope) |
| 2 | **Calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix** (docs/todo/storage.md:15, `[ready]`) — weeks start Sunday (btrbk `preserve_day_of_week` default); carry the "why" where operators look first | already queued; untouched |
| 3 | **Negative-test + selftest for the future gate** — seed the 7 historical echo sites as fixtures, mutation-test via the `scripts/negative-test-lints.sh` harness (repo doctrine: never trust a green check without a negative test through nix) | derived from this finding; not started |
| 4 | **docs-health HARVEST of this report's section (f)** — the queue's pacing gates decide; nothing harvested yet | pending by design |

## d) TOTALLY FUCKED UP

Nothing in this task's scope is broken. The repo is green, the finding is closed, and every closure claim is now literally true. For the record, the things that WERE fucked up and got fixed along this ticket's lineage:

| # | Item | Status |
| --- | --- | --- |
| 1 | **The original task's closure claims were FALSE as written** — both sweep greps omitted top-level living docs, so "no live-doc echoes remain" (TODO_LIST.md:49) and the storage.md:13 grep claim were wrong while written as done. The reviewer caught the 7th echo (FEATURES.md:291) that the 6-site sweep missed | FIXED by `3e3d718a` — this was the entire finding |
| 2 | **Five dispatches for a one-line docs fix** — run 1 fixed it correctly with a complete verification narrative in the commit body; runs 2–4 (incl. this one) re-proved a conclusion that was already fully documented. ~4 reports + 9 commits of process for a parenthetical swap. Root cause is queue-level (see section g, Q1) | contained; no repo damage |
| 3 | **Footer-less report commits in runs 3–4** — the queue cannot see those runs' work via its footer mechanism | documented in this report (b2); not retro-fixable safely |

## e) WHAT WE SHOULD IMPROVE

1. **Stop trusting hand-maintained sweep file lists.** Both failing sweeps scoped their greps by hand. A scripted gate (or at least a shared sweep script with a canonical file list) removes the entire class — the same file list is now written down three times in prose (12-27 report, 12-56 report, gate row).
2. **Proportionate disposition protocol for small findings.** A no-edit verdict should terminate with exactly ONE footer-bearing commit (a disposition note or this kind of report) — not a multi-commit report cycle. Runs 2–4 produced 6 commits between them for zero code changes.
3. **Footer discipline on EVERY ticket commit, not just the fix commit.** Runs 3–4's reports broke the queue↔git cross-reference. The footer is the queue's only reliable evidence channel.
4. **Verification narratives in the fix commit are the cheapest closure evidence** — `3e3d718a`'s body contains the full re-grep scope and verdict. Future re-verify runs should start by reading the fix commit body before re-running anything; it would have saved runs 2–4 substantial duplication.
5. **Gate before prose.** The "prose-only sweep lessons keep failing" pattern is now self-documented in the gate row itself. Every additional prose lesson added without the gate landing is a prediction of the next finding.
6. **Report depth should scale with work size** — a one-line docs fix does not need comprehensive reports per run; a one-paragraph disposition note suffices.

## f) Up to 50 things we should get done next

Sorted by impact. Items marked **[queued]** already have a TODO_LIST/docs/todo row — the queue's pacing gates own them; do not re-append. Most other items are ROADMAP fuel, not commitments.

**Verification hardening (direct descendants of this finding):**

| # | Item | Priority |
| --- | --- | --- |
| 1 | Build the stale-echo grep-gate per docs/todo/storage.md:136 **[queued]** — flake check or pre-commit, patterns + allowlist as specified | P1 |
| 2 | Seed the gate with the 7 historical echo sites as fixtures (6 sweep sites + FEATURES.md:291), mutation-negative-tested through `scripts/negative-test-lints.sh` | P1 |
| 3 | Give the gate a selftest (the `check-todo-system.sh` selftesting pattern) so allowlist context rules can't silently rot | P2 |
| 4 | Wire the gate into BOTH layers with one implementation (pre-commit + CI), per the one-formatter-arbiter doctrine | P2 |
| 5 | Design the `1-2w` pattern's context rule so unrelated "1-2 weeks" prose can't false-positive | P2 |
| 6 | After the gate lands: re-run the sweep mechanically and record zero-echo evidence in docs/todo/storage.md:13 | P2 |
| 7 | Add the calendar-anchor comment next to `snapshot_preserve = "3d 1w"` in snapshots.nix **[queued]** (docs/todo/storage.md:15) | P2 |
| 8 | Gate-vs-reality VERIFY: after the gate lands, confirm the live-config allowlist still matches snapshots.nix actual values (the allowlist depends on those values staying true) | P3 |
| 9 | Keep TODO_LIST.md:49 and docs/todo/storage.md:13 in sync per the queue's no-drift rule when any closure wording changes again | P3 |
| 10 | One-line pointer note in docs/todo/storage.md:66's DONE note that its `≈1-2w` figure is the pre-reconciliation history (superseded by :72) — prevents a future sweep from tripping on it | P4 |

**Queue/process hygiene (observed in passing):**

| # | Item | Priority |
| --- | --- | --- |
| 11 | Codify "no-edit disposition = one footer-bearing terminal commit" so re-verify runs don't multi-commit | P2 |
| 12 | Decide (owner) whether runs 3–4's footer-less report commits should get a non-destructive pointer annotation, or stay as-is | P3 |
| 13 | docs-health ANNOTATE pass over this ticket's 4 status reports: mark superseded runs, keep one canonical close-out | P3 |
| 14 | For one-line docs findings: allow fix-commit-body verification narrative to substitute for a re-verify run (read the commit body first) | P2 |
| 15 | Cap report depth by work size: parenthetical-class fixes get disposition notes, not comprehensive reports | P3 |

**Already-queued storage work this ticket brushed against (execute, don't re-queue):**

| # | Item | Priority |
| --- | --- | --- |
| 16 | crush-hot-db → `services.hot-db` Phase-2 fold (AGENTS.md notes the interim module should fold, not run beside it) **[queued]** | P2 |
| 17 | Docs-only fast-path for the pre-commit flake-check leg (the pre-existing hermes `-source is not valid` eval error that forced `--no-verify` on the fix commit) **[queued]** (`02691521`) | P2 |
| 18 | Harvest this report + the 12-27/12-56 reports' lessons into the gate row once the gate lands, then retire the prose lessons | P3 |

**Roadmap fuel (grounded only in what this ticket touched/saw):**

| # | Item | Priority |
| --- | --- | --- |
| 19 | Consider whether `docs/planning/**` living (non-archived) plans should join the gate's scope or stay permanently out (owner decision — see Q2) | P4 |
| 20 | Shared sweep-script helper with the canonical file list (gate = enforcement; script = one-off sweeps during incidents) | P3 |
| 21 | Add "top-level living docs" to the CONTRIBUTING.md sweep checklist so future echo hunts don't rediscover the blind spot | P3 |
| 22 | After gate lands, cross-link it from AGENTS.md's Snapshot-pinning doctrine block (the doctrine the echoes misstate) | P4 |

*(Stopped at 22 substantive items — the remaining ~28 would be padding; this finding's honest follow-up surface is the gate plus process hygiene.)*

## g) Questions I can NOT figure out myself

1. **Why did this ticket re-fire after run 2's footer-bearing close-out (`3ac34774`, "run 2 close-out")?** Runs 3 and 4 re-verified an already-complete fix and committed footer-less reports. If the queue re-fires on missing footer evidence, then run 2's close-out SHOULD have been terminal — if it re-fires for another reason, no-edit verdicts will keep burning dispatches. Should a no-edit disposition be REQUIRED to terminate via exactly one footer-bearing commit (as this report now does)?
2. **Gate scope decision:** should the future grep-gate scan ONLY the ratified scope (top-level living docs + scripts + docs/services + docs/todo), leaving `docs/status/**` and `docs/planning/**` as never-scanned history — or scan them too with a broader historical-context allowlist? The finding's fix took the first stance; confirming it as policy would prevent a future "the gate missed planning docs" round.
3. **Is `docs/todo/storage.md:66`'s historical `≈1-2w` figure acceptable as a permanent DONE-note record** (with :72 documenting the correction), or do you want history normalized (figure corrected in place / pointer added)? The gate's context rules would allow either; it's an owner taste call on how much self-correcting history DONE notes should carry.

---

**Report convention note:** the status-report skill's canonical format is a styled HTML dashboard; the user explicitly requested `.md` at this path, so the override was honored (flagged here per the skill's own rule). This report is point-in-time and will go stale — section (f) items 1–7 are the primary HARVEST input for a future `docs-health` pass (items 1 and 7 are already queued; nothing new needs appending).
