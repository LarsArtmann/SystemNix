# Status Report — Post-DAS-Recovery Convergence Watch (final leg)

**Task-Queue-ID:** `000001a0ac3c6744c3202e4c02a9851e5f22`
**When:** 2026-09-17 00:25 CEST
**Scope discipline:** this report covers ONLY this task's work and what was noticed in passing during it. No unrelated research.

---

## a) FULLY DONE

| # | Item | Evidence |
|---|------|----------|
| 1 | **Dump-backup fleet green**: all 9 registered backups healthy, ages 19–23h | `backup_all_healthy 1` in `/var/lib/prometheus-node-exporter/textfile_collectors/backups.prom` (cv, forgejo, immich, inboxclean, manifest, miniflux, paperless, pocket-id, twenty) |
| 2 | **Bank-sync outage-era failure backlog cleared**: `bank_sync_sync_errors_total 0`, last successful sync fresh (~00:07) | `:8097/metrics` |
| 3 | **No outage-era Gatus reds**: every current red traced to the LIVE 00:00 nix-gc + fstrim storm on the Samsung (self-healing), not DAS-recovery residue | Gatus alert journal lines 2026-09-16 23:51 → 2026-09-17 00:01 (guard TRIP/RESOLVE cycle) |
| 4 | **Memory-emergency-guard alive and doing its job**: trip fired 00:01:30 Sep 17 (nix-gc storm), `system_memory_guard_metrics_fresh 1` | `system_health.prom` + gatus journal |
| 5 | **TODO_LIST item updated with fresh re-confirmation** (third verification pass this boot: 00:05 prior queue run + 00:16 this run) | `TODO_LIST.md` line 27, commit `dd635f2b` |
| 6 | **Root-cause of the Sep 16 miss confirmed from primary evidence**: the `btrfs send -p @.20260915T2300 @.20260916T2300` command WAS issued at 23:00:01 as root — the send started, then the guard's churn-stop killed it mid-send; the garbled target was cleaned by `btrbk-pool-clean` at 23:45 | journalctl `-g 'btrbk.*root'` + gatus alert timeline |

## b) PARTIALLY DONE

| # | Item | Done | Missing |
|---|------|------|---------|
| 1 | **The task's headline check: tonight's root incremental received pool-side** | Verified the send ATTEMPTED and the failure mechanism (guard Zone 6 churn-stop, by design) | The receive did NOT land — newest pool-side receive is `@.20260915T2300`. Cannot confirm until the **2026-09-17 23:00 window** (~23h away). Time-gated, not effort-gated. |
| 2 | **btrfs-verify-pool-backups trajectory** | Arithmetic from observed state: tonight's 00:28 run reads "2 days old" (threshold 3) = OK | Have not independently re-run or read the verify unit's own latest journal output (took the 00:05 run's "Sep 16 00:28 OK" as given). If misses continue, the verify **FAILs on Sep 19 00:28** — that is the hard deadline. |
| 3 | **Guard-trip forensics** | Confirmed trip/alert cycle via gatus journal + guard freshness metric | Never read the guard's OWN journal (`memory-emergency-guard-check` syslog id — the AGENTS.md-documented trap) to confirm trip #299's second-by-second SIGTERM match. Inferred, not proven, this pass. |

## c) NOT STARTED

| # | Item | Why |
|---|------|-----|
| 1 | **Confirmation of the 2026-09-17 23:00 root incremental** | The window is ~23h in the future. Structurally impossible from a 00:15 session. |
| 2 | **Wise SCA approval (bank-sync statement gap backfill)** | Explicitly user-dashboard-gated (`docs/services/bank-sync-sca.md`); not outage-era, not agent-executable. 260 SCA challenges counted. |
| 3 | **A scheduled post-23:30 automated verification probe** (so the check stops depending on midnight queue sessions) | Considered, not built — queue pacing decision first (see g.1). |

## d) TOTALLY FUCKED UP

Nothing in this task's scope is broken — but three honest process failures across the three consecutive verification runs (00:05 prior queue, 00:16 this run):

1. **The task is in a structural BLOCKED loop**: it was harvested at ~00:00 two nights in a row, and its only remaining check completes at 23:00. Two queue sessions have now spent effort re-confirming the same green signals and re-stamping the same BLOCKED note. The queue's pacing, not the work, is failing.
2. **Nightly convergence is NOT converging**: the 2026-09-16 23:00 send was churn-stopped by the guard *at 23:06:40*, six minutes in. If Zone 6 trips every night around the 23:00 btrbk window (nix-gc at 00:00, IO-sensitive QLC), the Sep 19 verify FAIL is on a collision course and no amount of re-verification sessions fixes it.
3. **I trusted prior-run claims for two signals** (verify unit's 00:28 OK; "I/O Stall Rate" red classification) instead of re-reading their primary sources in this pass. Both were almost certainly correct, but "re-verified" should mean re-evidenced.

## e) WHAT WE SHOULD IMPROVE

1. **Queue pacing for time-gated work** — harvest this item after 23:15, not at midnight; or build the §c.3 probe and make the check self-converging.
2. **Zone 6 vs. the nightly backup window** — the guard's churn-stop of `btrbk-root` during its ONLY nightly slot means every stormy night silently costs a backup day. Candidate mitigations: shift btrbk-root earlier, give it a bounded grace, or have the guard re-arm btrbk in the same night's quiet tail instead of waiting for the next 23:00 tick. Needs an owner decision (backup-freshness RPO vs. IO-storm safety).
3. **Re-evidence, don't re-assert** — each BLOCKED re-verification should read at least ONE signal the prior run didn't (this pass did: the 23:00:01 send-command journal line), and should never carry forward unverified claims.
4. **Stop re-writing the same TODO line** — the note has grown with each pass; future passes should APPEND a dated one-liner or the line becomes unreadable.

## f) What we should get done next (impact-ordered; §1–§5 are this task's orbit, §6+ are items noticed in passing)

1. **After 2026-09-17 23:15: confirm the root incremental lands pool-side** (`ls /mnt/pool/backups/root` shows `@.20260917T2300`) and `backup_all_healthy`/verify stay green — closes this task.
2. **Before Sep 19 00:28: decide the Zone-6-vs-btrbk-window conflict** (see e.2) — otherwise the verify FAILs and we're back to a red backup posture.
3. **Build a post-23:30 self-verifying probe** (timer + textfile metric or Gatus check on receive-name age) so convergence no longer depends on midnight agent sessions.
4. **Read the guard's own journal for trips #298–#300** and correlate each with btrbk-root send attempts — quantify how many of the last N nightly sends were churn-stopped (is nightly-landing the exception, not the rule?).
5. **Append a dated one-liner** to the TODO item instead of growing the paragraph (process rule, 1 min).
6. **Wise SCA approval** — user dashboard flow (`docs/services/bank-sync-sca.md`); 260 challenges and counting; unblocks the statement gap permanently.
7. **Bank-sync `sca status`/`sca approve` CLI path verification** once the dashboard approval lands (same repo, AGENTS-documented chain).
8. **Verify bank-sync `statement_coverage` stays canonical** post-SCA-approval (the 2026-09-06 phantom-RFC3339-writer watch: if new RFC3339 values appear, the writer is statement-sync-coupled and INSIDE the daemon — dump goroutines then).
9. **btrbk-data EIO P0** — still correctly re-failing every night (noticed in passing: `/mnt/pool/backups/data` has zero complete received subvols); the /data corruption repair remains the blocker.
10. **btrbk-data oom-kill containment** (TODO line 28) — the second failure mode beside the EIO; `MemoryHigh`/`OOMScoreAdjust` sizing or send split.
11. **Owed reboot** — the 2026-09-07-boot flm corpse still pins :52626 (EADDRINUSE, start-limit-hit); reboot clears it and reclaims the D-state corpse pile inflating node IO PSI to ~73-75%.
12. **Post-reboot: staged v1.0.3 flm go-live** — the candidate fix for the corpse class; requires live-serve validation + Q4_K re-pull discipline.
13. **Re-enable `llama-rag`** once the llama.cpp mid-load CPU-spin regression is pinned (`llama-cpp-rocwmma` back to 20260905-era build or upstream bisect); paperless RAG is dark until then.
14. **Crush-DBs-off-QLC migration soak check** — `/mnt/hot` crush-hot-db deployed gated on the /nix soak (~2026-09-17); verify symlink sweep + `node_psi_io_some_avg60` vs the 40-60% baseline.
15. **Watch whether the crush-hot-db migration itself reduces Zone 6 trips** — the 5+ concurrent sessions churning QLC-root `.crush/` DBs were a named Freeze-4 driver; tonight's trip data is the first post-migration datapoint (noticed in passing: this run's storm was nix-gc, not crush DBs).
16. **Hetzner StorageBox + BorgBackup offsite leg** — decided 2026-09-11, not deployed; the backup story currently has a same-chassis single point of failure.
17. **Dead `@nix` QLC subvol deletion** (`/mnt/btrfs-root/@nix`) — tracked Phase 1; free the space as snapshots allow.
18. **Mail relay go-live completion** — Resend domain verification (`larsartmann.cloud` SPF/DKIM) is still the user step; until then NDRs are silently discarded and smartd's only remote channel is dead.
19. **After Resend verification: re-run the mail-relay runbook test send** and confirm the journal `status=sent` (replacing the `status=bounced` pending signal).
20. **smartd remote alerting path** — once the relay works, verify the DAS-pool drive death alerts actually arrive (the Gatus SMART checks are the only remote path today).
21. **`google-sync` go-live or honest removal** — DORMANT since 2026-08-31 with a 4-step user checklist; either execute it or stop carrying the section as if live.
22. **PMA upstream CI guard for the go-commit lock floor** (≥ `22f0e4c` + `OPENAI_BASE_URL` read check) — the 2026-09-07 recurrence is one lock churn away.
23. **InboxClean OAuth "In production" flip verification** — `work`'s 14-day survival is UNRESOLVED (AGENTS.md 2026-09-12 correction); `main`'s on-schedule death still stands until the flip + re-consent happens.
24. **InboxClean retro-decrypt repair deploy** — upstream `--backfill --decrypt-repair` landed 2026-09-12, NOT yet deployed (needs upstream push + flake bump).
25. **miniflux `disableLocalAuth` go-live gate** — one proven live SSO login then flip; the break-glass password form is still active by design.
26. **DiscordSync Turso decision** — upgrade the plan or commit to permanent local-only and drop the TURSO_* env + the deliberately-red Gatus check; the standing red is alert fatigue.
27. **signoz pair bump as MIGRATION-REVIEW task** — schema-migrator runs on service start; not a hash-refresh chore (2026-09-16 audit left it pinned).
28. **DiscordSync master still hash-broken (+129)** — keep the `c0604e46` pin doctrine until upstream builds.
29. **go-taskqueue git+file interim → `github:` flip** once origin carries the commits (CI cannot fetch git+file).
30. **Remaining droppable go-tarball overrides** (browser-history, papdashboard, crush-daily, PMA) — drop each upstream on touch; none currently break.
31. **`btrfs-emergency-reserve` re-provision check** — confirm present after the recent storm activity (`btrfs_emergency_reserve_present 1`).
32. **Confirm `/run/booted-system` == `/run/current-system` anchoring** after the next deploy that hits a failing unit (the exit-4 profile-skip class).
33. **DNS functional check post-next-deploy** — the resolv.conf hand-edit class; `system_local_dns_resolves` metric green.
34. **kdump retention sanity** — `/var/crash` bounded (keep 2, 20G) after any future livelock; confirm the last capture (if any) didn't fill root.
35. **Freeze-4 forensics follow-through** — route future repo-tree scans through `heavy-job` (the stray `find /home/lars/projects` owner-check was a named amplifier); audit whether any cron/timer still walks the tree un-wrapped.
36. **nvtop/btop terminal PSI-amplification** — user monitor terminals spiked to 68-89% io-PSI scopes during storms; consider nice/ionice defaults for interactive monitors (cosmetic-but-misleading accounting).
37. **btop per-PID stale-cache trap documentation** — hit 2026-09-16 in passing; the AGENTS.md entry exists, but a `Vmhwm` cross-check habit belongs in runbooks that debug per-process memory.
38. **`btrbk-pool-clean` behavior under repeated guard-kills** — it correctly deleted the garbled target this time; verify it can't race a LIVE send on a partially-written target (read its condition logic once).
39. **Backup-coordination maxAge for btrbk-received root** — the dump-fleet monitor is green while the ROOT RECEIVE (the actual NVMe disaster-recovery artifact) ages; consider a dedicated `root_receive_age_hours` metric + Gatus check so tonight's miss pages independently of agent sessions.
40. **DAS-link + pool SMART dashboards review** — 9-day outage lessons (Aug) vs. current metrics; is `pool_usb_recovery` still exercising its healthy path?
41. **Gatus check inventory audit** — confirm every enable-gated check that vanished with `llama-rag.enable = false` and any other disabled service is documented as dormant, not silently missing.
42. **SigNoz traces coverage** — remaining upstream gaps (overview, PMA, papdashboard, hermes) still need real instrumentation; ratchet `maxUpstreamGaps` 4 → lower as gaps close.
43. **`system-health` oomd/journal counters** — re-verify all `--since`+`timeout` bounded journal scans still pass under the current storm cadence (the 2026-08-31 sev1 class).
44. **sev1 overlay tier audit** — confirm no memory condition can overlay (the 2026-09-02 hard rule) after the recent guard-trip flurry; the 00:01 trip tonight paged correctly as notify-tier.
45. **`fastflowlm.socket` restore budget** — maxRestoresPerDay=3 spent on storm trips? Check `restored_total` + whether flm is currently capped-down (LLM consumers dark by design).
46. **Hermes GitHub PAT** — still PLACEHOLDER? `hermes-github-verify.service` skips cleanly, but the private-clone capability remains unwired.
47. **Crush-config repo phase-2 parity re-check** — after provider/model churn this week, run `bash scripts/crush-rc-test.sh` + `crush models` parity once.
48. **TODO_LIST hygiene pass** — several items reference pre-Samsung-flip state; a docs-health VERIFY sweep would retire stale claims (the "status reports are point-in-time" doctrine cuts both ways).
49. **VM-test suite health** — run the full flake check during a quiet window (never while flm resident + zram filling) to re-arm all eval guards post-2026-09-15 fixes.
50. **Consider a "watch" task template** — tasks like this one (verify X at time T) should carry their trigger time explicitly so the queue can pace them instead of midnight-harvesting into BLOCKED loops.

## g) Questions I can NOT figure out myself

1. **Should the guard's Zone 6 churn-stop learn about the nightly backup window?** Every stormy-night trip during 23:00–23:59 silently costs the only root-backup slot of the day, and nix-gc storms reliably coincide. Do you want btrbk-root moved to a quieter hour, a one-shot nightly re-arm after the storm drains, or is a 3-day verify threshold with occasional misses the accepted RPO?
2. **Is the task queue able to schedule time-gated items at their trigger time** (e.g. "check after 23:15"), or should I build the §f.3 self-verifying probe so no agent session is needed at all?
3. **Is tonight's 23:00 send likely to survive?** If Zone 6 trips again (nix-gc at 00:00 was the trigger tonight, but 23:06 was yesterday's), the Sep 19 verify FAILs. Do you want a one-off manual `systemctl start btrbk-root` in a quiet window tonight as insurance (requires your sudo), or strict by-design patience?

---

*Point-in-time snapshot. The loop closes when §f.1 confirms the 2026-09-17 23:00 receive pool-side; §f.2–§f.5 are the pre-deadline work.*

*Format note: user explicitly requested `.md`; the status-report skill's HTML default was overridden for this report only.*
