# Task `000001a0db75dfafa622748a00da7cf19ff9` — FULL STATUS + BRUTAL SELF-REVIEW (fire 2)

**Date:** 2026-09-26 05:05 CEST · **Scope:** THIS task only (re-fire verification of the already-closed `@home-hermes` 09-16 snapshot investigation) + what was noticed in passing. No unrelated research.
**Fire-1 report:** `docs/status/2026-09-26_04-26_task-000001a0db75dfafa622748a00da7cf19ff9.md` (closure verdict IN-SPEC). This report covers fire 2: the re-dispatch verification + §f harvest.

**Headline:** the re-dispatch protocol was executed fully and the closure STANDS, but the commit dance lost THREE daemon races (~10 min churn) and I landed TWO wrong line-number citations — the exact class fire 1's d3 documented, repeated within the hour. Both citation errors are fixed in this commit; the race analysis is the run's most useful output.

---

## a) FULLY DONE

| # | Item | Evidence |
| - | ---- | -------- |
| a1 | Protocol step 1 — footer + queue-surface closure re-verified, no drift: footer-bearing `3ada6e25` present; `TODO_LIST.md:52` `[x]` and `docs/todo/storage.md:16` `[x]` mutually consistent (IN-SPEC, different effective retention). | `git log --all --grep 000001a0db75…` → `3ada6e25`; both rows read |
| a2 | Protocol step 2 — INDEPENDENT live spot-checks (not inherited from fire 1): snapshot inventory at 04:44 CEST shows BOTH prefixes at exactly 4 dailies + 2 weeklies (`@`: 09-13, 09-20, 09-22..25; hermes: 09-16, 09-20, 09-22..25) — matching the source-derived model; `snapshots.nix` re-read confirms the hermes block sets ONLY pool-side retention with the in-tree inheritance comment, and the root block carries the corrected first-of-group calendar-anchor comment. | `ls /mnt/btrfs-root/.snapshots`; `sed -n '210,265p'` + `sed -n '325,350p'` snapshots.nix |
| a3 | Protocol step 3 — §f sweep of the fire-1 report: f12 already landed (AGENTS.md:660), f1/f2 = the tracked `[watch]` row, f3-f11 UN-LANDED → 8 follow-ups appended to storage.md (6 rows) + pipeline.md (2 rows) with matching TODO_LIST one-liners. No duplicates created. | this run's first commit (`f237932c`), 4 files +52 lines |
| a4 | Premise verification BEFORE queueing every appended item (fire 1's e3 lesson, applied): forgejo cadence grep'd (3 slots/day at snapshots.nix:336, `snapshot_preserve = "3d"` :341 — so "~4 live, not ~9" is source-derived); verify-snapshots newest-only gate read (:836-856); hermes block read (:246-263); f7 dedup-checked (candidate mention in the 2026-09-23 07-47 report §38, never tracked as a row). | greps + reads logged in the run; the landed rows carry the verified figures |
| a5 | Protocol step 4 — footer-bearing landing: `f237932c` ("docs: verify closed @home-hermes re-fire, harvest §f follow-ups") after the daemon dance (see d1). All hooks green ×3 (gitleaks, eval-only flake check); `check-todo-system.sh` green after the appends. | `git log -1`; hook output; script output "OK: TODO queue/library structure clean" |
| a6 | Line-citation audit of EVERY reference I landed (grep-verified at report time): found my own two errors (d2) + fire 1's two now-stale refs — all four fixed/era-annotated in storage.md in this commit. | `grep -n 'Local snapshot\|~9 snaps\|snapshot_preserve = "3d"' snapshots.nix` → 255-256 / 339 / 341 |
| a7 | Report self-harvest: every §f item below carries its tracking pointer — nothing new is filed untracked (the root cause of the re-dispatch churn class, not repeated here). | §f table routing column |

## b) PARTIALLY DONE

| # | Item | What works | What remains | Blocker / effort |
| - | ---- | ---------- | ------------ | ---------------- |
| b1 | The [watch] prune predictions (fire 1's falsifiable closure) | Live set at 04:44 matches the model exactly; the 09-22 dailies are correctly still present | Tonight's (09-26) and tomorrow's (09-27) 23:00 runs must delete `09-22`×2 then `09-16`+`09-13` — one `ls` + journal grep after each | Pure time gate. S |
| b2 | Pool-side continuity watch (my appended row) | Landed with mechanism pointer (newest-RECEIVE-per-prefix gate, snapshots.nix:708-740) | The 09-27 gate ride-through + the FIRST pool-side hermes prune (~mid-Oct, `14d 4w` — never yet exercised) | Time gate. S |
| b3 | Forgejo "(~9 snaps)" comment fix | Source half fully verified (~4 predicted, cadence + preserve grep'd); row landed [blocked:user] | The live count (`sudo ls /mnt/hot/.snapshots`) + the one-line comment correction | sudo. S |
| b4 | Citation accuracy | My two wrong refs fixed; fire 1's two stale refs era-annotated in storage.md (library surface) | `TODO_LIST.md:52`'s closed DONE row still carries the pre-shift `snapshots.nix:227` figure — left as-is deliberately (closed historical note; the library carries the correction) | Optional micro-pass. S |
| b5 | The re-dispatch protocol itself | Executed end-to-end this run (steps 1-4) | Codifying it in docs/CONTRIBUTING.md is still an open tracked row (not this task) | Tracked, pipeline.md. S |

## c) NOT STARTED (deliberately — contract says follow-ups belong to the backlog)

| # | Item | Why not started | Still wanted? |
| - | ---- | --------------- | ------------- |
| c1 | All eight harvested follow-ups' EXECUTION (comment fix, restore drill, comment blocks, codifications) | This run's contract: investigate the closed item, harvest, land the verification — executing the harvested rows is future dispatches' work | Yes — all queued with correct tags |
| c2 | Fire 1's §g owner questions (retention preference for dedicated subvols; premise-verification policy) | Owner decisions, both now backed by tracked rows (storage.md [decision]; pipeline.md f9 row) | Yes — unblocking those rows |
| c3 | Any of the tracked-neighbor storage work (the /data P0, boot-mirror, offsite-borg go-live, Phase-2 waves) | Out of scope; untouched, listed in §f as pointers only | Tracked |

## d) TOTALLY FUCKED UP (radical honesty)

| # | What | Severity | Root cause | Mitigation |
| - | ---- | -------- | ---------- | ---------- |
| d1 | **Lost THREE daemon races during the commit dance** (attempt 1: single `add && commit` — daemon swept between add and commit-finalize; attempt 2: `reset --soft HEAD~2 && commit` fold — daemon swept the re-staged index DURING the hook run; attempt 3: amend-forward — finally stuck). Two full wasted hook cycles, ~10 min churn, and the intermediate `a22e081e`/`c67d95ed`/`d3d644b4` heuristic commits existed for minutes each. | Medium (process waste, zero data loss — every daemon commit verified to contain exactly my files before folding) | I APPLIED fire 1's e1 mitigation ("single bash call") — but the 2026-09-25 echo-sweep lesson ALREADY said that doesn't work: "The race window is the daemon sweep, not the commit shape… fast commits after staging narrow it." The real amplifier I ignored: the hook chain (gitleaks + eval-only flake check ≈ 10-20 s) runs INSIDE the commit, widening the window on every attempt — and my commit commands kept auto-backgrounding at the 60 s threshold, adding blind latency I never factored in | For docs-only commits the durable fix is the tracked docs-only flake-check skip (TODO_LIST:59) + committing the INSTANT staging finishes. Fold-vs-amend: amend-first onto the newest daemon commit was strictly better than the soft-fold (same end state, half the race surface) |
| d2 | **Landed TWO wrong line-number citations** in my own appended rows: `snapshots.nix:257-258` for the inheritance sentence (actual 255-256 — I miscounted from a sed range instead of grepping) and `:336-340` for the forgejo block (actual 336-341 — the `3d` line sits at 341). **This is fire 1's d3 class, repeated ~40 min after reading its self-review.** | Low-Medium (both fixed in this commit; nothing cited was load-bearing) | Casual citation from sed output instead of `grep -n` at write time; I verified content but transcribed positions by eye | a6: grep-audit every line reference in one batch before committing; fire 1 + fire 2 hitting the same class in one hour = it's a norm gap, not a slip (§f12) |
| d3 | The fold attempt itself (reset --soft HEAD~2) was the wrong play: it traded cosmetic history cleanliness (one commit instead of two daemon commits + amend) for double race exposure. House doctrine says amend-forward and be done. | Low | Aesthetics over doctrine | d1's mitigation covers it |
| d4 | f7's dedup check was grep-light: the fire-1 report said "dedup-check 2026-09-23 report §82 at harvest" and I concluded "never tracked" from a TODO_LIST/docs grep miss plus a §38 title match, without reading that report's §38/§82 context | Low (conclusion almost certainly right — the mention is a candidate list, not a queue row) | Harvest-time economy | If a harvest pass ever doubts it, re-read the 07-47 report §38 before executing the drill row |
| d5 | NOT fucked up, stated explicitly so the absence is informative: zero code touched; every appended row premise-verified before landing; no push; no contact with `.crushrc`/`crush.json`/`.tq-verify`; the already-closed item's closure was preserved untouched; tree clean at both commits; no empty commits (banned) were needed |

## e) WHAT WE SHOULD IMPROVE

| # | Pattern | Impact | Concrete fix |
| - | ------- | ------ | ------------ |
| e1 | The daemon race is a LATENCY race, not a command-shape race — hooks inside `git commit` are the window | 3 races/run on docs-only commits; each costs a hook cycle + a verify-amend loop | Land the docs-only flake-check skip (tracked); until then: `git add` the instant the last edit lands and never re-edit between stage and commit |
| e2 | `grep -n` for every cited line number, in ONE batch, immediately before commit — never transcribe from sed/range output | Two wrong refs landed by fire 1's author, two by mine, same hour | One-liner in the AGENTS.md TODO-system section (§f12); apply to the era-annotation convention row too |
| e3 | Amend-first, fold-never (unless daemon commits are provably disjoint from yours) | The fold bought nothing and doubled exposure | Daemon-race policy row (tracked, pipeline.md) should say this explicitly — amend the newest daemon commit when `git show --stat` shows exactly your files |
| e4 | Read the PRIOR run's self-review BEFORE executing (I read it — and still half-applied its lessons) | e1/e2 were both written down 40 min before I repeated them | Re-dispatch protocol step 0 candidate: read the previous fire's §d/§e first, then act |
| e5 | Auto-backgrounding of commit commands (60 s threshold) hides commit failures until a `job_output` poll | Masked two failed commits behind a "completed" status with exit 1 | Note for tq agents: commit commands are the one class that should disable auto-background (or accept the poll) |
| e6 | What WORKED (keep): premise-verification of every queued row before appending (a4); dedup-check before creating rows; the falsifiable [watch] row pattern; protocol execution instead of blind redo; verifying daemon commit contents before every fold/amend | — | Codify the first two alongside the f9/f10 rows already filed |

## f) TOP NEXT TASKS (HARVEST input — routing column marks tracking; do NOT duplicate)

| # | Task | Impact | Effort | Category | Routing |
| - | ---- | ------ | ------ | -------- | ------- |
| 1 | After the 2026-09-26 23:00 btrbk-root run: confirm BOTH `09-22` dailies pruned locally (`ls /mnt/btrfs-root/.snapshots/`) | High | S | Verification | [watch] storage.md (fire 1) |
| 2 | After the 2026-09-27 23:00 run: confirm `@home-hermes.20260916` + `@.20260913` pruned; if yes prune the [watch] rows to CHANGELOG, if no reopen the doctrine | High | S | Verification | [watch] storage.md |
| 3 | Pool-side continuity: hermes freshness gate rides through the 09-27 weekly expiry + FIRST pool-side hermes prune (~mid-Oct) | Medium | S | Verification | [watch] storage.md (this run, f3/f4) |
| 4 | Fix forgejo "(~9 snaps)" comment — live count via sudo, then the one-liner | Medium | S | Quality | [blocked:user] storage.md (f5) |
| 5 | Owner decision: explicit `snapshot_preserve` on the hermes block vs documented inheritance | Low | S | Decision | [decision] storage.md (f6) |
| 6 | Hermes restore drill: receive one `@home-hermes.*` into a scratch subvol + diff vs live | Medium | M | Verification | [blocked:user] storage.md (f7) |
| 7 | Owner decision: btrbk `transaction_log` persistence (retention-bounded) | Low | S | Feature | [decision] storage.md (f8) |
| 8 | Codify premise spot-verification at queueing time (one sentence, AGENTS TODO-system) | Low | S | Quality | pipeline.md (f9) |
| 9 | Surface re-fire counts in tq verify output (go-taskqueue upstream) | Low | M | Quality | pipeline.md (f10) |
| 10 | One comment block in `btrfs-verify-snapshots`: gate checks NEWEST age per prefix only | Low | S | Docs | [ready] storage.md (f11) |
| 11 | **NEW** — Fix the doubled `|| true || true` on the two `LATEST=$(find … \| sort \| tail -1)` lines (snapshots.nix:801 and :836 — noticed verifying f11; second `|| true` is dead) | Low | S | Quality | new |
| 12 | **NEW** — Codify "grep -n every cited line number at write time" (one clause in the AGENTS TODO-system or CONTRIBUTING TODO section) — fire 1 d3 + fire 2 d2 = same class twice in 90 min | Medium | S | Quality | new |
| 13 | **NEW** — Era-annotate the remaining stale line ref in `TODO_LIST.md:52`'s closed DONE row (`snapshots.nix:227` → superseded, now 228; storage.md already carries the note) | Low | S | Docs | new |
| 14 | **NEW** — Append the fire-2 race data point (3 races on docs-only commits; hook latency is the window; fold-vs-amend verdict) to the tracked daemon-race policy row in pipeline.md | Low | S | Docs | new (extends tracked row) |
| 15 | Docs-only fast path for the pre-commit flake-check leg — now has fire-2 evidence: it is ALSO the daemon-race amplifier | High | S | Pipeline | tracked, TODO_LIST:59 |
| 16 | Codify the re-dispatch verification protocol in docs/CONTRIBUTING.md (executed twice now: 00-39 re-fire + this run) | Medium | S | Quality | tracked, pipeline.md |
| 17 | Queue dedup preflight (this item re-fired twice; 4 IDs fired 7× on 09-23/24) | High | M | Quality | tracked, TODO_LIST dedup row |
| 18 | DONE-note era-annotation convention — this run applied it live (storage.md line-ref note) as its second real use; codification still open | Low | S | Quality | tracked, pipeline.md |
| 19 | Commit-msg hook: reject subjects >72 chars (this run's subjects: 63 ✓ — the guard still doesn't exist) | Low | S | Quality | tracked, TODO_LIST pipeline |
| 20 | "Status reports self-harvest §f at authoring time" — this report complies (§f fully routed); codification open | Low | S | Quality | tracked, TODO_LIST pipeline |
| 21 | /data corruption repair P0 (gates: scrub-mechanism fix deploy + scrub delta + T06a sign-off; 10 files ~49G enumerated) | High | L | Storage | tracked, storage.md [blocked:user] |
| 22 | Mystery snapshot `data.20260905T2330` root-cause | Low | S | Forensics | tracked, storage.md [blocked:user] |
| 23 | Crush-hot-db ↔ hot-db Phase-2 fold (both modules in-tree, one must win) | Medium | M | Storage | tracked, TODO_LIST:62-63 |
| 24 | Boot-mirror activation + reboot (pre-reboot-check green required) | High | M | Stability | tracked, storage.md [blocked:deploy] |
| 25 | T14 hot-db monitoring metric + Gatus; `migrate-hot-db.sh` stub-fixture test (both before first migration window) | Medium | M | Storage | tracked, TODO_LIST:65-66 |
| 26 | Offsite Borg go-live (owner inputs: hostname/username/host-key pin) + first real-repo restore drill | High | M | Backup | tracked, storage.md + TODO_LIST |
| 27 | Borg drill unknown-flags junk-file fix (push-protection breaker) | Medium | S | Quality | tracked, TODO_LIST:53 |

**Already tracked — noticed while working, listed ONLY for HARVEST dedup (do not re-add):** items 15-27 above carry their tracking pointers inline; nothing in this table creates a second row for tracked work except the four marked **NEW** (11-14).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Should time-gated `[watch]` items get TODO_LIST queue one-liners at all?** The TODO-system header says the queue is agent-actionable `[ready]` one-liners only and watches are library-only — yet existing queue rows carry inline BLOCKED markers, and I appended a `[watch]` one-liner (pool-side continuity) following that drifted convention. Harmless at harvest (any dispatch date ≥ 09-27 can execute it), but should I move it library-only, or is the drifted convention now the convention?
2. **f10's row shape:** fire 1 routed it as "extends the tracked dedup-preflight row"; I landed it as a separate pipeline row (different ask: verify-output telemetry vs harvest-time skipping). Keep separate, or fold into the dedup row to honor the no-fragmentation doctrine?
3. **Daemon sweep vs docs commits:** three races this run with single-command discipline — the levers are (a) the tracked docs-only hook fast path, (b) tuning the auto-commit daemon (exclude `TODO_LIST.md`/`docs/status/`/`docs/todo/` from heuristic sweeps, or raise the idle threshold), or (c) accepting the churn. (a) is already queued; do you want (b) pursued, and if so is the daemon's config something I may read/propose changes to?

*(Only 3 of the allowed 3 — all three are policy/owner-preference calls nothing in the repo answers.)*
