# SystemNix TODO List

**Updated:** 2026-08-24 (docs/status/2026-08-2* full harvest: +13 rows — IO-PSI phantom saturation + guard tier, niri false negatives, boot-generation freshness, btrbk-data oom, dnsblockd health wedge, DAS-recovery sweep, BuildFlow caches, attic-class sweep, forgejo upstream filing, paperless embeddings E2E, crush-daily 0-sessions, device-timeout; 5 live bugs fixed same day — see CHANGELOG Unreleased)

---

## Priority 0: Critical (Data Loss / System Risk)

- [ ] **DEPLOY the 2026-08-31 16:34 freeze fixes BEFORE tonight's 23:00/23:30 btrbk catch-up** — in-repo, verified, NOT deployed: guard Zone 4 (sustained memory-PSI trip; VM-tested incl. burst-resistance), sev1 "MEMORY STALL SUSTAINED" desktop page (VM-tested), scrub deferral guard (eval-verified on all 3 units), fastflowlm v1.0.3 (built clean). After deploy: one-time `flm pull qwen3.6-moe:35b-a3b` (v1.0.3 Q4_K weights re-pull, old files hash-mismatch — v1.0.2→1.0.3 dance). During the catch-up window: no `nix flake check` VM-test storms (violated by parallel sessions today, including mine). Watch: whether v1.0.3 fixes the prefill core-dump (4× in the crashed boot; release notes don't claim it). **Source:** `docs/status/2026-08-31_16-50_crash-forensics-freeze-incident-3.md`
- [ ] **/data corruption recovery (1.3MB, 22 extents, 3 windows ~595G + ~627-639G)** — Weekly scrub 2026-08-17 found 1,351,271 uncorrectable csum errors; aborts `btrfs send` → btrbk-data seed blocked. NVMe SMART clean (torn-write class). **User decisions:** aggressive posture (minimal fix + read-only `btrfs check --mode=low-risk` in a docker-down window), monitor365 DuckDB on /data NOT disposable (safety-copy 54G to pool archive first), reserve after triage. Plan: `docs/planning/2026-08-17_14-41_data-corruption-recovery-pool-completion-master-plan.md` (T04-T08)
  - **Forensics addendum (2026-08-18 13:00 session):** the 01:50:48 `btrbk send ioctl -5` crossed `root 256 ino 1331118` (~67MB offset, 10 csum-fail lines, all with IDENTICAL csum `0x8941f998` = drive returned repeated garbage for one small range). Inode is in a root-only dir (`/data/{docker,containers,monitor365}` — resolve with `sudo find /data -xdev -inum 1331118`). btrbk then DELETED pool-side parent `backups/data/data.20260721T2330` (`subvolume delete --commit-each`) — tonight's run re-sends from an older parent and WILL EIO again on the same extents until the file is repaired/deleted (T04-T08 covers this; expect the nightly failure to recur until then). Also live: `/` shows `BTRFS health: CRITICAL (unalloc=4% meta=75%)` since the seed churn — new `btrfs_health_critical` Gatus alert (this session) pages on it.
  - **Nightly-failure STANCE (decided 2026-08-18 14:45 session): keep btrbk-data failing until T04-T08 executes.** Reasoning: (1) an older-parent resend CANNOT avoid the EIO — incremental `btrfs send -p` must read every extent in the delta, and the corrupted file lives under docker/container dirs that churn daily, so it is in EVERY delta until repaired; (2) suspending trades an INFORMED red (OnFailure Discord alert with kernel csum receipts + journal EIO) for a LESS informed red (backup_all_healthy staleness fires either way — you lose the "attempted and failed" distinction); (3) churn is bounded (oneshot, no Restart, fails minutes in). The failure IS the tripwire that T04-T08 is outstanding. Repair runbook (sudo, user-run): `sudo find /data -xdev -inum 1331118` → if under docker/containers: likely disposable (runtime state, containers recreate) → confirm no irreplaceable volume data, then delete the file, `sudo btrfs scrub start -B /data` to verify the csum errors stop, and let that night's btrbk-data prove the chain green. If under monitor365/: the DuckDB is user-declared NOT disposable — safety-copy to pool archive per the master plan first.
- [ ] **BTRFS emergency-reserve freshness semantics** — **Re-provision half DONE (verified 2026-08-18 evening pass): the 10 GiB reserve file is present** (`/btrfs-emergency-reserve`, Aug 17 21:41). Remaining: the old reserve extents were snapshot-pinned so a future deletion frees nothing until retention expires — either a periodic rewrite timer or a documented pinning caveat in AGENTS.md. Plan T14
- [ ] **Google Sync go-live (user steps; module built 2026-08-17, multi-mirror rework 2026-08-18, ships disabled)** — `services.google-sync` mirrors ALL THREE remotes every 5 min (30d deletion-grace each): `gdrive` (private My Drive, ~1.9 TB), `gdrive-shared` (private shared-with-me), `gwork` (Workspace work account). Blocked on: (1) Google Cloud OAuth client (Drive API enabled, **publishing status "In production"** — testing-mode refresh tokens die after 7d), (2) `rclone authorize "drive" "<client_id> <client_secret>"` **once per account** (use an incognito window for the second account to avoid authorizing the wrong one), (3) fill the EXISTING sops scaffold (`platforms/nixos/secrets/google-sync.yaml`, 3-remote placeholder committed 2026-08-18; replace the two `REPLACE_WITH_*_token` lines — needs `SOPS_AGE_KEY` from sudo per AGENTS.md Sops section; work-account layout UNSURE per user 2026-08-18 — if it's a shared/team drive, uncomment the `team_drive` line in `[gwork]`), (4) flip `services.google-sync.enable = true` in configuration.nix + deploy, (5) verify the 1.9 TB first seed (runs many hours-to-days under the 48h timeout) + `backup_healthy{google-sync}=1`. Photos (Takeout→immich-go) deliberately deferred until the mirror proves stable (user decision 2026-08-18). **Source:** this session, AGENTS.md "Google Sync" section
- [ ] **Off-site backup decision (3rd copy)** — **Pool safety net is LIVE (2026-08-17):** btrbk root+data sends + ALL application backups (forgejo, pocket-id, immich, twenty, manifest, paperless) land on the 2×16 TB BTRFS mirror — irreplaceables now exist on different physical disks than the NVMe. Remaining gap: the copy never leaves the house. User states important photos/docs already live in Google Photos/Drive — decide: 3-2-1 satisfied, periodic sdf (WOOACME) vault rotation, or Hetzner StorageBox + Borg (evaluated in `docs/research/hetzner-storagebox-borgbackup.md`, never executed). Flagged since 2026-06-25. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md`
- [ ] **Free root below 95% — ROOT CAUSE FOUND + FIXED (2026-08-21 self-review), awaiting deploy + user reclaims** — Root at **96%** (33G free of 723G). The dominant cause was a **self-inflicted GC deadlock**: `btrfs-gc-guard` blocked `nix-gc` on `UNALLOC_PCT < 10` (= 72 GiB on this device!) every night since 2026-08-17 (5 consecutive failures; 44 generations accumulated Aug 16→21 as a direct symptom) while ~20 GiB unallocated sat idle — GC frees extents WITHIN allocated chunks and can never restore chunk-level unalloc, so a %-gate deadlocks exactly when reclamation is most needed. The monitor365-gating change in the old item LANDED and is deployed (`configuration.nix:744`) — that sub-item is DONE. **Fixed 2026-08-21: guard now gates on an absolute 5 GiB unalloc floor + META_PCT > 90 hard-block (the real 2026-06-26 precursor), live-verified: current fs (2% unalloc = 20.4 GiB, META 82%) now passes → after deploy, the next nightly GC runs and auto-prunes generations >3d.** Remaining USER levers (sudo): delete `~/backups/activitywatch-sqlite-preDecimation-13GB.db` (13G, settled since 08-18), audit `~/tmp` (12G), trash `~/.cache.pre-subvol` (2.1G, migration leftover); /home frees arrive as btrbk snapshots expire (CoW pinning). **Source:** `docs/reviews/` 2026-08-21 brutal self-review + btrfs-health.nix guard history
- [ ] **Add `ManagedOOMPreference=omit` to dnsblockd** — dnsblockd is the sole DNS resolver on :53 and lacks oomd exemption. It was killed **730x/day** at the 50%/20s threshold. The 60%/30s raise (deployed 2026-08-14) should cut the rate, but exemption is the proper fix. Mitigations applied: MemoryMax 2G→4G, GOMEMLIMIT 3GiB. **Verified:** Aug 14 live data
- [ ] **Run foreground BTRFS scrub on `/`** — `/dev/nvme0n1p6` (`/`) has NEVER been scrubbed. Same physical NVMe as `/data` which had 13 corrupted files. SMART says drive is healthy (11% wear, 0 media errors), but root FS corruption would be catastrophic. **Manual command:** `sudo btrfs scrub start -B /`
- [ ] **Turso plan decision — DiscordSync cloud sync DOWN since 2026-08-16** — Turso free-plan quota exhausted (`quota_exceeded … SQL read operations are forbidden`); push+pull failing, sync circuit breaker tripped. Local SQLite healthy (local-first data safe); the CLOUD OFFSITE COPY is what's stale. Decide: keep sqlite-only (then remove sync env/keys from sops + module), re-auth, or upgrade plan. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §d.1, §f.1
- [x] **Hermes fixPermissions chown landmine vs RO projects bind** — FIXED 2026-08-20 (commit `962d433d`): recursive walk replaced with `find <stateDir> -xdev -path '<stateDir>/workspace/projects' -prune -o …` + `|| true` per walk. DETONATED live 09:18→09:35 (start-limit-hit, EROFS spam in journal) before the fix deployed — was NOT dormant after all. Regression-tested in `tests/test-hermes.nix`; generalized eval-time WARNING in flake check `chown-vs-bind-audit`. **Source:** `docs/status/2026-08-20_09-15_hermes-projects-readonly-access-status.md` §d.1
- [x] **Hermes: git `dubious ownership` breaks ALL git ops on the projects bind** — FIXED 2026-08-20: `GIT_CONFIG_GLOBAL` env (projectsDir-gated) points at a read-only store gitconfig with `[safe] directory` for the mount root + `/*` (recursive form documented in `git help config`). Verified live: agent sessions run git inside `workspace/projects` (journal 10:29). VM-tested incl. negative control (no env var → git fails).

## Priority 1: High (Service Outages / Security)

- [ ] **Forgejo mirror syncs spam `AddAuthCredentialHelperForRemote Error` 2,450×/day since 2026-08-18 15:28 (found 2026-08-21 review)** — every mirror sync cycle logs `SyncMirrors [repo: …]: AddAuthCredentialHelperForRemote Error open /tmp/forgejo-clone-credentials-N: no such file or directory` for each of the ~93 mirrored repos. Verified NOT fatal: mirrors still complete (`UPDATE mirror SET updated_unix` in the journal; all affected repos are public, syncs succeed unauthenticated) — but (1) any PRIVATE mirror would fail silently, (2) 2.4k error-level journal lines/day is pollution, (3) nothing monitors mirror-sync health. Onset correlates with the Aug 18 15:28 deploy (forgejo 15.0.6 running since); the PrivateTmp hypothesis was checked and is WRONG (that comment is on the backup oneshot, not the main unit). Investigate: what changed in that deploy's forgejo path (version bump? credential config?), fix or file upstream; then add a mirror-health signal (e.g. system-health journal error-rate metric + Gatus). **Source:** `docs/reviews/` 2026-08-21 brutal self-review
- [ ] **Audit module-level body-parsing curls for the implicit-gzip trap** — curl 8.21+ sends `Accept-Encoding` by default; any gzip-compressing API breaks jq/grep parsing silently. Check scripts were fixed (2026-08-16), but module-level curls that PARSE bodies were never audited: `pocket-id.nix:79,284` (provisioner, parses with jq), `forgejo-repos.nix:69,90`, `_forgejo-scripts.nix:55,188,541`. Go APIs may not gzip — unverified risk, not confirmed broken. Add `--compressed` where bodies are parsed. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §b.1, §f.4
- [x] **Hermes: investigate `Slash command sync timed out` failure** — RESOLVED 2026-08-20 (benign): the string has ZERO occurrences in 24h of journal; the live variant is `Discord rate-limited slash command sync; retrying after NNNs` (HTTP 429) — every restart re-syncs slash commands and Discord rate-limits, upstream retries with backoff. Platform-side behavior, no SystemNix action. The 09:09:32 `status=1/FAILURE` was likewise classified: SIGTERM deploy stop, gateway shutdown path exits 1 by design. **Source:** `docs/status/2026-08-16_17-09_DEPLOY-FAILURE-BUILDCACHE-MOUNT-STALENESS-BROWSER-HISTORY-OIDC-RACE.md` queue item 2
- [x] **Hermes monitoring: service is invisible to Gatus** — DONE 2026-08-20: `hermes` in system-health `monitoredServices`; Gatus "Hermes Agent Gateway" (failed/start-limit, fail-closed pat()s) + "Hermes Memory Pressure" (90% of MemoryMax), both Discord-alerting, verified green live. Post-deploy smoke section added (gateway-PID mountinfo `ro` bind + deployed-unit `GIT_CONFIG_GLOBAL` parse check). **Source:** `docs/status/2026-08-20_09-15_hermes-projects-readonly-access-status.md` §b.3, §f.4-5
- [ ] **Hermes workspace disk strategy — DECIDED 2026-08-20: DEFER (user)** — user chose to keep the workspace inside `@` for now: agent clones remain btrbk 14d+12w snapshot-pinned + pool-sent on the 95%-gated root fs. Risk accepted consciously; REVISIT TRIGGER: root back above 90% or workspace clones exceeding ~20G (then: sibling subvolume `@hermes-workspace`, the documented recommendation — NOT `/data`, un-repaired EIO). A clone-GC timer on the current layout remains an option without any layout change. Runbook + workspace AGENTS.md DONE. **Source:** `docs/planning/2026-08-20_09-18_hermes-projects-hardening-pareto-plan.md` Q2
- [x] **Hermes VM test `tests/test-hermes.nix`** — DONE 2026-08-20, GREEN: two nodes (bound + bare/null); asserts RO bind in unit namespace (EROFS for root, writable beside), env ×3 present / absent on null, git through the bind with the deployed gitconfig (+ negative control proving the env var is load-bearing), acl-revoke removes a seeded stale grant exactly once, D1 regression (perm drift + bind → no crash-loop, walk ran, zero EROFS). Gateway replaced by `sleep infinity` via mkForce. **Source:** `docs/status/2026-08-20_09-15_hermes-projects-readonly-access-status.md` §c.1, §f.8
- [x] **Hermes private-repo clone creds + write-back policy — DECIDED + SCAFFOLDED 2026-08-20** — user: Q1 yes (read-only fine-grained PAT), Q3 permanently read-only (no push creds ever, locked into runbook + module option doc). Shipped: sops `hermes-github-token.yaml` (placeholder, public-key-created), `HERMES_GITHUB_READ_TOKEN` in `hermes-env`, `hermes-git-credential` store helper wired via the safe-directory gitconfig (inert until a real `github_pat_`), `hermes-github-verify.service` auth canary (boot + deploy, skip-cleanly on placeholder). **REMAINING (user): create the PAT (Contents: Read-only, LarsArtmann private repos) and `sops --set` it — go-live block in `docs/services/hermes.md`.** Supersedes the SSH-deploy-key idea below. **Source:** pareto plan Q1/Q3
- [x] **Hermes: investigate parked `mnemosyne` MCP server** — RESOLVED 2026-08-20 (classified, no action): `mnemosyne` is a user-configured `mcp_servers` entry in hermes' RUNTIME config.yaml (settings-UI owned, not SystemNix config); its backing server closes immediately (`McpError: Connection closed`) and upstream parks it after 3 attempts BY DESIGN — warnings stop on the next restart, no gateway impact. Removing the dead entry is a user/settings-UI action. Acl-revoke retirement split out (dated item below). **Source:** `docs/status/2026-08-20_09-15_hermes-projects-readonly-access-status.md` §c.5, §f.13
- [ ] **Hermes: retire `hermes-acl-revoke` script (EARLIEST 2026-09-03)** — the ExecStartPre oneshot is self-neutralizing (removes the stale `g:hermes` home ACL, no-op since 2026-08-20). After ≥2 clean weeks: verify `getfacl /home/lars | grep hermes` is EMPTY, then delete the script + ExecStartPre entry + eval + deploy. **Verify:** `getfacl /home/lars 2>/dev/null | grep -c hermes` → 0
- [ ] **Hermes post-deploy smoke: Discord gateway connectivity line** — hermes has NO HTTP endpoint; current smoke proves unit/binds/env but not that the gateway actually reached Discord. Add a post-deploy-check journal assertion for the Discord "connected"/gateway-ready line within N minutes (mind the 429 rate-limit restart noise — match the success line, not the sync warning). **Source:** `docs/status/2026-08-21_02-38_hermes-latest-upgrade-session-report.md` §f.7
- [ ] **Verify the browser-history registration gate is LIVE in the deployed binary** — Deployed input is `4e7604d` (storage/v4.7.0 era: async startup drain + readiness gate + paged checkpoints — startup is fast now). The registration-lock release chain had a tag-ordering break (browser-history tag `v0.5.0` was not nix-buildable; fixes committed since). Verify `POST /auth/register` returns 403 while logged-out AND a second Pocket ID first-login is rejected on the LIVE system; if the gate is still a no-op, finish the chain (tag → flake bump → deploy). **Source:** `docs/status/archived/2026-08-15_01-44_pareto-t0-t4-outage-truth-found.md` §b.1, `2026-08-16_04-32_BROWSER-HISTORY-REPLAY-ROOT-CAUSE-STORAGE-V4.7.0-RELEASE.md`
- [ ] **Gate `import_export.go` user-import path (registration lock hole #3)** — cqrs-htmx `importUsers()` (`usermgmt/import_export.go:156`) dispatches `RegisterUserCmd` directly, bypassing both the `MaxUsers` check and the `registrationMu` lock. Admin-only (CSV upload), so lower risk than the OAuth2 bypass, but the gate is incomplete until this is closed. ~15-min fix + re-run `rg "NewRegisterUserCmd" usermgmt/*.go | grep -v test` for a 4th dispatch site. **Source:** `docs/status/archived/2026-08-14_10-41_oauth2-gate-toctou-fix-self-critical-review.md`
- [x] **Fix forgejo-oidc-setup deploy restart race** — DONE 2026-08-22: `forgejo-oidc-setup` carries the full `mkOidcGate` (curl-polls the OIDC discovery endpoint DNS → TLS → HTTP for up to 120s, `forgejo.nix` `forgejoOidcGate`), matching gatus/oauth2-proxy/browser-history. Boot-proven on the 2026-08-22 crash-recovery boot where the OLD dns-only gate failed exactly as described (`dial tcp :443 connection refused`). **Source:** `docs/status/archived/2026-08-14_13-44_hermes-registration-lifecycle-fixed-deploy-partial.md`
- [x] **Recreate Dozzle runtime container** — DONE 2026-08-22, and the ROOT CAUSE was worse than "needs recreate": the 256m/hardening config lived in the DORMANT `modules/nixos/services/dozzle.nix` (gated on `services.dozzle.enable`, never set) while an INLINE definition in `configuration.nix` actually ran the container — a split brain; even the original `--memory=256m` never reached `docker run`. Fixed by enabling the module + deleting the inline block (configuration.nix); deployed + container recreated: verified `mem=268435456 sec=[no-new-privileges:true] capdrop=[ALL]`, `:8084` answers 200. **Source:** 09-14 report annotation, 2026-08-14
- [ ] **PMA discovery daemon starvation — upstream root cause** — SystemNix mitigations deployed (MemoryHigh 12G/MemoryMax 16G, `PMA_DISCOVERY_WORKERS=2`, `pma-daemon-watchdog`). Root fix belongs upstream in projects-management-automation: bound per-worker scan memory, or make the daemon goroutine reclaim-insensitive. Also upstream: unquoted `GIT_AUTHOR_NAME=Lars Artmann` env entries (systemd drops "Artmann", journal spam; SystemNix carries quoted duplicates as workaround). **Source:** 2026-08-14 buildcache session monitor logs
- [ ] **Monitor365 re-enable decision (G7)** — Service deliberately disabled since 2026-08-12: the vendored `wireguard-collector` crate lives in a PRIVATE repo the Nix build can never fetch. Owner decision required: publish crate to crates.io / make repo public / vendor into the workspace — or remove the module. Its pool subvol (`services/monitor365`) is reserved. **Source:** `docs/status/archived/2026-08-15_01-44_pareto-t0-t4-outage-truth-found.md` §g G7
- [ ] **Upgrade Twenty CRM v2.7.3 → v2.31.1 (24 minors behind)** — Detected by `scripts/check-image-updates.sh` (2026-08-18). NOT blind-bumped: Twenty runs DB migrations on startup; 24 minors of schema drift needs changelog review first. Backup exists (daily pg_dump → `/mnt/pool/backups/twenty`). After review: bump `lib/images.nix`, deploy, verify login + workspace roundtrip; the daily image-updates workflow keeps an open issue until done.
- [ ] **Rogue git-identity audit across all repos + declarative global identity** — the 13-53 session found foreign author identities (`noreply@anthropic.com`, `unknown@example.com`, `Claude`) in PMA-managed repo histories and the PMA committer running with an ad-hoc identity for 162 Crush-authored commits. (1) audit all `~/projects` repos for non-LarsArtmann identities; (2) make global git identity + PMA `gitIdentity` fully declarative so no repo can commit as anyone else; (3) user decision: rewrite the 162-commit history or leave it (attribution honesty vs. churn). **Source:** `docs/status/2026-08-18_13-53_pma-commit-failures-crush-identity-and-daemon-recovery.md` §b/§f
- [ ] **Sweep ALL LarsArtmann Go repos for `InvokeNamed[interface]` on concrete `do` registrations** — the samber/do trap that took DiscordSync down for 2 days (`DI: service found, but type mismatch` → startup-fatal exit 69). Grep every samber/do consumer for `do.InvokeNamed[` with an interface type parameter; fix upstream with the concrete type + the resolution test pattern (`TestInvokeHealthCheckServices_ResolvesNamedCheckers`). **Source:** `docs/status/2026-08-18_02-27_discordsync-di-crash-loop-outage-fix.md` §f.28
- [ ] **🔑 PERSISTENT NAG: rotate leaked/stale API keys (user decision 2026-08-18 22:45: park as nag, not a one-shot ask)** — (1) **Resend key in sops is REVOKED → Pocket ID email is BROKEN** (verification mails dead since revocation; highest impact — rotate at resend.com, then `SOPS_AGE_KEY=$(sudo cat /etc/ssh/ssh_host_ed25519_key | ssh-to-age -private-key) sops --set '["resend_api_key"] "<new>"' platforms/nixos/secrets/<file>.yaml` per the AGENTS Sops section, redeploy); (2) **Synthetic key** live-assumed (rotate at dashboard); (3) **Context7 `ctx7sk-a5b19…`** VERIFIED LIVE 2026-08-18 (HTTP 200) and present 61× in local `.crush/` session dbs — rotate at context7.com dashboard AND update MCP config. Related: history-purge push is HELD indefinitely (user decision) — rotation is the entire fix. **Source:** `docs/status/2026-08-18_19-45_resend-revocation-gitleaks-noop-synthetic-second-leak.md`, AGENTS.md "Secret Leak Incident"
- [ ] **IO-PSI is phantom-saturated by D-state tasks on dead automounts — deploy gate + gatus can lie (crash3)** — one uninterruptible probe against the absent buildcache/pool automount fakes 80-99% PSI some avg10 on an otherwise IDLE NVMe: the deploy pressure gate (exit 12) can falsely block deploying the FIX, and the PSI warning tier stays red on phantom pressure. Correlate PSI with disk %util (or iostat idle check) in deploy.sh's pressure read + the gatus PSI conditions; separately fail-fast/timeout-bound ALL automount probing for absent devices (fish guard fixed 2026-08-24 for the login chain; systemd units + scripts still probe bare). **Source:** `docs/status/2026-08-24_08-00_crash3-balance-freeze-das-absent-io-diagnosis.md` §e1, §f.12/14
- [ ] **IO-PSI emergency guard tier (freeze #3 class is un-prevented)** — the memory-emergency-guard covers zram/MemAvail/PSI-memory, but crash #3 (manual btrfs balance at 0% chunk-unalloc) was an IO livelock: sustained IO-PSI with healthy memory. A Zone for sustained iostat/IO-PSI → stop churn sources (balance jobs, flm cold loads, clickhouse merges) before the kernel dies. Balance jobs are already PSI/zram-gated (fa9e56b7); the general tier is missing. **Source:** crash3 report §c6, §f.15
- [ ] **Niri gatus endpoints false-negative during hard-down (sddm incident)** — the niri liveness checks stayed GREEN while the compositor was down through the whole 2026-08-24 login black-screen window (metrics-based conditions keep serving stale/absent-semantics). Verify which exact conditions went stale, fix fail-closed. **Source:** `docs/status/2026-08-24_08-00_sddm-login-blackscreen-root-cause-fix.md` §b5, §f P1

- [ ] **Post-DAS-recovery verification sweep (blocks on user physical reseat + power-cycle reboot)** — once the link is back: `bash scripts/das-link-recovery-check.sh` (expect [1][2][3] green; re-run with sudo for shadow triage), then verify `/mnt/pool` mounted with BOTH Toshibas (`btrfs device stats`), `curl http://127.0.0.1:8200/` (atticd-bootstrap should have run green via the deploy.sh provisioner restart), immich/paperless/bank-sync vHosts 200, `nix run .#post-deploy-check` — the 8-9 DAS-cascade FAILs must flip green; watch the first nightly btrbk-data catch-up (oom risk below). **Source:** `docs/status/2026-08-24_08-04_attic-bootstrap-das-outage-exit4-fix.md` §c/§f
- [ ] **btrbk-data oom-kill containment (2nd failure mode beside the /data EIO)** — the nightly send died `oom-kill` (page cache charged to the unit cgroup, 20.6G peak) on 2026-08-20; the EIO abort masks it today, but post-repair the oom class returns. `MemoryHigh` + `OOMScoreAdjust` sizing on the btrbk-data unit, or split the send. **Source:** `docs/status/2026-08-21_23-35_btrbk-clean-garbled-receive-fix-session.md` §e1, §f.9
- [ ] **dnsblockd `/health` wedges on SQLite starvation while DNS :53 stays fine** — observed during the crash-night IO storm: health endpoint stops answering (SQLite lock contention) but the resolver keeps serving — liveness probes conflate the two. Move `/health` off the DB (or make it answer from a lock-free path) so a wedged dashboard/DB doesn't fail the wrong signal. **Source:** 23-35 report §c, §e.4
- [ ] **BuildFlow fallback caches: 11.3 GB on the QLC root + reap-list gap (user decision + code half)** — during DAS outages BuildFlow rebuilds `~/.cache/{gobuild,gocache,gomod}`-named fallback caches on the space-critical root; `buildcache-usb-recovery`'s reap list doesn't know those names. USER: keep vs quarantine/remove the current 11.3 GB. CODE: extend the reap list once the keep/remove call is made. **Source:** `docs/status/2026-08-22_17-31_das-link-down-buildcache-diagnosis-and-recovery-runbook.md` §c2/c3
- [ ] **Attic-class follow-ups from the exit-4 fix** — (1) sweep OTHER `wantedBy=multi-user.target` oneshots with mount-gated deps for the same `ConditionPathIsDirectory` skip treatment (the activation-blocking class that bit deploys on 08-22 AND 08-24); (2) collapse dependency-failure cascade FAILs in post-deploy-check into one root-cause SKIP; (3) dedupe the doubled python3 readiness probe in `attic.nix` bootstrap; (4) VM test for the pool-mounted-but-atticd-wedged loud-fail path. **Source:** attic 08-04 report §e/f.18-22
- [ ] **Boot-generation-freshness check (crash3 lesson)** — the machine once ran a stale Aug-16 rollback boot for days (13 min of ClickHouse telemetry written to root BTRFS, guards missing). deploy.sh now PRINTS the generation trail + anchor warning (2026-08-24); still missing: a gatus/sev1-side check that the BOOTED system matches the newest profile (covers the reboot-into-stale case the deploy print can't). **Source:** crash3 report §f.8
- [ ] **buildcache automount `x-systemd.device-timeout` 10s → 2s** — every probe of the dead mount costs the full timeout; with the fish guard fixed the remaining cost is in systemd units/scripts, but the mount option itself still admits 10s D-states per lookup. **Source:** sddm 08-24 report §f P1
- [ ] **Forgejo upstream: file the 3 verified mirror-outage issues** — dead-queue silence (100% dead mirror, zero log lines), TouchMirror masking failures (advances updated_unix on failure), ENOENT aborts of clone credential helpers. All source-verified 2026-08-22, never filed (verify-before-filing against CURRENT upstream main first). **Source:** `docs/status/2026-08-22_08-52_forgejo-mirror-outage-diagnosis-monitoring-curl-hardening.md` §b2, §c
- [ ] **Verify Paperless AI actually USES the llama-rag embeddings end-to-end** — env vars are wired but UI-saved values OVERRIDE env (`paperless/config.py`: DB rows beat env) — if the settings UI ever saved a value, RAG silently points elsewhere or nowhere. Check the paperless-ai logs/config surface for the effective embedding endpoint; also wire `PAPERLESS_AI_LLM_RERANKER_*` if the version supports it. **Source:** `docs/status/2026-08-20_08-06_llama-rag-model-fetch-deploy-fix.md` §g.1, §f.2
- [ ] **Investigate Crush Daily 0-sessions (2026-08-23; user question never answered)** — the daemon is healthy (API 200s every 5 min) but recorded zero sessions for 2026-08-23. Either genuinely no crush usage (benign — then add a 0-sessions-for->2-days informational rule) or a counting regression. Check what feeds the sessions metric after pool return. **Source:** attic 08-04 report §c/§g Q3

## Priority 2: Manual Steps (Blocked on Human)

- [ ] **Twenty: ENCRYPTION_KEY rotation decision + digest-pin `twenty-postgres`/`twenty-redis`** — (1) the ENCRYPTION_KEY secret has no documented rotation procedure (rotation invalidates encrypted fields — decide: document rotate-and-accept-loss, or leave pinned forever with a backup copy in the pool archive); (2) twenty-postgres/redis images float on latest-family tags while other DB-backed images are digest-pinned (AGENTS.md rule) — pin them after the v2.32 upgrade proves stable. **Source:** `docs/status/2026-08-18_20-38_issue-138-twenty-v2.32-upgrade-self-review.md` §c.1-c.2/§f.6-7
- [ ] **Paperless admin password handover** — Generated randomly, stored encrypted in sops (`platforms/nixos/secrets/paperless.yaml`, key `paperless_admin_password`). Log in as `admin` at `paperless.home.lan`, change the password in the UI. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §g.3
- [ ] **Browser-test the SigNoz UI + eyeball the 5 `systemnix-*` dashboards** — Data, convergence, and scrape targets are API-verified; nobody has confirmed rendering in a browser (JS bundles load, assets 200, panels lay out). Also confirms the external oauth2-proxy forward-auth path round-trips to the SPA. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §B.1, `2026-08-16_21-25` §g.1
- [ ] **Runtime-verify wf-recorder screen recording on niri (ffmpeg_8)** — Build success proven; a real recorded clip was never tested (SSH-only sessions). Low risk (nixpkgs' own default combination now). **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §c.1
- [ ] **Boot-resilience test: DAS powered off** — Boot with the pool absent must stay clean: failed-but-contained btrbk units, NO `/mnt/pool` contamination on the root fs. `nixos-rebuild build-vm` or a real boot. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §f.11
- [ ] **`e2fsck -f /dev/sdc1` (buildcache) during the next unplug window** — The fs took WRITE errors during the 08-16 flap storm and is mounted RW again (cache-only data, hash-verified consumers — acceptable, but fsck is proper hygiene). Same window: decide the JMS567 enclosure swap (physical root cause of the flap storms; ZFS drives dropped simultaneously — shared DAS power rail suspicion). **Source:** `docs/status/2026-08-16_18-39_buildcache-zombie-mount-incident-and-self-healing-deploy.md` §b, `19-12` §f.5-6
- [ ] **Satellite GOEXPERIMENT sweep (21 repos)** — they import `encoding/json/v2` but set `GOEXPERIMENT=jsonv2` nowhere in nix → devShells hard-fail for contributors without evo-x2's machine-global env (evo-x2 itself covered since 2026-08-15 via home.nix). List: `scripts/report-goexperiment-gaps.sh`. Fix pattern: dnsblockd (`nix/devshell/default.nix` + `nix/packages/default.nix` `env.GOEXPERIMENT = "jsonv2"`). **Source:** `docs/planning/2026-08-15_21-23_SMART-BUILDCACHE-OVERHAUL.md` §8
- [ ] **go-nix-helpers: default `GOEXPERIMENT=jsonv2` in template/devShell** — future repos self-carry the flag for all contributors
- [ ] **btrfs+zstd buildcache conversion (maintenance window)** — `scripts/buildcache-btrfs-convert.sh`: ~2x effective capacity + checksummed corruption detection. Requires quiesced gopls/builds + the fsType flip documented in the script
- [ ] **go-codec: align `go 1.26.6` floor with nixpkgs 1.26.5** (or wait for the nixpkgs bump) — with `GOTOOLCHAIN=local` that repo fails loudly until aligned. After aligning: `go clean -cache` NOT needed
- [ ] **Retire the direnv `use_go_env` GOEXPERIMENT sniffer** — obsolete once all satellites self-carry the flag
- [ ] **Smart-audio: verify audible output + reverse direction** — Play a test sound on the TV, switch focus to DP-1, confirm the profile switches back. PipeWire routing verified via `wpctl status` but audibility NEVER tested — flagged as a pattern failure in three consecutive reports. Needs test-tone tooling (`alsa-utils`/`pw-cat`). **Source:** `docs/status/archived/2026-08-14_08-24_smart-audio-daemon-built-deployed-with-gaps.md`
- [ ] ~~**Hermes: install SSH deploy key**~~ — SUPERSEDED 2026-08-20 by the read-only PAT approach (see the private-repo creds item above): one fine-grained PAT covers all LarsArtmann private repos; per-repo deploy keys would need 4 keys. Reopen only if the PAT approach proves insufficient.
- [ ] **Hermes: set fallback model** — `sudo -u hermes hermes config set fallback_model`
- [ ] **Test browser-history OAuth2 login end-to-end** — Visit `https://history.home.lan`, click "Login with Pocket ID", verify redirect flow completes and dashboard loads with data
- [ ] **Verify dnsblockd dashboard auth** — Visit `https://dnsblock.home.lan/dashboard`, enter token (retrieve via sops), confirm stats load
- [ ] **WebAuthn `.lan` RP ID browser validation** — Verify browsers accept passkey registration on `history.home.lan` (`.lan` is not a real TLD; Chrome/Firefox may reject)
- [ ] **MiniMax quota decision (carried ×4)** — MiniMax provider quota exhausted; decide: upgrade plan / pay-as-you-go / wait for reset. **Source:** 20-31 §g, 20-52 §g, 15-01-44 §g G5
- [ ] **Deploy to macOS** — Darwin registry override for nixpkgs written in config (`platforms/darwin/nix/settings.nix`) but NOT deployed. Run `nix run .#deploy` on `Lars-MacBook-Air`
- [ ] **Clean up orphaned dnsblockd tracking DB** — `/var/lib/dnsblockd/dnsblockd_tracking.db` (724 MB, last modified Jul 15) is the old database from before the rename. **Manual:** `sudo trash /var/lib/dnsblockd/dnsblockd_tracking.db`
- [ ] **BIOS fix for DAS boot hang** — GLMtec logo hang: disable USB boot, enable Fast Boot, NVMe-only boot priority. Manual BIOS change; recurs on every cold boot with the DAS connected until done
- [x] **Hermes: bump flake input past v0.20.1 and DELETE the `registration_lifecycle` patch** — DONE 2026-08-21: input at `63c6d9a4` (v0.20.4), import verified against the sealed uv2nix venv (`hermes-agent-env` site-packages), patch deleted from `hermes.nix`, package rebuilt + VM test extended. AGENTS.md/FEATURES/runbook updated.
- [x] **`/home/hermes` is 58G — STALE CLAIM, CLOSED 2026-08-21** — re-measured: **3.2M**. The 58G figure (2026-08-17 report) no longer matches reality; the move-to-pool question is moot. Lesson: point-in-time size claims must be re-measured before acting (the global AGENTS.md cross-cutting lesson, now honored here). The audit script (`scripts/hermes-state-audit.sh`) can be deleted or kept as a generic tool — user call.
- [ ] **Hermes upstream: propose `projectsDir` RO-bind pattern for the NixOS module** — outline drafted 2026-08-20: add an upstream `services.hermes.projectsDir` option wiring `BindReadOnlyPaths` + `TERMINAL_CWD`/`HERMES_WRITE_SAFE_ROOT` + the safe.directory gitconfig (SystemNix `modules/nixos/services/hermes.nix` as reference; include the chown-vs-bind ExecStartPre pitfall as a doc note). File at NousResearch/hermes-agent once `verify-before-filing` has confirmed against CURRENT upstream main.
- [ ] **Hermes: migrate `TERMINAL_CWD` env → generated config.yaml when upstream supports it** — WEAKENED 2026-08-21: upstream v0.20.4 (commit `31561e37`) reads the `.env` FILE for the deprecation warning and explicitly calls process-env `TERMINAL_CWD` legitimate — the warning no longer fires for our systemd env bridge, so the only remaining motivation is config cleanliness. BLOCKED on upstream offering declarative/generated config injection (injecting runtime-owned config.yaml today = split-brain). Do NOT "fix" by hand.
- [ ] **Promote `chown-vs-bind-audit` flake check from WARNING to FAILING** — shipped WARNING-only 2026-08-20 (negative-tested: catches `chown -R` + unscoped `find … -exec chown/chmod` in modules with `Bind*Paths`; currently zero warnings). After one clean CI cycle (~1 week), flip `exit 0` → `exit 1` on `$warn`.
- [ ] **Move Docker data-root to SSD 2 (btrfs)** — sdd (SanDisk serial 174244451713, btrfs, ~224 GiB) still awaits its Docker-storage role (`/data/docker` on QLC NVMe today). Keep SigNoz/ClickHouse volumes on `/data`. Declarative `fileSystems` entry (e.g. `/mnt/docker`), migrate, wire `virtualisation.docker.daemon.settings.data-root`. **Source:** `docs/status/2026-08-14_13-15_ssd-repurposing-options.md`
- [ ] **`/rust-cache` p9 deletion + retired-subvol cleanup — USER-RUN commands (config side DONE 2026-08-17)** — Decision: delete-only, freed 100 GiB left UNALLOCATED (p9's only neighbor is `/data` p8 — root p6 is NOT adjacent; "grow root" was never physically possible). Automounts for `@go`/`@npm`/`@cargo` removed from `snapshots.nix`, `CARGO_HOME=/mnt/buildcache/cargo` (seeded), `@cache-home` deliberately KEPT (16 GB live app caches; snapshot-exclusion is its job). **Remaining (sudo):** deploy, then `sudo btrfs subvolume delete /mnt/btrfs-root/@go @npm @cargo` (after unmount), `sudo sgdisk -d 9 /dev/nvme0n1`, `sudo rmdir /rust-cache`; restore `~/go/bin` from `/mnt/buildcache/go-bin-salvage`. **Source:** AGENTS.md "/rust-cache" entry

## Priority 2.5: Black-screen follow-ups (2026-08-18 self-review execution session)

- [ ] **VM test: linger + SDDM login → exactly one niri, none pre-login** — locks the 2026-08-18 bug class into CI (`tests/default.nix`, `pkgs.testers.runNixOSTest`). Partial version sufficient: enable linger, reach multi-user, assert NO niri process + graphical-session.target inactive; the real-login half needs SDDM scripting. **Interim guards now TWICE-layered (2026-08-18 15:00 session):** `session-boot-audit.nix` eval-time guard (catches the pull-in class, negative-tested against the exact historical bug) AND `tests/test-session-boot-audit.nix` — a permanent pure-eval CI check (4 cases: historical bug, `allowedUnits` escape hatch, suffix-spelled `.service` reference merging, raw-text unit parser; guard canonical-name dedupe added same session). The VM test would add the RUNTIME half (does lingering boot actually stay niri-free) the eval guard cannot see
- [ ] **Post-deploy desktop settle check (after the black-screen deploy + reboot)** — verify `smart-audio.service`, `shutdown-overlay.service`, `niri-flake-polkit` (Qt gtk2→adwaita/fusion fix, switched from the deprecated `gnome` 2026-08-18 15:00 — ALSO verify polkit dialogs RENDER correctly, the one runtime check adwaita needs), and DMS recover in the REAL session (all were start-limit-hit against the zombie during 2026-08-18). Then `nix run .#post-deploy-check`
- [ ] **helium SIGTRAP coredump (5/TRAP 03:30:56, boot -1)** — poll-thread stack, likely collateral of the kill wave; verify helium stability post-reboot before spending time
- [ ] **aw-watcher gate monitoring preference (user decision)** — the gate now waits indefinitely for a wayland socket (trades failure for silence). Option: Gatus alert if the watcher hasn't attached within N min of `niri_graphical_session 1`. Best-effort tracking noise vs silent gap — preference call
- [ ] **emeet-pixyd: rate-limit the absent-device probe WARN** — upstream (LarsArtmann/emeet-pixyd): the `/sys/class/video4linux/video1/uevent` WARN re-fires per scan cycle when the webcam is absent. Measured benign 2026-08-18 (~1/hr now); quiet it or downgrade to DEBUG upstream

## Priority 2.6: Pixel 6 Phone Recovery follow-ups (2026-08-20/21 sessions)

_Extraction DONE: ~62.7 GB / 17.5k files verified on `/mnt/pool/backups/pixel6/2026-08-20/` (591/591 UCR call WAVs triple-verified, Signal backup byte-exact, full shared-storage pull, UCR APK, index.csv + SHA256SUMS, pool README). Phone 100% untouched (read-only mandate). **Source:** `docs/status/2026-08-20_09-46_pixel6-full-phone-recovery-to-hdd-pool.md` + `2026-08-21_05-19_pixel6-recovery-followup-day2.md`_

- [ ] **Udev rule for Google USB vendor 18d1 (adb access BROKEN again since overnight replug)** — systemd 258 `uaccess` tag lands ONLY on graphical seat sessions; evo-x2 runs TTY/SSH sessions → no ACL on `/dev/bus/usb/*`. Yesterday's manual `setfacl` fix was node-specific and expired on replug exactly as predicted. Durable fix: `services.udev.extraRules` (`MODE="0660", GROUP="users"` for vendor `18d1`, or uaccess+logind-session doc note) in `boot.nix` or a tiny module + deploy. Single blocker for every remaining phone-side pull. **Also:** add `pkgs.android-tools` to system packages (kill per-call `nix shell` overhead)
- [ ] **User: install "SMS Backup & Restore" → export SMS/call logs/contacts XML** — Android 17 removed shell `content query` access (tested; fails). User installs app (Play Store, whitelisted for SMS/call-log perms), grants perms, destination = Phone storage, Back Up NOW. Then agent pulls the XMLs (read-only) + verifies counts. Bonus: call-log XML resolves the 428 anonymous-number WAVs to contact names
- [ ] **User: WhatsApp "Back up now" → re-pull fresh msgstore** — captured `Android/media/com.whatsapp` msgstore is only as fresh as the phone's last local backup; a fresh in-app backup + re-pull makes the chat DB current as of extraction day
- [ ] **Enrich `universal-call-recorder/index.csv` with call-log contact names** — join the 428 bare-number recordings (number+timestamp from filename) against the SMS B&R call-log XML → named rows. Depends on the XML pull above
- [ ] **SHA256SUMS for Signal + WhatsApp + Cube ACR sets** — only the UCR WAVs have an integrity manifest; bitrot in the others would be silent. Same `sha256sum` pattern as `universal-call-recorder/SHA256SUMS`
- [ ] **Full ffprobe sweep over all 591 UCR WAVs** — only 5 spot-checked; a truncated/corrupt file in the emotional archive should be caught now, not on playback day
- [ ] **Add `backups/pixel6` to btrbk-pool snapshot set + backup-coordination freshness** — pool snapshots currently cover `services/*` only; the irreplaceable phone archive has no snapshot/protection layer. Also register last-success freshness (Gatus `backup_all_healthy` family)
- [ ] **Manual `btrfs scrub` on the pool** — ~66 GB of new data since the pool's last scrub; verify checksums on fresh writes
- [ ] **`SIGNAL_RECOVERY_KEY.txt` placement decision (USER)** — plaintext passphrase sits BESIDE the encrypted backup on the pool (user-created 2026-08-21). Works, but defeats the encryption if the share ever leaks. Options: password manager + delete file (recommended), sops, or accept + document in pool README threat model. Agent won't touch it without a decision
- [ ] **Recover `/tmp/pixel6-*.sh` transfer scripts into `scripts/`** — the working per-directory tar+pull logic (stall detection, count verification, stdin-safe loops) is in `/tmp` and may already be lost to reboot; if lost, reconstruct from the status reports (the runbook value is the 4 failure lessons: stall-watch, toybox-tar desync on denied files, adb-pull aborts at first denial, fd-3 stdin)
- [ ] **WAV→FLAC mirror of UCR archive** — 54 GB → ~18 GB lossless (keep originals); tag during conversion (contact=artist, year/month=album) so ANY music server can organize it
- [ ] **Navidrome audio-archive server (DECIDED 2026-08-21: Navidrome over Jellyfin after deep-dive)** — serve the UCR call archive (and later any phone audio) with browse+search+mobile playback. Why: 4-10× lighter (~100 MB RAM), SQLite FTS5 full-text search, custom `Tags.*` config (can carry per-call transcript snippets), native Subsonic API → Symfonium/Tempo on the Pixel, single Go binary. Plan:
  1. WAV→FLAC conversion of the 591 UCR WAVs (~54 GB → ~18 GB, lossless; originals untouched) + tag from `index.csv` during conversion: artist=contact, album=`<YYYY-MM>`, title=`<date> <time> <contact>`. Navidrome is tags-ONLY (refuses folder browsing) — tagging is mandatory, not optional
  2. `modules/nixos/services/navidrome.nix`: port in `lib/ports.nix`, `harden {}` + `ioTier.background`, MusicFolder = pool FLAC mirror (`RequiresMountsFor`), Layer 2 `protectedVHost "music"` in caddy.nix + homepage tile, Gatus check (200 + login-body pat)
  3. Clients: Tempo (free) or Symfonium (Android), Feishin/Supersonic (desktop)
  4. Later: whisper transcripts → custom tags → smart-playlist search over what was said
- [ ] **Whisper transcription batch (GPU) over the 591 calls** — whisper.cpp/ROCm on evo-x2; feeds transcript column into index.csv, RAG (llama-rag `:8848`/`:8849` already run), and full-text "what did we say" search — the killer feature no music server has
- [ ] **Call analytics + prefix decode** — decode UCR filename prefixes (0/1/3/5 = direction?) definitively; per-contact frequency/relationship timeline from filenames + Cube ACR JSON metadata
- [ ] **Immich ingestion of DCIM + WhatsApp media** — phone photos into the existing Immich (immich-go / direct library import)
- [ ] **Human-friendly renamed mirror tree** — `2022-01-28_Luana-Mencarelli.wav` hardlink mirror (no extra space) for non-technical browsing
- [ ] **Phone care while it remains a primary copy** — 95% storage full (Signal backups could fail at 100%), keep charge ≥50% until pool snapshot+scrum verify lands. User-only; agent never deletes anything on the phone
- [ ] **Pool README day-2 update** — fold in `SIGNAL_RECOVERY_KEY.txt`, pending XMLs, Navidrome/FLAC plans; cross-link TODO items

## Priority 3: Infrastructure

- [ ] **ClickHouse telemetry backup coverage (XFS move follow-up)** — Since the 2026-08-22 XFS migration (`/var/lib/clickhouse` own partition), btrbk root snapshots exclude telemetry entirely — zero backup. Decide: accept (derived data, rebuildable by re-ingestion) vs `clickhouse-backup`/`FREEZE`→rsync-to-pool job. Sizing reference: 52 GiB pre-TTL-cleanup high-water mark. **Source:** `docs/planning/2026-08-22_02-38_clickhouse-xfs-migration.md`
- [ ] **Remove stray `/var/lib/paperless` remnants** — The untracked-module misdeploy (upstream defaults, ~5 min live) wrote its initial DB to the wrong path. Verify the pool instance healthy first, then trash the stray dir. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §d.1, §f.4
- [ ] **restic repo on pool for app dumps (dedup)** — Forgejo full zips hit 7.4G after 2 nights (26G steady state at 7d); zips share ~0 extents so duperemove is useless. Content-addressed chunking via `pg_dump | restic backup --stdin`-style jobs onto `/mnt/pool/backups/restic/<app>` gives real retention (90d) for less space. Zero urgency at 14T free. Plan T17/backlog
- [ ] **Own-tools NVMe→pool migrations** — `discordsync` + `browser-history` state dirs → their reserved pool subvols (`/mnt/pool/services/*`). DONE 2026-08-18: atticd (`/data/atticd` → pool, live flip) + monitor365 (`/data/monitor365` + `/data/monitor365-archive` → pool; service still disabled). Same verified-rsync + `RequiresMountsFor` pattern as immich (`data-to-pool-migration.service`, self-neutralizing). **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §f.9
- [x] **Pool-usage Gatus alert (>80% warn)** — DONE (live, `gatus-config.nix:1391` "Pool Usage"): `pool_usage_over_threshold` asserted-0 condition + Discord alert naming the fill sources. STALE since 2026-08-2x — the item predated the check landing. The optional same-pass items (hd-idle spin-down, monthly `/mnt/pool/backups` snapshots) were NEVER part of the shipped check and remain open ideas — split off below if wanted. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §f.12, e.7
- [ ] **Commit the dashboard generator + eval-time dashboard JSON lint** — `/tmp/gen_dashboards.py` was EPHEMERAL (lost); the deterministic uuid5 pattern must live in `scripts/`. Add schema lint to pre-commit/CI (schemaVersion v6, exactly 1 query per panel, layout `$ref`s resolve, 12-col geometry) so a v1-format regression fails CI instead of warning at deploy. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §C.4, F.8; `2026-08-14_10-00` step 18
- [ ] **Test-fire "Telemetry Export Failures" → Discord** — No synthetic export failure has proven the alert → Discord delivery end-to-end (provisioning + eval health verified only). E.g. `systemctl stop clickhouse` for 60s in a maintenance window. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §B.3, F.4
- [ ] **SigNoz migrator-gap guard** — Provisioner should assert applied-migration IDs ⊆ known list per DB (the squash-gap class that silently skipped migration 1010 for months — would catch the same for logs/metrics DBs). **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §E.2, F.5
- [ ] **Recreate remaining json-file Docker containers** — Docker's default log driver is journald now (twenty-worker-1 flows enriched), but pre-change containers keep json-file until recreated — not audited which. One `--force-recreate` per stack so ALL container logs flow to SigNoz. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §B.2, F.3
- [ ] **`file_storage` cursor persistence for the journald receiver** — `start_at=end` means logs emitted during collector downtime are LOST (gap, no duplication). Persist the cursor so restarts resume. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §C.2, F.6
- [ ] **Caddy access.log ingestion (filelog receiver)** — request-level logs in SigNoz; currently only prometheus metrics from caddy. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §C.1, F.7
- [ ] **Log-ingestion-volume anomaly alert** — sudden silence (>10min during uptime = pipeline dead) or flood detection. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §C.5, F.10
- [ ] **deploy.sh: lock-wait on activation contention** — Concurrent deploys (auto-git daemon) currently abort the whole pipeline after the expensive build (`Could not acquire lock`). Poll/serialize instead. Also consider a `nix run .#update` app wrapping update + `flake check --no-build` gate + switch. **Source:** `docs/status/2026-08-16_21-25_signoz-web-ui-404-fix.md` §d.2, §f.6; `22-00` §f.13
- [ ] **Flake-check guard: HTML `pat()` needles must start with `<`** — The pre-deploy phantom-metric extractor only runs at deploy; a lint-time rule prevents the `pat(*data-…*)` self-block class. **Source:** `docs/status/2026-08-16_21-25_signoz-web-ui-404-fix.md` §e, §f.7
- [ ] **Derive AUTH_VHOSTS from caddy.nix (or eval-time assert names match)** — 5/7 auth-gateway probes used hostnames that never existed (phantom SKIPs since inception). Kill the drift class by deriving expected names from config. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §c.2, §f.7
- [ ] **Centralize curl in `scripts/lib.sh` `fetch()` helper** — each script hand-rolls flags; `--compressed` had to be added call-site by call-site (and was missed once before). Migrate check scripts. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §c.3, §f.8
- [ ] **Investigate Pocket ID SQLITE_BUSY restart bursts** — transient `database is locked` around service restarts (WAL mode? provision-burst write contention?); recurring across deploys, never root-caused. Also consider requiring N occurrences before the smoke check FAILs. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §c.4, §f.9
- [ ] **Identify the residual post-deploy-check WARNs** — one "null byte" warning (~line 233, another gzip/NUL source) + quickshell 1-error-line WARN (wrong journal unit queried — re-read with correct unit) + File Renamer "0 operations" (split-brain or fresh dir?). **Source:** `docs/status/2026-08-16_22-00` §b.2, §c.5, §f.5/10/11; `19-12` §f.9-11
- [ ] **USB flap-counter metric + pre-deploy zombie-mount detector** — count JMS567 (`152d:0567`) disconnects/hour → Gatus (quantifies whether the enclosure swap helped); cross-check `findmnt` source device letters against `lsblk` in `pre-deploy-check.sh` (stale major:minor = zombie). **Source:** `docs/status/2026-08-16_19-12_buildcache-gc-prune-fix-and-nvme-decontamination.md` §f.12-13; `18-39` §f.4-5
- [ ] **buildcache-gc observability** — emit `buildcache_gc_last_success_timestamp` + `_prune_ok` metrics (this week's prune failure hid in the journal for a week); decide `--no-block` semantics for the post-switch gc start (45min worst-case deploy block); Discord alert when the watermark nukes go-build. **Source:** `docs/status/2026-08-16_19-12_buildcache-gc-prune-fix-and-nvme-decontamination.md` §b.1/3, §f.1/3/31
- [ ] **post-deploy-check: poll browser-history /health + drop deploy.sh explicit restart** — startup is FAST since storage/v4.7.0; the double-restart + 4.5-min-delay false-FAIL window is obsolete. Re-run `pre-deploy-check` end-to-end on next deploy too (validates the `--compressed` metrics scrapes). **Source:** `2026-08-16_03-44` §e-1/e-2, `22-00` §f.16
- [ ] **Add eval-time assertion for `StartLimitBurst` placement** — In systemd 261+, `StartLimitBurst`/`StartLimitIntervalSec` in `serviceConfig` (=[Service]) are SILENTLY IGNORED. This caused the 2026-08-11 WDT crash chain (592 restarts). Create `start-limit-audit.nix` (precedent: `timeout-audit.nix`, `otel-endpoint-audit.nix`). **Source:** `docs/status/archived/2026-08-11_23-28_wdt-crash-postmortem-deploy-blockers.md`
- [ ] **Fix browser-history `expires_at` session reaper error** — Every 5 min: `session reaper failed: no such column: expires_at`. SQLite sessions table missing column, migration gap in browser-history upstream. **Source:** `docs/status/archived/2026-08-12_14-17_browser-history-oidc-secret-desync-fix.md`
- [ ] **Browser-history DB backup** — `/var/lib/browser-history/data.db` (SQLite WAL) is NOT in `backup-coordination`. Periodic `sqlite3 .backup` job + entry in `configuration.nix`. Natural batch: the NVMe→pool migration item above
- [ ] **BTRFS `/data` subvolume migration** — currently toplevel (subvolid=5). Migration to `@data` would enable separate CoW semantics. Requires ~1h downtime
- [ ] **Create Attic cache + CI token** — Attic module deployed but cache not yet created. Steps: `attic cache create monitor365`, `atticadm make-token --sub ci --validity 1y --push monitor365 --pull monitor365`, configure Forgejo runner. See `docs/services/nix-binary-cache-setup.md`. Consider pushing `signoz-frontend` (122 MiB) AND the `hermes-agent` 0.20.4 build tree (huge uv2nix Python build — without a cache push the daily nixpkgs-compat CI rebuilds it from scratch). **Source:** hermes 08-21 session report §f.18
- [ ] **Enable niri blur** — Terminal transparency added but niri's blur option is NOT configured. Transparent terminals without blur are hard to read
- [ ] **Caddy reload root-cause fix** — `PrivateTmp=true` in `harden {}` blocks `systemctl reload caddy` on every deploy (exit code 4). Currently band-aided with unconditional restart in `deploy.sh`
- [ ] **Declarative health-check** — `criticalSystemServices` in `scheduled-tasks.nix` is a hand-maintained list of only 4 services. Generate from Nix config instead
- [ ] **Verify `audio.nix` WirePlumber priority rules don't fight smart-audio** — `device.restore-profile = false` + `device.profile.priority.rules` were designed for static preference; the daemon switches profiles dynamically on niri focus. They may fight on device events. Verify coexistence, then remove the superseded rules. **Source:** `docs/status/archived/2026-08-14_08-24_smart-audio-daemon-built-deployed-with-gaps.md`
- [ ] **Extend mkOidcGate with optional diagnostic output** — the oauth2-proxy refactor lost the TLS fingerprint diagnostic (`openssl x509 -fingerprint` on failure). Add optional `diagnosticMessage` parameter
- [ ] **Post-deploy-check failure semantics + escalation** — decide warn-vs-hard-fail semantics in deploy.sh; make repeated identical FAILs loud (persist a failure-streak file the next run reports). The monitor365 SKIP-gating landed; the escalation half remains. **Source:** 20-35 §f.29-30, §d.1
- [ ] **System-health collector hardening** — (1) `while IFS= read -r` instead of word-splitting `$(docker ps …)`; (2) `timeout 5` on `docker inspect`/`docker ps` (hanging Docker daemon blocks the whole collector); (3) MemoryMax 128M → 256M; (4) NRestarts triple-read → single read. **Source:** `docs/status/archived/2026-08-14_08-46_monitoring-gap-closures-self-review.md`
- [ ] **VM tests for new Gatus patterns** — Extend `tests/test-gatus-patterns.nix` with the 5 patterns added 2026-08-14. **Source:** monitoring-gap-closures report §C.9
- [ ] **Hermes periodic bump workflow** — upstream moves ~250 commits/day; the lock rots in days and release-tag pinning is an open user question (report §g.1). Once pin policy is decided: either a scheduled bump PR/workflow or a documented manual cadence + re-read of curated release notes when v0.21.0 ships (notes cover everything since v0.20.0 — check for TERMINAL_CWD/HERMES_WRITE_SAFE_ROOT/write-path behavior changes our wiring depends on). **Source:** hermes 08-21 session report §f.23 + §f.26
- [ ] **Hermes: classify live `tools.registry` journal warnings** — `check_bfl_requirements` + kanban-mode checks returning False appear in the live journal (2026-08-21); benign config state or misconfig? One-time read of the relevant upstream code paths. **Source:** hermes 08-21 session report §f.21
- [ ] **`tests/test-buildcache.nix` VM test** — mount + init + metrics emission, following `test-attic.nix`; never written since the module shipped 2026-08-14. **Source:** `docs/status/archived/2026-08-14_18-29_buildcache-ssd-setup-status.md` §f.18
- [ ] **Provisioner idempotency VM test** — runNixOSTest covering rules+policies+dashboards convergence incl. signoz-restart-mid-run simulation (the offline mock harness from the routing session was /tmp-ephemeral — commit a regression version). **Source:** `docs/status/2026-08-16_06-38_SIGNOZ-ROUTING-PERMANENT-FIX-SESSION.md` §b.4, §c.1
- [ ] **Hermes build-time import smoke test** — Flake check running `python -c "import hermes_cli.plugins"` against the sealed venv would have caught the `registration_lifecycle` py-modules drift at build time. Variant (08-21): replace the VM test's `sleep infinity` ExecStart mkForce with a cheap real-binary exec (`hermes --version`) so CI proves the venv imports at least once per run. **Source:** hermes 08-14 report §e.6 + 08-21 session report §f.13
- [ ] **ClickHouse backup before SigNoz upgrade** — Schema migrator runs on startup with no pre-upgrade backup. `clickhouse-client -q "BACKUP DATABASE signoz TO Disk('backups', 'pre-signoz-upgrade.zip')"`
- [ ] **Add Dozzle container security hardening** — missing `security_opt = ["no-new-privileges:true"]`, `cap_drop = ["ALL"]` (Docker socket access can escape to the host)
- [ ] **Standardize Docker container hardening** — no single helper for memory limits, log rotation, security options across Compose (`mkDockerService`) and `oci-containers` (`extraOptions`). Create a helper analogous to `harden {}`
- [ ] **Verify vendorHash pre-deploy check patterns at runtime** — Check #11 grep patterns not yet verified against real `nix build --dry-run` output
- [ ] **Add `--force` flag to deploy.sh for phantom metrics** — documented escape hatch instead of bypassing safety gates manually
- [ ] **Refactor discordsync to use shared gate helper** — its `waitDnsReady` probes `https://discord.com` via curl (external HTTP, not local DNS). Extend `mkDnsGate` with HTTP mode or create `mkHttpGate`
- [ ] **Move gate helpers to `lib/gates.nix`** — `lib/default.nix` is 300+ lines; gates are a cohesive unit
- [ ] **Add eval-time assertions to gate helpers** — validate `domain` non-empty, `serviceName` has no spaces
- [ ] **SearXNG streaming exploration** — progressive rendering (SSE fork, streaming proxy, or Caddy flush_buffers)
- [ ] **Pin go-cqrs-lite benchstat `rev = "master"`** — floating rev; hashes drift on every `nix flake update` (already required 2 hash fixes). Pin or drop
- [ ] **Switch root `go-cqrs-lite` flake input from `git+ssh://` to `github:`** — last remaining `git+ssh://` root input (NAR-divergence vector). Requires `sudo systemctl restart nix-daemon` if the daemon cached the ssh hash. **Source:** 16-20 §f.19, `19-01` §f.38
- [ ] **Add `file-and-image-renamer.inputs.go-nix-helpers.follows`** — the LAST Go flake input missing the follows override. **Source:** 16-20 §f.18
- [ ] **ADR: zram-only swap decision** — 28 GiB zram as ONLY swap: document the failure mode (zram full → page-cache reclaim → BTRFS I/O storms) + fallback strategy. Live metrics exist (`system_zram_*`). **Source:** 16-20 §f.22, 15-04 io-pressure analysis
- [ ] **Scoped polkit rule: service restarts without interactive auth** — allow `lars` to restart a specific unit list (browser-history, monitor365-server if re-enabled) — outages lingered partly because sessions can't `systemctl restart` without sudo. **Source:** 20-35 §e
- [ ] **Migrate `dns-blocker.nix` to the upstream dnsblockd module (split-brain convergence)** — SystemNix carries a parallel hand-rolled module while dnsblockd ships `nixosModules.default`; converge via the Monitor365/DiscordSync pattern (`imports` + `lib.mkMerge` for sops, gates, onFailure, ports, OTel). Deliberately deferred while production DNS is stable. **Source:** `docs/status/2026-08-16_00-01_DNSBLOCKD-BUILD-FIX.md` §f.6-10
- [ ] **Retire stale ZFS-VM scripts/workflow remnants** — `scripts/zfs-vm-*.sh` + `systems/zfs-vm.nix` are dead code since datapool was destroyed (2026-08-16). Review, archive or delete. **Source:** `docs/status/2026-08-16_21-24_three-drive-repurposing-execution-status.md` §f.26
- [ ] **Clean up stale qmd references in planning docs** — qmd is BACK (2026-08-20, global CLI + Crush MCP via upstream flake) but the planning docs still describe the retired port-8181 HTTP-service shape: `docs/planning/service-integration-plan.md` (276-295), `docs/crash-analysis-2026-08-11.md:143`
- [x] **PapDashboard: pre/post-deploy-check coverage** — DONE (STALE when written): post-deploy-check.sh carries the full section (`/api/health` 200, unauth `/api/ingest` → 401 = route-exists with 404/405 discriminating stale-pin/method-case, gatus ingest journal probe). Verified present ~line 456. **Source:** `docs/status/2026-08-18_15-03_smart-alerting-round3-self-review-and-full-status.md` §f.4
- [x] **Add `crm.home.lan` (enable-gated) to post-deploy external checks** — DONE 2026-08-22: `test -e /etc/systemd/system/twenty.service && check "Twenty CRM (HTTPS)" "https://crm.$DOMAIN/" "200" "<html"` in the External vHost section (banksync gate pattern). **Source:** `docs/status/2026-08-18_20-38_issue-138-twenty-v2.32-upgrade-self-review.md` §c.3/§f.3
- [ ] **sops manifest check-mode in pre-deploy-check** — run `sops-install-secrets -check-mode` (compares rendered vs. on-disk secrets) so a secrets drift/missing-key deploy fails BEFORE switch-to-configuration instead of as a runtime crash-loop (the bank-sync class). **Source:** `docs/status/2026-08-18_20-38_issue-138-twenty-v2.32-upgrade-self-review.md` §e.1/§f.4
- [ ] **Upstream OTel instrumentation for overview + PMA + phantom-telemetry detection** — both carry `OTEL_EXPORTER_OTLP_ENDPOINT` (wired) but were NEVER instrumented upstream (0 spans ever exported; the env var is a documented noop). Either add otel SDK spans to both Go services upstream or drop the env + audit the `otel-endpoint-audit` expectations registry. Plus: a tripwire alerting when a registered OTel exporter ships zero spans for N hours (phantom telemetry). **Source:** `docs/status/2026-08-18_02-38_otel-coverage-audit-buildflow-overview-pma.md`
- [ ] **PMA: commit-failure + journald-staleness Gatus checks** — the 13-53 diagnosis was blinded twice: 171 commit failures were invisible (no metric) and journald was frozen (stale logs read as absence-of-errors). Add `pma_commits_failed_total` upstream + Gatus condition, and a journal-mtime staleness probe in system-health. **Source:** `docs/status/2026-08-18_13-53_pma-commit-failures-crush-identity-and-daemon-recovery.md` §e.2-3
- [ ] **Gatus lint: HTTP methods must be UPPERCASE + synthetic ingest probe + smoke enable-gate sweep** — (1) the papdashboard `method = "post"` bug 405'd for hours because nothing validates method tokens (Go ServeMux is case-sensitive) — eval-time or pre-commit lint; (2) a synthetic authenticated POST-to-ingest probe proves the alert path end-to-end (401-only probes cannot catch method/body bugs); (3) audit remaining smoke checks for the `test -e` unit-file enable-gate pattern (the bank-sync fired-while-disabled class — the paperless fix landed, sweep the rest); (4) papdashboard ingest success-count / last-success-age metric in `system-health` — the 405 saga ran invisible because nothing counts successful ingests. **Source:** `docs/status/2026-08-18_20-52_paperless-round2-deploy-batch-papdashboard-ingest-405-saga.md` §e.3/§f.9-10
- [ ] **FastFlowLM smoke: assert model NAME in `/v1/models` + idle-check unit test** — the E2E smoke only checks HTTP 200 through the socket; a dead backend behind a live socat bridge passes (the 17-55 class). Assert the body contains `qwen3.6-moe:35b-a3b`. Plus a unit test for the idle-check math (`/proc/uptime` age + `fastflowlm@*.service` instance guard — the monotonic-timestamp bug class). **Source:** `docs/status/2026-08-18_19-56_paperless-v3-followup-fastflowlm-embed-oom-idle-fixes.md` §e.3/§f.21-22
- [ ] **deploy-window journal anchoring + retry/backoff on external HTTP checks** — post-deploy-check greps use hard-coded `--since -30min` windows that miss deploy-time entries under I/O contention (13-53 diagnosis gap) and external HTTP checks fail transiently on DNS warmup. Anchor `--since` to the NEW generation's mtime and add bounded retry (3×5s) to external probes. **Source:** `docs/status/2026-08-18_20-38_issue-138-twenty-v2.32-upgrade-self-review.md` §e.6-7
- [ ] **visionreviewd: inject `rocmEnv` into the upstream llama-server service** — `modules/nixos/services/visionreviewd.nix` sets only `package` + `llamaServer.port`; no `HSA_OVERRIDE_GFX_VERSION`/`HSA_ENABLE_SDMA`/`LD_LIBRARY_PATH` reaches the upstream llamaServer unit (system services do NOT inherit `environment.sessionVariables` — the 17-42 fix only covers interactive shells). Service disabled today = latent gap, same class as the 136-day llama-server CPU fallback. Fix via `systemd.services.<name>.environment` + `lib.mkMerge`. **Source:** `docs/status/2026-08-18_17-42_llama-server-vram-fix-rocm-session-vars.md` §F.6
- [ ] **hermes: verify ROCm env (`MemoryMax=24G` claims PyTorch+ROCm) + runtime llama-server VRAM verify** — `modules/nixos/services/hermes.nix:323` reserves 24G for "PyTorch + ROCm + HIP libraries" but carries NO `rocmEnv` (verified 2026-08-18) — if Hermes runs ROCm PyTorch it silently CPU-falls-back. Verify whether the ROCm path is live; if yes inject `rocmEnv` (+ wrapper `LD_LIBRARY_PATH` for dlopen'd libs). Same pass: runtime-verify `llama-server-rocm` (VRAM jump in `node_amdgpu_mem_info_vram_used_bytes`) and bare `llama-server` via session vars. **Source:** `docs/status/2026-08-18_17-42_llama-server-vram-fix-rocm-session-vars.md` §F.2/5/9/49
- [ ] **Add 3 gotcha entries from the fastflowlm 203-fix session** — (1) "deploy.sh from restricted-PATH shells", (2) "flat package layouts vs `lib.getExe`", (3) "phantom metric same-changeset rule". Missed by two docs-health harvests; only visible as an untouched line in the source report. **Source:** `docs/status/2026-08-17_22-55_fastflowlm-203-exec-fix-and-deploy-recovery.md` §f.10
- [ ] **dns-blocker: link `docs/dnsblockd-oidc-recovery.md` from the docs index/AGENTS.md** — the OIDC recovery runbook (secret flow, fail-closed semantics, 4 recovery paths incl. `regenerateSecretsFor`) exists but nothing links it; discoverability is zero for a future mid-incident session. **Source:** dnsblockd `docs/status/2026-08-21_23-38_OIDC-P1-HARDENING-AND-TOKEN-RETIREMENT.md` §f.21 (harvested 2026-08-22)
- [ ] **dns-blocker: consider a `dnsblockd-oidc-secret` healthcheck** — the secret-bridge oneshot fails visibly on provision errors (fail-closed), but a unit-level healthcheck would catch "Pocket ID provision ran without creating the secret" before the login page does. **Source:** dnsblockd `docs/status/2026-08-21_22-32_OIDC-SSO-CQRS-HTMX-LIBRARY-ADOPTION.md` §f.33 (harvested 2026-08-22)
- [ ] **Migrate the 4 allowlisted Gatus metrics to the anchored pat form + delete the lint allowlist** — `btrfs_scrub_error_free`, `btrfs_emergency_reserve_present`, `backup_all_healthy`, `secret_rotation_all_fresh` sit on the shrinking allowlist in the `gatus-pattern-lint` flake check (deliberate migration debt from the 2026-08-22 phantom-green fix). Not vulnerable today (HELP text doesn't embed `<name> 1`), but migrate to `[BODY] != pat(*<metric> 0\n*)` + `[BODY] == pat(*\n<metric> *)` and delete the allowlist. **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §b.2, §f.6
- [x] **Add a DAS-link-presence metric (`system_das_link_present`) + Gatus check** — DONE 2026-08-22 (commit `c121f8cf`): `dasUsbPath` option + collector detection in system-health.nix, "DAS USB Link" Gatus endpoint with anchored (real-newline) conditions + Discord alert naming the CAUSE. Deploys with the 2026-08-22 anchored-pattern fix batch. **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §e.4, §f.7
- [x] **Document Gatus `pattern.Match` semantics in AGENTS.md's Gatus section** — DONE 2026-08-22: AGENTS.md item 9 covers `!`-literal/no-glob-negation, whole-string anchoring, HELP-comment collision (phantom green), the anchored form with REAL-newline requirement, and all three lint traps; Design Patterns section carries the "escape layers" bullet. **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §d, §f.9
- [x] **Extend `scripts/pre-deploy-check.sh` section 10 (phantom metrics) to flag gatus `pat(*<metric> 1*)` conditions on HELP-emitting metrics** — DONE 2026-08-22: §10 now mirrors BOTH flake-lint traps at deploy time (bare `pat(*<metric> 1*)` phantom-green + literal `\\n` escape form), mutation-tested to fire on broken input. **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §f.10
- [ ] **Evaluate a second USB path for the two pool Toshiba members** — all 4 external disks share ONE USB link (`8-1`); one JMicron bridge flap takes out buildcache AND both pool members AND the SSDs simultaneously. The recovery stack handles flaps-with-reconnect (7 successful recoveries this week) but a drop-without-reconnect has no software answer. USER question §g.3 decides if this is actionable or documentation-only. **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §e.2, §f.11
- [ ] **Review memory-emergency-guard thresholds/cadence against the 00:27 freeze** — the guard trips on zram ≥92% + MemAvailable <10% (or <5% absolute) but the 00:27:09 freeze killed the previous boot anyway. Did it trip? If not, why not? The guard's thresholds or cadence may need review. **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §e.3, §f.12
- [x] **Write `scripts/das-link-recovery-check.sh`** — DONE 2026-08-22 (v2 same day, post-review hardening): read-only diagnostics (USB storage tree, by-id presence matrix + fstab-drift guard, zombie-mount + real-I/O probe, ext4 damage scan across current AND previous boot with printed e2fsck decision, root-fs shadow triage with scaffolding-vs-data split, NVMe fallback-cache regrowth check, unexpected-debris check incl. the doubled-path class). v2 fixed two in-script phantom greens (autofs+device-absent printed "✓ will mount on access"; current-boot-only damage scan missed the crash-boot evidence) — the exact class the sibling Gatus check was condemned for. Live-verified against the 2026-08-22 DAS-down state (14 real findings incl. 11.3 GB BuildFlow fallback caches); shellcheck-warning clean; check_mount branch-unit-tested (4/4). **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §e.5, §f.13; v2: `docs/status/2026-08-22_18-04_das-link-recovery-check-v2-hardening.md`
- [ ] **Consider gatus `alerting` dedup so N endpoints down from one root cause produce one Discord message** — the 2026-08-22 outage would have fired buildcache + pool + (future) DAS-link alerts for one physical cause. **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §f.14
- [ ] **USER: confirm btrfs balance/gc-guard stack after the pool returns** — `btrfs_health_critical 1` (unalloc 2%, 16.5 GB of 775 GB) is the documented metadata-ENOSPC precursor class. Independent of the DAS incident; needs user attention. **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §c.1, §f.15
- [ ] **Add "fetch and read the actual upstream source before writing pattern/config fixes" as an explicit verification-culture step** — the 2026-08-22 near-miss (first fix used `*!metric 0*` believing `!` was glob negation; corrected only after reading gatus source) is the canonical example. **Source:** `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md` §d, §f.16
- [ ] **Bump `file-and-image-renamer` lock subtree once go ≥ 1.26.6 is in nixpkgs** — the `go-nix-helpers.follows` is DECLARED in flake.nix (2026-08-22) but the subtree stays on its old independent pin: upstream master (`f0c00579`) now requires `go >= 1.26.6` while nixpkgs has 1.26.5 + `GOTOOLCHAIN=local` — the subtree bump fails the vendor FOD loudly (verified 2026-08-22; the go-codec "deliberate signal" class). When nixpkgs Go catches up: `GIT_CONFIG_GLOBAL=/dev/null nix flake lock --update-input file-and-image-renamer` (encodes the follows + bumps rev), then build the package before deploy
- [ ] **PapDashboard upstream: enricher LLM backoff + guard-trip circuit break (T2.4 of the stability plan)** — the 2026-08-22 freeze #2 feedback loop: every gatus alert → PapDashboard ingest → insight enricher → flm connection → 21.6 GB cold load into a zram-full machine. The guard now kills the socket (ECONNREFUSED = graceful degrade), but the enricher should still skip LLM calls while `memory_emergency_guard_last_trip_recent = 1` and back off during alert storms. Upstream change in the papdashboard repo; SystemNix side is done. **Source:** `docs/planning/2026-08-22_07-01_STABILITY-DEFENSE-IN-DEPTH-PARETO-PLAN.html` T2.4
- [ ] **Monitor-the-monitors meta-audit (T3.2 of the stability plan)** — nightly: every textfile .prom mtime < 2× its collector interval (staleness = phantom green); smoke-needle drift sweep (gatus pat() needles still match live bodies after service updates); one meta metric for Gatus. Consolidates the phantom-green kill list (pattern lint, sqlite freshness) into a single audit. **Source:** stability plan T3.2
- [ ] **zram recompression study (T3.3 of the stability plan)** — kernel zram `recompress` (zstd for huge/incompressible pages) could reclaim chunks of the 28 GB zram that sat pinned at 98.6% for 2h during freeze #2 (something held ~30 GiB — root cause still unknown; the new `system_cgroup_mem_*` census makes the next occurrence answerable). Measure before/after, document as ADR either way (there is already an ADR TODO for zram-only swap). **Source:** stability plan T3.3
- [ ] **flm upstream release watch for the v1.0.2 SIGABRT heap bug (T3.4)** — two more `double free or corruption (fasttop)` crashes documented 2026-08-22 (01:39, 02:37) at 26G peak under pressure; self-heals via restart backoff. Check FastFlowLM releases periodically; on fix: bump `pkgs/fastflowlm.nix` + re-pull weights if the manifest changes (the v1.0.2 lesson). Coredump retention is already bounded (systemd-coredump MaxUse=1G). **Source:** freeze #2 postmortem, stability plan T3.4
- [ ] **USER (physical): powered USB hub / enclosure swap + UPS evaluation (T3.5)** — all 4 external disks share one JMS567-bridge USB link that dropped live mid-write (2026-08-22 00:59); 58 unsafe shutdowns on the NVMe are a power story as much as a software one. A powered hub or per-disk enclosures remove the shared failure domain; a cheap UPS removes the shutdown class. **Source:** stability plan T3.5, `docs/status/2026-08-22_01-46_das-usb-drop-gatus-phantom-green-fix.md`

## Priority 4: Code Quality

- [x] **Run `scripts/validate-gomemlimit.sh` after next deploy** — created but NEVER executed (systemctl permission wall during authoring). First run will likely reveal grep-pattern bugs — **Done (2026-08-22):** first run found 2 bug classes: bash integer arithmetic crashing on decimal GOMEMLIMIT values and scientific-notation heap metrics (awk `printf` fixes), plus a phantom/stale SERVICES list (removed `file-and-image-renamer-server`, added papdashboard/signoz/PMA, fixed signoz-collector port 0→8888). Live run: 12 OK / 0 warnings / exit 0, shellcheck clean. MARKER comment tells future editors to register new GOMEMLIMIT services there
- [x] **Create dep-audit script for LarsArtmann Go repos** — cross-reference ALL `go.mod` require lines against flake.nix pinned revs before deploy — **Done (2026-08-22):** `scripts/audit-go-deps.sh`: flake.lock pin inventory → exact store-path sources → per-repo go.mod parse → tag/pseudo-version commit resolution (incl. the release-commit heuristic: tag commits never merged to master, compare against `tag^`) → ancestry via `git merge-base --is-ancestor` → tree-diff fallback distinguishing OK-TREE from WARN-DIVERGED. Live: 1569 OK / 7 OK-TREE / 46 WARN-DIVERGED / 452 SKIP-LOCAL / 388 INFO-UNPINNED / 0 errors. Exit 1 only on ERROR-MISSING. Pre-deploy/CI wiring PENDING owner decisions (status report 2026-08-22_05-30 Q1–Q3)
- [x] **Extract the VM-test DNS-gate `/etc/hosts` trick into `tests/test-helpers.nix`** — `networking.hosts."192.0.2.1" = [ "github.com" ]` (test-hermes.nix) makes mkDnsGate-gated units deterministic inside sandboxed builds; generalize as a reusable option/helper for every future DNS-gated VM test. **Source:** hermes 08-21 session report §f.11 — **Done (2026-08-22):** `test-helpers.dnsGateHosts` option (hostname list → TEST-NET-1 via `networking.hosts`), option docs encode WHEN to use it (gate needs resolution only) vs when NOT (unit would connect — mkForce the gate away, cf. test-oauth2-proxy). Adopted in test-hermes; flake check + VM test green
- [x] **Audit VM tests for restart-count vs `startLimitBurst` collisions** — test-hermes restarts its unit 7× under the module's default burst=5/600s (successful restarts COUNT); needed an mkForce 20. Same latent failure class in any other restart-heavy VM test (grep `systemctl("restart` across `tests/`). **Source:** hermes 08-21 session report §f.12 — **Done (2026-08-22):** full `tests/` grep — only 3 files restart/stop-start services. test-hermes: 7×, ALREADY carries `startLimitBurst = lib.mkForce 20`; test-searxng: 2 restarts vs module burst 5 (safe); test-memory-emergency-guard: stop/start pairs + the guard module itself `reset-failed`s before `start` (safe by design). ZERO unfixed collisions
- [x] **Shared `wait_for_200` helper in post-deploy-check** — the restart-race wait pattern now exists 3× (browser-history health gate, bank-sync 6×5s retry 2026-08-21, llama-rag 12×10s warmup wait 2026-08-21). Extract one helper, convert the copies, and audit the remaining one-shot checks for the same warmup-race class. **Source:** `docs/status/2026-08-21_05-30_hermes-followup-execution-session-report.md` §e.4, §f.9 — **Done (2026-08-22):** TWO helpers extracted (`wait_for_200` status readiness; `wait_body_pattern` content-signal with SIGPIPE-safe herestring grep + diagnostic body capture). All 4 actual polling loops converted (DiscordSync 3×5s, llama-rag 2×12×10s, bank-sync dashboard + errors 2×6×5s). Audit verdict: paperless uses curl-native `--retry` (fine), Monitor365 grace waits carry domain-specific verdicts (left). Verified: shellcheck clean, 6/6 isolated helper tests, full end-to-end run with all converted verdicts intact

## Priority 5: Desktop

- [x] **Niri multiscreen "Now" batch (TV-not-adjacent plan, agreed 2026-08-16)** — skip edge-crossing rebinds until a 2nd identical LG arrives. Implement now: `Mod+Tab` = `focus-monitor-next`, `Mod+Shift+Tab` = `move-column-to-monitor-next`, named-workspace jump bind, optional `move-workspace-to-monitor`, idle DPMS via swayidle (~20min; TV may be an audio sink). When 2nd LG arrives: clone DP-1 output entry + rebind `Mod+H/L`. **Source:** 2026-08-16 niri session, 03-47 status report #17 — **Done (2026-08-22):** `Mod+Tab`/`Mod+Shift+Tab`/`Mod+Ctrl+Tab` monitor cycling + `Mod+M/B/E/C/V` named-workspace jumps + 20min swayidle DPMS via `niri msg action power-off-monitors` (`sway-audio-idle-inhibit` handles the TV-as-audio-sink case). Deploy and verify hotplug behavior; when 2nd LG arrives, clone DP-1 output entry + rebind `Mod+H/L`
- [x] **Test removing `--enable-zero-copy`** — if it prevents display hotplug crashes, `--disable-gpu-watchdog` may become unnecessary — **Done (2026-08-22):** Flag removed from Helium wrapper in `base.nix`. `--disable-gpu-watchdog` retained pending observation. Deploy and observe display hotplug crash behavior; if crashes stop, try removing `--disable-gpu-watchdog` too
- [x] **Add test-tone tooling for audio debugging** — `alsa-utils` (speaker-test) or `pw-cat` in PATH. Blocks the smart-audio audibility verification (Priority 2) — **Done (2026-08-22):** `alsa-utils` (speaker-test, aplay, amixer) and `pipewire` (pw-cat, pw-play, pw-cli) added to `linuxUtilities` in `base.nix`

## Priority 6: Upstream Contributions

### nixpkgs

- [ ] **`aw-watcher-utilization` poetry-core migration** — `pkgs/aw-watcher-utilization.nix:19-24`. Add `postPatch` to nixpkgs package
- [ ] **`valkey` / `aiocache` / `timm` / `xformers` broken tests** — 4 packages with `doCheck = false`. Investigate and PR fixes
- [ ] **`taskwarrior3` build flags** — `SYSTEM_CORROSION=on` + `ENABLE_TLS_NATIVE_ROOTS=on` should be nixpkgs defaults
- [ ] **Kitty GC resilience patch** — After `nix-collect-garbage`, kitty's bundled binary lookup breaks
- [ ] **KeePassXC Chromium manifests** — nixpkgs only ships Firefox-format native messaging manifests

### Home Manager

- [ ] **Darwin user definition requirement** — HM on Darwin requires explicit `users.users.<name>.home` — tracks issue #6036

### Third-Party

- [ ] **`jscpd` lockfile** — PR upstream to publish `pnpm-lock.yaml` (the vendored lockfile needed a typo fix, `72115c62`)
- [ ] **XRT boost 1.87+ compat** — PR to `nix-amd-npu` to pin `boost187` for XRT build
- [ ] **Upstream direnv caching pattern** — fish-native mtime gate (46ms→0.7ms) + `_nix_add_gcroot` optimization
- [ ] **File btop upstream issue** — automounted disks missing in io_mode (verify-then-file per verify-before-filing skill)

### LarsArtmann Apps

- [ ] **cqrs-htmx/browser-history: registration-lock polish** — (1) frontend register form surfaces raw 403 (needs friendly "registration closed" state); (2) `browser_history_user_count` metric + Gatus bypass detector; (3) document per-process mutex limitation; (4) `slog.Warn` when `NewOAuth2Service` gets nil mutex + maxUsers > 0. **Source:** `docs/status/archived/2026-08-14_10-41_oauth2-gate-toctou-fix-self-critical-review.md`
- [ ] **dnsblockd: fix OTEL cardinality leak** — drop or bucket `dns_domain`, `http_path`, `proxy_domain` labels (each unique value = permanent in-memory time series; root cause of the 730 oomd-kills/day era)
- [ ] **DiscordSync: fix chattr ExecStartPre upstream** — push proper fix to upstream NixOS module
- [ ] **PMA daemon: stop committing broken flake.lock** — auto-commit daemon runs unscoped `nix flake update` which triggers tarball regression
- [ ] **`golangci-lint-auto-configure`: fix incomplete vendoring** — disabled in `lib/lars-packages.nix` due to local-dep (gogenfilter) vendoring gaps. Fix upstream or remove the input
- [ ] **`hermes`**: auto-create directory structure on first run; own state migration; sane `OLLAMA_API_KEY` defaults; PID-file/socket single-instance locking
- [ ] **vendor-hash drift CI for crush-daily, PMA, erraudit** — the 13-22 sweep caught 5 stale hashes across 11 repos; these 3 have no automated drift check. **Source:** 16-20 §f.25, 13-22 report
- [ ] **BuildFlow: pre-commit needs missing devShell binaries** — `go-licenses`, `tsc`, `npm`, `tailwindcss`, `vulnix`, `codespell`, `shellcheck`, `eslint` referenced by pre-commit but absent from devShell
- [ ] **picoclaw: bump `modernc.org/sqlite` v1.48.0 → v1.56.0** — v1.48 lacks mattn-compat shorthand support
- [ ] **Tag CreditReformBilanzampel + Kernovia DSN fixes** — applied but untagged; downstream pinning pending
- [ ] **Roll out go-nix-helpers `eca72e1` (templ-committed eval check) across ~20 go-standard consumers** — per-repo `nix flake lock --update-input go-nix-helpers`; decide the release vehicle (tag v1.0.0 vs master drift). **Source:** `docs/status/2026-08-16_06-45_PREVENTION-LAYERS-OTEL-AUDIT-TEMPL-ZRAM.md` §f.4, §f.9

### SystemNix docs debt

- [ ] **Annotate appendix-only ARCHIVED reports** — 11 archived 2026-08-1x files carry the generic "harvested" note but zero inline `done at` markers (authoritative list in `docs/status/archived/2026-08-12_20-52_docs-health-audit-brutal-self-review.md` §b.1/§b.2)
- [ ] **AGENTS.md compression session** — ~80KB, well over 2× the 30KB quality threshold. Dedicated session: move incident narratives to gotchas-archive, keep enduring rules
- [ ] **Triage `docs/planning/` remaining files** — decide per file: living (→ROADMAP/TODO), historical (annotate + archive), or dead
- [ ] **Annotate archived `2026-05-06_07-10` session-37 report** — one-line note: GitLab is the HaGeZi source since the 16-11 fix
- [ ] **gotchas-archive narratives missing** — (1) foreign-NixOS `/etc` absolute-symlink escape (read `/nix/var/nix/profiles` of the MOUNTED disk, not host-resolved `/etc`), (2) tar `--one-file-system` never crosses ZFS mounts (archived emptiness with exit 0), (3) WDT 2026-08-11 crash chain (StartLimitBurst-in-[Service] root cause), (4) nix-daemon oomd kill chain (socket activation + start-limit), (5) hermes outage, (6) HaGeZi GitHub-lock incident. **Sources:** `docs/status/2026-08-16_19-12_private-cloud-recovery-final-verification.md` §H6/§D.2; `docs/status/archived/2026-08-12_20-52_docs-health-audit-brutal-self-review.md` §f.6-7; `2026-08-14_15-24` §f.15-18
- [ ] **Verify README / docs/CONTRIBUTING / docs/DOMAIN_LANGUAGE freshness** — never checked in the 2026-08-12, 08-14, or 08-17 docs-health passes (always descoped). One pass: feature claims vs FEATURES.md, stale patterns, drifted terms.

## Priority 7: Long-Term

- [ ] **Provision Pi 3** for DNS failover cluster — hardware required
- [ ] **Auditd enablement** — blocked on NixOS 26.05 bug #483085
- [ ] **AppArmor enablement** — commented out in security-hardening.nix
- [ ] **Darwin Home Manager parity** — disk constrained (256GB, 90%+ full)
- [ ] **Disabled service triage** — voice-agents, minecraft, monitor365 (G7): decide enable or remove
- [ ] **Overview upstream: retry discovery** — Overview runs discovery ONCE at startup; if PMA daemon is slow, it caches nil and returns 503
- [ ] **NVMe drive replacement evaluation** — SMART healthy (11% wear) but 58 unsafe shutdowns (WDT resets) are the real risk. Consider TLC replacement, RAID1 for `/data`, or UPS
- [ ] **Deploy.sh backup retention** — timestamped `.bak` files on every deploy, never cleaned. Keep last 3
- [ ] **Re-evaluate oomd thresholds after reboot** — if 60%/30s proves insufficient, consider per-slice refinement. Watch `system_oomd_kills_total` + Twenty worker restarts
- [ ] **HaGeZi blocklist refresh workflow** — lists track the GitLab mirror `main` with SRI-hash pinning; hashes must be refreshed periodically. Consider `scripts/update-dns-blocklists.sh` or scheduled bump
- [ ] **ZFS-VM/QEMU legacy** — `pkgs/freebsd-zfs-vm.nix` + survey scripts kept for history; fold into the ZFS-scripts retirement item or delete with it
- [ ] **Proper `@home` subvolume layout** — the empty install-residue `@home` (Dec 2025, never mounted) will be deleted; ONE day set up home as a real separate subvolume (own btrbk instance, own retention, snapshot/restore without root). Not now — requires a migration window + updating `hardware-configuration.nix` + btrbk config together

---

## Deploy Verification Checklist

**All 11 items below are automated in `scripts/post-deploy-check.sh`.** Run `nix run .#post-deploy-check` after every deploy.

| #  | Item                                     | Automated Check                                                         |
| -- | ---------------------------------------- | ----------------------------------------------------------------------- |
| 1  | Post-deploy check                        | Self (the script itself)                                                |
| 2  | Pocket ID — SQLITE_BUSY/panic scan       | `journalctl -u pocket-id --since -30min` grep                           |
| 3  | SearXNG — functional search              | `curl --compressed /search?q=test` grep for `<article\|result-default`  |
| 4  | Attic cache                              | `check_local 8200`                                                      |
| 5  | BTRFS — commit=300 + fstrim              | `grep commit=300 /proc/mounts` + `systemctl is-enabled fstrim.timer`    |
| 6  | Shell — fish startup + direnv            | `date +%s%N` around `fish -i -c exit` + direnv lib check                |
| 7  | Desktop — DMS wallpaper + quickshell     | `dms ipc call wallpaper get` + `journalctl --user -u quickshell -p err` |
| 8  | Registry — nixpkgs github vs tarball     | `nix registry list \| grep nixpkgs`                                     |
| 9  | Monitor365 — auto-SKIP when disabled     | `systemctl list-unit-files 'monitor365*'` presence gate                 |
| 10 | DNS — resolution + memory                | `getent hosts` + `systemctl show -p MemoryCurrent dnsblockd`            |
| 11 | Browser History — liveness + agent timer | `check_local 8087` + `systemctl is-active browser-history-agent.timer`  |

---

_Completed work is tracked in [CHANGELOG.md](./CHANGELOG.md)._
