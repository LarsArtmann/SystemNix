# SystemNix TODO List

**Updated:** 2026-08-17 (docs-health harvest of 2026-08-16/17 reports: pool-backups + SigNoz deep-integration + nixpkgs-bump chains harvested; monitor365 outage items closed as moot — service deliberately disabled; SigNoz dashboard migration + post-deploy monitor365 gating + aw-watcher gate + KSM test closed as DONE; paperless/pool/Turso/hermes-race items added)

---

## Priority 0: Critical (Data Loss / System Risk)

- [ ] **/data corruption recovery (1.3MB, 22 extents, 3 windows ~595G + ~627-639G)** — Weekly scrub 2026-08-17 found 1,351,271 uncorrectable csum errors; aborts `btrfs send` → btrbk-data seed blocked. NVMe SMART clean (torn-write class). **User decisions:** aggressive posture (minimal fix + read-only `btrfs check --mode=low-risk` in a docker-down window), monitor365 DuckDB on /data NOT disposable (safety-copy 54G to pool archive first), reserve after triage. Plan: `docs/planning/2026-08-17_14-41_data-corruption-recovery-pool-completion-master-plan.md` (T04-T08)
- [ ] **Delete broken pool receive `@.20260814T2300` BEFORE 23:00** — The 6h-timeout kill left an incomplete receive: 403,251 `o<ino>-<handle>-<dev>` staging dirs (pnpm node_modules from ~/projects), `Received UUID: -`. Collides with tonight's btrbk resume. Delete subvol, verify @.20260812/13 intact, tonight's run re-receives 0814-0817 incrementally under the deployed 24h timeout. Plan T01
- [ ] **Google Sync go-live (user steps; module built 2026-08-17, deployed disabled)** — `services.google-sync` mirrors Google Drive → `/mnt/pool/backups/google-drive` every 5 min with a 30d deletion-grace dir + Gatus freshness via backup-coordination. Blocked on: (1) Google Cloud OAuth client (Drive API enabled, **publishing status "In production"** — testing-mode refresh tokens die after 7d), (2) `rclone authorize "drive" "<client_id> <client_secret>"` on a desktop → paste the token JSON + creds into sops `google_sync_rclone_config` (`platforms/nixos/secrets/google-sync.yaml`, needs `SOPS_AGE_KEY` from sudo per AGENTS.md Sops section), (3) flip `services.google-sync.enable = true` in configuration.nix + deploy, (4) verify first seed + `backup_healthy{google-sync}=1`. Optional later: scheduled Takeout ("Add to Drive") rides the mirror for free; immich-go can ingest takeouts into Immich. **Source:** this session, AGENTS.md "Google Sync" section
- [ ] **Off-site backup decision (3rd copy)** — **Pool safety net is LIVE (2026-08-17):** btrbk root+data sends + ALL application backups (forgejo, pocket-id, immich, twenty, manifest, paperless) land on the 2×16 TB BTRFS mirror — irreplaceables now exist on different physical disks than the NVMe. Remaining gap: the copy never leaves the house. User states important photos/docs already live in Google Photos/Drive — decide: 3-2-1 satisfied, periodic sdf (WOOACME) vault rotation, or Hetzner StorageBox + Borg (evaluated in `docs/research/hetzner-storagebox-borgbackup.md`, never executed). Flagged since 2026-06-25. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md`
- [ ] **Free root below 95% and deploy the stranded monitor365-gating change** — Root hit 95% (38G free) on 2026-08-17: the old immich copy was removed late (CoW — 14d of root snapshots still reference the extents, and the running btrbk seed is sending them), so only ~5G freed. The monitor365 backup-entry gating config sits in the working tree undeployed behind the disk gate (until then `backup_healthy{monitor365}=0` keeps `backup_all_healthy` at 0 → recurring Discord alerts). Levers: `/nix` store GC, daily 00:00 GC + snapshot expiry, `/home/hermes` (58G — see user question). **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §d.5, §f.1
- [ ] **Add `ManagedOOMPreference=omit` to dnsblockd** — dnsblockd is the sole DNS resolver on :53 and lacks oomd exemption. It was killed **730x/day** at the 50%/20s threshold. The 60%/30s raise (deployed 2026-08-14) should cut the rate, but exemption is the proper fix. Mitigations applied: MemoryMax 2G→4G, GOMEMLIMIT 3GiB. **Verified:** Aug 14 live data
- [ ] **Scrub-result Gatus coverage for /data** — The 2026-08-17 /data corruption surfaced only as a failed unit + journal line; verify `btrfs_scrub_*` metrics/Gatus wiring covers /data (and /mnt/pool), add if missing. Fail-closed pattern. Plan T12
- [ ] **Re-provision `/btrfs-emergency-reserve` + fix freshness semantics** — Service `active` but file removed (2026-08-17); old reserve extents were snapshot-pinned so deletion freed nothing. After triage: re-run service, then either periodic rewrite timer or documented pinning caveat in AGENTS.md. Plan T14
- [ ] **Run foreground BTRFS scrub on `/`** — `/dev/nvme0n1p6` (`/`) has NEVER been scrubbed. Same physical NVMe as `/data` which had 13 corrupted files. SMART says drive is healthy (11% wear, 0 media errors), but root FS corruption would be catastrophic. **Manual command:** `sudo btrfs scrub start -B /`
- [ ] **Turso plan decision — DiscordSync cloud sync DOWN since 2026-08-16** — Turso free-plan quota exhausted (`quota_exceeded … SQL read operations are forbidden`); push+pull failing, sync circuit breaker tripped. Local SQLite healthy (local-first data safe); the CLOUD OFFSITE COPY is what's stale. Decide: keep sqlite-only (then remove sync env/keys from sops + module), re-auth, or upgrade plan. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §d.1, §f.1

## Priority 1: High (Service Outages / Security)

- [ ] **Audit module-level body-parsing curls for the implicit-gzip trap** — curl 8.21+ sends `Accept-Encoding` by default; any gzip-compressing API breaks jq/grep parsing silently. Check scripts were fixed (2026-08-16), but module-level curls that PARSE bodies were never audited: `pocket-id.nix:79,284` (provisioner, parses with jq), `forgejo-repos.nix:69,90`, `_forgejo-scripts.nix:55,188,541`. Go APIs may not gzip — unverified risk, not confirmed broken. Add `--compressed` where bodies are parsed. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §b.1, §f.4
- [ ] **Hermes: investigate `Slash command sync timed out` failure** — `hermes.service: status=1/FAILURE` on 2026-08-16 deploy with `Slash command sync timed out` in the journal. Not a deploy blocker (not Required-By multi-user) but an unexplained regression. **Source:** `docs/status/2026-08-16_17-09_DEPLOY-FAILURE-BUILDCACHE-MOUNT-STALENESS-BROWSER-HISTORY-OIDC-RACE.md` queue item 2
- [ ] **Verify the browser-history registration gate is LIVE in the deployed binary** — Deployed input is `4e7604d` (storage/v4.7.0 era: async startup drain + readiness gate + paged checkpoints — startup is fast now). The registration-lock release chain had a tag-ordering break (browser-history tag `v0.5.0` was not nix-buildable; fixes committed since). Verify `POST /auth/register` returns 403 while logged-out AND a second Pocket ID first-login is rejected on the LIVE system; if the gate is still a no-op, finish the chain (tag → flake bump → deploy). **Source:** `docs/status/2026-08-15_01-44_pareto-t0-t4-outage-truth-found.md` §b.1, `2026-08-16_04-32_BROWSER-HISTORY-REPLAY-ROOT-CAUSE-STORAGE-V4.7.0-RELEASE.md`
- [ ] **Gate `import_export.go` user-import path (registration lock hole #3)** — cqrs-htmx `importUsers()` (`usermgmt/import_export.go:156`) dispatches `RegisterUserCmd` directly, bypassing both the `MaxUsers` check and the `registrationMu` lock. Admin-only (CSV upload), so lower risk than the OAuth2 bypass, but the gate is incomplete until this is closed. ~15-min fix + re-run `rg "NewRegisterUserCmd" usermgmt/*.go | grep -v test` for a 4th dispatch site. **Source:** `docs/status/archived/2026-08-14_10-41_oauth2-gate-toctou-fix-self-critical-review.md`
- [ ] **Fix forgejo-oidc-setup deploy restart race** — Recurring activation blocker: `forgejo-oidc-setup.service` runs before Caddy is up → `dial tcp 192.168.1.150:443: connection refused` → `nh os switch` exit 4 → partial activation (seen 08-13/08-14, boot-proven 08-14 20:05). Fix: `after`/`wants` on `caddy.service`, or reuse `mkOidcGate`. **Source:** `docs/status/archived/2026-08-14_13-44_hermes-registration-lifecycle-fixed-deploy-partial.md`
- [x] **immich database backup health — re-verify on pool** — Immich now serves from `/mnt/pool/services/immich` and the DB backup moved with `mediaLocation` (pool-side). Was 999h stale 08-14 (broken pg_dump timer, `backup_healthy{immich} 0`); the collector capability fix landed 08-15 — confirm `backup_healthy{immich}=1` now that paths moved. **Verified DONE 2026-08-17:** first full overnight cycle on the pool all green — `backup_healthy=1` for all seven monitored backups incl. immich-db (10-28 §a.1). **Source:** `docs/status/2026-08-17_10-28_data-corruption-triage-status.md` §a.1
- [ ] **Recreate Dozzle runtime container** — Config sets 256m memory limit but the RUNNING container has `Memory=0` (unbounded); oci-containers does not recreate on config change. Needs `docker rm -f dozzle` + redeploy (or `docker update --memory 256m dozzle` as interim). **Source:** 09-14 report annotation, 2026-08-14
- [ ] **PMA discovery daemon starvation — upstream root cause** — SystemNix mitigations deployed (MemoryHigh 12G/MemoryMax 16G, `PMA_DISCOVERY_WORKERS=2`, `pma-daemon-watchdog`). Root fix belongs upstream in projects-management-automation: bound per-worker scan memory, or make the daemon goroutine reclaim-insensitive. Also upstream: unquoted `GIT_AUTHOR_NAME=Lars Artmann` env entries (systemd drops "Artmann", journal spam; SystemNix carries quoted duplicates as workaround). **Source:** 2026-08-14 buildcache session monitor logs
- [x] **Bump `overview` flake input past `a9321f0`** — Upstream fix (StartLimit* moved to top-level/[Unit]) is committed locally in `/home/lars/projects/overview` but NOT pushed. After push: `nix flake lock --update-input overview`, deploy, then delete the stale `[Service]` StartLimit copies and the interim comment in `overview.nix`. **Verified DONE 2026-08-17:** pushed, lock at `a9321f02` (flake.lock), deployed; stale copies gone via upstream move — the retained `overview.nix` comment documents the mkForce-null trap deliberately. **Source:** 2026-08-14 buildcache session
- [ ] **Monitor365 re-enable decision (G7)** — Service deliberately disabled since 2026-08-12: the vendored `wireguard-collector` crate lives in a PRIVATE repo the Nix build can never fetch. Owner decision required: publish crate to crates.io / make repo public / vendor into the workspace — or remove the module. Its pool subvol (`services/monitor365`) is reserved. **Source:** `docs/status/2026-08-15_01-44_pareto-t0-t4-outage-truth-found.md` §g G7

## Priority 2: Manual Steps (Blocked on Human)

- [ ] **Paperless admin password handover** — Generated randomly, stored encrypted in sops (`platforms/nixos/secrets/paperless.yaml`, key `paperless_admin_password`). Log in as `admin` at `paperless.home.lan`, change the password in the UI. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §g.3
- [ ] **Browser-test the SigNoz UI + eyeball the 5 `systemnix-*` dashboards** — Data, convergence, and scrape targets are API-verified; nobody has confirmed rendering in a browser (JS bundles load, assets 200, panels lay out). Also confirms the external oauth2-proxy forward-auth path round-trips to the SPA. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §B.1, `2026-08-16_21-25` §g.1
- [ ] **Runtime-verify wf-recorder screen recording on niri (ffmpeg_8)** — Build success proven; a real recorded clip was never tested (SSH-only sessions). Low risk (nixpkgs' own default combination now). **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §c.1
- [ ] **Verify smartd is actually polling both TOSHIBA MG08 drives** — Declarative config is right (by-id, `-d sat`); runtime verification failed (NixOS renders smartd.conf elsewhere — `/etc/smartd.conf` grep missed). Check `smartctl` data timestamps / find the rendered config path. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §d.6, §f.5
- [ ] **Boot-resilience test: DAS powered off** — Boot with the pool absent must stay clean: failed-but-contained btrbk units, NO `/mnt/pool` contamination on the root fs. `nixos-rebuild build-vm` or a real boot. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §f.11
- [ ] **`e2fsck -f /dev/sdc1` (buildcache) during the next unplug window** — The fs took WRITE errors during the 08-16 flap storm and is mounted RW again (cache-only data, hash-verified consumers — acceptable, but fsck is proper hygiene). Same window: decide the JMS567 enclosure swap (physical root cause of the flap storms; ZFS drives dropped simultaneously — shared DAS power rail suspicion). **Source:** `docs/status/2026-08-16_18-39_buildcache-zombie-mount-incident-and-self-healing-deploy.md` §b, `19-12` §f.5-6
- [ ] **Satellite GOEXPERIMENT sweep (21 repos)** — they import `encoding/json/v2` but set `GOEXPERIMENT=jsonv2` nowhere in nix → devShells hard-fail for contributors without evo-x2's machine-global env (evo-x2 itself covered since 2026-08-15 via home.nix). List: `scripts/report-goexperiment-gaps.sh`. Fix pattern: dnsblockd (`nix/devshell/default.nix` + `nix/packages/default.nix` `env.GOEXPERIMENT = "jsonv2"`). **Source:** `docs/planning/2026-08-15_21-23_SMART-BUILDCACHE-OVERHAUL.md` §8
- [ ] **go-nix-helpers: default `GOEXPERIMENT=jsonv2` in template/devShell** — future repos self-carry the flag for all contributors
- [ ] **btrfs+zstd buildcache conversion (maintenance window)** — `scripts/buildcache-btrfs-convert.sh`: ~2x effective capacity + checksummed corruption detection. Requires quiesced gopls/builds + the fsType flip documented in the script
- [ ] **go-codec: align `go 1.26.6` floor with nixpkgs 1.26.5** (or wait for the nixpkgs bump) — with `GOTOOLCHAIN=local` that repo fails loudly until aligned. After aligning: `go clean -cache` NOT needed
- [ ] **Retire the direnv `use_go_env` GOEXPERIMENT sniffer** — obsolete once all satellites self-carry the flag
- [ ] **Smart-audio: verify audible output + reverse direction** — Play a test sound on the TV, switch focus to DP-1, confirm the profile switches back. PipeWire routing verified via `wpctl status` but audibility NEVER tested — flagged as a pattern failure in three consecutive reports. Needs test-tone tooling (`alsa-utils`/`pw-cat`). **Source:** `docs/status/archived/2026-08-14_08-24_smart-audio-daemon-built-deployed-with-gaps.md`
- [ ] **Hermes: install SSH deploy key** — private key to `/home/hermes/.ssh/id_ed25519`, add public key to GitHub deploy keys
- [ ] **Hermes: set fallback model** — `sudo -u hermes hermes config set fallback_model`
- [ ] **Test browser-history OAuth2 login end-to-end** — Visit `https://history.home.lan`, click "Login with Pocket ID", verify redirect flow completes and dashboard loads with data
- [ ] **Verify dnsblockd dashboard auth** — Visit `https://dnsblock.home.lan/dashboard`, enter token (retrieve via sops), confirm stats load
- [ ] **WebAuthn `.lan` RP ID browser validation** — Verify browsers accept passkey registration on `history.home.lan` (`.lan` is not a real TLD; Chrome/Firefox may reject)
- [ ] **MiniMax quota decision (carried ×4)** — MiniMax provider quota exhausted; decide: upgrade plan / pay-as-you-go / wait for reset. **Source:** 20-31 §g, 20-52 §g, 15-01-44 §g G5
- [ ] **Deploy to macOS** — Darwin registry override for nixpkgs written in config (`platforms/darwin/nix/settings.nix`) but NOT deployed. Run `nix run .#deploy` on `Lars-MacBook-Air`
- [ ] **Clean up orphaned dnsblockd tracking DB** — `/var/lib/dnsblockd/dnsblockd_tracking.db` (724 MB, last modified Jul 15) is the old database from before the rename. **Manual:** `sudo trash /var/lib/dnsblockd/dnsblockd_tracking.db`
- [ ] **BIOS fix for DAS boot hang** — GLMtec logo hang: disable USB boot, enable Fast Boot, NVMe-only boot priority. Manual BIOS change; recurs on every cold boot with the DAS connected until done
- [ ] **Hermes: bump flake input past v0.20.1 and DELETE the `registration_lifecycle` patch** — Upstream main (v0.20.1) NOW ships `registration_lifecycle` in `[tool.setuptools] py-modules`. After the input bump the SystemNix PYTHONPATH-suffix patch is a no-op hiding a second source of truth — delete it. **Source:** `docs/status/archived/2026-08-14_13-44_hermes-registration-lifecycle-fixed-deploy-partial.md`
- [ ] **`/home/hermes` is 58G on the 95%-full root — user question** — biggest single reclaim after `/nix` GC. Is that Hermes' model/state data, and may it move to `/mnt/pool` (or archive) the way immich moved? **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §g.1
- [ ] **Move Docker data-root to SSD 2 (btrfs)** — sdd (SanDisk serial 174244451713, btrfs, ~224 GiB) still awaits its Docker-storage role (`/data/docker` on QLC NVMe today). Keep SigNoz/ClickHouse volumes on `/data`. Declarative `fileSystems` entry (e.g. `/mnt/docker`), migrate, wire `virtualisation.docker.daemon.settings.data-root`. **Source:** `docs/status/2026-08-14_13-15_ssd-repurposing-options.md`
- [ ] **`/rust-cache` p9 deletion + retired-subvol cleanup — USER-RUN commands (config side DONE 2026-08-17)** — Decision: delete-only, freed 100 GiB left UNALLOCATED (p9's only neighbor is `/data` p8 — root p6 is NOT adjacent; "grow root" was never physically possible). Automounts for `@go`/`@npm`/`@cargo` removed from `snapshots.nix`, `CARGO_HOME=/mnt/buildcache/cargo` (seeded), `@cache-home` deliberately KEPT (16 GB live app caches; snapshot-exclusion is its job). **Remaining (sudo):** deploy, then `sudo btrfs subvolume delete /mnt/btrfs-root/@go @npm @cargo` (after unmount), `sudo sgdisk -d 9 /dev/nvme0n1`, `sudo rmdir /rust-cache`; restore `~/go/bin` from `/mnt/buildcache/go-bin-salvage`. **Source:** AGENTS.md "/rust-cache" entry

## Priority 3: Infrastructure

- [ ] **Monitor btrbk pool seeds → verify the nightly cycle** — `btrbk-root`/`btrbk-data` seeds were mid-send at report time (424G received; 712G+ expected root-side). Confirm: seeds complete, nightly incrementals are minutes-scale, `btrfs-verify-pool-backups` turns green (its 00:46 FAIL was a known seed-race false-positive), and the first full nightly cycle (btrbk ×3 + verify ×2 + dumps ×5 + exporter) runs clean. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §b.1, §d.4, §f.2-3, f.13
- [ ] **Remove stray `/var/lib/paperless` remnants** — The untracked-module misdeploy (upstream defaults, ~5 min live) wrote its initial DB to the wrong path. Verify the pool instance healthy first, then trash the stray dir. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §d.1, §f.4
- [ ] **restic repo on pool for app dumps (dedup)** — Forgejo full zips hit 7.4G after 2 nights (26G steady state at 7d); zips share ~0 extents so duperemove is useless. Content-addressed chunking via `pg_dump | restic backup --stdin`-style jobs onto `/mnt/pool/backups/restic/<app>` gives real retention (90d) for less space. Zero urgency at 14T free. Plan T17/backlog
- [ ] **Own-tools NVMe→pool migrations** — `discordsync` + `browser-history` state dirs → their reserved pool subvols (`/mnt/pool/services/*`; monitor365's stays reserved while disabled). Same verified-rsync + `RequiresMountsFor` pattern as immich. **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §f.9
- [ ] **Pool-usage Gatus alert (>80% warn)** — cheap while near-empty; early warning on fill-rate. Optional same pass: hd-idle spin-down for the MG08s + coarse monthly snapshots of `/mnt/pool/backups` (ransomware/typo protection). **Source:** `docs/status/2026-08-17_00-59_pool-backups-completion-status.md` §f.12, e.7
- [ ] **Commit the dashboard generator + eval-time dashboard JSON lint** — `/tmp/gen_dashboards.py` was EPHEMERAL (lost); the deterministic uuid5 pattern must live in `scripts/`. Add schema lint to pre-commit/CI (schemaVersion v6, exactly 1 query per panel, layout `$ref`s resolve, 12-col geometry) so a v1-format regression fails CI instead of warning at deploy. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §C.4, F.8; `2026-08-14_10-00` step 18
- [ ] **Test-fire "Telemetry Export Failures" → Discord** — No synthetic export failure has proven the alert → Discord delivery end-to-end (provisioning + eval health verified only). E.g. `systemctl stop clickhouse` for 60s in a maintenance window. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §B.3, F.4
- [ ] **SigNoz migrator-gap guard** — Provisioner should assert applied-migration IDs ⊆ known list per DB (the squash-gap class that silently skipped migration 1010 for months — would catch the same for logs/metrics DBs). **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §E.2, F.5
- [ ] **Recreate remaining json-file Docker containers** — Docker's default log driver is journald now (twenty-worker-1 flows enriched), but pre-change containers keep json-file until recreated — not audited which. One `--force-recreate` per stack so ALL container logs flow to SigNoz. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §B.2, F.3
- [ ] **`file_storage` cursor persistence for the journald receiver** — `start_at=end` means logs emitted during collector downtime are LOST (gap, no duplication). Persist the cursor so restarts resume. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §C.2, F.6
- [ ] **Caddy access.log ingestion (filelog receiver)** — request-level logs in SigNoz; currently only prometheus metrics from caddy. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §C.1, F.7
- [ ] **Log-ingestion-volume anomaly alert** — sudden silence (>10min during uptime = pipeline dead) or flood detection. **Source:** `docs/status/2026-08-16_23-27_signoz-deep-integration.md` §C.5, F.10
- [ ] **deploy.sh: lock-wait on activation contention** — Concurrent deploys (auto-git daemon) currently abort the whole pipeline after the expensive build (`Could not acquire lock`). Poll/serialize instead. Also consider a `nix run .#update` app wrapping update + `flake check --no-build` gate + switch. **Source:** `docs/status/2026-08-16_21-25_signoz-web-ui-404-fix.md` §d.2, §f.6; `22-00` §f.13
- [ ] **Flake-check guard: HTML `pat()` needles must start with `<`** — The pre-deploy phantom-metric extractor only runs at deploy; a lint-time rule prevents the `pat(*data-…*)` self-block class. **Source:** `docs/status/2026-08-16_21-25_signoz-web-ui-404-fix.md` §e, §f.7
- [ ] **Derive AUTH_VHOSTS from caddy.nix (or eval-time assert names match)** — 5/7 auth-gateway probes used hostnames that never existed (phantom SKIPs since inception). Kill the drift class by deriving expected names from config. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §c.2, §f.7
- [ ] **Centralize curl in `scripts/lib.sh` `fetch()` helper** — each script hand-rolls flags; `--compressed` had to be added call-site by call-site (and was missed once before). Migrate check scripts. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §c.3, §f.8
- [ ] **Investigate Pocket ID SQLITE_BUSY restart bursts** — transient `database is locked` around service restarts (WAL mode? provision-burst write contention?); recurring across deploys, never root-caused. Also consider requiring N occurrences before the smoke check FAILs. **Source:** `docs/status/2026-08-16_22-00_NIXPKGS-BUMP-RESCUE-SMOKE-CHECK-OVERHAUL.md` §c.4, §f.9
- [ ] **Identify the residual post-deploy-check WARNs** — one "null byte" warning (~line 233, another gzip/NUL source) + quickshell 1-error-line WARN (wrong journal unit queried — re-read with correct unit) + File Renamer "0 operations" (split-brain or fresh dir?). **Source:** `docs/status/2026-08-16_22-00` §b.2, §c.5, §f.5/10/11; `19-12` §f.9-11
- [ ] **USB flap-counter metric + pre-deploy zombie-mount detector** — count JMS567 (`152d:0567`) disconnects/hour → Gatus (quantifies whether the enclosure swap helped); cross-check `findmnt` source device letters against `lsblk` in `pre-deploy-check.sh` (stale major:minor = zombie). **Source:** `docs/status/2026-08-16_19-12_buildcache-gc-prune-fix-and-nvme-decontamination.md` §f.12-13; `18-39` §f.4-5
- [ ] **buildcache-gc observability** — emit `buildcache_gc_last_success_timestamp` + `_prune_ok` metrics (this week's prune failure hid in the journal for a week); decide `--no-block` semantics for the post-switch gc start (45min worst-case deploy block); Discord alert when the watermark nukes go-build. **Source:** `docs/status/2026-08-16_19-12_buildcache-gc-prune-fix-and-nvme-decontamination.md` §b.1/3, §f.1/3/31
- [ ] **post-deploy-check: poll browser-history /health + drop deploy.sh explicit restart** — startup is FAST since storage/v4.7.0; the double-restart + 4.5-min-delay false-FAIL window is obsolete. Re-run `pre-deploy-check` end-to-end on next deploy too (validates the `--compressed` metrics scrapes). **Source:** `2026-08-16_03-44` §e-1/e-2, `22-00` §f.16
- [ ] **Add eval-time assertion for `StartLimitBurst` placement** — In systemd 261+, `StartLimitBurst`/`StartLimitIntervalSec` in `serviceConfig` (=[Service]) are SILENTLY IGNORED. This caused the 2026-08-11 WDT crash chain (592 restarts). Create `start-limit-audit.nix` (precedent: `timeout-audit.nix`, `otel-endpoint-audit.nix`). **Source:** `docs/status/archived/2026-08-11_23-28_wdt-crash-postmortem-deploy-blockers.md`
- [ ] **Fix browser-history `expires_at` session reaper error** — Every 5 min: `session reaper failed: no such column: expires_at`. SQLite sessions table missing column, migration gap in browser-history upstream. **Source:** `docs/status/archived/2026-08-12_14-17_browser-history-oidc-secret-desync-fix.md`
- [ ] **Browser-history DB backup** — `/var/lib/browser-history/data.db` (SQLite WAL) is NOT in `backup-coordination`. Periodic `sqlite3 .backup` job + entry in `configuration.nix`. Natural batch: the NVMe→pool migration item above
- [ ] **BTRFS `/data` subvolume migration** — currently toplevel (subvolid=5). Migration to `@data` would enable separate CoW semantics. Requires ~1h downtime
- [ ] **Create Attic cache + CI token** — Attic module deployed but cache not yet created. Steps: `attic cache create monitor365`, `atticadm make-token --sub ci --validity 1y --push monitor365 --pull monitor365`, configure Forgejo runner. See `docs/services/nix-binary-cache-setup.md`. Consider pushing `signoz-frontend` (122 MiB) so CI/other machines skip the rebuild
- [ ] **Enable niri blur** — Terminal transparency added but niri's blur option is NOT configured. Transparent terminals without blur are hard to read
- [ ] **Caddy reload root-cause fix** — `PrivateTmp=true` in `harden {}` blocks `systemctl reload caddy` on every deploy (exit code 4). Currently band-aided with unconditional restart in `deploy.sh`
- [ ] **Declarative health-check** — `criticalSystemServices` in `scheduled-tasks.nix` is a hand-maintained list of only 4 services. Generate from Nix config instead
- [ ] **Verify `audio.nix` WirePlumber priority rules don't fight smart-audio** — `device.restore-profile = false` + `device.profile.priority.rules` were designed for static preference; the daemon switches profiles dynamically on niri focus. They may fight on device events. Verify coexistence, then remove the superseded rules. **Source:** `docs/status/archived/2026-08-14_08-24_smart-audio-daemon-built-deployed-with-gaps.md`
- [ ] **Extend mkOidcGate with optional diagnostic output** — the oauth2-proxy refactor lost the TLS fingerprint diagnostic (`openssl x509 -fingerprint` on failure). Add optional `diagnosticMessage` parameter
- [ ] **Post-deploy-check failure semantics + escalation** — decide warn-vs-hard-fail semantics in deploy.sh; make repeated identical FAILs loud (persist a failure-streak file the next run reports). The monitor365 SKIP-gating landed; the escalation half remains. **Source:** 20-35 §f.29-30, §d.1
- [ ] **System-health collector hardening** — (1) `while IFS= read -r` instead of word-splitting `$(docker ps …)`; (2) `timeout 5` on `docker inspect`/`docker ps` (hanging Docker daemon blocks the whole collector); (3) MemoryMax 128M → 256M; (4) NRestarts triple-read → single read. **Source:** `docs/status/archived/2026-08-14_08-46_monitoring-gap-closures-self-review.md`
- [ ] **VM tests for new Gatus patterns** — Extend `tests/test-gatus-patterns.nix` with the 5 patterns added 2026-08-14. **Source:** monitoring-gap-closures report §C.9
- [ ] **`tests/test-buildcache.nix` VM test** — mount + init + metrics emission, following `test-attic.nix`; never written since the module shipped 2026-08-14. **Source:** `docs/status/archived/2026-08-14_18-29_buildcache-ssd-setup-status.md` §f.18
- [ ] **Provisioner idempotency VM test** — runNixOSTest covering rules+policies+dashboards convergence incl. signoz-restart-mid-run simulation (the offline mock harness from the routing session was /tmp-ephemeral — commit a regression version). **Source:** `docs/status/2026-08-16_06-38_SIGNOZ-ROUTING-PERMANENT-FIX-SESSION.md` §b.4, §c.1
- [ ] **Hermes build-time import smoke test** — Flake check running `python -c "import hermes_cli.plugins"` against the sealed venv would have caught the `registration_lifecycle` py-modules drift at build time. **Source:** hermes 08-14 report §e.6
- [ ] **ClickHouse backup before SigNoz upgrade** — Schema migrator runs on startup with no pre-upgrade backup. `clickhouse-client -q "BACKUP DATABASE signoz TO Disk('backups', 'pre-signoz-upgrade.zip')"`
- [ ] **Add Dozzle container security hardening** — missing `security_opt = ["no-new-privileges:true"]`, `cap_drop = ["ALL"]` (Docker socket access can escape to the host)
- [ ] **Standardize Docker container hardening** — no single helper for memory limits, log rotation, security options across Compose (`mkDockerService`) and `oci-containers` (`extraOptions`). Create a helper analogous to `harden {}`
- [ ] **Verify vendorHash pre-deploy check patterns at runtime** — Check #11 grep patterns not yet verified against real `nix build --dry-run` output
- [ ] **Add `--force` flag to deploy.sh for phantom metrics** — documented escape hatch instead of bypassing safety gates manually
- [ ] **Refactor discordsync to use shared gate helper** — its `waitDnsReady` probes `https://discord.com` via curl (external HTTP, not local DNS). Extend `mkDnsGate` with HTTP mode or create `mkHttpGate`
- [ ] **Move gate helpers to `lib/gates.nix`** — `lib/default.nix` is 300+ lines; gates are a cohesive unit
- [ ] **Add eval-time assertions to gate helpers** — validate `domain` non-empty, `serviceName` has no spaces
- [ ] **SearXNG streaming exploration** — progressive rendering (SSE fork, streaming proxy, or Caddy flush_buffers)
- [ ] **Pin go-cqrs-lite benchstat `rev = "master"`** — floating rev; hashes drift on every `nix flake update` (already required 2 hash fixes). Pin or drop
- [ ] **Switch root `go-cqrs-lite` flake input from `git+ssh://` to `github:`** — last remaining `git+ssh://` root input (NAR-divergence vector). Requires `sudo systemctl restart nix-daemon` if the daemon cached the ssh hash. **Source:** 16-20 §f.19, `19-01` §f.38
- [ ] **Add `file-and-image-renamer.inputs.go-nix-helpers.follows`** — the LAST Go flake input missing the follows override. **Source:** 16-20 §f.18
- [ ] **ADR: zram-only swap decision** — 28 GiB zram as ONLY swap: document the failure mode (zram full → page-cache reclaim → BTRFS I/O storms) + fallback strategy. Live metrics exist (`system_zram_*`). **Source:** 16-20 §f.22, 15-04 io-pressure analysis
- [ ] **Scoped polkit rule: service restarts without interactive auth** — allow `lars` to restart a specific unit list (browser-history, monitor365-server if re-enabled) — outages lingered partly because sessions can't `systemctl restart` without sudo. **Source:** 20-35 §e
- [ ] **Migrate `dns-blocker.nix` to the upstream dnsblockd module (split-brain convergence)** — SystemNix carries a parallel hand-rolled module while dnsblockd ships `nixosModules.default`; converge via the Monitor365/DiscordSync pattern (`imports` + `lib.mkMerge` for sops, gates, onFailure, ports, OTel). Deliberately deferred while production DNS is stable. **Source:** `docs/status/2026-08-16_00-01_DNSBLOCKD-BUILD-FIX.md` §f.6-10
- [ ] **Retire stale ZFS-VM scripts/workflow remnants** — `scripts/zfs-vm-*.sh` + `systems/zfs-vm.nix` are dead code since datapool was destroyed (2026-08-16). Review, archive or delete. **Source:** `docs/status/2026-08-16_21-24_three-drive-repurposing-execution-status.md` §f.26
- [ ] **Clean up stale qmd references in planning docs** — `docs/service-integration-plan.md` (276-295), `docs/crash-analysis-2026-08-11.md:143`

## Priority 4: Code Quality

- [ ] **Run `scripts/validate-gomemlimit.sh` after next deploy** — created but NEVER executed (systemctl permission wall during authoring). First run will likely reveal grep-pattern bugs
- [ ] **Create dep-audit script for LarsArtmann Go repos** — cross-reference ALL `go.mod` require lines against flake.nix pinned revs before deploy

## Priority 5: Desktop

- [ ] **Niri multiscreen "Now" batch (TV-not-adjacent plan, agreed 2026-08-16)** — skip edge-crossing rebinds until a 2nd identical LG arrives. Implement now: `Mod+Tab` = `focus-monitor-next`, `Mod+Shift+Tab` = `move-column-to-monitor-next`, named-workspace jump bind, optional `move-workspace-to-monitor`, idle DPMS via swayidle (~20min; TV may be an audio sink). When 2nd LG arrives: clone DP-1 output entry + rebind `Mod+H/L`. **Source:** 2026-08-16 niri session, 03-47 status report #17
- [ ] **Test removing `--enable-zero-copy`** — if it prevents display hotplug crashes, `--disable-gpu-watchdog` may become unnecessary
- [ ] **Add test-tone tooling for audio debugging** — `alsa-utils` (speaker-test) or `pw-cat` in PATH. Blocks the smart-audio audibility verification (Priority 2)

## Priority 6: Upstream Contributions

### nixpkgs

- [ ] **`aw-watcher-utilization` poetry-core migration** — `pkgs/aw-watcher-utilization.nix:19-24`. Add `postPatch` to nixpkgs package
- [ ] **`valkey` / `aiocache` / `timm` / `xformers` broken tests** — 4 packages with `doCheck = false`. Investigate and PR fixes
- [ ] **`taskwarrior3` build flags** — `SYSTEM_CORROSION=on` + `ENABLE_TLS_NATIVE_ROOTS=on` should be nixpkgs defaults
- [ ] **Kitty GC resilience patch** — After `nix-collect-garbage`, kitty's bundled binary lookup breaks
- [ ] **KeePassXC Chromium manifests** — nixpkgs only ships Firefox-format native messaging manifests

### Home Manager

- [ ] **Darwin user definition requirement** — HM on Darwin requires explicit `users.users.<name>.home` — tracks issue #6036

### Third-Party

- [ ] **`jscpd` lockfile** — PR upstream to publish `pnpm-lock.yaml` (the vendored lockfile needed a typo fix, `72115c62`)
- [ ] **XRT boost 1.87+ compat** — PR to `nix-amd-npu` to pin `boost187` for XRT build
- [ ] **Upstream direnv caching pattern** — fish-native mtime gate (46ms→0.7ms) + `_nix_add_gcroot` optimization
- [ ] **File btop upstream issue** — automounted disks missing in io_mode (verify-then-file per verify-before-filing skill)

### LarsArtmann Apps

- [ ] **cqrs-htmx/browser-history: registration-lock polish** — (1) frontend register form surfaces raw 403 (needs friendly "registration closed" state); (2) `browser_history_user_count` metric + Gatus bypass detector; (3) document per-process mutex limitation; (4) `slog.Warn` when `NewOAuth2Service` gets nil mutex + maxUsers > 0. **Source:** `docs/status/archived/2026-08-14_10-41_oauth2-gate-toctou-fix-self-critical-review.md`
- [ ] **dnsblockd: fix OTEL cardinality leak** — drop or bucket `dns_domain`, `http_path`, `proxy_domain` labels (each unique value = permanent in-memory time series; root cause of the 730 oomd-kills/day era)
- [ ] **DiscordSync: fix chattr ExecStartPre upstream** — push proper fix to upstream NixOS module
- [ ] **PMA daemon: stop committing broken flake.lock** — auto-commit daemon runs unscoped `nix flake update` which triggers tarball regression
- [ ] **`golangci-lint-auto-configure`: fix incomplete vendoring** — disabled in `lib/lars-packages.nix` due to local-dep (gogenfilter) vendoring gaps. Fix upstream or remove the input
- [ ] **`hermes`**: auto-create directory structure on first run; own state migration; sane `OLLAMA_API_KEY` defaults; PID-file/socket single-instance locking
- [ ] **vendor-hash drift CI for crush-daily, PMA, erraudit** — the 13-22 sweep caught 5 stale hashes across 11 repos; these 3 have no automated drift check. **Source:** 16-20 §f.25, 13-22 report
- [ ] **BuildFlow: pre-commit needs missing devShell binaries** — `go-licenses`, `tsc`, `npm`, `tailwindcss`, `vulnix`, `codespell`, `shellcheck`, `eslint` referenced by pre-commit but absent from devShell
- [ ] **picoclaw: bump `modernc.org/sqlite` v1.48.0 → v1.56.0** — v1.48 lacks mattn-compat shorthand support
- [ ] **Tag CreditReformBilanzampel + Kernovia DSN fixes** — applied but untagged; downstream pinning pending
- [ ] **Roll out go-nix-helpers `eca72e1` (templ-committed eval check) across ~20 go-standard consumers** — per-repo `nix flake lock --update-input go-nix-helpers`; decide the release vehicle (tag v1.0.0 vs master drift). **Source:** `docs/status/2026-08-16_06-45_PREVENTION-LAYERS-OTEL-AUDIT-TEMPL-ZRAM.md` §f.4, §f.9

### SystemNix docs debt

- [ ] **Annotate appendix-only ARCHIVED reports** — 11 archived 2026-08-1x files carry the generic "harvested" note but zero inline `done at` markers (authoritative list in `docs/status/2026-08-12_20-52_docs-health-audit-brutal-self-review.md` §b.1/§b.2)
- [ ] **AGENTS.md compression session** — ~80KB, well over 2× the 30KB quality threshold. Dedicated session: move incident narratives to gotchas-archive, keep enduring rules
- [ ] **Triage `docs/planning/` remaining files** — decide per file: living (→ROADMAP/TODO), historical (annotate + archive), or dead
- [ ] **Annotate archived `2026-05-06_07-10` session-37 report** — one-line note: GitLab is the HaGeZi source since the 16-11 fix
- [ ] **gotchas-archive narratives missing** — (1) foreign-NixOS `/etc` absolute-symlink escape (read `/nix/var/nix/profiles` of the MOUNTED disk, not host-resolved `/etc`), (2) tar `--one-file-system` never crosses ZFS mounts (archived emptiness with exit 0), (3) WDT 2026-08-11 crash chain (StartLimitBurst-in-[Service] root cause), (4) nix-daemon oomd kill chain (socket activation + start-limit), (5) hermes outage, (6) HaGeZi GitHub-lock incident. **Sources:** `docs/status/2026-08-16_19-12_private-cloud-recovery-final-verification.md` §H6/§D.2; `docs/status/archived/2026-08-12_20-52_docs-health-audit-brutal-self-review.md` §f.6-7; `2026-08-14_15-24` §f.15-18
- [ ] **Verify README / docs/CONTRIBUTING / docs/DOMAIN_LANGUAGE freshness** — never checked in the 2026-08-12, 08-14, or 08-17 docs-health passes (always descoped). One pass: feature claims vs FEATURES.md, stale patterns, drifted terms.

## Priority 7: Long-Term

- [ ] **Provision Pi 3** for DNS failover cluster — hardware required
- [ ] **Auditd enablement** — blocked on NixOS 26.05 bug #483085
- [ ] **AppArmor enablement** — commented out in security-hardening.nix
- [ ] **Darwin Home Manager parity** — disk constrained (256GB, 90%+ full)
- [ ] **Disabled service triage** — voice-agents, minecraft, monitor365 (G7): decide enable or remove
- [ ] **Overview upstream: retry discovery** — Overview runs discovery ONCE at startup; if PMA daemon is slow, it caches nil and returns 503
- [ ] **NVMe drive replacement evaluation** — SMART healthy (11% wear) but 58 unsafe shutdowns (WDT resets) are the real risk. Consider TLC replacement, RAID1 for `/data`, or UPS
- [x] **NPU utilization** — ~~FastFlowLM + Qwen3.6-35B-A3B on the XDNA2 NPU since 2026-08-15, still hand-started (fragile) — systemd integration designed, see FastFlowLM item below.~~ DONE 2026-08-17: systemd integration shipped (`541a6a1a` + `25790607` — socket activation, no more hand-starting). Remaining exploration: ONNX Runtime / Ryzen AI SDK offloading
- [x] **FastFlowLM NPU LLM server: package + module + socket activation + 1h idle TTL** — design complete in `docs/planning/2026-08-15_19-22_fastflowlm-npu-server-systemd-integration.md`. Replaces the fragile hand-patchelf'd `~/.local` install with: `pkgs/fastflowlm` (autoPatchelf), `modules/nixos/services/fastflowlm.nix` (socket-activated `systemd-socket-proxyd` :52625 → backend :52626), idle-TTL oneshot, health metrics WITHOUT port probes. Ports 52625/52626 → `lib/ports.nix`. **DONE 2026-08-17:** module + ports verified shipped (`541a6a1a`, `25790607`); planning doc carries the EXECUTED banner. **Source:** the planning doc
- [ ] **Deploy.sh backup retention** — timestamped `.bak` files on every deploy, never cleaned. Keep last 3
- [ ] **Re-evaluate oomd thresholds after reboot** — if 60%/30s proves insufficient, consider per-slice refinement. Watch `system_oomd_kills_total` + Twenty worker restarts
- [ ] **HaGeZi blocklist refresh workflow** — lists track the GitLab mirror `main` with SRI-hash pinning; hashes must be refreshed periodically. Consider `scripts/update-dns-blocklists.sh` or scheduled bump
- [ ] **ZFS-VM/QEMU legacy** — `pkgs/freebsd-zfs-vm.nix` + survey scripts kept for history; fold into the ZFS-scripts retirement item or delete with it

---

## Deploy Verification Checklist

**All 11 items below are automated in `scripts/post-deploy-check.sh`.** Run `nix run .#post-deploy-check` after every deploy.

| # | Item | Automated Check |
|---|------|----------------|
| 1 | Post-deploy check | Self (the script itself) |
| 2 | Pocket ID — SQLITE_BUSY/panic scan | `journalctl -u pocket-id --since -30min` grep |
| 3 | SearXNG — functional search | `curl --compressed /search?q=test` grep for `<article\|result-default` |
| 4 | Attic cache | `check_local 8200` |
| 5 | BTRFS — commit=300 + fstrim | `grep commit=300 /proc/mounts` + `systemctl is-enabled fstrim.timer` |
| 6 | Shell — fish startup + direnv | `date +%s%N` around `fish -i -c exit` + direnv lib check |
| 7 | Desktop — DMS wallpaper + quickshell | `dms ipc call wallpaper get` + `journalctl --user -u quickshell -p err` |
| 8 | Registry — nixpkgs github vs tarball | `nix registry list \| grep nixpkgs` |
| 9 | Monitor365 — auto-SKIP when disabled | `systemctl list-unit-files 'monitor365*'` presence gate |
| 10 | DNS — resolution + memory | `getent hosts` + `systemctl show -p MemoryCurrent dnsblockd` |
| 11 | Browser History — liveness + agent timer | `check_local 8087` + `systemctl is-active browser-history-agent.timer` |

---

_Completed work is tracked in [CHANGELOG.md](./CHANGELOG.md)._
