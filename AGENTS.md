# SystemNix: Agent Guide

Cross-Platform Nix Configuration (macOS + NixOS) — `github:LarsArtmann/SystemNix`

---

## Architecture

```
flake.nix              # Thin entry point: inputs + flake-parts wiring (~680 lines)
systems/               # Host assembly: evo-x2.nix, darwin.nix, rpi3-dns.nix
lib/                   # Helpers — import via lib/default.nix (single import point)
  lars-packages.nix    # mkLarsPackages — single source of truth for LarsArtmann Go tools
  systemd/             # harden / serviceDefaults / serviceOneshotDefaults
modules/nixos/         # flake-parts NixOS modules, auto-discovered by filename
  services/            # Server/networking/app daemons (Caddy, Immich, SigNoz, DNS, …)
  desktop/             # Desktop-environment config (audio, display-manager, niri, steam, …)
pkgs/                  # Custom packages (buildGoModule, dms-plugins/)
overlays/              # shared.nix (callPackage + activitywatch + d2 Darwin stub), linux.nix (flake-input overlays)
platforms/common/      # Shared (~80%): home-base.nix, programs/, packages/, theme.nix, locale.nix
platforms/darwin/      # macOS (nix-darwin) — user: larsartmann
platforms/nixos/       # NixOS — user: lars
  desktop/quickshell.nix # Quickshell HM module (DankMaterialShell)
scripts/               # Shell + Python operational scripts
```

**Module auto-discovery:** `flake.nix` scans `modules/nixos/{services,desktop}/` — filenames MUST be unique across both dirs (filename → `flake.nixosModules.<name>`). `_`-prefixed files are helpers (skipped). docs/patches live in `docs/services/`, not the module tree.

| System | Hostname           | Platform       | Constraints                      |
| ------ | ------------------ | -------------- | -------------------------------- |
| macOS  | `Lars-MacBook-Air` | aarch64-darwin | 24GB RAM, 256GB SSD (90%+ full)  |
| NixOS  | `evo-x2`           | x86_64-linux   | 128GB RAM, AMD Ryzen AI Max+ 395 |

---

## Key Procedures

### Adding a Service

1. Create `modules/nixos/services/<name>.nix` (or `modules/nixos/desktop/` for desktop config) — filename IS the module name, auto-discovered. Filenames must be unique across both dirs. Prefix `_` for non-module helpers
2. Enable in `platforms/nixos/system/configuration.nix`
3. Ports go in `lib/ports.nix` — never hardcode. Caddy vHosts go in `caddy.nix` via `protectedVHost "subdomain" port`
4. Import `import ../../../lib/default.nix lib` for `harden`, `serviceDefaults`, `onFailure`, `serviceTypes`, `ports`, etc.
5. Use `harden {} // serviceDefaults {}` for systemd. **Must** set `startLimitBurst = 5; startLimitIntervalSec = 300;`
6. All vHosts in `caddy.nix`, all Homepage tiles in `homepage.nix` (guard conditional tiles with `lib.optionalString`)
7. `WatchdogSec` ONLY on services that send `WATCHDOG=1` via `sd_notify()` — Type=notify alone is NOT sufficient
8. For native OIDC SSO: add the client to `pocket-id.nix` `oidcClients` default, add a provisioning oneshot that reads the secret from `/var/lib/pocket-id/client-secrets/<clientId>` and configures the service via its CLI/API. Use a direct TLS Caddy vHost (NOT `protectedVHost`) — forward-auth + native OIDC causes double-auth loops. See Forgejo (`forgejo-oidc-setup`) as the reference pattern
9. **Add a Gatus health check** in `gatus-config.nix`. Use `mkHttpCheck` for HTTP endpoints, raw attrset for TCP/DNS checks. Add `alerts = discordAlert "..."` for any service whose failure should notify. Add `[RESPONSE_TIME] < N` conditions for user-facing services (500ms-2s depending on service). Every new service MUST be monitored — silent failures are unacceptable
10. **For OTLP tracing**: set `OTEL_EXPORTER_OTLP_ENDPOINT` in the service environment. Go services: `localhost:4318` (no scheme). Rust: `http://localhost:4317` (with scheme, gRPC). Python: `http://localhost:4318`. Docker: `http://host.docker.internal:4318`. The env var is a noop if the upstream binary lacks OTel instrumentation — see DiscordSync as the reference. Do NOT add `siteMonitor` to Homepage tiles — Gatus owns all health alerting
11. **For backup-producing services**: add the backup directory to `services.backup-coordination.backups.<name>` in `configuration.nix` with `directory`, `filePattern`, and `maxAgeHours`. Stagger schedules (01:00, 02:00, 02:30, 03:00) to avoid IO spikes

### Consuming LarsArtmann Flakes (DiscordSync/Monitor365 pattern)

When a service has an upstream LarsArtmann flake that exports `nixosModules`, **always consume the upstream module** — never hand-roll a parallel one:

```nix
{
  imports = [ inputs.X.nixosModules.default ];
  config = lib.mkIf cfg.enable {
    services.X = {
      # Override defaults with lib.mkDefault so upstream values still win
      # if they have higher priority. Use lib.mkForce only for values that
      # MUST differ (e.g. MemoryMax, startLimitBurst).
      package = lib.mkDefault inputs.X.packages.${pkgs.system}.default;
      someOption = lib.mkDefault "value";
    };
    systemd.services.X = {
      # Layer SystemNix specifics via lib.mkMerge — preserves mkDefault/mkForce
      serviceConfig = lib.mkMerge [
        { /* SystemNix-only additions */ }
        (harden { MemoryMax = lib.mkForce "2G"; })
      ];
    };
  };
}
```

**What to layer (SystemNix-only, upstream cannot provide):** sops templates, DNS-gate (`waitDnsReady`), `onFailure` alert routing, port wiring from `lib/ports.nix`, GCS/OTel env vars, activation scripts for subdir creation. **What NOT to re-declare:** `enable`, `package`, `user`, `group`, `dataDir`, `backend`, or any option upstream already declares — these arrive via `imports`.

**Fix application bugs upstream, not in SystemNix.** When a LarsArtmann service has a code-level bug (migration error, logic bug, schema drift), the fix belongs in the upstream repo (`/home/lars/projects/<repo>`) with tests, not as a local patch under `patches/` or an `overrideAttrs` hack in SystemNix. Downstream patches are reserved for build-environment problems (sandbox paths, missing dependencies). Patching logic downstream creates a hidden second source of truth, bypasses upstream tests, and makes rollback/rebuild fragile. The DiscordSync crash-loop was resolved by fixing `internal/db/backfill_nulls.go` in DiscordSync and bumping the flake input, not by maintaining a SystemNix patch.

Reference implementations: `modules/nixos/services/monitor365.nix` (gold standard), `modules/nixos/services/discordsync.nix` (converged to the pattern).

### Private Go Repos (LarsArtmann)

**GOPRIVATE setting** (`platforms/common/home-base.nix`): `privateGoPattern = "github.com/larsartmann/go-cqrs-lite,github.com/larsartmann/go-finding,github.com/larsartmann/go-structure-linter,github.com/LarsArtmann/go-commit"`. These 4 repos return 404 on `sum.golang.org` (checksum database). The Go proxy (`proxy.golang.org`) CAN serve their tagged versions, but the checksum DB can't verify them, so `GOPRIVATE` is required to skip sumdb verification. All other LarsArtmann repos are public and work fine WITHOUT GOPRIVATE.

All private repos use `git+ssh://` URLs. Go tool packages defined in `mkLarsPackages` in `flake.nix` — NOT overlays.

`mkPreparedSource` (from `go-nix-helpers`) auto-strips local replaces, normalizes pseudo-versions, generates `replace` directives. Features: `subModules` (handles `/v2` suffixes — include version in list entry, kept in path, stripped from dir), `stripLocalReplaces`, `subModuleVersionNormalize`.

**vendorHash breaking?** Set `vendorHash = ""`, build, paste `got:` hash.
**Core dep cascade?** Update dep repo first → publish tags → each consumer: `vendorHash = ""` → `nix flake lock --update-input <repo>`
**`proxyVendor = true`:** `go mod tidy` safe in both phases. **`proxyVendor = false`:** AVOID `overrideModAttrs` with `go mod tidy` — causes "inconsistent vendoring"
**Versioning:** Published = hardcode semver. Internal = `self.shortRev or self.dirtyShortRev or "dev"` for the **package version** (keeps the store-path name short, e.g. `pkg-ff1f0db`). For full-commit traceability inside the _binary_, add a separate `commit = self.rev` and pass it via ldflags (`-X main.commit=${commit}`) — NEVER use the full `self.rev` as the package `version`, it pollutes every derivation name (40-char hash in nvd/store paths)

### Quickshell (DankMaterialShell)

Quickshell is a QtQuick desktop shell replacing Waybar, Dunst, Wlogout, polkit_gnome, **and rofi** (launcher, clipboard, keybinds, emoji, calc). Configured via DankMaterialShell's upstream HM module.

- **Input:** `dankMaterialShell` (github:AvengeMedia/DankMaterialShell/stable) — brings `quickshell` transitively, no separate quickshell input
- **HM module:** `platforms/nixos/desktop/quickshell.nix` — imports DMS upstream, sets `programs.systemnix-quickshell.enable = true`, enables `systemd.enable = true` (defaults to false!)
- **DMS plugins:** `pkgs/dms-plugins/` — 13 SystemNix-native widgets + 2 community launcher plugins (dms-emoji-launcher, DankCalculator) declaratively installed via DMS's `plugins` option with port-templated settings from `lib/ports.nix`. Community plugins use `fetchFromGitHub`. Each uses `PluginComponent` + `plugin.json`
- **DevShell:** `nix develop .#quickshell` for hot-reload QML development with `qmlls` LSP
- **Wallpaper management:** DMS owns wallpapers natively. awww is RETIRED. `dms-wallpaper-init` service seeds a random wallpaper from `~/.local/share/wallpapers/` (installed from `wallpapers-src` flake input) on first launch. DMS derives cycling directory from current wallpaper's parent dir. `Mod+W` = `dms ipc call wallpaper next`. Dynamic theming (`enableDynamicTheming = false`) is DISABLED — matugen overrides Catppuccin Mocha (our global theme). Re-enable if committing to Material You dynamic colors
- **Waybar RETIRED:** Completely removed (import, package, service, scripts). DMS is the sole shell
- **Rofi migrated to DMS (2026-06-30):** niri's 5 rofi keybindings rewired to DMS IPC (`spotlight toggle`, `clipboard toggle`, `keybinds toggle niri`, `spotlight toggleQuery ":e"`, `spotlight toggleQuery "="`). Rofi leaked 7 GB and OOM-killed niri. Rofi config (`rofi.nix`) remains for Sway backup WM only. DMS service has `MemoryMax=4G` as defense-in-depth. Emoji via dms-emoji-launcher plugin (trigger `:e`), calculator via DankCalculator plugin (trigger `=`)
- **Runtime verified:** DMS owns `org.freedesktop.Notifications`, `org.gnome.ScreenSaver`, `org.kde.StatusNotifierWatcher` DBus names
- **DMS niri module:** Import `dankMaterialShell.homeModules.niri` for niri-specific integration (workspace IPC via `$NIRI_SOCKET`)
- **`inputs.nixpkgs.follows`** on the DMS input is MANDATORY — mismatched Qt causes runtime crashes

### Qmd (On-Device Markdown Search)

**Module:** `modules/nixos/services/qmd-config.nix` (filename → `services.qmd-config`, named `-config` to avoid colliding with the upstream `pkgs.qmd` attribute)

- **Input:** GitHub `tobi/qmd` (Nix-native: VCS source + real `pnpm-lock.yaml`). Packaged in `pkgs/qmd.nix` via `fetchFromGitHub` + `pnpmConfigHook` + `pnpm run build` to produce `dist/`. The npm tarball is no longer used — this avoids vendoring a regenerated lockfile.
- **Service:** `qmd mcp --http` runs as a Home Manager **user** systemd service (`qmd-mcp.service`) on `127.0.0.1:8181` so embedding/reranker GGUF models (~2 GiB) stay loaded across requests. StdIO MCP mode spawns a fresh process per connection, paying 5-15s model-reload cost on every reconnect.
- **Index location:** `~/.cache/qmd/index.sqlite` (XDG_CACHE_HOME). Models cached in `~/.cache/qmd/models/`. Config in `~/.config/qmd/index.yml`. Declarative bootstrap collections via `services.qmd-config.bootstrapCollections` option.
- **CLI:** Available system-wide from `base.nix`. Use `qmd collection add ~/notes --name notes`, `qmd search ...`, `qmd query ...`, `qmd get ...`, `qmd status`.
- **MCP integration:** HTTP mode at `http://localhost:8181/mcp` for clients (Crush, Claude Code MCP config). Any MCP client with `qmd` on PATH can also use stdio MCP via `qmd mcp` (auto-discovered by Claude Code, requires explicit Crush `mcpServers` config for stdio).
- **CPU-only by default** (`QMD_FORCE_CPU=1`) — node-llama-cpp Vulkan probing is brittle on Strix Halo and competes with Ollama for VRAM. Override `services.qmd-config.qmdForceCpu = false` + set `QMD_LLAMA_GPU` to opt in to GPU acceleration.
- **Native deps:** `better-sqlite3` builds from source via `node-gyp` during `pnpm install` (no prebuilt download in sandbox). `autoPatchelfHook` ignores missing `libvulkan.so.1` / `libcuda.so.1` / `libcudart.so.*` / `libcublas.so.*` because GPU backends are disabled by default.
- **User-service sandboxing:** `qmd-mcp` runs as a Home Manager user service. The `hardenUser` helper omits `ProtectHome`/`ProtectSystem`/`ReadWritePaths` (these are system-only in the SystemNix helper), so the service runs within the user's normal filesystem namespace. It relies on `PrivateTmp`, `MemoryMax=4G`, `NoNewPrivileges`, and `RestrictNamespaces` for containment. This is intentional: qmd must read user-owned collections and write to `~/.cache/qmd/index.sqlite` and `~/.config/qmd/index.yml`.
- **Embedding model:** Override via `services.qmd-config.qmdEmbedModel = "hf:Qwen/Qwen3-Embedding-0.6B-GGUF/Qwen3-Embedding-0.6B-Q8_0.gguf"` for multilingual (CJK) support. Re-run `qmd embed -f` after switching — vectors are not cross-compatible between models.
- **Models:** Three auto-downloaded GGUF models on first use (cached in `~/.cache/qmd/models/`): `embeddinggemma-300M-Q8_0` (300MB), `qwen3-reranker-0.6b-q8_0` (640MB), `qmd-query-expansion-1.7B-q4_k_m` (1.1GB). HuggingFace fetch on first run requires INTERNET.
- **Restart triggers:** `restartTriggers` not yet needed (no store-path content observed to affect runtime). Add if `$out/lib/qmd/node_modules/` changes cause the same `_next/static/*` 404 pattern as Homepage.
- **Gatus:** `qmd MCP HTTP Server` health check at `http://localhost:8181/health` (5min interval, Discord alert on down). Health endpoint returns uptime JSON per docs.

### Sops + Age

**Encrypting a NEW secret file** — NO sudo needed. The age PUBLIC key in `.sops.yaml` is sufficient:

```bash
echo "secret_key: $(openssl rand -base64 32)" > platforms/nixos/secrets/newservice.yaml
sops -e -i platforms/nixos/secrets/newservice.yaml
git add -f platforms/nixos/secrets/newservice.yaml  # secrets/ matches .gitignore pattern
```

**Modifying or decrypting an existing secret** — requires the age PRIVATE key (from SSH host key, needs sudo):

```bash
SOPS_AGE_KEY=$(sudo cat /etc/ssh/ssh_host_ed25519_key | ssh-to-age -private-key) sops --set '["key"] "value"' file.yaml
```

- `SOPS_AGE_KEY` in RAM only — never write age key to disk. `SOPS_AGE_SSH_PRIVATE_KEY_FILE` does NOT work with `sops` CLI
- `sops -e` (encrypt): needs only the PUBLIC key recipient from `.sops.yaml` — NO sudo. The private key is only needed at DEPLOY time (sops-nix activation script runs as root on the target host)
- `sops --set` / `sops -d` (modify/decrypt): needs `SOPS_AGE_KEY` env var with the private key — requires `sudo` to read the SSH host key
- Secrets with service-specific owners MUST be guarded with `lib.optionalAttrs config.services.X.enable` — one bad owner blocks ALL secrets atomically
- See `.crush/skills/sops-secret-management/SKILL.md` for full workflow

### Hermes

Active pip extras: `messaging`, `anthropic`, `firecrawl`, `edge-tts`, `fal`, `exa`. Do NOT add blindly — `voice` has complex native deps, `matrix` needs python-olm (Linux-only).

### SearXNG (Privacy Metasearch)

**Module:** `modules/nixos/services/searxng.nix` (wraps nixpkgs `services.searx` with package `searxng`)

- **Service:** SearXNG built-in Granian ASGI server on `127.0.0.1:${ports.searxng}` (8889). NOT uWSGI — the nixpkgs module's built-in server mode is simpler and sufficient for a homelab
- **No Redis/Valkey:** The rate limiter (`server.limiter = false`) is disabled — Redis was only used for bot detection sliding-window counters, pointless on a private LAN instance where all traffic is passlisted. Removing Redis eliminates a synchronous unix-socket round-trip on every search request
- **Secret key:** Auto-generated by `searxng-secret-key` oneshot service (`openssl rand -hex 32` → `/var/lib/searxng/searxng.env`, mode 0600, root:root). NOT sops — this is a machine-local random value, not a shared credential. systemd reads `EnvironmentFile` as PID 1, so the DynamicUser `searx` process receives the env var without file access
- **SSO:** Layer 2 (`protectedVHost`) — SearXNG has no native OIDC support. External access via oauth2-proxy forward-auth; LAN bypass open
- **Key settings:** `http_protocol_version = "1.1"` (keep-alive behind Caddy), `method = "GET"` (shareable URLs), `autocomplete = "duckduckgo"` (Yandex was slow from EU), `favicon_resolver = "duckduckgo"`, `theme_args.simple_style = "auto"` (dark mode), `query_in_title = true`, `formats = [ "html" ]` (no API surface)
- **`restartTriggers`** on `searx.service` references settings JSON + package — ensures searx restarts when config changes (the nixpkgs module only sets restartTriggers for uWSGI mode, not direct server mode)
- **Browser integration:** SearXNG is set as the default Chromium/Helium search engine via `programs.chromium.extraOpts` in `configuration.nix` (conditional on `services.searx.enable`). The `DefaultSearchProviderSuggestURL` uses SearXNG's `/autocompleter` endpoint — suggestions are proxied through SearXNG (not sent directly to Google). LAN access bypasses forward-auth, so search works without SSO prompts

### SSO / OIDC Architecture

Two SSO layers, both backed by **Pocket ID** (passkey-only OIDC IdP at `auth.<domain>`):

| Layer                                                    | How                                                                                                                                                                       | Services                                                                                         |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **Layer 1 — Native OIDC**                                | App integrates directly with Pocket ID (in-app login button). Provisioned as OIDC clients in `pocket-id.nix`; Caddy uses **plain `reverse_proxy`** (NOT `protectedVHost`) | Forgejo, Immich, **Gatus**                                                                       |
| **Layer 2 — oauth2-proxy forward-auth**                  | App has no native auth; Caddy `protectedVHost` gates external access behind a Pocket ID login. LAN access is open                                                         | Homepage, Twenty, Taskchampion, Manifest, OpenSEO†, Crush Daily, Dozzle, Monitor365, **SearXNG** |
| **Layer 2+ — oauth2-proxy forward-auth (unconditional)** | App internal auth disabled (impersonation mode); Caddy applies `forwardAuth` to ALL requests — no LAN bypass. Pocket ID is the sole auth boundary                         | **SigNoz**                                                                                       |

> **†** OpenSEO uses a **hand-rolled Caddy vHost** (not `protectedVHost`) to exempt `/api/gsc/oauth/callback` from forward-auth — see gotcha table. All other paths follow standard Layer 2 behavior (forward-auth for external clients, LAN bypass).

**Adding Layer 1 (native OIDC) to a service** — follow the immich/gatus pattern:

1. Register the OIDC client in `pocket-id.nix` `provision.oidcClients` (clientId, callbackURLs)
2. The provisioner writes the client secret to `/var/lib/pocket-id/client-secrets/<clientId>` (owned `pocket-id:pocket-id`, 640)
3. The service reads it: either via upstream `_secret` (immich), a runtime script `cat` (forgejo), or systemd `LoadCredential` (gatus — needed because gatus is a **DynamicUser** that can't own files)
4. Order the service `after`/`wants` `pocket-id-provision.service`
5. **In Caddy, use plain `reverse_proxy`** (like Forgejo/Gatus), NOT `protectedVHost` — a service with native OIDC behind `protectedVHost` causes a **double-auth** conflict

**Native OIDC is NOT free for most services** — verify upstream support before assuming:

- Homepage: no built-in auth at all (proxy-only by design)
- SigNoz: OIDC is Enterprise-only ($4k/mo). Uses impersonation mode (no internal auth) + unconditional Caddy forward-auth — Pocket ID is the sole auth boundary, no LAN bypass
- Twenty: SSO gated behind a billing entitlement
- Custom LarsArtmann Go services: require upstream OIDC code in their repos

**Single Logout (SLO) is partial, not coordinated.** Layer 2 apps share the oauth2-proxy session cookie (`.${domain}`) — logging out via oauth2-proxy's `/oauth2/sign_out` clears them together. Layer 1 apps (Forgejo, Immich, Gatus) each keep their **own** session cookie and do NOT participate in coordinated logout — visiting them after an IdP logout may still show the cached app session until it expires or the user explicitly logs out per-app. Pocket ID supports RP-initiated logout, but wiring it into every Layer 1 app's logout flow is per-app work and not currently done.

### BTRFS (evo-x2)

**Subvolume layout:** `/` mounts `@` (root). `/nix` and `/home` live INSIDE `@` — NOT separate `@nix`/`@home` subvolumes (wikis recommend flat layout; deferred to next reinstall). `/data` is a separate BTRFS partition (subvolid=5, toplevel). Cache dirs (`@cache-home`, `@go`, `@npm`, `@cargo`) are separate top-level subvolumes with automount. Rust `target/` dirs live on ext4 (`/rust-cache`) to avoid COW fragmentation.

**Snapshots:** Root (`@`) daily via btrbk at 23:00, `/data` (toplevel) daily at 23:30, both 14d+4w retention. Staggered BEFORE nix-gc (00:00) so expired snapshots free extents before GC runs. Snapshot freshness verified daily (alerts if >3 days old). `/mnt/btrfs-root` (subvolid=5) automounted for btrbk access.

**No remote backup:** All snapshots are LOCAL-ONLY. If the NVMe fails, everything is lost. This is the #1 data loss risk (flagged since 2026-06-25).

**Compression:** `compress=zstd` on `/` (filesystem-wide — covers all `@` subvolumes). `/data` uses `compress=zstd:3`. NOT `compress-force` (against upstream Btrfs guidelines). BTRFS compression is filesystem-wide: setting it on any mount applies to ALL subvolumes on that filesystem. Only `subvol`/`subvolid` and VFS options like `noatime` are per-mount-point.

**Scrub:** `autoScrub` monthly on `/` and `/data`. Scrub results collected as Prometheus metrics (`btrfs_scrub_errors_total`, `btrfs_scrub_status`, `btrfs_scrub_error_free`) via `btrfs-health-metrics` every 5 min. Gatus alerts on Discord when `btrfs_scrub_error_free` drops to 0 (errors found).

**TRIM:** `services.fstrim.enable = true` (periodic). All BTRFS/ext4 mounts explicitly set `nodiscard` — QLC NAND causes 253ms discard latency → BTRFS commit stalls → WDT reset. `fstrim.timer` handles weekly TRIM without competing with host I/O.

**bees dedup:** NOT recommended on this hardware. bees does random 4KB reads across the entire filesystem to hash blocks — QLC NAND random IO sensitivity makes this counterproductive. `auto-optimise-store` (whole-file hardlink dedup) captures the biggest win with zero extra IO.

**BTRFS quotas (qgroups):** NOT enabled. `btrfs quota enable` adds metadata overhead to every transaction for qgroup accounting. On QLC NAND, this I/O tax is not worth per-subvolume usage tracking. If the NVMe is upgraded to TLC/MLC, enable quotas and re-add the qgroup metrics (removed from `btrfs-health.nix` — search git history for `btrfs_qgroup_referenced_bytes`).

**Scrub monitoring:** `btrfs-health-metrics` (every 5 min) collects `btrfs_scrub_errors_total`, `btrfs_scrub_status`, `btrfs_scrub_duration_seconds`, and `btrfs_scrub_error_free` via `btrfs scrub status`. Requires `CAP_SYS_ADMIN` (the kernel's `btrfs_ioctl_scrub_progress` checks `capable(CAP_SYS_ADMIN)`). The service overrides `harden {}`'s empty `CapabilityBoundingSet` with `CAP_SYS_ADMIN`. Gatus "BTRFS Scrub Health" endpoint alerts on Discord when `btrfs_scrub_error_free` drops to 0.

**Balance (chunk consolidation):** Weekly automated balance in `btrfs-health.nix` prevents the 2026-06-26 metadata ENOSPC crash mode. Metadata balance (`-musage=50`) runs Mon 04:00, data balance (`-dusage=50 -dlimit=10`) runs Mon 05:00 — staggered after metadata (which is fast) and before nix-gc (00:00 next day). Both are guarded by `btrfs-chunk-check`: skip if a balance is already running, skip if device-unallocated < 5 GiB (metadata) or < 10 GiB (data) bounce room. Data balance is bounded (`-dlimit=10` = max 10 chunks per run, ~10 GiB) to prevent runaway IO. Both use `ProtectSystem = false` (balance ioctl needs raw filesystem access) + `CAP_SYS_ADMIN`.

**Emergency reserve:** A 10 GiB `fallocate`d file at `/btrfs-emergency-reserve`, created on boot by `btrfs-emergency-reserve.service`. Delete it (`rm /btrfs-emergency-reserve`) for instant 10 GiB free space when you need to run balance, repair, or survive metadata ENOSPC. The file is NOT auto-recreated after deletion — manually re-provision with `sudo systemctl start btrfs-emergency-reserve` after recovery. Tracked via Prometheus metrics (`btrfs_emergency_reserve_present`, `btrfs_emergency_reserve_bytes`) and Gatus alerts on Discord if the reserve goes missing.

---

## Critical Rules

- **Use flake commands** — `nix run .#deploy`, never raw `nixos-rebuild`/`darwin-rebuild`
- **Test first** — `nix flake check --no-build` (syntax) or `nix eval .#nixosConfigurations.evo-x2.config.system.build.toplevel` (eval)
- **`trash` not `rm`**, **`git mv` not `mv`**, **2-space indentation**, **`config.allowBroken = false`**, **No OpenZFS on macOS** (kernel panics, ADR-003)
- **Open new terminal** after deploy (shell changes need new session)
- **Never hardcode** `localhost:PORT` — derive from config. All ports in `lib/ports.nix`, all images in `lib/images.nix`

---

## Non-Obvious Gotchas

| Issue                                                                                                   | Why It Matters                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lib.mkMerge` + flake-parts                                                                             | `mkMerge` on top-level `config` in flake-parts modules does not work — use inline config or imports. However, `mkMerge` on `serviceConfig` inside `systemd.services.<name>` IS safe and correct — it's a local attrset value, not a flake-parts config merge. All serviceConfig `//` chains have been converted to `lib.mkMerge [...]` to preserve `mkDefault`/`mkForce` priority annotations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| d2 Darwin overlay                                                                                       | Re-instantiates d2 with stubs; removing it breaks Darwin eval                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Niri `BindsTo` → `Wants=`                                                                               | `BindsTo` kills niri on deploy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DMS owns wallpaper management                                                                           | awww is RETIRED. DMS manages wallpapers natively (`dms ipc call wallpaper set/next/prev`). `dms-wallpaper-init` service seeds a random wallpaper from `~/.local/share/wallpapers/` on first launch. DMS derives cycling directory from the current wallpaper's parent dir. `Mod+W` = `dms ipc call wallpaper next`. `enableDynamicTheming = false` — matugen conflicts with Catppuccin Mocha                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DNS `dnsIPv6Enabled = false`                                                                            | evo-x2 has no global IPv6 — dnsblockd's `dns_ipv6_enabled: false` omits IPv6 root servers from the sdns config, avoiding 5-15s timeouts on uncached queries (matches Unbound's former `do-ip6 = false`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| otel-tui on Darwin                                                                                      | Never add — 40+ min builds + disk exhaustion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Darwin HM user                                                                                          | `users.users.larsartmann.home` required in `platforms/darwin/default.nix`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Pocket ID bootstrap                                                                                     | `pocket-id-config.provision.enable = true` — creates admin + clients automatically. Only manual step: register passkey at `/setup`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Caddy `handle_path`                                                                                     | STRIPS prefix before proxying. Use `handle` when backend expects full path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Caddy `commonConfig` snippet                                                                            | All vhosts MUST include `${commonConfig}` — provides security headers (HSTS, nosniff, frame-options), compression (zstd/gzip), and `-Server` header suppression. Applied via `protectedVHost` automatically; manual vhosts (auth, forgejo, status) must include it explicitly                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Caddy `auto_https off` + HTTP redirect                                                                  | `auto_https off` means Caddy won't auto-redirect HTTP→HTTPS. The `:80` catch-all vhost handles this. TLS certs are sops-managed (`dnsblockd_server_cert`/`dnsblockd_server_key`), not ACME                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Caddy TLS protocols                                                                                     | `tlsConfig` enforces TLS 1.2+ (drops 1.0/1.1). `strict_sni_host on` in `servers` block prevents serving certs for unrecognized hostnames                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Caddy admin API (port 2019)                                                                             | **Intentionally left unauthenticated.** Single-admin homelab: port 2019 is firewalled out (only 80/443 open), all services run under systemd `harden {}`. Risk requires local process compromise — at which point Caddy config injection is irrelevant. `admin off` would also kill `/metrics` (SigNoz + Gatus depend on it). If hardening is ever needed: use `admin off` + separate `:2019 { metrics }` site block, NOT `admin off` alone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Gatus endpoint convention                                                                               | Every application endpoint MUST have: (1) a Discord alert, (2) `[RESPONSE_TIME] < N` condition for HTTP services. Metrics-scrape endpoints (node exporter, cAdvisor) are exempt from response-time checks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Dozzle module eval                                                                                      | `modules/nixos/services/dozzle.nix` with options breaks `nix flake check`. Use inline `virtualisation.oci-containers`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `signoz.target`                                                                                         | SigNoz/ClickHouse use custom `signoz.target` (NOT `multi-user.target`) — all SigNoz components use `wantedBy = ["signoz.target"]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `svcEnabled` helper                                                                                     | In `sops.nix`, use `svcEnabled "name"` — safer than `config.services.X.enable` (rpi3 doesn't import all modules)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| sops secret owners                                                                                      | Non-existent user/group blocks ALL secrets atomically. Guard with `lib.optionalAttrs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `harden` vs `hardenUser`                                                                                | User services (systemd --user) must use `hardenUser`. Desktop notify services pass `hardenFn = hardenUser`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| OOM crash chain                                                                                         | Helium/Electron renderers grow unbounded in `user-1000.slice` → journald starved → sp5100-tco WDT hard reset (60s). This is WHY `user-${uid}.slice` has `MemoryHigh=56G; MemoryMax=64G` in `boot.nix`, oomd is tuned to 50%/20s, and per-service MemoryMax alone is insufficient (user processes run outside it)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| MGLRU thrashing prevention                                                                              | `min_ttl_ms=1000` set via `mglru-thrash-protection.service` in `boot.nix`. Protects youngest page generation from eviction for 1s under pressure — prevents the thrash spiral that starves journald. Sysfs-only (`/sys/kernel/mm/lru_gen/`), cannot use `boot.kernel.sysctl`. Compiled in (`0x0007`) but defaults to 0 (disabled)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Docker services target                                                                                  | All Docker/container services use `multi-user.target` (NOT `graphical.target`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `harden` ExecStart trap                                                                                 | `harden {}` now passes through extra args, but NEVER put `ExecStart`/`Type`/`RemainAfterExit` inside it — merge with `//` outside instead. The `harden` function only processes named hardening params; extras go to `passthrough`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `runuser` + `harden {}` = PAM failure                                                                   | `runuser` uses PAM for session setup. In a hardened service (`harden {}`), PAM cannot complete session init — `runuser: cannot open session: Permission denied` — even with `CAP_SETGID CAP_SETUID` and `NoNewPrivileges = false`. The PAM `pam_limits` module also fails on `memlock`. **Fix:** run the service as the target user directly (`User = "forgejo"`) and add a passthrough `runuser() { shift 2; shift; "$@"; }` shell function so the script doesn't need rewriting. This eliminates PAM entirely and is more secure (no capabilities needed). See `forgejo-oidc-setup` for the pattern                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ext4 `discard=async`                                                                                    | **BTRFS-only mount option.** ext4 uses bare `discard` (boolean). `discard=async` on ext4 → `fsconfig() failed` → mount fails → `local-fs.target` fails → **emergency shell**. Caused the 2026-06-23 boot emergency                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Non-`nofail` mounts = boot hazard                                                                       | Any `fileSystems` entry without `nofail` that fails to mount brings down `local-fs.target` → emergency shell. ALWAYS add `nofail` for non-root mounts (cache, data, etc.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `oci-containers` backend defaults to Podman                                                             | `virtualisation.oci-containers.backend` defaults to `"podman"`, silently pulling in a full Podman daemon alongside Docker. Set `backend = "docker"` when Docker is already enabled                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Docker 29.x `userland-proxy-path`                                                                       | Docker 29.x moved `docker-proxy` to the internal moby derivation, which nixpkgs doesn't expose. Daemon fails with "invalid userland-proxy-path". Fix: `daemon.settings.userland-proxy = false`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Docker containerd bbolt corruption                                                                      | OOM/hard reset corrupts `/data/docker/containerd/daemon/io.containerd.metadata.v1.bolt/meta.db`. Recovery: stop docker → `mv meta.db meta.db.bak` → remove `containers/`, `containerd/`, `network/` dirs → restart. Preserves volumes/images                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| dnsblockd + `ProtectSystem=strict`                                                                      | SQLite needs a writable CWD. Set `WorkingDirectory = "/var/lib/dnsblockd"` alongside `StateDirectory` or SQLite CANTOPEN errors                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `mkFilesystem` helper                                                                                   | `lib/filesystems.nix` validates mount options at eval time. Use it instead of raw `fileSystems` attrsets to catch cross-fs contamination (e.g. `discard=async` on ext4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Pre-deploy validation                                                                                   | `nix run .#pre-deploy-check` catches boot-breaking issues before switch. Runs automatically as part of `nix run .#deploy`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DMS `inputs.nixpkgs.follows`                                                                            | MANDATORY — mismatched Qt versions between DMS/quickshell and system nixpkgs cause silent runtime crashes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DMS notification conflict                                                                               | Only one DBus notification daemon can run. Dunst is disabled (`services.dunst.enable = lib.mkForce false`) — DMS owns `org.freedesktop.Notifications`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Quickshell pre-1.0                                                                                      | Breaking changes expected before 1.0. Pin DMS to `stable` branch. Migration guides promised                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| QML is programming                                                                                      | Quickshell configs are applications, not config files. Requires QML knowledge. Use `nix develop .#quickshell` for LSP + hot-reload                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `nixpkgs-unstable` vs `nixos-unstable`                                                                  | **Use `nixos-unstable` in flake inputs.** Hydra only caches expensive builds (ROCm, CUDA) on the `nixos-unstable` jobset — `nixpkgs-unstable` produces different hashes with no binary cache for these. `ollama-rocm` was a 30+ min local build on `nixpkgs-unstable` but substitutes instantly from `nixos-unstable`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DMS `systemd.enable` defaults false                                                                     | DMS HM module's `systemd.enable` defaults to `false`. MUST explicitly set `programs.dank-material-shell.systemd.enable = true` or DMS won't auto-start                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DMS plugin system                                                                                       | DMS `plugins` option takes `attrsOf (submodule { enable, src, settings })`. `src` = path/package, `settings` = JSON written to `plugin_settings.json`. Plugins read settings via `pluginData.<key>` in QML                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| deploy.sh `SCRIPT_DIR`                                                                                  | Deploy script in nix store can't find `pre-deploy-check.sh` via `SCRIPT_DIR`. Fixed to use `nix run .#pre-deploy-check` instead                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `switch-to-configuration` exit code 4                                                                   | Activation fails with `ExitStatus(Exited(4))` when a service whose unit definition changed is in `start-limit-hit` state — systemd refuses to restart it. **deploy.sh now runs `systemctl reset-failed` (system + user) before `nh os switch`** to clear the start-limit counter. Without this, any service that crash-looped at boot blocks ALL deploys until manually reset or rebooted. `nh` also swallows the per-unit error — run `sudo /run/current-system/bin/switch-to-configuration test` directly to see which unit failed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| pre-deploy-check `((PASS++))`                                                                           | Bash `((0++))` returns exit code 1, killing scripts under `set -e`. Fixed with `PASS=$((PASS + 1))`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DMS Quickshell `Process.onFailed`                                                                       | Quickshell's `Process` has NO `onFailed` signal. Use `onStreamFinished` with text-length check or try/catch for error handling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Stale polkit-gnome after deploy                                                                         | polkit-gnome process lingers from previous generation after removing it from packages. Self-resolves on reboot. DMS polkit agent warns "already exists" until then                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| DMS `plugin_settings.json` read-only                                                                    | HM symlinks this to Nix store — user can't change plugin settings via DMS UI. Settings are declarative from `programs.dank-material-shell.plugins.<name>.settings`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| cliphist service RETIRED                                                                                | cliphist (wl-paste --watch) was retired when Alt+C migrated to DMS clipboard modal. DMS owns clipboard history exclusively. cliphist CLI remains in base.nix for manual use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Rofi OOM crash (2026-06-30)                                                                             | Rofi leaked to 7 GB + 2.3 GB swap over 5h22m, triggering global OOM that killed niri, ghostty, signal, pipewire, unbound, clickhouse, immich. Root cause: long-lived `-dmenu`/`-drun` instances leaking. Fix: all 5 niri rofi bindings migrated to DMS spotlight/clipboard/keybinds IPC. DMS has MemoryMax=4G. Rofi remains for Sway backup WM only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DMS `settings.json` vs `plugin_settings.json`                                                           | **Split-brain:** `settings.json` is user-owned/mutable (bar layout, theme, lock config). `plugin_settings.json` is Nix-managed/read-only symlink (plugin URLs, ports). DMS UI changes to plugin settings silently disappear on rebuild. Document this tradeoff                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `find -L` for Nix store symlinks                                                                        | `find` does NOT follow starting-point symlinks by default. Wallpaper directories installed via HM symlinks need `find -L "$dir"` or trailing slash. Without `-L`, find silently returns nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `serviceOneshotDefaults` for oneshot services                                                           | `serviceDefaults`/`serviceDefaultsUser` default to `Restart=always` which is INVALID for `Type=oneshot` — systemd refuses to start. Use `serviceOneshotDefaults` (system) or `serviceOneshotDefaultsUser` (HM user) which default to `Restart=no`. Override to `on-failure` if retry-on-error is desired                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `%h` vs `$HOME` in ExecStart                                                                            | systemd user services may NOT expand `$HOME` in ExecStart (especially hardened services). Use `%h` (systemd specifier) instead, which always resolves to the user's home directory                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `/nix/var/nix/builds` stale sandboxes                                                                   | OOM crashes / hard resets leave orphaned build sandboxes in `/nix/var/nix/builds/` that can accumulate to 100+ GB. The `nix-build-cleanup` timer (every 4h + on boot) cleans dirs untouched >1h, but BTRFS snapshots may hold references — space isn't freed until snapshots expire (14d retention). Manual: `sudo rm -rf /nix/var/nix/builds/nix-*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| BTRFS CoW + snapshots block space reclamation                                                           | `rm` on BTRFS doesn't immediately free space when snapshots reference the data. After deleting large files/dirs, check `btrfs filesystem df /` — actual reclamation happens as btrbk snapshots expire. Don't assume `df` reflects freed space instantly                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| BTRFS compression is filesystem-wide                                                                    | `compress=zstd` set on any mount applies to ALL subvolumes on that BTRFS filesystem. Only `subvol`/`subvolid` and VFS options like `noatime` are per-mount-point. Setting `compress=zstd` on `/` already covers `/nix`, `/home`, and all other `@` subvolumes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| BTRFS `/nix` not a separate subvolume                                                                   | `/nix` lives inside `@` (root subvolume), NOT as its own `@nix`. Consequence: btrbk snapshots of `@` include the full nix store (CoW means only changed blocks, but still larger than necessary). Cannot do erase-your-darlings without destroying `/nix`. Wikis recommend flat `@nix` as a top-level subvolume. Deferred to next reinstall                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| BTRFS scrub has no alerting                                                                             | `autoScrub` runs monthly on `/` and `/data` but results are NOT monitored or alerted. If scrub finds csum errors, nobody knows. Check manually: `btrfs scrub status /`. TODO: add scrub status to Prometheus metrics + Gatus Discord alert                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| PocketID client-secret file desync                                                                      | `pocket-id-provision` had a migration block that seeded `client-secrets/<id>` from the OLD sops secret, then the skip-if-exists check (`if [ -f ] && [ -s ]`) prevented calling `POST /secret` to generate the correct value. Since `POST /api/oidc/clients/{id}/secret` ALWAYS generates a NEW secret (rotating the old), and `POST /api/oidc/clients` does NOT auto-generate one, the DB had null/stale while the file had old sops → `401 invalid client secret` at token exchange. **Recovery (order matters):** (1) `sudo rm /var/lib/pocket-id/client-secrets/<id>`, (2) `sudo systemctl RESTART pocket-id-provision.service` (NOT `start` — `RemainAfterExit=true` makes `start` a no-op on already-active service), (3) `sudo systemctl reset-failed <consumer>.service && sudo systemctl start <consumer>.service`. **NEVER restart the consumer before provision completes** — `LoadCredential` makes the file load-bearing at service start; missing file = instant crash-loop (`status=243/CREDENTIALS`). Migration block now removed from provision script. **NEW declarative recovery:** set `pocket-id-config.provision.regenerateSecretsFor = [ "clientId" ]` in configuration.nix, deploy, then clear the list                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| oauth2-proxy whitelist-domain REQUIRED (FIXED 2026-07-22)                                               | Without `--whitelist-domain=.${domain}`, oauth2-proxy rejects the post-login redirect back to the originating vHost with `domain / port not in whitelist`. The user sees a 500 on `/oauth2/callback` because oauth2-proxy can't redirect to `https://signoz.home.lan/`. ALL Layer 2/Layer 2+ vhosts are affected (they all use the same oauth2-proxy instance). The `whitelist-domain` is set in `extraConfig` as `[ ".${domain}" ]` (dot-prefixed wildcard matches all subdomains)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| oauth2-proxy partOf pocket-id-provision (FIXED 2026-07-22)                                              | oauth2-proxy loads its client secret via systemd `LoadCredential` at process start time. When `pocket-id-provision` regenerates the secret (e.g. after desync recovery via `regenerateSecretsFor`), the running oauth2-proxy process still holds the OLD credential in memory — token exchanges fail with `invalid_client` until the process restarts. The `partOf = [ "pocket-id-provision.service" ]` dependency ensures oauth2-proxy is restarted whenever provision runs, so the new credential is always loaded. Without this, a deploy can regenerate the secret file without restarting oauth2-proxy, leaving auth broken until the next deploy that happens to restart it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| BTRFS metadata ENOSPC crash (2026-06-26)                                                                | Nightly `nix-gc` timer fires metadata transactions on a filesystem with zero device-unallocated space → I/O deadlock → WDT reset. `df` reports Data-pool free space (statfs), NOT chunk-level allocation — the entire monitoring stack was blind. **Fix:** `btrfs-health.nix` gates `nix-gc` + `nix-build-cleanup` via `ExecStartPre` guard (aborts when device-unallocated < 10%), collects Prometheus metrics every 5 min, Gatus sends Discord alerts, DMS widget shows device-unallocated %. btrbk staggered to 23:00 (before GC at 00:00) so expired snapshots free extents before GC runs. Recovery for acute ENOSPC: grow partition (sfdisk → partx → btrfs resize), `rm /btrfs-emergency-reserve` for instant 10 GiB, or `btrfs balance start -musage=50 /` if unallocated permits. See `docs/troubleshooting/btrfs-metadata-enospc-recovery.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DiscordSync always-on API server                                                                        | Upstream (go-cqrs-lite v3) ALWAYS starts an HTTP API (`/metrics`, `/api/events/stream`, `/api/export`) — no flag to disable. Defaults to `:8080` which **conflicts with SigNoz**. SystemNix overrides `apiAddr` to `127.0.0.1:8085` (localhost-only) via `lib.mkDefault`. GCS attachment backup is opt-in: set `services.discordsync.gcsBucket` + add `discordsync_gcs_credentials` (service account JSON) to `discordsync.yaml` sops file. DiscordSync now consumes upstream `nixosModules.default` (Monitor365 gold-standard pattern) — the old 160-line hand-rolled module was replaced with a thin wrapper that layers SystemNix specifics (DNS-gate, sops, onFailure, GCS, OTel) via `lib.mkMerge`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DiscordSync module consumption pattern                                                                  | DiscordSync and Monitor365 both follow the SystemNix standard for consuming LarsArtmann flakes: `imports = [ inputs.X.nixosModules.default ]` + layer host-specifics with `lib.mkDefault`/`lib.mkMerge`. This eliminates option re-declaration drift. The wrapper file adds ONLY what upstream cannot: sops templates, DNS-gate (`waitDnsReady`), `onFailure` alert routing, port wiring from `lib/ports.nix`, and SystemNix-specific hardening overrides (MemoryMax, ReadWritePaths)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DiscordSync schema-drift migration (FIXED)                                                              | Migration crashed on old databases (`invalid expression in CREATE INDEX: guild_id`) because the initial schema lacked `guild_id` and `CREATE TABLE IF NOT EXISTS` was a no-op. **Fixed upstream:** Migrate now parses schemaDDL as single source of truth, runs `ALTER TABLE ADD COLUMN` for any missing column before creating indexes, and gracefully degrades when FTS5 is unavailable (turso engine lacks it). The `database is locked` errors during initial backfill are transient (single-connection SQLite pool under contention)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| SigNoz stale SQLite migration_lock                                                                      | After a crash/OOM/hard-reset mid-migration, `migration_lock` table in `/var/lib/signoz/signoz.db` keeps a row → next boots loop `attempt to acquire lock failed` → start-limit-hit. **Now self-healing:** `signoz.service` has an `ExecStartPre` (`signoz-clear-migration-lock`) that `DELETE FROM migration_lock` — safe because ExecStartPre runs with no signoz process alive. Do NOT remove this ExecStartPre. Manual recovery (pre-fix): `sqlite3 /var/lib/signoz/signoz.db "DELETE FROM migration_lock;"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| monitor365 hardened user services                                                                       | ExecStart/ExecStartPre MUST use systemd specifiers (`%t` for runtime dir), NOT `$XDG_RUNTIME_DIR` — hardened user services don't reliably expose it (same class as the `%h` vs `$HOME` gotcha). The agent's inject-auth script receives `%t` as `$1`. Also: monitor365 binaries are a private LarsArtmann package whose CLI changed — the **server dropped `--config <file>`** (uses `MONITOR365_SERVER__*` env vars + XDG `~/.config/monitor365/server.toml` auto-load); the agent keeps `-c/--config`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| monitor365 runtimeDeps PATH                                                                             | The agent's `runtimeDeps` option (CLI tools for collectors) is wired into the systemd service `PATH` via `Environment = [ "PATH=${makeBinPath cfg.runtimeDeps}:..." ]`. Without this, tools like `pgrep`, `xdotool`, `scrot` were invisible to the agent. The `graphicalUsers` option (set to `[ primaryUser ]` in SystemNix) grants the user IPC socket access. Display discovery: the upstream module's start script uses pgrep + heredoc command-substitution to extract DISPLAY/WAYLAND_DISPLAY/XAUTHORITY/etc from the displayUser's compositor `/proc/<pid>/environ`. This is verified working — the heredoc `$(...)` pattern correctly propagates exports to the exec'd binary. The prior "subshell issue" diagnosis was wrong; the real blockers were (1) `ProtectProc=invisible` hiding niri's PID from pgrep (fixed via `mkForce "default"`), (2) the service starting at boot before niri exists (fixed via path-unit restart), and (3) `config.users.users.${primaryUser}.uid` being `null` at eval time (isNormalUser assigns uid at activation, not eval) which broke the old hardcoded `Environment=` approach. Keystroke/mouse collectors need the `input` group (`extraGroups = [ "input" "video" ]`), camera needs `video` group                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| monitor365 agent auth model                                                                             | The agent reads its auth token via systemd `LoadCredential` (not `sed` config mutation). The Nix module's `cloud.authTokenFile` points to a sops secret path. systemd loads it as `cloud_auth_token` credential. The start script reads `$CREDENTIALS_DIRECTORY/cloud_auth_token` and exports `MONITOR365__CLOUD__AUTH_TOKEN`. The Rust config system (figment) reads this env var as an override over the TOML config. No secrets are in the Nix store or the TOML config file                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| monitor365 API key desync (FIXED)                                                                       | **Root cause:** The tenant projection replay (`tenant_projection.rs:46`) INSERTs tenants with `api_key = ''` because `TenantCreated` events didn't carry the api_key hash. **Fix (upstream commits `58ae68d03` + `6e6537082`):** (1) `DomainEvent::TenantCreated` now carries `api_key_hash: Option<ApiKeyHash>` (serde `rename = "api_key"` for backward compat). `ApiKeyHash` is a `#[serde(transparent)]` newtype — impossible to accidentally put plaintext where a hash belongs. (2) Both bootstrap AND API paths (`TenantCommand::Create`) thread the hash through the full event chain. (3) Bootstrap re-syncs api_key on every startup. DuckDB-deletion workaround REMOVED. Server hashes `SHA256`, agent sends `x-api-key` header                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| monitor365 DuckDB SQL compat (FIXED)                                                                    | DuckDB has stricter SQL semantics than SQLite. Three classes of errors fixed in `app_usage.rs`: (1) `now() - INTERVAL` fails because DuckDB's `now()` returns TIMESTAMPTZ and the `-` operator has no `(TIMESTAMPTZ, INTERVAL)` overload — fix: `CAST(now() AS TIMESTAMP)`. (2) `MAX(x, 1)` is parsed as nested aggregate, not scalar "greatest" — fix: `GREATEST(x, 1)`. (3) The `events.timestamp` column is VARCHAR (legacy schema) — comparing it with TIMESTAMP expressions fails — fix: `CAST(timestamp AS TIMESTAMP)`. (4) DuckDB requires all non-aggregate SELECT columns in GROUP BY (no functional dependency inference) — fix: add missing columns. Upstream commits `6582b2766` + `664debdee`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| monitor365 no JIT SSO provisioning                                                                      | The SSO flow (`handlers/sso/flow.rs`) performs a full OIDC code-exchange + userinfo fetch from Pocket ID, but does **NOT** auto-create user accounts. The user MUST be pre-provisioned via bootstrap (admin email) or `monitor365-server create-admin` CLI. If the Pocket ID email doesn't match an existing user, login fails with `"SSO user not found — user must be provisioned before SSO login"`. The error is passed to the WASM UI as a URL query param on the sso-callback redirect. Pocket ID's `email_verified` flag is NOT hard-rejected (passkey-only IdPs authenticate identity without email verification)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `pkgs.nss` is libs-only                                                                                 | `certutil` (and other NSS tools) are in the **`pkgs.nss.tools`** output, NOT `pkgs.nss` (which is the library). A service with `path = [pkgs.nss]` will get `status=127` at runtime for `certutil`. Use `pkgs.nss.tools` (+ `coreutils` for `mkdir`/`sleep`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| xdg-document-portal needs fusermount3                                                                   | The portal fails at login (`posix_spawn for fusermount3 failed: No such file or directory`) unless a setuid `fusermount3` wrapper exists. Added via `security.wrappers.fusermount3` in `configuration.nix` (pulls `fuse3` into the closure). Removing it re-breaks the portal every login                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Network interface boot race                                                                             | **FIXED** for dnsblockd/keepalived; pending deploy. dns-blocker now uses a dedicated `dnsblockd-attach-ip.service` (CAP_NET_ADMIN oneshot, ordered after `sys-subsystem-net-devices-eno1.device`) to add the block IP, and `dnsblockd.service` depends on it. `networking.localCommands` with `                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | true`is removed. keepalived and the dual-wan services (when re-enabled) also order after the`.device` unit. The old race where the IP was never added and dnsblockd crash-looped is closed. |
| SSH control-master socket stale-refuse                                                                  | `ControlMaster auto` + `ControlPersist 600` (set by the `nix-ssh-config` module for `github.com`) leaves orphaned socket files in `~/.ssh/sockets/` when a master dies uncleanly (OOM, suspend, logout). SSH then prints `ControlSocket ... already exists, disabling multiplexing` on every `git push/fetch` (ops still succeed — SSH falls back to a direct connection). **Fixed** in `platforms/common/programs/ssh-config.nix`: `home.activation.ssh-sockets-dir` ensures the dir exists, and a `ssh-socket-cleanup` systemd **user** timer (every 5 min, Linux-only via `lib.optionalAttrs stdenv.isLinux`) probes each socket via AF_UNIX `connect()` and unlinks dead ones. Darwin has no systemd so it gets only the dir-creation activation. One-time manual clear: `rm ~/.ssh/sockets/*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `-config` option suffix is intentional                                                                  | A module named `audio.nix` exposes `services.audio-config.enable` (not `services.audio`). The `-config` suffix avoids colliding with the **upstream** NixOS option the module configures (e.g. `services.displayManager`, `services.pipewire`). Modules wrapping a same-named upstream service (immich, caddy, forgejo) reuse the upstream `.enable` directly. This is a deliberate convention, not a naming bug — do NOT "fix" it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Homepage stale prerender cache → 404 on `_next/static/*`                                                | Homepage is a Next.js standalone app. The nixpkgs package patches `serverDistDir` to `/var/cache/homepage-dashboard/homepage` for persistent ISR cache. When nixpkgs bumps homepage-dashboard, the buildId changes, but the OLD prerendered pages in `/var/cache/homepage-dashboard/` still reference the OLD chunk hashes → browser gets 404 on CSS/JS → broken dashboard (MIME errors are just Caddy's 404 body served as `text/plain`). **Fix:** The module has `CacheDirectory = "homepage-dashboard"`, `NIXPKGS_HOMEPAGE_CACHE_DIR` in Environment, and a `preStart` that clears the cache (`rm -rf /var/cache/homepage-dashboard/*`) on every service restart. See nixpkgs#346016. If the dashboard breaks after a deploy: `systemctl restart homepage-dashboard` to trigger cache clear                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| qmd pre-deploy-check false FAIL on first run                                                            | qmd's HTTP MCP server takes 30-60 s on first start to load three GGUF models from `~/.cache/qmd/models/` (~2 GiB first fetch from HuggingFace). If `~/.cache/qmd/models/` is empty, expect `nix run .#post-deploy-check` to SKIP for `qmd MCP HTTP Server` (process up, /health not yet responding). Allow up to 2 min for first-boot warmup. Subsequent restarts complete in <5 s (models stay cached)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| qmd index corrupt → silent "no results"                                                                 | If a power-loss / OOM terminates qmd mid-write to `index.sqlite`, the index may end up inconsistent and `qmd query` returns empty results without errors. Recovery: `systemctl --user stop qmd-mcp && rm ~/.cache/qmd/index.sqlite && systemctl --user start qmd-mcp && qmd update && qmd embed`. Vector embeddings regenerate from scratch (~10 min for typical corpus). Models cached in `~/.cache/qmd/models/` survive — no re-download                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| qmd stdio MCP vs HTTP MCP race                                                                          | stdio MCP (`qmd mcp`) loads models on every subprocess spawn. HTTP MCP (`qmd mcp --http`) loads once. If both are configured in a single Crush `mcpServers` block, stdio wins on first call and loads its own models in parallel with the HTTP server's models — 4 GiB VRAM pressure on Strix Halo. Always pick one mode per client. Crush uses HTTP (`http://localhost:8181/mcp`); stdio is for one-shot per-session clients (Claude Code plugin)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `import ../../../lib/default.nix lib` boilerplate is required                                           | Every module re-imports `lib/default.nix` rather than receiving helpers via `_module.args`. **This is correct:** `nix flake check` evaluates each `nixosModule` standalone (no injected args), so helpers MUST be self-imported. Injecting via `_module.args` would either break the standalone check or keep the import as a default (no win). Canonical form: `inherit (import ../../../lib/default.nix lib) harden serviceDefaults onFailure ports;`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Forgejo OIDC SSO via Pocket ID                                                                          | Forgejo uses **native OIDC** (not Caddy forward-auth). The `forgejo-oidc-setup` oneshot service (in `forgejo.nix`) reads the client secret from `/var/lib/pocket-id/client-secrets/forgejo` and creates the "PocketID" auth source in Forgejo's DB via `forgejo admin auth add-oauth`. The auth source name ("PocketID") IS the URL slug — callback URL is `https://forgejo.${domain}/user/oauth2/PocketID/callback`. **Do NOT use spaces in the auth source name** — the callback URL would need `%20` encoding. Caddy vHost for forgejo is a direct TLS proxy (no `protectedVHost`/forward-auth) because forward-auth + native OIDC causes a double-auth redirect loop. `oauth2_client.ENABLE_AUTO_REGISTRATION = true` auto-creates users on first OIDC login (bypasses `service.DISABLE_REGISTRATION`). `ACCOUNT_LINKING = "auto"` links OIDC users to existing accounts by email match                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Forgejo GitHub-sync API token trap (FIXED)                                                              | The `forgejo-github-sync` + `forgejo-ensure-repos` services authenticate to the Forgejo API with `FORGEJO_TOKEN` from `/var/lib/forgejo/.admin-token.env` — auto-generated by `forgejo-generate-token.service` (runs as `forgejo`, writes a fresh token via `forgejo admin user generate-access-token`). This file is loaded by BOTH sync services via `EnvironmentFile = [ sops-template "-${stateDir}/.admin-token.env" ]` (the `-` ignores missing). **NEVER put `FORGEJO_TOKEN` in the sops `forgejo-sync.env` template** — a stale placeholder value (the encrypted secret was literally `"CHANGE_ME"`) makes every `/api/v1/repos/migrate` reject with `access token does not exist [sha: CHANGE_ME]` and Forgejo silently shows ZERO repos while the sync reports "N repos processed". The template must carry only `GITHUB_TOKEN` + `GITHUB_USER`. `tokenGen` VALIDATES the existing token against `/api/v1/user` before skipping regeneration — a bare `[ -f ] && exit 0` skip-guard would freeze on a stale/empty token file forever with no error. Repo ownership: migrate uses `uid: 1` (= the admin user `primaryUser`/`lars`); ALL Forgejo-side URLs (existence check, `push_mirrors`) must use `primaryUser`, NOT `GITHUB_USER` ("LarsArtmann") — mixing them causes perpetual "404 → re-migrate → already exists" churn. Pass `auth_token: $GITHUB_TOKEN` in the migrate payload or private repos fail to clone (401). **Orphan git dirs (FIXED):** interrupted migrates (past OOM/crash) leave git dirs on disk with NO DB record; subsequent migrates reject with `Files already exist for this repository. Adopt them or delete them` (HTTP 409) while the script falsely printed `✓ Created mirror` because bare `curl` exits 0 on HTTP errors. Fix: the script now calls `DELETE /api/v1/admin/unadopted/{owner}/{repo}` before every migrate (safe, only after the existence GET confirms no DB record, so only true orphans clear, never registered repos), checks migrate HTTP status (200/201), and exits non-zero with a FAILED count so `onFailure` alerts fire. Never trust `curl &&` for API success, always `-w "%{http_code}"` and compare |
| Forgejo declarative SSH keys                                                                            | Forgejo does **not** read the NixOS user's `openssh.authorizedKeys.keys`. SystemNix adds a `services.forgejo.sshKeys` option (default: the primary user's authorized keys) and a `forgejo-ssh-keys` oneshot service that provisions them via the admin API (`POST /api/v1/admin/users/{username}/keys`). The service runs after `forgejo-generate-token.service` and reads the token from `/var/lib/forgejo/.admin-token.env`. Keys are matched by raw string and are **additive only** — manually added keys are not removed. Override `services.forgejo.sshKeys` explicitly if you want different keys than the NixOS user's authorized keys                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Gatus native OIDC + DynamicUser                                                                         | Gatus uses **native OIDC** (Layer 1) via `security.oidc` in `gatus-config.nix`. The NixOS gatus module sets `DynamicUser = true`, so the `gatus` user does NOT exist at sops-decrypt time and can't own files. Therefore the client secret is NOT a sops secret — it's the provisioned file `/var/lib/pocket-id/client-secrets/gatus`, loaded via systemd `LoadCredential` (read by root, exposed in `$CREDENTIALS_DIRECTORY`), then an `ExecStartPre` (`gatus-oidc-env`) copies it into `/run/gatus/oidc.env` which gatus reads via config.yaml `$GATUS_OIDC_CLIENT_SECRET` interpolation. Caddy `status.${domain}` is a plain `reverse_proxy` (NOT `protectedVHost`) — same double-auth rule as Forgejo. The Gatus self-health probe must use `[STATUS] < 400` (not `== 200`) when OIDC is on, because unauthenticated probes get a 302 redirect to the IdP login                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Native OIDC ≠ free for every service                                                                    | Before adding Layer 1 OIDC to a service, verify upstream support. Homepage has **no** built-in auth (proxy-only). SigNoz OIDC is **Enterprise-only** ($4k/mo) — uses impersonation mode + unconditional Caddy forward-auth instead. Twenty SSO is **billing-entitlement-gated** (contact twenty.com). SearXNG has **no user accounts at all** (privacy metasearch, no login concept). Custom LarsArtmann Go services need OIDC code in their repos. These stay on Layer 2 (oauth2-proxy forward-auth). Each blocked service has a header comment in its `.nix` documenting why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| SigNoz impersonation mode                                                                               | SigNoz CE OIDC/SAML is Enterprise-only ($4k/mo). Instead, `signoz.nix` sets `SIGNOZ_IDENTN_IMPERSONATION_ENABLED=true` + `SIGNOZ_IDENTN_TOKENIZER_ENABLED=false` + `SIGNOZ_IDENTN_APIKEY_ENABLED=false` — every request is treated as root admin with zero internal auth. The Caddy vHost (`signoz.${domain}`) applies `forwardAuth`UNCONDITIONALLY (not`protectedVHost`) — no LAN bypass, because impersonation mode means SigNoz itself has zero access control. A root user is auto-provisioned at startup (`SIGNOZ_USER_ROOT_*`) with a random password (never used for login). The provisioning service (`signoz-provision`) works unchanged because its unauthenticated curl calls are treated as root admin in impersonation mode                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| dnsblockd `restartTriggers` prevents stale-process DNS outage                                           | Without `restartTriggers`, `switch-to-configuration` may not restart dnsblockd.service on certain deploys (observed during the unbound→dnsblockd migration). The running process keeps its OLD config (e.g., no `dns_enabled`) while unbound is stopped, leaving :53 with no listener. Every DNS-dependent service cascades to failure (`connection refused` on 127.0.0.1:53). The module now uses `restartTriggers = [ dnsblockdConfigFile pkgs.dnsblockd ]` to force a restart on ANY config or binary change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| dnsblockd embedded resolver = sole DNS resolver                                                         | dnsblockd replaces unbound entirely on :53. The embedded sdns resolver handles DNSSEC, DoT/DoH, local zones (home.lan. with NXDOMAIN boundaries), LAN ACLs, blocklist hot-reload with cache flush, and IPv6 disable. Config flows via `dns_enabled: true` + `dns_local_records`/`dns_local_zones`/`dns_allowed_networks`/`dns_forwarders`/`dns_ipv6_enabled` in the YAML. The `dns-blocker.nix` module generates this config from NixOS options (`localRecords`, `localZones`, `allowedNetworks`, `dnsForwarders`, `dnsIPv6Enabled`). All former unbound config is removed; all service dependencies point to `dnsblockd.service`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| iotop needs `delayacct` boot param                                                                      | `iotop` shows `CONFIG_TASK_DELAY_ACCT not enabled` even though evo-x2's kernel has `CONFIG_TASK_DELAY_ACCT=y` compiled in — the feature is **inert without the `delayacct` boot param**. Added to `boot.nix` kernelParams. eBPF tools (`bcc` → `biotop`/`biosnoop`/`biolatency`, `bpftrace`, `sysstat` → `iostat`) are also installed in `security-hardening.nix` as the primary I/O tracing stack — they use BPF tracepoints (need `CONFIG_DEBUG_INFO_BTF=y`, which evo-x2 has) and don't depend on delay accounting at all                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| dnsblockd wildcard `*.home.lan` does not resolve (FIXED 2026-07-24)                                     | dnsblockd's embedded sdns resolver does NOT support wildcard local records (`*.home.lan.` → serverIP is in `localRecords` but silently ignored). Only EXPLICITLY listed subdomains in `dnsLocal.localSubdomains` (`platforms/common/dns-local.nix`) resolve. Consequence: any new service subdomain MUST be added to `localSubdomains` or it will be unreachable by hostname (e.g. `dnsblock.home.lan` was missing → smoke test `000`, browser "name not known"). The wildcard record in `localRecords` is a no-op and should be removed or documented as aspirational. All service subdomains used in Caddy vHosts must cross-reference `dns-local.nix`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| dnsblockd cache CNAME-chase bug (unwired Queryer)                                                       | **Root cause of build "Could not resolve host" failures.** dnsblockd creates the sdns response cache (`cache.New`) but **never calls `cache.SetQueryer`**, so the cache's CNAME-chase path (`additionalAnswer` → `internalExchange`) returns `errQueryerNotWired` and silently drops the chase. Result: the cache intermittently serves **partial CNAME answers** (the CNAME record WITHOUT its terminal A/AAAA records). On evo-x2 (no global IPv6) glibc cannot use a bare CNAME → `getent`/`getaddrinfo` return nothing → `curl: (6) Could not resolve host` for any CNAME-chained CDN hostname (`static.crates.io`, `tarballs.nixos.org` — both → Fastly). Only surfaces when builds fetch crates NOT in the binary cache (e.g. monitor365 pulling `arrow 58.3.0`). Independent of DNSSEC, `dns_ipv6_enabled`, and transport (DoT vs plain DNS) — all tested. **Fix (upstream dnsblockd):** wire a forwarder-backed `subPipelineQueryer` via `c.SetQueryer`/`c.SetPrefetchQueryer` in a `newCache()` helper (used by `EnableCache` + `FlushCache`). Verified: 30/30 concurrent queries return full chains with A records. See `internal/dns/queryer.go`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Strix Halo unified memory — GPUActive tracks AI VRAM usage                                            | evo-x2 has 128 GiB physical RAM but only **~94 GiB visible to Linux** (34 GiB BIOS VRAM carveout). When local AI workloads (Ollama, llama-cpp, etc.) are running, **GPUActive (GTT buffer objects) consumes 50+ GiB** — this is expected unified-memory behavior, NOT a leak or chronic pressure. When AI workloads are idle, GPUActive drops and the RAM is available. `/proc/meminfo` has a dedicated `GPUActive` counter — standard `free`/`htop` do NOT show it. Do NOT cite "chronic memory pressure" when sizing service MemoryMax — that was a misdiagnosis                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `/tmp` tmpfs size capped at 48 GiB + stale-entry cleanup timer                                          | `boot.tmp.useTmpfs` defaults tmpfs size to 50% of RAM (~47 GiB). go-build caches and dev tool temp files accumulated 59 GiB in a single 21-hour session. Capped at 48 GiB via `systemd.mounts` (static `tmp.mount` unit). `size=` is a CEILING, not a reservation — RAM is only consumed by files actually written. Stale accumulation during long uptimes is handled by the `tmp-cleanup` timer (every 4h, removes top-level /tmp entries untouched >4h with per-entry descendant check — protects active builds). **NEVER use `fileSystems."/tmp"` for this** — it generates a runtime fstab entry instead of a static Nix store unit, so `switch-to-configuration` sees `tmp.mount` disappear between generations and tries to unmount `/tmp` → fails (busy) → activation exit code 1 (2026-07-02 deploy failure). zram-backed /tmp was considered and rejected: the system already has zram swap (100% full) which compresses tmpfs cold pages automatically; a separate zram device would add CPU overhead on hot data and compete for the same scarce RAM                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Unbound RSS 8x cache size (historical)                                                                  | Former unbound config: 192 MiB of caches but 1.5 GiB RSS due to unbounded DNSSEC key cache and NXDOMAIN cache. **Moot** — unbound removed entirely (2026-07-13). dnsblockd's sdns cache is bounded by `CacheSize` in the sdns config                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Helium GPU SIGBUS crash on display hotplug (rare)                                                       | Chromium's GPU process can hit `SIGBUS / BUS_ADRERR` during slow display pipeline reconfiguration on Strix Halo (DCN 3.5.1). This is a REAL GPU fault (generates a minidump in `~/.config/net.imput.helium/Crash Reports/`), NOT a clean exit. Amplified by `--enable-zero-copy` (DMA-BUFs invalidated on connector change) and high GPUActive when AI workloads are running. **Fix:** `--disable-gpu-watchdog` flag in the Helium wrapper (`base.nix`). `MESA_VK_WSI_PRESENT_MODE=immediate` in `amd-gpu.nix` also worsens the race window. Last observed: Jul 17, 2026 (minidump `adb06b83-96a2-4836-a39e-bbc8c4acf4be.dmp`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Helium niri zero-output client death (common)                                                           | **The actual cause of "Helium crashing" during monitor swaps.** Niri has NO virtual/headless output support. When all physical outputs disconnect (USB-C cable swap from monitor to TV), niri disconnects the connector (`disconnecting connector: "DP-2"`), leaving zero Wayland outputs. All Wayland clients lose their surfaces and exit cleanly (no crash dump). Helium logs `There are no outputs - creating placeholder screen` then exits. The output reconnects 5-7s later but Helium is already dead. **Fix:** `helium` systemd user service in `niri-wrapped.nix` with `Restart=always`, `RestartSec=5`, `StartLimitBurst=10`. `--restore-last-session` + `--disable-session-crashed-bubble` (in the wrapper) reopen tabs automatically. This is a band-aid — a DP/HDMI EDID dummy plug on a separate port would prevent the crash entirely by keeping an output alive during the swap. See `docs/status/archived/2026-07-22_19-16_helium-display-switch-crash-diagnosis-and-autostart.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| helium.service empty-window loop (FIXED 2026-07-24, RE-FIXED 2026-08-01)                                               | `helium.service` has `Restart=always` (required for zero-output clean exits) + `ExecStart = helium-launch`. **Original bug:** bare `ExecStart` with no guard → 11 empty windows in 36s. **First fix (2026-07-24):** added `helium-launch` with a 300s timeout cap — but the timeout DEFEATED the guard: every 5 min it fired "launching anyway" → Chromium found SingletonLock held → "Opening in existing browser session" → empty window → exit 0 → `Restart=always` → repeat forever (one empty window every 5 min). **Final fix (2026-08-01):** removed the timeout entirely. `helium-launch` now waits indefinitely — it becomes a monitor that blocks until the existing instance dies, then `exec`s fresh. systemd sees the service as "running" during the wait. To recover a stuck/zombie helium: kill it manually, `helium-launch` detects the death within 5s and launches fresh                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Monitor365 agent circuit-breaker deadlock + start-limit death spiral (FIXED 2026-07-24)                 | **Root cause chain:** (1) Server down → agent's circuit breaker opens (716K+ failures). (2) CB is in-memory → only a process restart clears it. (3) But the agent burns 95% CPU on CB error spam + buffer at 95% capacity → eventually crashes or gets killed. (4) On restart, `monitor365-graphical-restart.path` (triggered by niri DRM zombie bounce) fires `systemctl restart` multiple times in 1 second → 6 start/stop cycles → `start-limit-hit` (upstream had no `StartLimitBurst`/`StartLimitIntervalSec`, systemd defaults = 5/10s). (5) Deploy runs `systemctl reset-failed` but NEVER starts the agent → stays dead indefinitely. **Fix (4 layers):** (A) `startLimitBurst=10` + `startLimitIntervalSec=300` on `monitor365.service` (`monitor365.nix`). (B) `monitor365-graphical-restart.service` now debounces — skips restart if agent was started <60s ago, and skips if agent is not active (lets watchdog handle it). (C) `monitor365-agent-watchdog` timer (every 5min) checks agent process + metrics endpoint; if dead, resets start-limit and starts. (D) `deploy.sh` now explicitly starts enabled-but-inactive services after `reset-failed`. **Post-deploy check** verifies BOTH sides: agent process alive + metrics responding (port 9191) AND server reports >0 connected devices. If agent alive but 0 devices, attempts restart to clear CB deadlock                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `harden {}` defaults to `ProtectHome = true`, making `/home` an empty tmpfs. Services that read user data (e.g., crush-daily reading `~/.local/share/crush/.crush/crush.db`) silently find nothing — no crash, no error, just empty output for weeks. **Always** set `ProtectHome = false` + scoped `ReadOnlyPaths` for services that need user data. A pre-commit hook (`protect-home-audit`) now catches this pattern. The post-deploy smoke test (`nix run .#post-deploy-check`) verifies functional outcomes after deploy |
| Package alias traps (monitor365)                                                                        | `pkgs.monitor365` resolves to `monitor365-cli` (the **agent**), NOT `monitor365-server` (the symlinkJoin with bundled WASM UI + `UI_DIST_PATH` wrapper). Using the wrong package → server runs as API-only, `/ui/` returns 404, health check passes (API is alive), but dashboard is broken. Always verify which package attribute contains the runtime artifacts you need. The upstream overlay exposes all three: `monitor365`, `monitor365-server`, `monitor365-ui`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| systemd `PathExists` creates re-fire loop during deploy (FIXED 2026-07-24)                              | A systemd `.path` unit with `PathExists=/run/user/1000/wayland-1` fires the target service IMMEDIATELY when the path already exists at unit start. During deploy, systemd reloads the path unit (unit file changed). On restart, the file exists → fires the service → service exits 0 (oneshot) → systemd re-evaluates the path condition → file STILL exists → fires again → 8 starts in 1 second → `start-limit-hit` on BOTH the service AND the path unit. Setting `StartLimitBurst=20` on the service did NOT fix it — the path unit itself has its own start-limit. **Fix:** use `PathChanged` instead of `PathExists`. `PathChanged` only fires when the file is CREATED or MODIFIED, not when the path unit starts with the file already present. During deploy (user logged in), the socket doesn't change → no trigger. After boot (user logs in), the socket is created → triggers once. This is the correct trigger type for "restart when session appears" use cases                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Post-deploy smoke test                                                                                  | `nix run .#post-deploy-check` (or automatic in `deploy.sh`) verifies services are **functional**, not just alive: checks vHosts return expected HTML, API endpoints return expected JSON, and specific bug patterns (monitor365 UI body, crush-daily reports non-empty). Always run after deploy to catch silent failures early                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `discard=async` on QLC NAND = I/O latency death spiral                                                  | On the Lexar NQ790 (QLC NVMe), `discard=async` caused 253 ms discard latencies → 17.7 s BTRFS commit freezes → WDT hard reset. **Fix:** all BTRFS/ext4 mounts explicitly set `nodiscard` (defense-in-depth). BTRFS auto-adds `ssd` for non-rotational devices but does NOT auto-enable discard. `fstrim.timer` handles weekly TRIM. See `docs/status/2026-08-03_*` reports                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `buildGoModule` silently drops unknown `env` attrs                                                      | nixpkgs' `buildGoModule` only forwards a whitelist of `env` attributes (`CGO_ENABLED`, `GOWORK`, `GOFLAGS`, `GOTOOLCHAIN`, `GO111MODULE`, `GOARCH`, `GOOS`). Arbitrary attrs like `GOEXPERIMENT`, `GOPROXY`, `GOPRIVATE` are **silently dropped**. Use `export VAR=value` in `preBuild` for anything else. This caused the `buildflow` silent empty binary bug                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `buildGoDir` swallows "build constraints exclude all Go files"                                          | nixpkgs' `buildGoDir` catches build-constraint errors and returns 0, producing a silent empty output instead of failing. Defense: add a post-build assertion that `$out/bin/` is non-empty, especially when experimenting with `GOEXPERIMENT`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Helium wrapper double-wrap `--enable-features` collision                                                | The upstream helium-browser-nix-flake already wraps `$out/opt/helium/helium` with makeWrapper (adding `--ozone-platform-hint=auto`, `--enable-features=WaylandWindowDecorations`, privacy flags). SystemNix was wrapping the UPSTREAM WRAPPER (`$heliumPackage/bin/helium`) again — two `--enable-features` switches in the final command. Chromium's `GetSwitchValueASCII` returns the LAST value for duplicate switches, silently dropping features from whichever layer appeared first. **Fix:** SystemNix now wraps the REAL binary (`$out/opt/helium/helium`) directly in a single makeWrapper layer, combining all flags from both upstream and SystemNix. Includes `LD_LIBRARY_PATH` prefix (replicating upstream)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Helium VA-API feature flag renames (Chromium 131+)                                                      | `VaapiVideoDecodeLinuxGL` → `AcceleratedVideoDecodeLinuxGL`, `VaapiVideoDecodeLinuxZeroCopyGL` → `AcceleratedVideoDecodeLinuxZeroCopyGL`, `VaapiVideoEncoder` → `AcceleratedVideoEncoder`. The old `AcceleratedVideoDecoder` (no suffix) was NEVER a valid feature name — it was silently ignored. Helium is Chromium 150, so all 131+ names apply. For AMD: also add `VaapiIgnoreDriverChecks` and `UseMultiPlaneFormatForHardwareVideo`. Since Chromium 143+, VA-API works out of the box, but explicit flags are kept as defense-in-depth for the blocklisted GFX1151                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Helium backgrounding flags → OOM contributor                                                            | `--disable-backgrounding-occluded-windows` and `--disable-renderer-backgrounding` prevented Chromium from throttling/discarding background tabs. This was a contributing factor to the 2026-06-19 OOM cascade (renderers grew unbounded for 66h). **Removed.** Chromium's Memory Saver / tab discarding now works naturally — background tabs get throttled and eventually discarded under memory pressure, which is the correct behavior on a system where GPU memory usage fluctuates with AI workload                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Helium `--disable-background-networking` kills extensions (FIXED 2026-07-29)                            | The wrapper flag `--disable-background-networking` silently blocks **ALL** `force_installed` enterprise-policy extensions from downloading. It disables the `ExtensionDownloader` subsystem that fetches CRX files from the `update_url` in `ExtensionSettings`. The policies appear correctly in `chrome://policy`, but zero extensions ever appear in `chrome://extensions` — no error, no warning. `--disable-component-update` was also removed (disables the Chromium component updater, which is redundant since Widevine is bundled and Helium strips Safe Browsing). Both flags were originally added for privacy (prevent Google telemetry phoning home), but Helium's ungoogled-chromium base already strips Google integration and anonymizes Chrome Web Store requests via its own proxy. The privacy tradeoff of allowing background networking is acceptable. **Verified:** `clients2.google.com/service/update2/crx` is reachable and returns valid XML. Do NOT re-add these flags — extensions depend on background networking being enabled                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Helium is Chromium 150 (ungoogled-chromium)                                                             | NOT Electron — a full Chromium fork with Google services stripped. Includes built-in uBO fork, anti-fingerprinting, DuckDuckGo-style !bangs. Widevine is bundled separately by SystemNix (upstream has no DRM). The `--simulate-outdated-no-au` flag suppresses "outdated" warnings since there's no Google updater                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `with pkgs;` hides missing attributes                                                                   | `with pkgs; [ llama-cpp-rocwmma ]` silently resolves to a `let`-bound variable of the same name when `pkgs.llama-cpp-rocwmma` doesn't exist. `with` falls through to the enclosing scope on missing attrs. Using explicit `pkgs.` prefixes or bare `let` names makes the source clear and catches missing packages at eval time                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `//` on `serviceConfig` discards priority                                                               | The `//` operator is a shallow merge that silently clobbers `mkDefault`/`mkForce` priority annotations from `harden` and `serviceDefaults`. Always use `lib.mkMerge [ (harden {}) (serviceDefaults {}) { ... } ]` instead — it preserves module-system priority. All serviceConfig `//` chains have been converted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `monitor365.nix` port collision bypass (FIXED)                                                          | Was importing `lib/ports.nix` raw, skipping the duplicate-port detection wrapper in `lib/default.nix`. Now correctly imports via `lib/default.nix lib`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Use `pkgs.formats.yaml{}.generate` not hand-rolled YAML                                                 | Homepage settings/services/widgets YAML, docker-compose files, and all config generation now use `pkgs.formats.yaml{}.generate` or `builtins.toJSON` over structured attrsets instead of manual string concatenation. This provides type-safe serialization, proper quoting, and structural validation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Use `lib.generators.toKeyValue` for .env files                                                          | All sops `.env` templates now use `lib.generators.toKeyValue {}` over structured attrsets instead of hand-rolled `KEY=value` strings. Cleaner, less error-prone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| flake-parts `mkFlake` does NOT accept a bare list                                                       | Passing `[ moduleA moduleB ]` to `flake-parts.lib.mkFlake` causes nixpkgs `setDefaultModuleLocation` to wrap it in `{ imports = [ [ moduleA moduleB ] ]; }` — a nested list, which nixpkgs rejects with `Module imports can't be nested lists`. Use an attrset with `imports` instead: `{ imports = [ moduleA ]; ... }`. This broke nix-ssh-config's flake-parts migration (commit `1539f00`) and blocked ALL SystemNix evaluation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| catppuccin-gtk + Python 3.14 = build failure                                                            | nixpkgs unstable uses Python 3.14 as default `python3`. The catppuccin-gtk build script uses `argparse.BooleanOptionalAction` with `type=` kwarg, removed in Python 3.14. Also, the `catppuccin` Python package's `mpl.style.core` API broke with newer matplotlib. **Fixed** via overlay in `overlays/shared.nix`: `catppuccin-gtk.override { python3 = prev.python312; }` + `catppuccin.overridePythonAttrs { pythonImportsCheck = []; doCheck = false; }`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DMS/Quickshell ScriptModel UAF crash (unfixed upstream)                                                 | Quickshell 0.3.0 + Qt 6.11.1 has an **unfixed use-after-free** in `ScriptModel` (upstream issue #863, duplicates #477, #843, #877, #885, #889). When Wayland toplevel handles are rapidly created/destroyed (polkit dialog storms, window open/close bursts, MPRIS metadata flooding), `ScriptModel::updateValuesUnique` synchronously triggers delegate incubation via `endInsertRows()`. During incubation, a QVariantMap containing a `QObject*` (typically a `ToplevelHandle`) is written to a required delegate property. If the QObject was deleted between model update and delegate creation, `QQmlMetaType::propertyCache()` calls a pure virtual function → `__cxa_pure_virtual` → `std::terminate` → SIGABRT. Our crash signature: `positionViewAtIndex` → `refill` → `createItem` → `RequiredPropertiesInitializer` → `writeKnownVarProperty` → `VariantAssociationObject::init` → `QObjectWrapper::wrap` → `propertyCache` → `__cxa_pure_virtual`. **Not fixable in SystemNix** — the QML lives in upstream DMS. **Mitigation:** systemd `Restart=always` + `RestartSec=2` + `StartLimitBurst=30` (see `quickshell.nix`). Crash logs at `~/.cache/quickshell/crashes/`. On 2026-07-11, DMS crashed 288 times in one day during a polkit auth storm                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| SDDM hides boot/activation logs on tty1                                                                 | SDDM's `display-manager.service` VT-switches to tty1 and takes DRM/KMS exclusive control, switching it to graphics mode. The kernel text console has no scanout buffer to render to — all kernel/systemd/`nixos-activation.service` output is invisible. The system had `consoleLogLevel = 7`, `initrd.verbose = true`, `systemd.log_level=debug`, `systemd.show_status=true` — all maximally verbose — but SDDM's DRM takeover masked everything. **Fix:** `console=tty2` kernel param in `boot.nix` redirects all boot messages to tty2 (a plain text VT with getty that SDDM never claims). `console=tty1` kept as last param so `/dev/console` still resolves to tty1 (NixOS default). After deploy: `Ctrl+Alt+F2` = full boot log, `Ctrl+Alt+F1` = SDDM
| display-watchdog false-positive at login screen (FIXED 2026-08-04)                                       | The `display-watchdog` service (`scripts/display-watchdog.sh`) treats any connected DRM connector with `enabled=disabled + dpms=Off` as a "dead display" and restarts `display-manager.service` (SDDM). At the **SDDM login screen** (no user logged in, no niri running), Xorg's idle DPMS timeout (~10 min) fully tears down the connector CRTC → both conditions match → watchdog restarts SDDM → monitor wakes → sleeps again 10 min later → **infinite restart loop** (one restart every ~10 min, observed continuously in the journal). The watchdog's Scenario 2 ("niri dead + display dead → restart SDDM") assumed niri-not-running = failure, but at the login screen that is the EXPECTED state. **Fix:** a login-screen guard in the script checks `loginctl` for any session with `Class=user` AND `Type=(wayland|x11)`. If none exists (login screen), DPMS-off is normal idle power-saving — the watchdog resets state and exits WITHOUT recovering. The SDDM greeter session has `Class=greeter` (excluded); SSH/tty sessions have `Type=tty` (excluded). The guard is placed AFTER dead-display detection but BEFORE Scenario 1/2 recovery, so genuine mid-session display death (compositor crash while a user session is active) still recovers. **NOT caused by the Quickshell/DMS update** — the watchdog (`d5e48c4c`) predates DMS; the bug was always present but only surfaced noticeably at idle login screens |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| OpenSEO native build (Cloudflare Workers in Nix)                                                        | OpenSEO is a **Cloudflare Workers app** (TanStack Start + Vite 7 + React 19). It depends on `workerd` (Cloudflare's runtime, bundled in the `wrangler` npm package) at both build time (for `@cloudflare/vite-plugin` SSR build) and runtime (for `vite preview` D1/KV/R2/DO emulation). `workerd` is NOT in nixpkgs (issue #355460, closed "not planned"). The `pkgs/openseo.nix` derivation uses `fetchPnpmDeps` + `pnpmConfigHook` + `autoPatchelfHook` (with `stdenv.cc.cc.lib` in `buildInputs` for `libgcc_s`/`libstdc++`). `LD_LIBRARY_PATH` is set in `preBuild` (NOT `autoPatchelf`) to avoid `/build/` RPATH references. The service module (`openseo.nix`) stages the Nix store project to `/var/lib/openseo/project/` via symlinks (read-only from Nix store) with `.wrangler` → `/var/lib/openseo/.wrangler` (writable D1 SQLite). `CLOUDFLARE_INCLUDE_PROCESS_ENV=true` is CRITICAL — exposes process env vars as Workers bindings (DATAFORSEO_API_KEY, AUTH_MODE, etc.). Migrations run at `ExecStartPre` via `wrangler d1 migrations apply DB --local`. **v0.1.1 additions:** `OPENSEO_TELEMETRY_DISABLED=1` opts out of anonymous telemetry (heartbeats every 5 min during first 2h, then daily). New module options: `googleSearchConsole.enable` (requires `google_client_id`, `google_client_secret`, `better_auth_secret` in `openseo.yaml`; GSC callback at `/api/gsc/oauth/callback` exempt from Caddy forward-auth) and `aiFeatures.enable` (requires `openrouter_api_key`). `openseo-validate` ExecStartPre refuses to start if enabled features lack their secrets. `restartTriggers = [ pkg ]` prevents stale `vite preview` serving GC'd static files (same pattern as homepage-dashboard)                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| systemd `WorkingDirectory` runs BEFORE `ExecStartPre`                                                   | systemd changes to `WorkingDirectory` before running ANY `ExecStartPre` commands. If the directory is created BY an `ExecStartPre` script, the service fails with `status=200/CHDIR` — the directory doesn't exist yet at chdir time. **Fix:** set `WorkingDirectory` to a parent that exists (e.g., the `StateDirectory` path, which systemd creates before chdir), and have the `ExecStart` script explicitly `cd` into the subdirectory. See openseo.nix (`openseo-serve` wrapper) for the pattern                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `compsize` is memory-intensive on large BTRFS                                                           | `compsize` walks the BTRFS extent tree via kernel ioctls (`BTRFS_IOC_SEARCH`). The kernel reads metadata pages from disk into page cache to process these ioctls, and the page cache is **charged to the service's cgroup**. On a 47 GiB Nix store, compsize triggered **752 MB of page cache reads**, exceeding a 256M MemoryMax and killing the process (status=127). `MemoryMax=2G` is the minimum for compsize on this filesystem. The `btrfs-compsize` timer runs every 6h (not 5min like `btrfs-health`) precisely because compsize is slow and memory-hungry                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| monitor365 DuckDB vs SQLite compat mode                                                                 | monitor365-server uses DuckDB (`duckdb::DuckdbConnectionManager`), not SQLite. Legacy `sqlite:` connection strings with `.db` paths caused DuckDB to open existing SQLite files in compatibility mode, rejecting DuckDB syntax like `CREATE SEQUENCE` with "Binder Error: SQLite databases do not support creating sequences". **Fixed upstream** (commit `aa46ee8f`): all config defaults now use `.duckdb` extension, and `normalize_db_path` converts any `.db` path to `.duckdb` as a safety net. The NixOS module default is `${stateDir}/monitor365.duckdb` (no `sqlite:` prefix). The old `monitor365.db` SQLite file remains on disk but is unused — DuckDB creates a fresh `monitor365.duckdb` on first start                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| homepage-dashboard `enableLocalIcons = false` (default)                                                 | nixpkgs' `pkgs.homepage-dashboard` defaults to `enableLocalIcons = false` — NO icons are bundled in the build. Every service tile requests `/icons/<name>.png` that doesn't exist, producing ~25 browser 404s per page load. SystemNix overrides: `pkgs.homepage-dashboard.override { enableLocalIcons = true; }` bundles 4276 icons from `homarr-labs/dashboard-icons`. Also: many icon names that feel "obvious" (`shield`, `ai`, `monitor`, `voice`, `whisper`, `search`, `camera`, `twenty`, `taskwarrior`, `hermes-icon`) DON'T exist in the pack — verify against `/nix/store/*-homepage-dashboard-*/share/homepage/public/icons/` before using                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `*.home.lan` wildcard DNS + Caddy catch-all                                                             | dnsblockd's `LocalZoneStore` serves `home.lan.` zone records for all service subdomains (`auth`, `immich`, `forgejo`, etc.). Unknown subdomains within the zone return NXDOMAIN (zone boundary). A wildcard record `*.home.lan.` → serverIP is included in `localRecords` so ALL `*.home.lan` resolve. Caddy has a `https://*.${domain}` catch-all vHost that redirects unknown subdomains to `dash.${domain}`. The `home.lan.` apex also resolves                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| deadnix `--fix` removes lambda params WITHOUT adding `...`                                              | deadnix removes unused named params from attribute set lambda patterns (`{ pkgs, lib, ... }:` → `{ ... }:`) but does NOT add `...` when it removes ALL named params — it just produces `{ }:` or a bare `_:`. For NixOS/HM modules with `...` already present, removal of individual params is SAFE (the `...` catches them). But for **local functions** called by `builtins.filter`/`map` (e.g., `lib/filesystems.nix`) or **non-module functions** called from flake.nix (e.g., `tests/default.nix`), removing a param that the caller still passes causes `function called with unexpected argument` at eval time. Always verify deadnix output leaves `...` in patterns whose callers pass extra attrs. The `buildflow --fix` deadnix:repair step caused this twice (2026-07-14)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| monitor365 utoipa-swagger-ui PermissionDenied in sandbox                                                | `utoipa-swagger-ui` v9.0.2's build script uses `std::fs::copy` to copy the swagger-ui zip from the nix store to `OUT_DIR`. Rust's `fs::copy` propagates source permissions (0444 for nix store files). Crane's `buildDepsOnly` runs `cargo check` THEN `cargo build` — the build script runs twice in the same `OUT_DIR`. The first run creates a 0444 zip copy; the second run's `fs::copy` tries to truncate it → `EACCES` (PermissionDenied). **Fix:** `monitor365SwaggerUiFixOverlay` in `overlays/linux.nix` overrides the deps `buildPhase` to `find target -path '*/utoipa-swagger-ui-*/out/*.zip' -delete` between `cargo check` and `cargo build`. The overlay also rebuilds `monitor365-server` (symlinkJoin) to use the fixed CLI — the upstream overlay bakes the unpatched CLI into the server. When the upstream utoipa-swagger-ui fixes this (e.g., by using `fs::remove_file` before `fs::copy` or `fs::read`+`fs::write`), this overlay can be removed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Homepage orphaned process after nix-gc (FIXED)                                                          | Without `restartTriggers`, `switch-to-configuration` may not restart homepage-dashboard when the package store path changes between generations. The old process keeps running (server.js in memory) while nix-gc deletes the old store path's `.next/static/` files. Result: HTML pages load (from memory) but ALL `/_next/static/*` requests 404 — JS chunks vanish, CSS served as `text/plain` (Next.js fallback response). Browser shows "MIME type text/plain not supported" + dozens of 404s. **Fix:** `restartTriggers = [ homepagePkg ]` in `systemd.services.homepage-dashboard` forces a restart whenever the package changes. Same pattern as dns-blocker's `restartTriggers`. General lesson: ANY service serving static files from the nix store (Next.js standalone, SPA builds) MUST have `restartTriggers` referencing its package — an orphaned process with GC'd static files is a silent, confusing failure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Pre-commit hook statix multi-file bug (FIXED)                                                           | The pre-commit hook (`.githooks/pre-commit`) passed all staged `.nix` files to `statix check` via `xargs`, but statix only accepts ONE target — causing `unexpected argument` errors when multiple `.nix` files were staged. Also, `nix fmt` (treefmt) formats the ENTIRE project including HTML/MD docs, silently damaging 25+ docs files. **Fix:** statix now iterates per-file via `while IFS= read -r f; do ... done`. The treefmt whole-project damage is eliminated by calling `alejandra` directly on staged `.nix` files only (instead of `nix fmt`/treefmt which formats the entire tree). `nix fmt` remains available as a manual command for full-project formatting                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| statix.toml disables `repeated_keys` (NixOS module false positive) (FIXED 2026-08-02)               | statix's `repeated_keys` (W20) rule flags multiple `systemd.X = { ... }` blocks in the same module as a lint warning. This is a FALSE POSITIVE for NixOS modules — the module system merges these blocks at eval time, and splitting service+timer pairs into separate blocks is idiomatic for readability. **Fix:** `statix.toml` (non-dotted, auto-discovered by statix) has `disabled = ["repeated_keys"]`. Also: all `manual_inherit` (W03) and `manual_inherit_from` (W04) warnings fixed via `statix fix` across 15 files (`x = attrs.x;` → `inherit (attrs) x;`). The pre-commit hook no longer needs `--no-verify` to bypass statix. **Note:** `statix.toml` must NOT be dotted (`.statix.toml`) — statix only auto-discovers the non-dotted form. The `ignore = ['.direnv']` and `nix_version = '2.4'` settings from the original config are preserved                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| dnsblockd blocklist dir-vs-file path bug (FIXED 2026-07-18)                                             | `filterBlocklist` produces a derivation whose OUTPUT is a DIRECTORY containing the hosts file at `$out/${name}`. Both `processorArgs` and `blocklistPaths` were passing `bl.file` (the directory) to dnsblockd, which silently read 0 entries — `mapping.json` came out `{}`, `dnsBlocklistEntries` was 0, and ZERO ad/malware blocking happened (ads.google.com resolved to real IPs). No error, no warning — just an empty blocklist. **Fix:** always reference `${bl.file}/${bl.name}` (the file inside the dir). Verified: `dns-blocker-processed` now reports 2,535,619 domains. The `dnsBlockResponse = "zero_ip"` DOES return the block IP (192.168.1.200), so the Gatus "DNS Blocking Active" check (`[BODY] == ${blockIP}`) is correct once blocklists load                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Ollama `wantedBy = mkForce []` silent non-start (FIXED 2026-07-18)                                      | `ai-stack.nix` set `systemd.services.ollama.wantedBy = lib.mkForce [ ]`, suppressing nixpkgs' default `WantedBy=multi-user.target`. Result: with `ai-stack.enable = true`, ollama was enabled but NEVER started — no log entries, no crash, just silent absence. The Gatus Ollama check (unconditional) reported permanent unhealthy. **Fix:** removed the `mkForce []` line entirely so nixpkgs' default applies; made the Gatus Ollama check conditional on `ai-stack.enable` (consistent with voice-agents/monitor365 pattern). Idle ollama uses ~34 MB RSS (models load on demand via OLLAMA_KEEP_ALIVE)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Immich redis defaults to unix-socket-only (port 0) (FIXED 2026-07-18)                                   | The nixpkgs immich module sets `services.immich.redis.port = 0` (TCP disabled) and `host = unixSocket` by default. The Gatus `tcp://127.0.0.1:6379` Redis check therefore always failed. **Fix:** `services.immich.redis.port = ports.redis` (6379) + `services.redis.servers.immich.bind = lib.mkForce "127.0.0.1"` — redis listens on BOTH the unix socket (immich connects via REDIS_SOCKET, faster) AND TCP localhost (for monitoring). The redis `bind` option is `null or string`, NOT a list (a list throws a type error). Note: a Docker-container redis (Twenty CRM) also shows `*:6379` in `ps` but that's in the container netns, not the host                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Monitor365 agent metrics have no "monitor365" string (FIXED 2026-07-18)                                 | The Gatus "Monitor365 System Agent" check used `[BODY] == pat(*monitor365*)`, but the agent's `/metrics` endpoint (port 9191) emits Prometheus metrics named `collector_events_collected`, `middleware_dedup_events_passed`, `cloud_sync_*`, etc. — NONE contain the literal "monitor365". **Fix:** pattern matches `collector_events_collected` (a core, always-present metric). Also notable: `cloud_sync_consecutive_failures` may be high (agent can't reach cloud) but that's a separate upload issue, not an agent-health issue                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| PMA `Type=notify` without `sd_notify(READY=1)` = 90s timeout crash loop (FIXED 2026-07-18)              | The upstream `projects-management-automation` NixOS module sets `Type = "notify"` + `WatchdogSec = 30s` (commit `6cdf05e5` "enable systemd notify"), but the Go binary NEVER calls `sd_notify(READY=1)` — the `go-systemd` dep is indirect only. Result: systemd waits the full `TimeoutStartSec` (90s default), times out, kills the service, `Restart=on-failure` cycles it forever (observed 90+ restart cycles). **Fix:** SystemNix wrapper overrides `Type = lib.mkForce "exec"` + `WatchdogSec = lib.mkForce "0"` in `modules/nixos/services/projects-management-automation.nix`. Remove this override once upstream adds `sd_notify` support. `Type=exec` considers the service started once `execve` succeeds; `WatchdogSec` is inert without `READY=1`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Overview OOM-kills when PMA discovery daemon is absent (FIXED 2026-07-18)                               | Overview (`services.overview`) delegates project discovery to a shared daemon over a unix socket (`/run/project-discovery/daemon.sock`). The daemon is co-located in PMA (`enableDiscoveryDaemon = true`). When PMA was crash-looping (the Type=notify bug above), the socket never appeared, and Overview fell back to LOCAL discovery — which peaks at 4+ GB and gets kernel-OOM-killed every ~20s. **The fix chain:** PMA Type=exec (starts reliably) → PMA starts discovery daemon (socket appears) → Overview connects to socket (no local discovery, stays at ~250 MB). Overview's `memoryMax = "512M"` in config is the correct steady-state limit; the 4 GB local-discovery fallback exceeds it. **Dependency:** Overview depends on PMA being healthy — if PMA is down, Overview WILL OOM-loop. No Gatus alert existed for PMA (no HTTP endpoint, discovery daemon disabled by default in the module)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Deploy generation mismatch: `nix eval` vs `nh os switch` may differ (2026-07-18)                        | After committing config changes, a single `nix run .#deploy` may build from a CACHED intermediate state that doesn't include the latest commits. Symptom: `nix eval .#nixosConfigurations.X.config.systemd.services.Y.serviceConfig.Environment` shows the correct values, but the DEPLOYED unit file (`/run/current-system/etc/systemd/system/Y.service`) has STALE values. **Diagnosis:** compare `nix eval --raw .#nixosConfigurations.evo-x2.config.system.build.toplevel` with `readlink /run/current-system` — if they differ, the deployed generation is stale. **Fix:** deploy AGAIN (the second deploy builds from the fully-updated source). Root cause likely: `nh`/`nixos-rebuild` evaluates at a slightly different point than `nix eval`, or a git-index/working-tree race during the first build                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Monitor365 integrity hash serialization mismatch (FIXED 2026-07-18, upstream `9ea1f1000` + `ebb26a0bd`) | **THE ROOT CAUSE** (not the symptom): `Event::new` (`crates/domain/src/lib.rs:166`) hashed payload bytes via `serde_json::to_vec::<P>(&struct)` — struct field-declaration order. But the server recomputes the hash via `serde_json::to_vec(&Value)` — BTreeMap alphabetical key order (no crate enables serde_json's `preserve_order` feature). Different byte order → different SHA-256 → **EVERY event with non-alphabetical struct fields rejected** since launch. The poison-pill crash loop hid this as 100% silent data loss. Proof: `{"zebra":1,"apple":"hello"}` (agent) ≠ `{"apple":"hello","zebra":1}` (server). **Fix (4 layers):** (A) **`Event::new` canonicalization** — payload bytes now go through a `serde_json::Value` round-trip before hashing, making stored bytes identical to what the server re-serializes. (B) **`event_to_cloud_event` recompute** — at upload time, recomputes the hash over canonical bytes, recovering ALL 597M buffered legacy events created before fix A. (C) **Server graceful degradation** (prior commit `b40ed0c98`) — skip-and-continue instead of fail-fast, so one bad event never blocks the batch. (D) **Agent self-heal** — unconditional cursor advance on server Ok. **Health signals:** `cloud_sync_zero_accept_cycles` gauge + ERROR after 3 consecutive zero-accept cycles (catches the "false victory" failure mode). `ingest_rejected_events_total` + `cloud_sync_upload_rejected_events_total` track dropped events. Server per-event WARN replaced with single aggregated WARN per batch. Regression tests: `canonicalization_regression.rs`, `test_event_to_cloud_event_recomputes_hash_for_legacy_events`. **Verified post-deploy:** zero integrity failures, 5016 events uploaded before hitting daily limit. Remaining: 597M backlog blocked by 10K/day tenant limit (needs purge or limit raise — requires sudo)                                                                                                                                                                                                                                                                               |
| Gatus `[BODY].jsonpath.X` not working in v5.36.0 (FIXED 2026-07-18)                                     | Gatus 5.36.0's `[BODY].jsonpath.X` placeholder does not evaluate correctly for the dnsblockd health endpoint (returns valid JSON with `Content-Type: application/json`, but both boolean `== true` and numeric `> 0` jsonpath conditions evaluate to false). Other Gatus checks using `[STATUS]`, `[BODY] == pat(*)`, `[CONNECTED]`, `[DNS_RCODE]` all work fine. **Fix:** the DNS Blocker check uses only `[STATUS] == 200` and `[RESPONSE_TIME] < 500` — the actual blocking validation is handled by the separate "DNS Blocking Active" DNS check (queries `ads.google.com`, expects block IP). The `[BODY].jsonpath.realtime` check on the EMEET PIXY camera may also be affected. Verify the jsonpath syntax against the specific Gatus version before using it in new checks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `settings.signing` invalid + `"gpg.ssh"` dotted-key drops allowedSignersFile (FIXED 2026-07-19)         | Two bugs in `platforms/common/programs/git.nix`: (1) `settings.signing = { key; signByDefault }` writes a `[signing]` git-config section that git IGNORES — `user.signingKey` was never set. A non-declarative `~/.gitconfig` (read AFTER `~/.config/git/config`) carried `commit.gpgsign=false`, overriding HM's `true` — exactly the "tags signed, commits not" symptom. **Fix:** use HM's structured `programs.git.signing = { format="ssh"; key; signByDefault; }` which writes the REAL keys (`user.signingKey`, `commit.gpgSign`, `tag.gpgSign`, `gpg.format`, `gpg.ssh.program`). Then trash the rogue `~/.gitconfig` so HM is the single source of truth. (2) `settings."gpg.ssh".allowedSignersFile` (dotted-string-key form) gets DROPPED during rendering: nixpkgs `lib.generators.toGitINI`'s `gitFlattenAttrs` flattens the signing module's nested `gpg.ssh.program` to dotted key `"gpg.ssh"`, then `//` (shallow merge) collides with settings' `"gpg.ssh"` key — one silently wins, the other vanishes. **Fix:** use NESTED form `settings.gpg.ssh.allowedSignersFile` so `mkMerge` deep-merges `gpg.ssh.{program,allowedSignersFile}` BEFORE flattening — both survive in `[gpg "ssh"]`. HM has NO `allowedSignersFile` option in the `signing` submodule; it must go in `settings`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| post-deploy-check SIGPIPE false-FAIL (FIXED 2026-07-21)                                                 | `echo "$body" \| grep -q` under `set -o pipefail` fails on large HTTP bodies (>64KB pipe buffer). grep exits at the first match, echo gets SIGPIPE (exit 141) writing the remaining bytes, and pipefail makes the whole pipeline return 141. `! 141` is treated as success, producing a FALSE FAIL for any body-pattern check with a large response (Overview at 149KB). **Fix:** read the pattern from the body file directly: `grep -qiE "$pattern" /tmp/.smoke-body` — no pipe, no SIGPIPE                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| btrfs-verify-snapshots `stat` reads wrong timestamp (FIXED 2026-07-21)                                  | The verify script used `stat -c %Y "$snapshot"` to check freshness, but BTRFS snapshot subvolumes INHERIT the source subvolume's root directory mtime. Since `@` root dir was last modified Jun 26, ALL snapshots (even today's) show Jun 26 → false "24 days old" alerts. **Fix:** parse the snapshot NAME (`@.YYYYMMDDTHHMM` btrbk format) instead of using stat — the name encodes the real creation date                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| btrbk-data missing snapshot_dir (FIXED 2026-07-21)                                                      | btrbk requires `snapshot_dir` to EXIST before creating snapshots. `/data/.snapshots` was never created, so btrbk-data failed every night with "Failed to fetch subvolume detail for snapshot_dir" — `/data` (Docker volumes, Immich DB, AI models) had ZERO snapshots. **Fix:** tmpfiles rule `d /data/.snapshots 0755 root root -` in `snapshots.nix`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DiscordSync API startup race (5-11 min)                                                                 | DiscordSync's API server binds in a goroutine AFTER `discordBot.Start()` returns, which blocks on a thumb-hash backfill (4651 attachments at ~7/sec = ~11 min). During this window, `/api/stats` and `/healthz` are unreachable. The post-deploy-check retries `/healthz` 3× (5s apart), then distinguishes "process alive but not ready" (SKIP) from "process dead" (FAIL). Do NOT add a Gatus alert OR an `ExecStartPost` readiness gate that expects DiscordSync API to be up immediately after restart — an `ExecStartPost` will crash-loop the service (see "DiscordSync ExecStartPost crash-loop trap" above)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Crush Daily `/api/reports` returns date strings (not objects)                                           | The API returns `["2026-07-19", "2026-07-18", ...]` (array of ISO date strings), NOT objects with `"id"`. The post-deploy-check was looking for `"id"` and always SKIPped. **Fix:** match `"[0-9]{4}-[0-9]{2}-[0-9]{2}"` date pattern instead                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Crush Daily silent-zero-data — three independent bugs (FIXED 2026-07-28)                                | Three bugs surfaced as the same symptom (every report: 0 sessions). **Bug #1 (ARCHITECTURAL):** crush-daily shells `crush projects --json` which reads per-user `~/.local/share/crush/projects.json`. When run as the default `crush-daily` system user, that user has its own empty state. The crush CLI's `Load()` swallows ENOENT → silent `{"projects":[]}`. Worse, `/home/lars` is mode 700 with `getfacl` showing `mask::---` (the ACL mask zeroes out group `users:r-x` from /home). Even `SupplementaryGroups=users` does not let the system user traverse. **Fix:** upstream flake.nix now exposes `services.crush-daily.runAsUser`; SystemNix sets `runAsUser = config.users.primaryUser` in `configuration.nix`. SystemNix wrapper adds `preStart` chown for the data dir. **Bug #2 (CRUSH CLI SCHEMA DRIFT):** crush CLI v0.86 moved `prompt_tokens`/`completion_tokens`/`cost` from per-message to per-session columns. The scanner's `scanModelBreakdown` SQL queried `messages.prompt_tokens` → "no such column" on EVERY project. Fixed upstream in `4b94ed8`: query joins `sessions` and aggregates per-session cost/tokens. **Bug #3 (SQLITE DSN):** `sql.Open("sqlite", dbPath+"?_loc=...")` without the required `file:` URI prefix opened an empty in-memory database (treated `?` as part of filename). Every project lookup failed "missing required tables". Fixed in `83cb19d`: `sql.Open("sqlite", "file:"+dbPath+"?_loc=auto&_time_format=sqlite")`. **Lesson:** the post-deploy-check (`scripts/post-deploy-check.sh`) now asserts `latest_report.session_count > 0` — the missing check that should have caught this class of bug for weeks. The check is a hard FAIL (not WARN) so silent-zero regressions break deploys immediately. Every service that produces numerical reports needs a similar "data is real, not zero" assertion. **Also:** Go 1.26's `html/template` reverted `printf` arg order — `{{"%.2f"                                                                                                                                                                                                                         | printf .TotalCost}}`triggers "expected string; got float64"; fixed to`{{printf "%.2f" .TotalCost}}`(commit`b8095de`). **Source-of-truth:** the fresh `[{\"date\":\"2026-07-27\",\"projects\":[...16 detailed projects...],\"total_stats\":{\"session_count\":93,\"message_count\":13124,\"cost_usd\":4.157}}]` event in SQLite proves all three bugs were the cause.                                                                                                                                                          |
| upstream crush-daily `data_dir` resolution for sub-projects                                             | `crush projects --json` returns multiple entries pointing to the SAME `data_dir` when projects share a `.crush` parent (e.g. `/home/lars/projects/.crush/crush.db` is shared by `/home/lars/projects/foo`, `/home/lars/projects/foo/sub`, etc.). The collector iterates ALL of them and re-opens the same DB N times. Not a correctness bug but it's why error/warn logs explode at N=293 projects. The logs also masked the real failure when those re-opens all failed (before the `file:` fix). Dashboards should expect an `idempotent aggregate by data_dir` invariant, not an artifact-free iteration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| crush-daily `errgroup.WithContext` cancels best-effort parallel work (FIXED 2026-07-29)                 | crush-daily's `GenerateAllProjectInsights` used `errgroup.WithContext(ctx)` to run 5-concurrent LLM calls across ~229 projects per date. A SINGLE transient API failure (503, timeout, connection error) cancelled the context, killing ALL pending goroutines — hundreds of successful LLM calls already in flight were discarded. This caused 31 dates to have ZERO project insights (not just missing cross-project, as previously misdiagnosed). **Fix (upstream `868fe33`):** replaced `errgroup.WithContext` with plain `errgroup.Group` + a mutex-guarded `[]error` slice. All goroutines now run to completion regardless of individual failures. **Second bug in same pipeline:** `round1ProjectInsights` returned the error BEFORE the storage loop, so even partial results (from the few goroutines that finished before cancellation) were discarded. Fix: storage loop moved BEFORE the error return. **Lesson:** `errgroup.WithContext` is for all-or-nothing pipelines where partial success is meaningless. For best-effort parallel work where partial results have value, use plain `errgroup.Group` with error collection. Always persist/checkpoint partial results BEFORE returning an error — a function that returns `(results, error)` should never discard `results` when `error != nil`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| crush-daily `Yesterday()` timezone bug (FIXED 2026-07-29)                                               | `collector.Yesterday()` used `time.Now().AddDate(0,0,-1).Truncate(24*time.Hour)` — but `Truncate(24h)` snaps to **UTC midnight**, not local midnight (documented Go behavior). In CEST (+02:00), the collect job at 00:30 and the insights job at 03:00 compute **different "yesterday" dates**: collect (00:30 CEST = 22:30 UTC prev day) truncates to UTC midnight → 2 days behind; insights (03:00 CEST = 01:00 UTC) truncates to UTC midnight → 1 day behind. Collect writes data for date X, insights reads date X+1 → `ErrNoDataCollected` → classified as `Permanent` by the retry classifier → never retried → silently fails every night. This is why 2026-07-19 through 2026-07-25 had data but zero insights. **Fix (upstream):** replaced `Truncate` with `time.Date(y, m, d, 0, 0, 0, 0, now.Location())` which correctly truncates to local midnight. **Lesson:** Go's `time.Time.Truncate(d)` operates on the absolute time instant (UTC-based), ignoring the `Time`'s `Location`. For local-midnight truncation, use `time.Date()` with the local location. This is a common Go timezone trap — `Truncate` looks like it should respect the timezone, but it doesn't                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ProtectHome = "read-only"` + binary defaults to `$HOME` state (FIXED 2026-07-21)                       | The `file-and-image-renamer` health service runs under `ProtectHome = "read-only"` with only `cfg.dataDir` in `ReadWritePaths`. The Go binary defaults to writing state to `~/.renamer-history.json` and `~/.file-renamer-hashes.db` (in `$HOME`, read-only). `history.New()` and `hashdb.New()` fail on the write attempt; `initServiceOrWarn` swallows the error and returns nil. Every handler calling `.GetStats()` on nil panics → HTTP 500. **Fix:** redirect state via env vars `HISTORY_FILE_PATH=${cfg.dataDir}/history.json` + `HASHDB_PATH=${cfg.dataDir}/hashes.db` (upstream commit `ca95be5` added `utils.LoadPathFromEnv` support). This is the canonical escape hatch: any hardened service with a binary defaulting to `$HOME` state must override the path via env var into a writable `ReadWritePaths` directory. Audited: no other SystemNix service has this bug class                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Two services sharing state = split-brain risk (FIXED 2026-07-21)                                        | `file-and-image-renamer` has TWO services: watcher (HM user service) + health dashboard (system service). Both must point at the SAME state files. When only the health service was reconfigured to use `dataDir` paths, the watcher kept writing to `~/.renamer-history.json` (real data) while the dashboard read `~/.file-renamer/history.json` (empty `[]`). The dashboard showed `total_operations: 0` despite weeks of real history — a green dashboard silently lying. **Lesson:** when reconfiguring state paths on a service that SHARES state with another service, update ALL consumers. Verify data correctness (not just HTTP 200) after deploy — a `fetch /status` returning 0 on a weeks-old system is suspicious. The post-deploy-check now asserts `history.total_operations > 0`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `initServiceOrWarn` nil-swallow anti-pattern (upstream)                                                 | Upstream `file-and-image-renamer` has `initServiceOrWarn()` in `cmd/file-renamer/helpers.go` that logs a WARN and returns nil on init failure instead of hard-failing. Callers that dereference the returned `*history.Log`/`*hashdb.DB` without a nil check panic. **Defense-in-depth (upstream `ca95be5`):** `pkg/healthd/safe_accessors.go` wraps all call sites with `safeHashStats()`, `safeHistoryStats()`, `safeHistoryEntries()` that return zero values on nil instead of panicking. Future init failures degrade gracefully. This pattern (swallow + nil-safe callers) is acceptable for optional features but dangerous for required state — prefer fail-loud (`initServiceOrFail`) when the backing store is mandatory                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `health-status.json` hardcoded path (latent)                                                            | The `file-renamer health` command writes `health-status.json` to a HARDCODED `<homeDir>/.file-renamer/health-status.json` (no env var override, unlike history/hashdb). This works because the default `dataDir` is `~/.file-renamer`, but if `cfg.dataDir` is changed to a non-default path, `health-status.json` would still write to `~/.file-renamer/` which may NOT be in `ReadWritePaths` under `ProtectHome = "read-only"`. Not a current bug (dataDir defaults correctly), but a latent issue if dataDir is ever customized. Written only by the `health` command (not the watcher). Transient — regenerated on every health check, safe to delete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DiscordSync ExecStartPost crash-loop trap                                                               | DiscordSync's API server binds in a goroutine AFTER thumb-hash backfill (3139+ attachments at ~7/sec = 5-11 min). `ExecStartPost` runs IMMEDIATELY after `ExecStart` — any `/readyz` probe during backfill fails → systemd marks service failed → `Restart=on-failure` → infinite crash loop. NEVER add an `ExecStartPost` readiness gate to discordsync. This is a subclass of the existing "API startup race" gotcha but more severe — it actively kills the service. Health monitoring must use Gatus (60s interval, tolerates startup delay). The upstream `healthCheck` option also has a URL bug (`http://localhost:${apiAddr}` expands to a malformed three-colon URL when apiAddr contains host:port) — set `healthCheck = false` and rely on Gatus                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| OTel endpoint format (`otlptracehttp.WithEndpoint`)                                                     | Go's `otlptracehttp.WithEndpoint()` expects `host:port` WITHOUT scheme. The SDK constructs the full URL internally (`http://<endpoint>/v1/traces`). Setting `OTEL_EXPORTER_OTLP_ENDPOINT = "http://localhost:4318"` produces `http://http:%2F%2Flocalhost:4318/v1/traces` — a malformed URL with `:%2F%2F` parsed as the port. Correct: `"localhost:4318"` (no scheme). This is NOT caught by `nix eval` — it validates string rendering, not API contract correctness. Always verify env var formats against the target library's API docs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| monitor365 graphical collector prerequisites (FIXED 2026-07-22)                                         | Three prerequisites for graphical collectors: (1) **Display env** — the upstream `displayUser` option triggers pgrep + heredoc discovery of DISPLAY/WAYLAND_DISPLAY/XAUTHORITY/etc from the compositor's `/proc/<pid>/environ`. Requires `ProtectProc = "default"` (upstream default `invisible` hides niri's PID from cross-user pgrep). (2) **Device access** — keystroke/mouse collectors need the `input` group for `/dev/input/event*` read access; camera needs the `video` group for V4L2 `/dev/video*`. Added via `users.users.monitor365.extraGroups = [ "input" "video" ]`. (3) **Timing** — monitor365 starts at boot (`default.target`) before niri exists; graphical collectors fail 3 times and are "permanently disabled" for the process lifetime. A systemd path unit (`monitor365-graphical-restart`) watches for `/run/user/1000/wayland-1` and restarts the agent when the Wayland session appears, re-running display discovery. The hardcoded `Environment=` approach was REMOVED — `config.users.users.lars.uid` is `null` at eval time (`isNormalUser` assigns uid at activation), making `/run/user/${toString uid}` evaluate to `/run/user/` (empty uid)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| monitor365 backup health monitoring (FIXED 2026-07-22)                                                  | AGENTS.md rule 9 mandates monitoring every new service. The nightly DuckDB backup (`monitor365-server-backup.timer`) is a systemd oneshot with no HTTP endpoint. A Prometheus textfile collector (`monitor365-backup-health` timer, every 5min) writes `monitor365_backup_healthy` (1 if most recent `*.backup_*.db` is <25h old, 0 otherwise), `monitor365_backup_age_hours`, and `monitor365_backup_last_success_timestamp` to `/var/lib/prometheus-node-exporter/textfile_collectors/monitor365-backup.prom`. Gatus checks `monitor365_backup_healthy 1` via the node_exporter `/metrics` endpoint and alerts on Discord if stale/missing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| monitor365 DuckDB WAL corruption crash-loop (FIXED 2026-07-22)                                          | After an unclean shutdown (OOM, WDT reset, power loss), DuckDB's WAL replay fails with "Failure while replaying WAL file … Calling DatabaseManager::GetDefaultDatabase with no default database set" — an INTERNAL/assertion error. The server crash-loops indefinitely (291+ restarts), port 3001 stays down, the agent's circuit breaker opens (220K+ consecutive failures), and all telemetry accumulates in the disk buffer with events being dropped. **Fix:** `ExecStartPre` (`monitor365-duckdb-heal`) always removes the `.wal` file before startup. DuckDB checkpoints the WAL into the main DB on graceful shutdown and deletes it — a `.wal` present at startup ALWAYS means unclean shutdown. Always removing it eliminates this crash class. Data loss is limited to the last uncheckpointed session (acceptable for monitoring). Also restores from the nightly backup if the main DB is missing/empty. Same defensive pattern as the SigNoz migration-lock clear                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| PMA daemon `DefaultChain()` vs `DefaultChainFromEnv()` (FIXED 2026-07-22)                               | The PMA daemon's committer fell back to `providers.DefaultChain()` (creates providers with empty `HTTPProviderConfig{}` — no API keys), while the CLI path used `providers.DefaultChainFromEnv()` (reads `MINIMAX_API_KEY` from env). The systemd `EnvironmentFile` correctly set `MINIMAX_API_KEY`, but `DefaultChain()` never calls `os.Getenv`. Every commit failed with "no AI provider available". **Fix (upstream PMA `d1d013d2`):** `committer.New()` now uses `DefaultChainFromEnv()` when no provider is injected. Lesson: when a library has two factory functions with similar names, verify which one actually reads configuration. The split between CLI and daemon code paths made this invisible — the CLI worked fine while the daemon silently failed on every batch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `system-health` module                                                                                  | `modules/nixos/services/system-health.nix` is a Prometheus textfile collector for systemd service state, `user-1000.slice` memory, GPUActive thresholds, and monitor365 DuckDB buffer pressure. Gatus 5.36.0's `pat()` cannot do numeric comparison, so the collector pre-computes boolean flags (0=ok, 1=alert) like `system_service_start_limit_hit`, `system_gpu_active_over_threshold`, `system_monitor365_buffer_pressure`, and `system_any_service_cpu_over_threshold`. `system_service_nrestarts` is typed as a `gauge` (not `counter`) because systemd `NRestarts` resets on reboot. The monitor365 collector auto-disables when `services.monitor365-server.enable` is false and reads `stateDir` from the upstream option rather than hardcoding. Disable `collectUserSlice`/`collectGpuActive` on non-desktop hosts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `wrapWithMemoryLimit` helper                                                                            | `lib/default.nix` provides `wrapWithMemoryLimit pkgs { name, maxMemory, command, extraArgs ? [] }` which creates a `-memlimit` wrapper script. It runs the command under `systemd-run --user --collect --wait --same-dir --setenv="*" -p MemoryMax=...` so cwd and environment propagate. Designed for dev/test commands in user packages/devShells (NOT inside Nix build sandboxes). Usage: `go-test-memlimit ./...` runs `go test ./...` with a 4G cgroup limit. `cargo-test-memlimit` and `pnpm-test-memlimit` are wired into the desktop HM package set                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| go-git `repo.Config()` only reads local scope (FIXED 2026-07-22, DEPLOYED 2026-07-23)                   | go-git's `repo.Config()` reads ONLY `.git/config` (local scope) — it does NOT merge global (`~/.gitconfig`, `~/.config/git/config`) or system (`/etc/gitconfig`) scopes. Since Home Manager writes `user.name`/`user.email` to `~/.config/git/config`, PMA's auto-commit daemon always saw empty values → fell back to `Unknown Author <unknown@example.com>`. **Fix:** both `go-commit` (v0.4.0, `pkg/commit/git/gogit.go`) and PMA (`internal/application/services/git/service_gogit.go`) now use `git config user.name`/`git config user.email` via CLI (`exec.CommandContext`), which respects all config scopes. The `git` binary is already in the service `PATH`. Lesson: NEVER use go-git's config reader for user identity — always shell out to `git config` which merges all scopes like the real git CLI. **SystemNix fix (2026-07-23):** SystemNix flake.lock pinned go-commit to `3f74fd19` (master, pre-fix). `mkPreparedSource` overrides go.mod v0.4.0 with the flake input source — buggy code compiled despite go.mod pin. **Fix:** go-commit added as top-level flake input pinned to `refs/tags/v0.4.0`, with `projects-management-automation.inputs.go-commit.follows = "go-commit"` (same pattern as `samber-do-auditlog`). Generic `mkPreparedSource` trap: flake input source ALWAYS overrides go.mod version                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| monitor365 libspa-sys bindgen breakage (5ee717e3+) (RESOLVED 2026-07-29)                                | Upstream commit `5ee717e3` switched from `cargoVendorDir` (runCommand + `nix/patch-libspa-vendor.sh`) to `[patch.crates-io] libspa-sys = { path = "vendor-patches/libspa-sys" }` + `.clang_macro_fallback()` in build.rs. This produces EMPTY bindgen output — 699 errors (`spa_pod`, `spa_dict`, `SPA_TYPE_*` all missing from `spa_sys`). Root cause: bindgen cannot resolve SPA headers through the patch.crates-io path override in the sandbox. **Now unpinned to `ref=master`** — the bindgen issue was resolved upstream and builds successfully. If it breaks again, re-pin to the last working commit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| file-and-image-renamer auth fallback + vision-review-agent integration (UPDATED 2026-07-23)             | **Current pin:** `b181444` (includes auth fix `8bf60bd` + provider architecture redesign). **Root cause (auth):** 401/403 auth errors were classified as `ErrorTypeAPI` (retryable), causing the fallback provider to retry the same invalid key 3 times and NEVER switch to the secondary provider. **Fix (3 layers):** (A) **Upstream** (`8bf60bd`): added `ErrorTypeAuth` — non-retryable, triggers fallback by default. 401/403 and "API key not configured" now map to it. (B) **SystemNix sops**: `synthetic_api_key` is provisioned via `mkKeyedSecrets` from `crush-daily.yaml` as `file_renamer_synthetic_api_key` (owned by `primaryUser:users`). (C) **SystemNix module**: `apiKeyFile` is `nullOr` (default `null`); when null, no `ZAI_API_KEY_FILE` is emitted, so the binary uses Synthetic directly. **Provider redesign (`b181444`):** Vision layer now delegates to `github.com/larsartmann/vision-review-agent` (v0.1.0 library) + `charm.land/fantasy` (v0.35.1 AI provider abstraction) instead of raw HTTP. New error types: `ErrorTypeRateLimit` (HTTP 429, triggers fallback), `ErrorTypeContextTooLarge` (image exceeds model limit, never retryable). `FallbackProvider` gained `FallbackOnRateLimit`. Config env vars UNCHANGED — `ZAI_API_KEY[_FILE]`, `SYNTHETIC_API_KEY[_FILE]`, `GLM_MODEL`, `SYNTHETIC_MODEL`, `DEAD_LETTER_PATH`, `WATCH_PATHS`, `HISTORY_FILE_PATH`, `HASHDB_PATH`. Health dashboard routes: `/health`, `/status`, `/metrics`, `/events`, `/events.csv`, `/partials/*`. SystemNix module is fully compatible — no changes needed. All 26 upstream test packages pass                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| cqrs-lint samber-do-auditlog version drift (FIXED 2026-07-23)                                           | go-cqrs-lite's `mkPreparedSource` replaces `github.com/larsartmann/samber-do-auditlog` in go.mod with the flake input source. The flake input was `ref=master`, which pulled v0.6.0+ code where `ServiceByName` takes `auditlog.ServiceName` (a typed string). cmdguard's `AuditLogServiceByName` passes a bare `string` → type mismatch compilation error. cmdguard's own go.mod pins v0.5.0 (where `ServiceByName` takes `string`), but `mkPreparedSource` overrides it with the flake input. **Fix:** SystemNix adds `samber-do-auditlog` as a top-level flake input pinned to `refs/tags/v0.5.0`, and `go-cqrs-lite.inputs.samber-do-auditlog.follows = "samber-do-auditlog"` ensures the correct version propagates. The vendorHash is UNCHANGED because the local replace doesn't affect go.sum                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

| mr-sync outputs signature missing `...` (FIXED 2026-07-23) | Upstream commit `6492eef` removed `nixpkgs` from the `outputs` function parameter list (it's unused — pkgs come via flake-parts `perSystem`) but forgot to add `...` to catch the extra argument. Since `nixpkgs` IS declared in upstream's `inputs`, the flake evaluator passes it to `outputs`, producing `function 'outputs' called with unexpected argument 'nixpkgs'`. **Fix:** SystemNix pins mr-sync to `3db4fb2` (last commit before the break). Unpin to `ref=master` once upstream adds `...` to the outputs signature. Same class of bug as the temporary hierarchical-errors `d8fd4ca` break — all other LarsArtmann repos use `...` in their outputs params and are unaffected |
| OpenSEO GSC OAuth callback Caddy vHost hand-rolled | OpenSEO's `seo.${domain}` vHost in `caddy.nix` is NOT using `protectedVHost` — it's hand-rolled to exempt `/api/gsc/oauth/callback` from oauth2-proxy forward-auth. The GSC callback IS browser-initiated (the browser carries the `_oauth2_proxy` cookie, `SameSite=lax`, sent on top-level GET navigations), so it would pass forward-auth anyway. But exempting OAuth callback endpoints from auth layers is best practice — makes the flow deterministic and immune to SameSite policy changes or cookie-expiry edge cases. Do NOT "simplify" by switching back to `protectedVHost`. The vHost still applies forward-auth to all other paths for external clients, with LAN bypass. The `openseo-validate` ExecStartPre checks that GSC env vars (`GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `BETTER_AUTH_SECRET`) are non-empty when `services.openseo.googleSearchConsole.enable = true` — sops placeholders render as empty strings for missing keys |
| SearXNG `enable` infinite recursion | The SystemNix wrapper module MUST NOT set `services.searx.enable = true` inside `lib.mkIf cfg.enable` where `cfg = config.services.searx` — this creates a self-referential cycle that causes `nix flake check` to fail with infinite recursion. The `enable` is set in `configuration.nix`; the module only configures around it via `lib.mkIf cfg.enable` |
| SearXNG Redis/Valkey removed (limiter disabled) | Redis was only used for bot detection sliding-window counters (`server.limiter = true`). On a private LAN instance with all traffic passlisted, the limiter was doing nothing useful but adding a synchronous unix-socket round-trip on every search. Disabled `limiter`, removed `redisCreateLocally`, removed `limiterSettings`, removed `services.redis.servers.searx`. If re-enabling the limiter for public access, you MUST set `redisCreateLocally = true` and configure `limiterSettings` with `trusted_proxies` + `pass_ip` (BOTH `127.0.0.0/8` AND LAN subnet) |
| SearXNG no `restartTriggers` in direct server mode | The nixpkgs searx module only sets `restartTriggers` for uWSGI mode (`configureUwsgi = true`). In direct server mode (the default), settings changes regenerate `settings.yml` via `searx-init` but the `searx` process itself is NOT restarted → stale config. SystemNix adds `restartTriggers` on `searx.service` with `builtins.toJSON` of settings + package path |
| SearXNG limiter behind reverse proxy | MOOT — limiter is disabled (`server.limiter = false`), Redis removed. If re-enabled: Caddy proxies from `127.0.0.1` (in default `trusted_proxies`) and sets `X-Forwarded-For`. SearXNG's ProxyFix middleware walks the chain to find the first untrusted IP. The LAN subnet must be in `pass_ip` for unrestricted private access |
| SearXNG port 8888 conflict with SigNoz OTel collector | Port 8888 was originally assigned to SearXNG in `lib/ports.nix`, but it was ALREADY in use by SigNoz's OTel collector metrics endpoint (`/metrics` with ClickHouse exporter metrics). The SigNoz OTel collector binds to `127.0.0.1:8888` at startup and is not configurable via the NixOS module. SearXNG was changed to port **8889**. Always check `ss -tlnp` for port availability before assigning — the pre-deploy-check now validates this (check #9) |
| SearXNG limiter `pass_ip` must include localhost | MOOT — limiter is disabled. If re-enabled: without `127.0.0.0/8` in `botdetection.ip_lists.pass_ip`, direct localhost access (health checks, functional tests) gets rate-limited with 429 Too Many Requests. The `pass_ip` list must include BOTH the LAN subnet (for LAN clients) AND `127.0.0.0/8` (for local health checks and Caddy-internal requests without X-Forwarded-For) |
| SearXNG autocomplete privacy | `autocomplete = "google"` leaked every keystroke to Google's autocomplete API. Changed to `duckduckgo`. Google general search is also `inactive: true` in SearXNG defaults — the `engines` section re-enables Google (general, images, videos) + Bing for result quality. SearXNG proxies requests through itself (server IP, not user IP), so privacy is still far better than direct Google use |
| SearXNG hostnames plugin | The `hostnames` plugin is `active: true` by default but does NOTHING without a `hostnames:` config section in settings — `init()` returns False and the plugin is never registered. Configured with `high_priority` (Stack Overflow, MDN, docs.rs, pkg.go.dev, GitHub, NixOS/Arch wikis) and `remove` (Pinterest spam). Regex patterns match on `parsed_url.netloc`. Priority: HIGH overrides LOW if both match |
| SearXNG `formats = [ "html" ]` blocks JSON API | Setting `search.formats = [ "html" ]` prevents API scraping by blocking `format=json` requests (returns 403 Forbidden). This is a deliberate privacy hardening measure. The `/healthz` endpoint is exempt from this restriction. When testing search functionality, use HTML mode (no format param), not JSON |
| SearXNG XFF health-check noise | SearXNG logs `X-Forwarded-For nor X-Real-IP header is set!` as ERROR for direct localhost health checks (Gatus, post-deploy-check) that bypass Caddy. Benign — `/healthz` is exempt from the limiter. Caddy-proxied traffic gets `X-Forwarded-For` (auto) + `X-Real-IP` (via `proxyTo` helper). Not fixable for direct localhost access without a proxy in the path |
| SearXNG favicon cache + SQLite ResourceWarning | `faviconsSettings.cache.db_url = "/var/cache/searx/faviconcache.db"`. The nixpkgs searx module sets `CacheDirectory = "searx"` (creates `/var/cache/searx/` with DynamicUser ownership). The `ResourceWarning: unclosed database` in logs is an upstream SearXNG Python issue (favicon cache SQLite connection not properly closed in all code paths), not a config problem |
| SearXNG engine init DNS race (FIXED 2026-07-29) | SearXNG engines that call network during `init()` (wikidata SPARQL property fetch, radio browser server-list DNS, ClearURLs tracker-pattern download) fail with `[Errno -2] Name or service not known` at boot and stay **permanently disabled** for the process lifetime — SearXNG never retries init. Root cause: `searx.service` had `After=network.target` but NO dependency on `dnsblockd.service`; `network.target` is reached before DNS is ready. The wikidata failure was previously misdiagnosed as a "403" — it never reached the remote API. **Fix:** `searx.service` + `searx-init.service` now have `after = [ "dnsblockd.service" ]` + `wants` + an `ExecStartPre searxng-wait-dns` gate that probes `getent hosts wikidata.org` (same DNS-gate pattern as discordsync). The gate exits 0 on timeout (degradation, not hard failure — SearXNG still starts with engines that don't require init-time network). Brave 429 "too many requests" is a separate, genuinely transient issue — SearXNG auto-bans and retries after `ban_time_on_fail=5s` |
| sops crush-daily user mismatch (FIXED 2026-07-28) | sops secrets for `crush-daily.yaml` used `owner = "crush-daily"; group = "crush-daily"` but the service runs as `primaryUser` (via `runAsUser`), and the `crush-daily` system user was never created. `sops-install-secrets` fails with `failed to lookup user 'crush-daily'` which blocks ALL secrets atomically — every secret deployment fails silently. Fix: use `owner = primaryUser; group = "users"` (same pattern as `file-and-image-renamer`). The `svcEnabled` guard doesn't help — it checks service `.enable`, not whether the user exists |
| Caddy `proxyTo` helper adds X-Real-IP | `protectedVHost` now uses a `proxyTo port` helper that wraps `reverse_proxy` with `header_up X-Real-IP {remote_host}`. This benefits ALL services behind `protectedVHost` (Homepage, SearXNG, Twenty, etc.) — they can now see the real client IP, not just `127.0.0.1`. Caddy already sends `X-Forwarded-For` automatically, but some apps prefer `X-Real-IP` |
| Caddy `proxyTo` is canonical — no bare `reverse_proxy` | ALL reverse_proxy directives in `caddy.nix` (including non-protectedVHost ones like Forgejo, SigNoz, Gatus, Pocket ID, oauth2-proxy, OpenSEO, Monitor365) now use `${proxyTo PORT}`. Never write bare `reverse_proxy localhost:PORT` — it omits `X-Real-IP` and services see `127.0.0.1` as the client IP, causing XFF log noise and breaking IP-based rate limiting |
| Homepage `mdi-*` icon names don't exist | The dashboard-icons pack (bundled via `enableLocalIcons = true`) contains PNG files named by service (e.g., `searxng.png`, `forgejo.png`). It does NOT contain Material Design Icons (`mdi-*` names). Using `icon: mdi-shield` produces a 404. Verify icon names against `/nix/store/*-homepage-dashboard-*/share/homepage/public/icons/` before assigning. Many "obvious" names also don't exist: `shield`, `ai`, `monitor`, `voice`, `whisper`, `search`, `camera`, `twenty`, `taskwarrior` |
| Prebuilt ELF binaries in Go modules break FOD purity | Go modules that ship prebuilt ELF binaries (e.g., `go-branded-id` v0.5.0) violate Nix's Fixed-Output Derivation purity. The binary's buildID differs per build environment, causing hash mismatches. The `md-go-validator` catches this pattern. Workaround: use `buildGoModule` with `subPackages` to only build the Go source, or vendor the binary via `fetchurl` with a fixed hash. See `docs/status/archived/2026-07-28_18-16_md-go-validator-fod-purity-break-from-prebuilt-binary.md` |
| `switch-to-configuration` ignores `restartTriggers` on `Type=oneshot` + `RemainAfterExit=true` | systemd's `switch-to-configuration` does NOT restart `Type=oneshot` + `RemainAfterExit=true` services even when `restartTriggers` change. The service stays "active (exited)" and the new script in the Nix store never executes. This is a fundamental systemd/NixOS limitation. **Fix:** `deploy.sh` now explicitly runs `sudo systemctl restart` on all provisioner oneshots after `nh os switch`. The 8 affected services: signoz-provision, pocket-id-provision, forgejo-generate-token, forgejo-github-sync, forgejo-ensure-repos, twenty-fix-collation, dnsblockd-attach-ip, monitor365-schema-migrate |
| monitor365 daily event limit override (FIXED 2026-07-29) | Upstream hardcodes a 10K/day tenant event upload limit (`crates/api-types/src/tenant.rs:39`), stored in the DuckDB `tenants.max_events_per_day` column. The 597M backlog from the integrity-hash fix would take ~163 years to drain at 10K/day. `monitor365-schema-migrate` now runs `UPDATE tenants SET max_events_per_day = 1000000000` (1B) on every boot/deploy, making the limit declarative and ensuring the backlog drains in ~1 day. The `event_to_cloud_event` hash recompute (`ebb26a0bd`) makes legacy events recoverable despite the old hash format. Do NOT remove the UPDATE — the server re-syncs the upstream default on bootstrap |
| SigNoz provisioner `\|\| true` anti-pattern (FIXED 2026-07-29) | The entire `signoz-provision` script used `\|\| true` on every curl, swallowing ALL errors (HTTP 400/500, network failure, wrong API format). The script ALWAYS exited 0, making failure detection impossible. Alert rules POSTed but silently failed (GET returned empty). **Fix:** POST calls now use `curl -s -o /dev/null -w "%{http_code}"` and check the status code. A final verification step asserts `GET /api/v1/rules → .data.rules length > 0`. Script exits 1 on any failure, triggering `onFailure` alerting. DELETE calls keep `\|\| true` (idempotent cleanup — 404 is expected) |
| monitor365 circuit breaker + early-flush CPU burn (FIXED 2026-07-29) | The cloud sync loop's early-flush optimization bypassed backoff sleep when the segment buffer had ≥200 events. With a massive backlog and the circuit breaker open (1.15M consecutive failures), every operation short-circuited in microseconds — the loop busy-spun at ~16Hz with zero progress, burning 295% CPU (~3 cores) for 23+ hours. **Fix (upstream `f72cf1073`):** the `should_early_flush()` path now respects the circuit breaker state and always sleeps when the CB is open. **Defense-in-depth:** SystemNix adds `CPUQuota=200%` to `harden()` (`lib/systemd.nix`) — ALL services get a 2-core hard cap by default. AI services (ollama 400%, hermes 400%, immich-ml 300%, minecraft 300%, whisper-asr 300%) have explicit overrides. Gatus alert "CPU Runaway (Any Service)" fires when ANY monitored service exceeds 150% average CPU over a 5-min window |
| monitor365 server COALESCE version NULL crash-loop (FIXED 2026-07-29) | The `tenants.version` and `users.version` columns (added by migration `062_add_version_columns.sql` as `NOT NULL DEFAULT 0`) contain NULL values in legacy rows from projection replay. The COALESCE wrappers (`COALESCE(tenants.version, 0) AS version`) were removed on 2026-07-22 after the DuckDB alias-shadow binder fix, under the assumption columns were non-nullable. They weren't. Server crash-looped on bootstrap: `Invalid column type Null at index: 9, name: version`. **Fix (upstream `b900d3454`):** restored COALESCE with qualified table prefix (`COALESCE(tenants.version, 0) AS version`) — the qualified reference avoids the DuckDB alias-shadow binder error AND handles legacy NULLs. Same fix applied to `users.version`. Lesson: "confirmed non-nullable" is never confirmed until you've audited every INSERT path including event projection replays |
| CPUQuota defense-in-depth in `harden()` | `lib/systemd.nix` now defaults `CPUQuota = "200%"` (2 cores) for ALL services using `harden {}` or `hardenUser {}`. This prevents CPU runaway from code bugs (like the monitor365 busy-loop). Override per-service by passing `CPUQuota = "400%"` to `harden {}` or using `lib.mkForce "400%"` in the serviceConfig. AI/compute services that need more: ollama (400%), hermes (400%), immich-machine-learning (300%), minecraft (300%), whisper-asr (300%). The `mkDefault'` wrapper preserves priority — explicit service-level values override the default |
| Per-service CPU alerting via `system-health` | `system-health.nix` collector tracks `CPUUsageNSec` per monitored service between 5-min intervals, computes average CPU% via delta/elapsed, and emits `system_service_cpu_percent{service=...}` + `system_service_cpu_over_threshold{service=...}` (threshold=150%). A summary metric `system_any_service_cpu_over_threshold` is 1 if ANY service exceeds the threshold. Gatus check "CPU Runaway (Any Service)" alerts on Discord when this fires. The CPU state file (`/var/lib/prometheus-node-exporter/textfile_collectors/.system_health_cpu_state`) stores previous readings between runs |
| monitor365 + go-commit both unpinned to `ref=master` (2026-07-29) | Both `monitor365` and `go-commit` flake inputs were changed from pinned commits/tags to `ref=master`. **monitor365:** unpinned from `0615301` after the libspa-sys bindgen breakage was resolved upstream. The `[patch.crates-io]` path override works correctly now. **go-commit:** unpinned from `refs/tags/v0.4.0` — the `git config` CLI fix IS present on master (`fd9a9664`). Both are verified safe. If libspa-sys breaks again, re-pin monitor365 to the last working commit |
| `git insteadOf` causes flake.lock SSH pollution (restored on user demand 2026-07-30) | The global git config rule `url.git@github.com:.insteadof=https://github.com/` rewrites ALL `https://github.com/` URLs to `git@github.com:` (SSH). This causes `nix flake lock` to record `ssh://git@github.com/...` URLs in lock files instead of `github:` type entries. Re-introduced per user request after the 2026-07-29 removal. **Workaround for fresh locks:** `GIT_CONFIG_GLOBAL=/dev/null nix flake update <input>` bypasses the rewrite (and the libsecret credential helper). All other repos are public and work fine WITHOUT the rule via `nix.conf` `access-tokens = github.com=gho_...`. 4 GOPRIVATE repos use `git+ssh://` URLs directly in `mkLarsPackages`. Use `github:` URLs in all flake inputs so SSH rewriting is a no-op |
| SigNoz v0.127 v5 alerting API (FIXED 2026-07-29) | SigNoz 0.127.1 replaced the legacy alert-rules payload. The OLD `{"data":{"rule":{...}}}` shape POSTs to `/api/v1/rules` with `HTTP 400 condition: field is required`. The NEW v5 schema is flat: top-level `{"alert","alertType":"METRIC_BASED_ALERT","ruleType":"promql_rule","version":"v5","condition":{"compositeQuery":{"queryType":"promql","panelType":"graph","queries":[{"type":"promql","spec":{"name":"A","query","step"}}]},"op","target","matchType":"last"}}`. Operator mapping preserved from legacy: `AND`→`above_or_equal`, `AND_NOT`→`below`. v5 REQUIRES `preferredChannels` non-empty (every rule must reference a channel). The list endpoint returns rule objects keyed by `.alert` (NOT `.name`); idempotent delete must match `select(.alert == $n)`. See `_signoz-alerts.nix` `mkRule` |
| SigNoz channel recreation conflicts (FIXED 2026-07-29) | In v0.127.1 channels are alertmanager receivers. Alert rules reference `"Discord Alerts"` via `preferredChannels`, registering that receiver. The provision script's old delete+recreate of the standalone channel then fails with `HTTP 400 alertmanager_config_conflict: the receiver name has to be unique` because the rules still reference it. **Fix:** channel creation is now idempotent — skip when the channel already exists (it persists with its webhook across runs; only create when absent). The webhook rarely rotates; if it does, update the channel out-of-band |
| Forgejo `GET /admin/users/{u}/keys` → 405 (FIXED 2026-07-29) | Forgejo removed the GET method on `/api/v1/admin/users/{username}/keys` (returns `405 Method Not Allowed`), causing `forgejo-ssh-keys` to exit 22 (`curl -sf` fails on 4xx). The POST (add key) on that path still works. **Fix:** the dedup GET now uses the public `GET /api/v1/users/{username}/keys` (exists, works with a token, returns objects with `.key`). POST stays on `/admin/users/{u}/keys`. See `_forgejo-scripts.nix` `addKeysScript` |
| Overview 503 after every deploy + watchdog (FIXED 2026-07-29) | Overview runs project discovery ONCE at startup and never retries. On deploy, PMA restarts and re-scans ~293 projects (slow), so Overview's `/v1/discover` request to the daemon times out (`context deadline exceeded`) → Overview caches nil → permanent `HTTP 503`. Three-layer fix in `modules/nixos/services/overview.nix`: (1) ExecStartPre gate waits for the daemon `/v1/health` before starting; (2) `partOf = projects-management-automation.service` restarts Overview when PMA restarts; (3) `overview-discovery-watchdog` timer (every 2 min) restarts Overview when it is 503 BUT the daemon is healthy — converges on its own once PMA's scan finishes, no extra deploy. Overview wrapper module owns the upstream import now (removed the direct `inputs.overview.nixosModules.default` from `systems/evo-x2.nix`). Upstream real fix would be Overview retrying discovery |
| PMA discovery OOM during restart (FIXED 2026-07-29) | PMA's project-discovery daemon re-scans all ~293 projects on every restart, spiking memory past the upstream `MemoryMax=8G` → kernel OOM-kill (`status=9/KILL`). An OOM mid-deploy kills the daemon socket, stranding Overview's in-flight discovery (see above). **Fix:** SystemNix PMA wrapper raises `MemoryMax = lib.mkForce "12G"` (`projects-management-automation.nix`). Steady state is far lower; the spike is transient |
| DiscordSync Turso quota hard-fail (FIXED 2026-07-29) | Turso returns `HTTP 403 "SQL read operations are forbidden (reads are blocked, do you need to upgrade your plan?)"` when the plan quota is exhausted. Upstream `OpenTursoSync` (`internal/db/factory.go`) retried 3x then HARD-FAILED (exit 69) instead of degrading, crash-looping the service to `start-limit-hit`. The SystemNix comment assumed graceful fallback but the code regressed. **Fix (deployed):** `OpenTursoSync` now detects the quota error via `tursostorage.IsQuotaExceeded` and falls back to opening the local replica as plain SQLite (`openSQLite`, `syncHandle=nil`, which the runtime already supports) → service runs local-only until the quota resets. Flake input bumped (`nix flake lock --update-input discordsync`) after pushing the fix to DiscordSync master. Regression test: `internal/db/factory_test.go`. The Turso plan quota itself is EXTERNAL — upgrade the plan or wait for reset for cloud sync to resume |
| SigNoz alert rule `target=0` + `above_or_equal` = always firing (FIXED 2026-07-30, ASSERTION ADDED 2026-07-30) | Four rules used `target = 0` with `op = "above_or_equal"` (the default), which means "alert when metric >= 0" — mathematically ALWAYS true for any non-negative metric. Three rules (`service-down`, `nvme-critical-warning`, `nvme-media-errors`) were permanently `"state": "firing"` in the live API. The fourth (`dnsblockd-crashes`) was inactive only because its query `rate(...) > 0` returned no series when idle (the PromQL `> 0` filter hid the always-true condition). **Fix:** all four changed to `target = 1` (alert when metric >= 1 — i.e., at least one failure/error/crash). `dnsblockd-crashes` query also changed from `rate(dnsblockd_dns_crashes_total[5m]) > 0` to `increase(dnsblockd_dns_crashes_total[5m])` for semantic clarity (count of crashes in 5 min, not per-second rate). **Assertion:** `mkRule` now has `validateTarget` that `throw`s at eval time on `target=0` + `above_or_equal` (always true) AND `target=0` + `below` (never true for non-negative metrics). Prevents recurrence — `nix flake check` fails before deploy. **Lesson:** `target = 0` with `above_or_equal` is an always-true condition for non-negative metrics — it's almost never the intent. Use `target = 1` for "at least one" semantics, or use `op = "below"` with `target = 1` for "alert when service goes down" (value drops below 1) |
| OTLP tracing endpoint format is language-specific                                              | The `OTEL_EXPORTER_OTLP_ENDPOINT` env var format DIFFERS by language. **Go** (otlptracehttp): `localhost:4318` (host:port, NO scheme — the SDK constructs the URL internally). **Rust** (tonic/gRPC): `http://localhost:4317` (WITH scheme, gRPC port 4317 not HTTP 4318). **Python** (opentelemetry-sdk): `http://localhost:4318` (WITH scheme — Python SDK reads the env var directly). **Docker** containers: `http://host.docker.internal:4318` (with scheme + Docker host gateway). Setting the wrong format is NOT caught by `nix eval` — it validates string rendering, not API contract correctness. The Go services read the env var via `os.Getenv` and pass to `otlptracehttp.WithEndpoint()` manually, not via the SDK's auto-configuration. Reference implementations: DiscordSync (Go/cqrsotel), crush-daily (Go/cqrsotel), PMA/Overview/File-Renamer (Go/raw SDK), monitor365 (Rust/tonic `--features otel` cargo flag). Hermes (Python) and Manifest (Node.js/Docker) have NixOS env vars set but upstream instrumentation not yet implemented |
| `backup-coordination` module replaces per-service backup monitors                            | The generic `services.backup-coordination` module (`modules/nixos/services/backup-coordination.nix`) monitors ALL backup directories via Prometheus textfile metrics (`backup_healthy{backup=...}`, `backup_all_healthy`). It REPLACED the former `monitor365-backup-health` service which wrote `monitor365_backup_*` metrics. The monitor365-specific Gatus check was also removed — the generic "All Backups Healthy" check covers it. New backups: add to `configuration.nix` `services.backup-coordination.backups.<name>` with `directory`, `filePattern`, `maxAgeHours`. The module runs as root (backup dirs owned by different users) with `harden {}` + `serviceOneshotDefaults` |
| Homepage `siteMonitor` entries removed — Gatus owns health alerting                           | ALL `siteMonitor` entries and `statusStyle = "dot"` were removed from Homepage tiles. Homepage is now navigation-only. Gatus owns ALL health alerting (HTTP checks, response time, Discord alerts). Adding `siteMonitor` back creates redundant probes that duplicate Gatus checks and add latency to page loads. The Homepage settings support Gatus badge URLs via the standard Gatus integration if visual status indicators are needed |
| Secret rotation monitoring via Prometheus textfile                                           | `pocket-id-secret-rotation` oneshot service (in `pocket-id.nix`) checks all OIDC client secret files in `/var/lib/pocket-id/client-secrets/` for freshness (threshold: 90 days). Writes `secret_rotation_age_days{client=...}`, `secret_rotation_stale{client=...}`, `secret_rotation_all_fresh` to the shared textfile collector. Timer: every 1h. Gatus "Secret Rotation Health" check alerts on Discord when `secret_rotation_all_fresh` drops to 0. Does NOT auto-rotate — use `pocket-id-config.provision.regenerateSecretsFor = [ "clientId" ]` for declarative rotation |
| Attic binary cache (attic-config) — RS256 + DynamicUser + no server-info endpoint                                       | The nixpkgs `atticd` module requires `ATTIC_SERVER_TOKEN_RS256_SECRET_BASE64` (RSA PEM PKCS1, generate via `openssl genrsa -traditional 4096 \| base64 -w0`) — NOT HS256 and NOT `openssl rand`. The module also sets `DynamicUser = true`, so `atticd` does NOT exist at sops-decrypt time: the sops secret + template MUST be `owner = "root"` (systemd reads the `EnvironmentFile` as PID 1 and injects vars into the process — same pattern as Gatus). The module sets `StateDirectory = "atticd"` (creates `/var/lib/atticd`) and adds the custom storage path to `ReadWritePaths` automatically — do NOT set `ProtectSystem`/`ReadWritePaths` in `harden {}` (nixpkgs overrides them; only `MemoryMax` takes effect). The `atticd-size-guard` restarts atticd to trigger GC — this WORKS because Attic's GC loop (`gc.rs:34-64`) calls `run_garbage_collection_once` FIRST (before sleeping for the interval), so every startup runs an immediate GC sweep. Sops encryption does NOT need sudo — the age PUBLIC key in `.sops.yaml` is sufficient for `sops -e`; the private key (from SSH host key) is only needed for decryption at deploy time. **No `/api/v1/server-info` endpoint exists** — the Attic server only exposes `/` (placeholder HTML), `/{cache}/nix-cache-info`, `/{cache}/{path}`, `/{cache}/nar/{path}`, and `/_api/v1/*` (internal API, underscore prefix). Gatus health check uses `GET /`. **No `attic token` subcommand exists** — CI tokens are minted via `atticadm make-token --sub <SUB> --validity <P> --pull <CACHE> --push <CACHE> ...` (server-side admin tool, not the client). `atticadm make-token` REQUIRES permission flags (`--pull`, `--push`, `--create-cache`, etc.) — without them the token is useless. **Prometheus metrics are split into a separate `atticd-metrics` service** (5min timer, 128M, only `du` + write `.prom`), decoupled from the `atticd-size-guard` GC trigger (30min timer, 256M, only `du` + `systemctl restart`). The metrics service follows the same textfile collector pattern as `system-health`, `btrfs-health`, etc. Both run `du -sb` independently — negligible redundancy for decoupled failure modes. **VM test exists** (`tests/test-attic.nix`) verifying: atticd starts (DynamicUser storage write), health endpoint, Prometheus metrics format (heredoc indentation), storage directory, size guard. Uses `mock-sops.nix` + real RSA key generation |
| DynamicUser eval-time assert (replaces bash-grep pre-commit hook)                                | `modules/nixos/services/dynamic-user-audit.nix` is an auto-discovered module that cross-references `config.systemd.services` (DynamicUser=true) with `config.sops.secrets` and `config.sops.templates` (owner != root) at EVAL TIME. It catches ANY DynamicUser service — present or future — including nixpkgs upstream modules (atticd, gatus, searx). Previously a fragile bash grep in `.githooks/pre-commit` only checked for `atticd` and `gatus` by name. The assert uses `builtins.tryEval` to safely handle standalone module evaluation when sops-nix is not imported. Bug class history: Gatus (DynamicUser couldn't own LoadCredential file), crush-daily (sops owner mismatch blocked ALL secrets), Attic (same pattern) |
| NixOS VM test infrastructure                                                                     | `tests/default.nix` uses `pkgs.testers.runNixOSTest` (modern API, replaces deprecated `make-test-python.nix`). `tests/mock-sops.nix` mocks sops-nix in VMs — creates empty files at secret paths so sops-dependent services evaluate and start without the age key. `tests/test-helpers.nix` provides common mocks: `networking.domain`, `networking.local.*` (lanIP, subnet, gateway, etc.), `users.primaryUser`, and the Prometheus textfile collector directory. Import it alongside mock-sops to reduce boilerplate. **Attic test** (`test-attic.nix`): 8 assertions including `atticd-atticadm make-token` + full cache lifecycle (login, create cache) — uses `lib.mkForce` to override the sops template path with a build-time generated RSA key (same pattern as nixpkgs upstream test). **SearXNG test** (`test-searxng.nix`): 7 assertions covering service startup, health endpoint, Redis socket, settings file validity. Tests that need to mock options that the SystemNix module reads at eval time (e.g., `services.pocket-id-config`) must define those options in a separate inline module — top-level `options` + top-level `config` attributes in the same module cause "unsupported attribute" errors. **oauth2-proxy** is NOT testable in a VM without Pocket ID (requires a real OIDC provider at startup). **CI:** `.github/workflows/nix-check.yml` runs VM tests in a separate `vm-tests` job with KVM enabled via udev rules. Run: `nix build .#checks.x86_64-linux.{boot,attic,searxng}` |
| `writeShellApplication` pipefail + `\|\| echo 0` produces multi-line output (FIXED 2026-08-02)    | `pkgs.writeShellApplication` adds `set -o pipefail` by default. The pattern `var=$(cmd \| cut -f1 \|\| echo 0)` produces MULTI-LINE output when `cmd` exits non-zero (e.g., `du` on directories with permission-denied subdirs): pipefail makes the pipeline fail, `|| echo 0` runs and APPENDS `0` to whatever `cut` already produced. Result: `var="12345\n0"` → arithmetic syntax error in `$(())`, and the variable is never assigned → `unbound variable` on the next line under `set -u`. **Fix:** use `|| true` (no output) + `var=''${var:-0}` (default if empty). NEVER use `|| echo 0` on pipelines under pipefail. Affected: `tmp-cleanup`, `nix-build-cleanup`, `cargo-sweep` in `scheduled-tasks.nix` |
| `writeShellApplication` pipefail + `\| sort \| head` SIGPIPE (FIXED 2026-08-02)                  | `pkgs.writeShellApplication` adds `set -o pipefail`. The pattern `find ... \| sort -rn \| head -1 \| cut -f2` fails when `head -1` closes stdin after 1 line → SIGPIPE (exit 141) to `sort`/`find`. Under pipefail, the pipeline exits 141, and `set -e` kills the script. Also: `find` exits non-zero if the directory doesn't exist. **Fix:** append `|| true` to the pipeline: `LATEST="$(find ... \| sort -rn \| head -1 \| cut -f2 \|\| true)"`. The output is preserved (captured before failure), and the script continues. Same bug class as the `post-deploy-check SIGPIPE false-FAIL` (2026-07-21). Affected: `backup-health-metrics`, `monitor365-duckdb-heal` |
| Attic tmpfiles unsafe path transition on `/data` (FIXED 2026-08-02)                             | systemd-tmpfiles refuses to create directories when the path traverses an "unsafe ownership transition" — a parent owned by a non-root user. `/data` is owned by `lars:users` (the Docker volumes partition). The tmpfiles rule `d /data/atticd/storage 0755 root root - -` is SKIPPED because `/data` (lars) → `/data/atticd` (root) is an unsafe transition. Result: `/data/atticd/storage` is never created → atticd fails with `status=226/NAMESPACE` (systemd can't bind-mount ReadWritePaths for a non-existent path). **Fix:** `atticd-storage-dir.service` (a dedicated oneshot running as root WITHOUT namespace hardening) creates the directory via `mkdir -p` before atticd starts. tmpfiles is kept as a secondary mechanism but is insufficient alone. ANY service with storage on `/data` needs a similar storage-dir service, not just tmpfiles. The `atticd-storage-dir.service` must be in the deployed generation — verify with `nix eval ...config.systemd.services.atticd.after` (includes `atticd-storage-dir.service`) |
| forgejo-oidc-setup DNS gate (FIXED 2026-08-02)                                                  | `forgejo-oidc-setup` had `after = ["dnsblockd.service"]` but NO DNS gate ExecStartPre. During deploy, dnsblockd restarts and there's a brief window where DNS isn't ready. The OIDC setup script resolves `auth.home.lan` to fetch the OpenID configuration from Pocket ID → `dial tcp: lookup auth.home.lan: no such host`. **Fix:** added `forgejo-oidc-wait-dns` ExecStartPre that probes `getent hosts auth.home.lan` with 30×2s retries. Same DNS-gate pattern as SearXNG (`searxng-wait-dns`) and DiscordSync (`discordsync-wait-dns`). Unlike SearXNG (exit 0 on timeout = degraded mode), this gate exits 1 on timeout (OIDC setup is mandatory — degraded mode is meaningless) |
| Pocket ID 2.10.0 francis crash-loop (FIXED 2026-08-03, UPGRADED TO 2.12.0)                     | Pocket ID v2.10.0 introduced the `francis` actor framework (`github.com/italypaleale/francis v0.1.0-beta.6`), which runs 8+ cleanup jobs concurrently on startup. The jobs all contend for SQLite's single-writer lock → SQLITE_BUSY → actor bootstrap fails ("failed to begin transaction: context canceled") → nil-pointer panic in `quic-go.(*baseServer).accept` → `SIGSEGV` → `start-limit-hit`. The crash loop left a stale WAL that made every subsequent restart worse. **Resolution (2026-08-03):** Pocket ID upgraded to 2.12.0 (francis `v0.1.0-beta.17+`) which fixes the crash-loop. The `pocketIdUpgradeOverlay` in `overlays/linux.nix` was REMOVED — nixpkgs default (2.12.0) is now used directly. The WAL-clearing `ExecStartPre` is KEPT as defense-in-depth (protects against any future unclean shutdown). `ACTORS_HOST=127.0.0.1` and `MemoryMax=1G` also retained — single-instance setup doesn't need QUIC on 0.0.0.0, and 1G is conservative headroom |
| Docker backup service ordering (FIXED 2026-08-02)                                               | The `mkDockerService` backup service had `after = ["${name}.service"]` + `requires = ["docker.service"]` but NOT `after = ["docker.service"]`. During deploy, Docker is stopped and restarted. If the backup timer fires (or `Persistent = true` catches up a missed run) during this window, `docker-compose exec` fails with `service "postgres" is not running`. **Fix:** added `"docker.service"` to the `after` list. `requires` without `after` doesn't guarantee ordering — always include both when a service depends on another being ready |
| Smart direnv library (`zz-smart-nix.sh`) (ADDED 2026-08-03)                                    | `platforms/common/programs/direnv.nix` installs `direnv-smart-lib.sh` to `~/.config/direnv/lib/zz-smart-nix.sh` via `home.file`. The filename `zz-*` ensures it loads AFTER `hm-nix-direnv.sh` (alphabetical order), so function overrides take effect. **Two functions:** (1) `_nix_add_gcroot` override — replaces nix-direnv's per-input `nix build --out-link` (25ms × N_inputs) with instant `ln -sfn` for `/nix/store/*` paths. Automatic for ALL `use flake` calls globally. Measured 5.1x cold-path speedup on SystemNix (14.8s → 2.9s). (2) `use_go_env` — auto-detects `GOEXPERIMENT=jsonv2` (from `flake.nix` content or `grep -rq --include='*.go' --exclude-dir=vendor --exclude-dir=node_modules --exclude-dir=.git 'encoding/json/v2'`) and `GOPRIVATE` (from `go.mod` containing `github.com/[Ll]ars[Aa]rtmann`). Both respect existing env values. All 66 LarsArtmann project `.envrc` files were migrated to the thin pattern (`use flake` + `use_go_env` + project-specific extras only) via `scripts/migrate-envrc.py`. The migration removed: `source_url` bootstrapping (nix-direnv 3.1.2 ships via HM), `watch_file flake.nix/lock` (nix-direnv handles internally), explicit `GOEXPERIMENT`/`GOPRIVATE` exports (auto-detected), and all verbose comments (library is self-documenting). `nix-direnv`'s `use_flake()` already watches `flake.nix`, `flake.lock`, AND `devshell.toml` internally — explicit `watch_file` for these is redundant. Regression test: `scripts/test-direnv-smart-lib.sh` (12 tests covering all detection paths, vendor exclusion, env-respect, and GC root symlink correctness) |
| Fish per-prompt direnv caching + init caching (ADDED 2026-08-03)                              | `platforms/common/programs/fish.nix` `interactiveShellInit` contains a `direnvCacheHook` that replaces HM's stock direnv fish integration with an mtime-gated version. Stock direnv spawns `direnv export fish` on EVERY prompt (~43ms per command). The cached version checks `.envrc flake.nix flake.lock shell.nix default.nix .env` mtimes natively in fish (instant `test -nt`), only calling direnv when something changed (0.7ms cache hit vs 43ms). **HM bypass trick:** HM generates `if not functions -q __direnv_export_eval; direnv hook fish \| source; end` at the end of config.fish. By defining `__direnv_export_eval` in `interactiveShellInit` (which runs BEFORE HM module integrations), HM sees the function exists and skips its own hook. **Per-session sentinel:** the mtime reference file is `/tmp/.direnv-cache-$USER-$fish_pid` — includes `$fish_pid` so each fish session gets its own sentinel. Without this, two sessions in the same directory share a sentinel: session A processes a flake.nix change and touches the sentinel, hiding it from session B. **Init caching:** `fzf`, `starship`, and `carapace` init outputs are cached to `~/.cache/fish-init/<tool>-<version>.fish`. Cache keys are computed at BUILD TIME via `pkgs.X.name` (Nix interpolation), zero subprocess at runtime. `enableFishIntegration = false` for both fzf and starship in their HM modules — the cached version replaces them. `carapace` caching is in `shells.nix` (NixOS) with build-time key `pkgs.carapace.name`. Measured: fish interactive startup 67ms → 44.6ms (33% faster), per-command 46ms → 0.7ms (65x faster on cache hit) |
| monitor365 libspa-sys `[lints] workspace = true` breaks crane build (FIXED 2026-08-03)        | monitor365's `vendor-patches/libspa-sys/Cargo.toml` contains `[lints]\nworkspace = true`. crane's `buildDepsOnly` runs `cargo check --locked` in the Nix sandbox, which parses ALL vendored Cargo.tomls. The vendored libspa-sys is a STANDALONE crate (not part of a workspace in the sandbox) — `workspace = true` tells cargo to inherit lints from the workspace root, but the workspace root manifest doesn't define `[workspace.lints]`. Error: `failed to parse manifest at vendor-patches/libspa-sys/Cargo.toml — error inheriting 'lints' from workspace root manifest's 'workspace.lints' — 'workspace.lints' was not defined`. **Fix (upstream `d125048de`):** removed `[lints]` section from both `vendor-patches/libspa-sys/Cargo.toml` and `vendor-patches/pipewire-sys/Cargo.toml`. The vendored Cargo.tomls are normalized copies that don't need workspace inheritance — they're standalone crates patched into the vendor tree. SystemNix monitor365 input bumped to `7cf8162c1` to pick up the fix. When regenerating vendor patches from a new upstream pipewire-rs release, ALWAYS strip `[lints]` sections from the vendored Cargo.tomls |
| nixpkgs tarball lock entry defeats `nix flake update` (FIXED 2026-08-03)                       | The `flake.lock` `nixpkgs.original` was `{"type":"tarball","url":"https://channels.nixos.org/nixos-unstable/nixexprs.tar.xz"}` even though `flake.nix` declares `github:NixOS/nixpkgs/nixos-unstable`. The nix global registry resolves the GitHub URL through the registry, rewriting it to a tarball. `nix flake update nixpkgs` silently re-fetched the SAME stale tarball — the lock never changed. **Fix:** manually `jq` the lock to use `type: "github"` with the correct rev/narHash for both `original` and `locked`. Always `jq '.nodes.nixpkgs.original' flake.lock` BEFORE running `nix flake update nixpkgs` — if `type` is `"tarball"`, manual surgery is required |
| Dual cargo build paths need different `outputHashes` key formats (2026-08-03)                  | monitor365 has TWO separate cargo build paths for the same git dep (`segment-buffer`): (1) crane's `commonArgs` uses `outputHashes` with the FULL git source URL from Cargo.lock as the key: `"git+https://github.com/LarsArtmann/segment-buffer.git?rev=38a03100#38a03100..."`. (2) `rustPlatform.importCargoLock` (in `monitor365-ui`) uses `outputHashes` with the PACKAGE-VERSION as the key: `"segment-buffer-0.6.0"`. Both need the SAME hash VALUE but DIFFERENT key formats. If only one is fixed, the build fails on the other path with a missing-hash error. Always `grep -r 'importCargoLock\|outputHashes\|cargoLock'` the ENTIRE upstream flake when adding git-dep hashes |
| Go vendorHash drift on 7-month nixpkgs jump (FIXED 2026-08-03)                                | A nixpkgs jump from Jan 2026 to Aug 2026 broke `vendorHash` for multiple Go packages: crush-daily, library-policy. The `got:` hash from the build error is the correct new value — paste it into the upstream flake, push, and bump the SystemNix flake input. **Prevention:** before a full system build, batch-test individual Go packages: `nix build .#crush-daily .#library-policy .#cqrs-lint ...` to catch vendorHash breaks early. Eval (`nix flake check --no-build`) does NOT catch vendorHash mismatches — they are fixed-output derivations that only fail at build time |

| `builtins.toString null` = `""` — silent uid=null slice bug (FIXED 2026-08-03)              | `config.users.users.<name>.uid` is `null` at eval time for `isNormalUser` (UID assigned at activation). `toString null` returns `""` silently — it does NOT throw. `boot.nix` used `"user-${toString config.users.users.lars.uid}"` for the MemoryHigh=56G/MemoryMax=64G slice cap, which silently became `"user-"` — the limits applied to a **nonexistent slice** while `user-1000.slice` ran UNCAPPED (`memory.max = max`). This was the **root cause of the 2026-08-03 WDT crash** — user processes (Helium/Electron) grew unbounded for 2 days → total RAM exhaustion → journald starved → WDT reset. Every previous "Helium renderers grew unbounded" crash was enabled by this bug. **Fix:** hardcode `"user-1000"` (or any UID). NEVER use `config.users.users.<name>.uid` in slice/service names without null-checking. Same class as the monitor365 graphical-restart `uid=null` bug. **Verify your fix:** `cat /sys/fs/cgroup/user.slice/user-1000.slice/memory.max` must show `96636764160` (90G), NOT `max` |

| DiscordSync SQLite corruption from WDT/OOM (FIXED 2026-08-03)                              | Unclean shutdown (WDT reset, OOM kill, power loss) corrupts the DiscordSync SQLite DB at `/var/lib/discordsync/discordsync.db`. The libSQL/Go binding panics with `internal error: entered unreachable code: cell_index_read_payload_ptr called on non-index page` (B-tree page type mismatch) and crash-loops to `start-limit-hit`. **Fix:** `discordsync-db-heal` ExecStartPre runs `PRAGMA integrity_check` on every startup. If the result is anything other than `ok`, the corrupt DB is moved to `discordsync.db.corrupt-<timestamp>` and WAL/SHM sidecars are cleaned. DiscordSync recreates from scratch or re-syncs from Turso cloud. Attachments in `attachments/` are preserved. Same self-healing pattern as SigNoz migration-lock clear and monitor365 DuckDB WAL heal |
| dnsblockd OOM-kill memory leak — hourly DNS outages (FIXED 2026-08-04)                          | dnsblockd was OOM-killed ~hourly (20+ times in 7 days, Jul 28-Aug 3). Each kill = ~10s DNS outage. **Root cause:** unbounded OTEL cardinality — the Prometheus SDK reader retains every unique `dns_domain`/`http_path`/`proxy_domain` label value as an in-memory time series with no eviction (dnsblockd `internal/server/telemetry.go`, `internal/otel/otel.go:72-80`). Combined with Go's default GC (GOGC=100 = run when heap doubles from 600M base ≈ 1.2G), MemoryMax=1G killed the process before GC could collect. **Fix (SystemNix):** raised `MemoryMax` from 1G → 2G + added `GOMEMLIMIT=1500MiB` in `dns-blocker.nix` serviceConfig. GOMEMLIMIT forces Go GC to run aggressively below MemoryMax, collecting reachable-but-stale heap before the OOM wall. **Real fix needed upstream:** drop high-cardinality labels from `telemetry.go` (dns_domain, http_path, proxy_domain) or bucket/hash them — these are the only true unbounded accumulators. Also: the DNS observer path (`dns_observer.go:71-91`) dispatches tracking writes synchronously per-query (no semaphore, no batching) unlike the HTTP path which uses a 32-slot semaphore — under high QPS, SQLite WAL pages accumulate in memory. Bounded structures (stats hits map ≤1000, rate limiter ≤10K, sdns cache ≤256K) are NOT the leak |
| Manual resolv.conf 9.9.9.9 addition breaks local DNS (FIXED 2026-08-04)                         | The user manually prepended `nameserver 9.9.9.9` to `/etc/resolv.conf` as a fallback during dnsblockd's hourly OOM outages. This replaced the Nix-managed symlink (0444 store file) with a regular file containing `9.9.9.9` BEFORE `127.0.0.1`. **Why it breaks local DNS:** glibc queries nameservers in ORDER and accepts the first NXDOMAIN — Quad9 returns NXDOMAIN for `*.home.lan` (private TLD), so glibc never falls through to dnsblockd on 127.0.0.1. `getent hosts dash.home.lan` returns exit 2 (NOT FOUND). All `*.home.lan` vHost smoke checks fail. **Fix:** the Nix config (`platforms/common/dns-resolver.nix`) already writes a static `/etc/resolv.conf` with ONLY `127.0.0.1` — every deploy restores the correct symlink. The user must STOP manually editing resolv.conf. The dnsblockd OOM fix (above) eliminates the motivation for the fallback. If a fallback is truly needed, use `options rotate` in resolv.conf (glibc will round-robin between nameservers) or configure dnsblockd's upstream forwarders (already set to `tls://1.1.1.1:853` + `tls://9.9.9.9:853`) — dnsblockd IS the fallback, via DoT to Quad9/Cloudflare |
| DiscordSync upstream chattr ExecStartPre shell-syntax bug (FIXED 2026-08-04)                    | The upstream DiscordSync NixOS module ships `ExecStartPre=chattr -R +C /var/lib/discordsync 2>/dev/null \|\| true`. systemd ExecStartPre is NOT shell — `2>/dev/null` and `\|\| true` are passed as LITERAL FILE ARGUMENTS to chattr. Log evidence: `chattr: No such file or directory while trying to stat 2>/dev/null`, `stat \|\|`, `stat true`. Additionally, the line lacks `+` prefix, so it runs as the `discordsync` user → `Operation not permitted` on the DB files. **Fix (SystemNix):** `modules/nixos/services/discordsync.nix` uses `ExecStartPre = lib.mkForce [...]` to REPLACE the upstream list entirely (removing the broken chattr). BTRFS +C (nodatacow) is nice-to-have for SQLite but not required — DiscordSync uses WAL mode. **Upstream fix:** wrap in `pkgs.writeShellApplication` or use `ExecStartPre=+/bin/sh -c 'chattr -R +C /var/lib/discordsync 2>/dev/null \|\| true'`. The `lib.mkForce` priority (50) overrides the upstream's unannotated (100) ExecStartPre list |

---

## Build & Deploy

```bash
nix flake check --no-build  # Validate syntax (fast)
nix run .#deploy            # Build + deploy via nh
nix fmt                     # treefmt + alejandra
```

For contributor style, module templates, and verification commands, see [docs/CONTRIBUTING.md](./docs/CONTRIBUTING.md).

---

## Platform Constraints

**Darwin:** 256GB SSD 90-95% full, 24GB RAM. `nix-collect-garbage` hangs; clear caches before builds. Never add packages that build from source >10min. HM config is minimal — no terminal/editor/theme parity with NixOS.

**GPU (NixOS):** `OLLAMA_GPU_OVERHEAD=8589934592` (8 GiB) reserves headroom for compositor. Memory fractions are per-service, not system-wide.
