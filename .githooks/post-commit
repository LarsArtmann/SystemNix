#!/usr/bin/env bash
# Post-commit hook — show commit summary and remind about deployment
set -euo pipefail

hash=$(git rev-parse --short HEAD)
subject=$(git log -1 --pretty=%s)

echo "[post-commit] $hash $subject"
echo "  Deploy: nix run .#deploy"
